<!-- stamp: a92e570 2026-08-26 status: DEGRADED 16 stale, 0 orphan, 18 missing-role -->
ExpoTheExplorer/Assets/Data/ExpoTheExplorer.Data.asmdef | MISSING-role | sys: ? | api: asmdef ExpoTheExplorer.Data | dep?: - | used: ? | crit: ? | note: auto-added; AI must complete | h:eda37c7e
ExpoTheExplorer/Assets/Data/DataScripts/BoardAnimationConfig.cs | every board/tray/ticket-card animation number | sys: BoardUI | api: SnapBackDuration; TraySettleDuration; PopInDuration; PopInJumpPower; SlotClearDuration; ScatterShakeDuration; ScatterShakeStrength; ScatterShakeFrequency; ScatterShakeItemMultiplier; DeliveryGrowScale; DeliveryGrowDuration; DeliveryLiftDistance; DeliveryFadeDuration; DeliveryReentryDuration; TicketExitLiftDistance; TicketExitDuration; TicketEntryDropDistance; TicketEntryDuration; TicketDangerBlinkPeriod; LifeLostHeartRiseDistance; LifeLostHeartDuration; TutorialDimOpacity; TutorialGhostOpacity; TutorialGhostTravelDuration; TutorialGhostLoopPause; TutorialMessageScreenHeight; TutorialMessageFontSize; TutorialHintFadeDuration | dep: - | used: BoardView,BoardItemDragHandler,WorldTrayView,TicketCardView,TicketCardsView,TutorialSpotlightView | crit: K2 | note: D-083 (2026-08-26) added three more Tutorial numbers for the step message and the arrow/message fade. The message POSITION is a config value rather than a constant for a reason worth keeping: it is the one tutorial element with no object to hang off, so it has to miss the board, the trays and the cards, and only the scene knows where those are. ONE asset for three systems' animation feel (BoardUI's spawn/drag, TraySystem's settle/scatter/delivery, TicketSystem's card exit/entry and the danger pulse) rather than one per system -- the durations have to agree across a single choreography (a tray lifting off while its card slides away), and splitting them would let two assets disagree about the same beat. Timing and strength only: nothing here decides an outcome, so it is K2 -- a wrong number looks bad and costs nothing. D-073 added the two Ticket Danger numbers and D-074 took one back out: the dim alpha died with the alpha pulse it existed for, and the flash's COLOURS live on TicketCardVisualsConfig -- this asset kept only the period, which is what it is for. That period is the one field here where 0 has a MEANING (flash off, danger image solid) instead of being a broken value, which is why the code branches on it rather than clamping -- a clamped 0 would be a 20Hz strobe. D-078 added the two Life Lost numbers (the broken heart's rise, in WORLD units, and how slowly it drifts). D-082 (2026-08-26) added the four Tutorial Spotlight numbers -- the dim opacity behind the forced first move, and the looping ghost's opacity, travel time and pause. They are here rather than in the Day content because they are how the step LOOKS, not what it asks: which cell and which tray are authored per Day in the Day JSON. THE OLD "STALE BY OMISSION" WARNING ON THIS LINE IS RESOLVED and has been deleted rather than left to scare the next reader: the asset serialized only 4 of these keys when that was written, and it now carries every one, so the Inspector no longer silently re-serializes 17 initializer values the moment a field is touched. Note the asset DISAGREES with three C# initializers on purpose (blink 0.8 vs 0.6, heart rise 200 vs 120, heart duration 2 vs 1) -- the asset is the authority and those are the tuned values. | h:99a9b004
ExpoTheExplorer/Assets/Data/DataScripts/BoardVisualsConfig.cs | board color config (checkerboard, placeholder, frame) | sys: BoardUI | api: - | dep: - | used: BoardView,DayEditorDayStartPreview,DayEditorModel,DayEditorWindow | crit: K3 | note: - | h:7f662692
ExpoTheExplorer/Assets/Data/DataScripts/DragFeelConfig.cs | MISSING-role | sys: ? | api: - | dep?: - | used: ? | crit: ? | note: auto-added; AI must complete | h:d1839f3f
ExpoTheExplorer/Assets/Data/DataScripts/EconomyConfig.cs | tip tier thresholds + the three tip rates | sys: EconomySystem | api: - | dep: - | used: GameManager,EconomyCalculator,TicketCardsView,EconomySystemTests | crit: K2 | note: single authority for WarningRatio/CriticalRatio, which decide BOTH the tip tier and the timer bar's colour -- TicketCardsView reads them via GameManager.EconomyConfig so screen and wallet cannot disagree; PatienceDecayStep + the speed-tier knobs were deleted from here (GDD v0.9 dropped patience from the tip, v1.1 dropped the speed tiers) | h:62df38a0
ExpoTheExplorer/Assets/Data/DataScripts/MetaCatalog.cs | meta prop schema: locations, items, unlock triggers | sys: MetaSystem | api: MetaUnlockKind{Purchase,DayUnlock}; MetaItemDefinition(Id; DisplayName; Sprite; Unlock; Price; UnlockAtDayIndex; NormalizedPosition; Pivot; SortOrder; RequiresAreaId; UnlocksArea); MetaLocation(Id; DisplayName; BackgroundSprite; UnlockAtDayIndex; Items); MetaCatalog.Locations; MetaCatalog.OwnershipKey(string locationId, string itemId) | dep: - | used: MetaCatalogValidator,MetaCatalogValidatorTests | crit: K2 | note: D-046 added an OPTIONAL backgroundBgSprite per location: a LARGER copy of the grounds drawn behind them so the edges keep going when the map zooms in. CENTRED with no authored offset, and that is a contract on the ART -- the extension must grow outward equally on all four sides. Meta1's first export did not (measured at 117,387 inside 1074x2550 where centring would be 110,353) and the author re-exported rather than have the catalog carry a correction, which is what keeps this one field instead of a field plus a pair of numbers that go stale on the next export. Empty = exactly the old behaviour. D-039 added an OPTIONAL shopIcon per item, and ShopIcon resolves the fall-back (icon, else the prop sprite) INSIDE the data class -- "empty means use the art" is one rule, and a rule spread across callers is one a caller eventually forgets, which here would draw nothing and read as missing art rather than missing code. Sprite stays the map/ghost art: a preview drawn from a list icon would be a preview that lies. An empty icon is valid, not a validation error, so every catalog authored before the field stays correct. The constructor parameter went in AFTER sprite, which is safe only because every caller passes named arguments past the first two -- checked, not assumed. single authority for every meta prop's price/position/art/unlock (decisions.md D-015). Multi-location from line one so ownership keys never need a save migration; OwnershipKey is the ONE place the "<location>.<item>" spelling lives. ONE sprite per item and absent until active (D-016 narrowed D-015's two-sprite pair): the whole view layer is image.enabled = active. The removed unownedSprite was the only way to express "buying this removes it" -- the ruined building is baked into the background instead, so a future prop needing that behaviour needs a new mechanism, not a field. Unlock is an enum discriminator + one field per branch, NOT SerializeReference (same shape as GuaranteedTicketCountMode, D-004); the enum SURVIVED D-017 because two int fields (price, unlockAtDayIndex) need something to say which one governs. D-017 deleted DayUnlockTrigger and its content scan: a Day-unlocked prop is now `CurrentDayIndex >= unlockAtDayIndex`, the SAME rule a location uses, which is why this file no longer references FoodItemConfig/FoodCategory/ModificationConfig at all. unlockAtDayIndex is a 0-based CATALOG POSITION, not the day number the player sees (that is +1). No size/scale field by design -- a prop's size follows from its pixel size times the background's scale factor, and authoring it again would be a second authority | h:1261a741
ExpoTheExplorer/Assets/Data/DataScripts/MetaCatalogValidator.cs | authoring-time gate for the meta catalog | sys: MetaSystem | api: MetaCatalogValidationResult(IsValid; Errors; Warnings); Validate(MetaCatalog catalog); Validate(IReadOnlyList<MetaLocation> locations) | dep: MetaCatalog,MetaItemDefinition,MetaLocation,MetaUnlockKind | used: MetaCatalogValidatorTests | crit: K3 | note: errors/warnings split mirrors DayValidationResult -- warnings never gate, since "this looks odd" is not a validator's call about a design decision. Lives in Data (not MetaSystem) because it needs nothing outside this reference-less assembly, so both the resolver and the Editor placement tool can use it without a dependency. The cross-location area-gate check is the one this file exists for: a requiresAreaId naming a real item in ANOTHER location is reported as that, not as "not found", which is the mistake a multi-location catalog invites. Deliberately does NOT check balance, visual placement, or whether the day a prop is pinned to is the RIGHT one -- UNWRITEABLE here, not merely unwritten: Data cannot see the Day catalog, and since D-017 nothing derives an unlock from content | h:58bb7763
ExpoTheExplorer/Assets/Data/DataScripts/FoodCatalog.cs | authoritative food/modification item list + id lookup | sys: ? | api: GetById(string id); GetModificationById(string id) | dep: FoodItemConfig,ModificationConfig | used: GameManager,DayCatalogParser,DayContentGenerator,DayEditorModel,DayEditorWindow,TicketFactory | crit: K1 | note: shared Data asset -- consumed by TicketSystem/DaySystem/DayEditor/BoardDistribution alike, so sys: stays ? rather than naming one owner (same call as GameConfig/GameState) | h:80080fa3
ExpoTheExplorer/Assets/Data/DataScripts/FoodItemConfig.cs | one food item: id, art layers, category, price | sys: ? | api: - | dep: ModificationConfig | used: FoodCatalog,BoardItem,RequiredItemKey,Ticket,BoardDistributor,TicketFactory,DayCatalogParser,DayContentGenerator,DayDefinition,DayValidator,BoardView,TicketCardView,DayEditorModel,FoodItemConfigEditor | crit: K1 | note: shared Data asset -- every ticket/board item IS one of these, consumed by TicketSystem/DaySystem/BoardDistribution/TraySystem/UI/DayEditor alike, so sys: stays ? rather than naming one owner (same call as FoodCatalog/GameState); dep? draft's BoardItem+Modification were comment/tooltip-only edges (the class comment states Data must NOT reference Core.Modification) and were dropped; basePrice added 2026-08-18 as the Order Value authority (CLAUDE.md Food Pricing), no reader yet | h:ed4587f8
ExpoTheExplorer/Assets/Data/DataScripts/GameConfig.cs | board grid dimensions + the new-player coin grant + the star-to-gem rate | sys: BoardUI | api: BoardWidth; BoardHeight; StartingSoftMoney; GemsPerStar | dep: - | used: GameState,BoardGrid,GameManager,GameSession,DayEditorWindow,DayEditorModel,DayEditorDayStartPreview | crit: K1 | note: was a shared sys:? grab-bag until the Starting Resources + Daily Goal sections were deleted; now board-only, so it maps to the one board system blueprint.md names (BoardGrid, the model it actually feeds, is itself still sys:? -- blueprint has no board-model row). D-026 put a starting-resource field BACK on it: StartingSoftMoney, the coins a brand-new player opens with. It is authored content that must not sit in code, and this is the only config BOTH scene roots already hold -- EconomyConfig is the per-ticket tip math consumed by EconomyCalculator and is not reachable from the main screen, so a field there would be one more serialized reference that can sit quietly unwired. Exactly one reader: GameSession, which hands it to PlayerProfileStore.NewPlayer. GameConfig.asset was given 1000 in the same commit, because a new serialized field reads 0 out of an .asset that already exists. D-056 put a SECOND reward number here for the same reasons: GemsPerStar, what one of a completed day's stars is worth. The star COUNT stays a rule in code (DayLifecycleManager.StarCount); only the exchange rate is authored. Exactly one reader: GameManager.AwardStarGems. GameConfig.asset got gemsPerStar: 1 in the same task -- same trap, same fix. D-060 note: the star COUNT's own four numbers went to a separate StarScoreConfig asset, NOT here -- this file holds what a star is worth once earned, that one holds what earns it | h:259f0a2c
ExpoTheExplorer/Assets/Data/DataScripts/LivesConfig.cs | continue-cost knobs (gem / soft money) | sys: LivesSystem | api: - | dep: - | used: GameManager,LivesManager,LivesSystemTests | crit: K3 | note: both values are placeholders; continueSoftMoneyCost used to equal GameConfig.startingSoftMoney, but that field is gone and a fresh run now starts at 0 SoftMoney / 0 Gems, so neither continue is affordable without an income source | h:411acd92
ExpoTheExplorer/Assets/Data/DataScripts/KeyConfig.cs | key economy knobs: cap, regen minutes, refill gem cost | sys: KeySystem | api: MaxKeys; RegenMinutes; RefillGemCost | dep: - | used: KeyManager,KeySystemTests | crit: K3 | note: D-065. The defaults ARE the user's authored values (5 / 30 / 40) rather than placeholders, and that is deliberate: creating KeyConfig.asset in Unity then needs no typing, which matters because hand-written .asset YAML has silently shipped MISSING fields four times in this project (D-004, EconomyConfig, TicketCardVisualsConfig, BoardAnimationConfig) and the asset is therefore made through the CreateAssetMenu item rather than written by the AI. [Min(1)] on maxKeys and regenMinutes is the guard chosen over a runtime check: a zero cap locks the player out forever and a zero interval divides by zero, and both are far better refused in the Inspector than defended against on every Refresh. The asset itself does not exist yet -- it is step 2 of .claude/key-plan.md | h:b42ee4c2
ExpoTheExplorer/Assets/Data/DataScripts/ModificationConfig.cs | MISSING-role | sys: ? | api: - | dep?: Modification | used: ? | crit: ? | note: auto-added; AI must complete | h:cdd2b564
ExpoTheExplorer/Assets/Data/DataScripts/TicketCardVisualsConfig.cs | ticket card sprites, tints, timer bar + danger colours | sys: TicketSystem | api: NormalTicketSprite; ImpatientTicketSprite; PatientTicketSprite; NormalModificationBoxColor; ImpatientModificationBoxColor; PatientModificationBoxColor; AdditionSprite; RemovalSprite; TimerSegmentSeconds; TimerFillColor; TimerWarningColor; TimerCriticalColor; DangerFlashColor; DangerFlashRestColor | dep: - | used: TicketCardsView,DayEditorTicketCardPreview,DayEditorWindow,DayEditorModel | crit: K2 | note: D-074 added the danger flash's two colours here rather than beside its PERIOD on BoardAnimationConfig: how loud the alarm is, is a visual choice and belongs with the ticket sprites and the bar's three colours; how fast it blinks is timing. The rest colour is white by default, which makes the paper flash red-and-back; a second loud colour there makes it ALTERNATE instead -- the whole "how garish" question answered by one field. zero assembly refs by design, so it holds no PatienceType lookup -- callers switch themselves; the 2 urgency ratios MOVED to EconomyConfig because they also pick the tip tier, leaving 3 fill colours plus timerSegmentSeconds, which is purely cosmetic tick spacing and must never be wired to a payout | h:944c44fa
STALE ExpoTheExplorer/Assets/Data/DataScripts/TicketGenerationConfig.cs | ticket-generation balancing config + MainDishWeight, the per-dish spawn weight / modification rate / modification ceiling authority | sys: TicketSystem | api: ToRuntimeSettings(); CloneForDayGeneration(float sideInclusionChance, float drinkInclusionChance, float modificationCountLambda, float modificationAdditionChance, List<MainDishWeight> mainDishWeights); MainDishWeight(FoodItemConfig food, float weight, float modificationCountLambda, int maxModificationCount); MainDishWeight.NormalizeMaxModificationCount(int value) | dep: FoodItemConfig,TicketRuntimeSettings | used: GameManager,TicketFactory,TicketEntryFactory,DayContentGenerator,DayTicketSequenceProvider,DayEditorModel,DayEditorWindow,DayEditorSettingsPreviews,TicketGenerationConfigEditor | crit: K2 | note: also used by most TicketSystem/DaySystem EditMode test suites. This asset is a SEED since D-006 -- the live game reads DayDefinition, never this. MainDishWeight.maxModificationCount is absent from any Day file or asset written before it existed, where it deserializes to 0; NormalizeMaxModificationCount is the single place that turns that 0 back into the default, called from the getter and from the Day Editor's own FromJson, and is why the slider range starts at 1 | h:1a4224cc
STALE ExpoTheExplorer/Assets/Scripts/Bootstrap/GameManager.cs | central Day/board runtime bootstrap + lifecycle (BoardDistributor rebuilt per Day from that Day's own balancing, D-004) | sys: Bootstrap | api: EconomyConfig; KeyManager; PowerupManager; Tutorial; TutorialStepChanged; IsBoardPickupAllowed(int x, int y); IsTrayDropAllowed(int slotIndex); IsBoardRelocationAllowed(); PowerupConfig; PendingRewardSoftMoney; PendingRewardGems; ClaimRewardGems(int count); ClaimRewardSoftMoney(int amount); CompleteRewardHandover(); RetryDay(); RetryCompletedDay(); AdvanceToNextDay(); ReturnToMainScreenFromCompletedDay(); ReturnToMainScreenAbandoningDay() | dep: DayRewardPurse,BoardDistributionSettings,BoardDistributor,DayBoardTimelinePlayer,DayCatalogNavigator,DayCatalogParser,DayDefinition,DayJsonSource,DayFailureCause,DayLifecycleManager,DayTicketSequenceProvider,EconomyCalculator,EconomyConfig,FoodCatalog,PlayerProfile,PlayerProfileStore,SceneFlow,StarScoreConfig,Wallet,GameConfig,GameState,LivesConfig,LivesManager,Ticket,TicketFactory,TicketGenerationConfig,TicketSlotManager,TrayManager,PowerupConfig,PowerupManager,PowerupEffects,PowerupType,AutoCollectRunner,TutorialDirector,ResolvedTutorial | used: BoardItemDragHandler,BoardView,DayCompletePopupView,DebugTicketDeliveryController,GameOverPopupView,GemsView,LivesView,SoftMoneyView,TicketCardsView,TicketCardView,TrayFillCounterView,WorldTrayView,DayTicketSlotManagerIntegrationTests,TraySystemTests | crit: K1 | note: D-099 (2026-08-26) Update, after its three early-returns and BEFORE the clock, drains what the Continue hold postponed: TrayManager.ResolveDeferredScatters then TicketSlotManager.ResolveDeferredTimeouts. Tray first because that is the order the two failures happened in and because a deferred cancellation assigns a new ticket whose required items are spawned FROM the board -- the scattered items belong on it before that asks. The resume is detected by READING IsAwaitingContinue here, not by subscribing to a "day resumed" event: the hold is lifted by four callers (both paid Continues, RetryDay, SRDebugger's refill) and a publisher any one of them forgot would strand a slot empty for the rest of the day. There is no flag to forget, because it is the same one the return above already reads. D-087 (2026-08-26) translates DaySystem's step kind into the Tutorial system's, written out as a switch rather than cast, so a kind added upstream without teaching Tutorial about it stops the build instead of quietly becoming whatever number lines up (the stance HapticConfig takes on its vendor mirror). This is the ONE file holding both enums, hence the using-alias at the top. Update() stops the ticket clock for the WHOLE tutorial (D-097, 2026-08-26), gated on Tutorial.IsActive -- its own condition, not a second use of IsAwaitingContinue, which means "the Continue popup is up" and is read by four other places. It began (D-087) as the reading steps alone, on the reasoning that a three-description panel outlasts a Patient ticket; the same proved true of a forced MOVE, which is taught rather than raced while three unordered tickets drain behind the dim. Safe against a stuck clock by construction: every way a tutorial ends -- last step, an impossible step aborting, Abort() -- moves the same IsActive this reads. IsHoldingForReading is left to its other reader, PowerupBarView's intro panel. EnsureCurrentTutorialStepIsPossible skips the board check for a non-move step, without which an intro step would be validated against its unused (0,0) and abort the tutorial. D-084 (2026-08-26) added the third null-safe tutorial forwarder, IsBoardRelocationAllowed. D-083 (2026-08-26) arms a step LIST and re-validates EVERY step against the live board as it begins, not just the first -- the board moves while the tutorial is played, so an authoring-time check cannot see whether step two's item is still there. A step that has become impossible aborts the whole tutorial with a logged reason rather than leaving the player facing a board that refuses every touch. TutorialArmed was renamed TutorialStepChanged because that is now what it means. The Abort -> StepChanged -> handler path does not recurse: the second pass finds no active step and simply republishes. D-082 (2026-08-26) made this the place the forced first move is armed, and it is armed INSIDE ApplyDayStartBoardPreSeed rather than from Awake because all FOUR day-start paths run through there (first load, both retries, the advance to the next Day) -- one call site instead of four, and it cannot drift out of step with the board it gates. Rebuilding the director rather than keeping one instance is what makes Day 1 correct: it authors no tutorial, so the field goes back to null and every gate opens; a leftover director would silently lock Day 1 to one cell. It REFUSES TO ARM when the authored cell is empty at Day Start, which turns a softlock (nothing pickable, no tray accepting, reads as a freeze) into an ordinary day plus a sentence naming the cell -- the runtime half of a guard DayValidator also makes at authoring time, covering the hand-edited Day file that never passes through the editor. Powerups joined IsAwaitingContinue and IsDayComplete behind CanUsePowerups for a reason of the same kind: Auto-Collect would sweep the very item the player is being told to drag. D-078: both life-loss entry points now take the slot (HandleLifeLoss(cause, slotIndex)) and ask an optional LifeLostHeartView for a broken heart AFTER LivesManager.LoseLife has actually run -- a heart for a loss that did not happen is the one lie this readout must not tell. Both causes are treated alike here: to the player a heart is a heart, and which mistake spent it is what the shake or the vanishing card already says. D-068 (key-plan step 4) charges ONE KEY for giving up on a lost day, on both exits -- RetryDay and ReturnToMainScreenAbandoningDay -- through a single private SpendKeyForLostDay, because this class is unreachable from the EditMode suite (predefined Assembly-CSharp, D-012) and one copy of the rule is the only protection it has. TWO ORDERING RULES, both silent if broken: the IsAwaitingContinue flag is read BEFORE LivesManager.RefillForNewDay clears it (otherwise giving up quietly becomes free), and RetryDay's new SaveProfile is LAST, after DayRetried has already reverted the wallet (otherwise a failed attempt's earnings get banked, breaking the atomic-day contract). **RetryDay writing to disk at all is a narrowing of a documented rule**: that path deliberately wrote nothing because a failed day was never permanent, but an unwritten key spend lets a player press Retry, force-quit and get the key back, which makes retries free and the economy decorative. What reaches disk is the ROLLED-BACK money, i.e. the same figure the abandon path already writes, so nothing new is banked -- only the key becomes permanent. Gating on the flag rather than on the method also keeps DebugTicketDeliveryController's R key from eating a key when it replays a day that was going fine. RetryCompletedDay stays FREE: a voluntary redo of a day that SUCCEEDED is not losing one. The spend result is ignored on purpose -- at zero keys the player must still be able to leave, since blocking that is a softlock. D-065 step 2 added a REQUIRED keyConfig [SerializeField] and a guard that runs BEFORE the session is built -- unlike the optional metaCatalog/starScoreConfig checks after it, a missing KeyConfig means GameSession cannot construct a KeyManager at all, so the alternative is a NullReferenceException from inside a constructor instead of a sentence naming the field and the menu that creates the asset. D-064 REVERSED AdvanceToNextDay's deliberate skip of LivesManager: it now calls RefillForNewDay like the retry paths, so every day opens at a full bar rather than carrying a mistake forward. Leaving the skip would have split behaviour by ROUTE -- menu-then-Play giving 3 hearts while Next Day kept a half-empty row. The two D-014 ordering rules it also names below are now DEAD: lives left the save file in v6, so neither ReturnToMainScreenAbandoningDay's refill-before-save nor RetryCompletedDay's SaveProfile-last decides anything any more (both lines stay, and their comments say why). D-042 added an OPTIONAL metaCatalog and TryContinueIntoNextDay: "Next Day" now leaves for the main screen when the next day opens a Day-unlocked prop, so D-041's celebration cannot be skipped. Both exit reasons (a prop opens; no next Day authored) hand off to ReturnToMainScreenFromCompletedDay rather than AdvanceToNextDay -- that method's own comment explains why, and the reasoning covers the new case identically: clearing the board and refilling slots microseconds before the scene unloads is either wasted or an event cascade through tearing-down views. The catalog is optional so a missing reference cannot break a day scene over a routing nicety, but Awake logs its absence once, because an empty field and a catalog with no upcoming unlocks look identical on screen. This class writes nothing new -- it decides which scene loads. Base class is SessionHost since D-022, not MonoBehaviour, so the shared HUD can bind to "whichever component provides this scene's session" -- changing the base class does not change the component's type identity, so nothing serialized in SampleScene was disturbed. Awake's SESSION half moved to GameSession (decisions.md D-021): state, wallet + persisted balances, lives + persisted lives, the Day catalog and the clamped day index, along with the four ordering rules between them. What stayed is the DAY runtime -- ticket slots, tray, board distribution, the economy calculator, the sequence provider, the subscriptions, the pre-seed, FillEmptySlots and the per-frame tick -- because a running day behind the main screen would time tickets out and cost lives. State and LivesManager are now FORWARDING properties with their original names, which is the only reason roughly a dozen hand-wired views in the scene needed no rewiring. SaveProfile() is a one-liner onto Session.Save(): WHEN to save is game flow and stays here (the four points that own the atomic-day contract), WHAT gets written is schema and does not. subscribes only TicketAssigned/TicketDelivered since D-009 removed the XP commit/discard handlers on DayCompleted/DayRetried; CaptureDayStartSnapshot now snapshots SoftMoney alone; exposes EconomyConfig so TicketCardsView reads the tier ratios that colour the timer bar from the same asset that pays them; owns the Wallet (constructed right after State) and no longer keeps a dayStartSoftMoney field of its own -- CaptureDayStartSnapshot is gone, the two call sites call wallet.CaptureDayStart(). Adım 2 re-created the OnDayRetried handler and its DayRetried subscription (D-009 had deleted both with the XP discard), which is what stops a failed day from keeping its earnings. Adım 4 re-created the OnDayCompleted handler the same way and owns the only disk writes in the game: SaveProfile on day completion and on RetryCompletedDay (which corrects an already-banked figure), never on a failed retry. D-012 added two more writes and the day index to the payload: AdvanceToNextDay persists the new index (so quitting mid-day does not relaunch onto an already-beaten day) and ReturnToMainScreenAbandoningDay writes the REVERTED wallet, the one place a failed attempt reaches disk -- without it, walking out would refund a Gem-paid Continue. D-014 added Lives to the payload and two ORDERING rules that WERE load-bearing until D-064 removed the field: ReturnToMainScreenAbandoningDay calls LivesManager.RetryDay() BEFORE saving (it is only reachable at 0 lives, so persisting that would write an unplayable save), and RetryCompletedDay's SaveProfile moved to LAST (RetryDay refills lives, so saving first wrote the pre-refill count). ReturnToMainScreenFromCompletedDay deliberately does NOT reuse AdvanceToNextDay: only the index has to survive a scene that is about to be destroyed, and the board/tray/slot reset would cascade events through views mid-teardown. ResolveStartingDayIndex clamps a persisted index against the parsed catalog -- unclamped it resolves CurrentDay to null and CreateNextTicket throws on the first fill. Structurally untestable from the EditMode suite: this file is in the predefined Assembly-CSharp, which an asmdef test assembly cannot reference. Board spawning is triggered from OnTicketAssigned and NOWHERE else -- Update ticks ticket timers and nothing more. D-044 did build a per-slot urgency edge detector here (RefreshBoardForNewlyUrgentTickets, to give BoardDistributor's urgency override a time trigger) and then REVERTED it on the user's instruction: spawning is order-triggered, full stop, and the about-to-expire check belongs on that same path. Do not re-add a timer or a per-frame board poll here; if the guarantee ever looks starved again the lever is the active-first budget in BoardDistributor, or guaranteedTicketCount in the Day file, not a new trigger. D-056 made a completed day pay DayLifecycleManager.StarCount x GameConfig.GemsPerStar in Gems; D-057 then made the whole payout DEFERRED and superseded how it is delivered (the AwardStarGems helper D-056 added is gone). OnTicketDelivered no longer calls the wallet at all -- nothing reaches a balance during a day, and DayLifecycleManager's running Total is the debt, to the coin the same number the per-delivery calls used to add up to. OnDayCompleted now fills a DayRewardPurse (Total + the star gems) and still saves, and still saves NONE of the reward: that write banks lives and the day index, so a force-quit on the popup costs only the money in flight. The purse is released by the Day Complete popup's reward flight through ClaimRewardGems/ClaimRewardSoftMoney, one call per icon that lands, which is what makes the HUD counters tick with the animation without a single change to the views. Four ordering rules are load-bearing: AdvanceToNextDay commits BEFORE CaptureDayStart (snapshot first and the new day's first retry reverts the reward away), ReturnToMainScreenFromCompletedDay commits before its existing SaveProfile, RetryCompletedDay DISCARDS instead of committing (a voluntary redo voids the debt; RevertToDayStart takes back whatever already landed -- together they stop a finished day being farmed), and ReturnToMainScreenAbandoningDay discards defensively so "a failed day pays nothing" holds by construction. The trigger still carries the correctness: TicketSlotManager.IsDayComplete guards the publish, so it fires exactly once per attempt and only on the winning path. D-060 added a REQUIRED starScoreConfig (Awake logs an ERROR when it is empty, unlike optional metaCatalog) and split HandleLifeLoss into HandleTicketTimeout/HandleWrongDelivery -- the two systems still take a plain Action, so the failure CAUSE is decided only here, in the one class that hands both their delegate. Every day-start path now passes CurrentDayTicketSeconds into ResetForNewDay (Awake included, which is why Awake calls it at all), so advancing to a longer day scores against the longer day D-092 redirected both CurrentDayIndex assignments (the completed-day exit and AdvanceToNextDay) to Session.GoToDay, which is now the field's single writer. Pure redirection: the bounds test in the exit path is KEPT rather than left to GoToDay's clamp, because clamping would silently pin the player to the last Day where that branch means to leave the index alone. | D-071 adds ONE thing and it owns nothing: an optional HapticsBinder field and a single KeySpent request beside TrySpendKey in SpendKeyForLostDay. Requested unconditionally rather than on TrySpendKey's result, because at zero keys the spend floors and returns false and that is precisely when the player most needs telling the resource is gone; D-068's rule (which exits cost a key, and that the exit is never blocked) is untouched. KNOWN GAP, not a silent one: most routes through here replace the scene, so the request can be lost before LateUpdate flushes it -- worth a device check, and not worth a special immediate path that would bypass the coalescer for one moment only. Every OTHER day-scene haptic reaches HapticsBinder through GameState's events without passing through this class. D-094 put a THIRD gate on Update beside IsAwaitingContinue and the tutorial's reading hold: State.IsPaused, written only by SettingsPopupView. Three separate conditions rather than one combined one, so a stuck ticket clock still says which holder forgot to let go. | h:e3cd027d
ExpoTheExplorer/Assets/Scripts/Session/ExpoTheExplorer.Session.asmdef | asmdef ExpoTheExplorer.Session | sys: Bootstrap | api: asmdef ExpoTheExplorer.Session | dep: ExpoTheExplorer.Core,ExpoTheExplorer.Data,ExpoTheExplorer.Systems.ProgressionSystem,ExpoTheExplorer.Systems.LivesSystem,ExpoTheExplorer.Systems.KeySystem,ExpoTheExplorer.Systems.PowerupSystem,ExpoTheExplorer.Systems.DaySystem | used: ExpoTheExplorer.Tests.EditMode,Assembly-CSharp | crit: K2 | note: exists so GameSession is TESTABLE, which is the whole point of the extraction -- GameManager sits in the predefined Assembly-CSharp and no asmdef test assembly can reach it (D-012). It cannot live in Scripts/Core/: it needs ProgressionSystem, LivesSystem and DaySystem, all three of which already reference Core, so that would be a cycle. And Scripts/Bootstrap/ keeps NO asmdef on purpose -- adding one there would pull GameManager into it and put every hand-wired view reference in the scene at risk | h:a7d574b0
ExpoTheExplorer/Assets/Scripts/Session/GameSession.cs | the player's half of a session: state, wallet, lives, keys, powerup stock, day catalog, owned props | sys: Bootstrap | api: State; LivesManager; KeyManager; PowerupManager; DayCatalog; OwnedMetaItemIds; LastCelebratedDayIndex { get; set; }; HapticsEnabled { get; set; }; CurrentDay; Wallet; GoToDay(int index); Save() | dep: GameState,GameConfig,LivesConfig,FoodCatalog,Wallet,PlayerProfileStore,PlayerProfile,LivesManager,DayCatalogParser,DayJsonSource,DayCatalogNavigator,DayDefinition,PowerupConfig,PowerupManager,PowerupType | used: GameManager,MainScreenRoot,GameSessionTests,PowerupSystemTests | crit: K1 | note: D-065 step 2 added a FIFTH ordering rule and the key half of the save: the ctor takes a KeyConfig (required, not optional -- a null KeyManager would push a null check into every caller) plus an injectable utcNow, builds KeyManager AFTER the wallet because the 40-Gem refill is charged through it, and calls ApplyPersisted immediately. That one call does two jobs: it resolves the profile's -1 marker into a full bar and pays out the keys earned while the game was CLOSED. Save() now writes BOTH halves of the key state -- the count alone would restart the interval in flight on every launch, handing a player who quit 29 minutes into a wait a fresh 30 every time. D-064 removed BOTH lives lines: the ctor no longer calls LivesManager.ApplyPersistedLives (a fresh GameState opens at a full bar, which is now the only thing deciding a day's starting lives) and Save() no longer writes Lives. Ordering rule 2 survives in weakened form -- LivesManager is still built before anything that can cost a life, it just has nothing persisted to apply. D-041 added LastCelebratedDayIndex, and its SINGLE WRITER is MetaGroundsView -- the thing that plays the celebration, because "has this been shown" is only knowable by whatever showed it. GameManager stays the single writer of the balances and the day index; this datum simply has a different one, which the root invariant allows. carries FOUR ordering rules in its constructor and they are the reason it exists as one class: wallet before ApplyPersistedBalances (GameState's setters are internal to ProgressionSystem); LivesManager before anything that can cost a life; the catalog parse before the day-index clamp (the clamp reads Count); and ApplyPersistedBalances re-taking the day-start snapshot, so a session's first retry reverts to the RESTORED balance rather than to zero. A second class re-implementing those four is the failure the single-writer invariant exists to prevent, which is why a separate main-screen controller was rejected. Save() is the single COMPOSER of what reaches the file while PlayerProfileStore stays the file boundary -- no third saver class, because it would need a copy of every field this object holds and a field forgotten in that copy writes a silent zero over real player data rather than failing. Deliberately holds NO day runtime (ticket slots, tray, board, economy, the per-frame tick): those stay in GameManager because a running day behind the main screen would time tickets out and cost lives. Looks nothing up -- every config is a ctor argument -- which is what lets a test build one with no scene and no assets; profileStore and dayCatalog are injectable for exactly that. Since D-026 the load passes PlayerProfileStore.NewPlayer(gameConfig.StartingSoftMoney) as its FALLBACK, which makes "no readable save" and "a new player's opening coins" one fact instead of two: the grant reaches the wallet through the same ApplyPersistedBalances call as any restored balance, so no new write path appears (Wallet is still the single writer) and the grant lands INSIDE the day-start snapshot, which is what stops a new player's first retry from reverting it away. A saved file always wins, so nobody is topped up on a later launch; an UNREADABLE file (corrupt, or written by a newer build) is treated as a new player on purpose, since this build cannot know what those numbers meant D-092 made this the SINGLE WRITER of GameState.CurrentDayIndex via GoToDay. It was not before: the field was assigned in three places (GameManager's day advance and completed-day exit, plus this constructor) while fingerprint.md claimed GameManager owned it -- false, and unnoticed only because the three never ran together. A main-screen cheat needed a fourth caller where GameManager does not exist, which is where split ownership stops being a doc error. GoToDay clamps (an index past the last Day resolves CurrentDay to null and throws on the first ticket) and deliberately does NOT save: callers already decide that, two of them save several fields at once, and a save in here would double-write on those paths. The constructor routes through it too, since a single-writer rule that exempts its own constructor is worth nothing. | h:b5b02f21
ExpoTheExplorer/Assets/Scripts/Session/SessionHost.cs | the abstract "this scene provides a session" component | sys: Bootstrap | api: abstract SessionHost.Session | dep: GameSession | used: GameManager,MainScreenRoot,HudWalletSource | crit: K2 | note: an abstract MonoBehaviour rather than an interface, and that is a UNITY CONSTRAINT not a preference: [SerializeField] cannot serialize an interface, so an Inspector field that is both draggable and type-checked must name a concrete type. The alternatives were a MonoBehaviour field plus a runtime cast (any component becomes draggable, failing at play time instead of in the Inspector) or a runtime lookup, which the user ruled out for this binding in D-013. It lives in the Session assembly because it must SEE GameSession and because Assembly-CSharp can derive from an asmdef type while the reverse is impossible -- both implementers live there. Session is deliberately allowed to be null: a host assigns it in its own Awake and Unity does not order Awake across GameObjects, so every reader is a view's Start | h:5cbf0faa
ExpoTheExplorer/Assets/Scripts/Core/BoardGrid.cs | raw grid mechanics: capacity, cell occupancy, the full-board spawn queue, per-cell change notification | sys: BoardUI | api: IsInBounds(int x, int y); IsCellEmpty(int x, int y); ItemAt(int x, int y); TryGetFirstEmptyCell(out int x, out int y); TryPlaceItem(BoardItem item, int x, int y); RequestSpawn(BoardItem item, Random random = null); Clear(); RemoveItem(int x, int y) | dep: BoardItem,EventBus,GameConfig | used: GameState,BoardDistributor,TrayManager,DayBoardTimelinePlayer,BoardView,BoardItemDragHandler,DayEditorDayStartPreview,BoardGridTests | crit: K1 | note: the ONLY writer of board cells, and deliberately knows nothing about WHAT to spawn (that is BoardDistribution's call) -- callers only ever RequestSpawn. Dimensions come from GameConfig, never hardcoded (root CLAUDE.md, and the grid size is still an open question). Two ordering rules are load-bearing: RemoveItem backfills the freed cell from pendingSpawns before publishing CellChanged, so a full board keeps its promise that a queued spawn lands the moment a cell opens; and Clear drains pendingSpawns FIRST, or that same backfill refills cells mid-loop and the board never empties. RequestSpawn with a Random picks uniformly among ALL empty cells so spawns do not cluster in scan order -- passing null falls back to first-empty, which existing tests rely on. TicketSlotManager's apparent dependency is a comment stating it deliberately has none | h:7076732f
ExpoTheExplorer/Assets/Scripts/Core/BoardItem.cs | one concrete item on the board (food + resolved modifications) plus the sprite stack that draws THAT variant | sys: BoardUI | api: ResolvedLayer.SourceModification | dep: FoodItemConfig,LayerVisibility,Modification,SpriteLayer | used: BoardGrid,BoardDistributor,TrayManager,TraySlot,DayBoardTimelinePlayer,BoardView,BoardItemDragHandler,RequiredItemKey,FoodItemConfigEditor,BoardItemLayerTests | crit: K1 | note: D-083 (2026-08-26) gave ResolvedLayer a SourceModification, which is a fact this class already computed and threw away: a HiddenByDefault layer is on the item BECAUSE of a modification, and without carrying that, "which of these sprites IS the extra mustard" is unanswerable -- a food's layers are otherwise just a stack of sprites. Null for the bun and the sausage, and null for a layer merely still visible because a modification did NOT remove it. BoardView draws every layer identically and ignores it; the tutorial's arrow is the only reader. immutable passive data except for ResolvedLayers, which lives here rather than on FoodItemConfig because resolving visibility needs Modification (Core), and Data cannot depend on Core. Layer rules: AlwaysVisible always draws; VisibleByDefault hides if the tied modification is present in EITHER direction (added or removed -- the base cheese layer must go whether cheese was removed or replaced by extra cheese); HiddenByDefault draws only on the exact (modification, direction) pair. OverallScale multiplies BOTH offset and size so the stack zooms as one rigid object instead of each layer inflating in place. A modification with no authored layer is therefore INVISIBLE on the board while still counting for matching -- a content gap that reads as a gameplay bug (checked when diagnosing D-044; all three hotdog mods do have layers). Falls back to the flat Config.Sprite when no layer resolves | h:d7422f26
ExpoTheExplorer/Assets/Scripts/Core/EventBus.cs | MISSING-role | sys: ? | api: Subscribe(Action<T> handler); Unsubscribe(Action<T> handler); Publish(T payload) | dep?: GameState | used: ? | crit: ? | note: auto-added; AI must complete | h:41319a1d
ExpoTheExplorer/Assets/Scripts/Core/ExpoTheExplorer.Core.asmdef | MISSING-role | sys: ? | api: asmdef ExpoTheExplorer.Core | dep?: ExpoTheExplorer.Data | used: ? | crit: ? | note: auto-added; AI must complete | h:7ddab159
ExpoTheExplorer/Assets/Scripts/Core/GameState.cs | central session state + reactive event buses | sys: Bootstrap | api: const DefaultStartingLives | dep: BoardGrid,EventBus,GameConfig,Ticket | used: GameManager,HudWalletSource,TicketSlotManager,TrayManager,BoardDistributor,BoardGrid,LivesManager,DayLifecycleManager,SoftMoneyView,GemsView,LivesView,BoardView,TicketCardsView,TicketCardView,DayCompletePopupView,GameOverPopupView | crit: K1 | note: read/written by every system but constructed and owned by Bootstrap (GameManager.Awake), which is what sys: names; starting Lives is a constant here since GameConfig's Starting Resources section was deleted, and the starting WALLET now comes from the saved profile (Adım 4), not from this const. D-094 added IsPaused beside IsAwaitingContinue: same shape (a plain bool GameManager.Update reads to hold TicketSlotManager.Tick), different owner -- SettingsPopupView is its single writer, and it is deliberately NOT a second use of IsAwaitingContinue, which four other places read to decide whether a day was lostructor's 0/0 -- those are only a brand-new player's values; Xp/Level and their event buses removed in D-009; SoftMoney/Gems setters are `internal` as of step 1, visible only to ProgressionSystem (+ the test assembly) via Core/AssemblyInfo.cs, so Wallet is the single writer and any other assembly assigning a balance is a BUILD ERROR; the values and their change events stay here, only the rules moved out | CurrentDayIndex is PERSISTED since D-012 (the comment on it used to say the opposite and pointed at a then-open question, DaySystem_Roadmap Q4, which that decision closed); GameManager is still its single writer, and the value it applies on load is CLAMPED against the parsed catalog. DefaultStartingLives gained a second reader in D-013: HudWalletSource reports it as the main screen's lives label, where no live state exists. | h:85f698a5
ExpoTheExplorer/Assets/Scripts/Core/Modification.cs | MISSING-role | sys: ? | api: - | dep?: ModificationConfig | used: ? | crit: ? | note: auto-added; AI must complete | h:5daf59ef
ExpoTheExplorer/Assets/Scripts/Core/PatienceType.cs | MISSING-role | sys: ? | api: - | dep?: - | used: ? | crit: ? | note: auto-added; AI must complete | h:d197e97d
ExpoTheExplorer/Assets/Scripts/Core/RequiredItemKey.cs | (food + modification-combo) identity, order- and instance-independent -- the one type that decides "is this the item that ticket wants" | sys: BoardDistribution | api: Equals(RequiredItemKey other); Equals(object obj); GetHashCode() | dep: FoodItemConfig,Modification,ModificationConfig | used: BoardDistributor,TraySlot,BoardDistributionTests | crit: K2 | note: the single reason spawning and matching cannot disagree: BoardDistributor counts "how many of exactly this combo are on the board" and TraySlot.Matches compares the tray as a multiset, both through this key. Equality is a HashSet SetEquals over (ModificationConfig, IsAddition) pairs, so two lists built by two different tickets in any order are the same key; GetHashCode XORs the entries to match that order-independence. Consequence worth knowing: a plain hotdog and a ketchup hotdog are DIFFERENT keys, which is exactly why a noise item leaked from a queued ticket does not satisfy an active one (see decisions.md D-044). The draft deps on BoardDistributor/BoardItem/Ticket were comment references only | h:50210cdf
ExpoTheExplorer/Assets/Scripts/Core/SceneFlow.cs | the two scene names + the navigations between them | sys: Bootstrap | api: const MainScreenSceneName; const DaySceneName; LoadMainScreen(); LoadDay() | dep: - | used: GameManager,MainScreenView | crit: K2 | note: static and stateless on purpose. Both loads are Single mode, so leaving a scene destroys it -- that is what lets GameManager keep its "Awake builds the whole Day" shape with no teardown path, and what keeps the day runtime from living on behind the main screen. Every decision that travels WITH a navigation (which day index to persist, whether an abandoned attempt settles first) is deliberately NOT here but in GameManager and the two popups, where a test can reach it; SceneManager cannot be tested. A scene name is a structural identifier owned by blueprint.md's scene inventory, not authored content, so naming them here does not run into the "no content data in code" invariant -- the same call PlayerProfileStore makes for its filename. The day scene keeps Unity's template name (SampleScene): renaming it would move a GUID that Build Settings and every hand-wired reference in it point at | h:faed8d7c
ExpoTheExplorer/Assets/Scripts/Core/Ticket.cs | one order as domain data: required foods, ticket-level modifications, patience/time, arrival sequence, lifecycle state | sys: TicketSystem | api: - | dep: FoodItemConfig,Modification,PatienceType | used: GameState,GameManager,TicketSlotManager,TicketFactory,TicketEntryFactory,DayTicketSequenceProvider,BoardDistributor,TraySlot,EconomyCalculator,TicketCardsView,TicketCardView,WorldTrayView,DayValidator,DayContentGenerator,DayEditorTicketCardPreview | crit: K1 | note: Modifications live on the TICKET, not per required item -- every consumer that needs a per-item key re-derives it the same way (mods apply to the Main item only, Array.Empty for Side/Drink), which is why that two-line rule is duplicated in BoardDistributor and TraySlot rather than living here; RequiredItemKey is the shared type those two build. RemainingSeconds and State are the only mutable members, and TicketSlotManager.Tick is the only writer of the former. ArrivalSequence exists because slot index is reused on delivery/cancellation and so cannot order tickets; BoardDistributor's lottery weights read it. The draft dep on BoardDistributor was a comment reference only -- the arrow runs the other way | h:a7ecf0ac
ExpoTheExplorer/Assets/Scripts/Core/TruncatedPoisson.cs | Poisson(lambda) truncated to 0..n and renormalized -- the sampler behind every "how many" roll | sys: BoardDistribution | api: Sample(int n, float lambda, Random random); Probabilities(int n, float lambda) | dep: - | used: BoardDistributor,TicketFactory,DayEditorModel,DayEditorSettingsPreviews,TruncatedPoissonTests,BoardDistributionTests | crit: K2 | note: every caller has a hard ceiling (guaranteed-ticket budget, un-leaked upcoming tickets, available modifications), which is why truncation is built in rather than left to a Clamp at each site. Uses the unnormalized term ratio lambda^k/k! via term(k)=term(k-1)*lambda/k, so e^-lambda cancels and is never computed and term(0)==1 keeps the sum >= 1 (no divide-by-zero). Lambda is deliberately UNCLAMPED by callers (see BoardDistributionSettings): a huge rate is the legitimate way to author "always the maximum", and the tests rely on it (ExtremeLambda) | h:c53a4089
ExpoTheExplorer/Assets/Scripts/Systems/BoardDistribution/BoardDistributor.cs | live required-pool + noise-pool board spawner (active-first guaranteed-ticket budget, arrival-weighted lottery, unconditional urgency override) | sys: BoardDistribution | api: OnOrderPlaced(IReadOnlyList<Ticket> activeTickets, IReadOnlyList<Ticket> upcomingTickets) | dep: BoardDistributionSettings,BoardItem,FoodCategory,FoodItemConfig,GameState,GuaranteedTicketCountMode,Modification,RequiredItemKey,Ticket,TicketState,TruncatedPoisson | used: GameManager,BoardDistributionTests | crit: K2 | note: D-044 fixed a real stuck position: the guarantee budget is now counted over ACTIVE tickets only and reaches the queued lookahead solely once every active ticket is covered. Drawing from one combined active+upcoming pool let the single budget slot (shipped balancing: 1, with a 10-deep lookahead) land on a ticket the player cannot see, and because selection is sticky it then held that slot for many rounds -- 18.7% of rounds left ZERO active ticket guaranteed, 2.3% left all three slots full with nothing completable, and from there nothing recovers because no delivery means no assignment means no further round. That last part is exactly why the active-first rule is load-bearing rather than cosmetic: spawning is order-triggered ONLY -- OnOrderPlaced is the single entry point, there is no timer and no per-frame poll (the user's call, reaffirmed 2026-08-23 after a time-driven urgency trigger was built and then reverted), so the urgency override is evaluated only on that path and a round that left no on-screen ticket completable could never be revisited. Covering an active ticket every round is what removes the need for any rescue. dep corrected here too: BoardDistributionConfig was deleted by D-007 and the real dependency is the plain BoardDistributionSettings (D-004) | h:f36fbb7c
ExpoTheExplorer/Assets/Scripts/Systems/BoardDistribution/ExpoTheExplorer.Systems.BoardDistribution.asmdef | MISSING-role | sys: ? | api: asmdef ExpoTheExplorer.Systems.BoardDistribution | dep?: ExpoTheExplorer.Core,ExpoTheExplorer.Data | used: ? | crit: ? | note: auto-added; AI must complete | h:cd5a2743
ExpoTheExplorer/Assets/Scripts/Systems/DayLifecycle/DayLifecycleManager.cs | per-day receipt bookkeeping + the star score rule | sys: DayLifecycle | api: StarCount; StarScore; ScoreProgressToMaxStars; TwoStarProgressPosition; TimeEfficiency; FailurePenalty; SavedSeconds; TotalTicketSeconds; WrongDeliveryCount; TimeoutCount; RecordDelivery(DeliveryPayoutResult payout); RecordFailure(DayFailureCause cause); ResetForNewDay(float totalTicketSecondsForDay) | dep: DeliveryPayoutResult,GameState,StarScoreConfig | used: GameManager,DayCompletePopupView,DayLifecycleManagerTests,TicketSystemTests,DayTicketSlotManagerIntegrationTests | crit: K2 | note: D-060 replaced D-008's "3 minus failures" with a SCORE: seconds handed back across the day over the day's total authored ticket seconds, minus 0.20 per wrong delivery and 0.05 per timeout, floored at one star for finishing. Also defines DayFailureCause -- the two causes are separate because a timeout has already forfeited its ticket's whole share of the denominator, so its explicit penalty is the smaller one. Ticket seconds, never wall clock: 3 slots run concurrently, so the two are not the same currency. An unwired StarScoreConfig reads 0 stars and never falls back to a second rule in code; GameManager.Awake is what says so. D-061 added the two numbers the score BAR is positioned from -- derived here, not in the view, because a view holding its own copy of ThreeStarScore starts lying the first time the asset is retuned; the bar is scaled so full == three stars, so the progress is score/ThreeStarScore rather than the score itself. Rounds once per delivery so OrdersDeliveredValue+TipsValue equals the day's SoftMoney exactly; TipsValue can no longer go negative -- the tip is its own non-negative addend now, not the old TotalTip-BaseTip subtraction; public property names deliberately unchanged so DayCompletePopupView needed no edit | h:5cf55156
ExpoTheExplorer/Assets/Scripts/Systems/DayLifecycle/ExpoTheExplorer.Systems.DayLifecycle.asmdef | DayLifecycle assembly boundary | sys: DayLifecycle | api: asmdef ExpoTheExplorer.Systems.DayLifecycle | dep: ExpoTheExplorer.Core,ExpoTheExplorer.Data,ExpoTheExplorer.Systems.EconomySystem | used: - | crit: K2 | note: the one system-to-system arrow in the economy cluster (needs DeliveryTipResult) | h:5792c653
ExpoTheExplorer/Assets/Scripts/Systems/DaySystem/DayBoardTimelinePlayer.cs | deterministic BoardTimeline-entry replay (Day Start only at runtime, see decisions.md D-001/D-002); skips not-yet-authored (null-Item) entries | sys: DaySystem | api: ApplyForStep(BoardGrid board, IReadOnlyList<ResolvedBoardSpawnEntry> boardTimeline, long stepIndex) | dep: BoardGrid,BoardItem,ResolvedBoardSpawnEntry | used: GameManager,DayEditorDayStartPreview,DayBoardTimelinePlayerTests | crit: K2 | note: - | h:f1c95f4c
STALE ExpoTheExplorer/Assets/Scripts/Systems/DaySystem/DayCatalogNavigator.cs | Day-catalog index lookup | sys: DaySystem | api: GetDayAt(IReadOnlyList<DayDefinition> catalog, int index) | dep: DayDefinition | used: GameManager,DayCatalogNavigatorTests | crit: K2 | note: GetEffectiveDay removed with the retry-variant feature (decisions.md D-003) | h:a75f275f
ExpoTheExplorer/Assets/Scripts/Systems/DaySystem/DayCatalogParser.cs | raw Day JSON -> resolved DayDefinition parsing (drops unresolvable Days loudly) | sys: DaySystem | api: ParseAll(IReadOnlyList<DayJsonFile> files, FoodCatalog catalog); ResolveTutorial(TutorialJson tutorial) | dep: BoardDistributionJson,BoardSpawnEntryJson,DayDefinition,DayJson,DayJsonFile,FoodCatalog,FoodItemConfig,GuaranteedTicketCountMode,ResolvedBoardDistribution,ResolvedTutorial,TutorialJson | used: GameManager,DayJsonSource,DayEditorModel,DayCatalogParserTests,DayContentGeneratorTests,DayTicketSlotManagerIntegrationTests,DayValidatorTests | crit: K2 | note: D-087 (2026-08-26) an unreadable `kind` drops the whole tutorial rather than defaulting: a typo in "PowerupIntro" would otherwise become a forced move at cell (0,0), a step the player can never complete. Silence is only safe for a field whose default is meaningful, which this one's is -- but only when it is ABSENT, not when it is wrong. D-083 (2026-08-26) ResolveTutorial now builds the step list, and returns NULL for an enabled block that resolved to zero steps -- which is what lets every downstream reader treat "a ResolvedTutorial exists" as "there is at least one move to make". D-082 (2026-08-26): ResolveTutorial is the one exception to this class's "a bad block drops the whole Day" rule -- an unusable tutorial resolves to null instead, because taking a playable day off the calendar to punish a decoration is the wrong trade, and the two real failure modes are already caught where they can be acted on (DayValidator at authoring time, GameManager at arm time). It is PUBLIC so the Day Editor's validation preview resolves the same block through the same method: a second copy of the enabled-means-present rule could disagree, and the disagreement would surface only as a Day that validates in the editor and behaves differently at runtime. a missing/incomplete runtime.boardDistribution block drops the Day -- detected by content (empty mode string, sub-1 counts) because JsonUtility cannot represent a null nested class | h:8f568d69
STALE ExpoTheExplorer/Assets/Scripts/Systems/DaySystem/DayContentGenerator.cs | offline ticket-sequence roller for Day authoring + the per-Day allowed-food pool rule + the patience-mix plan (board content generation removed Phase 4, see decisions.md D-002 -- Day Start is hand-authored now) | sys: DaySystem | api: Generate(FoodCatalog catalog, TicketGenerationConfig baseTicketConfig, DayEditorMetaJson editorMeta, int ticketsRequiredForDay, int seed); ResolveFoodPool(FoodCatalog catalog, DayEditorMetaJson editorMeta); BuildPatiencePlan(int patientCount, int normalCount, int impatientCount, int ticketsRequiredForDay) | dep: DayEditorMetaJson,FoodCatalog,FoodItemConfig,Modification,PatienceType,Ticket,TicketEntryJson,TicketFactory,TicketGenerationConfig,TicketGenerationJson | used: DayEditorModel,DayContentGeneratorTests,DayValidatorTests,DayTicketSlotManagerIntegrationTests | crit: K3 | note: Editor-tooling-only (no UnityEditor dep, stays EditMode-testable) -- never runs at play time. BuildPatiencePlan is the whole Patience Mix rule (D-055) and is public so it can be tested without the randomness around it -- the Day Editor's readout calls it too rather than re-deriving the arithmetic, which is what keeps the preview from lying about what Generate does. It builds in PatienceOrder (Patient, Normal, Impatient), deliberately NOT PatienceType's declaration order, which is the exact reverse -- so the sequence is sorted by construction and there is no second sort pass to disagree with. An EMPTY return means unauthored (all three counts zero) and GenerateCore then falls back to PickRandomPatienceType, which is how every Day predating the feature keeps its behaviour; a shortfall pads with Normal and an excess is cut off the tail, taking Impatient first | h:af15ca3b
ExpoTheExplorer/Assets/Scripts/Systems/DaySystem/DayDefinition.cs | resolved Day/ticket/board-spawn/board-distribution/tutorial domain models | sys: DaySystem | api: TotalTicketSeconds; TutorialStepKind; ResolvedTutorialStep.Kind; Tutorial; ResolvedTutorial.Steps; ResolvedTutorialStep.SourceX/SourceY/TargetTraySlotIndex/Message/HighlightModification; ResolvedTicketEntry.TimeLimitSecondsWith(TicketRuntimeSettings ticketRuntime) | dep: FoodItemConfig,GuaranteedTicketCountMode,Modification,PatienceType,TicketRuntimeSettings | used: GameManager,DayContentGenerator,DayValidator,DayTicketSequenceProvider,DayCatalogParser,DayCatalogNavigator,DayBoardTimelinePlayer,TicketEntryFactory,DayEditorModel | crit: K2 | note: D-087 (2026-08-26) added TutorialStepKind (ForcedMove / PowerupIntro). Two genuinely different authored shapes in one sequence -- a move to make and a panel to read -- which is the repetition abstraction-level.md asks for; it is NOT an extension point, and a third kind is a design decision to be argued for here. D-083 (2026-08-26) split the tutorial into ResolvedTutorial (a step list) and ResolvedTutorialStep. The list is never null and never empty when a ResolvedTutorial exists -- the parser returns null for an enabled block with no usable steps -- so no reader has to handle "a tutorial with nothing to do". Message is normalised to empty rather than null for the same reason. D-082 (2026-08-26) added ResolvedTutorial and the optional ctor param that carries it, keeping the existing 11 call sites compiling untouched. It is a REFERENCE TYPE and null means "this Day has no forced first move", which is the normal case: presence IS the answer, so no reader can forget an enabled flag, and a half-configured tutorial is unrepresentable. The JSON side needs its own `enabled` bool only because JsonUtility cannot express absence. ResolvedBoardDistribution is optional on the ctor for authoring-side callers; the runtime load path always supplies it. Also used by most DaySystem EditMode test suites. D-060 added TotalTicketSeconds (the star score's denominator, summed rather than authored -- a Day already states its length twice and a third total would be the one free to disagree) and moved the override-vs-patience limit rule here as TimeLimitSecondsWith, so the ticket the player plays and the score they are graded on read ONE rule | h:b688651c
ExpoTheExplorer/Assets/Scripts/Systems/DaySystem/DayJson.cs | plain serializable Day JSON schema (runtime data incl. per-Day board distribution, ticket runtime and the forced-opening tutorial steps + editor-meta food selection/generation settings) | sys: DaySystem | api: - | dep: - | used: DayCatalogParser,DayContentGenerator,DayEditorModel,DayEditorWindow,DayFileIO,GameManager,DayValidatorTests,DayTicketSlotManagerIntegrationTests,DayContentGeneratorTests,DayFileIOTests,DayCatalogParserTests,DayEditorModelConverterTests | crit: K2 | note: D-087 (2026-08-26) added `kind` to a tutorial step ("ForcedMove" default, "PowerupIntro"). A STRING, like guaranteedTicketCountMode and for the same reason -- JsonUtility writes an enum as a bare ordinal, unreadable in a hand-edited Day and silently degrading a typo to whatever is 0. Unlike the `enabled` flag above it, an empty kind has a sensible default, so this field is purely additive and the two steps authored before kinds existed did not change. D-083 (2026-08-26) turned `tutorial` into a LIST of steps (TutorialStepJson) when a second step was authored -- the repetition abstraction-level.md asks for before generalising. `enabled` survived the change: it is still the absence marker a zeroed block cannot provide, and it doubles as an authoring switch that does not delete the steps. Each step adds `message` (content, so never a literal in code) and `highlightModification`. a field added here is absent from every already-authored Day file and deserializes to CLR zero, so each new field needs a reader that can tell "unauthored" from "authored zero" -- mainDishWeights[].maxModificationCount does it via MainDishWeight.NormalizeMaxModificationCount, which is why its slider range starts at 1, and ticketGeneration's three patienceTicketCounts do it by treating ALL THREE ZERO as unauthored (DayContentGenerator.BuildPatiencePlan is the single reader of that rule, D-055) -- 0/0/0 cannot mean an empty Day because the ticket count already governs length. TutorialJson (D-082, 2026-08-26) could not use that trick at all: every field of a zeroed tutorial block is a REAL cell and a REAL tray index, so it carries an explicit `enabled` bool as its absence marker instead -- the one case where a bool here is not a redundant toggle. No clamping lives in this schema: the readers own that rule | h:70952a45
ExpoTheExplorer/Assets/Scripts/Systems/DaySystem/DayJsonSource.cs | thin Resources/TextAsset boundary loading raw Day JSON files (keeps DayCatalogParser Unity-free) | sys: DaySystem | api: LoadAll() | dep: - | used: GameManager | crit: K2 | note: - | h:dec742f0
ExpoTheExplorer/Assets/Scripts/Systems/DaySystem/DayTicketSequenceProvider.cs | authored TicketSequence cursor + non-consuming peek | sys: DaySystem | api: NextTicket(); PeekUpcoming(int count) | dep: ResolvedTicketEntry,Ticket,TicketEntryFactory,TicketFactory,TicketGenerationConfig | used: GameManager,DayTicketSequenceProviderTests,DayTicketSlotManagerIntegrationTests,DayContentGeneratorTests | crit: K2 | note: - | h:a501aafe
ExpoTheExplorer/Assets/Scripts/Systems/DaySystem/DayValidator.cs | authoring-time Day validation gate, errors + non-blocking warnings (ticketSequence.Count, food-selection conformance, per-Day settings blocks, tutorial/board pairing; solvability pre-check removed Phase 4, RetryVariant recursion removed with the feature, see decisions.md D-002/D-003) | sys: DaySystem | api: Validate(DayDefinition day, IReadOnlyList<FoodItemConfig> allowedFoods) | dep: BoardDistributionSettings,DayDefinition,FoodItemConfig,GameState,ResolvedTicketEntry,TicketRuntimeSettings | used: DayEditorModel,DayValidatorTests,DayEditorModelConverterTests | crit: K3 | note: D-087 (2026-08-26) every cell/tray rule now applies to ForcedMove steps only. An intro step names neither, so the old rules would have been checking fields it does not use -- and would have rejected a good step for leaving them at zero. D-083 (2026-08-26) the tutorial check became per-step, and gained a second rule that only a whole-file reader can make: two steps naming the SAME source cell is a guaranteed softlock, because the first step takes that item off the board and the second could then never be finished. For steps after the first, the Day Start check is a NECESSARY condition rather than a sufficient one -- the board moves as the tutorial is played -- which is why GameManager re-checks each step against the live board as it begins. D-082 (2026-08-26) added the tutorial check, and it is the kind of rule only this file can make: the tutorial names a cell and the boardTimeline fills cells, so only a reader holding BOTH halves of one Day file can see them disagree. Errors rather than warnings, unlike most of this file -- a tutorial pointing at an empty cell is a softlock on the game's first day, and refusing to save an unplayable Day is a call this gate is entitled to make. Day Start entries with useExactCell only: an item arriving later is not there when the gate arms, and an entry without the flag lands wherever there is room. WHAT IT DELIBERATELY DOES NOT CHECK is that the source item actually SATISFIES the target tray's ticket -- which would have caught day_00 first being authored at (2,0), a hotdog with extra mustard, against a tray wanting a plain one, i.e. a forced move that costs a life. It is left out because the rule for what a ticket requires lives in TicketRequirements.RequiredCounts and takes a runtime Ticket; re-deriving it from a ResolvedTicketEntry here would be the FOURTH copy of a rule that was deliberately consolidated to stop exactly that. sees RESOLVED settings objects, whose ctors already clamp -- so a rule shaped like DayCatalogParser's raw-JSON range checks is dead on arrival here, which is how three unreachable rules got written and removed. Only the time-limit error survives (its ctor uses Max(0f,x), so a 0 passes through); range protection lives in DayEditorModel.FromJson instead. Everything advisory is a Warning, which never gates Save | h:4e75e279
ExpoTheExplorer/Assets/Scripts/Systems/DaySystem/ExpoTheExplorer.Systems.DaySystem.asmdef | MISSING-role | sys: ? | api: asmdef ExpoTheExplorer.Systems.DaySystem | dep?: ExpoTheExplorer.Core,ExpoTheExplorer.Data,ExpoTheExplorer.Systems.TicketSystem,ExpoTheExplorer.Systems.BoardDistribution | used: ? | crit: ? | note: auto-added; AI must complete | h:9c04f088
ExpoTheExplorer/Assets/Scripts/Systems/DaySystem/TicketEntryFactory.cs | ResolvedTicketEntry -> runtime Ticket adapter | sys: DaySystem | api: Create(ResolvedTicketEntry entry, TicketRuntimeSettings ticketRuntime, TicketFactory ticketFactory, long arrivalSequence) | dep: PatienceType,ResolvedTicketEntry,Ticket,TicketFactory,TicketRuntimeSettings | used: GameManager,DayContentGenerator,DayTicketSequenceProvider,TicketEntryFactoryTests | crit: K2 | note: api/dep repaired 2026-08-24 -- the param has been the per-Day TicketRuntimeSettings since D-005, not TicketGenerationConfig. The time-limit arithmetic itself is gone from here (D-060): it asks entry.TimeLimitSecondsWith, the one place that rule lives | h:b1ff7fd7
ExpoTheExplorer/Assets/Scripts/Systems/EconomySystem/EconomyCalculator.cs | delivery payout: order value + tier tip | sys: EconomySystem | api: CalculatePayout(Ticket ticket) | dep: EconomyConfig,FoodItemConfig,Ticket | used: GameManager,DayLifecycleManager,EconomySystemTests,DayLifecycleManagerTests,TicketSystemTests,DayTicketSlotManagerIntegrationTests | crit: K2 | note: also defines DeliveryPayoutResult + TipTier, which is why the DayLifecycle/TicketSystem suites reference this file; D-060 put the delivered ticket's RemainingSeconds on the payout -- the tier was READ from it, so the tip paid and the time the star score credits come from one measurement of one instant, and it is the last moment that number exists; item count and PatienceType are deliberately absent from the formula (GDD v1.1) and the tier bound is inclusive in the same order TicketCardsView uses so colour and tier match at the boundary | h:f2db43b7
ExpoTheExplorer/Assets/Scripts/Systems/EconomySystem/ExpoTheExplorer.Systems.EconomySystem.asmdef | EconomySystem assembly boundary | sys: EconomySystem | api: asmdef ExpoTheExplorer.Systems.EconomySystem | dep: ExpoTheExplorer.Core,ExpoTheExplorer.Data | used: - | crit: K2 | note: - | h:80f37b93
ExpoTheExplorer/Assets/Scripts/Systems/LivesSystem/ExpoTheExplorer.Systems.LivesSystem.asmdef | LivesSystem assembly boundary | sys: LivesSystem | api: asmdef ExpoTheExplorer.Systems.LivesSystem | dep: ExpoTheExplorer.Core,ExpoTheExplorer.Data,ExpoTheExplorer.Systems.ProgressionSystem | used: - | crit: K2 | note: the ProgressionSystem reference landed in step 1 -- paid Continue spends through Wallet now. One-directional: ProgressionSystem does not reference LivesSystem | h:6b653ac9
ExpoTheExplorer/Assets/Scripts/Systems/LivesSystem/LivesManager.cs | life loss + paid continue (soft money / gems) + persisted-lives load | sys: LivesSystem | api: LoseLife(); TryContinueWithSoftMoney(); TryContinueWithGems(); RefillForNewDay() | dep: GameState,LivesConfig,Wallet | used: GameManager,GameOverPopupView,LivesSystemTests | crit: K2 | note: D-064 renamed RetryDay -> RefillForNewDay and DELETED ApplyPersistedLives. The rename is because GameManager.AdvanceToNextDay now calls it too -- every day opens at a full bar, so three of the four callers were no longer retries. The deletion follows lives leaving the save file (PlayerProfile v6): the load path has nothing to seed, a fresh GameState already opens full, and this class is left the single writer of Lives with one fewer entry point. charges both Continue prices through Wallet since step 1 -- it could not assign a balance now even if it tried (internal setters); the affordability check moved INTO TrySpend so check and deduction cannot drift apart; deliberately does not reset board/tickets or scale difficulty | h:6b4f69f7
ExpoTheExplorer/Assets/Scripts/Systems/KeySystem/ExpoTheExplorer.Systems.KeySystem.asmdef | KeySystem assembly boundary | sys: KeySystem | api: asmdef ExpoTheExplorer.Systems.KeySystem | dep: ExpoTheExplorer.Core,ExpoTheExplorer.Data,ExpoTheExplorer.Systems.ProgressionSystem | used: - | crit: K2 | note: identical reference set to the LivesSystem asmdef and for the identical reason -- the 40-Gem refill is charged through Wallet, so KeySystem -> ProgressionSystem, one-directional, and ProgressionSystem does not know keys exist. Being OUTSIDE Core's InternalsVisibleTo list is load-bearing rather than incidental: it is what makes it a COMPILE ERROR for KeyManager to assign GameState.Gems instead of asking the Wallet, and the step-1 verification compiled KeyManager under this exact assembly name to prove it | h:a2069bd3
ExpoTheExplorer/Assets/Scripts/Systems/KeySystem/KeyManager.cs | key count + real-time regen + gem refill | sys: KeySystem | api: KeyManager(KeyConfig config, Wallet wallet, Func<DateTime> utcNow = null); KeysChanged; Keys; MaxKeys; RefillGemCost; LastRegenUtcTicks; IsFull; HasKey; ApplyPersisted(int savedKeys, long savedAnchorTicks); Refresh(); TrySpendKey(); TryRefillWithGems(); DebugSetKeys(int count) [dev-only]; SecondsUntilNextKey() | dep: KeyConfig,Wallet,EventBus | used: KeySystemTests | crit: K2 | note: D-065, step 1 of .claude/key-plan.md -- written with NO caller on purpose, so the step changes no behaviour. SINGLE WRITER of the key count, and unlike LivesManager that is compiler-enforced: the count is a PRIVATE field here rather than on GameState (a key outlives the day, and GameState.Lives' public setter leaves its own rule resting on a comment). THE CLOCK IS INJECTED and this is the project's only wall-clock dependency; a class calling DateTime.UtcNow directly would put every rule below out of reach of a test. Refresh() is idempotent and cheap and every mutating method calls it first, so CORRECTNESS NEVER DEPENDS ON A TICK EXISTING -- a per-second Update is display only. Three rules a naive version gets wrong: a FULL bar drags the anchor to now so idle time is never banked (spending after three idle days still costs a full wait); a partial interval is preserved by advancing the anchor in WHOLE intervals, never to now, or every Refresh would rob up to one interval; and a BACKWARD clock pulls the anchor to now rather than freezing the player until real time catches up. TryRefillWithGems checks the cap BEFORE touching the wallet, so a full player is never charged. ApplyPersisted resolves the -1 'absent' marker into the cap, because 0 keys is a locked-out player and PlayerProfileStore has no config to fill it in with; a nonsense anchor falls back instead of throwing, since new DateTime(ticks) throws rather than clamping D-092 added DebugSetKeys, inside UNITY_EDITOR |  | DEVELOPMENT_BUILD: there was otherwise no FREE way to move the count (TryRefillWithGems charges 40 Gems and refuses at the cap, TrySpendKey only goes down), and it lives HERE rather than as a field the debug panel writes because this class is the compiler-enforced single writer and a cheat is not a reason to hand out a second one. It restarts the anchor rather than preserving it, so a tester gets a known clean interval instead of whatever fraction was in flight. | h:87e9a1ee
ExpoTheExplorer/Assets/Scripts/Systems/MetaSystem/ExpoTheExplorer.Systems.MetaSystem.asmdef | asmdef ExpoTheExplorer.Systems.MetaSystem | sys: MetaSystem | api: asmdef ExpoTheExplorer.Systems.MetaSystem | dep: ExpoTheExplorer.Data | used: ExpoTheExplorer.Tests.EditMode | crit: K3 | note: references Data and NOTHING else. D-015 had planned a MetaSystem -> ProgressionSystem arrow ("a purchase spends through Wallet") and D-019 dropped it: the rules take the owned-key set and the balance as PARAMETERS, so the money and the owned list keep their existing single writers in ProgressionSystem and the screen's composition root joins the two -- the same shape GameManager uses to join systems. The payoff is testability: zero system dependencies, zero MonoBehaviour | h:33b9fb4d
ExpoTheExplorer/Assets/Scripts/Systems/MetaSystem/MetaResolver.cs | what is on the expo grounds right now | sys: MetaSystem | api: IsOwned(...); IsLocationUnlocked(...); UnlockedLocations(...); DefaultLocationIndex(...); IsActive(...); IsAreaSatisfied(...); NextDayUnlock(MetaLocation location, ISet<string> ownedKeys, int currentDayIndex); ActiveItems(...) | dep: MetaCatalog,MetaLocation,MetaItemDefinition,MetaUnlockKind | used: MetaPurchase,MetaSystemTests | crit: K2 | note: D-042 added a CATALOG overload of DayUnlocksBetween for callers holding the asset rather than one location (the day scene asking "will tomorrow open something"). It resolves the SAME location the meta screen opens on, which is what makes "sent back to the menu but shown nothing" unrepresentable: the celebration only starts from Start, and at that moment the viewed location IS the newest unlocked one. Two overloads mean a bare null names neither -- the CS0121 that already bit DefaultLocationIndex -- so cast at the call site if one is ever needed. D-041 added DayUnlocksBetween: which Day-unlocked props opened between two days, i.e. what the main screen owes a celebration for. EXCLUSIVE of sinceDayIndex and inclusive of currentDayIndex -- inclusive on both ends would replay the same fanfare on every visit, and strictly-after is also what stops a new player (marker 0, day 0) being shown a day-0 prop as an event. Authored catalog order, because two props may share a day and the screen shows them one after another. Area-gated props are excluded: not on screen, nothing to zoom in on, and buying the area later brings the prop in without a fanfare because the purchase already was the moment. D-040 added NextDayUnlock: the ONE upcoming Day-unlock prop and how far the wait has come, as a MetaDayUnlockPreview struct. Nothing new is authored for it -- the wait starts at the most recent Day-unlock milestone already passed, day 0 when there is none, which was the user's pick among three options and the only one needing no second number per prop. The consequence, accepted: pace varies with the catalog's own rhythm (eight days apart fills slowly, two days apart fast). Only the NEAREST future prop is returned, ties by authored order -- previewing every future prop would fill the grounds with translucent objects and blur the one the player is close to. Area gating applies to the PREVIEW too: what holds such a prop back is a purchase, not time, so a bar creeping to full would promise "wait and it comes" when the honest answer is "buy the square" -- a prop whose day has passed with its area unowned therefore shows nothing, the same silence IsActive gives it. Span is guarded against zero even though it cannot be zero in practice, because a catalog is data an author is mid-way through. every fact arrives as a parameter -- no asset loading, no GameState, no save file -- which is what keeps this assembly Data-only and lets the rules be pinned by tests before any UI exists. Consequence for callers: a caller that passes the wrong day index gets a confidently wrong answer, not an exception. AREA GATING APPLIES TO IsActive, not only to purchasability, and that is deliberate: owning a prop whose area is not owned should be unreachable, but an author can add requiresAreaId to a prop players already have, and then their table would hang over ungravelled grass. An area id naming nothing (or naming a non-area item) counts as UNSATISFIED so the content bug stays visible instead of hiding behind a working prop. ActiveItems uses insertion sort, NOT List.Sort, because List.Sort is unstable and equal SortOrders would swap between calls -- the visible symptom is overlapping props flickering. The asset overload and the list overload have the SAME arity, so a literal `null` names neither and a caller passing one needs a cast -- MetaCatalogValidator has the identical pair and the identical trap; real callers pass a real reference, so it only bites tests. DefaultLocationIndex walks back from the END rather than picking the highest unlock day, since catalog order is authored order and two locations may share a day; -1 means nothing is unlocked, which the validator already treats as a content error rather than something to paper over | h:290471f3
ExpoTheExplorer/Assets/Scripts/Systems/MetaSystem/MetaPurchase.cs | can this prop be bought, and if not why | sys: MetaSystem | api: MetaPurchaseVerdict{Ok,NotForSale,AlreadyOwned,LocationLocked,AreaLocked,NotEnoughMoney}; Evaluate(MetaLocation location, MetaItemDefinition item, ISet<string> ownedKeys, int currentDayIndex, int softMoney); CanBuy(MetaLocation location, MetaItemDefinition item, ISet<string> ownedKeys, int currentDayIndex, int softMoney); ShopItems(MetaLocation location, ISet<string> ownedKeys, int currentDayIndex) | dep: MetaResolver,MetaLocation,MetaItemDefinition,MetaUnlockKind | used: MetaSystemTests | crit: K2 | note: S5 (D-035) changed ShopItems' CONTRACT and its signature: it now takes softMoney, DROPS area-locked props entirely, and returns the list ORDERED -- affordable first, then ascending price. Both halves are rules, not presentation, which is why they live here with the tests rather than in the view. The area filter reverses half of the plan's MS2 on the user's instruction: a prop the player cannot reach yet is not shown at all. The named cost is that nothing now tells the player buying the Square opens more props. Unaffordable props STAY -- seeing a price is what makes it worth saving for -- and the view dims those rows. The sort is an INSERTION sort, never List.Sort, because List.Sort is unstable and two props at one price would swap places between one open and the next, which reads as a bug that only happens sometimes; ties keep authored catalog order, pinned by a test. SortsBefore answers false on equal keys for exactly that reason. a VERDICT rather than a bool, because the shop has to say six different things and none of them is recoverable from true/false -- "you cannot afford it" and "buy the square first" are different screens. Check ORDER is load-bearing and runs most-fundamental-first: NotForSale before anything about money (a day-unlocked prop is never buyable, and reporting NotEnoughMoney would send the player off to earn money for nothing), and AreaLocked before NotEnoughMoney (an area-gated prop's price is irrelevant until the area is owned). ShopItems deliberately KEEPS unaffordable and area-gated props -- seeing what is still to come, and its price, is most of what a shop is for -- and drops only owned and day-unlocked ones. It spends nothing and writes nothing: Wallet and the profile writer keep their single-writer roles and the screen orchestrates | h:9fc2b08f
ExpoTheExplorer/Assets/Scripts/Systems/ProgressionSystem/ExpoTheExplorer.Systems.ProgressionSystem.asmdef | ProgressionSystem assembly boundary | sys: ProgressionSystem | api: asmdef ExpoTheExplorer.Systems.ProgressionSystem | dep: ExpoTheExplorer.Core,ExpoTheExplorer.Data | used: - | crit: K2 | note: holds only the (currently unwired) profile save boundary after D-009; becomes the friend assembly for Core's internal balance setters in economy-plan.md step 1 |  | h:ff33144a
ExpoTheExplorer/Assets/Scripts/Systems/ProgressionSystem/PlayerProfile.cs | persisted profile DTO: schema version + wallet + day index + keys + powerup charges + owned props | sys: ProgressionSystem | api: Version; SoftMoney; Gems; CurrentDayIndex; Keys; LastKeyRegenUtcTicks; AutoCollectCharges; TimeResetCharges; NoiseClearCharges; OwnedMetaItemIds; LastCelebratedDayIndex; HapticsEnabled | dep: - | used: PlayerProfileStore,GameManager,GameSession,MainScreenView,PlayerProfileStoreTests,PowerupSystemTests | crit: K2 | note: v8 added the three powerup charge fields (GDD 5.2) and v9 RENAMED one of them (BoardClarityCharges -> NoiseClearCharges) when the user corrected what that powerup does; JsonUtility matches on the key, so the rename needed a real migration or a v8 file would read the field as 0 -- a real count meaning 'none' -- same marker rule as Keys, and THREE NAMED FIELDS rather than an array because the runtime indexes charges by (int)PowerupType, so a named field per type keeps the FILE correct through a renumbering that would scramble an array. D-065 added Keys + LastKeyRegenUtcTicks (v7). Keys is the third field where 0 is not safe, but it differs IN KIND from v3's Lives and v5's marker: the correct value for an older file is KeyConfig's cap, which PlayerProfileStore cannot see and must not gain a reference to. So -1 is written as an 'absent' MARKER and KeyManager.ApplyPersisted resolves it where the cap lives. 0 could not double as absent because it is a real, reachable count meaning 'locked out'. The anchor needs no branch at all -- 0 means 'no timestamp' and the load path starts the clock then, which is also what keeps System.DateTime out of the store. It is TICKS rather than a DateTime because JsonUtility round-trips a DateTime as {} with no error, the same trap this class already documents for auto-properties and HashSet. D-064 REMOVED Lives (v6) -- the first version to drop a field rather than add one. Lives are a per-day resource again, refilled at the start of every day including a successful advance, so a saved count would be a staler second authority on a number GameState already opens at full. A removal needs no upgrade branch: JsonUtility drops a JSON key with no matching field, so every v3-v5 file's Lives is simply ignored. MaxLives was never here and still is not. D-041 added LastCelebratedDayIndex (v5). It is STORED rather than derived because "has this been celebrated" is the one fact about a Day-unlocked prop that does not fall out of comparing day indices. ZERO IS NOT SAFE: read as 0, an existing save owes a fanfare for every prop the player got days ago, so v5 carries a real migration (to CurrentDayIndex), the second in the project after v3's Lives. A NEW player at 0 is correct without one, because the comparison is strictly-after. v4 added OwnedMetaItemIds (List<string> of qualified "<location>.<item>" keys, decisions.md D-020) -- the first REFERENCE-typed field, so the safe default is an empty list rather than a zero and PlayerProfileStore normalizes it on every load. A LIST, not a HashSet: JsonUtility round-trips a HashSet as "{}" with no error, the same trap this class already documents about auto-properties. The key FORMAT is not decided here -- MetaCatalog.OwnershipKey is its single authority and this class only carries strings. Holds PURCHASES only; location unlock and Day-prop visibility stay derived. public FIELDS, not properties -- JsonUtility round-trips an auto-property as {} with no error. Held Xp/Level until D-009; Adım 4 gave it the wallet plus the Version the root CLAUDE.md save invariant demands. Adding a field means bumping PlayerProfileStore.CurrentVersion, and every existing file will read the new field as 0, so 0 must be a safe default. CurrentDayIndex (v2, D-012) is the worked example of that rule: 0 means "the first Day", which is exactly what a v1 file's absent field should mean, so it shipped with no upgrade code -- and it is a catalog POSITION, not a Day file's own dayIndex. Lives (v3, D-014) is the field that BROKE that pattern: 0 lives is a dead player, not a fresh one, so PlayerProfileStore.UpgradeToCurrent fills it explicitly for pre-v3 files -- the case the version number exists for. MaxLives is deliberately absent: nothing varies it. HapticsEnabled (v10, D-094) is the FIRST bool and the first device preference rather than progress: a bool has no spare value to use as an absent marker the way Keys and the charge fields use -1, so it takes v5's shape instead -- a plain semantic migration writing true. Carries NO `= true` initializer on purpose: that would cover the migration case by accident and make the two answers inseparable |  | h:0498d6e4
ExpoTheExplorer/Assets/Scripts/Systems/ProgressionSystem/PlayerProfileStore.cs | versioned JSON load/save boundary + schema upgrades | sys: ProgressionSystem | api: const CurrentVersion; const KeysAbsentMarker; const PowerupChargesAbsentMarker; PlayerProfileStore(); PlayerProfileStore(string filePath); Load(PlayerProfile fallback = null); NewPlayer(int startingSoftMoney); Save(PlayerProfile profile); Delete() | dep: PlayerProfile | used: GameManager,GameSession,MainScreenView,PlayerProfileStoreTests,PowerupSystemTests | crit: K2 | note: CurrentVersion is 9. v9 is the first version whose ONLY change is a NAME (BoardClarityCharges -> NoiseClearCharges) and it still needs a branch, because JsonUtility matches on the key: a v8 file's old key is simply not read and the new field returns 0, a real count meaning 'none'. Its branch is `== 8` rather than `< 9` so it touches only the file that has the problem. v8 (+AutoCollectCharges/+TimeResetCharges/+BoardClarityCharges, GDD 5.2) is v7's shape repeated exactly -- three more fields where 0 is a real count, so UpgradeToCurrent writes PowerupChargesAbsentMarker (-1) and PowerupManager resolves it against PowerupConfig's authored stock. It got its OWN named constant rather than borrowing KeysAbsentMarker: identical value, different meaning, and a borrowed name survives until someone changes one of the two. D-065: CurrentVersion was 7 (+Keys, +LastKeyRegenUtcTicks) and UpgradeToCurrent gained a Version<7 branch that writes KeysAbsentMarker (-1) rather than a count. That marker is the interesting part: every migration before it knew the right answer, and this one does NOT -- the answer is KeyConfig's cap, and giving this class a config reference would break both its file-boundary role and the single-authority rule for a balancing number. The constant is public so the value has ONE name across the upgrade, NewPlayer and the tests; the reading end (KeyManager.ApplyPersisted) deliberately accepts ANY negative rather than referencing it, so a hand-edited -2 cannot lock a player out. NewPlayer takes the same path for the same reason: 'never played' and 'played before keys existed' deserve the identical answer. Three tests pin it -- an older file gets the marker and not a 0, a CURRENT-version file's 0 is left alone (an unconditional upgrade would mint infinite keys on relaunch), and the anchor stays 0. D-064: CurrentVersion was 6, and v6 REMOVES Lives -- the only schema change so far that drops a field. It carries NO upgrade branch by design (JsonUtility ignores an unmatched key), which is why v3's Lives refill inside UpgradeToCurrent is gone; the version is still bumped, because the number is what tells a reader the file's shape. Two tests pin the removal from both sides: a literal v3 payload still carrying "Lives":2 loads with every other field intact, and Save is asserted against the file's raw TEXT to prove no Lives key is written again (a reloaded object could not tell the difference). This class no longer references Core at all -- 'an older file meant full lives' stopped being a schema fact. D-041: CurrentVersion was 5, and UpgradeToCurrent gained its second real migration -- LastCelebratedDayIndex = CurrentDayIndex for anything older, because 0 would replay every past unlock. Two tests pin both directions: an old file gets today's index without its other fields being disturbed, and a CURRENT-version file is left alone (an unconditional upgrade would push the marker forward every launch and silently swallow a real unlock). CurrentVersion is 4 since D-020. Normalize() sits OUTSIDE UpgradeToCurrent on purpose and that is load-bearing: UpgradeToCurrent returns early once the file is already current, which is right for a version-specific fix like v3 Lives but wrong for a reference type that can arrive null from ANY version -- a hand-edited or half-written v4 file included. It also wraps every fallback return, since a caller-supplied fallback is an object someone else built. It is a normalization, not a migration: "owns nothing" is the correct meaning of an absent list, so there is no semantic call to get wrong. CurrentVersion is 3 since D-014 (v1 = version+wallet, v2 = +CurrentDayIndex, v3 = +Lives), and the version RANGE below is what carries an older player's file forward untouched. UpgradeToCurrent is the project's first real MIGRATION: it fills Lives for pre-v3 files because absent-reads-as-0 would launch a dead player, and it touches only fields a version ADDED -- a v2 file's money/gems/day index must survive, which PlayerProfileStoreTests pins against a literal v2 payload. NewPlayer(grant) is the single answer to "what does a player who has never played hold": the authored coin grant, FULL lives, first Day, nothing owned (D-026). The grant is an ARGUMENT rather than a number written here, and the private Defaults() behind a caller that supplied no fallback deliberately grants ZERO -- inventing a balance in this class is exactly the embedded content the invariant forbids, so a caller who wants the authored one passes NewPlayer(...) in (GameSession, the only place a wallet is ever seeded). Delete() throws the file away for the main screen's Start Over button; deleting rather than saving defaults over it is what keeps this class ignorant of the payload -- a reset that wrote every field would have to know every field, and one forgotten there leaves a scrap of the old player behind. An already-absent file is success, and a failed delete returns false rather than throwing, because the caller is a UI button. Reads one const from Core (GameState.DefaultStartingLives) because 'an older file meant full lives' is a schema fact and a second copy of that number would break single-authority. The main screen READS this file (there is no GameState in that scene, so the profile is the hand-off between the two scenes) but never writes -- GameManager is still the only writer. Wired to the game in Adım 4 (GameManager.Awake loads, DayCompleted + RetryCompletedDay save). Missing/corrupt/unreadable-version files all fall back instead of throwing. Load accepts 1..CurrentVersion and refuses 0 or newer: refusing anything != CurrentVersion would delete a real v1 wallet the day a v2 field lands, while accepting 0 makes an unversioned file indistinguishable from a genuine 0 balance. Save STAMPS the version onto the instance it is handed rather than copying, which is what keeps this class ignorant of the payload. Owns the file location too (parameterless ctor -> persistentDataPath/player_profile.json), so callers do not compose paths and System.IO stays out of Bootstrap; the explicit-path ctor exists for tests. CurrentVersion is 10 since D-094 (HapticsEnabled): the branch is `Version < 10` rather than `== 9` because the field is absent from every older file equally, and it writes true -- absent-reads-as-false would switch haptics off for every existing player with nothing on screen to explain it. NewPlayer answers the same field separately, which is the split the two v10 tests pin |  | h:4440a309
ExpoTheExplorer/Assets/Scripts/Systems/TicketSystem/ExpoTheExplorer.Systems.TicketSystem.asmdef | MISSING-role | sys: ? | api: asmdef ExpoTheExplorer.Systems.TicketSystem | dep?: ExpoTheExplorer.Core,ExpoTheExplorer.Data | used: ? | crit: ? | note: auto-added; AI must complete | h:a4063667
STALE ExpoTheExplorer/Assets/Scripts/Systems/TicketSystem/TicketFactory.cs | random ticket assembly (weighted main pick, side/drink inclusion chance, modification count/selection, patience type) from a caller-supplied item pool | sys: TicketSystem | api: PickRandomPatienceType(); PickRandomCustomerName(); Create(IReadOnlyList<FoodItemConfig> pool, string customerName, PatienceType patienceType) | dep: FoodCategory,FoodItemConfig,MainDishWeight,Modification,ModificationConfig | used: GameManager,DayContentGenerator,DayEditorModel,DayTicketSequenceProviderTests,TicketEntryFactoryTests,DayTicketSlotManagerIntegrationTests,TicketSystemTests | crit: K1 | note: the modification roll truncates at Math.Min(the dish's AvailableModifications count, its MainDishWeight.MaxModificationCount) -- two independent ceilings, so raising the authored cap past the dish's own list is a no-op. DayEditorSettingsPreviews.DrawModCountBreakdown mirrors that same Math.Min; if this line changes, that preview goes stale with no test holding it | h:c74aec4a
STALE ExpoTheExplorer/Assets/Scripts/Systems/TicketSystem/TicketSlotManager.cs | active-slot ticket lifecycle (assignment/delivery/cancellation/timeout); no board/BoardDistributor reference by design | sys: TicketSystem | api: PauseForDayComplete(); FillEmptySlots(); DeliverTicket(int slotIndex); CancelTicket(int slotIndex); ResetSlotsForNewDay(); Tick(float deltaSeconds); ResolveDeferredTimeouts() | dep: GameState,Ticket | used: GameManager,DebugTicketDeliveryController,DayTicketSlotManagerIntegrationTests,TicketSystemTests | crit: K1 | note: D-099 (2026-08-26) DEFERS the cancellation when the timeout took the last life: loseLife still runs first (the popup goes up inside it), then Tick reads state.IsAwaitingContinue and leaves the ticket standing at 0 seconds instead of cancelling, because CancelTicket cascades a whole board round -- replacement ticket, required-item spawn, orphaned tray emptying -- behind a popup the player is reading. ResolveDeferredTimeouts runs them on the first live frame after the hold, called from GameManager.Update by READING the flag rather than from an event LivesManager would publish: four callers lift the hold (two paid Continues, RetryDay, SRDebugger's refill) and a forgotten publish would strand a slot empty and make the day uncompletable. Deferrals are per-SLOT because two tickets can expire in one Tick and deferring only the first leaves the second at 0 to expire again on resume, spending a life from the freshly refilled bar; each flag is cleared BEFORE its CancelTicket, since that call's cascade re-enters this class. ResetSlotsForNewDay clears them -- a deferral carried across a reset would cancel a ticket of the NEW day on its first frame. Side effect worth keeping: with the ticket left standing, AllSlotsEmpty cannot go true behind the popup, so a last-ticket-last-life frame can no longer raise Day Complete on top of Game Over. D-078 widened its loseLife delegate to Action<int>, so the timeout hands on the SLOT it expired on -- not the cause (D-060's split still lives in GameManager's two entry points), just the index this class already loops over. The tray could not work it out itself: its own poll only sees a timeout that had items to scatter. - | h:254bcf89
ExpoTheExplorer/Assets/Scripts/Systems/TraySystem/ExpoTheExplorer.Systems.TraySystem.asmdef | TraySystem assembly boundary | sys: TraySystem | api: asmdef ExpoTheExplorer.Systems.TraySystem | dep: ExpoTheExplorer.Core,ExpoTheExplorer.Data | used: - | crit: K2 | note: - | h:3b386ae5
STALE ExpoTheExplorer/Assets/Scripts/Systems/TraySystem/TrayManager.cs | all 3 trays + batched order validation + scatter | sys: TraySystem | api: GetContents(int slotIndex); RemoveItem(int slotIndex, BoardItem item); TryAddItem(int slotIndex, BoardItem item, Action onAccepted = null); DiscardAllForNewDay(); ResolveDeferredScatters(); OnTicketAssigned(int slotIndex) | dep: BoardItem,GameState,TicketState,TraySlot | used: GameManager,WorldTrayView,TrayFillCounterView,BoardItemDragHandler,TraySystemTests | crit: K1 | note: D-099 (2026-08-26) DEFERS the wrong-order scatter when that life was the last one, the mirror of TicketSlotManager's deferred timeout and for the same reason: flinging a tray of food across the board is the most visible thing that can happen behind a Game Over popup. The tray is deliberately NOT cleared either, so the resume starts from the picture that was on screen; ResolveDeferredScatters (GameManager.Update, flag-driven, before the ticket resolver so the items are back before a new ticket asks the board for its required food) scatters and clears. DiscardAllForNewDay clears the flags for the reason it does not scatter in the first place -- the board is wiped in the same reset. D-078 widened loseLife to Action<int> alongside deliverTicket, at no cost: the wrong batch was resolved for one known slot, so a view can show the loss on the tray it happened on. takes deliverTicket/loseLife delegates instead of TicketSlotManager/LivesManager refs so it depends only on Core; the clear-before-deliver ordering is load-bearing against the delivery cascade | h:3e8bf538
ExpoTheExplorer/Assets/Scripts/Systems/TraySystem/TraySlot.cs | one slot's tray contents + multiset match | sys: TraySystem | api: Add(BoardItem item); Remove(BoardItem item); Clear(); IsFull(Ticket ticket); Matches(Ticket ticket) | dep: BoardItem,RequiredItemKey,Ticket,TicketRequirements | used: TrayManager | crit: K2 | note: modifications are compared only for the Main item (TicketFactory never attaches any to Side/Drink). SINCE powerup-plan Adım 5 THE REQUIRED SIDE OF Matches() LIVES IN TicketRequirements (Core): this file used to spell the Main-only rule out itself, and two powerups now need the same rule. The ACTUAL side stayed here deliberately -- it reads BoardItems, which already carry the modifications they were built with, so it contains no rule at all, just a count | h:7493abfe
ExpoTheExplorer/Assets/Tests/EditMode/BoardDistributionTests.cs | EditMode tests: BoardDistributor.OnOrderPlaced required-pool selection (Manual/Poisson budget, arrival-weighted lottery, urgency override) + noise-leak behavior + the Day-JSON-to-distributor balancing path | sys: BoardDistribution | api: SetUp(); TearDown(); Settings_ClampOnlyTheFieldsTheConfigGettersClamped(); GuaranteedTicketCountAuthoredInDayJson_ChangesHowManyTicketsGetCovered(); OnOrderPlaced_ActiveTicketNeedsMissingItem_SpawnsIt(); OnOrderPlaced_SerializedGuaranteedCountIsZero_StillGuaranteesOneTicket(); OnOrderPlaced_RequiredItemAlreadyOnBoard_DoesNotSpawnDuplicate(); OnOrderPlaced_DefaultGuaranteedCount_OnlyGuaranteesEarliestArrivedTicket(); OnOrderPlaced_QueuedTicketsCouldWinLottery_ActiveTicketIsAlwaysGuaranteed(); OnOrderPlaced_QueuedTicketAlreadyHoldsTheBudget_NewActiveTicketStillGetsCovered() | dep: BoardDistributionJson,BoardDistributionSettings,BoardDistributor,BoardGrid,BoardItem,DayCatalogParser,DayJson,DayJsonFile,FoodCatalog,FoodCategory,FoodItemConfig,GuaranteedTicketCountMode,Modification,ModificationConfig,PatienceType,Ticket,TicketEntryJson | used: - | crit: K3 | note: the clamp regression tests (serialized-zero cases) now guard BoardDistributionSettings' ctor, which is where those bounds moved. The two D-044 cases pin the active-first budget; the lottery one uses decay 1f (uniform) deliberately -- under the old combined active+upcoming pool the active ticket won only ~1 round in 4, so the in-loop assertion fails almost at once, while under active-first it is deterministic and cannot flake on trial count | h:cde0757d
ExpoTheExplorer/Assets/Tests/EditMode/BoardGridTests.cs | MISSING-role | sys: ? | api: SetUp(); TearDown(); NewBoardGrid_SizeMatchesConfig_AndStartsEmpty(); TryPlaceItem_OnEmptyCell_Succeeds(); TryPlaceItem_OnOccupiedCell_Fails(); RequestSpawn_WhenGridIsFull_QueuesInsteadOfPlacing() | dep?: BoardGrid,BoardItem,FoodItemConfig,GameConfig,Modification | used: ? | crit: ? | note: auto-added; AI must complete | h:befed31b
ExpoTheExplorer/Assets/Tests/EditMode/BoardItemLayerTests.cs | MISSING-role | sys: ? | api: TearDown(); ResolvedLayers_WithNoLayersConfigured_FallsBackToBaseSprite(); ResolvedLayers_AlwaysVisibleLayer_IsAlwaysIncluded(); ResolvedLayers_VisibleByDefaultLayer_HiddenWhenTiedModificationPresent(); ResolvedLayers_HiddenByDefaultLayer_VisibleWhenTiedModificationPresent(); ResolvedLayers_PushAmount_OnlyAppliesToLaterLayersWhenTriggeringLayerIsVisible() | dep?: BoardItem,FoodItemConfig,LayerVisibility,Modification,ModificationConfig,ModificationDirection | used: ? | crit: ? | note: auto-added; AI must complete | h:af9d95df
ExpoTheExplorer/Assets/Tests/EditMode/DayBoardTimelinePlayerTests.cs | MISSING-role | sys: ? | api: TearDown(); ApplyForStep_MatchingExactCellEntry_PlacesAtThatCell(); ApplyForStep_MatchingEntryWithoutExactCell_PlacesInFirstEmptyCell(); ApplyForStep_ExactCellOccupied_FallsBackToRequestSpawn(); ApplyForStep_NonMatchingStepIndex_DoesNothing(); ApplyForStep_MultipleMatchingEntries_AppliesAll() | dep?: BoardGrid,BoardItem,DayBoardTimelinePlayer,FoodItemConfig,GameConfig,Modification | used: ? | crit: ? | note: auto-added; AI must complete | h:32ea0759
STALE ExpoTheExplorer/Assets/Tests/EditMode/DayCatalogNavigatorTests.cs | MISSING-role | sys: ? | api: GetDayAt_ValidIndex_ReturnsThatDay(); GetDayAt_NegativeIndex_ReturnsNull(); GetDayAt_IndexEqualToCount_ReturnsNull(); GetDayAt_EmptyCatalog_ReturnsNull(); GetDayAt_NullCatalog_ReturnsNull(); GetEffectiveDay_NotRetrying_ReturnsBaseDay() | dep?: DayCatalogNavigator,DayDefinition,ResolvedBoardSpawnEntry,ResolvedTicketEntry | used: ? | crit: ? | note: auto-added; AI must complete | h:c154a48a
ExpoTheExplorer/Assets/Tests/EditMode/DayCatalogParserTests.cs | EditMode tests: DayCatalogParser JSON->DayDefinition resolution, loud drop on unresolvable ids/enums, per-Day board-distribution block validation, dayIndex dedupe/sort | sys: DaySystem | api: SetUp(); TearDown(); ParseAll_ValidSingleDay_ResolvesAllFields(); ParseAll_PoissonGuaranteedTicketCountMode_ParsesFromTheStringForm(); ParseAll_MissingBoardDistributionBlock_LogsErrorAndSkipsDay(); ParseAll_InvalidGuaranteedTicketCountMode_LogsErrorAndSkipsDay(); ParseAll_BoardDistributionWithZeroedCounts_LogsErrorAndSkipsDay(); ParseAll_EditorMetaFieldsIgnored_DoesNotAffectResolution(); ParseAll_UnknownMainItemId_LogsErrorAndSkipsDay(); ParseAll_UnknownModificationId_LogsErrorAndSkipsDay(); ParseAll_InvalidPatienceTypeString_LogsErrorAndSkipsDay(); ParseAll_DuplicateDayIndex_LogsErrorButKeepsBothDays(); ParseAll_SortsByDayIndexAscending_RegardlessOfInputOrder() | dep: BoardDistributionJson,BoardSpawnEntryJson,DayCatalogParser,DayDefinition,DayEditorMetaJson,DayJson,DayJsonFile,FoodCatalog,FoodItemConfig,GuaranteedTicketCountMode,ModificationConfig,ModificationEntryJson,PatienceType,TicketEntryJson | used: - | crit: K3 | note: ParseAll_EditorMetaFieldsIgnored is the executable form of the editorMeta contract -- runtime resolution must never depend on that section | h:35fbfc4d
STALE ExpoTheExplorer/Assets/Tests/EditMode/DayContentGeneratorTests.cs | EditMode tests: DayContentGenerator ticket-sequence rolling (seed determinism, ticket-gen overrides, JSON round-trip playback) + per-Day food-pool resolution | sys: DaySystem | api: SetUp(); TearDown(); Generate_ProducesExactlyTicketsRequiredForDayEntries(); Generate_SameSeed_ProducesIdenticalOutput(); Generate_WithTicketGenerationOverride_ForcesInclusionExtremes(); Generate_WithFoodSelection_OnlyRollsSelectedItems(); ResolveFoodPool_NothingSelected_ReturnsEmptyPool(); ResolveFoodPool_KeepsSelectedIdsAndDropsUnknownOnes(); ResolveFoodPool_EverythingSelected_ReturnsCatalogOrder(); Generate_ThenPlayback_TicketContentMatchesGeneratedSequence(); BuildPatiencePlan_ExactCounts_RunsPatientThenNormalThenImpatient(); BuildPatiencePlan_FewerThanTheDayNeeds_PadsWithNormal(); BuildPatiencePlan_MoreThanTheDayNeeds_CutsOffTheTail(); BuildPatiencePlan_AllZero_IsEmptyRatherThanZeroTickets(); Generate_WithAPatienceMix_LaysTheSequenceOutInThatOrder(); Generate_WithNoPatienceMix_StillFillsEveryTicket() | dep: BoardDistributionJson,DayCatalogParser,DayContentGenerator,DayEditorMetaJson,FoodCatalog,FoodCategory,FoodItemConfig,TicketGenerationConfig | used: - | crit: K3 | note: the patience-plan cases assert the ORDER, which PatienceType's own declaration order (Impatient, Normal, Patient) cannot satisfy by accident -- it is the exact reverse of the authored one. - | h:f3539d99
STALE ExpoTheExplorer/Assets/Tests/EditMode/DayEditorModelConverterTests.cs | EditMode tests: DayEditorModel <-> DayJson round-trip (incl. board distribution + ticket runtime), main-dish-weight sync derivation, modification direction seeding, patience-mix round-trip, Clone independence, load-time clamping, unresolvable-id and missing-block tolerance | sys: DayEditor | api: SetUp(); TearDown(); FromDayJson_ThenToDayJson_RoundTripsAllFields(); FromDayJson_ThenToDayJson_KeepsTheSettingsBlocks(); FromDayJson_ThenToDayJson_KeepsMainDishWeights(); FromDayJson_MainDishWeightWithoutMaxModificationCount_SavesTheDefault(); FromDayJson_NoBoardDistributionBlock_FallsBackToDesignedDefaults(); FromDayJson_OutOfRangeCounts_AreClampedSoSaveCannotWriteThemBack(); FromDayJson_ZeroTimeLimit_IsNotSilentlyRepaired(); SyncMainDishWeights_AddsARowPerSelectedMain_InCatalogOrder(); SyncMainDishWeights_KeepsTheAuthoredNumbersOfARowItAlreadyHad(); SyncMainDishWeights_DropsTheRowOfADeselectedMain(); SyncMainDishWeights_IgnoresSidesDrinksAndUnselectedMains(); SyncMainDishWeights_AlreadyInSync_DoesNotRebuildTheList(); ForConfig_AdditionOnly_ComesInAsAnAddition(); ForConfig_RemovalOnly_ComesInAsARemoval(); ForConfig_Both_ComesInAsAnAdditionAndCanBeFlipped(); CanFlipDirection_NullConfig_IsFalse(); FromDayJson_ThenToDayJson_KeepsThePatienceMix(); FromDayJson_NoPatienceMix_StaysAllZero(); ToDayDefinition_FeedsDayValidatorWithoutError(); Clone_ProducesIndependentCopy_MutatingCloneDoesNotAffectOriginal(); FromDayJson_UnresolvableId_LeavesFieldNullWithoutDroppingTheDay() | dep: BoardDistributionJson,BoardSpawnEntryJson,DayEditorBoardSpawnEntry,DayEditorMetaJson,DayEditorMetaModel,DayEditorModel,DayEditorTicketEntry,DayJson,DayValidator,FoodCatalog,FoodCategory,FoodItemConfig,GuaranteedTicketCountMode,MainDishWeight,ModificationConfig,ModificationDirection,DayEditorModification,TicketGenerationJson | used: - | crit: K3 | note: the round-trip test compares whole JsonUtility output, so every new DayJson/editorMeta field must be added to its fixture AND given a non-default value there -- a defaulted field passes even when the model drops it. The SetUp fixture leaves Category unset, and FoodCategory.Main is 0, so its three items are ALL Main-category -- the sync tests build their own catalogs with explicit categories rather than reusing it | h:1f5594fb
STALE ExpoTheExplorer/Assets/Tests/EditMode/DayFileIOTests.cs | MISSING-role | sys: ? | api: SetUp(); TearDown(); Save_ThenLoadAll_RoundTripsCorrectly(); Save_TwiceWithChangedDayIndex_DoesNotRemoveThePreviousFile(); Delete_RemovesFile(); LoadAll_EmptyOrMissingFolder_ReturnsEmptyList() | dep?: BoardSpawnEntryJson,DayEditorMetaJson,DayEditorWindow,DayFileIO,DayJson,DayRuntimeJson | used: ? | crit: ? | note: auto-added; AI must complete | h:6aec67f1
ExpoTheExplorer/Assets/Tests/EditMode/DayLifecycleManagerTests.cs | EditMode tests: per-day receipt bookkeeping + the star score rule | sys: DayLifecycle | api: SetUp(); TearDown(); StarCount_ComesFromTheSecondsHandedBack(float savedSeconds, int expectedStars); StarCount_WrongDeliveryCostsAStar_WhereATimeoutIsAbsorbed(); StarCount_AfterThreeFailures_IsZeroEvenOnAPerfectClock(); StarScore_NeverGoesNegative(); TimeEfficiency_WithNoBudget_IsZero_AndTheDayStillEarnsItsFirstStar(); TimeEfficiency_ClampsAtOne(); StarCount_WithNoConfig_IsZero(); ScoreProgressToMaxStars_IsTheScoreOverTheThreeStarThreshold(float savedSeconds, float expected); TwoStarProgressPosition_FollowsTheAuthoredThresholds(); ScoreProgress_WithAZeroThreeStarThreshold_ReadsFull_AndCollapsesTheNotch(); ScoreProgress_WithNoConfig_IsEmpty(); RecordDelivery_SumsTheSecondsHandedBack(); RecordFailure_CountsEachCauseApartAndTogether(); RecordDelivery_IncrementsCounter(); RecordDelivery_NeverPublishesDayCompleted(); ResetForNewDay_ClearsEveryFigure_AndTakesTheNewDaysBudget(); RecordDelivery_SplitsOrderValueAndTipIntoSeparateTotals(); RecordDelivery_WorstTier_LeavesTipsValueAtZero_NeverNegative(); RecordFailure_IncrementsOrdersFailedCount() | dep: DayLifecycleManager,DayFailureCause,DeliveryPayoutResult,GameConfig,GameState,StarScoreConfig,TipTier,TicketSlotManager | used: - | crit: K3 | note: the WorstTier case is the regression guard for the old negative Tips row. Star thresholds are built at 0.5/0.25 rather than the shipped 0.55/0.3 for the reason EconomySystemTests gives about its own ratios: a boundary test must land on a number floats represent exactly | h:bdc52b03
ExpoTheExplorer/Assets/Tests/EditMode/DayTicketSequenceProviderTests.cs | EditMode tests: cursor + PeekUpcoming behavior | sys: ? | api: TearDown(); NextTicket_ReturnsEntriesInOrder_WithSequentialArrivalSequence(); NextTicket_PastEndOfSequence_Throws(); PeekUpcoming_DoesNotAdvanceCursor(); PeekUpcoming_ThenNextTicket_ReturnsTheSameInstance(); PeekUpcoming_PastEndOfSequence_ReturnsFewerThanRequested(); NextTicket_InterleavedWithPeekUpcoming_StaysInOrder() | dep: DayTicketSequenceProvider,FoodItemConfig,Modification,PatienceType,ResolvedTicketEntry,TicketFactory | used: - | crit: K3 | note: - | h:391cacf5
ExpoTheExplorer/Assets/Tests/EditMode/DayTicketSlotManagerIntegrationTests.cs | EditMode integration tests: real TicketSlotManager + DayTicketSequenceProvider, full-day ticket-exhaustion + completion regression coverage | sys: DaySystem | api: SetUp(); TearDown(); FullDayLifecycle_RealTicketSlotManagerAndProvider_RequestsExactlyNTickets_NeverThrows(); FullDayLifecycle_AllTicketsDeliveredIncludingFinalSlots_CompletesDayWithoutThrowing() | dep: BoardDistributionJson,DayCatalogParser,DayContentGenerator,DayEditorMetaJson,DayJson,DayJsonFile,FoodCatalog | used: - | crit: K3 | note: covers the DaySystem<->TicketSystem seam; filed under DaySystem because its fixture is a full authored Day | h:a75522aa
ExpoTheExplorer/Assets/Tests/EditMode/DayValidatorTests.cs | EditMode tests: DayValidator.Validate (ticketSequence.Count, food-selection conformance, settings-block errors/warnings; solvability check removed Phase 4, retry-variant recursion removed D-003) | sys: DaySystem | api: SetUp(); TearDown(); Validate_TicketCountMismatch_ReportsError(); Validate_TicketCountMatches_NoErrors(); Validate_TicketUsesFoodOutsideTheSelection_ReportsErrorPerTicket(); Validate_NullSelection_SkipsTheFoodCheck(); Validate_GeneratedDayIsAlwaysValid() | dep: BoardDistributionJson,DayCatalogParser,DayContentGenerator,DayDefinition,DayEditorMetaJson,DayJson,DayJsonFile,DayValidator,FoodCatalog,FoodItemConfig,Modification,PatienceType,ResolvedTicketEntry | used: - | crit: K3 | note: - | h:cf04d897
ExpoTheExplorer/Assets/Tests/EditMode/EconomySystemTests.cs | EditMode tests: order value, prices, tip tiers | sys: EconomySystem | api: TearDown(); CalculatePayout_OrderValue_IsSummedItemPrices_NotItemCount(); CalculatePayout_TwoTicketsSameItemCount_DifferentPrices_PayDifferently(); CalculatePayout_AboveWarningRatio_IsFullTier(); CalculatePayout_ExactlyAtWarningRatio_IsWarningTier(); CalculatePayout_ExactlyAtCriticalRatio_IsCriticalTier(); CalculatePayout_SameRemainingSeconds_DifferentTimeLimit_DifferentTier(); CalculatePayout_ItemCount_DoesNotScaleTierThresholds(); CalculatePayout_PatienceType_DoesNotAffectPayoutDirectly(); CalculatePayout_LatestPossibleDelivery_StillPaysOrderValueInFull(); CalculatePayout_ZeroTimeLimit_IsCriticalTier_NotFull(); CalculatePayout_NullRequiredItem_IsSkipped_NotThrown(); CalculatePayout_UnpricedItem_ContributesZero() | dep: EconomyCalculator,EconomyConfig,FoodItemConfig,Modification,PatienceType,Ticket,TipTier | used: - | crit: K3 | note: builds configs AND priced food via SerializedObject since every knob is a private SerializeField; ratios 0.5/0.25 against a 100s limit so the boundary cases land on exactly representable seconds instead of testing float arithmetic | h:4c7471cb
ExpoTheExplorer/Assets/Tests/EditMode/ExpoTheExplorer.Tests.EditMode.asmdef | the EditMode test assembly and what it can see | sys: Testing | api: asmdef ExpoTheExplorer.Tests.EditMode | dep: ExpoTheExplorer.Systems.Tutorial,ExpoTheExplorer.Systems.HapticsSystem,ExpoTheExplorer.Core,ExpoTheExplorer.Data,ExpoTheExplorer.Systems.TicketSystem,ExpoTheExplorer.Systems.BoardDistribution,ExpoTheExplorer.Systems.TraySystem,ExpoTheExplorer.Systems.EconomySystem,ExpoTheExplorer.Systems.LivesSystem,ExpoTheExplorer.Systems.KeySystem,ExpoTheExplorer.Systems.PowerupSystem,ExpoTheExplorer.Systems.ProgressionSystem,ExpoTheExplorer.Systems.DayLifecycle,ExpoTheExplorer.Systems.DaySystem,ExpoTheExplorer.Systems.MetaSystem,ExpoTheExplorer.Editor,UnityEngine.TestRunner,UnityEditor.TestRunner | used: - | crit: K2 | note: D-082 (2026-08-26) added ExpoTheExplorer.Systems.Tutorial, and it is one more instance of the trap this entry exists to record: a new system's assembly is invisible here until it is added by hand, and the symptom appears in the TEST file rather than anywhere near the new system. filed under the `Testing` blueprint entry added in D-065's turn. It carried an unfilled owner field before that, argued as honest because the assembly belongs to no single system -- but that is an argument for a SHARED owner, not for a question mark, and the schema counts a question mark as unfinished. (Spelling that token out literally here used to trip codemap_guard.sh, which substring-matches the whole line: a note ABOUT an unfinished field read as one.) The test FILES stay filed under the system each one tests; only this boundary is common ground. Adding a system whose code needs testing means adding it HERE too -- the reference list is EXPLICIT, a test file compiles against nothing it cannot see, and the symptom of forgetting appears in the TEST rather than anywhere near the new system. NOT hypothetical: it broke the build in D-065 step 1 (KeySystemTests could not see KeyManager, CS0234/CS0246) and cost a round trip, because the step-1 verification reproduced assembly BOUNDARIES without reproducing Unity's asmdef GRAPH -- so it never asked whether the reference was declared. K2 because losing an entry silently drops a whole suite's worth of coverage. It is also the reason GameManager has no tests: a predefined assembly (Assembly-CSharp) cannot be referenced by an asmdef, so nothing here can reach it (decisions.md D-012) | h:7292c4d1
ExpoTheExplorer/Assets/Tests/EditMode/FoodCatalogTests.cs | MISSING-role | sys: ? | api: SetUp(); TearDown(); GetById_ExistingId_ReturnsItem(); GetById_UnknownId_ReturnsNull(); GetModificationById_ExistingId_ReturnsConfig(); GetModificationById_UnknownId_ReturnsNull() | dep?: FoodCatalog,FoodItemConfig,ModificationConfig | used: ? | crit: ? | note: auto-added; AI must complete | h:bb7a69fd
ExpoTheExplorer/Assets/Tests/EditMode/GameStateTests.cs | EditMode tests: GameState construction defaults + change-event publishing | sys: Bootstrap | api: SetUp(); TearDown(); NewGameState_AlwaysHasThreeTicketSlots(); NewGameState_StartsAtDefaultLivesAndAnEmptyWallet(); DefaultStartingLives_IsThree(); SoftMoney_SetToDifferentValue_PublishesSoftMoneyChangedWithNewValue(); SoftMoney_SetToSameValue_DoesNotPublishSoftMoneyChanged() | dep: GameConfig,GameState | used: - | crit: K3 | note: DefaultStartingLives_IsThree is the only remaining guard on the GDD Section 6 life count now that it is a code constant; the two publish tests sat on Xp/Level and were re-pointed at SoftMoney in D-009 to keep the publish-only-on-change guard alive |  | h:4e47835f
ExpoTheExplorer/Assets/Tests/EditMode/LivesSystemTests.cs | EditMode tests: life loss + paid continue affordability | sys: LivesSystem | api: SetUp(); TearDown(); LoseLife_DecrementsLives(); LoseLife_WhenLivesReachesZero_SetsIsAwaitingContinue_AndPublishesLivesDepleted(); LoseLife_WhenAlreadyAtZero_DoesNotGoNegative_AndDoesNotPublishAgain(); LoseLife_AboveOne_DoesNotSetIsAwaitingContinue_OrPublish(); NewState_OpensAtAFullBar_WithNothingSeedingIt(); RefillForNewDay_RefillsLivesToMaxLives_ClearsIsAwaitingContinue_NoCurrencyCheck() | dep: GameConfig,GameState,LivesConfig,LivesManager,Wallet | used: - | crit: K3 | note: D-064 dropped the three ApplyPersistedLives clamp cases with the method and replaced them with NewState_OpensAtAFullBar, which pins the ONE thing that now decides a day's starting lives; RetryDay's case was renamed with the method. constructs LivesManager with a Wallet since step 1; still seeds balances by direct GameState assignment, which the test assembly alone may do (Core/AssemblyInfo.cs) -- Gems have no earn path in the game, so there is nothing else to seed them with | h:d3fcce42
ExpoTheExplorer/Assets/Tests/EditMode/KeySystemTests.cs | EditMode tests: key regen, cap, clock tampering, gem refill, save round-trip | sys: KeySystem | api: SetUp(); TearDown(); NewPlayer_StartsFull(); Refresh_BeforeAFullInterval_GrantsNothing(); Refresh_AfterOneInterval_GrantsExactlyOne(); Refresh_AfterSeveralIntervals_GrantsThatMany(); Refresh_CarriesTheUnfinishedRemainder(); Refresh_NeverGrantsPastTheCap(); Refresh_WhileFull_DoesNotBankTime(); SpendingAfterALongIdleFullPeriod_StartsAFreshWait_WithNoTicksInBetween(); Refresh_WhenTheClockGoesBackwards_GrantsNothing_AndDoesNotStall(); TrySpendKey_AtZero_ReturnsFalse_AndStaysAtZero(); TrySpendKey_PublishesTheNewCount(); TrySpendKey_AtZero_PublishesNothing(); TryRefillWithGems_WithEnoughGems_FillsToTheCapAndCharges(); TryRefillWithGems_FromAPartialBar_StopsAtTheCap(); TryRefillWithGems_WithoutEnoughGems_ChangesNothing(); TryRefillWithGems_WhenAlreadyFull_TakesNoGems(); TryRefillWithGems_RestartsTheClock(); ApplyPersisted_WithTheAbsentMarker_TreatsThePlayerAsFull(); ApplyPersisted_WithAnOldAnchor_PaysOutTheOfflineTime(); ApplyPersisted_WithOfflineTimePastTheCap_StopsAtTheCap(); ApplyPersisted_WithNoAnchor_StartsTheClockNow(); ApplyPersisted_WithNonsenseAnchor_FallsBackInsteadOfThrowing(); ApplyPersisted_ClampsASavedCountAboveTheCap(); SecondsUntilNextKey_WhenFull_IsZero(); SecondsUntilNextKey_CountsDownTowardTheNextGrant(); LastRegenUtcTicks_RoundTripsThroughApplyPersisted() | dep: KeyConfig,KeyManager,GameConfig,GameState,Wallet | used: - | crit: K3 | note: the whole safety net for step 1, which ships code with no caller -- nothing but these tests exercises KeyManager at all. Time is moved by hand through a closure over a fixed Start instant, never DateTime.UtcNow, because a test whose result depends on when it runs fails at midnight. The two cases worth protecting from a well-meaning simplification: Refresh_WhileFull_DoesNotBankTime and its no-ticks-in-between twin pin that a full bar earns nothing, which is what stops a key spent after a long idle period being handed straight back; Refresh_CarriesTheUnfinishedRemainder pins that the anchor moves by whole intervals, the rule a 'just set it to now' version silently breaks. KeyConfig's private fields are set through SerializedObject, so the property-name strings must stay in step with the field names | h:612ebcc6
ExpoTheExplorer/Assets/Tests/EditMode/MetaCatalogValidatorTests.cs | meta catalog validation rules, one test per rule | sys: MetaSystem | api: TearDown(); Validate_NullCatalogAsset_IsAnErrorNotAnException(); Validate_NoLocations_IsInvalid(); Validate_MinimalWellFormedCatalog_IsCleanWithNoWarnings(); Validate_LocationWithoutBackground_IsInvalid(); Validate_DuplicateLocationId_IsInvalid(); Validate_BlankLocationId_IsInvalid(); Validate_NothingUnlockedAtDayZero_IsInvalid(); Validate_LocationsOutOfUnlockOrder_WarnsButStaysValid(); Validate_LocationWithNoItems_WarnsButStaysValid(); Validate_ItemWithBlankId_IsInvalid(); Validate_DuplicateItemIdWithinOneLocation_IsInvalidAndNamesTheOwnershipKey(); Validate_SameItemIdInTwoDifferentLocations_IsFine(); Validate_ItemWithNoSprite_IsInvalid(); Validate_PurchasableItemPricedAtZero_IsInvalid(); Validate_PositionFarOutsideTheBackground_WarnsButStaysValid(); Validate_PositionSlightlyOutsideTheBackground_DoesNotWarn(); Validate_DayUnlockAtALaterDay_IsFine(); Validate_DayUnlockAtDayZero_IsFine(); Validate_DayUnlockCarryingAPrice_WarnsThatThePriceIsIgnored(); Validate_ItemRequiringARealAreaInItsOwnLocation_IsFine(); Validate_ItemRequiringAnUnknownArea_IsInvalid(); Validate_ItemRequiringAnItemThatIsNotAnArea_IsInvalid(); Validate_ItemRequiringItsOwnArea_IsInvalid(); Validate_AreaGateReachingIntoAnotherLocation_IsInvalidAndNamesThatLocation(); Validate_AreaNothingRequires_WarnsButStaysValid(); OwnershipKey_JoinsLocationAndItemWithADot(); Validate_RealisticMeta1Shape_IsCleanWithNoWarnings() | dep: MetaCatalog,MetaCatalogValidator,MetaItemDefinition,MetaLocation,MetaUnlockKind | used: - | crit: K3 | note: needs no SerializedObject, unlike EconomySystemTests -- the schema's plain classes carry public constructors precisely so they can be built without an asset; only sprites are spawned, since "has a sprite at all" is a rule under test. The realistic-Meta1-shape test guards the COMBINATION (ruin + area gate + three trigger shapes) that the single-rule tests cannot | h:f2e9c384
ExpoTheExplorer/Assets/Tests/EditMode/MetaSystemTests.cs | meta rules: what is active, what is buyable, why not | sys: MetaSystem | api: TearDown(); IsOwned_MatchesOnTheQualifiedKey_NotTheLocalId(); IsOwned_SameIdInAnotherLocation_IsNotOwnedHere(); IsLocationUnlocked_OpensOnItsOwnDayAndStaysOpen(); UnlockedLocations_KeepsCatalogOrderAndDropsLockedOnes(); DefaultLocationIndex_IsTheNewestUnlockedOne(); DefaultLocationIndex_TwoLocationsSharingADay_PicksTheLaterOne(); DefaultLocationIndex_NothingUnlocked_IsMinusOne(); IsActive_PurchaseProp_OnlyOnceOwned(); IsActive_DayUnlockedProp_AppearsOnItsDayWithoutBeingOwned(); IsActive_AreaGatedProp_NeedsTheAreaOwnedToo(); IsAreaSatisfied_AreaIdNamingNothing_IsNotSatisfied(); IsAreaSatisfied_AreaIdNamingANonAreaItem_IsNotSatisfied(); ActiveItems_ReturnsDrawOrderAndOmitsInactive(); ActiveItems_EqualSortOrders_KeepAuthoredOrderStably(); Evaluate_AffordablePurchasableProp_IsOk(); Evaluate_OneCoinShort_IsNotEnoughMoney(); Evaluate_DayUnlockedProp_IsNotForSaleEvenWhenRich(); Evaluate_AlreadyOwned_SaysSo(); Evaluate_LockedLocation_SaysSoRatherThanSelling(); Evaluate_AreaLockedAndUnaffordable_ReportsTheAreaFirst(); Evaluate_AreaOwned_LetsTheGatedPropThrough(); ShopItems_KeepsUnaffordableAndGatedProps_DropsOwnedAndDayUnlocked(); ShopItems_LockedLocation_OffersNothing(); NullsAreAnsweredNotThrown() | dep: MetaResolver,MetaPurchase,MetaPurchaseVerdict,MetaCatalog,MetaLocation,MetaItemDefinition,MetaUnlockKind | used: - | crit: K3 | note: D-042 added two catalog-overload tests: it asks the location the screen will OPEN on (a prop opening on an older location must not trigger a return), and nothing unlocked yields nothing. D-041 added four DayUnlocksBetween tests: the window itself, the exclusive lower bound (or every menu visit replays the fanfare), a new player not being shown a day-0 prop, and area-gated props being skipped until the area is bought. D-040 added five NextDayUnlock tests: nearest-future pick, progress measured from the PREVIOUS milestone (8/12 would be the wrong answer and is what measuring from day zero gives), fall-back to day zero when there is no earlier milestone, nothing to show once all are open, and an area-locked prop staying invisible until the area is bought. DayUnlocked() gained a requiresAreaId parameter for the last one rather than a test-only setter. D-039 added two ShopIcon tests: an authored icon wins AND leaves Sprite untouched (a list icon must never leak into the prop on the ground), and no icon falls back to the art. S5 (D-035) rewrote the ShopItems tests against the new contract instead of deleting them, and added three: area-locked props are absent, owning the area brings the prop BACK (so the first test cannot pass by dropping gated props forever), affordable-first-then-cheapest ordering, and equal prices keeping authored order -- the last one is what pins the insertion sort against a future List.Sort. the catalog helper seeds the SO's private list by REFLECTION rather than through SerializedObject the way EconomySystemTests does, and the difference is what is being set: EconomyConfig's knobs are floats that SerializedObject writes in one line each, while this is a list of plain serializable objects whose every field would have to be walked by name -- more fragile than the reflection it replaces, and needing an update on every schema change. A public setter would exist purely for tests. Two cases guard rules that would otherwise regress invisibly: equal SortOrders keeping authored order (List.Sort is unstable) and AreaLocked being reported before NotEnoughMoney. The null cases cast their nulls because the resolver's asset and list overloads share an arity -- that CS0121 shipped once and was caught by Unity rather than by the temporary-csproj check, whose test build had been linked against a MetaSystem DLL predating the overloads | h:4d4e31e3
ExpoTheExplorer/Assets/Tests/EditMode/GameSessionTests.cs | day-index clamp + what the profile seeds + what Save writes | sys: Bootstrap | api: SetUp(); TearDown(); DayIndex_WithinTheCatalog_IsUsedAsIs(); DayIndex_PastTheLastDay_ClampsToTheLastOne(); DayIndex_Negative_ClampsToTheFirstDay(); DayIndex_WithAnEmptyCatalog_IsZero(); Balances_ComeFromTheProfile_ThroughTheWallet(); FirstRetryOfASession_RevertsToTheRestoredBalance_NotToZero(); Lives_IgnoreTheProfile_AndOpenAtAFullBar(); Keys_FromASaveThatPredatesThem_LoadAsAFullBar(); Keys_WithNoSavedFile_StartFull(); Keys_FromACurrentVersionSave_AreNotRefilled(); Keys_AccrueWhileTheGameIsClosed(); OwnedMetaItems_ComeFromTheProfile(); WithNoSavedFile_StartsWithTheAuthoredGrantAndFullLives(); WithASavedFile_TheGrantIsNotHandedOutAgain(); TheGrantSurvivesARetry_LikeARestoredBalance(); Save_ThenLoad_ReturnsEveryField(); Save_PicksUpMetaItemsAddedSinceLoad(); Save_WritesLiveValues_NotTheOnesLoaded() | dep: GameSession,GameState,GameConfig,LivesConfig,KeyConfig,PlayerProfile,PlayerProfileStore,DayDefinition | used: - | crit: K2 | note: D-065 step 2 added four key cases and a settable `now`, so the session's clock is a closure a test can move. Keys_FromASaveThatPredatesThem is the one that matters most: it asserts the -1 marker END TO END, through the store's v7 upgrade and out of the session as a full bar, because if that regresses every existing player launches locked out of their own game with nothing on screen to explain it. Its opposite, Keys_FromACurrentVersionSave_AreNotRefilled, is what stops the fix from becoming infinite keys on relaunch. D-064 INVERTED Lives_ComeFromTheProfile into Lives_IgnoreTheProfile_AndOpenAtAFullBar -- kept rather than deleted, because 'the load path stopped reading a value' is exactly the removal a later save-field addition could quietly undo. Save_ThenLoad_ReturnsEveryField no longer asserts Lives, since Save no longer writes it. the clamp had guarded a real crash since D-012 (an index past the last Day leaves CurrentDay null and throws on the first ticket) with NO test, because it lived in a MonoBehaviour in a predefined assembly -- closing that gap is what the extraction bought. Save_ThenLoad_ReturnsEveryField asserts each field at a DISTINCT value on purpose: a bug that copies the wrong field would pass if two of them happened to match, and the failure mode here is silent (a zero written over real money), not an exception. FirstRetryOfASession pins ordering rule 4, which is invisible until someone reorders the constructor. foodCatalog is null throughout: passing dayCatalog means the real Resources parse is never reached. UnityEngine.Object is spelled out because this file imports both System (for Guid) and UnityEngine, so a bare Object is ambiguous -- MetaCatalogValidatorTests uses the short form legitimately, having no System import. The three D-026 grant cases assert against gameConfig.StartingSoftMoney rather than a literal, so re-authoring the asset cannot make them lie, and together they pin both halves of the rule: a player with no save gets the grant, a player WITH one is never topped up again (including one who legitimately spent down to zero) | h:c51a0fa9
ExpoTheExplorer/Assets/Tests/EditMode/PlayerProfileStoreTests.cs | EditMode tests: profile file behavior (missing/corrupt/empty/round-trip/mkdir) | sys: ProgressionSystem | api: SetUp(); TearDown(); Load_WhenFileDoesNotExist_ReturnsDefaultProfile(); Load_WhenFileIsCorruptedJson_ReturnsDefaultProfile_DoesNotThrow(); Load_WhenFileIsEmpty_ReturnsDefaultProfile(); Load_WhenFileDoesNotExist_WithFallback_ReturnsFallback(); Save_ThenLoad_RoundTripsWithoutFallingBack(); Save_WhenCalledTwice_OverwritesRatherThanAppending(); Save_CreatesParentDirectory_WhenMissing(); Save_StampsTheCurrentSchemaVersion_EvenWhenTheCallerLeftItZero(); SaveThenLoad_PreservesTheWallet(); SaveThenLoad_PreservesTheCurrentDayIndex(); Load_WhenFileCarriesNoVersion_IgnoresItsValues_AndFallsBack(); Load_WhenFileVersionIsNewerThanThisBuild_FallsBack(); Load_WhenFileIsAnOlderButVersionedSchema_StillReadsTheFieldsItHas(); Load_WhenFileIsV2_KeepsEveryFieldItCarried(); Load_WhenFileIsV3_KeepsEveryFieldAndDropsTheRetiredLivesKey(); Save_WritesNoLivesKey(); Load_WhenFileIsOlderThanV7_MarksKeysAsAbsentRatherThanZero(); Load_WhenFileIsOlderThanV7_LeavesTheKeyAnchorAtZero(); Load_WhenFileIsCurrentVersion_DoesNotOverwriteKeys(); Load_WhenFileDoesNotExist_DefaultsToNoMoneyAndTheFirstDay(); NewPlayer_CarriesTheGrantAndNothingElse(); NewPlayer_ClampsANegativeGrantToZero(); Load_WithANewPlayerFallback_PrefersTheSavedFile(); Load_WithANewPlayerFallback_AndNoFile_GrantsTheMoney(); Delete_RemovesTheFile_AndTheNextLoadIsANewPlayer(); Delete_WhenThereIsNoFile_ReportsSuccess(); Load_WhenFileIsV9_TurnsHapticsOn_AndDisturbsNothingElse(); SaveThenLoad_PreservesHapticsSwitchedOff(); NewPlayer_StartsWithHapticsOn() | dep: PlayerProfile,PlayerProfileStore | used: - | crit: K3 | note: D-065 step 2 added the v7 pair plus the anchor case, following the same both-directions shape every migration here uses: an older file gets the ABSENT MARKER (asserted to be not-0, since 0 is a real 'locked out' count), a current-version file's 0 is left untouched, and the anchor gets no branch at all. D-064 rewrote every lives case for v6's REMOVAL: Save_WritesNoLivesKey asserts against the file's raw text (a reloaded object cannot tell an ignored key from an absent one), and the v3 payload deliberately still carries "Lives":2 so that dropping an unmatched key is proven safe rather than assumed. dep no longer includes GameState. D-041 added the v5 migration pair, mirroring the v3 Lives pair: an older file gets LastCelebratedDayIndex = CurrentDayIndex with its other fields untouched, and a current-version file keeps whatever it carried. two groups -- the older file-behaviour cases assert fallback IDENTITY (AreSame), the Adım 4 version cases pin down which files are readable. Load_WhenFileIsAnOlderButVersionedSchema is the v1->v2 migration guard now that D-012 added CurrentDayIndex: it asserts the absent field reads as Day 0, which IS the whole migration. The Overwrites case compares against the instance it saved, not a fresh PlayerProfile(), because Save stamps Version onto what it is given. The D-026 group is a third: the grant is handed to a player with no readable file and NEVER on top of a saved one, and Delete leaves nothing behind -- asserted on the day index and the owned props as well as the money, because a PARTIAL reset is the quiet failure there. The D-094 group is a fourth, and it pins the v10 migration from BOTH sides: a literal v9 payload must come back with haptics ON and every other field it carried untouched, and a saved false must survive a round trip -- the second is what a field initializer would have quietly satisfied while the branch it replaced went untested |  | h:7c31bcb0
ExpoTheExplorer/Assets/Tests/EditMode/TicketEntryFactoryTests.cs | MISSING-role | sys: ? | api: TearDown(); Create_WithCustomerNameOverride_UsesOverride(); Create_WithoutOverride_UsesRandomNameFromConfig(); Create_WithTimeLimitOverride_UsesOverride(); Create_WithoutTimeLimitOverride_UsesConfigDefaultForPatienceType(); Create_BuildsRequiredItemsInMainSideDrinkOrder_SkippingNullSideOrDrink() | dep?: FoodItemConfig,Modification,PatienceType,ResolvedTicketEntry,TicketEntryFactory,TicketFactory | used: ? | crit: ? | note: auto-added; AI must complete | h:3b53cb21
STALE ExpoTheExplorer/Assets/Tests/EditMode/TicketSystemTests.cs | EditMode tests: TicketSlotManager slot lifecycle/events/day-completion + TicketFactory rolls (weighted main pick, Poisson modification count and its per-dish ceiling, direction resolution) | sys: TicketSystem | api: SetUp(); TearDown(); FillEmptySlots_OnFreshGameState_FillsAllThreeSlots(); DeliverTicket_MarksDeliveredAndRefillsSameSlotWithNewTicket(); DeliverTicket_PublishesTicketDeliveredEvent_WithTheDeliveredTicketAndSlotIndex(); FillEmptySlots_PublishesTicketAssigned_ForEachFilledSlot(); Tick_WhenTheLastLifeTimesOut_LeavesTheTicketStandingInsteadOfCancellingBehindThePopup(); ResolveDeferredTimeouts_OnceTheDayResumes_CancelsAndRefillsTheSlot(); Tick_WhenTwoTicketsExpireOnTheSameFrame_BothAreDeferred_AndBothResolveOnResume(); ResolveDeferredTimeouts_WithNothingDeferred_DoesNothing(); ResetSlotsForNewDay_ClearsADeferredTimeout_SoTheNewDayKeepsItsTickets() | dep: DayLifecycleManager,DeliveryPayoutResult,FoodCategory,FoodItemConfig,GameConfig,GameState,LivesManager,MainDishWeight,ModificationConfig,ModificationDirection,PatienceType,Ticket,TicketFactory,TicketGenerationConfig,TicketSlotManager | used: - | crit: K3 | note: MainDishWeight is Inspector-serialized with no setters, so SetMainDishWeights writes its private fields through SerializedProperty -- a new field on that type must be added there or the suites silently roll on CLR zero. Two overloads: the 3-tuple one fills MaxModificationCount with the default, only the cap tests pass the 4-tuple. The ExtremeLambda idiom exists because a Poisson pmf never assigns probability exactly 1, so those tests assert over many seeds instead of one deterministic outcome | h:738dcf49
STALE ExpoTheExplorer/Assets/Tests/EditMode/TraySystemTests.cs | EditMode tests: batch validation, accept-callback order, scatter | sys: TraySystem | api: SetUp(); TearDown(); TryAddItem_CorrectItemsAndModifications_DeliversAndClearsTray(); TryAddItem_OnAcceptedCallback_FiresBeforeDeliverTicketCascade(); TryAddItem_RejectedDrop_OnAcceptedCallbackNeverFires(); TryAddItem_WrongItem_LosesLifeAndScattersToBoardAndClearsTray(); TryAddItem_WhenTheWrongOrderTakesTheLastLife_HoldsTheScatterAndKeepsTheTray(); ResolveDeferredScatters_OnceTheDayResumes_ScattersAndClearsTheTray(); ResolveDeferredScatters_WithNothingDeferred_DoesNothing(); DiscardAllForNewDay_ClearsADeferredScatter_SoTheNewDayOpensOnAnEmptyBoard() | dep: BoardItem,FoodCategory,FoodItemConfig,GameConfig,GameState,TrayManager,TraySlot | used: - | crit: K3 | note: TryAddItem_WrongItem used to assert the scattered item landed on cell (0,0); since TrayManager always carries a Random, BoardGrid picks uniformly among empty cells, so that assertion passed 1 time in 30 and was long mislabelled 'known flaky'. Scatter tests assert the item is on the board, never where | h:0d83b4fd
ExpoTheExplorer/Assets/Tests/EditMode/TruncatedPoissonTests.cs | MISSING-role | sys: ? | api: Sample_LambdaZero_AlwaysReturnsZero(); Sample_NZero_AlwaysReturnsZero(); Sample_ExtremeLambda_AlwaysReturnsN(); Sample_ModerateLambda_ClustersNearLambda_NotMonotonicallyDecreasing(); Probabilities_SumToOne(); Probabilities_LambdaZero_AllMassAtZero() | dep?: TruncatedPoisson | used: ? | crit: ? | note: auto-added; AI must complete | h:b4cf42da
ExpoTheExplorer/Assets/Data/DataScripts/BoardDistributionSettings.cs | immutable per-Day board-distribution balancing values consumed by BoardDistributor (replaces the config asset on the runtime path, D-004) | sys: BoardDistribution | api: BoardDistributionSettings(float noiseLeakCountLambda, GuaranteedTicketCountMode guaranteedTicketCountMode, int guaranteedTicketCount, float guaranteedTicketCountLambda, float earlyTicketWeightDecay, float urgentTimeThresholdSeconds, int leakDepth, int maxLeakCount) | dep: - | used: BoardDistributionConfig,BoardDistributor,DayCatalogParser,DayDefinition,GameManager,DayEditorModel,BoardDistributionTests | crit: K2 | note: also DEFINES GuaranteedTicketCountMode, moved here when BoardDistributionConfig was deleted (D-007). Its ctor mirrors BoardDistributionConfig's GETTERS field for field (not its OnValidate -- copying the latter put a stray Clamp(0,3) on GuaranteedTicketCountLambda and made the ExtremeLambda test nondeterministic); it is the single place those bounds are enforced; NoiseLeakCountLambda is intentionally unclamped (Poisson rate, bounded downstream by MaxLeakCount) | h:599d9112
ExpoTheExplorer/Assets/Data/DataScripts/TicketRuntimeSettings.cs | immutable per-Day ticket play-time values (3 patience time limits + upcoming-queue size) consumed by TicketEntryFactory/GameManager | sys: TicketSystem | api: TicketRuntimeSettings(float impatientTimeLimitSeconds, float normalTimeLimitSeconds, float patientTimeLimitSeconds, int upcomingQueueSize) | dep: - | used: TicketGenerationConfig,TicketFactory,TicketEntryFactory,DayTicketSequenceProvider,DayDefinition,DayCatalogParser,GameManager,DayEditorModel,TicketRuntimeSettingsExtensions,TicketRuntimeSettingsExtensionsTests | crit: K2 | note: holds no PatienceType logic on purpose -- Data cannot reference Core, so the patience switch lives in Core as TicketRuntimeSettingsExtensions; the >=TicketSlotCount floor on UpcomingQueueSize stays in GameManager for the same reason | h:17f377b9
ExpoTheExplorer/Assets/Scripts/Core/TicketRuntimeSettingsExtensions.cs | the single Impatient/Normal/Patient time-limit switch (GDD Section 8) | sys: TicketSystem | api: TimeLimitSecondsFor(this TicketRuntimeSettings settings, PatienceType patienceType) | dep: PatienceType,TicketRuntimeSettings | used: TicketFactory,TicketEntryFactory,TicketRuntimeSettingsExtensionsTests | crit: K2 | note: replaces TicketGenerationConfigExtensions, which hung the same switch off the config asset before these values became per-Day (D-005); lives in Core because Data must not reference PatienceType | h:0d122c4c
ExpoTheExplorer/Assets/Tests/EditMode/TicketRuntimeSettingsExtensionsTests.cs | EditMode tests: patience-type to time-limit mapping, incl. two Days disagreeing | sys: TicketSystem | api: TimeLimitSecondsFor_Impatient_ReturnsImpatientValue(); TimeLimitSecondsFor_Normal_ReturnsNormalValue(); TimeLimitSecondsFor_Patient_ReturnsPatientValue(); TimeLimitSecondsFor_TwoDifferentSettings_ReturnDifferentLimits() | dep: PatienceType,TicketRuntimeSettings | used: - | crit: K3 | note: successor to TicketGenerationConfigExtensionsTests; needs no ScriptableObject scaffolding now that the settings are plain data | h:73366a0c
STALE ExpoTheExplorer/Assets/Tests/EditMode/TicketGenerationConfigCloneForDayGenerationTests.cs | EditMode tests: per-Day generation clone applies all five values, leaves the shared asset untouched, and turns a null weight list into an empty one | sys: TicketSystem | api: SetUp(); TearDown(); CloneForDayGeneration_AppliesEveryValue(); CloneForDayGeneration_ReturnsDistinctInstance_DoesNotMutateBase(); CloneForDayGeneration_NullWeights_BecomesEmptyList() | dep: FoodItemConfig,MainDishWeight,TicketGenerationConfig | used: - | crit: K3 | note: successor to TicketGenerationConfigCloneWithOverridesTests; the old "no overrides given inherits the base" case is gone with the override layer (D-006) | h:922628dd
ExpoTheExplorer/Assets/Scripts/Core/AssemblyInfo.cs | friend-assembly grants for Core's internals | sys: ? | api: InternalsVisibleTo ExpoTheExplorer.Systems.ProgressionSystem; InternalsVisibleTo ExpoTheExplorer.Tests.EditMode | dep: - | used: - | crit: K1 | note: shared Core metadata with no owning system, same call as GameState/GameConfig. This file IS the single-writer enforcement for SoftMoney/Gems -- adding a name here hands out a second writer and is an architectural decision, not a build fix. dep? draft (GameState/GameStateTests/Wallet) was comment-only: the attributes take assembly-name STRINGS, so this file has no code dependency at all | h:93e9982b
ExpoTheExplorer/Assets/Scripts/Systems/ProgressionSystem/Wallet.cs | the only writer of SoftMoney/Gems | sys: ProgressionSystem | api: ApplyPersistedBalances(int softMoney, int gems); EarnSoftMoney(int amount); EarnGems(int amount); TrySpendSoftMoney(int cost); TrySpendGems(int cost); CaptureDayStart(); RevertToDayStart() | dep: GameState | used: GameManager,LivesManager,WalletTests | crit: K1 | note: holds no balance of its own -- GameState keeps the values and publishes the change events, this owns the rules. Adım 2 added the per-day spend ledger and made RevertToDayStart enforce max(0, dayStartBalance - spentThisDay) on BOTH currencies (decision D1): earnings taken back, spending never refunded, never negative. Snapshots and ledger reset only in CaptureDayStart (day start / day advance), never on a retry, so repeated retries of one day all measure against that day's baseline. Adım 4 added ApplyPersistedBalances: the LOAD path goes through here too, since GameManager cannot assign a balance, and it re-captures the day-start snapshot so the session's first retry reverts to the restored wallet rather than to 0; negatives from a hand-edited save file clamp to 0. dep? draft's GameManager/LivesManager were comment-only edges -- they call INTO this, they are not dependencies of it. D-056 added EarnGems, the currency's FIRST earn path -- until then Gems could only go down (paid Continues spent them and nothing put them back). One-directional like EarnSoftMoney, so it cannot become a second spending route around TrySpendGems' check, which also makes a 0-star day a no-op instead of a caller-side case. Deliberately NOT in the spend ledger: gems earned during a day are taken back wholesale by RevertToDayStart's day-start snapshot, and that is exactly what stops a finished day being replayed for gems over and over | h:6c3a68fd
ExpoTheExplorer/Assets/Tests/EditMode/WalletTests.cs | EditMode tests: single-writer wallet rules | sys: ProgressionSystem | api: SetUp(); TearDown(); EarnSoftMoney_AddsToBalance(); EarnSoftMoney_WithZeroOrNegative_ChangesNothing(); TrySpendSoftMoney_WithEnough_DeductsAndReturnsTrue(); TrySpendSoftMoney_WithoutEnough_SpendsNothingAndReturnsFalse(); TrySpendSoftMoney_WithExactBalance_Succeeds(); TrySpendGems_WithEnough_DeductsAndReturnsTrue(); TrySpendGems_WithoutEnough_SpendsNothingAndReturnsFalse(); RevertToDayStart_ReturnsSoftMoneyToTheCapturedBalance(); Constructor_CapturesDayStart_SoRevertWorksWithoutAnExplicitCapture(); RevertToDayStart_DoesNotRefundGemsSpentThisDay(); RevertToDayStart_TakesBackWhatTheDayEarned(); RevertToDayStart_DoesNotRefundSoftMoneySpentThisDay(); RevertToDayStart_WhenContinueWasFundedByTheDaysEarnings_ClampsAtZero(); RevertToDayStart_AcrossTwoRetriesOfTheSameDay_KeepsBothContinuesSpent(); CaptureDayStart_ClearsTheSpendLedger(); RevertToDayStart_WithNoActivity_LeavesBothBalancesAlone(); ApplyPersistedBalances_SeedsBothBalances(); ApplyPersistedBalances_ClampsNegativesToZero(); ApplyPersistedBalances_ReCapturesDayStart_SoTheFirstRetryDoesNotWipeTheWallet(); WalletWrites_StillPublishGameStateChangeEvents(); EarnGems_AddsToBalance(); EarnGems_WithZeroOrNegative_ChangesNothing(); EarnGems_PublishesTheGemsChangedEvent(); RevertToDayStart_TakesBackGemsEarnedThisDay(); RevertToDayStart_WhenAContinueWasFundedByTheDaysGems_ClampsAtZero(); CaptureDayStart_AfterEarningGems_MakesThemTheNewBaseline() | dep: GameConfig,GameState,Wallet | used: - | crit: K3 | note: the spend/earn cases lock in Adım 1's behaviour-neutral move; the revert cases are Adım 2's atomic-day rule and change behaviour on purpose. The Gems case keeps the SAME expected number across both steps (Gems have no earn path, so 'untouched' and 'spending not refunded' are indistinguishable by balance) -- only its name and reason changed. Seeds balances by direct GameState assignment, which only the test assembly may do -- that used to be the ONLY way to put Gems on the books, and the old note saying Gems have no earn path is now history: D-056's EarnGems is one, and the six cases at the bottom cover it, including the two halves of the atomic-day rule reaching the new currency (earnings taken back, a Continue funded by the day's own gems still spent and clamped at 0) and the baseline case that keeps a banked day's gems safe from the NEXT day's revert | h:18ec63ca
ExpoTheExplorer/Assets/Scripts/Systems/MetaSystem/MetaLayout.cs | where a prop stands and how big it is | sys: MetaSystem | api: PropScale(float backgroundWidth, Sprite backgroundSprite); PropSize(MetaItemDefinition item, float scale); PropRect(MetaItemDefinition item, Vector2 areaSize, float scale) | dep: MetaItemDefinition | used: MetaGroundsView,MetaBackdropView | crit: K2 | note: D-050 extracted this from MetaGroundsView the moment a SECOND thing began drawing the same prop (the day scene's backdrop). MetaGroundsView's own comment had already stated the rule -- every place that draws a prop must compute it identically or a preview lies about where the thing will land -- back when there was one such place; two places meant the arithmetic moved rather than being copied. Pure functions, no Unity objects, no side effects, so it can be reasoned about without a scene. PropScale is a FUNCTION rather than catalog data on purpose: D-015 keeps a prop's size derived from how wide the background is drawn, so re-exporting art at another resolution moves nothing. It guards a zero-width background sprite instead of dividing by it -- the symptom would be every prop at infinity, which reads as the props being gone and sends you to the catalog instead of to the art. PropRect is the anchor rule spelled out for a medium with NO anchors (compositing into a texture); MetaGroundsView deliberately does NOT call it, because Unity's anchors implement the same rule better -- an anchored prop rides the background through every resize -- and that anchored version stays the reference if the two ever disagree. Returned in the catalog's own bottom-up space; the flip to a top-left drawing surface belongs to the caller, because one of the two conventions has to be the one this file speaks and the catalog's is the one that cannot change. NOTE no test pins PropRect against a real RectTransform yet; that would be the mechanical guarantee and it fell outside the approved manifest -- carried to the user. | h:61eb1808
ExpoTheExplorer/Assets/Scripts/Systems/ProgressionSystem/DayRewardPurse.cs | what a completed day owes but has not handed over | sys: ProgressionSystem | api: SoftMoneyRemaining; GemsRemaining; IsEmpty; TakeSoftMoney(int amount); TakeGems(int amount); TakeAllSoftMoney(); TakeAllGems(); ShareOf(int total, int index, int count) | dep: - | used: GameManager,DayRewardFlightView,DayRewardPurseTests | crit: K2 | note: D-057. Holds a DEBT that has never been in the wallet, which is the only reason it is safe next to the single-writer rule: Wallet still owns both balances and every Take here is paired with exactly one Earn there by GameManager, the class that holds both. Every Take returns what was ACTUALLY taken, so a miscounted animation can underpay but can never mint money the day did not make. ShareOf differences two running totals instead of dividing and patching the last share -- the shares then sum to exactly the total with no drift, and the version that breaks when the sequence is skipped halfway is the one it avoids; widened to long first, because total x index overflows int once a config is tuned up and an overflow would be a negative payout. Plain C# with no Unity dependency, so the arithmetic deciding what each flying coin is worth is testable -- the alternative was working it out inside a coroutine, which is the one place this project cannot run a test | h:c9891b89
ExpoTheExplorer/Assets/Tests/EditMode/DayRewardPurseTests.cs | EditMode tests: the deferred payout's arithmetic | sys: ProgressionSystem | api: Constructor_ExposesWhatTheDayOwes(); Constructor_ClampsNegativeDebtsToZero(); TakeSoftMoney_DeductsAndReturnsWhatWasTaken(); TakeSoftMoney_MoreThanIsOwed_TakesOnlyWhatIsLeft(); TakeSoftMoney_WithZeroOrNegative_TakesNothing(); TakeGems_BehavesLikeTheOtherCurrency(); TakingOneCurrency_LeavesTheOtherAlone(); TakeAll_EmptiesThePurseAndReportsTheRemainder(); TakeAll_OnAnAlreadyEmptyPurse_ReportsNothingCredited(); ShareOf_SharesAlwaysSumToTheTotal(int total, int count); ShareOf_SplitsAsEvenlyAsIntegersAllow(); ShareOf_WithNothingToSplitOrNonsenseArguments_IsZero(); ShareOf_WithALargeTotal_DoesNotOverflowIntoANegativeShare(); AFullHandover_PaysExactlyTheDebtAndEmptiesThePurse() | dep: DayRewardPurse | used: - | crit: K3 | note: D-057. Covers the two properties that matter more than any single case: a Take can never hand out more than the day earned (so a miscounted animation underpays instead of minting), and a split's shares always sum back to exactly the total (so no coin is lost to rounding). Tested here rather than in place because the only other home for this arithmetic was a coroutine in a MonoBehaviour -- the one part of the project an EditMode test cannot reach -- and what it computes is how much real money each flying coin carries. The large-total case is the guard on the long widening: an int overflow would not throw, it would pay out a negative share | h:6a3dc2aa
ExpoTheExplorer/Assets/Data/DataScripts/StarScoreConfig.cs | the day's star thresholds + the two failure penalties | sys: DayLifecycle | api: ThreeStarScore; TwoStarScore; WrongDeliveryPenalty; TimeoutPenalty | dep: - | used: GameManager,DayLifecycleManager,DayLifecycleManagerTests | crit: K2 | note: added by D-060. The star RULE stays in code (DayLifecycleManager.StarCount, testable); these four numbers are content, so they cannot. Its own asset rather than a header on GameConfig: GameConfig holds GemsPerStar -- what a star is WORTH once earned -- and a star curve retuned dozens of times during balancing should not share an asset with the board dimensions that must never move. Every value is a fraction of the DAY'S OWN ticket clock, which is what makes one set of numbers work for a 5-ticket day and a 10-ticket one. The two penalties are deliberately unequal; see the file's own comment for why. Starting estimates, NOT balanced | h:022b3268
ExpoTheExplorer/Assets/Data/DataScripts/HapticConfig.cs | which moment plays which haptic | sys: HapticsSystem | api: HapticMoment{ItemPickup,ItemDroppedInTray,OrderDelivered,LifeLost,GameOver,StarSeated,PropPurchased,PropLanded}; HapticPreset{None..SoftImpact}; HapticConfig.Entry(moment; preset; priority; enabled); TryResolve(HapticMoment moment, out HapticPreset preset, out int priority) | dep: - | used: HapticsService,HapticsBinder,BoardItemDragHandler,DayRewardFlightView,MetaShopView,MetaGroundsView,HapticsSystemTests | crit: K3 | note: the script's dep? draft named HapticPatterns/HapticsBinder/HapticsService/PresetType -- all four are COMMENT mentions, not references; this file compiles against UnityEngine alone, which is the whole point. HapticPreset is a deliberate MIRROR of Nice Vibrations' HapticPatterns.PresetType so that ExpoTheExplorer.Data keeps its empty reference list (decisions.md D-070); the translation lives in HapticsBinder. Two silences, both valid and both needed: `enabled: false` keeps an authored preset while muting it, and a row simply MISSING resolves to false as well -- which is why Reset() seeds all eight moments, since a missing row is silent and silence is the hardest failure to notice. Priorities are spaced by tens so a ninth moment can be slotted between two without renumbering D-071 adds seven moments (UiTap, CoinLanded, GemLanded, DropRejected, KeySpent, PurchaseRefused, BlockedByNoKeys), APPENDED to the enum rather than sorted in: Unity serializes an enum field by VALUE and every authored row stores one, so an insertion would silently re-point every later row at the wrong moment -- an asset that still loads, still validates, and plays the wrong haptic. It also gains an editor-only OnValidate that GROWS an existing asset with any missing moment at its default, touching nothing already there. Reset() only runs on a brand-new asset, so without it an asset made before a moment existed would carry no row for it, and a missing row is SILENT. Telling the author to hit Reset would work and would also throw away every preset they had tuned. Append-only is consistent with what this file already asks: silence a moment by unticking Enabled, not by deleting its row -- so a deleted row reads as an accident and comes back. Runtime meaning is unchanged, since OnValidate does not exist in a build. D-072 appends three more (KeysRefilled, ContinuePurchased, PropUnlocked), again at the END of the enum for the serialization reason above -- the asset is at fifteen rows by now, and every one of them stores a value. OnValidate grows it to eighteen on the next load. | h:bdc4d084
ExpoTheExplorer/Assets/Scripts/Bootstrap/HapticsBinder.cs | the scene's haptics surface, and the vendor boundary | sys: HapticsSystem | api: Request(HapticMoment moment) | dep: GameManager,GameState,HapticConfig,HapticsService,HapticPatterns | used: BoardView,BoardItemDragHandler,DayRewardFlightView,MetaShopView,MetaGroundsView | crit: K3 | note: THE ONLY FILE IN THE PROJECT THAT NAMES Lofelt.NiceVibrations (decisions.md D-070) -- swapping the vendor is a one-file change, and the HapticPreset -> PresetType switch is written out rather than cast so a renumbering upstream breaks the build instead of playing the wrong haptic. It lives in Bootstrap and NOT in the HapticsSystem asmdef for the mechanical reason the blueprint states: it needs GameManager, which has no asmdef and so lands in Assembly-CSharp, and an asmdef cannot reference a predefined assembly. ONE COMPONENT, TWO MODES: with gameManager wired (day scene) it subscribes to TicketDelivered/LivesChanged/LivesDepleted; left EMPTY (main screen, which has no GameState at all) it is only a play surface the shop calls directly -- an empty field there is a valid setup, which is why it is not validated. Start not Awake, same reason as every other view here. Life loss is read from LivesChanged's DROP, so a refill or a paid Continue never buzzes. D-094 added an OPTIONAL sessionHost and the player's mute: checked in LateUpdate AFTER TryTakePending, which is what makes it a mute rather than a second set of rules -- every request still reaches the service, so coalescing and priority are identical either way, and no stale moment is left pending to fire when the switch comes back on. FAILS ON (unwired host, or a session not built yet): always-on haptics are a nuisance, a silently unfeelable game looks like the vendor being broken on that device | h:8eef981b
ExpoTheExplorer/Assets/Scripts/Systems/HapticsSystem/ExpoTheExplorer.Systems.HapticsSystem.asmdef | the haptics assembly and what it can see | sys: HapticsSystem | api: asmdef ExpoTheExplorer.Systems.HapticsSystem | dep: ExpoTheExplorer.Data | used: ExpoTheExplorer.Tests.EditMode | crit: K3 | note: ONE reference, and the two absences are the design. No Lofelt.NiceVibrations: the vendor call lives in HapticsBinder, outside this assembly, so nothing here compiles against a haptics library. No ExpoTheExplorer.Core either -- the preflight expected one and the code turned out not to need it, since the service resolves moments and never touches GameState. Kept as narrow as it actually is rather than as wide as it was planned | h:bd39f0a6
ExpoTheExplorer/Assets/Scripts/Systems/HapticsSystem/HapticsService.cs | picks the frame's winning haptic | sys: HapticsSystem | api: HapticsService(HapticConfig config); Request(HapticMoment moment); TryTakePending(out HapticPreset preset) | dep: HapticConfig | used: HapticsBinder,HapticsSystemTests | crit: K3 | note: the dep? draft's EventBus and HapticsBinder are COMMENT mentions; the only real reference is HapticConfig. It never PLAYS anything -- playback is HapticsBinder's, which is exactly what keeps this class plain C# and reachable from EditMode with no device. Its one rule: at most one haptic per frame, highest priority wins, ties to whoever asked FIRST (`>=`, deliberately) because the earlier request is the one closer to what the player did. That is a correctness fix, not tuning: EventBus.Publish is synchronous and cascades, so a correct drop queues ItemDroppedInTray then OrderDelivered, and the last life queues LifeLost then GameOver. Draining is the CALLER's job so that "once per frame" lives at the one place that knows what a frame is. A null config makes Request a no-op rather than a throw -- an unwired reference should cost a haptic, never a frame | h:f0ab94d6
ExpoTheExplorer/Assets/Tests/EditMode/HapticsSystemTests.cs | pins the one-haptic-per-frame rule | sys: HapticsSystem | api: AResolvedMoment_IsTakenOnce_AndTheQueueIsThenEmpty(); NothingRequested_TakesNothing(); WithinOneFrame_TheHighestPriorityWins_WhicheverArrivedFirst(); TheLastLife_IsFeltAsGameOver_NotAsALifeLost(); EqualPriority_GoesToWhoeverAskedFirst(); APreviousFramesWinner_DoesNotOutrankTheNextFrame(); ADisabledMoment_IsSilent_AndDoesNotBlockAnotherMoment(); ANonePreset_IsSilent_LikeADisabledRow(); AMomentMissingFromTheTable_IsSilent(); WithNoConfigAtAll_RequestsAreDropped_NotThrown(); TheDefaultTable_CoversEveryMoment() | dep: HapticConfig,HapticsService | used: - | crit: K3 | note: the dep? draft's HapticsBinder and LivesManager are COMMENT mentions -- neither is reachable from here, HapticsBinder because Assembly-CSharp cannot be referenced by an asmdef. Each test authors its OWN table through SerializedObject rather than leaning on Reset(), so a retune of the shipped presets cannot break the suite; the one test that does read the defaults asserts only that every moment HAS a row, never which preset it got. The two cascades pinned here are the real ones from the game: drop-then-deliver, and life-lost-then-game-over | h:5d830551
ExpoTheExplorer/Assets/Data/DataScripts/PowerupType.cs | the three GDD 5.2 powerups + their iteration surface | sys: PowerupSystem | api: enum PowerupType {AutoCollect,TimeReset,NoiseClear}; PowerupTypes.All; PowerupTypes.Count | dep: - | used: PowerupConfig,PowerupManager,GameSession,PowerupSystemTests | crit: K2 | note: the NUMBERS are load-bearing -- PowerupManager indexes its charge and effect arrays by (int)type, so a renumbering silently hands each powerup another's charges. PowerupSystemTests pins all three. The SAVE FILE is deliberately immune (PlayerProfile carries a named int per type), which is exactly why nothing else would catch it. Lives in Data rather than the system assembly because PowerupConfig has to name it and Data references nothing | h:581fde68
ExpoTheExplorer/Assets/Data/DataScripts/PowerupConfig.cs | every number behind GDD 5.2: per-type stock / Gem price / per-day grant | sys: PowerupSystem | api: PowerupSettings.DisplayName; PowerupSettings.Description; For(PowerupType type); PowerupSettings(int startingCharges, int gemCost, int chargesPerDayCompleted); StartingCharges; GemCost; ChargesPerDayCompleted | dep: PowerupType | used: PowerupManager,GameManager,MainScreenRoot,GameSession,MainScreenSessionSetup,PowerupSystemTests | crit: K3 | note: D-087 (2026-08-26) each powerup gained a display name and a one-line description, and they live HERE rather than in the tutorial that first shows them because they describe the POWERUP, not the moment it is explained. The main-screen shop hand-labels its three rows in the scene today and can read these two strings instead, rather than keeping a second copy free to disagree. values are PLACEHOLDERS (LivesConfig's shape), not authored balance -- unlike KeyConfig, whose defaults are the user's real numbers. Three named blocks rather than an array indexed by the enum: an array shows up as Element 0/1/2 with no way to tell which powerup is which, and can be resized by a stray drag. For() is a written-out switch so a fourth PowerupType stops the build here. SUBJECT TO THE STALE-ASSET TRAP (D-075): re-serialize after adding a field | h:cd37a8ee
ExpoTheExplorer/Assets/Scripts/Systems/PowerupSystem/ExpoTheExplorer.Systems.PowerupSystem.asmdef | PowerupSystem assembly boundary | sys: PowerupSystem | api: asmdef ExpoTheExplorer.Systems.PowerupSystem | dep: ExpoTheExplorer.Core,ExpoTheExplorer.Data,ExpoTheExplorer.Systems.ProgressionSystem | used: ExpoTheExplorer.Session,ExpoTheExplorer.Tests.EditMode | crit: K2 | note: identical reference set to the KeySystem and LivesSystem asmdefs and for the identical reason -- the Gem purchase is charged through Wallet. It names NO other system on purpose: the three effects would need TraySystem, TicketSystem and BoardUI, so they arrive as injected delegates instead (blueprint.md) | h:5477bceb
ExpoTheExplorer/Assets/Scripts/Systems/PowerupSystem/PowerupManager.cs | powerup charges: hold, earn on day complete, buy with Gems, spend | sys: PowerupSystem | api: PowerupManager(PowerupConfig config, Wallet wallet); ChargesChanged; ChargesOf(PowerupType type); GemCostOf(PowerupType type); RegisterEffect(PowerupType type, Func<bool> effect); ApplyPersisted(int autoCollectCharges, int timeResetCharges, int noiseClearCharges); CanUse(PowerupType type); TryUse(PowerupType type); TryBuyWithGems(PowerupType type); DebugGrant(PowerupType type, int amount) [dev-only]; GrantForDayCompleted() | dep: EventBus,PowerupConfig,PowerupType,PowerupTypes,Wallet | used: GameSession,GameManager,PowerupSystemTests | crit: K2 | note: SINGLE WRITER of the counts, held in a private array rather than on GameState -- KeySystem's departure from LivesSystem, same two reasons. It does not know what a powerup DOES: effects are Func<bool> delegates, and a null one is how a scene with no board says so, which is what makes a charge unspendable on the main screen. The bool is a RULE (GDD 5.2): false means nothing to do, so no charge. TryUse deducts AFTER the effect so a wasted press never publishes a count it has to walk back; the cost is one re-entrancy case (a day-completion grant landing inside an effect's synchronous cascade), which nets out because both sides are +/-1 on the same field D-092 added DebugGrant, dev-only. It is deliberately NOT ApplyPersisted: that method assigns the three counts and publishes NOTHING (at load time no view is subscribed yet), so reusing it for a cheat would set the numbers correctly and leave the HUD showing the old ones -- the cheat would look broken while working perfectly. Publishing is the entire difference. Floors at zero so a negative stock cannot need N presses before the first visible effect. | h:d827bea0
ExpoTheExplorer/Assets/Tests/EditMode/PowerupSystemTests.cs | EditMode tests: powerup stock, the -1 marker, wasted presses, purchase, grant, save round-trip | sys: PowerupSystem | api: SetUp(); TearDown(); PowerupType_NumbersAreThePositionsTheManagerIndexesBy(); PowerupTypes_All_ListsEveryTypeOnceInEnumOrder(); NewManager_OpensOnTheAuthoredStartingStock(); GemCostOf_ReadsTheAuthoredPricePerType(); ApplyPersisted_NegativeMarker_ResolvesToTheAuthoredStartingStock(); ApplyPersisted_AnyNegative_IsTreatedAsAbsent(); ApplyPersisted_Zero_IsARealCountAndIsKept(); ApplyPersisted_SavedCounts_AreUsedAsIs(); TryUse_WithNoEffectRegistered_RefusesAndSpendsNothing(); CanUse_IsFalseWithoutAnEffect_EvenHoldingCharges(); TryUse_WhenTheEffectRuns_SpendsExactlyOneAndPublishesTheNewCount(); TryUse_WhenTheEffectReportsNothingToDo_SpendsNothing(); TryUse_AtZeroCharges_NeverRunsTheEffect(); TryUse_SpendsOnlyTheTypeThatWasPressed(); TryUse_WhenADayCompletionGrantLandsInsideTheEffect_NetsOutExactly(); RegisterEffect_Twice_ReplacesRatherThanStacks(); TryBuyWithGems_Affordable_AddsOneChargeAndChargesTheWallet(); TryBuyWithGems_Unaffordable_ChangesNothingAtAll(); TryBuyWithGems_HasNoStockCeiling(); GrantForDayCompleted_AddsTheAuthoredAmountPerType(); GrantForDayCompleted_WithZeroAuthored_GrantsNothingAndPublishesNothingForThatType(); Profile_FromBeforePowerupsExisted_OpensOnTheStartingStock(); Profile_FromTheRenamedVersion_ResolvesOnlyTheRenamedFieldAndKeepsTheRest(); Profile_AlreadyCurrent_KeepsItsOwnCountsIncludingZero(); Session_SaveThenLoad_RoundTripsEveryCharge(); Session_WithNoPowerupConfig_HasNoManager(); Session_WithNoPowerupConfig_WritesBackTheChargesItLoaded() | dep: GameConfig,GameSession,GameState,PlayerProfileStore,PowerupConfig,PowerupManager,PowerupType,PowerupTypes,Wallet | used: - | crit: K2 | note: the two suites that earn their length guard SILENT failures. The -1 marker: read as a plain 0 an existing player launches owning nothing and the screen says nothing -- the fourth field in this save schema where zero is a real value rather than an absence (Lives v3, Keys v7). The wasted press: a powerup that takes a charge for doing nothing is not a crash, it is a player being robbed quietly. Config values are set through SerializedObject with a BLOCK-PREFIXED path (autoCollect.startingCharges), so the strings must track the nested field names | h:bcf9ce7f
ExpoTheExplorer/Assets/Scripts/Systems/PowerupSystem/PowerupEffects.cs | what the powerups DO: reset timers, clear the board, choose the next auto-collect item | sys: PowerupSystem | api: ResetActiveTicketTimers(GameState state); ClearUnneededItems(GameState state); TryFindAutoCollectItem(GameState state, int slotIndex, IReadOnlyList<BoardItem> trayContents, out int x, out int y) | dep: BoardGrid,BoardItem,GameState,RequiredItemKey,Ticket,TicketRequirements,TicketState | used: GameManager,PowerupEffectsTests | crit: K2 | note: powerup-plan Adım 4. THE RETURN BOOL IS THE RULE, not a status code: GDD 5.2 says a powerup with no work to do costs no charge, and PowerupManager.TryUse only deducts on true -- so false means 'nothing to reset' and the player keeps the charge. Static, no interface and no strategy class: there are exactly three of these, the set is fixed by design rather than by content, and each is a few lines over existing state. THIRD WRITER of Ticket.RemainingSeconds and the boundary is deliberate -- the constructor opens it and TicketSlotManager.Tick spends it (both 'time moves forward'), this is the only place it moves BACK; routing it through TicketSlotManager to keep one writer was rejected because that class would have to learn what a powerup is. Restores each ticket to ITS OWN authored limit, never a flat bonus, or a powerup would quietly rewrite per-Day patience balancing. Skips non-Active tickets because a resolved one can still sit in the array for the rest of the cascade that resolved it. ClearUnneededItems (Adım 5) removes every board item no ACTIVE ticket wants -- instant and permanent, NO UI half at all because BoardGrid.RemoveItem publishes CellChanged and BoardView hides the cell itself. BY IDENTITY, NOT BY COUNT: a ticket wanting one burger protects every burger. Tray contents are deliberately not consulted (a wrong order scatters the tray straight back, and reading it would drag a TraySystem reference in). IT CANNOT BREAK THE GUARANTEED-TICKET RULE for two independent reasons, both pinned by tests: everything an active ticket requires is kept, and BoardDistributor caches nothing so a recount re-spawns any gap on the next order. The cell is re-read IMMEDIATELY before removal because RemoveItem BACKFILLS from the pending-spawn queue -- collecting coordinates first and deleting later would delete whatever landed in between. TryFindAutoCollectItem (Adım 6) is only the DECISION half of Auto-Collect -- the moving lives in AutoCollectRunner (UI), because a tray is drawn by the dragged object being reparented and that path is a MonoBehaviour no EditMode test can reach; pulling the choosing in here is what makes the powerup's rules testable at all. Outstanding = required multiset MINUS the tray, so COUNTS matter unlike in the clear. A tray item the ticket does NOT want decrements nothing -- that tray is already going to cost a life and treating the mistake as progress would have the powerup finish it. It can never produce a wrong order, because only items the ticket is still short of are offered; pinned by a test rather than trusted | h:7baa94b1
ExpoTheExplorer/Assets/Tests/EditMode/PowerupEffectsTests.cs | EditMode tests: all three powerups' rules, the no-wasted-press rule, the guaranteed-ticket rule | sys: PowerupSystem | api: SetUp(); TearDown(); ResetActiveTicketTimers_RestoresEachTicketToItsOwnAuthoredLimit(); ResetActiveTicketTimers_WhenEveryTicketIsAlreadyFull_ReportsNoWork(); ResetActiveTicketTimers_WithNoTicketsAtAll_ReportsNoWork(); ResetActiveTicketTimers_WithOnePartialTicket_DoesTheWorkAndLeavesFullOnesAlone(); ResetActiveTicketTimers_IgnoresDeliveredAndCancelledTickets(); ResetActiveTicketTimers_ResetsTheActiveTicketBesideAResolvedOne(); ResetActiveTicketTimers_WithNoState_ReportsNoWorkInsteadOfThrowing(); AfterAReset_TheTickerResumesFromTheRestoredValue(); ClearUnneededItems_KeepsEverySurplusCopyOfAWantedFood(); ClearUnneededItems_RemovesWhatNoActiveTicketWants(); ClearUnneededItems_TreatsADifferentModificationComboAsADifferentItem(); ClearUnneededItems_OnAnEmptyBoard_ReportsNoWork(); ClearUnneededItems_DoesNotProtectWhatOnlyAResolvedTicketWanted(); ClearUnneededItems_DoesNotDeleteAnItemTheQueueBackfilledIntoAClearedCell(); ClearUnneededItems_LeavesEveryItemAGuaranteedActiveTicketNeeds(); AfterAClear_TheDistributorRestoresWhatAGuaranteedTicketIsMissing(); TryFindAutoCollectItem_FindsAnItemTheTicketStillNeeds(); TryFindAutoCollectItem_SubtractsWhatTheTrayAlreadyHolds(); TryFindAutoCollectItem_DoesNotLetAnUnwantedTrayItemSatisfyARequirement(); TryFindAutoCollectItem_NeverOffersAnItemTheTicketDoesNotWant(); TryFindAutoCollectItem_DoesNotOfferAPlainItemForAModifiedOrder(); TryFindAutoCollectItem_OnAnEmptySlot_FindsNothing(); TryFindAutoCollectItem_OnAResolvedTicket_FindsNothing(); TryFindAutoCollectItem_WithAnOutOfRangeSlot_FindsNothingInsteadOfThrowing() | dep: BoardDistributionSettings,BoardDistributor,BoardItem,FoodCategory,FoodItemConfig,GameConfig,GameState,GuaranteedTicketCountMode,Modification,ModificationConfig,PatienceType,PowerupEffects,Ticket,TicketState | used: - | crit: K2 | note: split from PowerupSystemTests because the two answer different questions and share no setup (tickets here, wallet and save file there). The case that earns the file is the no-wasted-press rule: it CANNOT be tested from the manager's side, which only ever sees a bool -- whether that bool is honest is a property of the effect, and one that returned true unconditionally would take a charge for nothing while every stock test still passed. Adım 5 added the two GUARANTEED-TICKET cases the user asked for, and they are the reason this file references BoardDistribution: the answer to 'can the clear strand a day' is now a test rather than an argument. The backfill case is the other one worth keeping: it only fails on a FULL board, which is exactly where a coordinates-first implementation would look correct | h:53773d68
ExpoTheExplorer/Assets/Scripts/Core/TicketRequirements.cs | the single answer to what a ticket wants, as RequiredItemKeys | sys: TicketSystem | api: KeyFor(Ticket ticket, FoodItemConfig food); RequiredCounts(Ticket ticket); ActiveTicketKeys(GameState state) | dep: FoodCategory,FoodItemConfig,GameState,Modification,RequiredItemKey,Ticket,TicketState | used: TraySlot,PowerupEffects,TicketRequirementsTests | crit: K2 | note: extracted in powerup-plan Adım 5 with three consumers (TraySlot.Matches, Noise Clear, Adım 6's auto-collect). The rule it centralises is NOT cosmetic -- modifications count for the Main dish only, and that decides whether a delivered tray is correct, so a second copy is the kind that survives until one is fixed and a good order starts costing a life. RequiredCounts returns a DICTIONARY because the delivery check needs counts (two colas are not one); ActiveTicketKeys returns a SET because Noise Clear asks 'does any ticket want this at all' -- which is the by-identity-not-by-count rule in a type choice. KNOWN REMAINING DUPLICATE, recorded rather than hidden: BoardDistributor spells the same rule out twice more (SpawnMissingRequiredItems, LeakNoiseItems) and is outside this task's manifest | h:615c7a20
ExpoTheExplorer/Assets/Tests/EditMode/TicketRequirementsTests.cs | EditMode tests: the Main-only modification rule, counts, the active-ticket set | sys: TicketSystem | api: TearDown(); KeyFor_AttachesTheTicketsModificationsToTheMainDishOnly(); KeyFor_TheSameFoodOnTheSameTicket_ProducesEqualKeys(); KeyFor_TheSameFoodWithDifferentModifications_ProducesDifferentKeys(); RequiredCounts_CountsDuplicatesRatherThanCollapsingThem(); RequiredCounts_WithNoTicket_IsEmptyRatherThanNull(); ActiveTicketKeys_CollapsesDuplicatesAcrossSlots(); ActiveTicketKeys_IgnoresDeliveredAndCancelledTickets(); ActiveTicketKeys_WithNoState_IsEmptyRatherThanNull() | dep: FoodCategory,FoodItemConfig,GameConfig,GameState,Modification,ModificationConfig,PatienceType,Ticket,TicketRequirements,TicketState | used: - | crit: K2 | note: TraySystemTests already covers the OUTCOME end to end (a wrong delivery costs a life); this file covers the RULE, so a break points at the rule rather than at the tray. A wrong answer here does not crash -- it makes a correct delivery cost a life or lets a wrong one through | h:066821a1
ExpoTheExplorer/Assets/Scripts/Systems/Tutorial/ExpoTheExplorer.Systems.Tutorial.asmdef | asmdef ExpoTheExplorer.Systems.Tutorial | sys: Tutorial | api: asmdef ExpoTheExplorer.Systems.Tutorial | dep: - | used: ExpoTheExplorer.Tests.EditMode,Assembly-CSharp | crit: K3 | note: AN EMPTY REFERENCE LIST IS THE POINT, not an oversight -- in this project that is how "depends on nothing" is said (same as ExpoTheExplorer.Data). It is what makes every rule in TutorialDirector reachable from a test with no MonoBehaviour, no scene and no Day catalog. Assembly-CSharp reaches it without an entry anywhere because a PREDEFINED assembly auto-references every autoReferenced asmdef; the test assembly does NOT get that for free and had to be edited by hand in the same task -- the trap the Testing blueprint entry exists to make visible | h:9df136b3
STALE ExpoTheExplorer/Assets/Scripts/Systems/Tutorial/TutorialDirector.cs | the forced opening as rules: a list of steps, each one cell and one tray | sys: Tutorial | api: Current; IsActive; StepChanged; Abort(); IsPickupAllowed(int x, int y); IsTrayDropAllowed(int slotIndex); IsBoardRelocationAllowed(); IsHoldingForReading; SpotlightTraySlotIndex; NotifyTrayAccepted(int slotIndex); NotifyReadingFinished() | dep: - | used: GameManager,WorldTrayView,TutorialSpotlightView,PowerupBarView,TutorialDirectorTests | crit: K2 | note: D-098 (2026-08-26) added SpotlightTraySlotIndex, which answers "which tray does this step point at" as -1 for a step that names none. It exists because of a trap in TutorialStep's shape: a PowerupIntro step leaves the cell and tray fields UNSET, and unset is 0 -- so WorldTrayView, the one reader that compared TargetTraySlotIndex without asking the Kind first, had tray 0 raise a ghost from cell (0,0) over the powerup panel. Every other reader (GameManager, DayValidator) already gated on Kind; this closes the last one and puts the answer in the step list rather than in a view. D-087 (2026-08-26) gained a step KIND and its own mirror of the enum -- a mirror rather than a shared type for the reason TutorialStep itself is one: referencing DaySystem would be the first entry on this assembly's empty reference list. An intro step refuses ALL THREE gates rather than falling back on its unused cell and tray fields, because anything reachable behind a panel is a way to lose a life while reading. IsHoldingForReading was the day clock's question until D-097 (2026-08-26) handed the clock the whole tutorial (GameManager gates on IsActive, not on the kind); it is now read only by PowerupBarView, which builds and tears down the intro panel from it, and it stays a property of the step list rather than a Kind comparison at that call site. NotifyReadingFinished is deliberately separate from NotifyTrayAccepted: neither kind of step can be completed by the other's gesture. D-083 (2026-08-26) became a step MACHINE (a list and an index -- still no interface, no state machine class, no per-step subclass). Completed was replaced by StepChanged because with more than one step the interesting moment is the BOUNDARY, not only the end; it fires for the end and for Abort too, which is how the trays know to tear a spotlight down. TutorialStep is this system's OWN type rather than DaySystem's ResolvedTutorialStep: taking that one would have put the first entry on this assembly's empty reference list, and GameManager translates at the boundary instead. Abort() exists because a later step's item can be gone by the time its turn comes, and the alternative is a player facing a board that refuses every touch. THE AUTO-DRAFTED DEPENDENCY FIELD ON THIS LINE WAS WRONG -- it guessed BoardItem, EventBus, Ticket and TrayManager from words in the comments, and the truth is none of them: three ints come in, two questions and a completion go out. No BoardItem, no Ticket, no tray contents. Both gates answer TRUE once the step is done, so a finished tutorial and a Day that never had one are indistinguishable to a caller -- that is what keeps the two call sites reading as ordinary permission checks instead of tutorial special cases. NotifyTrayAccepted re-checks the slot even though the drop gate already did, so a future caller that forgets the gate cannot finish the step by dropping in the wrong tray, and it is idempotent so a multi-item order does not advance the step twice. K2 rather than K3: while it is active it is the only thing standing between the player and an unplayable board D-084 (2026-08-26) added the THIRD gate, IsBoardRelocationAllowed, which is false for the whole of a step. It closes a softlock rather than polishing anything: the pickup gate permits exactly one cell, so an item parked on any OTHER empty cell could never be picked up again and the step became uncompletable. It is deliberately its own question rather than IsPickupAllowed(destination) -- the two agree on today's content but ask different things, and one predicate serving both would be true by coincidence rather than by rule. | h:6ce0e575
STALE ExpoTheExplorer/Assets/Tests/EditMode/TutorialDirectorTests.cs | EditMode tests for the tutorial step machine and its four gates | sys: Tutorial | api: WhileAStepRuns_OnlyItsCellCanBePickedUp(); WhileAStepRuns_OnlyItsTrayAcceptsADrop(); FinishingAStep_MovesTheGatesToTheNextStep(); WhileAStepRuns_AnItemCannotBeParkedElsewhereOnTheBoard(); OnceEveryStepIsDone_ItemsCanBeMovedAroundTheBoardAgain(); Abort_AlsoReleasesTheBoard(); TheLastStep_CarriesItsAuthoredHints(); OnceEveryStepIsDone_EveryCellAndEveryTrayIsAllowedAgain(); StepChanged_FiresOncePerBoundaryAndNotAfterTheEnd(); AcceptanceOnAnotherTray_DoesNotAdvance(); Abort_EndsTheTutorialAndOpensEveryGate(); DuringAnIntroStep_TheWholeBoardIsRefused(); AnIntroStep_IsNotCompletedByATrayDrop(); AForcedMove_IsNotCompletedByDismissingAPanel(); DismissingThePanel_EndsTheTutorialAndReleasesEverything(); AForcedMove_IsNotAReadingStep(); AnIntroStep_PointsAtNoTray_ThoughItsUnsetFieldReadsAsTrayZero(); AForcedMove_PointsAtItsOwnTray_AndTheSpotlightFollowsTheBoundary(); AFinishedTutorial_PointsAtNoTray(); AnAbortedTutorial_PointsAtNoTray(); Abort_AfterTheTutorialIsOver_IsSilent() | dep: TutorialDirector | used: - | crit: K3 | note: D-098 (2026-08-26) pins the unset-field trap in four tests, and the first of them asserts the trap itself before the rule: a panel step's TargetTraySlotIndex really IS 0, and 0 really IS the tray day_00's first move uses, so the two are indistinguishable without the Kind -- an assertion of what is broken, kept next to the one that says it is fixed. AForcedMove_DoesNotStopTheClock was RENAMED to AForcedMove_IsNotAReadingStep in the same pass: after D-097 the clock holds for the whole tutorial, so the old name asserted the opposite of the shipped behaviour while its body stayed correct. D-087 (2026-08-26) added the intro-step half: the whole board refused while reading, the two completions kept separate (a tray drop must not skip an unread panel, a dismissal must not stand in for a move), and the clock stopping only for a reading step (widened to the whole tutorial by D-097). D-084 (2026-08-26) pinned the relocation gate in BOTH directions -- refused during a step, and released once the tutorial ends or aborts. The second half matters more than it looks: written backwards, that gate would lock every board in the game forever. D-083 (2026-08-26) rewritten for the step list, mirroring day_00's own two steps so the fixture and the shipped content cannot drift apart in shape. No GameConfig, no ScriptableObject, no scene and no Unity types at all -- which is the payoff of the empty reference list on this system's asmdef | h:5d663dfa
ExpoTheExplorer/Assets/Data/DataScripts/TutorialTextConfig.cs | the main screen tutorial's five strings | sys: Tutorial | api: WelcomeTitle; WelcomeBody; StoreHint; PlayBlockedHint; WelcomeDismissLabel; StoreHintDismissLabel | dep: - | used: MainScreenTutorialView,MainScreenTutorialSetup | crit: K3 | note: THE AUTO-DRAFTED DEPENDENCY FIELD GUESSED PowerupConfig, which this file does not name -- it is a plain ScriptableObject of strings and references nothing. An asset rather than fields on the view (D-089): the view is wired by a setup script now, so nobody would ever open it to edit those fields, and text is content that this project forbids embedding in code. THE DAY SCENE READS NONE OF THIS -- its tutorial words live per-Day in the Day JSON and on PowerupConfig, each beside the thing it describes; nothing about a welcome screen belongs to a Day. Five named strings rather than a table keyed by id, because there are five and a typo'd key would fail silently at the moment the tutorial is meant to speak. playBlockedHint (D-090) is deliberately NOT a reuse of storeHint: one is an invitation, the other answers "why did nothing happen when I pressed Play?" | h:5da03a2d
ExpoTheExplorer/Assets/Scripts/Debug/DebugMenuBinder.cs | the one scene->SRDebugger bridge, and the gesture that opens the panel | sys: DebugMenu | api: Host; Catalog | dep: SessionHost,GameSession,MetaCatalog,SROptions,SRDebug,Keyboard,Touchscreen | used: SROptions.Expo | crit: K3 | note: D-092. Serialized SessionHost + MetaCatalog, dragged by a human -- D-013 ruled out a runtime lookup for this binding and SROptions has no scene presence of its own to search from. Registers the HOST, never the session: SessionHost.Session is null until that host's own Awake runs and Unity does not order Awake across GameObjects, so caching the session here would be a per-load race; the panel dereferences at press time instead, which also picks up a session rebuilt since. OnEnable/OnDisable rather than Awake/OnDestroy so the outgoing scene releases before the incoming one registers. CARRIES ITS OWN OPENER because SRDebugger's shipped trigger is a tiny DPI-sized rect pinned to one screen CORNER -- tapping the middle of the screen three times does nothing, which reads as a broken install, and on a phone that corner is both hard to hit and where the game's own UI already sits. A whole-screen multi-finger touch cannot be missed and collides with nothing (no mechanic uses three fingers). The key path is MODIFIER-FREE on purpose: SRDebugger's shortcuts are Ctrl+Shift+F1..F4, so an unguarded F1 check would fire on both and toggle the panel open and shut in one frame -- which looks exactly like the shortcut doing nothing. Update() is frame-frequency but O(1) at the cheapest cost tier and compiled out of release builds | h:aa1d482a
ExpoTheExplorer/Assets/Scripts/Debug/SROptions.Expo.cs | the cheat/readout surface on SRDebugger's Options tab | sys: DebugMenu | api: Amount; AddCoins(); AddGems(); Charges; GrantAutoCollect(); GrantTimeReset(); GrantNoiseClear(); FillKeys(); EmptyKeys(); SpendKey(); RefillLives(); DayNumber; GoToDay(); NextDay(); RetryDay(); DeliverTicket1..3(); UnlockAllProps(); ResetOwnedProps(); SaveNow(); DeleteSaveFile(); SaveFilePath; Info* readouts | dep: DebugMenuBinder,GameSession,GameManager,Wallet,KeyManager,PowerupManager,LivesManager,TicketSlotManager,DayLifecycleManager,MetaCatalog,PlayerProfileStore,SceneFlow | used: - | crit: K3 | note: D-092. A partial half of SRDebugger's GLOBAL SROptions class, which is why the file has no namespace and Scripts/Debug/ has no asmdef -- a partial's halves must share an assembly and SRDebugger's half compiles into Assembly-CSharp. SRDebugger builds the panel by REFLECTING over public instance members, so every helper here is private/internal on purpose: making one public would put a junk row on screen. Commands owners, never assigns -- money via Wallet, keys via KeyManager, charges via PowerupManager, lives via LivesManager, day index via GameSession.GoToDay. AddCoins/AddGems re-take the day-start baseline and SAY SO in the log: without it the grant vanishes on the next retry (atomic-day contract), with it this day's legitimate earnings stop being revertible too. Go To Day is main-screen only and refuses inside a day rather than half-working, because leaving a running day is a wallet decision ReturnToMainScreenAbandoningDay already owns; it reloads MainScreen afterwards because MainScreenView reads the day once in Start and the meta views never subscribe. Replaces DebugTicketDeliveryController, whose keyboard-only shortcuts could not be pressed on a device D-095 gave DeleteSaveFile the scene RELOAD that made the old main-screen Start Over useful: the session, HUD, grounds and shop are all built from the profile in Awake/Start, so rebuilding the scene is the only path that resets each through its existing single writer. It reloads only on the main screen -- inside a day that would abandon a running attempt without settling its wallet, the same host test Go To Day makes. | h:132b87f5
