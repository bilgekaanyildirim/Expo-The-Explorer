<!-- stamp: 472a6ed 2026-08-27 status: DEGRADED 8 stale, 0 orphan, 2 missing-role -->
ExpoTheExplorer/Assets/Scripts/UI/BoardItemDragHandler.cs | one board item's pickup/drag/drop gesture, refusable per gesture | sys: BoardUI | api: CurrentItem; IsDragging; WasAcceptedByTray; Configure(BoardGrid board, Camera dragCamera, BoardView boardView, GameManager gameManager, DragFeelSettings dragFeel, BoardAnimationConfig animConfig, HapticsBinder haptics); SetCell(int x, int y, BoardItem item); OnPointerDown(PointerEventData eventData); OnPointerUp(PointerEventData eventData); OnBeginDrag(PointerEventData eventData); OnDrag(PointerEventData eventData); OnEndDrag(PointerEventData eventData); DetachFromBoard(); PlaceInSlot(Transform slotTransform, int slotIndex); PlaceInSlotAndDeliver(Transform slotTransform, int slotIndex, Action onSettled); ReleaseAndDestroy() | dep: BoardAnimationConfig,BoardGrid,BoardItem,BoardView,GameManager,WorldTrayView,HapticsBinder,HapticConfig | used: BoardView,WorldTrayView | crit: K2 | note: D-100 (2026-08-26) the drop that COMPLETES an order is seated in its slot like any other, by PlaceInSlotAndDeliver, and the tray's delivery animation runs on that settle tween's OnComplete -- the wait is the tween ITSELF rather than a DelayedCall on TraySettleDuration, so the flight and the hand-off cannot disagree about how long the item takes to arrive. PlayDeliverySuccessAndDestroy went with it: that method existed only because the delivering item was never parented, so it grew and lifted on its own transform in parallel with the tray; now that it IS a tray child, the tray's own sequence carries it and running the old one too would double both the grow and the lift. deliverySuccessInProgress outlives the deletion and does more than it used to -- it keeps OnEndDrag from re-enabling the collider during the settle, on a tray TrayManager has already emptied and handed to the next ticket, so the item cannot be picked back up while nothing owns it. D-084 (2026-08-26) put the tutorial in front of the board-to-board relocation branch too, which is where the first two gates left a hole big enough to strand the day: the pickup gate permits exactly ONE cell, so parking the item on any other empty cell moved it beyond reach and the step could never be finished. The fix adds no movement code -- refusing the branch falls through to the existing snap-back, which tweens to homePosition and touches BoardGrid not at all, so the item returns to its starting cell in the view and never left it in the model. D-082 (2026-08-26) added `pickupRefused`, and it FIXED A LEAK rather than only serving the tutorial: refusing in OnPointerDown alone never stopped a drag, because UGUI still calls OnBeginDrag/OnDrag for the same gesture and their `CurrentItem == null` guards pass -- so an item the Continue popup had "blocked" was still picked up and flown around, with none of the pickup bookkeeping done. The comment above OnPointerDown claimed the opposite and was wrong. All five handlers now check the flag. The tutorial gate rides the same flag and applies to BOARD cells only, since cellX/cellY are stale leftovers while an item sits in a tray. HAPTICS (D-070): pickup buzzes from OnPointerDown AFTER the IsAwaitingContinue gate -- a press the Continue popup swallowed produced no visual reaction either, and a buzz with nothing on screen reads as a broken game. The tray-drop buzz sits in PlaceInSlot, which is deliberately NOT called for the drop that resolves the batch (that one goes to PlaceInSlotAndDeliver or ReleaseAndDestroy, neither of which buzzes); the gap is correct rather than a hole, since the resolving drop is the one that delivers or costs a life and both outrank the drop tick in the same frame anyway. The binder arrives through Configure like every other dependency here -- no lookup, no singleton -- and is null-checked at both sites, so a scene that never wired one drags exactly as before. added in code by BoardView per item container, needs a Physics2DRaycaster on the scene camera; the SINGLE writer of a dragged item's transform, and never touches BoardGrid mid-drag (only on drop) so it can't fight BoardView's CellChanged/RefreshCell cycle; ApplyPickupVisuals COMPLETES any live tween (DOKill(true)) BEFORE snapshotting homePosition/homeScale, because a wrong-order scatter's fly-in is delayed for the tray shake and parks the container on the tray at scale ZERO -- a plain kill froze it there, so the item dragged invisibly and OnEndDrag re-applied the zero; X follows the finger by accumulated delta with no absolute anchor, so any other writer touching a held item's X strands it D-071 adds DropRejected, and only on the two branches of OnEndDrag that actually refuse the drop: the tray-pickup-dropped-nowhere branch and the snap-back branch the GDD names. The relocation branch above them is deliberately SILENT -- moving an item to another empty cell is the drop working, not failing. OnPointerUp's settle-back is silent too, even though its own comment calls it "the same feel as an invalid drop": that path is a tap that never became a drag, and ItemPickup has already fired on the press, so buzzing again on release would make every stray tap a double. D-109 adds ItemPlacedOnBoard on the SUCCESSFUL placement branch, correcting D-071: that turn left this branch silent because "a relocation is the drop working, not failing" -- a correct observation with the wrong conclusion, since dropping into a tray is also the drop working and has always buzzed. What the silence produced was half a gesture, pickup answered and release not. Fired for BOTH origins the branch covers (board-to-board and tray-back-to-board) because ItemPickup does not care about origin either; splitting would make one gesture feel finished or unfinished for a reason the player cannot see, and `wasOnBoard` sits right above it if board shuffling proves noisy on a device. The drag now has four distinct answers: LightImpact picking up, RigidImpact onto the board, MediumImpact into the tray (the real move), SoftImpact refused. | h:837c8f0d
ExpoTheExplorer/Assets/Scripts/UI/BoardView.cs | runtime board grid renderer + drag/drop | sys: BoardUI | api: ReleaseContainer(int x, int y); TryGetDragHandler(int x, int y, out BoardItemDragHandler handler); BeginFlyInOverride(Vector3? origin, float delay = 0f); EndFlyInOverride(); TryGetCellAt(Vector3 worldPosition, out int x, out int y) | dep: BoardAnimationConfig,BoardGrid,BoardItemDragHandler,BoardVisualsConfig,DragFeelConfig,GameManager,HapticsBinder | used: BoardItemDragHandler,WorldTrayView | crit: K2 | note: HAPTICS (D-070): holds the scene's HapticsBinder only to hand it down to every item it builds -- it plays nothing itself. Optional, and one more argument on the Configure call that already carries five. reactive, no per-frame polling -- it rebuilds only the cells BoardGrid reports via CellChanged, and cellSize/boardOrigin are DERIVED from the orthographic viewport so the board fits any aspect. RefreshCell recomputes a fly-in destination from CellPosition(x,y,-0.1f) and never reads it off container.position: a reused container only still stands on its cell if every tween that ever moved it completed, so one interrupted fly-in used to strand it and bake that spot in as the cell's destination for good. A DELAYED fly-in (only a wrong-order scatter, held for the tray's shake) also disables the container's collider for the length of the delay -- otherwise it sits active, scale-zero and grabbable right on the tray, and grabbing it snapshotted that zero as the item's home scale. BeginFlyInOverride/EndFlyInOverride are a plain flag pair, NOT nested or counted, so callers must not overlap their brackets | h:4cdb9fa4
STALE ExpoTheExplorer/Assets/Scripts/UI/DayCompletePopupView.cs | day-complete receipt popup + star rating and its score breakdown + the payout handover + the three exits from a finished day | sys: DayLifecycle | api: - | dep: DayLifecycleManager,DayRewardFlightView,GameManager,GameState,StarScoreBarView | used: - | crit: K2 | note: D-101 (2026-08-27) Show is now the WAIT and ShowNow is the old body: the popup is scheduled BoardAnimationConfig.DayCompletePopupDelay seconds out so the delivery that FINISHED the day -- the item settling into its slot (D-100), the tray growing, lifting and fading, the card sliding away, the tray growing back in -- plays out before the receipt covers it. IsDayComplete is set before the publish, so for the whole wait no ticket is assigned and every slot is empty, which means TrayManager refuses every drop anyway. The payout starts one wait later and that moves nothing: no money is credited during a day and every exit commits the purse regardless (D-057). The config reference is OPTIONAL and outside ValidateReferences for exactly the reason rewardFlight is -- an empty slot must never be able to suppress the only way out of a finished day. D-061 added an OPTIONAL score bar: this view hands it the two positions (before popupRoot activates, since anchors resolve on activation) and the FLIGHT drives it, because the bar's fill paces the star seating and this view does not own that sequence. D-060 added three OPTIONAL TMP_Text rows (time bonus / mistakes / net score, integer percents) -- optional because the popup already exists in SampleScene and a required field would stop the whole receipt from appearing until the scene is wired; unwired they stay blank and Start logs it once. Rounding happens only here, so a day a hair under a threshold displays as 55 and still scores two stars rather than being quietly promoted. D-042: "Next Day" now asks GameManager one question (TryContinueIntoNextDay) and hides only if there is still a day scene to hide in. It used to branch on "was there a next Day"; adding the second reason here would have made a popup the place where scene routing is decided, so both reasons moved to the class that owns flow. hand-wired in the Editor, never instantiates UI. It only RENDERS the star count -- the rule (one star per life still held) lives in DayLifecycleManager.StarCount since D-008, which also deleted the per-Day authored thresholds this view used to compare Total against. D-012 gave it two navigation behaviours: Go Back exits to the main screen persisting the NEXT day, and Next Day now falls back to that same exit when there is no next Day, instead of hiding the popup and leaving the player on a finished day with no UI. Neither exit calls Hide() -- the scene load destroys the popup. D-057 turned it from a REPORT into the place the day actually PAYS: nothing is credited during a day, so Show no longer switches the stars on -- it switches them OFF and hands the earned ones, plus the Total text as the coin origin, to DayRewardFlightView, which seats them and flies the money to the HUD. Two ordering rules: the stars are hidden AFTER popupRoot is activated, or the flight measures positions on an inactive hierarchy and drops every icon at the origin; and all three buttons call CompleteRewardFlight FIRST, so tapping through the payout is a skip rather than a forfeit. rewardFlight is a REQUIRED reference like every other field here, on the user's instruction (it was briefly made optional and reverted 2026-08-24). Worth knowing before touching it: ValidateReferences returns BEFORE the DayCompleted subscription, so if this field is ever empty the popup never appears at all and the player is stranded on a finished day with no exit -- the MONEY is safe either way, since GameManager commits the reward on the exits, so the failure is a navigation dead end rather than a lost payout | h:6d8860f1
STALE ExpoTheExplorer/Assets/Scripts/UI/GameOverPopupView.cs | lives-depleted continue/retry/abandon popup | sys: LivesSystem | api: - | dep: BoardAnimationConfig,GameManager,GameState,HapticsBinder,LivesConfig,LivesManager,NoKeysPopupView | used: - | crit: K2 | note: D-101 (2026-08-27) Show is now the WAIT and ShowNow is the old body: the popup is scheduled BoardAnimationConfig.GameOverPopupDelay seconds out so the broken heart rising off the tray that cost the last life (D-078) is seen before it is covered. The failure itself is NOT delayed -- LivesManager sets IsAwaitingContinue before it publishes, so the day is held for the whole wait and D-099's deferred scatter is still waiting behind it; delaying the EVENT would have let the day keep playing behind the animation. The config reference is OPTIONAL and outside ValidateReferences on purpose: a popup that never appears strands a lost day with no way out, so an unwired slot costs the pause and nothing else. D-069 added an OPTIONAL noKeysPopup checked inside OnRetryClicked, on the click rather than by disabling Retry. Hide() is deliberately NOT called on the refused branch -- this popup has to stay behind the explanation, or refusing a retry would drop the player onto a finished day with no UI at all. Main Menu beside it is deliberately NOT gated: blocking the only exit from a lost day at zero keys is a softlock, so it always works and simply spends nothing (D-068). re-verified against the file for D-064 (no public members, so api stays '-'); its behaviour is untouched by that change -- it still calls GameManager.RetryDay, which kept its name. hand-wired in the Editor; lives on an always-active object so Start runs while popupRoot begins inactive. Main Menu is live since D-012 and abandons the attempt rather than merely navigating -- the money rules for that (revert earnings, keep spending, then write) sit in GameManager.ReturnToMainScreenAbandoningDay, not in this view D-072 adds ContinuePurchased on the SUCCESS branch of OnGemClicked -- the most expensive tap in the game, which until then felt exactly like a menu button because the gem button's UiTap was all the frame produced: LivesChanged goes UP on a refill and HapticsBinder reads only the drop. The refusal side needs nothing here, unlike the shop's: Show() disables this button when the player cannot afford it, so an unaffordable Continue is never a click to answer. New optional haptics field, null-conditional like every other one. | h:10eb2afc
ExpoTheExplorer/Assets/Scripts/UI/GemsView.cs | HUD gem counter | sys: ProgressionSystem | api: - | dep: HudWalletSource | used: HudCanvasPrefabSetup | crit: K3 | note: reactive in the day scene (binds GemsChanged, never polls); on the main screen there is no event to bind to, so the value renders once -- correct, since nothing can change a balance while the player sits on the menu. Holds no GameManager since D-013: HudWalletSource decides where the number comes from, so the rule lives in one place instead of three views. Reads through the SOURCE, not through liveState, so a single call serves both modes. Still subscribes from Start, not Awake -- Awake order across objects is unspecified and GameManager.State is assigned in its Awake | h:3145e38c
ExpoTheExplorer/Assets/Scripts/UI/HudWalletSource.cs | per-scene answer to where the HUD reads its numbers, plus forwarded change events | sys: ProgressionSystem | api: SoftMoneyChanged; GemsChanged; KeysChanged; SoftMoney; Gems; KeysAvailable; KeyCount; MaxKeys | dep: GameManager,GameState,PlayerProfile,PlayerProfileStore | used: SoftMoneyView,GemsView,KeysView,HudCanvasPrefabSetup | crit: K2 | note: D-065 step 3 added the KEY half, and unlike lives it genuinely belongs here: the key readout is inside the shared prefab and shows on BOTH screens, which is the case this class exists for. Keys hang off the SESSION rather than off GameState (D-065 keeps the count private to KeyManager), so the subscribe sits OUTSIDE the live/save-file branch and the OnDestroy unsubscribe sits BEFORE that method's null-return -- pairing it with the state check would leak the subscription whenever a session exists but LiveState does not. It also gained an Update: a once-per-second DISPLAY refresh of KeyManager, on unscaled time so a popup that zeroes timeScale cannot freeze the wait the player is watching. Display-only by contract -- every gate calls Refresh for itself, so no rule depends on this running -- and once per second rather than per frame because a value that moves every 30 minutes does not need 60 clock reads a second. One ticker per scene rather than one per view. D-064 REMOVED Lives, MaxLives and LivesChanged from this class, so it is now exactly what its name says -- a wallet source. LivesView was their only reader and it binds to GameState directly now; the save-file fallback behind them died with the profile field. The two-mode rule below therefore covers SoftMoneyView and GemsView only, which are the views it was really about. Binds to SessionHost, not to GameManager, since D-022 -- which is what makes the MENU HUD live rather than a one-shot save-file read. [FormerlySerializedAs("gameManager")] on that field is LOAD-BEARING: SampleScene's prefab instance carries an override under the old name, and renaming without it drops the override, producing exactly the quiet failure this file already documents (the HUD stops following the live wallet for the whole day with nothing different on screen). A PRIVATE LiveState helper walks sessionHost?.Session?.State -- private on purpose, since D-014 removed the public one so the three views would stop branching on mode, and the double ?. matters because a host may exist while its Session is still unassigned. The mode log line now names the host TYPE, and its meaning changed: a missing host used to be expected on the menu, and since D-022 it means a lost reference in either scene. exists because D-013 put ONE HUD Canvas prefab in both scenes and the two scenes answer differently -- day scene: the live GameState via GameManager, reactive; main screen: the save file, read once (no GameManager there by design, D-012). Strictly a READER; Wallet stays the single writer. Takes the GameManager as a [SerializeField], on the user's instruction, so the binding is visible and theirs to control. A PREFAB ASSET cannot store a scene reference, so that field is necessarily a prefab OVERRIDE on the day scene's instance and empty in the prefab itself -- HudCanvasPrefabSetup step 2 sets it per scene rather than leaving a manual drag that is easy to forget. KNOWN FAILURE MODE: Apply All from the day scene drops the override (Unity will not push a scene reference into an asset) and the symptom is QUIET -- the HUD stops following the live wallet and sits on the save file's numbers all day. That is why Resolve logs which of the two modes it picked, once per scene load: an empty field is not an error (it is how the main screen works), so the log line is the only thing distinguishing the two. Resolves LAZILY on the first view's Start and never in an Awake: GameManager assigns State in its Awake and Awake order across objects is unspecified, so resolving early would reintroduce exactly the null the views' Start-not-Awake rule exists to avoid. D-014 gave it three forwarded EventBus<int>s so the views no longer branch on mode: live, it re-publishes GameState's events (LivesChanged fed by BOTH lives events, since the label it drives renders current/max); on the main screen they never fire, which is correct because nothing there can change a value. LiveState is gone from the API for that reason. Lives off the day scene now reads the SAVED count, not a constant -- that is what made the menu's lives readout real. MaxLives stays the constant: nothing varies it. OnDestroy mirrors the forwarding; EventBus removes by delegate equality so the method-group expressions match | h:91f9b333
ExpoTheExplorer/Assets/Scripts/UI/LivesView.cs | HUD heart row: one filled heart per remaining life | sys: LivesSystem | api: - | dep: GameManager,GameState | used: - | crit: K3 | note: REWRITTEN by D-064; it was a TMP label rendering "current/max". Now it drives a row of heart slots: each slot's own Image is the EMPTY heart and carries a filled-heart CHILD, so the view toggles only the children -- switching a parent off would take its filled child down with it, which is why there is one filledHearts array and deliberately NOT an emptyHearts one. Binds a serialized GameManager and subscribes to GameState's LivesChanged AND MaxLivesChanged from Start (never Awake: GameManager assigns State in its own Awake and Unity does not order Awake across objects). It no longer reads through HudWalletSource: that indirection existed so one HUD prefab could serve two scenes with different sources (D-013/D-014), and lives are a day-scene concern with one source since they stopped being persisted. The row itself is an object added to the HUD Canvas INSTANCE in SampleScene, not to the prefab asset, which is what keeps hearts off the main screen with no mode branch anywhere in code -- and is also the hazard: Apply All / Apply Added GameObject on that instance would push the row into the prefab and put hearts on the menu. Caches the GameState it subscribed to rather than re-walking gameManager.State in OnDestroy, so the unsubscribe cannot target a different object than the subscribe did. Refresh redraws the whole row from Lives instead of stepping one heart, because a refill jumps several slots at once; the SetActive is guarded by activeSelf since most lives events leave most hearts unchanged. A row shorter than MaxLives is a loud error at bind time, not a silently short row | h:3180ac1a
ExpoTheExplorer/Assets/Scripts/UI/KeysView.cs | HUD key counter, "current/max" | sys: KeySystem | api: - | dep: HudWalletSource | used: HudCanvasPrefabSetup | crit: K3 | note: D-065 step 3. Reads through HudWalletSource, which is the OPPOSITE of what LivesView does since D-064 -- and deliberately so, because the two widgets have opposite shapes: the heart row is a SampleScene object and can hold a scene reference to GameManager, while this readout is PREFAB content shown on both screens and a prefab asset cannot store a scene reference at all. That is the exact problem HudWalletSource was built for (D-013). No per-view tick either: the source runs one display refresh per second for the whole scene. When the source has no session it renders NOTHING and logs -- since D-022 that is a lost reference rather than a mode, and the only number this view could invent ("0/5") would tell the player they are out of keys, which is a worse failure than a stale label. Untestable from the EditMode suite for the structural reason D-012 recorded: it is in the predefined Assembly-CSharp | h:c11c8bde
ExpoTheExplorer/Assets/Scripts/UI/NoKeysPopupView.cs | out-of-keys explanation: countdown + gem refill + the gate itself | sys: KeySystem | api: HasKeyOrShow() | dep: SessionHost,KeyManager | used: MainScreenView,GameOverPopupView | crit: K2 | note: D-069, key-plan step 5. THE GATE LIVES HERE rather than in its two callers -- HasKeyOrShow returns true when the player may proceed and otherwise shows itself and returns false -- so there is one answer to 'what happens with no keys' instead of two copies, and neither caller needs a session reference (MainScreenView has never had one). The name states the side effect on purpose: a gate that silently opens a window is a trap at the call site. FAILS OPEN by design: missing references or no session return true and log, because a forgotten drag must never leave the player unable to play -- an uncharged key is the smaller wrong, and the Start-time error is what makes the misconfiguration findable. Nothing is ever disabled to enforce the gate (the user's rule): buttons stay pressable and the check is on the click, since a greyed control explains nothing. closeButton is REQUIRED and that is a softlock guard -- the popup only self-closes when a key ARRIVES, so a player with no keys and not enough Gems would otherwise sit in it for up to a full regen interval. Auto-closes on KeysChanged > 0 because its single claim stops being true. Owns a 1s unscaled redraw that runs ONLY while visible; SecondsUntilNextKey refreshes for itself, so this works in a scene with no HUD. A failed gem purchase leaves the window OPEN (the only possible cause is insufficient Gems, since it is shown at zero keys) so the player keeps seeing the price. One instance PER SCENE, not a shared prefab: sessionHost is a scene reference, which a prefab asset cannot carry (D-013's lesson) D-071 adds BlockedByNoKeys, requested inside Show() rather than in HasKeyOrShow's refusing branch, so every route that opens this window buzzes and no future caller has to remember to. It lands in the same frame as the UiTap of whichever button was pressed and outranks it (55 vs 5) -- the player feels the refusal, not the press. The haptics field is optional and null-conditional, which matches this class's existing fail-open stance: a forgotten drag costs a buzz, never the gate. D-072 adds KeysRefilled on the SUCCESS branch of OnGemClicked. Its own moment rather than sharing GameOverPopupView's ContinuePurchased, even though both are "Gems spent, resource restored": a rescue mid-day and a meta top-up are different stakes, and separate rows let them be tuned apart -- the defaults already differ. The not-enough-Gems branch stays SILENT, which is scope rather than oversight: it was offered in the same conversation and deferred, and PurchaseRefused would cover it in one line. | h:ec95bc4e
ExpoTheExplorer/Assets/Scripts/UI/MainScreenView.cs | main screen: day readout, Play, and Start Over | sys: MainScreen | api: - | dep: SessionHost,MainScreenTutorialView,PlayerProfile,PlayerProfileStore,SceneFlow | used: MainScreenSceneBuilder,MainScreenResetButtonSetup | crit: K2 | note: D-090 (2026-08-26) Play is GATED ON OWNING THE FIRST BUILDING, and the gate follows the key gate beside it rather than greying the button out -- the user asked for an unpressable Play and CLAUDE.md/D-069 forbids exactly that, so Play stays pressable and ANSWERS: it points at the store instead of starting a day. The condition is DERIVED (owns nothing) rather than a new profile flag, which is the same state the welcome tutorial reads, so the two can never disagree about who is new. Read from the LIVE session, never the Start-time profile snapshot this class also holds: buying the building is precisely the event that opens the gate, and a snapshot predates it. IsAwaitingFirstBuilding resolves every uncertainty toward LET THEM PLAY -- unwired reference, no session, null list -- because this gate sits on the only path into the game and failing closed is an unplayable build that looks like a save bug. D-069 added an OPTIONAL noKeysPopup and a check inside OnPlayClicked -- on the CLICK, never by disabling Play, because a greyed button does not say why. Starting a day is a GATE and spends nothing; the key is charged only if the day is later given up on (D-068). Unwired the field is a no-op rather than a lock, which is the deliberate direction to fail.  TWO serialized buttons since D-026, the day readout moved ONTO the Play button as "Continue Day X" (D-025). Since D-028 there is NO lookup here at all -- playLabel and resetLabel are serialized fields the author drags. D-025 had DEFENDED finding the Play caption with GetComponentInChildren ("a third field is a third thing to forget") and the user overruled it: a forgotten field is visible in the Inspector, while a search that starts matching the wrong object is visible nowhere. An unwired caption is a WARNING, not an error -- the button still works, it just says nothing. (two missing-reference errors had just come from exactly that). Still reads the PROFILE, not the session this scene has carried since D-022, so the profile loads twice here; accepted because taking it from the session needs another serialized reference and the day number cannot change on a menu. the meta side's navigation shell (D-012), deliberately content-free so the day<->meta flow is correct before any meta CONTENT is designed on it. Reads the SAVE FILE, not a GameState: there is no GameManager in this scene, which is exactly what keeps GameManager's "Awake builds the whole Day" shape intact, so the profile IS the hand-off between the two scenes. A pure reader -- it never saves, so the single-writer invariant on both balances and the day index still names GameManager alone. Gave up its own coins/gems labels in D-013: the shared HUD Canvas prefab is present in this scene too, and two labels showing one balance is how a screen contradicts itself -- MainScreenSceneBuilder no longer creates them and HudCanvasPrefabSetup deletes the four an earlier build made. Shows CurrentDayIndex + 1, a catalog POSITION and not a Day file's dayIndex, and can read one day high on a save that points past the last authored Day -- GameManager clamps that when the day itself loads. Hand-wired like the popups, but the wiring is done once by MainScreenSceneBuilder rather than by hand. D-026 added the OPTIONAL resetButton: two taps (the first swaps in the authored resetConfirmLabel for resetConfirmSeconds, and a coroutine puts the real caption back so the button cannot go on saying "tap again" after that tap stopped erasing anything) then PlayerProfileStore.Delete() and SceneFlow.LoadMainScreen(). It still writes no VALUE -- the delete is a command to the file's owner -- and the scene reload is deliberate: rebuilding the scene resets the session, the HUD, the grounds and the shop through their existing single writers, where patching the live GameState would need a second writer for the balance, the day index and the owned props. Optional on purpose: a MainScreen built before this existed still runs, and playButton stays the only reference ValidateReferences demands D-095 removed the Start Over button entirely -- the field pair (resetButton/resetLabel), the two authored confirm knobs, the arm/disarm coroutine and OnResetClicked. The capability moved to the SRDebugger panel's Save > Delete Save File, which now does the same delete-AND-reload. The two-tap confirm did not move and is not missed: it guarded against a PLAYER mis-tapping, and nobody opens a debug panel by accident. `using System.Collections` went with the coroutine. | h:90892d8c
ExpoTheExplorer/Assets/Scripts/UI/MainScreenRoot.cs | the main screen's composition root: builds the session | sys: MainScreen | api: MainScreenRoot.Session | dep: GameSession,SessionHost,GameConfig,LivesConfig,KeyConfig,PowerupConfig,FoodCatalog | used: MainScreenSessionSetup,HudWalletSource | crit: K2 | note: D-065 step 2 added a REQUIRED keyConfig [SerializeField], joining the existing missing-config guard rather than getting one of its own -- the menu needs a session for the same reason the day scene does, and a KeyManager cannot be built without it. The error message names which of the three is empty. gives the MENU a GameState and a Wallet, which is the precondition for a shop spending money at all (D-022) -- and lets the shared HUD follow it live instead of reading the save file once. Deliberately NOT a GameManager: it builds the session half only, because the day runtime (ticket slots, tray, board, the per-frame tick) behind a menu would time tickets out and cost lives, which is the concrete reason D-021 rejected moving GameManager here. Builds in Awake, never Start: every reader is a view's Start and Unity runs all Awakes first -- the same ordering GameManager relies on, so both scenes behave identically. Missing configs log an ERROR because the failure is otherwise quiet: with no session the HUD silently falls back to the save file and looks correct until the first purchase does not update the coin count | h:ffc89f61
STALE ExpoTheExplorer/Assets/Scripts/UI/MetaGroundsView.cs | draws one location's grounds and the props on it | sys: MetaSystem | api: Refresh(); RefreshAfterPurchase(MetaItemDefinition purchased); ViewedLocation { get; }; IsSettlingPurchase { get; }; ShowGhost(MetaItemDefinition item); ClearGhost(); TryGetGhostBounds(out Vector3 worldTopCentre, out Vector3 worldBottomCentre) | dep: MetaResolver,MetaCatalog,MetaLocation,MetaItemDefinition,GameSession,SessionHost,HapticsBinder,HapticConfig | used: MetaGroundsSetup | crit: K2 | note: D-096 (2026-08-26) added IsSettlingPurchase, the one question the shop asks before reopening itself after an area unlock: `placing |  | DOTween.IsTweening(content)`, so it covers the WHOLE payoff -- drop, shake, and RestoreFocus's journey home -- rather than only the drop. Two halves because the framing changes hands between them, and the second starts in the same call that clears the first, so there is no frame where it reads false in the middle; a placement that never zoomed leaves no tween and settles the instant the drop ends. A QUESTION, not a completion callback: the early-exit paths in RefreshAfterPurchase finish synchronously, so a callback would fire before the shop's own SetOpen(false) and be undone by it -- the grounds would have owed a "never called synchronously" promise nothing could enforce. Nothing else in this class changed. HAPTICS (D-070): PropLanded is requested in PlacePurchasedProp at the frame the prop reaches `landed`, and deliberately ABOVE the shake guard -- the prop has landed whether or not the ground is authored to react, so a scene with the shake turned off still gets the thump in the hand. It is the heaviest preset on the main screen, paired with the lightest one on the buy itself. D-046: the grounds Image is now a CONTAINER that never draws -- the art moved into runtime children so the continuation layer can sit behind it, since a child always renders over its parent's own graphic. Order is [0] continuation, [1] grounds art, [2..] props. Chosen over inserting a real container object, which would have meant a new serialized reference and another delete-and-rebuild round (D-036); as it stands the scene is untouched, `background` is still the ScrollRect content, the zoom target and the props' parent. The continuation is a CHILD, so scrolling, the preview zoom and the framing are inherited rather than recomputed -- a second transform calculation is the "second authority" trap this class keeps avoiding. It is sized from its own pixels times the SAME PropScale the grounds use, and centred with no offset. Scroll range is deliberately unchanged: the content is still sized from the grounds, so the extra art is clipped by the viewport and fills the edges under zoom rather than letting the player pan into empty space. artLayerCount exists because SortedInsertIndex returns SIBLING indices and props now start after these layers -- forgetting that shift would put ghosts behind the grounds art, invisible, which is the silent-depth defect D-045 was reported for. D-045 fixed a draw-order bug the Meta Editor had been exposing: the upcoming-prop silhouette was APPENDED after the active props, so it drew in front of everything regardless of its SortOrder, while the editor canvas has always sorted. Reported as "the fryer is behind the sauces in the editor but in front in game", and the catalog confirmed it -- fryer is a DayUnlock prop at sortOrder 0, still closed, therefore drawn as the upcoming ghost. It is now placed at the sibling index its SortOrder asks for (SortedInsertIndex), and the celebration copy takes over the hidden real prop's exact index. The reason to fix it this way rather than force the ghost forward is the reason the preview exists (D-032, D-040): it shows where the prop WILL stand, and depth is part of where -- a ghost pulled to the front tells the truth about position and lies about occlusion. Accepted consequence: a prop authored at a low SortOrder really is behind the scenery and its ghost can be partly hidden; the celebration zoom still finds it. NOT changed, and flagged as an inconsistency for the author to settle: the PURCHASE ghost is still forced to the front (D-031), because the shop zooms to it and names it in a popup, where occlusion costs comprehension. D-043: after the last unlock the map pans on to the NEXT prop's ghost and lingers before zooming out. Not a new drawing pass -- Refresh runs before the celebration with the new day index, so upcomingProp is already the prop AFTER the one that just opened, at the right progress; a second copy would be the same thing twice. The timing pays for itself: D-040 measures the wait from the PREVIOUS unlock, so a prop that just opened leaves the next reading exactly %0, and "the counter starts now" is on screen without a word of copy. Its own duration field, not the celebration hold, because the two are different beats and one number would mean breaking one while tuning the other. skipRequested is reset first, so skipping the reveal does not silently skip the peek as well. Nothing coming (all open, or the next one area-gated and therefore undrawn) skips straight to the caller's finally, which is still the single place the framing is restored. D-041 plays the unlock celebration: zoom to the prop, watch it rise from silhouette to solid, then zoom back. It works out what is owed ITSELF via MetaResolver.DayUnlocksBetween rather than being told by the day scene, which is what makes it independent of the forced-return step that follows -- the player sees it however they got back, including after quitting for two days. Started ONLY from Start, never Refresh: a purchase also calls Refresh and a shopping trip is not the moment for a day-unlock fanfare. FocusGhost became FocusOn because it now has two callers; a second framing routine would have broken D-032's single authority. The real prop is HIDDEN while an animated copy takes its place, so the opening is watched rather than already-done, and the fill layer is shared with the upcoming-prop preview. The marker is written only after the WHOLE queue completes: an interrupted celebration replays next time, which is the cheap failure, where swallowing it is the expensive one. Its single writer is this class. Skip is a transparent full-screen Button parented to the CANVAS as last sibling -- not to the grounds, which are the canvas's FIRST child, so a catcher inside them would sit under the market button and let the shop open mid-celebration; and a Button rather than Input.GetMouseButtonDown so it does not depend on which input backend is configured. try/finally guarantees the framing comes back and the catcher goes: a screen left zoomed with an invisible full-screen button over it is unusable. D-040 draws the upcoming Day-unlock prop: a faint silhouette with the solid version filling from the bottom as days pass, and the COMPLETED percentage over it. TWO layers, not one, because a single Image cannot be both faint and partly solid -- fillAmount CUTS, it does not fade. The silhouette goes through CreateProp so the preview stands exactly where and at what size the real prop will (same reason as the purchase ghost); the fill is its stretched CHILD so placement is inherited rather than computed twice. Built in CODE, not cloned from an authored template: a template would be nicer to style but would add an object reference to this component, which would mean re-running Build Meta Grounds -- the step that refuses to rebuild, i.e. the D-036 trap. Tuning is serialized instead, and new serialized values are born with defaults without touching the scene. preserveAspect is deliberately unused on these props (size already comes from sprite pixels times background scale) and interacts badly with Filled -- leave it alone. A missing TMP font costs the caption, not the fill. Refresh() is PUBLIC again as of S4 (D-034) and only now: public for the first shop, private again when that shop was reverted (D-027) because an entry point with no caller reads as one that is used somewhere, and kept private through S1 and S3 on the same argument since neither bought anything. A purchase changes what MetaResolver.IsActive answers and nothing else would notice, so the caller arrived exactly then. S3c (D-033) animates the travel with DOTween instead of snapping. The destination is now MEASURED, not derived: the map is placed at its destination for an instant, the end scale, the end position and the ghost SETTLED edges are read there, and it is snapped back before anything renders. That was the only way to keep the world-space delta framing (D-032 rejected pivot/anchor arithmetic) while also tweening -- the delta can only be read once the scale is applied. TryGetGhostBounds therefore reports the SETTLED edges, which is why MetaShopView needed no change and carries no timing dependency on this animation: it places the popup once, where the prop is going, and the map slides under it. ShowGhost destroys the old ghost WITHOUT restoring the framing (DestroyGhostObject) while ClearGhost restores -- load-bearing since the travel is a tween, because restoring first would re-capture the "before" framing from halfway through the return tween and cancelling would send the map somewhere it had never been. The pre-focus framing is captured only when not already focused, for the same reason. Scrolling is handed back in the return tween's OnComplete, never at its start: the ScrollRect is Clamped and pulls the content toward its bounds every LateUpdate, so both would be dragging one value. DOKill before every tween and in OnDisable. S3b (D-032) made ShowGhost also FRAME the prop: zoom toward it and slide the map so the confirm popup fits above. The zoom is not authored per prop -- it is the zoom that would make the prop fill previewFill of the viewport, clamped to [1, previewMaxZoom]. A trash bin zooms to the ceiling; a fence as wide as the grounds needs a ratio below one, so the clamp holds it at 1x and the map does not move. That is the user's own observation falling out of the arithmetic instead of being an exception, and a middling prop added later gets a sensible number for free. ScrollRect is DISABLED while previewing -- honest (the player is answering a question, not exploring) and it keeps the ScrollRect's bounds arithmetic out of a fight with a scaled content rect, plus horizontal scrolling is off so a zoomed map would otherwise be unreachable sideways. Framing is a WORLD-space delta, never anchor arithmetic: the background is width-stretched with a top pivot and hand-rolled maths would silently focus the wrong spot the day either convention changes. ForceUpdateCanvases after the scale change, or the ghost's corners are read one frame stale and the framing lands off by the zoom factor. RestoreFocus is guarded by a flag, not by comparing values, because a preview that zoomed by 1x still disabled scrolling. S3 brought the ghost back (D-031): ShowGhost draws the previewed prop through CreateProp -- the SAME path the owned props take -- so a preview cannot disagree with where the thing will actually land, and PropScale is now one method rather than two call sites for that reason. Only the alpha differs, and it is a serialized field because it is cosmetic. The ghost is NOT in spawnedProps: it has its own field, because tying two different lifetimes to one list would make "was the ghost destroyed or the prop" unanswerable in S4. Refresh clears it -- a ghost belongs to ONE location layout and would otherwise stand on ground it was never positioned against. ClearGhost is safe in any state, because every exit the shop has calls it. Refresh() is STILL private: S3 buys nothing, so nothing changes what IsActive answers; it goes public in S4 when there is a caller. ViewedLocation came BACK in S2 (D-030) after D-027 deleted it: the objection was "no caller", and MetaShopView is now the caller. ViewedLocationChanged did NOT come back -- the shop rebuilds its list every time its panel opens, and the only moment the player can walk to another location is while that panel is closed, so an event would be a publisher written before its subscriber. The location bar's four references are OPTIONAL since D-025 -- with one location there is nothing to switch between, so requiring them forces scene clutter for a feature with no content, and deleting the bar is a legitimate choice. Only sessionHost/catalog/scroll/background stay required, because without them nothing can be drawn at all. The shop's ghost preview, ViewedLocation and ViewedLocationChanged were REMOVED with the shop (D-027): they had exactly one caller, and an entry point with no caller reads as one used somewhere. Refresh() went back to private for the same reason. decides NOTHING -- what is active comes from MetaResolver, where prices, area gates and unlock days were already settled and tested (D-019); this turns three answers into RectTransforms. Props are INSTANTIATED from the catalog, never placed in the scene: nineteen hand-placed objects would be a second copy of the catalog free to disagree with it silently. Two things are load-bearing. Canvas.ForceUpdateCanvases() before measuring: prop sizes derive from the background's WIDTH, and on the frame Start runs a stretched rect's width is still zero, which would size every prop to nothing and look like an empty screen rather than an error. And props are CHILDREN of the background with their normalizedPosition written straight onto anchorMin/anchorMax -- that is what K5's "normalize to the background rect, not the screen" was for: they ride the art through every resize and every scroll with no layout code. Background fits by WIDTH and the height falls out of the aspect (M10, the user's choice): fitting by height would letterbox every device, while fitting by width leaves vertical scrolling as the only variable, and that is zero on the phones the art was drawn for. Refresh() is public because Adım 8's purchase changes what IsActive returns and nothing else would notice; it runs at EVENT frequency (screen open, purchase), never per frame. Props are raycastTarget: false so they cannot swallow drags meant for the scroll view. Location stepping is between UNLOCKED locations only -- letting the player walk into a locked one would duplicate in the UI the refusal MetaPurchase.LocationLocked already owns D-048 added the PURCHASE PLACEMENT animation: a just-bought prop is prepared translucent and placementDropHeight above its spot in the SAME FRAME DrawProps drew it (waiting even one frame would show it standing in place and then jerk it into the air), then -- after previewTravelSeconds, so the map has finished travelling back out of the preview zoom, the same wait CelebrateOne makes for the same reason -- it falls on an IN ease while its alpha rises LINEARLY toward the prop's own alpha, and the ground takes the hit as it lands. Animated by a frame loop on Time.unscaledDeltaTime rather than a tween, which is CelebrateOne's choice and buys the same two things: a try/finally that guarantees the finished state through any interruption, and no tween left pointing at a RectTransform Refresh is about to destroy. The shake moves the CONTENT, not the viewport -- a moved mask window would open a sliver of empty screen at the edges, while the content carries D-046's oversized continuation layer as a child, which is the art that keeps them covered -- and it suspends the ScrollRect for its duration (D-032's call: Clamped scrolling drags the content toward its bounds every LateUpdate and would turn the shake into a stutter), handing it back under RestoreFocus's own `!focused` condition so two places never own that flag. The swing is a damped sine, negative first, not per-frame randomness: an impact has a direction and noise at 60fps reads as a glitch. Clear() stops the routine BEFORE destroying props, which makes "the props are gone" and "nothing is still animating them" one moment. D-049 KEEPS THE PREVIEW ZOOM through the placement and only then travels out. `placing` is the claim and RestoreFocus is where it is honoured -- a flag next to the existing `focused` guard rather than a parameter threaded through the shop's SetOpen/ClosePreview, because "may the zoom be given back right now" is this class's question. The order in MetaShopView.OnConfirmBuyClicked is load-bearing and was found by wiring it, not by reasoning: RefreshAfterPurchase must run BEFORE SetOpen(false), because the shop's exit goes through ClearGhost whose second half is the request for the zoom back -- claiming after it would let the zoom-out start and the prop would land on a map already travelling away. Both calls share one frame, so nothing is visibly half-done. Two waits and a scroll block DIED with this: the fixed previewTravelSeconds wait (the map no longer moves) and the shake's disable/re-enable of the ScrollRect (FocusOn turned it off at D-032 and RestoreFocus turns it back on, so the whole placement runs inside a window where it is already suspended). What replaced the wait is `while (DOTween.IsTweening(content))`, zero frames in the normal case and exactly the remainder for a player who taps BUY before the travel INTO the zoom has finished. INTERRUPTION OWNERSHIP is the subtle part: `placementGeneration` is bumped by every claim, and the finally hands the map back only if its own generation still matches -- so a second purchase takes the framing from the first placement instead of having the first one's teardown travel out from under the second one's drop. A location change is the opposite case and is handled by its own caller: Step calls CancelPlacement, which bumps the generation and gives the framing back SNAPPED (RestoreFocus(animated:false)), because Step sets the new location's scroll position one line later and an animated return would still be moving and would win. Clear cannot make that call -- it cannot tell a location change from a purchase -- which is why the decision sits in Step. Both cases are unreachable in today's scene (one location, D-025's optional arrows) and are written down because the second location is what makes the first one bite. D-050 routed the prop ARITHMETIC through MetaLayout: PropScale and the prop's size are asked of it rather than computed here, because the day scene's backdrop now composites the same prop and the two must agree pixel for pixel. The POSITION stayed anchors + pivot -- that is the better implementation of the same rule, since an anchored prop rides the background through every resize without a line of layout code, and MetaLayout.PropRect is that rule written out for the backdrop, which has no anchors to lean on. So this file remains the reference if the two ever disagree. D-051 fixed a DEPTH bug that had been live since D-045 and only became visible when D-047 started closing the shop after a purchase. Object.Destroy is DEFERRED to the end of the frame, so a redraw's old art layers, props, silhouette and ghost were all still children of the background while the new ones were being appended -- the parent carried BOTH sets for the rest of that frame. Anything that only APPENDS is fine (the new props keep their relative order), but DrawUpcoming moves its silhouette to an ABSOLUTE sibling index computed as though the dead children were gone. On the report that surfaced it the dead block was 13 children and the computed index was 12, so the silhouette landed inside the corpses, BEHIND the new background art, and vanished -- which read as "the prop was deleted by my purchase". Every spawned child now dies through Detach, which does SetParent(null) BEFORE Destroy so it leaves the child list on that line and every index computed afterwards counts only what is really there. The user's own first description -- "arkaplanda kaldi" -- was literally right and was misread as a framing complaint; the positions of the props they COULD see were what made it look like a scroll problem. D-072 adds PropUnlocked in CelebrateOne, on the line that snaps the reveal to finished -- the one place BOTH routes reach, the fill running its course and the player tapping past it, because skipping skips the animation and not the event. Not inside the reveal loop, for the reason the coin burst established: that loop drives fillAmount frame by frame, so a request per iteration would be a second of rumble instead of one beat. A SEPARATE moment from PropLanded on purpose -- both end with a prop standing there, but a bought prop falls and hits the ground (HeavyImpact, the map shakes under it) while this one rises from silhouette to solid, and giving a gradual reveal an impact haptic would describe a blow the screen never struck. Several props unlocking at once each buzz, like the star row, and cannot machine-gun: CelebrateOne resets skipRequested per prop, so each needs its own tap, and the coalescer caps the frame anyway. | h:54f46774
STALE ExpoTheExplorer/Assets/Scripts/UI/MetaShopView.cs | the meta shop: market button + the panel it opens | sys: MetaSystem | api: - (all private; the shop is driven by its own buttons) | dep: MetaPurchase,MetaCatalog,MetaGroundsView,MetaShopRowView,SessionHost,HapticsBinder,HapticConfig | used: MetaShopSetup | crit: K2 | note: D-096 (2026-08-26) makes ONE kind of purchase reopen the shop: an item whose UnlocksArea is set. Not a reversal of D-047 -- the shop still closes on every purchase and the screen still ends on the prop -- but ShopItems HIDES area-locked props, so the square is the only buy whose payoff is partly a LIST of rows that were not there a moment ago. Read from the catalog flag, so a second expansion needs no code. The wait is a coroutine polling MetaGroundsView.IsSettlingPurchase (one bool + one tween query per frame, alive ~1s per purchase, at most once); started AFTER SetOpen(false) so the shop's own exit path runs whole first, and SetOpen now cancels any pending reopen -- which is how the player closing or opening the shop themselves, or entering a preview, silently abandons it. The coroutine clears its own handle before calling SetOpen(true), so that cancel cannot kill the coroutine calling it. The one abandon case SetOpen does not cover is walking to another location, checked explicitly against the location the purchase was made on. D-088 (2026-08-26) exposed MarketButtonRect, read-only, for the main-screen tutorial's arrow to point at. Deliberately the RectTransform and not the Button: a caller gets a position and a size, and no way to press the store on the player's behalf or to restyle it. the dep field read `-` before D-070 and that was simply wrong -- this screen has always compiled against MetaPurchase, the catalog, the grounds, its own row view and the session host; the haptics pass corrected it rather than adding one name to a lie. HAPTICS (D-070): PropPurchased is requested AFTER session.Save(), which is the first line at which the purchase is a fact -- the verdict passed, Wallet actually spent, and the file has the prop. On the tap it would sometimes fire for a purchase that then failed. It is the lightest preset in the table on purpose: the payoff the player is waiting for is the prop hitting the ground a moment later, in MetaGroundsView. D-039: rows bind offer.ShopIcon, not offer.Sprite. The ghost and the map prop still use Sprite. D-038: the confirm popup has NO backdrop -- the user removed it from the scene and the field went with it, so CANCEL is the only way out of a preview (MS6 is dropped) and the map is not dimmed while one is up. The backdrop's other job, eating taps meant for the map, costs nothing: the ScrollRect is already disabled for the duration of a preview (D-032). The cancel button is now the ONLY exit, which makes an unwired one the worst failure on this screen, so it stays required. D-037: owns both BUY colours and applies them on every bind, so a row always shows one of the two rather than sometimes inheriting the template's authored colour. The popup's BUY is red AND disabled when the money is short: the disabled state darkens the base colour, so it reads as "red, and off" instead of the generic grey that says nothing about which reason. The unaffordable red is the project's existing destructive-action red, so the game does not grow a second red. D-036: Start hides the panel and the popup BEFORE validating, with its own null checks. A missing reference must read as a console error, not as the shop covering the screen -- which is exactly what happened while the scene still held the S1-era shop: validation returned early, so nothing was switched off and the panel sat over the map with a market button that had no listener. An incomplete shop is invisible now, not stuck open. S5 (D-035): the view neither re-sorts nor re-filters -- ShopItems returns the list already filtered and ordered, so there is one authority for what a shop shows. Affordability per row is price-vs-balance rather than another Evaluate call: everything Evaluate could still refuse on has been filtered out, so money is the only question left and asking a six-verdict function for a yes/no would imply otherwise. The popup's BUY is now interactable only when the money is there -- until S5 it was live and did nothing on a short balance (Evaluate refused, logged, screen unchanged), which is a dead button in every sense the plan warned about; the commit still re-checks everything. S4 (D-034) made the purchase real, and it is the ONLY place the meta side spends money -- while owning no rule: MetaPurchase decides whether the buy is allowed, Wallet decides whether the balance carries it, GameSession.Save decides what reaches disk. Evaluate runs a SECOND time here, having already run when the row was drawn, because the panel stays open across purchases and the balance the row was drawn against goes stale; the cost is one call. The wallet is then asked anyway even though Evaluate just approved: Wallet is the single writer of the balance and its answer is the authority -- decision and transaction, not two answers to one question. ATOMIC BY CONSTRUCTION rather than by care: the balance and the owned set live on one GameSession and Save() writes both, so "money gone, item missing" is not expressible. A non-Ok verdict logs and leaves the popup OPEN -- saying why on screen is S5, and doing it here too would put the appearance of a verdict in two places. Panel stays open after a buy (MS4) and the row list is rebuilt so the bought prop drops out. S3b (D-032) moved the popup ONTO the prop: confirmBox has a bottom-centre pivot and is placed just above the ghost, flipping BELOW it when the prop sits near the top of the screen, and clamped away from the screen edges. Positions are computed in the parent RectTransform's local space, not world units, because the box's size is measured there and mixing the two drifts by whatever the canvas scaler is doing on the device. Recomputed on every preview -- a cached position would be wrong for the second prop. confirmBox is REQUIRED: unwired, the popup would quietly re-centre itself, and a silently-missing feature is worse than a named missing reference. S3 made this the whole state machine (D-031). Two fields hold it: isOpen (KAPALI/ACIK) and pendingItem (ONIZLEME when non-null, and it stores the ITEM so the popup and the S4 commit cannot disagree about which prop is being bought). Every transition goes through ApplyVisibility and every exit from a preview through ClosePreview -- ONE answer to "when does the ghost go away", which is exactly what the first shop lacked (D-024). The list and the popup are never both up: the panel is a bottom sheet covering the map the ghost stands on, so previewing with the list up would hide the thing being previewed and the feature would look broken rather than hidden. Panel visibility is therefore isOpen && !previewing -- activeSelf is neither field on its own. Rows are NOT destroyed while previewing; cancel brings the list back rather than rebuilding it. Tapping a second row REPLACES the preview (a change of mind, not an error). The popup BUY changes no state in S3 on purpose: closing would make S3's own verification ambiguous and S4 would write the closing twice. The popup's four interactive refs are REQUIRED -- an unwired cancel or backdrop strands the player in a preview with a ghost and no way back, the exact leak this step exists to make impossible; the two labels stay optional because they only say what is being bought. S2 filled the panel: a row per offer from MetaPurchase.ShopItems, cloned from an authored INACTIVE template in the scene rather than a prefab asset (the TicketCardView/modificationRowTemplate pattern) -- restyling that one object restyles the list, and there is no second asset to keep in sync. Which LOCATION is shopped is read from MetaGroundsView.ViewedLocation, NOT resolved again here, which is what keeps the MetaCatalog reference in exactly one place; two components free to point at two catalogs would break D-015 with nothing looking wrong. Rows are rebuilt on every OPEN (a purchase changes what ShopItems answers) and destroyed on close -- not pooled, same call MetaGroundsView.Clear makes. rowsParent is forced ACTIVE and the template forced INACTIVE in Start: hiding the container to get the sample row out of sight silently hides every real row, the trap TicketCardView records. Instantiate copies the template's inactive state, so each clone needs an explicit SetActive(true). All SIX references are required -- listing the first location's props when grounds is unwired would show a wrong list as though it were right, which is the failure D-028 was written about. Step S1 of meta-shop-plan.md, and DELIBERATELY thin: a market button and a panel that opens and closes. No rows (S2), no ghost, no confirm popup (S3). Reads nothing and writes nothing -- the catalog, the wallet and the six purchase verdicts stay in MetaPurchase where they were tested (D-019), and this screen will ASK them in S2 without owning a rule. The panel is toggled from CODE rather than by a Button-to-SetActive wire in the Inspector, even though that wire would do today's job: this class is going to own the shop's whole state machine (KAPALI/ACIK/ONIZLEME) and the ghost's lifetime hangs off these same transitions, so a machine half in the scene and half in code is one that disagrees with itself. SetOpen is the single place visibility changes, for the same reason -- the first shop's ghost leaked because its visibility had more than one owner (D-024/D-027). Forced CLOSED in Start whatever the scene was saved as: a shop already up hides the grounds the player came to look at, and the scene checkbox is easy to leave ticked after testing. Both references are REQUIRED, unlike the grounds' optional location bar -- a shop with no button cannot be opened and a button with no panel does nothing, so neither has a degraded mode worth shipping. isOpen is not persisted (same call K6-4 made for the viewed location: a profile field and a schema version for a preference nobody would miss) D-047: a SUCCESSFUL purchase closes the shop all the way to KAPALI instead of restoring the list. MS4 had deliberately kept the panel open so several props could be bought without tapping the market button between each one; the user reversed it ("bir sey satin alindiktan sonra yeniden dukkana donmesin") and the reason holds up in the geometry: the panel is a bottom sheet over most of the map, so returning to the list hides the very prop that was just paid for behind the ones that were not. OnConfirmBuyClicked therefore ends with SetOpen(false) then grounds.Refresh() -- through the single door rather than by clearing isOpen locally, so the ghost, the popup, the framing and the rows all come down on the one path that D-024's leak taught this class to keep. RebuildRows is GONE from that path, not moved: a closed panel has no rows, and the next open rebuilds them against the balance as it will be then. The FAILED paths (verdict != Ok, wallet refusal) still leave the screen exactly as it was -- closing on a refusal would read as a purchase that happened. D-048: the purchase's last call is grounds.RefreshAfterPurchase(item), not Refresh() -- the shop passes WHAT was bought and nothing about how it should look, so the drop-and-shake lives entirely in the grounds and this screen stays a thing that spends money and asks two views to catch up. D-049 moved this call BEFORE SetOpen(false) and the order is load-bearing: RefreshAfterPurchase claims the map's framing, and SetOpen's ClearGhost is the request to give it back -- closing first started the zoom-out before the grounds had heard about the purchase, so the prop landed on a map already travelling away from it. Same frame, no render between, so the panel and the drop begin together; SetOpen's ClearGhost is now a no-op on both halves by design, the ghost having been destroyed by the redraw above. D-071 adds PurchaseRefused on BOTH refusal branches -- Evaluate's non-Ok verdict and the wallet's own refusal. One moment rather than one per verdict, because to the hand "not enough money", "area locked" and "already owned" are the same answer; which one it was is the screen's job to say. The wallet branch is unreachable in practice today and is wired anyway, so the two paths cannot start feeling different if it ever is reached. D-108 (2026-08-27) gave it THREE exits, all one transition: the market button, a new optional closeButton (an X in the sheet's corner) and a new optional backdropButton (a tap outside). Both new ones call SetOpen(false) and never SetActive -- visibility keeps its single owner, the property whose absence leaked the first shop's ghost (D-024/D-027), and a button touching `panel` directly would skip ClosePreview and CancelPendingReopen. `panel` now points at the full-screen BACKDROP with the sheet as its child, so the block and the list come and go together and the outside-tap needs no hit-test (the sheet carries its own raycast-target Image, so UGUI gives it the taps that land on it). NOT a re-do of D-038, which removed a backdrop from the CONFIRM POPUP at the user's request -- the preview still has CANCEL as its only way out. | h:bc29e44e
ExpoTheExplorer/Assets/Scripts/UI/MetaShopRowView.cs | one shop row: icon, name, price, BUY | sys: MetaSystem | api: Bind(Sprite icon, string displayName, int price, bool affordable, Color buyColor, Action onBuyTapped) | dep: - | used: MetaShopView,MetaShopSetup | crit: K2 | note: D-037: the BUY turns red when the row cannot be afforded, on top of the dim. The colour is handed IN, not chosen here -- both colours live on MetaShopView, because this class is a binder that decides nothing and two components each holding the same pair is a pair that can drift. It paints Button.targetGraphic, the button's own declared graphic: no new serialized reference, so no scene rebuild (which D-036 had just cost a round). Writing to Graphic.color survives the ColorTint transition -- Selectable applies the state tint to the CanvasRenderer and MULTIPLIES with Graphic.color rather than overwriting it, which is also why the authored green shows through. S5 (D-035) added the dim: an unaffordable row fades via a CanvasGroup alpha, which over the panel's dark background reads as darker -- the user's wording. ALPHA only, never interactable: the row's BUY must still open the preview, because seeing where a prop would go and what it costs is what makes it worth saving for, and disabling the row would make it dead furniture. The popup's BUY is what refuses, visibly. Pure BIND component, the same shape ModificationSlotView has under TicketCardView and for the same reason: how many rows there are is something only the catalog and the player's purchases know, so they cannot be laid out in the Editor ahead of time. Decides NOTHING -- whether an item is listed comes from MetaPurchase.ShopItems, whether it can be afforded will come from MetaPurchase.Evaluate in S5. The BUY listener is wired ONCE in Awake and Bind only swaps the stored callback: adding the listener inside Bind would stack silently the first time a row is rebound, and a BUY that fires twice is the worst bug this screen could have. A null icon HIDES the Image rather than leaving a white box -- MetaCatalogValidator already reports a missing sprite as a content error, so this row does not add a second verdict about it. Price is a bare number: the HUD's coin icon is the unit on this screen | h:cf091478
ExpoTheExplorer/Assets/Scripts/UI/ModificationSlotView.cs | MISSING-role | sys: ? | api: SetModification(Sprite ingredientSprite, Sprite directionSprite, Color boxColor) | dep?: Editor,TicketCardView | used: ? | crit: ? | note: auto-added; AI must complete | h:3f9470ae
ExpoTheExplorer/Assets/Scripts/UI/SoftMoneyView.cs | HUD soft-money counter | sys: ProgressionSystem | api: - | dep: HudWalletSource | used: HudCanvasPrefabSetup | crit: K3 | note: reactive in the day scene (binds SoftMoneyChanged, never polls); on the main screen there is no event to bind to, so the value renders once. Holds no GameManager since D-013 -- see HudWalletSource for why a shared prefab cannot serialize one. Reads through the SOURCE so one call serves both modes; still subscribes from Start, not Awake | h:601afe0f
ExpoTheExplorer/Assets/Scripts/UI/TicketCardView.cs | one ticket slot's card: content, timer bar, danger flash | sys: TicketSystem | api: IsAnimating; SetTrayAnimating(bool animating); Initialize(GameManager gameManager, int slotIndex, TicketCardsView owner, BoardAnimationConfig animConfig); SuppressPollUntilDeliveryTransition(); PlayDeliveryTransition() | dep: BoardAnimationConfig,FoodCategory,FoodItemConfig,GameManager,GameState,ModificationSlotView,PatienceType,Ticket,TicketCardsView | used: TicketCardsView,TrayFillCounterView,WorldTrayView | crit: K2 | note: D-083 (2026-08-26) exposed FirstModificationRow, read-only and for POINTING AT: the tutorial's canvas-side arrow has to indicate the modification on the card, and the rows are built here at runtime so there is nothing in the scene to reference instead. It hands back the RectTransform rather than the ModificationSlotView so a caller gets a position and a size and no way to repaint the row -- the rows are rebuilt from the ticket on every refresh, so an outside write would vanish on the next one anyway. DANGER FLASH (D-073, rebuilt by D-074): once owner.IsInDangerZone says the bar has turned red, the ticket's PAPER (background.color) square-waves between the visuals config's two authored colours while the optional dangerImage blinks hard on/off in the same phase -- Mathf.Repeat off Time.time, so all three cards share one rhythm. D-074 replaced D-073's alpha pulse outright: dimming the CanvasGroup faded the customer name, the dish photo and the timer bar along with the paper, so the card was least readable exactly when it mattered, and a cosine ramp read as fading rather than as an alarm. Nothing here writes canvasGroup.alpha any more -- the delivery transition is its sole owner again. background.color returns to the PREFAB's own tint (captured once in Initialize), never to the flash's rest colour, which is a different thing. Computed inside RefreshTimer rather than run as a looping tween, and that is a lifetime decision: PlayDeliveryTransition kills and re-fades this same CanvasGroup, so a loop tween would fight it for the alpha, while Update is ALREADY silent for the whole transition -- the suppression that exists does what a tween would have needed new bookkeeping for. A 0 period means solid-on, never a clamped strobe. dangerImage is the ONE optional reference here (ValidateReferences would take the whole card down over a decoration that has a working degraded mode) and warns once at Initialize instead. polls its slot every frame because TicketDelivered/TicketCancelled fire BEFORE the slot is reassigned and FillEmptySlots fires nothing; the timer bar drains a Filled Image over the ticket's whole TimeLimitSeconds and clones one anchored divider tick per segment boundary, so an unevenly-divisible limit ends short rather than shifting the ticks; an empty slot hides the WHOLE card through canvasGroup.alpha=0 (never SetActive, which would pull it out of the parent HorizontalLayoutGroup and slide the other cards) and PlayDeliveryTransition's entry fade targets 0 instead of 1 when no ticket replaced the delivered one | h:dd5fe95a
ExpoTheExplorer/Assets/Scripts/UI/TicketCardsView.cs | spawns and owns the 3 ticket card slots | sys: TicketSystem | api: TicketSpriteFor(PatienceType patienceType); ModificationBoxColorFor(PatienceType patienceType); DirectionSpriteFor(bool isAddition); TimerSegmentSeconds; TimerFillColorFor(float remainingRatio); IsInDangerZone(float remainingRatio); DangerFlashColorFor(bool lit); GetCard(int slotIndex) | dep: BoardAnimationConfig,EconomyConfig,GameManager,GameState,PatienceType,TicketCardVisualsConfig,TicketCardView,TrayFillCounterView | used: TicketCardView,WorldTrayView | crit: K2 | note: D-074 added DangerFlashColorFor, which takes a HALF (lit/rested) and answers with a colour: the card asks which side of the blink it is on and holds no opinion about how loud the alarm is, which keeps this the single place PatienceType and the timer turn into visuals. D-073 named the critical tier as IsInDangerZone and made TimerFillColorFor CALL it, so "the bar is red" and "the card is pulsing" are one expression rather than two comparisons free to drift; no third threshold was authored, because a danger number of its own would be one the player can already see the wrong answer to. the single lookup point turning PatienceType (and the timer's remaining-time ratio) into visuals -- TicketCardVisualsConfig deliberately carries the values but no lookup logic; TimerFillColorFor takes its two THRESHOLDS from gameManager.EconomyConfig (they also pick the tip tier) and only its colours from the visuals config | h:b8ff4b13
ExpoTheExplorer/Assets/Scripts/UI/TrayFillCounterView.cs | MISSING-role | sys: ? | api: Initialize(GameManager gameManager, int slotIndex, TicketCardView ticketCardView) | dep?: GameManager,TicketCardView,TrayManager,WorldTrayView | used: ? | crit: ? | note: auto-added; AI must complete | h:e057ae2e
STALE ExpoTheExplorer/Assets/Scripts/UI/WorldTrayView.cs | one ticket slot's tray, its drop target, its return-to-board path, and (if it is the target) the tutorial spotlight's builder | sys: TraySystem | api: OnDrop(PointerEventData eventData); TryAcceptDrop(BoardItemDragHandler dragHandler); ReturnItemsToBoard(IReadOnlyList<BoardItem> items); SetHighlighted(bool active) | dep: BoardAnimationConfig,BoardItem,BoardItemDragHandler,BoardView,FoodCategory,GameManager,GameState,Ticket,TicketCardView,TicketCardsView,TrayManager,TutorialDirector,TutorialStep,TutorialSpotlightView | used: AutoCollectRunner,BoardItemDragHandler | crit: K2 | note: D-110 (2026-08-27) ReturnItemsToBoard is Auto-Collect's return path: items this ticket never asked for go back onto the board so the tray becomes completable again and the item rejoins the pot. WHICH items is PowerupEffects.UnwantedTrayItems' answer, handed here by instance -- this method only carries it out, because this tray is the only object that can: a tray item's GameObject is a child of one of these three slots, and removing it from TrayManager without also taking the object out would leave the item drawn in a tray that no longer holds it. The move itself is the one BoardItemDragHandler.OnEndDrag already performs for a tray pickup dropped somewhere invalid (remove from tray, RequestSpawn under a fly-in override from this tray, destroy the object), reused rather than reinvented so a powerup-returned item lands exactly like a hand-returned one. Three guards, each closing a hole rather than being defensive: it refuses while deliveryInProgress (the model is already a ticket ahead), it skips an item the player is holding mid-drag (BoardItemDragHandler is the single writer of a held item's transform -- the same reason ClearSlot checks IsDragging), and it RESYNCS lastKnownCount, because Update's poll reads a drop to exactly zero as "cleared externally" and would run ClearAllSlotVisuals over objects that are already gone. D-100 (2026-08-26) a successful delivery now WAITS for the item that caused it to reach its slot: TryAcceptDrop hands PlayDeliverySuccess to BoardItemDragHandler.PlaceInSlotAndDeliver as the settle tween's completion callback instead of calling it inline. The user's report was exactly that -- the ticket left without waiting for the last item to go to its place, because that one item was the only one never seated: it grew and lifted from wherever the finger let go. It is an ordinary slot child now, so PlayDeliverySuccess stopped taking a finalItem at all and treats it like any other tray content. Two guards come with the wait, and both close holes the longer window opens rather than adding a feature: deliveryInProgress makes this tray refuse every drop from acceptance until the slots are emptied, because the MODEL is already a ticket ahead -- an item accepted in that window would be counted by TrayManager and then destroyed unseen by DestroySlotChildrenImmediate; and Update releases that flag if the delivering item is destroyed before its tween completes (a retry), since DOTween KILLS such a tween rather than completing it, and without the release this tray would refuse drops for the rest of the session. The wrong-order path is deliberately untouched and still shakes the un-parented final item the instant the batch resolves: that beat reads as an interruption on purpose, and seating an item about to be thrown back onto the board would only lengthen it. D-098 (2026-08-26) IsTutorialTarget asks TutorialDirector.SpotlightTraySlotIndex instead of comparing Current.TargetTraySlotIndex itself. This was the project's one reader of a step's fields that did not gate on Kind first, and a panel step leaves those fields unset -- unset being 0, tray 0 claimed the powerup panel and tried to raise a ghost from the equally unset cell (0,0), which is the LogError the user saw. The two failure sentences in RaiseTutorialSpotlight only became true again with this fix: "the model has an item the view never built a container for" was being printed about a cell no step had named. D-083 (2026-08-26) the subscription moved from "a tutorial was armed" to "the step changed", which is what makes a tray both RAISE its spotlight when it becomes the target and TEAR ITS OWN DOWN when it stops being one -- every tray answers the same event and a tray that is neither does nothing, so the three need no coordination. The drop path deliberately does NOT dismiss anything itself: NotifyTrayAccepted publishes the boundary, every tray dismisses synchronously, and only a tween tick later does the new target raise the next spotlight. One mechanism for the boundary rather than a second copy of it. D-082 (2026-08-26) gave this class the tutorial's drop gate, the step's completion call, and the spotlight. The SPOTLIGHT lives here for a mechanical reason, not a tidiness one: this is the only object that already holds both of the ghost's endpoints -- a serialized boardView for the source cell and its own transform for the destination -- which is what let the whole effect ship with no new scene object and nothing to wire. It subscribes to GameManager.TutorialArmed AND syncs once in Start, because the first arm happens in Awake (before any subscriber exists) while a retry re-arms mid-scene (long after every Start has run); either half alone misses one of the two. The build is deferred one tween tick since BoardView creates item containers in its OWN Start and Unity does not order Starts between root objects. The drop gate refuses by returning false, which is already this method's "not accepted" answer, so TraySystem never learns tutorials exist. world-space on purpose, NOT Canvas UI, so a dragged item's real GameObject can be reparented straight in; a standalone scene object per slot with slotIndex set by hand and ONE BoxCollider2D for the whole tray -- which of the 3 slot transforms an accepted item lands in comes from its FoodCategory, and ResolvePlacementSlot overflows to another empty slot so two same-category items never render stacked; polls TrayManager's count every frame ONLY to catch a timeout scatter, reading count==0 as "externally cleared" because a manual pickup only ever decrements by one; the wrong-order shake (IsShakeable) and ClearSlot both skip an item the player is currently holding -- a second writer on a held item's transform is what pinned one at the tray's X while the finger moved away | h:575aa0dc
ExpoTheExplorer/Assets/Scripts/UI/MetaBackdropView.cs | day scene backdrop: the decorated grounds, blurred | sys: MetaSystem | api: - (all private; built once in Start) | dep: MetaResolver,MetaLayout,MetaCatalog,MetaLocation,GameSession,SessionHost | used: MetaBackdropSetup | crit: K3 | note: D-050. The day is played AT the expo the player decorates, so SampleScene's background becomes their own grounds -- the location art with the props they OWN standing on it -- blurred behind the board. Built ONCE in Start and never again: a purchase shows up in the NEXT day's backdrop, not halfway through the current one, which is correct because the grounds are not something the player can change while a day runs. After Start it is a small texture on a RawImage, so the per-frame cost is one draw call. NO CUSTOM SHADER: the blur is progressive halving through Graphics.Blit, and a bilinear sample at half size lands exactly between four source texels, so each step is an exact four-texel box average. That is the right answer HERE specifically because the project runs the BUILT-IN pipeline -- the URP package is installed but no pipeline asset is assigned in GraphicsSettings -- so renderer features are unavailable and the alternative was a multi-tap shader running full screen every frame for an image that never changes. The result is left SMALL: stretching it across the screen is itself a bilinear filter, so the upscale finishes the blur for free. Composed at composeScale (0.25) because the output is about to be blurred into soft shapes and full resolution would spend memory on detail thrown away one step later. WHICH location and WHAT stands on it are MetaResolver's two calls, the same ones the meta screen makes, so the backdrop cannot show a different expo than the one the player walks around in; it reads the catalog and the owned set and writes NOTHING. Every failure path leaves the RawImage exactly as the author left it -- that IS the fallback, and there is deliberately no fallbackTexture field, because a second place to author the same thing is a second place for it to be wrong. The final RenderTexture is OWNED and released in OnDestroy (graphics memory is not garbage collected, and a scene reloaded a few times would leak one backdrop per load), and it is cleared off the RawImage first so nothing draws a released texture. Sprites are drawn through the sprite's own textureRect, not the whole texture: drawing the whole texture works right up until the art is packed into an atlas, and then every prop becomes a collage of its neighbours. | h:67b027e3
ExpoTheExplorer/Assets/Scripts/UI/DayRewardFlightView.cs | the day's payout, performed: stars seat, gems and coins fly to the HUD | sys: DayLifecycle | api: Play(IReadOnlyList<GameObject> starsToSeat, RectTransform coinOrigin); CompleteImmediately() | dep: StarScoreBarView,GameManager,DayRewardPurse,HapticsBinder,HapticConfig | used: DayCompletePopupView | crit: K2 | note: HAPTICS (D-070): one buzz per star, requested on the frame the star POPS rather than when its bounce settles, because the pop is what the eye reads as the landing and the two senses agreeing is the point. Three in a row is the day's crescendo -- which is why the day's success is felt HERE and not on the popup opening, and why DayCompleted was left out of the haptic table entirely. The COIN landings deliberately have no haptic: their count scales with the day's earnings, so one per coin would turn the phone into a buzzer; if they are ever wanted it needs a rule (first + last, or every Nth), not a call site. D-057. Nothing is credited during a day, so THIS is what hands the earnings over -- each landing icon calls GameManager.ClaimRewardGems/ClaimRewardSoftMoney, which is why the HUD views needed no change at all: they are already reactive, so what the player watches arrive and what they own are one event instead of two kept in step. Amounts are never passed in; they are read from GameManager's pending purse, so this class cannot disagree with the ledger. It CANNOT lose money -- every exit from a finished day commits the remainder first, so an unwired reference, a skipped sequence or a mid-flight tap all still pay in full; killing a tween deliberately does not run its OnComplete, so a destroyed icon never claims its share and the commit pays it as one lump instead. Two ordering rules are load-bearing: CompleteImmediately guards on IsPlaying, or the call at the top of Play would commit the purse OnDayCompleted had just filled; and RunSequence clears starsBeingSeated before its own commit so a later tap cannot re-enter. Relies on Unity's Awake/OnEnable-before-Start guarantee for the purse to exist when Play runs (GameManager subscribes DayCompleted in OnEnable, the popup in Start) -- if that ever inverted the animation would render an empty debt and the exit would still pay, so the failure is degraded rather than broken. Lives on the always-active popup object, NOT under popupRoot (a coroutine dies with its GameObject); flightRoot is outside popupRoot too, which also makes icons draw above the popup. Icons are built in code from two serialized sprites rather than a prefab -- 13 per completed day is event frequency, so pooling would be cost with no saving. Positions cross two canvases (receipt on the popup, counters on the shared HUD prefab), so everything converts through screen space; CameraFor walks up from a reference already handed in, which is not a scene lookup. ONE flightRoot serves all three stars -- it is the coordinate space the icons live in, not a launch point; each gem is measured off the star that just seated, so the three seats give three different origins. ToFlightLocal takes a plain Transform rather than a RectTransform so a non-UI star still launches its gem: the earlier cast-and-branch skipped silently, which is the wrong failure here (the gem vanishes, the money still arrives on the exit commit, and nothing on screen says why) D-061: an OPTIONAL score bar now PACES the star loop -- each star waits on the fill reaching its own notch instead of a fixed starSeatInterval, so the thing that earned the star and the star itself are one motion rather than two animations that coincide. The gem shares are untouched (still per star index), so money cannot drift out of it. Three guards keep the wait from hanging a coroutine that is holding the day's payout: it also breaks on IsFillFinished, an unwired fill Image reads CurrentFill 1, and an out-of-range notch reads as the right edge. Unwired, the fixed-interval rhythm is kept verbatim. D-071 adds GemLanded and CoinLanded, both requested on ARRIVAL inside Launch's onArrive rather than at launch: the buzz belongs to the counter ticking up, which is when the money becomes the player's. COINS ONLY BUZZ FIRST AND LAST, and that is arithmetic rather than taste -- coinSpawnInterval is 0.06s and there are up to coinCount of them, so one haptic per coin fires faster than a preset lasts and the hand would feel half a second of undifferentiated rumble instead of ten ticks. Two beats say it better: the money started arriving, and it has finished. Gems need no such rule (at most three, spaced by the star rhythm). The first/last flag is captured into a PER-ITERATION local because this is a `for` loop -- closing over `j` itself would read whatever it had reached by the time the flight lands. | h:9000971d
ExpoTheExplorer/Assets/Scripts/UI/StarScoreBarView.cs | the star score as a fill bar, and the clock the star row runs on | sys: DayLifecycle | api: CurrentFill; IsFillFinished; PositionOf(int starIndex); Prepare(float scoreProgress, float twoStarDuePosition); BeginFill(); SnapToTarget(); ShowTargetWithoutAnimating() | dep: - | used: DayCompletePopupView,DayRewardFlightView | crit: K3 | note: added by D-061. Full == three stars, not 100% of the score: a bar that keeps filling past the last star communicates nothing. D-062 REMOVED its other half: it no longer positions the stars (three marker fields + PlaceMarker are gone), because the user rejected stars standing on their thresholds -- three 100px stars across a 450px bar stop reading as a three-star rating. A star's position is the scene's business now and no code writes one. Deleted rather than left unwired on purpose: PlaceMarker no-oped on a null marker, so the old behaviour would have been one drag away, and the scene's three markers all sat at the same point (0,0 inside Fill) so clearing the slots would have stacked the stars. It also PACES the seating: the flight waits on CurrentFill reaching PositionOf(i) instead of counting fixed intervals, which is what ties "my speed earned that star" to the star that pops. CurrentFill returns 1 when the fill Image is unwired, deliberately -- this value gates a sequence holding the day's payout, so an unwired bar must let it straight through rather than stall it | h:3f855661
ExpoTheExplorer/Assets/Scripts/UI/HapticButton.cs | makes one Button buzz when tapped | sys: HapticsSystem | api: - (no public surface; it hooks its own Button's onClick) | dep: HapticsBinder,HapticMoment | used: - (attached in the editor, never referenced from code) | crit: K3 | note: the dep? draft's five view classes are all COMMENT mentions -- this file references none of them, which is exactly its reason to exist. It replaces what would have been a haptics field plus a Request call in each of MainScreenView, DayCompletePopupView, GameOverPopupView, MetaShopView, MetaShopRowView, MetaGroundsView and NoKeysPopupView. THE DECIDING REASON IS NOT TIDINESS: the shop's BUY button lives on an INACTIVE template row that MetaShopView clones per offer, so the alternative design (a Button[] list on HapticsBinder) would have missed the button most worth feeling -- scene lists cannot name objects that do not exist yet. A component on the template rides along with every clone, and because Awake never runs on the inactive template, only the live rows register. The moment is a serialized field rather than a fixed UiTap so one button can be given its own character without code. Listener order against the view's own handler is irrelevant by construction: same frame, so HapticsService picks on priority and UiTap (5) sits below every outcome | h:c104933c
STALE ExpoTheExplorer/Assets/Scripts/UI/LifeLostHeartView.cs | a broken heart rising off the tray that cost a life | sys: LivesSystem | api: Show(int slotIndex) | dep: BoardAnimationConfig,GameState | used: GameManager | crit: K3 | note: D-078, reshaped by D-079 and D-080. THE AUTHORED OBJECT IS THE ANIMATED OBJECT: one INACTIVE canvas Image per slot, placed OVER that tray at the exact start position, switched on and drifted up and put back -- no template, no cloning, no anchors, because the thing you place should be the thing you see. Rest position and colour are captured once in Start and re-applied on every play, so a second heart never starts halfway up and half faded (a running flight is DOKilled and restarted from rest). D-080 moved it from a world SpriteRenderer inside the tray to a canvas Image over it, which killed two hazards at once: a sprite parked in the tray had to win a silent sorting-order fight with the tray (order -2) and its contents, and it sat under animations that sweep every child renderer. A canvas draws over the world, and these are not tray children, so there is no second writer on this alpha BY CONSTRUCTION rather than by checking. Rise is an anchoredPosition tween in RectTransform units (the ticket card's own 150-unit scale), never world units. Driven from GameManager.HandleLifeLoss because the tray sees only HALF the losses -- its poll notices a timeout only when there were items to scatter, so an untouched ticket expiring leaves no trace there, which is why the slot index was threaded through TicketSlotManager and TrayManager. An empty array slot warns and shows nothing: there is no sensible fallback position for a heart whose whole point is the position the author gave it | h:de0a3a78
ExpoTheExplorer/Assets/Scripts/UI/PowerupBarView.cs | the day scene's three powerup buttons + their charge counts | sys: PowerupSystem | api: - | dep: TutorialPowerupIntroView,PowerupConfig,GameManager,PowerupManager,PowerupType,PowerupButton,PowerupShopView | used: - | crit: K3 | note: D-087 (2026-08-26) this view builds the tutorial's powerup-intro step, for the reason the target tray builds a move step's spotlight: it is the object that already holds what the step needs. The three icons are the sprites on the three live Buttons, and nothing else in the project knows where those are. The button fields stay PRIVATE -- only a read-only IconFor is added, because a caller has no business reaching the Button itself, which is what spends a charge. Every failure path (no font, no config) skips the step rather than leaving a modal blocking a frozen day. powerup-plan Adım 2. SPENDS, never sells -- buying is the main screen's job, so there is no price and no purchase path here. BUILDS NOTHING: the panel, the three buttons and their Count labels are authored by hand in SampleScene and every reference is dragged, so no setup script exists (a generator would be a second authority over objects the author already arranged). Three NAMED blocks rather than an array because the scene's sibling order (AutoCollect, LeakCleaner, TimerReset) is NOT the enum's -- an array would pair the middle button with the middle enum value and put the wrong count under the wrong icon, which reads as a balancing oddity rather than a wiring bug. Dimming is keyed on the CHARGE COUNT, deliberately not on PowerupManager.CanUse: CanUse also asks whether an effect is registered, and effects only arrive in Adım 4-6, so gating on it would draw all three dead from day one. `interactable = false` is what makes an empty press a no-op with no branch in the handler, and is why there is no popup here (an empty key locks a player out of playing; an empty charge costs a convenience). Start not Awake, the project-wide rule -- GameManager.Awake builds the session and Unity does not order Awake across objects. The scene object is called LeakCleanerButton while the type is NoiseClear; same thing, and renaming a third time would have cost another save migration. D-105 (2026-08-27) REVERSED "pressing an empty powerup does nothing": with an optional PowerupShopView dragged in, a powerup at 0 stays interactable, lights the author's inactive "Add" child (a new optional emptyBadge per block) and opens that shop, holding the day still through GameManager.HoldPause/ReleasePause while it is up. The branch is read from the LIVE charge count in OnPressed, not from what Render last drew, because the count can move under an open shop. Order is load-bearing: shop.Open() must SUCCEED before the pause is taken, or a day freezes with nothing on screen to lift it. Gated on GameManager.CanUsePowerups, the same gate a USE passes. Fails all the way back to the old behaviour with no shop wired -- dim, unpressable, no popup -- and ShopIsReachable also requires a PowerupManager, since Start attaches no click listeners without one | h:74904297
STALE ExpoTheExplorer/Assets/Scripts/UI/PowerupShopView.cs | powerup shop panel: three rows, owned count, Gem price, BUY | sys: PowerupSystem | api: Closed; Open(); Close() | dep: SessionHost,GameSession,GameState,PowerupManager,PowerupType,PowerupShopRow | used: - | crit: K2 | note: powerup-plan Adım 3, the step that closes the economy loop (earn on day complete -> buy here -> spend in the day scene). BUYING IS MENU-ONLY by design, not layout: a store opened mid-service suspends the time pressure the powerup exists to relieve. Deliberately NOT a tab inside MetaShopView -- that file owns props + SoftMoney and belongs to MetaSystem, so a tab would teach MetaSystem's view what a powerup is and give one 30KB class a tab state, a second currency and a second row shape. It carries its OWN open button exactly as MetaShopView carries marketButton, which is what keeps MainScreenView unaware powerups exist and is why this step added NO new blueprint arrow. Three authored rows, no cloned template: MetaShopView clones because only the catalog knows the row count, here it is three and written in the enum. NO confirm popup, unlike MetaShopView -- a prop is expensive and a placement decision, a charge is a few Gems with a visible effect. Unaffordable BUY is non-interactable rather than dimmed-but-tappable (the opposite of MetaShopRowView) because that row's popup can EXPLAIN a refusal and this shop has none; a silent no-op tap would be the worst of the three. Subscribes to BOTH ChargesChanged and GemsChanged and redraws all three rows, because one purchase moves the shared balance and can change what the OTHER rows may do. Save() sits in the same method as the purchase (MetaShopView's rule): without it the player spends Gems now and finds the charge gone next launch. Panel hidden BEFORE validation (D-036: an incomplete shop must be invisible, not stuck open). Start not Awake, project-wide rule. D-105 (2026-08-27) made it serve the DAY SCENE too, which reverses "buying lives on the menu, not in the day" -- the objection (a store suspends the time pressure a powerup relieves) is answered by the day being FROZEN behind it, and this view still knows nothing about that: the caller holds the pause. Open() is public and returns whether the panel actually came up, Close() is idempotent and raises Closed, and openButton left ValidateReferences' required list because the day scene deliberately has no opener (an empty powerup is it). panel and closeButton stay required -- a shop with no way out is a trap when it is also holding the day still. OnDestroy raises Closed if the panel was still up, so a scene load mid-purchase cannot strand a caller's pause. D-107 (2026-08-27) added an optional backdropButton wired to Close, reversing D-105's refusal to let the scrim dismiss: a full-screen block with only a corner X reads as being trapped. It needs no inside/outside hit-test, and that is why it is a wired Button and not a pointer handler -- the sheet is a CHILD of the backdrop and carries its own raycast-target Image, so UGUI gives a tap on the sheet to the sheet and only an outside tap reaches the button. Wire it to the same object `panel` points at | h:c2e83bdc
STALE ExpoTheExplorer/Assets/Scripts/UI/AutoCollectRunner.cs | Auto-Collect's DOING half: returns tray junk, then carries out a closed plan | sys: PowerupSystem | api: Run() | dep: AutoCollectMove,BoardItem,BoardView,BoardItemDragHandler,GameManager,GameState,PowerupEffects,Ticket,TrayManager,WorldTrayView | crit: K2 | used: GameManager | note: D-110 (2026-08-27) a press is now three ordered steps instead of a per-slot search loop: (1) every tray's unwanted leftovers go back to the board via WorldTrayView.ReturnItemsToBoard -- BEFORE the snapshot, which is why a returned item is part of this press's budget rather than a phantom; (2) the active tickets are captured by instance and PowerupEffects.PlanAutoCollect decides the WHOLE press against the board as it stands; (3) the plan is walked in order. Each step is re-verified but never re-decided: the slot's ticket must still be the captured instance (its own last move may have delivered it) and the cell must still hold the exact BoardItem the plan reserved -- taking whatever landed there instead is precisely the bug this rewrite removes, since a delivery backfills the cells behind it. Run() still answers the "no wasted press" question, and it now counts a press that ONLY returned items as work: the board visibly changes and a tray that was guaranteed to cost a life is un-jammed (the user's call, asked and answered). MaxMovesPerSlot is gone with the loop -- a closed plan cannot spin. powerup-plan Adım 6. It calls WorldTrayView.TryAcceptDrop with the board item's OWN drag handler rather than writing into TrayManager, and that is not stylistic: a tray's contents are drawn entirely by the dragged GameObject being reparented, so a data-only collect would be perfectly correct in state and show an EMPTY tray. Reusing the drop path also inherits every rule already attached to it -- detach-before-batch-check, the delivery lift/fade, the card's exit, the tray's re-entrance -- none re-implemented, none able to drift. ONE PRESS = THE TICKETS IN HAND: the active tickets are captured BY INSTANCE first and a slot stops the moment its ticket is replaced, because filling a tray delivers it, which synchronously assigns a new ticket whose required items then spawn -- a loop that kept going would chain deliveries until the Day's sequence ran out. Reference equality, not slot emptiness, because the replacement occupies the same slot. The plan is re-asked every iteration for the same cascade reason. MaxMovesPerSlot is a hang guard, not a design number (a ticket needs at most three items) and logs if ever reached. NO EditMode test can cover this file -- Assembly-CSharp is predefined and no asmdef may reference it -- which is exactly why every rule was pulled into PowerupEffects. A paced version (one item every authored interval, via a coroutine) was written on 2026-08-26 and REVERTED at the user's request the same day; it is not in the code and this note is the only trace, so that the next reader knows the instant feel is the current state rather than an oversight D-100 (2026-08-26) a tray now refuses drops while it is delivering (WorldTrayView.deliveryInProgress). This file cannot trip that guard: the ReferenceEquals check above stops a slot the moment its ticket is replaced, which is the same instant the guard goes up. | h:f1c0e4be
ExpoTheExplorer/Assets/Scripts/UI/TutorialSpotlightView.cs | one tutorial step's look: dim, sorting lifts, ghost, arrows, message | sys: Tutorial | api: Create(TutorialDirector director, BoardAnimationConfig animConfig, Transform sourceItem, BoardItem sourceBoardItem, Transform trayTarget, IReadOnlyList<Transform> litExtras, IReadOnlyList<RectTransform> dimmedCards, TicketCardView targetCard); Dismiss() | dep: BoardAnimationConfig,BoardItem,ModificationConfig,TicketCardView,TutorialDirector | used: WorldTrayView | crit: K3 | note: D-086 (2026-08-26) THE CANVAS PREMISE THIS CLASS WAS BUILT ON WAS FALSE. It assumed a Canvas composites above everything the camera renders, so the target ticket card would stay lit for free -- true only for Screen Space - OVERLAY. This scene's InGameCanvas is Screen Space - CAMERA at sortingOrder -1, and the dim is a sprite at 500, so the entire canvas was drawn and then buried: the target card, its modification arrow, and the step message. Two fixes, one cause. The message now builds its OWN Overlay canvas (copying the game canvas's CanvasScaler so the authored font size still means the same thing), deliberately at sortingOrder -1 so it stays BELOW Popup Canvas -- a ticket can time out mid-step and a tutorial line floating over the Game Over popup would be worse than one hidden behind it. The target card is raised by adding a Canvas with overrideSorting at 800: above the dim, below a dragged item, and its arrow rides along as a child. That Canvas is removed on teardown -- the card is not ours to destroy, only the component we added. DimCard is KEPT even though the non-target cards now sit under the world dim anyway: it makes "the other cards are dark" hold by construction instead of by the current render mode, which is the exact assumption that failed here. D-085 (2026-08-26) THE MESSAGE'S PLATE IS THE PARENT AND THE TEXT ITS CHILD, and that order is this method's correctness rather than a style choice: UGUI draws a Graphic BEFORE its children, always, so the first version -- text on the outer object, plate parented under it -- painted the plate straight over the glyphs and no message ever appeared. SetAsFirstSibling did not save it, because sibling index orders siblings and a child is never behind its parent. BuildMessage now also LOGS every path on which it declines to draw (no card, no Canvas, no borrowable TMP font); it fails invisibly by nature -- no message is also what an unauthored step looks like -- so a silent return here is a bug that hides itself. THE TICKET ARROW SITS LEFT OF THE MODIFICATION ROW AND POINTS RIGHT, and the right-hand placement it replaced was wrong twice over: the arrow landed far from the ingredient icon it labels, and the 180-degree rotation used to aim it back turned it about its own pivot, swinging the body across the row and leaving the tip in the middle of the box. Anchoring the arrow's TIP to the row's left edge makes the placement independent of how wide the row is, and needs no rotation at all since the generated sprite already points right. D-083 (2026-08-26) one of these now exists PER STEP, and it gained the two arrows and the message. Dismiss() is the important addition: it restores the lifted sorting orders SYNCHRONOUSLY, because Unity defers Destroy to the end of the frame while the next step's spotlight is built immediately -- left to OnDestroy, a step boundary would restore the old step's renderers AFTER the new step had lifted its own. RestoreAll is idempotent so OnDestroy can still run it on the paths that never reach Dismiss (a retry, a scene unload). The arrow is drawn into a texture in code -- this feature still ships no art -- and the message borrows the ticket card's own font, so it matches the game's type with nothing wired by hand. The board arrow finds the modification's layer by INDEX into BoardItem.ResolvedLayers, which is BoardView's own construction order for the container's children; both ends say so. THE GHOST IS BORN DURING THE FLY-IN, which is why neither its size nor its origin is copied off the source item: at Day Start that item is running BoardView's pop-in from localScale ZERO at the Starting Point, and this clone is made a tween tick later -- captured naively, the ghost was invisible for the whole day and looped from a spot the item merely passed through. The scale comes from the item's PARENT (a board item rests at localScale one, so the parent's lossy scale is the resting size and is not moving) and the origin is re-read every lap, which is self-correcting and costs one property access. That lap callback null-checks the item because the drop that ends the tutorial destroys it while this object's OnDestroy -- which kills the tween -- does not run until end of frame. Built entirely at runtime and authored NOWHERE -- no prefab, no scene object, no art, nothing dragged into an Inspector; that is the whole reason D-082 (2026-08-26) had no editor step. IT NEVER WRITES A COLOUR IT DOES NOT OWN, which is the design decision worth keeping: tinting each renderer to darken the board would have lost to BoardView, which rewrites its layer colours on every RefreshCell, so the board would flicker back to full brightness on the next spawn. Instead a black quad covers the camera at sortingOrder 500 and the two lit things are lifted +600 above it -- under BoardItemDragHandler's +1000 drag boost, so an item the player actually picks up stays visible over the dim for free, with no cooperation between the two classes. Ticket cards need the OTHER half: Canvas UI composites above everything the camera renders, so the quad cannot cover them -- the non-target cards get a stretched black Image child each, which also leaves the HorizontalLayoutGroup undisturbed and lets the TARGET card stay lit by doing nothing at all. The ghost is a CLONE of the live board item (colliders and every MonoBehaviour stripped), which is why the effect needs no art and always shows the exact food and modifications being asked for. Restore lives in OnDestroy rather than the completion handler, so a retry or a scene unload mid-step cannot leave the game permanently dark | h:e98e7012
ExpoTheExplorer/Assets/Scripts/UI/TutorialPowerupIntroView.cs | the tutorial's powerup panel: three rows of icon + name + description | sys: Tutorial | api: Create(PowerupConfig config, Canvas gameCanvas, TMP_FontAsset font, Func<PowerupType, Sprite> iconFor, Action onDismissed) | dep: PowerupConfig,PowerupSettings,PowerupType,PowerupTypes | used: PowerupBarView | crit: K3 | note: THE AUTO-DRAFTED DEPENDENCY FIELD LISTED PowerupBarView AND THAT ARROW POINTS THE OTHER WAY -- the bar CREATES this view and is its only caller; this file names no view at all. Built at runtime and authored nowhere, like the rest of the tutorial: no prefab, no scene object, no art. It BUILDS ITS OWN SCREEN SPACE - OVERLAY CANVAS, which is the lesson of D-086 rather than a preference -- this scene's InGameCanvas is Screen Space - CAMERA at sortingOrder -1, so anything placed on it sits UNDER world sprites and gets drawn and then buried. It copies the game canvas's CanvasScaler so its proportions and font size resolve the way they do on a ticket card, and it carries its own GraphicRaycaster and a full-screen backdrop because it is MODAL: relying on the board gates to swallow presses would make this panel's correctness depend on a rule living in another system. Rows are positioned by hand rather than with a VerticalLayoutGroup -- three rows of known height need no layout pass, and a layout group would fight the explicit sizes the panel is built from. A powerup whose button has no sprite still gets a row (text is the part that teaches) plus a warning. Dismiss() clears the callback BEFORE invoking it, so a second press in the same frame cannot advance the tutorial twice | h:0746c57b
ExpoTheExplorer/Assets/Scripts/UI/MainScreenTutorialView.cs | the main screen's first-run welcome and store hint | sys: Tutorial | api: - (driven by its own buttons) | dep: GameSession,GameState,MetaShopView,SessionHost,TutorialTextConfig | used: - | crit: K3 | note: D-090 (2026-08-26) gained PointAtStoreAfterBlockedPlay, the entry point Play uses when it refuses. It is public because MainScreenView owns what Play DOES while this view owns what the store hint SAYS and looks like; the alternative was Play drawing an arrow of its own, i.e. a second place that knows where the store button is. It rebuilds the canvas and re-resolves the font if needed, because a press can arrive after the tutorial has torn itself down -- or on a profile that skipped it entirely, where Start returned early and left both unset -- and it no-ops when the hint is already up so a second press cannot stack a second arrow on the button. ITS CANVAS IS A ROOT OBJECT, NOT A CHILD OF THIS VIEW, and that is the fix for the bug the user screenshotted: this view sits under the screen's own Canvas, and a Canvas NESTED inside another Canvas does not get the screen's dimensions -- it inherits its parent's RectTransform, which here is zero-sized. Everything built inside was therefore crushed to zero width and TMP wrapped the hint one character per line down a sliver of the screen. Every proportion in this file assumes a screen-sized canvas. Two consequences of being root, both handled in Cleanup: the canvas no longer dies with this component, and neither does the ARROW, which is a child of the store BUTTON so it tracks the layout -- dismissing used to destroy only the plate and leave the arrow stuck on the button forever. The store hint's TEXT SIZE and ARROW SIZE are serialized (storeHintFontScale, storeArrowScale) rather than constants, and that is a considered exception to "no speculative knobs": three rounds of tuning them by eye showed neither can be judged from the code, only from the screen, so the author tunes them in the Inspector instead of through another edit. Calibration point for anyone changing them: the main screen's own body text sits at 30-34 against a 1080x1920 reference (~0.017 of its height), so a callout belongs meaningfully above that. D-089 (2026-08-26) the words moved to TutorialTextConfig and the wiring to a menu item; a missing config now SKIPS the tutorial with a named error rather than falling back to built-in strings, because a silent fallback would hide that the setup step was never run and would put the wording back in code. TWO LAYOUT BUGS were fixed after the user reported the store hint broken, and both are worth keeping as warnings: (1) THE ARROW WAS ROTATED ABOUT A BOTTOM-CENTRE PIVOT, which swung its whole body an arrow-length to the right -- and since the store button already sits in the bottom-right corner, that pushed it off the screen edge. Same mistake the ticket-card arrow made once; a centre pivot is what makes a rotation leave the thing where it was placed. (2) THE PLATE WAS TOO SHORT for its own contents: text occupied 1.9..4.0 and the dismiss button 0.8..3.3 of a 4.4-tall plate, so the button printed over the hint it was there to dismiss. The bands are now disjoint by arithmetic that is written down rather than eyeballed. THE AUTO-DRAFTED DEPENDENCY FIELD GUESSED MainScreenRoot, PlayerProfile AND TutorialDirector, none of which this file names -- it reaches the session through SessionHost (the same seam HudWalletSource uses) and never touches the save file. DELIBERATELY NOT the day scene's TutorialDirector (D-088, 2026-08-26): that system's whole surface is board cells, trays and drop gates, and this screen has no board, no trays and no Day, so reusing it would carry Day-shaped fields into a screen that has none. The two flows share a name and nothing else. "First opened" is DERIVED -- day index 0 and nothing owned -- rather than a once-ever profile flag, so there is no save-version bump and nothing to remember; it also re-offers itself to a player who ignored it, and stops for good the moment they play a day or buy a prop. THE WELCOME IS MODAL AND THE STORE HINT IS NOT, which is the load-bearing difference: the hint ASKS the player to tap the store, so the store must stay tappable, and opening it counts as dismissal. The arrow is parented to the store BUTTON so it follows the layout instead of a hard-coded corner -- which is also why Finish destroys it explicitly: it is the one piece of this view that lives outside this view's own hierarchy and would otherwise hang on the button forever. Its panel and the day scene's powerup panel are both "modal with text and a dismiss button", a repetition of TWO; abstraction-level.md's answer at two is to write it twice, and a shared builder waits for a third | h:20c2e249
ExpoTheExplorer/Assets/Scripts/UI/SettingsPopupView.cs | the day's pause menu: hold time, resume, retry, abandon, haptics switch | sys: Bootstrap | api: - (no public members; every entry point is a Button listener) | dep: GameManager,GameState,GameSession,DayLifecycleManager | used: - | crit: K2 | note: added by D-094. IT OWNS THE PAUSE: GameState.IsPaused is written here and nowhere else, and GameManager.Update reads it as a third gate beside IsAwaitingContinue and the tutorial's reading hold -- Time.timeScale is untouched, so only the ticket clock stops while tweens and UI keep running. It DECIDES NOTHING about what an exit costs: Retry and Main Menu call the same GameManager methods the Game Over popup calls, and leaving a day that was not lost spends no key by construction (SpendKeyForLostDay is gated on IsAwaitingContinue) -- no economy rule is invented here. TWO CONFIRMATION PANELS rather than one whose message code rewrites, because a shared panel would put both authored questions into C# and break the root invariant; same reason the day caption receives only a NUMBER. Refuses to open while IsAwaitingContinue, and the open button GREYS OUT then -- interactable only, never SetActive, since D-103 (LivesDepleted/LivesChanged/DayCompleted/TicketAssigned, no per-frame check) rather than standing there fully lit and refusing; Start switches the object on once and nothing touches its active state again. That LivesChanged edge depends on LivesManager clearing IsAwaitingContinue BEFORE it writes Lives -- the publish is synchronous, and the other order left this button dead for the whole retry. Close() is the single place the pause lifts, and ConfirmRetry calls it BEFORE RetryDay for a real reason: that path resets the day in place with no scene load to tidy up, so a pause left standing would freeze the fresh attempt with no menu on screen. The haptics switch writes GameSession, never disk -- a mid-day save would bank an unfinished day's earnings. D-105 (2026-08-27) took the assignment away from it: Open/Close now call GameManager.HoldPause/ReleasePause, because the powerup shop can freeze a running day too and two views writing one bool would let whichever closed first start the day under the other. OnDestroy releases as well, so a view destroyed on its own cannot leak a hold | h:ce045dd1
