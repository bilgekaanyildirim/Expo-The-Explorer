<!-- stamp: 6dbaf65 2026-08-31 status: DEGRADED 5 stale, 0 orphan, 85 missing-role -->
<!-- The twelve one-shot scene/prefab builders under Assets/Scripts/UI/Editor (ConfettiSetup,
     HudCanvasPrefabSetup, MainScreenSceneBuilder, MainScreenSessionSetup, MainScreenTutorialSetup,
     MetaBackdropSetup, MetaGroundsSetup, MetaShopSetup, MetaUnlockPopupSetup, PowerupShopSetup,
     SettingsPopupSetup, TutorialPopupSetup) were DELETED on 2026-08-30 (audit R-02). They are out of
     every dep:/used: field below. A note: that still names one is describing HISTORY -- how a prefab
     or scene came to exist -- not a menu step you can run. The scenes and prefabs on disk are the
     only copy now; the builders live in git history. -->
ExpoTheExplorer/Assets/Scripts/UI/BoardItemDragHandler.cs | one board item's pickup/drag/drop gesture, refusable per gesture | sys: BoardUI | api: CurrentItem; IsDragging; WasAcceptedByTray; Configure(BoardGrid board, Camera dragCamera, BoardView boardView, GameManager gameManager, DragFeelSettings dragFeel, BoardAnimationConfig animConfig, HapticsBinder haptics); SetCell(int x, int y, BoardItem item); OnPointerDown(PointerEventData eventData); OnPointerUp(PointerEventData eventData); OnBeginDrag(PointerEventData eventData); OnDrag(PointerEventData eventData); OnEndDrag(PointerEventData eventData); DetachFromBoard(); SettleForAutoCollect(); PlaceInSlot(Transform slotTransform, int slotIndex, float seatScale, float travelMultiplier = 1f); PlaceInSlotAndDeliver(Transform slotTransform, int slotIndex, Action onSettled, float seatScale, float travelMultiplier = 1f); ReleaseAndDestroy() | dep: BoardAnimationConfig,BoardGrid,BoardItem,BoardView,GameManager,WorldTrayView,HapticsBinder,HapticConfig | used: BoardView,WorldTrayView | crit: K2 | note: D-140 (2026-08-30) A TRAY ITEM IS DRAWN AT THE TRAY'S SIZE, NOT THE BOARD'S. SeatInSlot tweens localScale beside localPosition now, over the same TraySettleDuration, and that one missing half is the whole reported bug: items arrived at cellSize (~1.09 world units) into a 1.48-unit tray, so three of them could never sit apart no matter where the slots were, and their cellSize-square colliders overlapped by ~70% -- Physics2DRaycaster answers with the NEAREST hit only, so which item a finger got was decided by depth rather than by aim. The caller supplies seatScale because only it knows the tray's real world size; boardScale and boardHitSize are snapshotted in Configure at container creation rather than written as Vector3.one/cellSize constants, since BoardView owns both facts and a second copy here would be a second authority. homeScale is deliberately UNCHANGED and is now the TRAY scale for a seated item, which is right for both its readers (a tap that never became a drag, and a snap-back -- both leave the item in its slot); only the pickup GROW target switches to boardScale, since an item leaving a tray is headed for a board cell or another tray. The hitbox needs no resize to track the item -- collider and sprites are both exactly one cell, so both shrink by the same seatScale -- and only the authored multiplier is applied. OnEndDrag's scale reset gained !WasAcceptedByTray beside !deliverySuccessInProgress: on a DIRECT tray hit OnDrop->PlaceInSlot->SeatInSlot has already started the seat tween by the time UGUI calls it, and undoing the pickup grow there would write over the seat's target. The fallback drop path is unaffected because it runs after the reset. D-113 (2026-08-27) SettleForAutoCollect is the pickup half of the gesture for a caller with no finger, and it exists because homeScale WAS NEVER WRITTEN on that path: the field is assigned in exactly one place, ApplyPickupVisuals, reached only from OnPointerDown. PlaceInSlotAndDeliver -- and only it, the drop that COMPLETES an order -- then applied that unwritten field as `localScale = homeScale`, i.e. scale ZERO, so the item that finished a ticket was invisible for the whole delivery animation and then destroyed. PlaceInSlot never touches the scale, which is exactly the asymmetry the user saw: the rest of the tray looked right. The DOKill(true) half lands any fly-in still running, for the reason ApplyPickupVisuals already documents -- completed, the item sits at its own cell at full scale, which is both a trustworthy snapshot and where a finger would have taken it from; left running, BoardView's DOJump and the tray's settle write the same transform at once. homeScale's initialiser is now Vector3.one, since every board container rests at exactly that and zero was never a truthful default. D-112 (2026-08-27) PlaceInSlot/PlaceInSlotAndDeliver/SeatInSlot take a travelMultiplier that stretches the settle tween, defaulted to 1 so every finger drop is untouched and the number only exists on the path that asks for it -- Auto-Collect's, which sends several items off at once and needs them to read as separate flights rather than one scatter. D-100 (2026-08-26) the drop that COMPLETES an order is seated in its slot like any other, by PlaceInSlotAndDeliver, and the tray's delivery animation runs on that settle tween's OnComplete -- the wait is the tween ITSELF rather than a DelayedCall on TraySettleDuration, so the flight and the hand-off cannot disagree about how long the item takes to arrive. PlayDeliverySuccessAndDestroy went with it: that method existed only because the delivering item was never parented, so it grew and lifted on its own transform in parallel with the tray; now that it IS a tray child, the tray's own sequence carries it and running the old one too would double both the grow and the lift. deliverySuccessInProgress outlives the deletion and does more than it used to -- it keeps OnEndDrag from re-enabling the collider during the settle, on a tray TrayManager has already emptied and handed to the next ticket, so the item cannot be picked back up while nothing owns it. D-084 (2026-08-26) put the tutorial in front of the board-to-board relocation branch too, which is where the first two gates left a hole big enough to strand the day: the pickup gate permits exactly ONE cell, so parking the item on any other empty cell moved it beyond reach and the step could never be finished. The fix adds no movement code -- refusing the branch falls through to the existing snap-back, which tweens to homePosition and touches BoardGrid not at all, so the item returns to its starting cell in the view and never left it in the model. D-082 (2026-08-26) added `pickupRefused`, and it FIXED A LEAK rather than only serving the tutorial: refusing in OnPointerDown alone never stopped a drag, because UGUI still calls OnBeginDrag/OnDrag for the same gesture and their `CurrentItem == null` guards pass -- so an item the Continue popup had "blocked" was still picked up and flown around, with none of the pickup bookkeeping done. The comment above OnPointerDown claimed the opposite and was wrong. All five handlers now check the flag. The tutorial gate rides the same flag and applies to BOARD cells only, since cellX/cellY are stale leftovers while an item sits in a tray. HAPTICS (D-070): pickup buzzes from OnPointerDown AFTER the IsAwaitingContinue gate -- a press the Continue popup swallowed produced no visual reaction either, and a buzz with nothing on screen reads as a broken game. The tray-drop buzz sits in PlaceInSlot, which is deliberately NOT called for the drop that resolves the batch (that one goes to PlaceInSlotAndDeliver or ReleaseAndDestroy, neither of which buzzes); the gap is correct rather than a hole, since the resolving drop is the one that delivers or costs a life and both outrank the drop tick in the same frame anyway. The binder arrives through Configure like every other dependency here -- no lookup, no singleton -- and is null-checked at both sites, so a scene that never wired one drags exactly as before. added in code by BoardView per item container, needs a Physics2DRaycaster on the scene camera; the SINGLE writer of a dragged item's transform, and never touches BoardGrid mid-drag (only on drop) so it can't fight BoardView's CellChanged/RefreshCell cycle; ApplyPickupVisuals COMPLETES any live tween (DOKill(true)) BEFORE snapshotting homePosition/homeScale, because a wrong-order scatter's fly-in is delayed for the tray shake and parks the container on the tray at scale ZERO -- a plain kill froze it there, so the item dragged invisibly and OnEndDrag re-applied the zero; X follows the finger by accumulated delta with no absolute anchor, so any other writer touching a held item's X strands it D-071 adds DropRejected, and only on the two branches of OnEndDrag that actually refuse the drop: the tray-pickup-dropped-nowhere branch and the snap-back branch the GDD names. The relocation branch above them is deliberately SILENT -- moving an item to another empty cell is the drop working, not failing. OnPointerUp's settle-back is silent too, even though its own comment calls it "the same feel as an invalid drop": that path is a tap that never became a drag, and ItemPickup has already fired on the press, so buzzing again on release would make every stray tap a double. D-109 adds ItemPlacedOnBoard on the SUCCESSFUL placement branch, correcting D-071: that turn left this branch silent because "a relocation is the drop working, not failing" -- a correct observation with the wrong conclusion, since dropping into a tray is also the drop working and has always buzzed. What the silence produced was half a gesture, pickup answered and release not. Fired for BOTH origins the branch covers (board-to-board and tray-back-to-board) because ItemPickup does not care about origin either; splitting would make one gesture feel finished or unfinished for a reason the player cannot see, and `wasOnBoard` sits right above it if board shuffling proves noisy on a device. The drag now has four distinct answers: LightImpact picking up, RigidImpact onto the board, MediumImpact into the tray (the real move), SoftImpact refused. The seated collider is multiplied by the item's OverallScale for the same reason: the collider is a full CELL while the art in it is only OverallScale of one, so once the seat scale divides that padding out of the ART, a cell-sized box stands ~18% proud of its own item and reaches into the neighbour it is packed against. | h:6fff24c8
ExpoTheExplorer/Assets/Scripts/UI/BoardView.cs | runtime board grid renderer + drag/drop | sys: BoardUI | api: CellSize; ReleaseContainer(int x, int y); TryGetDragHandler(int x, int y, out BoardItemDragHandler handler); BeginFlyInOverride(Vector3? origin, float delay = 0f, float durationScale = 1f); EndFlyInOverride(); TryGetCellAt(Vector3 worldPosition, out int x, out int y) | dep: BoardAnimationConfig,BoardGrid,BoardItemDragHandler,BoardVisualsConfig,DragFeelConfig,GameManager,HapticsBinder | used: BoardItemDragHandler,WorldTrayView | crit: K2 | note: D-140 (2026-08-30) CellSize became public for ONE caller, WorldTrayView, and the reason is that this is where two coordinate spaces meet: a tray is a fixed-size world sprite, a board cell is derived from the camera here in FitToCamera, and turning an absolute authored tray size into an item scale is a division that needs both. Every item's sprite layers are fitted to a cell (ApplyFittedScale) and its hitbox IS a cell, so this one number describes a board item completely. It is zero until FitToCamera has run in Start -- the tray guards for that rather than dividing by zero, since a drop before this view exists cannot come from a finger. D-112 (2026-08-27) the fly-in bracket carries a durationScale beside its origin and delay, and RefreshCell multiplies PopInDuration by it for the appearances inside the bracket only. That is what lets an Auto-Collect return fly slowly while a spawn, a wrong-order scatter and a timeout keep their authored speed -- this class cannot tell those apart, and the bracket is the thing that knows. Passed rather than read from animConfig here, for that reason. HAPTICS (D-070): holds the scene's HapticsBinder only to hand it down to every item it builds -- it plays nothing itself. Optional, and one more argument on the Configure call that already carries five. reactive, no per-frame polling -- it rebuilds only the cells BoardGrid reports via CellChanged, and cellSize/boardOrigin are DERIVED from the orthographic viewport so the board fits any aspect. RefreshCell recomputes a fly-in destination from CellPosition(x,y,-0.1f) and never reads it off container.position: a reused container only still stands on its cell if every tween that ever moved it completed, so one interrupted fly-in used to strand it and bake that spot in as the cell's destination for good. A DELAYED fly-in (only a wrong-order scatter, held for the tray's shake) also disables the container's collider for the length of the delay -- otherwise it sits active, scale-zero and grabbable right on the tray, and grabbing it snapshotted that zero as the item's home scale. BeginFlyInOverride/EndFlyInOverride are a plain flag pair, NOT nested or counted, so callers must not overlap their brackets | h:8567e05f
ExpoTheExplorer/Assets/Scripts/UI/DayCompletePopupView.cs | day-complete receipt popup + star rating and its score breakdown + the payout handover + the three exits from a finished day | sys: DayLifecycle | api: - | dep: BoardAnimationConfig,ConfettiView,DayLifecycleManager,DayRewardFlightView,GameManager,GameState,PopupFade,StarScoreBarView | used: - | crit: K2 | note: D-131 (2026-08-29) fires the celebration confetti one line after popupRoot.SetActive(true), through ConfettiView.BurstBelow rather than BurstOnTop -- the user's rule for this screen is that the receipt stays readable and the paper covers everything else, so the burst is inserted at the popup's own sibling index instead of given a sorting number. AFTER the activation, because BurstBelow reads popupRoot's place in the canvas and the parent lookup skips an inactive branch. confettiPrefab is OPTIONAL and is not even REPORTED by ValidateReferences, unlike rewardFlight: the flight is the payout wearing a costume, this is decoration on a receipt that reads fine without it, and an error for a missing party trick trains people to ignore the console. D-101 (2026-08-27) Show is now the WAIT and ShowNow is the old body: the popup is scheduled BoardAnimationConfig.DayCompletePopupDelay seconds out so the delivery that FINISHED the day -- the item settling into its slot (D-100), the tray growing, lifting and fading, the card sliding away, the tray growing back in -- plays out before the receipt covers it. IsDayComplete is set before the publish, so for the whole wait no ticket is assigned and every slot is empty, which means TrayManager refuses every drop anyway. The payout starts one wait later and that moves nothing: no money is credited during a day and every exit commits the purse regardless (D-057). The config reference is OPTIONAL and outside ValidateReferences for exactly the reason rewardFlight is -- an empty slot must never be able to suppress the only way out of a finished day. D-061 added an OPTIONAL score bar: this view hands it the two positions (before popupRoot activates, since anchors resolve on activation) and the FLIGHT drives it, because the bar's fill paces the star seating and this view does not own that sequence. D-060 added three OPTIONAL TMP_Text rows (time bonus / mistakes / net score, integer percents) -- optional because the popup already exists in SampleScene and a required field would stop the whole receipt from appearing until the scene is wired; unwired they stay blank and Start logs it once. Rounding happens only here, so a day a hair under a threshold displays as 55 and still scores two stars rather than being quietly promoted. D-042: "Next Day" now asks GameManager one question (TryContinueIntoNextDay) and hides only if there is still a day scene to hide in. It used to branch on "was there a next Day"; adding the second reason here would have made a popup the place where scene routing is decided, so both reasons moved to the class that owns flow. hand-wired in the Editor, never instantiates UI. It only RENDERS the star count -- the rule (one star per life still held) lives in DayLifecycleManager.StarCount since D-008, which also deleted the per-Day authored thresholds this view used to compare Total against. D-012 gave it two navigation behaviours: Go Back exits to the main screen persisting the NEXT day, and Next Day now falls back to that same exit when there is no next Day, instead of hiding the popup and leaving the player on a finished day with no UI. Neither exit calls Hide() -- the scene load destroys the popup. D-057 turned it from a REPORT into the place the day actually PAYS: nothing is credited during a day, so Show no longer switches the stars on -- it switches them OFF and hands the earned ones, plus the Total text as the coin origin, to DayRewardFlightView, which seats them and flies the money to the HUD. Two ordering rules: the stars are hidden AFTER popupRoot is activated, or the flight measures positions on an inactive hierarchy and drops every icon at the origin; and all three buttons call CompleteRewardFlight FIRST, so tapping through the payout is a skip rather than a forfeit. rewardFlight is OPTIONAL and deliberately outside the missing-list, though still reported with a LogError -- the line this note carried until 2026-08-29 said the opposite and had been wrong for a while. The reason it is outside: ValidateReferences returns BEFORE the DayCompleted subscription, so a single empty slot used to mean the popup never appeared at all and the player was stranded on a finished day with no exit. Everything still in the list is something the receipt cannot be rendered without; the flight is a performance, and ShowNow falls back to seating the stars plainly. The MONEY is safe either way, since GameManager commits the reward on the exits 2026-08-31: the receipt now fades up through PopupFade at BoardAnimationConfig.PopupFadeInDuration, on the line after popupRoot.SetActive(true) and BEFORE the ConfettiView.BurstBelow call, which is unaffected -- the burst reads the popup's sibling index, not its alpha. It fades for the same reason the delay above exists: the popup deliberately waits for the last delivery to finish playing, and a hard cut there interrupts the very shot the wait bought. | h:b6a451a8
ExpoTheExplorer/Assets/Scripts/UI/DayNumberView.cs | HUD day badge, the day number alone | sys: ProgressionSystem | api: - | dep: HudWalletSource | used: - | crit: K3 | note: D-130 (2026-08-28). The user authored HUDPanel/DayUI and its child Text (TMP) by hand; this is the script that fills it. Reads through HudWalletSource rather than holding a session reference, for the reason KeysView does and LivesView does not (D-064/D-065): the badge is PREFAB content shown on both screens and a prefab asset cannot store a scene reference at all, so a field of its own would be a second per-scene override an "Apply All" could quietly drop. ONLY THE NUMBER is written -- the word beside it stays a label in the prefab, the split SettingsPopupView makes, because authored text in code is forbidden outright by the root CLAUDE.md. +1 on render since CurrentDayIndex is a catalog position and the player counts from one, the same conversion MainScreenView's "Continue Day X" caption makes. Event-driven, no Update: GameState's setter publishes only on an actual change and the day changes once per day, so one subscription plus one render at Start is the whole truth; on the main screen the event never fires, which is correct rather than a gap. Start() not Awake(), like every HUD view here -- the session is built in its host's Awake and Unity does not order Awake across GameObjects. Untestable from the EditMode suite for the structural reason D-012 recorded: it is in the predefined Assembly-CSharp | h:9a7f05c4
ExpoTheExplorer/Assets/Scripts/UI/GameOverPopupView.cs | lives-depleted continue/retry/abandon popup | sys: LivesSystem | api: - | dep: BoardAnimationConfig,GameManager,GameState,HapticsBinder,LivesConfig,LivesManager,NoKeysPopupView,PopupFade | used: - | crit: K2 | note: ONE paid continue since the 2026-08-30 audit, not two. GDD Section 6 and CLAUDE.md both promised a SoftMoney continue beside the Gem one, but the button was never built into the prefab, so OnSoftMoneyClicked was bound to nothing and LivesManager.TryContinueWithSoftMoney was reachable only from its own tests; the user's call was that the feature was dropped rather than half-built, so handler, manager method, tests and LivesConfig.continueSoftMoneyCost all went. LivesConfig stays in dep: -- ContinueGemCost is still read here. Earlier: D-101 (2026-08-27) Show is now the WAIT and ShowNow is the old body: the popup is scheduled BoardAnimationConfig.GameOverPopupDelay seconds out so the broken heart rising off the tray that cost the last life (D-078) is seen before it is covered. The failure itself is NOT delayed -- LivesManager sets IsAwaitingContinue before it publishes, so the day is held for the whole wait and D-099's deferred scatter is still waiting behind it; delaying the EVENT would have let the day keep playing behind the animation. The config reference is OPTIONAL and outside ValidateReferences on purpose: a popup that never appears strands a lost day with no way out, so an unwired slot costs the pause and nothing else. D-069 added an OPTIONAL noKeysPopup checked inside OnRetryClicked, on the click rather than by disabling Retry. Hide() is deliberately NOT called on the refused branch -- this popup has to stay behind the explanation, or refusing a retry would drop the player onto a finished day with no UI at all. Main Menu beside it is deliberately NOT gated: blocking the only exit from a lost day at zero keys is a softlock, so it always works and simply spends nothing (D-068). re-verified against the file for D-064 (no public members, so api stays '-'); its behaviour is untouched by that change -- it still calls GameManager.RetryDay, which kept its name. hand-wired in the Editor; lives on an always-active object so Start runs while popupRoot begins inactive. Main Menu is live since D-012 and abandons the attempt rather than merely navigating -- the money rules for that (revert earnings, keep spending, then write) sit in GameManager.ReturnToMainScreenAbandoningDay, not in this view D-072 adds ContinuePurchased on the SUCCESS branch of OnGemClicked -- the most expensive tap in the game, which until then felt exactly like a menu button because the gem button's UiTap was all the frame produced: LivesChanged goes UP on a refill and HapticsBinder reads only the drop. The refusal side needs nothing here, unlike the shop's: Show() disables this button when the player cannot afford it, so an unaffordable Continue is never a click to answer. New optional haptics field, null-conditional like every other one. D-135 (2026-08-29) OnRetryClicked now states the surrender at the call site -- RetryDay(givingUpOnAttempt: true) -- because GameManager can no longer infer the charge from IsAwaitingContinue once the settings menu also surrenders days. Behaviour here is unchanged: this popup only ever appears on a day that was lost, so it always paid the key and still does. 2026-08-31: fades up through PopupFade at BoardAnimationConfig.PopupFadeInDuration, first line of ShowNow after the activation. Same reasoning as the day-complete receipt: this popup is scheduled GameOverPopupDelay seconds out precisely so the broken heart and the scatter are seen, and a hard cut cuts off the shot it waited for. | h:1cbf6a69
ExpoTheExplorer/Assets/Scripts/UI/GemsView.cs | HUD gem counter | sys: ProgressionSystem | api: - | dep: HudWalletSource | used: - | crit: K3 | note: reactive in the day scene (binds GemsChanged, never polls); on the main screen there is no event to bind to, so the value renders once -- correct, since nothing can change a balance while the player sits on the menu. Holds no GameManager since D-013: HudWalletSource decides where the number comes from, so the rule lives in one place instead of three views. Reads through the SOURCE, not through liveState, so a single call serves both modes. Still subscribes from Start, not Awake -- Awake order across objects is unspecified and GameManager.State is assigned in its Awake | h:3145e38c
ExpoTheExplorer/Assets/Scripts/UI/HudWalletSource.cs | per-scene answer to where the HUD reads its numbers, plus forwarded change events | sys: ProgressionSystem | api: SoftMoneyChanged; GemsChanged; KeysChanged; CurrentDayIndexChanged; SoftMoney; Gems; CurrentDayIndex; KeysAvailable; KeyCount; MaxKeys | dep: GameManager,GameState,PlayerProfile,PlayerProfileStore | used: SoftMoneyView,GemsView,KeysView,DayNumberView | crit: K2 | note: D-065 step 3 added the KEY half, and unlike lives it genuinely belongs here: the key readout is inside the shared prefab and shows on BOTH screens, which is the case this class exists for. Keys hang off the SESSION rather than off GameState (D-065 keeps the count private to KeyManager), so the subscribe sits OUTSIDE the live/save-file branch and the OnDestroy unsubscribe sits BEFORE that method's null-return -- pairing it with the state check would leak the subscription whenever a session exists but LiveState does not. It also gained an Update: a once-per-second DISPLAY refresh of KeyManager, on unscaled time so a popup that zeroes timeScale cannot freeze the wait the player is watching. Display-only by contract -- every gate calls Refresh for itself, so no rule depends on this running -- and once per second rather than per frame because a value that moves every 30 minutes does not need 60 clock reads a second. One ticker per scene rather than one per view. D-064 REMOVED Lives, MaxLives and LivesChanged from this class, so it is now exactly what its name says -- a wallet source. LivesView was their only reader and it binds to GameState directly now; the save-file fallback behind them died with the profile field. The two-mode rule below therefore covers SoftMoneyView and GemsView only, which are the views it was really about. Binds to SessionHost, not to GameManager, since D-022 -- which is what makes the MENU HUD live rather than a one-shot save-file read. [FormerlySerializedAs("gameManager")] on that field is LOAD-BEARING: SampleScene's prefab instance carries an override under the old name, and renaming without it drops the override, producing exactly the quiet failure this file already documents (the HUD stops following the live wallet for the whole day with nothing different on screen). A PRIVATE LiveState helper walks sessionHost?.Session?.State -- private on purpose, since D-014 removed the public one so the three views would stop branching on mode, and the double ?. matters because a host may exist while its Session is still unassigned. The mode log line now names the host TYPE, and its meaning changed: a missing host used to be expected on the menu, and since D-022 it means a lost reference in either scene. exists because D-013 put ONE HUD Canvas prefab in both scenes and the two scenes answer differently -- day scene: the live GameState via GameManager, reactive; main screen: the save file, read once (no GameManager there by design, D-012). Strictly a READER; Wallet stays the single writer. Takes the GameManager as a [SerializeField], on the user's instruction, so the binding is visible and theirs to control. A PREFAB ASSET cannot store a scene reference, so that field is necessarily a prefab OVERRIDE on the day scene's instance and empty in the prefab itself -- HudCanvasPrefabSetup step 2 sets it per scene rather than leaving a manual drag that is easy to forget. KNOWN FAILURE MODE: Apply All from the day scene drops the override (Unity will not push a scene reference into an asset) and the symptom is QUIET -- the HUD stops following the live wallet and sits on the save file's numbers all day. That is why Resolve logs which of the two modes it picked, once per scene load: an empty field is not an error (it is how the main screen works), so the log line is the only thing distinguishing the two. Resolves LAZILY on the first view's Start and never in an Awake: GameManager assigns State in its Awake and Awake order across objects is unspecified, so resolving early would reintroduce exactly the null the views' Start-not-Awake rule exists to avoid. D-014 gave it three forwarded EventBus<int>s so the views no longer branch on mode: live, it re-publishes GameState's events (LivesChanged fed by BOTH lives events, since the label it drives renders current/max); on the main screen they never fire, which is correct because nothing there can change a value. LiveState is gone from the API for that reason. Lives off the day scene now reads the SAVED count, not a constant -- that is what made the menu's lives readout real. MaxLives stays the constant: nothing varies it. OnDestroy mirrors the forwarding; EventBus removes by delegate equality so the method-group expressions match D-130 (2026-08-28) adds the DAY readout beside the wallet and key ones, on the ticket KeysView came in on: the day badge is content of the shared HUD prefab and shows on both screens, which is the exact case this class exists for, while a prefab asset cannot hold the scene reference a session needs. CurrentDayIndex is a CATALOG POSITION, zero-based -- the +1 is the view's job, so this getter stays comparable with GameState.CurrentDayIndex and the profile field it falls back to. The subscription sits INSIDE the live branch, unlike the key one: the day genuinely has a save-file mode (PlayerProfile.CurrentDayIndex), so there is nothing to listen to when no session exists. GameManager remains the single writer -- nothing here can move the day. | h:603bab80
ExpoTheExplorer/Assets/Scripts/UI/LivesView.cs | HUD heart row: one filled heart per remaining life | sys: LivesSystem | api: - | dep: GameManager,GameState | used: - | crit: K3 | note: REWRITTEN by D-064; it was a TMP label rendering "current/max". Now it drives a row of heart slots: each slot's own Image is the EMPTY heart and carries a filled-heart CHILD, so the view toggles only the children -- switching a parent off would take its filled child down with it, which is why there is one filledHearts array and deliberately NOT an emptyHearts one. Binds a serialized GameManager and subscribes to GameState's LivesChanged AND MaxLivesChanged from Start (never Awake: GameManager assigns State in its own Awake and Unity does not order Awake across objects). It no longer reads through HudWalletSource: that indirection existed so one HUD prefab could serve two scenes with different sources (D-013/D-014), and lives are a day-scene concern with one source since they stopped being persisted. The row itself is an object added to the HUD Canvas INSTANCE in SampleScene, not to the prefab asset, which is what keeps hearts off the main screen with no mode branch anywhere in code -- and is also the hazard: Apply All / Apply Added GameObject on that instance would push the row into the prefab and put hearts on the menu. Caches the GameState it subscribed to rather than re-walking gameManager.State in OnDestroy, so the unsubscribe cannot target a different object than the subscribe did. Refresh redraws the whole row from Lives instead of stepping one heart, because a refill jumps several slots at once; the SetActive is guarded by activeSelf since most lives events leave most hearts unchanged. A row shorter than MaxLives is a loud error at bind time, not a silently short row | h:3180ac1a
ExpoTheExplorer/Assets/Scripts/UI/KeysView.cs | HUD key counter, "current/max" | sys: KeySystem | api: - | dep: HudWalletSource | used: - | crit: K3 | note: D-065 step 3. Reads through HudWalletSource, which is the OPPOSITE of what LivesView does since D-064 -- and deliberately so, because the two widgets have opposite shapes: the heart row is a SampleScene object and can hold a scene reference to GameManager, while this readout is PREFAB content shown on both screens and a prefab asset cannot store a scene reference at all. That is the exact problem HudWalletSource was built for (D-013). No per-view tick either: the source runs one display refresh per second for the whole scene. When the source has no session it renders NOTHING and logs -- since D-022 that is a lost reference rather than a mode, and the only number this view could invent ("0/5") would tell the player they are out of keys, which is a worse failure than a stale label. Untestable from the EditMode suite for the structural reason D-012 recorded: it is in the predefined Assembly-CSharp | h:c11c8bde
ExpoTheExplorer/Assets/Scripts/UI/NoKeysPopupView.cs | out-of-keys explanation: countdown + gem refill + the gate itself | sys: KeySystem | api: HasKeyOrShow() | dep: SessionHost,KeyManager,PopupFade,BoardAnimationConfig | used: MainScreenView,GameOverPopupView | crit: K2 | note: D-069, key-plan step 5. THE GATE LIVES HERE rather than in its two callers -- HasKeyOrShow returns true when the player may proceed and otherwise shows itself and returns false -- so there is one answer to 'what happens with no keys' instead of two copies, and neither caller needs a session reference (MainScreenView has never had one). The name states the side effect on purpose: a gate that silently opens a window is a trap at the call site. FAILS OPEN by design: missing references or no session return true and log, because a forgotten drag must never leave the player unable to play -- an uncharged key is the smaller wrong, and the Start-time error is what makes the misconfiguration findable. Nothing is ever disabled to enforce the gate (the user's rule): buttons stay pressable and the check is on the click, since a greyed control explains nothing. closeButton is REQUIRED and that is a softlock guard -- the popup only self-closes when a key ARRIVES, so a player with no keys and not enough Gems would otherwise sit in it for up to a full regen interval. Auto-closes on KeysChanged > 0 because its single claim stops being true. Owns a 1s unscaled redraw that runs ONLY while visible; SecondsUntilNextKey refreshes for itself, so this works in a scene with no HUD. A failed gem purchase leaves the window OPEN (the only possible cause is insufficient Gems, since it is shown at zero keys) so the player keeps seeing the price. One instance PER SCENE, not a shared prefab: sessionHost is a scene reference, which a prefab asset cannot carry (D-013's lesson) D-071 adds BlockedByNoKeys, requested inside Show() rather than in HasKeyOrShow's refusing branch, so every route that opens this window buzzes and no future caller has to remember to. It lands in the same frame as the UiTap of whichever button was pressed and outranks it (55 vs 5) -- the player feels the refusal, not the press. The haptics field is optional and null-conditional, which matches this class's existing fail-open stance: a forgotten drag costs a buzz, never the gate. D-072 adds KeysRefilled on the SUCCESS branch of OnGemClicked. Its own moment rather than sharing GameOverPopupView's ContinuePurchased, even though both are "Gems spent, resource restored": a rescue mid-day and a meta top-up are different stakes, and separate rows let them be tuned apart -- the defaults already differ. The not-enough-Gems branch stays SILENT, which is scope rather than oversight: it was offered in the same conversation and deferred, and PurchaseRefused would cover it in one line. 2026-08-31: fades up through PopupFade at BoardAnimationConfig.PopupFadeInDuration, inside Show() — so every route that opens this window fades and none has to remember to, exactly the argument that already put the haptic there. THIS VIEW EXISTS TWICE (SampleScene 'Popup Canvas' and MainScreen 'Canvas'), so the optional animConfig wants dragging into BOTH instances; an empty slot leaves that copy appearing instantly. | h:fc432f5e
ExpoTheExplorer/Assets/Scripts/UI/MainScreenView.cs | main screen: day readout, Play, and Start Over | sys: MainScreen | api: - | dep: SessionHost,MainScreenTutorialView,PlayerProfile,PlayerProfileStore,SceneFlow | used: MainScreenResetButtonSetup | crit: K2 | note: D-090 (2026-08-26) Play is GATED ON OWNING THE FIRST BUILDING, and the gate follows the key gate beside it rather than greying the button out -- the user asked for an unpressable Play and CLAUDE.md/D-069 forbids exactly that, so Play stays pressable and ANSWERS: it points at the store instead of starting a day. The condition is DERIVED (owns nothing) rather than a new profile flag, which is the same state the welcome tutorial reads, so the two can never disagree about who is new. Read from the LIVE session, never the Start-time profile snapshot this class also holds: buying the building is precisely the event that opens the gate, and a snapshot predates it. IsAwaitingFirstBuilding resolves every uncertainty toward LET THEM PLAY -- unwired reference, no session, null list -- because this gate sits on the only path into the game and failing closed is an unplayable build that looks like a save bug. D-069 added an OPTIONAL noKeysPopup and a check inside OnPlayClicked -- on the CLICK, never by disabling Play, because a greyed button does not say why. Starting a day is a GATE and spends nothing; the key is charged only if the day is later given up on (D-068). Unwired the field is a no-op rather than a lock, which is the deliberate direction to fail.  TWO serialized buttons since D-026, the day readout moved ONTO the Play button as "Continue Day X" (D-025). Since D-028 there is NO lookup here at all -- playLabel and resetLabel are serialized fields the author drags. D-025 had DEFENDED finding the Play caption with GetComponentInChildren ("a third field is a third thing to forget") and the user overruled it: a forgotten field is visible in the Inspector, while a search that starts matching the wrong object is visible nowhere. An unwired caption is a WARNING, not an error -- the button still works, it just says nothing. (two missing-reference errors had just come from exactly that). Still reads the PROFILE, not the session this scene has carried since D-022, so the profile loads twice here; accepted because taking it from the session needs another serialized reference and the day number cannot change on a menu. the meta side's navigation shell (D-012), deliberately content-free so the day<->meta flow is correct before any meta CONTENT is designed on it. Reads the SAVE FILE, not a GameState: there is no GameManager in this scene, which is exactly what keeps GameManager's "Awake builds the whole Day" shape intact, so the profile IS the hand-off between the two scenes. A pure reader -- it never saves, so the single-writer invariant on both balances and the day index still names GameManager alone. Gave up its own coins/gems labels in D-013: the shared HUD Canvas prefab is present in this scene too, and two labels showing one balance is how a screen contradicts itself -- MainScreenSceneBuilder no longer creates them and HudCanvasPrefabSetup deletes the four an earlier build made. Shows CurrentDayIndex + 1, a catalog POSITION and not a Day file's dayIndex, and can read one day high on a save that points past the last authored Day -- GameManager clamps that when the day itself loads. Hand-wired like the popups, but the wiring is done once by MainScreenSceneBuilder rather than by hand. D-026 added the OPTIONAL resetButton: two taps (the first swaps in the authored resetConfirmLabel for resetConfirmSeconds, and a coroutine puts the real caption back so the button cannot go on saying "tap again" after that tap stopped erasing anything) then PlayerProfileStore.Delete() and SceneFlow.LoadMainScreen(). It still writes no VALUE -- the delete is a command to the file's owner -- and the scene reload is deliberate: rebuilding the scene resets the session, the HUD, the grounds and the shop through their existing single writers, where patching the live GameState would need a second writer for the balance, the day index and the owned props. Optional on purpose: a MainScreen built before this existed still runs, and playButton stays the only reference ValidateReferences demands D-095 removed the Start Over button entirely -- the field pair (resetButton/resetLabel), the two authored confirm knobs, the arm/disarm coroutine and OnResetClicked. The capability moved to the SRDebugger panel's Save > Delete Save File, which now does the same delete-AND-reload. The two-tap confirm did not move and is not missed: it guarded against a PLAYER mis-tapping, and nobody opens a debug panel by accident. `using System.Collections` went with the coroutine. | h:083c25ed
ExpoTheExplorer/Assets/Scripts/UI/MainScreenRoot.cs | the main screen's composition root: builds the session | sys: MainScreen | api: MainScreenRoot.Session | dep: GameSession,SessionHost,GameConfig,LivesConfig,KeyConfig,PowerupConfig,FoodCatalog | used: HudWalletSource | crit: K2 | note: D-065 step 2 added a REQUIRED keyConfig [SerializeField], joining the existing missing-config guard rather than getting one of its own -- the menu needs a session for the same reason the day scene does, and a KeyManager cannot be built without it. The error message names which of the three is empty. gives the MENU a GameState and a Wallet, which is the precondition for a shop spending money at all (D-022) -- and lets the shared HUD follow it live instead of reading the save file once. Deliberately NOT a GameManager: it builds the session half only, because the day runtime (ticket slots, tray, board, the per-frame tick) behind a menu would time tickets out and cost lives, which is the concrete reason D-021 rejected moving GameManager here. Builds in Awake, never Start: every reader is a view's Start and Unity runs all Awakes first -- the same ordering GameManager relies on, so both scenes behave identically. Missing configs log an ERROR because the failure is otherwise quiet: with no session the HUD silently falls back to the save file and looks correct until the first purchase does not update the coin count | h:08c5149a
STALE ExpoTheExplorer/Assets/Scripts/UI/MetaGroundsView.cs | draws one location's grounds and the props on it | sys: MetaSystem | api: Refresh(); RefreshAfterPurchase(MetaItemDefinition purchased); ViewedLocation { get; }; IsSettlingPurchase { get; }; ShowGhost(MetaItemDefinition item); ClearGhost(); TryGetGhostBounds(out Vector3 worldTopCentre, out Vector3 worldBottomCentre); ViewedLocationChanged { get; } | dep: ConfettiView,MetaResolver,MetaCatalog,MetaLocation,MetaItemDefinition,MetaUnlockPopup,GameSession,SessionHost,HapticsBinder,HapticConfig,EventBus | used: MetaShopView | crit: K2 | note: D-140 (2026-08-31) the upcoming prop's percentage is centred ON the silhouette instead of floating above its head: anchor and pivot both (0.5,0.5), and `upcomingLabelOffset` re-read as a nudge from that centre (the scene's authored 16 rewritten to 0 in the same task, since the number's meaning changed). The caption belongs to the art that is filling up, and a top-anchored one could sit in a taller neighbour's airspace -- the same argument D-045 made about the ghost's depth, one axis over. |  | D-138 (2026-08-29) ViewedLocationChanged finally exists -- the event this file spent three steps explaining the absence of. The old argument (the shop rebuilds its list on every open, and the player can only walk while the panel is closed) held right up until the market button's can-buy badge became a thing that is ALIVE in exactly that closed window. Published from the END of Refresh and NOT from Step, on an actual change of the resolved location: Step's clamp can land back where it started, and Refresh is the only place that knows what was really drawn -- so a redraw (a purchase, a re-open) says nothing and "the location changed" cannot decay into "something happened". The FIRST resolve (null -> the opening grounds) publishes too, and that is load-bearing rather than tidy: Unity does not order Start() across GameObjects, so a subscriber cannot know whether it woke before or after these grounds resolved -- publishing the first one makes both orders end correct. |  | D-131 (2026-08-29) fires celebration confetti on BOTH of this file's payoff beats, on the same lines the two haptics already sit on: PlacePurchasedProp's PropLanded (above the shake guard, so a scene with the shake off still celebrates) and CelebrateOne's PropUnlocked (so skipping the reveal still throws the paper, and it lands BEFORE the hold and the unlock popup -- paper that starts falling after the explaining popup is up reads as a second, unrelated event). BurstOnTop here, unlike the day scene's BurstBelow: the user's rule is that on the main screen the popups are already closed at both beats, so nothing should be above the celebration. Fired and never awaited, and parented to nothing this view tears down. D-128 (2026-08-28) CelebrateOne ends by showing the prop's authored unlock popup and WAITING on it, which is what holds the zoom until the player presses the button: staying inside CelebrateOne means the focus that prop already has is simply not released, and returning hands off to the next prop's own FocusOn or to PeekAtWhatIsNext plus the caller's RestoreFocus. No camera code was added for it. The popup is deliberately NOT skippable by the skip catcher -- that catcher hurries an ANIMATION along, and this is the content the animation was introducing. The message on the catalog item is the opt-in, so a prop nobody has written for plays its reveal and moves on exactly as before. D-096 (2026-08-26) added IsSettlingPurchase, the one question the shop asks before reopening itself after an area unlock: `placing |  | DOTween.IsTweening(content)`, so it covers the WHOLE payoff -- drop, shake, and RestoreFocus's journey home -- rather than only the drop. Two halves because the framing changes hands between them, and the second starts in the same call that clears the first, so there is no frame where it reads false in the middle; a placement that never zoomed leaves no tween and settles the instant the drop ends. A QUESTION, not a completion callback: the early-exit paths in RefreshAfterPurchase finish synchronously, so a callback would fire before the shop's own SetOpen(false) and be undone by it -- the grounds would have owed a "never called synchronously" promise nothing could enforce. Nothing else in this class changed. HAPTICS (D-070): PropLanded is requested in PlacePurchasedProp at the frame the prop reaches `landed`, and deliberately ABOVE the shake guard -- the prop has landed whether or not the ground is authored to react, so a scene with the shake turned off still gets the thump in the hand. It is the heaviest preset on the main screen, paired with the lightest one on the buy itself. D-046: the grounds Image is now a CONTAINER that never draws -- the art moved into runtime children so the continuation layer can sit behind it, since a child always renders over its parent's own graphic. Order is [0] continuation, [1] grounds art, [2..] props. Chosen over inserting a real container object, which would have meant a new serialized reference and another delete-and-rebuild round (D-036); as it stands the scene is untouched, `background` is still the ScrollRect content, the zoom target and the props' parent. The continuation is a CHILD, so scrolling, the preview zoom and the framing are inherited rather than recomputed -- a second transform calculation is the "second authority" trap this class keeps avoiding. It is sized from its own pixels times the SAME PropScale the grounds use, and centred with no offset. Scroll range is deliberately unchanged: the content is still sized from the grounds, so the extra art is clipped by the viewport and fills the edges under zoom rather than letting the player pan into empty space. artLayerCount exists because SortedInsertIndex returns SIBLING indices and props now start after these layers -- forgetting that shift would put ghosts behind the grounds art, invisible, which is the silent-depth defect D-045 was reported for. D-045 fixed a draw-order bug the Meta Editor had been exposing: the upcoming-prop silhouette was APPENDED after the active props, so it drew in front of everything regardless of its SortOrder, while the editor canvas has always sorted. Reported as "the fryer is behind the sauces in the editor but in front in game", and the catalog confirmed it -- fryer is a DayUnlock prop at sortOrder 0, still closed, therefore drawn as the upcoming ghost. It is now placed at the sibling index its SortOrder asks for (SortedInsertIndex), and the celebration copy takes over the hidden real prop's exact index. The reason to fix it this way rather than force the ghost forward is the reason the preview exists (D-032, D-040): it shows where the prop WILL stand, and depth is part of where -- a ghost pulled to the front tells the truth about position and lies about occlusion. Accepted consequence: a prop authored at a low SortOrder really is behind the scenery and its ghost can be partly hidden; the celebration zoom still finds it. NOT changed, and flagged as an inconsistency for the author to settle: the PURCHASE ghost is still forced to the front (D-031), because the shop zooms to it and names it in a popup, where occlusion costs comprehension. D-043: after the last unlock the map pans on to the NEXT prop's ghost and lingers before zooming out. Not a new drawing pass -- Refresh runs before the celebration with the new day index, so upcomingProp is already the prop AFTER the one that just opened, at the right progress; a second copy would be the same thing twice. The timing pays for itself: D-040 measures the wait from the PREVIOUS unlock, so a prop that just opened leaves the next reading exactly %0, and "the counter starts now" is on screen without a word of copy. Its own duration field, not the celebration hold, because the two are different beats and one number would mean breaking one while tuning the other. skipRequested is reset first, so skipping the reveal does not silently skip the peek as well. Nothing coming (all open, or the next one area-gated and therefore undrawn) skips straight to the caller's finally, which is still the single place the framing is restored. D-041 plays the unlock celebration: zoom to the prop, watch it rise from silhouette to solid, then zoom back. It works out what is owed ITSELF via MetaResolver.DayUnlocksBetween rather than being told by the day scene, which is what makes it independent of the forced-return step that follows -- the player sees it however they got back, including after quitting for two days. Started ONLY from Start, never Refresh: a purchase also calls Refresh and a shopping trip is not the moment for a day-unlock fanfare. FocusGhost became FocusOn because it now has two callers; a second framing routine would have broken D-032's single authority. The real prop is HIDDEN while an animated copy takes its place, so the opening is watched rather than already-done, and the fill layer is shared with the upcoming-prop preview. The marker is written only after the WHOLE queue completes: an interrupted celebration replays next time, which is the cheap failure, where swallowing it is the expensive one. Its single writer is this class. Skip is a transparent full-screen Button parented to the CANVAS as last sibling -- not to the grounds, which are the canvas's FIRST child, so a catcher inside them would sit under the market button and let the shop open mid-celebration; and a Button rather than Input.GetMouseButtonDown so it does not depend on which input backend is configured. try/finally guarantees the framing comes back and the catcher goes: a screen left zoomed with an invisible full-screen button over it is unusable. D-040 draws the upcoming Day-unlock prop: a faint silhouette with the solid version filling from the bottom as days pass, and the COMPLETED percentage over it. TWO layers, not one, because a single Image cannot be both faint and partly solid -- fillAmount CUTS, it does not fade. The silhouette goes through CreateProp so the preview stands exactly where and at what size the real prop will (same reason as the purchase ghost); the fill is its stretched CHILD so placement is inherited rather than computed twice. Built in CODE, not cloned from an authored template: a template would be nicer to style but would add an object reference to this component, which would mean re-running Build Meta Grounds -- the step that refuses to rebuild, i.e. the D-036 trap. Tuning is serialized instead, and new serialized values are born with defaults without touching the scene. preserveAspect is deliberately unused on these props (size already comes from sprite pixels times background scale) and interacts badly with Filled -- leave it alone. A missing TMP font costs the caption, not the fill. Refresh() is PUBLIC again as of S4 (D-034) and only now: public for the first shop, private again when that shop was reverted (D-027) because an entry point with no caller reads as one that is used somewhere, and kept private through S1 and S3 on the same argument since neither bought anything. A purchase changes what MetaResolver.IsActive answers and nothing else would notice, so the caller arrived exactly then. S3c (D-033) animates the travel with DOTween instead of snapping. The destination is now MEASURED, not derived: the map is placed at its destination for an instant, the end scale, the end position and the ghost SETTLED edges are read there, and it is snapped back before anything renders. That was the only way to keep the world-space delta framing (D-032 rejected pivot/anchor arithmetic) while also tweening -- the delta can only be read once the scale is applied. TryGetGhostBounds therefore reports the SETTLED edges, which is why MetaShopView needed no change and carries no timing dependency on this animation: it places the popup once, where the prop is going, and the map slides under it. ShowGhost destroys the old ghost WITHOUT restoring the framing (DestroyGhostObject) while ClearGhost restores -- load-bearing since the travel is a tween, because restoring first would re-capture the "before" framing from halfway through the return tween and cancelling would send the map somewhere it had never been. The pre-focus framing is captured only when not already focused, for the same reason. Scrolling is handed back in the return tween's OnComplete, never at its start: the ScrollRect is Clamped and pulls the content toward its bounds every LateUpdate, so both would be dragging one value. DOKill before every tween and in OnDisable. S3b (D-032) made ShowGhost also FRAME the prop: zoom toward it and slide the map so the confirm popup fits above. The zoom is not authored per prop -- it is the zoom that would make the prop fill previewFill of the viewport, clamped to [1, previewMaxZoom]. A trash bin zooms to the ceiling; a fence as wide as the grounds needs a ratio below one, so the clamp holds it at 1x and the map does not move. That is the user's own observation falling out of the arithmetic instead of being an exception, and a middling prop added later gets a sensible number for free. ScrollRect is DISABLED while previewing -- honest (the player is answering a question, not exploring) and it keeps the ScrollRect's bounds arithmetic out of a fight with a scaled content rect, plus horizontal scrolling is off so a zoomed map would otherwise be unreachable sideways. Framing is a WORLD-space delta, never anchor arithmetic: the background is width-stretched with a top pivot and hand-rolled maths would silently focus the wrong spot the day either convention changes. ForceUpdateCanvases after the scale change, or the ghost's corners are read one frame stale and the framing lands off by the zoom factor. RestoreFocus is guarded by a flag, not by comparing values, because a preview that zoomed by 1x still disabled scrolling. S3 brought the ghost back (D-031): ShowGhost draws the previewed prop through CreateProp -- the SAME path the owned props take -- so a preview cannot disagree with where the thing will actually land, and PropScale is now one method rather than two call sites for that reason. Only the alpha differs, and it is a serialized field because it is cosmetic. The ghost is NOT in spawnedProps: it has its own field, because tying two different lifetimes to one list would make "was the ghost destroyed or the prop" unanswerable in S4. Refresh clears it -- a ghost belongs to ONE location layout and would otherwise stand on ground it was never positioned against. ClearGhost is safe in any state, because every exit the shop has calls it. Refresh() is STILL private: S3 buys nothing, so nothing changes what IsActive answers; it goes public in S4 when there is a caller. ViewedLocation came BACK in S2 (D-030) after D-027 deleted it: the objection was "no caller", and MetaShopView is now the caller. ViewedLocationChanged did NOT come back -- the shop rebuilds its list every time its panel opens, and the only moment the player can walk to another location is while that panel is closed, so an event would be a publisher written before its subscriber. The location bar's four references are OPTIONAL since D-025 -- with one location there is nothing to switch between, so requiring them forces scene clutter for a feature with no content, and deleting the bar is a legitimate choice. Only sessionHost/catalog/scroll/background stay required, because without them nothing can be drawn at all. The shop's ghost preview, ViewedLocation and ViewedLocationChanged were REMOVED with the shop (D-027): they had exactly one caller, and an entry point with no caller reads as one used somewhere. Refresh() went back to private for the same reason. decides NOTHING -- what is active comes from MetaResolver, where prices, area gates and unlock days were already settled and tested (D-019); this turns three answers into RectTransforms. Props are INSTANTIATED from the catalog, never placed in the scene: nineteen hand-placed objects would be a second copy of the catalog free to disagree with it silently. Two things are load-bearing. Canvas.ForceUpdateCanvases() before measuring: prop sizes derive from the background's WIDTH, and on the frame Start runs a stretched rect's width is still zero, which would size every prop to nothing and look like an empty screen rather than an error. And props are CHILDREN of the background with their normalizedPosition written straight onto anchorMin/anchorMax -- that is what K5's "normalize to the background rect, not the screen" was for: they ride the art through every resize and every scroll with no layout code. Background fits by WIDTH and the height falls out of the aspect (M10, the user's choice): fitting by height would letterbox every device, while fitting by width leaves vertical scrolling as the only variable, and that is zero on the phones the art was drawn for. Refresh() is public because Adım 8's purchase changes what IsActive returns and nothing else would notice; it runs at EVENT frequency (screen open, purchase), never per frame. Props are raycastTarget: false so they cannot swallow drags meant for the scroll view. Location stepping is between UNLOCKED locations only -- letting the player walk into a locked one would duplicate in the UI the refusal MetaPurchase.LocationLocked already owns D-048 added the PURCHASE PLACEMENT animation: a just-bought prop is prepared translucent and placementDropHeight above its spot in the SAME FRAME DrawProps drew it (waiting even one frame would show it standing in place and then jerk it into the air), then -- after previewTravelSeconds, so the map has finished travelling back out of the preview zoom, the same wait CelebrateOne makes for the same reason -- it falls on an IN ease while its alpha rises LINEARLY toward the prop's own alpha, and the ground takes the hit as it lands. Animated by a frame loop on Time.unscaledDeltaTime rather than a tween, which is CelebrateOne's choice and buys the same two things: a try/finally that guarantees the finished state through any interruption, and no tween left pointing at a RectTransform Refresh is about to destroy. The shake moves the CONTENT, not the viewport -- a moved mask window would open a sliver of empty screen at the edges, while the content carries D-046's oversized continuation layer as a child, which is the art that keeps them covered -- and it suspends the ScrollRect for its duration (D-032's call: Clamped scrolling drags the content toward its bounds every LateUpdate and would turn the shake into a stutter), handing it back under RestoreFocus's own `!focused` condition so two places never own that flag. The swing is a damped sine, negative first, not per-frame randomness: an impact has a direction and noise at 60fps reads as a glitch. Clear() stops the routine BEFORE destroying props, which makes "the props are gone" and "nothing is still animating them" one moment. D-049 KEEPS THE PREVIEW ZOOM through the placement and only then travels out. `placing` is the claim and RestoreFocus is where it is honoured -- a flag next to the existing `focused` guard rather than a parameter threaded through the shop's SetOpen/ClosePreview, because "may the zoom be given back right now" is this class's question. The order in MetaShopView.OnConfirmBuyClicked is load-bearing and was found by wiring it, not by reasoning: RefreshAfterPurchase must run BEFORE SetOpen(false), because the shop's exit goes through ClearGhost whose second half is the request for the zoom back -- claiming after it would let the zoom-out start and the prop would land on a map already travelling away. Both calls share one frame, so nothing is visibly half-done. Two waits and a scroll block DIED with this: the fixed previewTravelSeconds wait (the map no longer moves) and the shake's disable/re-enable of the ScrollRect (FocusOn turned it off at D-032 and RestoreFocus turns it back on, so the whole placement runs inside a window where it is already suspended). What replaced the wait is `while (DOTween.IsTweening(content))`, zero frames in the normal case and exactly the remainder for a player who taps BUY before the travel INTO the zoom has finished. INTERRUPTION OWNERSHIP is the subtle part: `placementGeneration` is bumped by every claim, and the finally hands the map back only if its own generation still matches -- so a second purchase takes the framing from the first placement instead of having the first one's teardown travel out from under the second one's drop. A location change is the opposite case and is handled by its own caller: Step calls CancelPlacement, which bumps the generation and gives the framing back SNAPPED (RestoreFocus(animated:false)), because Step sets the new location's scroll position one line later and an animated return would still be moving and would win. Clear cannot make that call -- it cannot tell a location change from a purchase -- which is why the decision sits in Step. Both cases are unreachable in today's scene (one location, D-025's optional arrows) and are written down because the second location is what makes the first one bite. D-050 routed the prop ARITHMETIC through MetaLayout: PropScale and the prop's size are asked of it rather than computed here, because the day scene's backdrop now composites the same prop and the two must agree pixel for pixel. The POSITION stayed anchors + pivot -- that is the better implementation of the same rule, since an anchored prop rides the background through every resize without a line of layout code, and MetaLayout.PropRect is that rule written out for the backdrop, which has no anchors to lean on. So this file remains the reference if the two ever disagree. D-051 fixed a DEPTH bug that had been live since D-045 and only became visible when D-047 started closing the shop after a purchase. Object.Destroy is DEFERRED to the end of the frame, so a redraw's old art layers, props, silhouette and ghost were all still children of the background while the new ones were being appended -- the parent carried BOTH sets for the rest of that frame. Anything that only APPENDS is fine (the new props keep their relative order), but DrawUpcoming moves its silhouette to an ABSOLUTE sibling index computed as though the dead children were gone. On the report that surfaced it the dead block was 13 children and the computed index was 12, so the silhouette landed inside the corpses, BEHIND the new background art, and vanished -- which read as "the prop was deleted by my purchase". Every spawned child now dies through Detach, which does SetParent(null) BEFORE Destroy so it leaves the child list on that line and every index computed afterwards counts only what is really there. The user's own first description -- "arkaplanda kaldi" -- was literally right and was misread as a framing complaint; the positions of the props they COULD see were what made it look like a scroll problem. D-072 adds PropUnlocked in CelebrateOne, on the line that snaps the reveal to finished -- the one place BOTH routes reach, the fill running its course and the player tapping past it, because skipping skips the animation and not the event. Not inside the reveal loop, for the reason the coin burst established: that loop drives fillAmount frame by frame, so a request per iteration would be a second of rumble instead of one beat. A SEPARATE moment from PropLanded on purpose -- both end with a prop standing there, but a bought prop falls and hits the ground (HeavyImpact, the map shakes under it) while this one rises from silhouette to solid, and giving a gradual reveal an impact haptic would describe a blow the screen never struck. Several props unlocking at once each buzz, like the star row, and cannot machine-gun: CelebrateOne resets skipRequested per prop, so each needs its own tap, and the coalescer caps the frame anyway. D-134 (2026-08-29) THE PURCHASE ZOOM NOW WAITS FOR THE CONFETTI: WaitForConfetti sits at the END OF PlacePurchasedProp's try, because ReleaseFraming -- the travel back out -- is the last line of its finally and C# forbids yield return inside one. The wait is DERIVED, not authored: the rig destroys itself when its last particle dies, so `while (burst != null)` follows the Start Lifetime on Confetti.prefab and there is no second duration to keep in step. ConfettiWaitCeilingSeconds is a const rather than a serialized field because it is a softlock GUARD, not a knob -- this coroutine holds the map zoomed with the ScrollRect off. The shake guard stopped being a `yield break` and became an `if` block in the same change, and that is load-bearing: a bail-out there would have skipped the wait entirely in any scene with the shake turned off, keeping the old timing silently. SIDE EFFECT: IsSettlingPurchase reads the same `placing` flag, so MetaShopView's post-area-unlock reopen (D-096) waits the extra time too -- consistent with D-096's "the WHOLE payoff", but a consequence rather than a decision. CelebrateOne was deliberately NOT given the same wait: it already holds the zoom on MetaUnlockPopup.IsDismissed. SETTLED, after three passes, on placementConfettiHoldSeconds (serialized, default 1.5, beside the other placement feel numbers): the wait is the burst ending OR that many seconds, WHICHEVER COMES FIRST. Full burst -> too long; plain cap -> reverted; cap again, and this time it stays, because the version that survived is the one the author can settle in the inspector without a code change. It is a CAP rather than a delay, which is what keeps it honest beside the lifetime it duplicates: set it past the burst and the poll wins, so the worst a stale number can do is stop mattering. Set it to the full Start Lifetime to watch the whole thing, half to leave on the way down. It is also this method's softlock bound -- an unbounded wait holds the map zoomed with the ScrollRect off. | h:500886b4
ExpoTheExplorer/Assets/Scripts/UI/MetaShopView.cs | the meta shop: market button + the panel it opens | sys: MetaSystem | api: MarketButtonRect | dep: MetaPurchase,MetaCatalog,MetaGroundsView,MetaShopRowView,SessionHost,HapticsBinder,HapticConfig,MetaLocation,GameState,DOTween,PopupFade,BoardAnimationConfig | used: - | crit: K2 | note: D-138 (2026-08-29) the market button carries an optional CAN-BUY BADGE: an authored RectTransform switched on whenever MetaPurchase.HasAffordableOffer says this location holds a prop the player can afford RIGHT NOW, hopping in place (one DOTween sequence, hop-hop-rest, ~1.5s period, three serialized knobs) while it is up. AFFORDABLE, not merely listed -- the list deliberately keeps unaffordable rows, so a badge derived from "the list is non-empty" would be lit for everyone who has not bought the location out. Hung on ApplyVisibility, which is what makes the user's two asked-for triggers cost NO new call sites: the screen opening and a purchase both end in SetOpen(false), and the purchase's one runs after the ownership key is written, so the bought prop is already out of the count. Two events cover what the state machine cannot see move: GameState.SoftMoneyChanged (the powerup shop sells on this same screen; subscribed to the state rather than to that view, since Wallet is the single writer every spender goes through) and MetaGroundsView.ViewedLocationChanged (walking locations while the panel is closed -- exactly the window the badge is up for). Hidden while the shop or a preview is up, because the market button draws ON TOP of the panel and a badge that stayed would hop over the list it is pointing at. OPTIONAL like comingSoonLabel and out of ValidateReferences for the same reason: an already-built shop keeps working with a null here. Never cached -- the answer depends on balance AND location AND owned set, and a cached bool would be a fourth place those three are known. |  | D-136 (2026-08-29) an UNFINISHED location shows a COMING SOON label instead of an empty list. The question is asked of the CATALOG (MetaLocation.HasNoItems -- the very condition MetaCatalogValidator warns about) and asked BEFORE MetaPurchase, which is the whole point: deriving it from an empty offer list would fold it together with "the player bought everything" and promise more props for a finished location. No id and no location count is in this file (D-015) -- whichever location is empty says so. comingSoonLabel is OPTIONAL and stays out of RequiredFields, so a shop built before this logs no error and simply behaves as it did; ClearRows is the single place it goes down, the same one-owner discipline ClosePreview enforces for the ghost. D-096 (2026-08-26) makes ONE kind of purchase reopen the shop: an item whose UnlocksArea is set. Not a reversal of D-047 -- the shop still closes on every purchase and the screen still ends on the prop -- but ShopItems HIDES area-locked props, so the square is the only buy whose payoff is partly a LIST of rows that were not there a moment ago. Read from the catalog flag, so a second expansion needs no code. The wait is a coroutine polling MetaGroundsView.IsSettlingPurchase (one bool + one tween query per frame, alive ~1s per purchase, at most once); started AFTER SetOpen(false) so the shop's own exit path runs whole first, and SetOpen now cancels any pending reopen -- which is how the player closing or opening the shop themselves, or entering a preview, silently abandons it. The coroutine clears its own handle before calling SetOpen(true), so that cancel cannot kill the coroutine calling it. The one abandon case SetOpen does not cover is walking to another location, checked explicitly against the location the purchase was made on. D-088 (2026-08-26) exposed MarketButtonRect, read-only, for the main-screen tutorial's arrow to point at. Deliberately the RectTransform and not the Button: a caller gets a position and a size, and no way to press the store on the player's behalf or to restyle it. the dep field read `-` before D-070 and that was simply wrong -- this screen has always compiled against MetaPurchase, the catalog, the grounds, its own row view and the session host; the haptics pass corrected it rather than adding one name to a lie. HAPTICS (D-070): PropPurchased is requested AFTER session.Save(), which is the first line at which the purchase is a fact -- the verdict passed, Wallet actually spent, and the file has the prop. On the tap it would sometimes fire for a purchase that then failed. It is the lightest preset in the table on purpose: the payoff the player is waiting for is the prop hitting the ground a moment later, in MetaGroundsView. D-039: rows bind offer.ShopIcon, not offer.Sprite. The ghost and the map prop still use Sprite. D-038: the confirm popup has NO backdrop -- the user removed it from the scene and the field went with it, so CANCEL is the only way out of a preview (MS6 is dropped) and the map is not dimmed while one is up. The backdrop's other job, eating taps meant for the map, costs nothing: the ScrollRect is already disabled for the duration of a preview (D-032). The cancel button is now the ONLY exit, which makes an unwired one the worst failure on this screen, so it stays required. D-037: owns both BUY colours and applies them on every bind, so a row always shows one of the two rather than sometimes inheriting the template's authored colour. The popup's BUY is red AND disabled when the money is short: the disabled state darkens the base colour, so it reads as "red, and off" instead of the generic grey that says nothing about which reason. The unaffordable red is the project's existing destructive-action red, so the game does not grow a second red. D-036: Start hides the panel and the popup BEFORE validating, with its own null checks. A missing reference must read as a console error, not as the shop covering the screen -- which is exactly what happened while the scene still held the S1-era shop: validation returned early, so nothing was switched off and the panel sat over the map with a market button that had no listener. An incomplete shop is invisible now, not stuck open. S5 (D-035): the view neither re-sorts nor re-filters -- ShopItems returns the list already filtered and ordered, so there is one authority for what a shop shows. Affordability per row is price-vs-balance rather than another Evaluate call: everything Evaluate could still refuse on has been filtered out, so money is the only question left and asking a six-verdict function for a yes/no would imply otherwise. The popup's BUY is now interactable only when the money is there -- until S5 it was live and did nothing on a short balance (Evaluate refused, logged, screen unchanged), which is a dead button in every sense the plan warned about; the commit still re-checks everything. S4 (D-034) made the purchase real, and it is the ONLY place the meta side spends money -- while owning no rule: MetaPurchase decides whether the buy is allowed, Wallet decides whether the balance carries it, GameSession.Save decides what reaches disk. Evaluate runs a SECOND time here, having already run when the row was drawn, because the panel stays open across purchases and the balance the row was drawn against goes stale; the cost is one call. The wallet is then asked anyway even though Evaluate just approved: Wallet is the single writer of the balance and its answer is the authority -- decision and transaction, not two answers to one question. ATOMIC BY CONSTRUCTION rather than by care: the balance and the owned set live on one GameSession and Save() writes both, so "money gone, item missing" is not expressible. A non-Ok verdict logs and leaves the popup OPEN -- saying why on screen is S5, and doing it here too would put the appearance of a verdict in two places. Panel stays open after a buy (MS4) and the row list is rebuilt so the bought prop drops out. S3b (D-032) moved the popup ONTO the prop: confirmBox has a bottom-centre pivot and is placed just above the ghost, flipping BELOW it when the prop sits near the top of the screen, and clamped away from the screen edges. Positions are computed in the parent RectTransform's local space, not world units, because the box's size is measured there and mixing the two drifts by whatever the canvas scaler is doing on the device. Recomputed on every preview -- a cached position would be wrong for the second prop. confirmBox is REQUIRED: unwired, the popup would quietly re-centre itself, and a silently-missing feature is worse than a named missing reference. S3 made this the whole state machine (D-031). Two fields hold it: isOpen (KAPALI/ACIK) and pendingItem (ONIZLEME when non-null, and it stores the ITEM so the popup and the S4 commit cannot disagree about which prop is being bought). Every transition goes through ApplyVisibility and every exit from a preview through ClosePreview -- ONE answer to "when does the ghost go away", which is exactly what the first shop lacked (D-024). The list and the popup are never both up: the panel is a bottom sheet covering the map the ghost stands on, so previewing with the list up would hide the thing being previewed and the feature would look broken rather than hidden. Panel visibility is therefore isOpen && !previewing -- activeSelf is neither field on its own. Rows are NOT destroyed while previewing; cancel brings the list back rather than rebuilding it. Tapping a second row REPLACES the preview (a change of mind, not an error). The popup BUY changes no state in S3 on purpose: closing would make S3's own verification ambiguous and S4 would write the closing twice. The popup's four interactive refs are REQUIRED -- an unwired cancel or backdrop strands the player in a preview with a ghost and no way back, the exact leak this step exists to make impossible; the two labels stay optional because they only say what is being bought. S2 filled the panel: a row per offer from MetaPurchase.ShopItems, cloned from an authored INACTIVE template in the scene rather than a prefab asset (the TicketCardView/modificationRowTemplate pattern) -- restyling that one object restyles the list, and there is no second asset to keep in sync. Which LOCATION is shopped is read from MetaGroundsView.ViewedLocation, NOT resolved again here, which is what keeps the MetaCatalog reference in exactly one place; two components free to point at two catalogs would break D-015 with nothing looking wrong. Rows are rebuilt on every OPEN (a purchase changes what ShopItems answers) and destroyed on close -- not pooled, same call MetaGroundsView.Clear makes. rowsParent is forced ACTIVE and the template forced INACTIVE in Start: hiding the container to get the sample row out of sight silently hides every real row, the trap TicketCardView records. Instantiate copies the template's inactive state, so each clone needs an explicit SetActive(true). All SIX references are required -- listing the first location's props when grounds is unwired would show a wrong list as though it were right, which is the failure D-028 was written about. Step S1 of meta-shop-plan.md, and DELIBERATELY thin: a market button and a panel that opens and closes. No rows (S2), no ghost, no confirm popup (S3). Reads nothing and writes nothing -- the catalog, the wallet and the six purchase verdicts stay in MetaPurchase where they were tested (D-019), and this screen will ASK them in S2 without owning a rule. The panel is toggled from CODE rather than by a Button-to-SetActive wire in the Inspector, even though that wire would do today's job: this class is going to own the shop's whole state machine (KAPALI/ACIK/ONIZLEME) and the ghost's lifetime hangs off these same transitions, so a machine half in the scene and half in code is one that disagrees with itself. SetOpen is the single place visibility changes, for the same reason -- the first shop's ghost leaked because its visibility had more than one owner (D-024/D-027). Forced CLOSED in Start whatever the scene was saved as: a shop already up hides the grounds the player came to look at, and the scene checkbox is easy to leave ticked after testing. Both references are REQUIRED, unlike the grounds' optional location bar -- a shop with no button cannot be opened and a button with no panel does nothing, so neither has a degraded mode worth shipping. isOpen is not persisted (same call K6-4 made for the viewed location: a profile field and a schema version for a preference nobody would miss) D-047: a SUCCESSFUL purchase closes the shop all the way to KAPALI instead of restoring the list. MS4 had deliberately kept the panel open so several props could be bought without tapping the market button between each one; the user reversed it ("bir sey satin alindiktan sonra yeniden dukkana donmesin") and the reason holds up in the geometry: the panel is a bottom sheet over most of the map, so returning to the list hides the very prop that was just paid for behind the ones that were not. OnConfirmBuyClicked therefore ends with SetOpen(false) then grounds.Refresh() -- through the single door rather than by clearing isOpen locally, so the ghost, the popup, the framing and the rows all come down on the one path that D-024's leak taught this class to keep. RebuildRows is GONE from that path, not moved: a closed panel has no rows, and the next open rebuilds them against the balance as it will be then. The FAILED paths (verdict != Ok, wallet refusal) still leave the screen exactly as it was -- closing on a refusal would read as a purchase that happened. D-048: the purchase's last call is grounds.RefreshAfterPurchase(item), not Refresh() -- the shop passes WHAT was bought and nothing about how it should look, so the drop-and-shake lives entirely in the grounds and this screen stays a thing that spends money and asks two views to catch up. D-049 moved this call BEFORE SetOpen(false) and the order is load-bearing: RefreshAfterPurchase claims the map's framing, and SetOpen's ClearGhost is the request to give it back -- closing first started the zoom-out before the grounds had heard about the purchase, so the prop landed on a map already travelling away from it. Same frame, no render between, so the panel and the drop begin together; SetOpen's ClearGhost is now a no-op on both halves by design, the ghost having been destroyed by the redraw above. D-071 adds PurchaseRefused on BOTH refusal branches -- Evaluate's non-Ok verdict and the wallet's own refusal. One moment rather than one per verdict, because to the hand "not enough money", "area locked" and "already owned" are the same answer; which one it was is the screen's job to say. The wallet branch is unreachable in practice today and is wired anyway, so the two paths cannot start feeling different if it ever is reached. D-108 (2026-08-27) gave it THREE exits, all one transition: the market button, a new optional closeButton (an X in the sheet's corner) and a new optional backdropButton (a tap outside). Both new ones call SetOpen(false) and never SetActive -- visibility keeps its single owner, the property whose absence leaked the first shop's ghost (D-024/D-027), and a button touching `panel` directly would skip ClosePreview and CancelPendingReopen. `panel` now points at the full-screen BACKDROP with the sheet as its child, so the block and the list come and go together and the outside-tap needs no hit-test (the sheet carries its own raycast-target Image, so UGUI gives it the taps that land on it). NOT a re-do of D-038, which removed a backdrop from the CONFIRM POPUP at the user's request -- the preview still has CANCEL as its only way out. 2026-08-31: both the sheet and the confirm popup fade up through PopupFade at BoardAnimationConfig.PopupFadeInDuration, and ApplyVisibility is the ONLY place that does it. It reads `!panel.activeSelf` / `!confirmPopup.activeSelf` BEFORE the two SetActive calls, because afterwards "did this just come on?" is unanswerable -- ApplyVisibility is called from three sites and re-runs while the shop is already open, so without the transition check the sheet would restart its fade from zero on every row change and read as a flicker. Only ONE of the two can be opening, which is deliberate: fading both sides of the swap would cross-dissolve the sheet into the confirm box and let the map show through the gap. | h:7f5a0a74
ExpoTheExplorer/Assets/Scripts/UI/MetaShopRowView.cs | one shop row: icon, name, price, BUY | sys: MetaSystem | api: Bind(Sprite icon, string displayName, int price, bool affordable, Color buyColor, Action onBuyTapped) | dep: - | used: MetaShopView | crit: K2 | note: D-037: the BUY turns red when the row cannot be afforded, on top of the dim. The colour is handed IN, not chosen here -- both colours live on MetaShopView, because this class is a binder that decides nothing and two components each holding the same pair is a pair that can drift. It paints Button.targetGraphic, the button's own declared graphic: no new serialized reference, so no scene rebuild (which D-036 had just cost a round). Writing to Graphic.color survives the ColorTint transition -- Selectable applies the state tint to the CanvasRenderer and MULTIPLIES with Graphic.color rather than overwriting it, which is also why the authored green shows through. S5 (D-035) added the dim: an unaffordable row fades via a CanvasGroup alpha, which over the panel's dark background reads as darker -- the user's wording. ALPHA only, never interactable: the row's BUY must still open the preview, because seeing where a prop would go and what it costs is what makes it worth saving for, and disabling the row would make it dead furniture. The popup's BUY is what refuses, visibly. Pure BIND component, the same shape ModificationSlotView has under TicketCardView and for the same reason: how many rows there are is something only the catalog and the player's purchases know, so they cannot be laid out in the Editor ahead of time. Decides NOTHING -- whether an item is listed comes from MetaPurchase.ShopItems, whether it can be afforded will come from MetaPurchase.Evaluate in S5. The BUY listener is wired ONCE in Awake and Bind only swaps the stored callback: adding the listener inside Bind would stack silently the first time a row is rebound, and a BUY that fires twice is the worst bug this screen could have. A null icon HIDES the Image rather than leaving a white box -- MetaCatalogValidator already reports a missing sprite as a content error, so this row does not add a second verdict about it. Price is a bare number: the HUD's coin icon is the unit on this screen | h:cf091478
ExpoTheExplorer/Assets/Scripts/UI/ModificationSlotView.cs | one modification row: ingredient icon, direction icon, tinted box | sys: TicketSystem | api: SetModification(Sprite ingredientSprite, Sprite directionSprite, Color boxColor) | dep: Editor,TicketCardView | used: MetaShopRowView,TicketCardView | crit: K3 | note: cloned per modification by TicketCardView and bound in one call; holds no state and reads no config -- the card decides the sprites AND the box tint (which follows the ticket's patience type), so a row cannot disagree with the card it sits on | h:3f9470ae
ExpoTheExplorer/Assets/Scripts/UI/SoftMoneyView.cs | HUD soft-money counter | sys: ProgressionSystem | api: - | dep: HudWalletSource | used: - | crit: K3 | note: reactive in the day scene (binds SoftMoneyChanged, never polls); on the main screen there is no event to bind to, so the value renders once. Holds no GameManager since D-013 -- see HudWalletSource for why a shared prefab cannot serialize one. Reads through the SOURCE so one call serves both modes; still subscribes from Start, not Awake | h:601afe0f
ExpoTheExplorer/Assets/Scripts/UI/TicketCardView.cs | one ticket slot's card: content, customer photo, timer bar, danger flash | sys: TicketSystem | api: IsAnimating; SetTrayAnimating(bool animating); Initialize(GameManager gameManager, int slotIndex, TicketCardsView owner, BoardAnimationConfig animConfig); SuppressPollUntilDeliveryTransition(); PlayDeliveryTransition() | dep: BoardAnimationConfig,FoodCategory,FoodItemConfig,GameManager,GameState,ModificationSlotView,PatienceType,Ticket,TicketCardsView | used: TicketCardsView,TrayFillCounterView,WorldTrayView | crit: K2 | note: D-146 (2026-08-31) EXPOSES TimerTrack, read-only and for pointing at, the second property on this card that exists for the tutorial (FirstModificationRow was the first). Derived from the divider template's PARENT -- the expression RebuildTimerDividers already treats as the track -- rather than a fourth serialized field, so two names for one object cannot disagree and re-parenting the bar moves both at once. D-139 (2026-08-30) THE CARD DRAWS THE CUSTOMER'S FACE, AND OWNS NONE OF IT. SetCustomerPhoto writes .sprite and .enabled on customerPhotoImage and NOTHING else -- no colour, no alpha, no SetActive -- which is the whole contract that lets CustomerPhotoFrame stay a frame: the reference is wired to a WHITE CHILD Image inside that frame, so the frame's authored beige sits behind a transparent portrait instead of tinting it, and the prefab author keeps the tint. Wiring it to the frame's own Image is legal and makes the photo REPLACE the frame; the tooltip says so. The portrait itself is drawn once by TicketEntryFactory and read off Ticket.CustomerPortrait, never picked here -- RebuildContent runs on every ticket-reference change, so a face chosen in this file would be free to change on a rebuild of the SAME ticket. This is the SECOND optional reference on the card (dangerImage is the first) and it is optional for that same reason: ValidateReferences failing would set isValid false and take the whole card down -- an invisible ticket -- over a decoration whose degraded mode is the card exactly as it looked before portraits existed. It warns once at Initialize instead, and it also has to survive the TicketCard UpDown variant until both prefabs are wired. D-083 (2026-08-26) exposed FirstModificationRow, read-only and for POINTING AT: the tutorial's canvas-side arrow has to indicate the modification on the card, and the rows are built here at runtime so there is nothing in the scene to reference instead. It hands back the RectTransform rather than the ModificationSlotView so a caller gets a position and a size and no way to repaint the row -- the rows are rebuilt from the ticket on every refresh, so an outside write would vanish on the next one anyway. DANGER FLASH (D-073, rebuilt by D-074): once owner.IsInDangerZone says the bar has turned red, the ticket's PAPER (background.color) square-waves between the visuals config's two authored colours while the optional dangerImage blinks hard on/off in the same phase -- Mathf.Repeat off Time.time, so all three cards share one rhythm. D-074 replaced D-073's alpha pulse outright: dimming the CanvasGroup faded the customer name, the dish photo and the timer bar along with the paper, so the card was least readable exactly when it mattered, and a cosine ramp read as fading rather than as an alarm. Nothing here writes canvasGroup.alpha any more -- the delivery transition is its sole owner again. background.color returns to the PREFAB's own tint (captured once in Initialize), never to the flash's rest colour, which is a different thing. Computed inside RefreshTimer rather than run as a looping tween, and that is a lifetime decision: PlayDeliveryTransition kills and re-fades this same CanvasGroup, so a loop tween would fight it for the alpha, while Update is ALREADY silent for the whole transition -- the suppression that exists does what a tween would have needed new bookkeeping for. A 0 period means solid-on, never a clamped strobe. dangerImage is the ONE optional reference here (ValidateReferences would take the whole card down over a decoration that has a working degraded mode) and warns once at Initialize instead. polls its slot every frame because TicketDelivered/TicketCancelled fire BEFORE the slot is reassigned and FillEmptySlots fires nothing; the timer bar drains a Filled Image over the ticket's whole TimeLimitSeconds and clones one anchored divider tick per segment boundary, so an unevenly-divisible limit ends short rather than shifting the ticks; an empty slot hides the WHOLE card through canvasGroup.alpha=0 (never SetActive, which would pull it out of the parent HorizontalLayoutGroup and slide the other cards) and PlayDeliveryTransition's entry fade targets 0 instead of 1 when no ticket replaced the delivered one | h:60cc4821
ExpoTheExplorer/Assets/Scripts/UI/TicketCardsView.cs | spawns and owns the 3 ticket card slots | sys: TicketSystem | api: TicketSpriteFor(PatienceType patienceType); ModificationBoxColorFor(PatienceType patienceType); DirectionSpriteFor(bool isAddition); TimerSegmentSeconds; TimerFillColorFor(float remainingRatio); IsInDangerZone(float remainingRatio); DangerFlashColorFor(bool lit); GetCard(int slotIndex) | dep: BoardAnimationConfig,EconomyConfig,GameManager,GameState,PatienceType,TicketCardVisualsConfig,TicketCardView,TrayFillCounterView | used: TicketCardView,WorldTrayView | crit: K2 | note: D-074 added DangerFlashColorFor, which takes a HALF (lit/rested) and answers with a colour: the card asks which side of the blink it is on and holds no opinion about how loud the alarm is, which keeps this the single place PatienceType and the timer turn into visuals. D-073 named the critical tier as IsInDangerZone and made TimerFillColorFor CALL it, so "the bar is red" and "the card is pulsing" are one expression rather than two comparisons free to drift; no third threshold was authored, because a danger number of its own would be one the player can already see the wrong answer to. the single lookup point turning PatienceType (and the timer's remaining-time ratio) into visuals -- TicketCardVisualsConfig deliberately carries the values but no lookup logic; TimerFillColorFor takes its two THRESHOLDS from gameManager.EconomyConfig (they also pick the tip tier) and only its colours from the visuals config | h:b8ff4b13
ExpoTheExplorer/Assets/Scripts/UI/TrayFillCounterView.cs | the "x/y" fill readout under a ticket card | sys: TraySystem | api: Initialize(GameManager gameManager, int slotIndex, TicketCardView ticketCardView) | dep: GameManager,TicketCardView,TrayManager | used: TicketCardsView | crit: K3 | note: a poll, and it stays one -- the tray's count is moved by a SYNCHRONOUS cascade (TrayManager.TryAddItem -> deliverTicket -> TicketSlotManager.AssignTicket) and by a timeout scatter, so an event-driven label would need re-subscribing at every one of those seams. What the 2026-08-30 audit changed is that polling no longer WRITES: Refresh compares contents.Count and RequiredItems.Count against the last pair it wrote and returns before touching TMP_Text.text when neither moved, and the enabled flag is diffed the same way. Before that it interpolated a fresh "x/y" and assigned .text every frame -- three trays at 60 fps was 180 allocations and 180 layout/vertex dirty marks a second, and it was the ONLY frame-frequency allocation left in the project (every other .text write is event-driven or throttled to 1 s). TMP does not diff the assignment, so a label only stays clean if nothing hands it a new string. Both caches are seeded deliberately: the two counts start at -1 because "0/0" is a legitimate reading between tickets and a 0-seeded cache would swallow the write that first puts it on screen, while lastEnabled is read off the label in Initialize because it has to start TRUE or a first frame wanting the counter hidden would compare false-to-false and skip hiding it. Hidden for the whole of the delivery/scatter animation (TicketCardView.IsAnimating), which already rewrites tray and ticket mid-flight, so the readout lands on the settled count instead of flickering through transition values | h:6af6851d
ExpoTheExplorer/Assets/Scripts/UI/WorldTrayView.cs | one ticket slot's tray, its drop target, its return-to-board path, and (if it is the target) the tutorial spotlight's builder | sys: TraySystem | api: OnDrop(PointerEventData eventData); TryAcceptDrop(BoardItemDragHandler dragHandler); TryAcceptAutoCollectDrop(BoardItemDragHandler dragHandler); ReturnItemsToBoard(IReadOnlyList<BoardItem> items); SetHighlighted(bool active) | dep: BoardAnimationConfig,BoardItem,BoardItemDragHandler,BoardView,FoodCategory,GameManager,GameState,Ticket,TicketCardView,TicketCardsView,TrayManager,TutorialDirector,TutorialStep,TutorialSpotlightView | used: AutoCollectRunner,BoardItemDragHandler | crit: K2 | note: D-140 (2026-08-30) THIS TRAY DECIDES HOW BIG ITS CONTENTS ARE, because it is the only object holding both halves of the sum: the authored world size (animConfig) and the live cell size (boardView.CellSize). ResolveSeatScale is that division and it is handed to PlaceInSlot / PlaceInSlotAndDeliver, which is what stopped tray items rendering stacked -- an item used to arrive at board size, ~1.09 units against a 1.48-unit tray, so no slot spacing could have separated three of them. ResolvePlacementCategory is split out of ResolvePlacementSlot (which is now one line on top of it) because the destination decides TWO things now, where the item goes and how big it is drawn, and two separate walks of the same overflow chain are how those would eventually disagree; TryAcceptDrop resolves it ONCE into a local and both branches read that. The overflow RULE and OverflowOrder are untouched. Slot POSITIONS stay the scene's single writer -- the three child transforms are authored on TrayArea.prefab (main -0.20,0 / side 0.39,0.20 / drink 0.39,-0.20, the L that fills the 1.184x0.803 inner surface edge to edge) and no code writes them. D-129 (2026-08-28) A TRAY BELONGS TO A TICKET, NOT TO THE SCENE. Once the Day's sequence is exhausted TicketSlotManager.AssignTicket hands this slot null forever and TicketCardView.RebuildContent(null) takes the card to alpha 0 -- but PlayDeliverySuccess ALWAYS finished by growing a new tray back in, so a full-size empty tray sat under an invisible ticket until the day ended. trayShown now follows State.TicketSlots[slotIndex], and it changes only when nothing is moving: the delivery path simply SKIPS its re-entrance (the lift has already faded the tray to nothing, so hiding costs no tween and cuts no animation short), while the timeout/external-clear path plays PlayTrayExit, the exact mirror of the entrance, and only after RefreshClearedTray's SlotClearDuration has elapsed -- which is why RefreshTrayPresence polls instead of listening: the slot is emptied inside a SYNCHRONOUS cascade that runs at the START of a delivery, so an event-driven hide would fire mid-lift. PlayTrayEntrance is one animation with two callers (a delivery's next tray, and a slot handed a ticket again on a retry or a new Day). Every SetTrayAnimating call in the file now goes through a private method that writes a local mirror at the same time, so "is this tray mid-animation" has one writer. SetHighlighted refuses to light a hidden tray; dropping on one was already impossible (TrayManager.TryAddItem refuses a slot with no active ticket). D-115 (2026-08-28) THE TUTORIAL'S TRAY GATE IS ASKED OF A FINGER ONLY. TryAcceptDrop grew a respectTutorialGate flag: the public finger overload passes true, TryAcceptAutoCollectDrop passes false. Found in the first play-test -- pressing Auto-Collect during the lesson that FORCES it advanced the step and collected nothing, because an armed PowerupUse step answers IsTrayDropAllowed false for every tray, so the effect found nothing it could do and (correctly, GDD 5.2) cost no charge. The gate constrains the PLAYER, and a powerup's own machinery is not the player; the two entry points D-112 already separated are what made the distinction expressible without a new parameter reaching AutoCollectRunner. Safe by construction: GameManager.CanUsePowerup refuses every powerup during a forced move and behind a panel, so this bypass is only reachable while a PowerupUse step names that powerup. D-112 (2026-08-27) TryAcceptAutoCollectDrop is the powerup's way in -- identical to a finger's drop in acceptance, batch check and delivery, differing only in the tween's LENGTH. The multiplier is read off this view's OWN serialized animConfig rather than passed in by AutoCollectRunner, which is what keeps the runner free of a config reference and leaves nothing new to drag in the Inspector; the private TryAcceptDrop overload is the one path both callers run. ReturnItemsToBoard scales its fly-in the same way. D-110 (2026-08-27) ReturnItemsToBoard is Auto-Collect's return path: items this ticket never asked for go back onto the board so the tray becomes completable again and the item rejoins the pot. WHICH items is PowerupEffects.UnwantedTrayItems' answer, handed here by instance -- this method only carries it out, because this tray is the only object that can: a tray item's GameObject is a child of one of these three slots, and removing it from TrayManager without also taking the object out would leave the item drawn in a tray that no longer holds it. The move itself is the one BoardItemDragHandler.OnEndDrag already performs for a tray pickup dropped somewhere invalid (remove from tray, RequestSpawn under a fly-in override from this tray, destroy the object), reused rather than reinvented so a powerup-returned item lands exactly like a hand-returned one. Three guards, each closing a hole rather than being defensive: it refuses while deliveryInProgress (the model is already a ticket ahead), it skips an item the player is holding mid-drag (BoardItemDragHandler is the single writer of a held item's transform -- the same reason ClearSlot checks IsDragging), and it RESYNCS lastKnownCount, because Update's poll reads a drop to exactly zero as "cleared externally" and would run ClearAllSlotVisuals over objects that are already gone. D-100 (2026-08-26) a successful delivery now WAITS for the item that caused it to reach its slot: TryAcceptDrop hands PlayDeliverySuccess to BoardItemDragHandler.PlaceInSlotAndDeliver as the settle tween's completion callback instead of calling it inline. The user's report was exactly that -- the ticket left without waiting for the last item to go to its place, because that one item was the only one never seated: it grew and lifted from wherever the finger let go. It is an ordinary slot child now, so PlayDeliverySuccess stopped taking a finalItem at all and treats it like any other tray content. Two guards come with the wait, and both close holes the longer window opens rather than adding a feature: deliveryInProgress makes this tray refuse every drop from acceptance until the slots are emptied, because the MODEL is already a ticket ahead -- an item accepted in that window would be counted by TrayManager and then destroyed unseen by DestroySlotChildrenImmediate; and Update releases that flag if the delivering item is destroyed before its tween completes (a retry), since DOTween KILLS such a tween rather than completing it, and without the release this tray would refuse drops for the rest of the session. The wrong-order path is deliberately untouched and still shakes the un-parented final item the instant the batch resolves: that beat reads as an interruption on purpose, and seating an item about to be thrown back onto the board would only lengthen it. D-098 (2026-08-26) IsTutorialTarget asks TutorialDirector.SpotlightTraySlotIndex instead of comparing Current.TargetTraySlotIndex itself. This was the project's one reader of a step's fields that did not gate on Kind first, and a panel step leaves those fields unset -- unset being 0, tray 0 claimed the powerup panel and tried to raise a ghost from the equally unset cell (0,0), which is the LogError the user saw. The two failure sentences in RaiseTutorialSpotlight only became true again with this fix: "the model has an item the view never built a container for" was being printed about a cell no step had named. D-083 (2026-08-26) the subscription moved from "a tutorial was armed" to "the step changed", which is what makes a tray both RAISE its spotlight when it becomes the target and TEAR ITS OWN DOWN when it stops being one -- every tray answers the same event and a tray that is neither does nothing, so the three need no coordination. The drop path deliberately does NOT dismiss anything itself: NotifyTrayAccepted publishes the boundary, every tray dismisses synchronously, and only a tween tick later does the new target raise the next spotlight. One mechanism for the boundary rather than a second copy of it. D-082 (2026-08-26) gave this class the tutorial's drop gate, the step's completion call, and the spotlight. The SPOTLIGHT lives here for a mechanical reason, not a tidiness one: this is the only object that already holds both of the ghost's endpoints -- a serialized boardView for the source cell and its own transform for the destination -- which is what let the whole effect ship with no new scene object and nothing to wire. It subscribes to GameManager.TutorialArmed AND syncs once in Start, because the first arm happens in Awake (before any subscriber exists) while a retry re-arms mid-scene (long after every Start has run); either half alone misses one of the two. The build is deferred one tween tick since BoardView creates item containers in its OWN Start and Unity does not order Starts between root objects. The drop gate refuses by returning false, which is already this method's "not accepted" answer, so TraySystem never learns tutorials exist. world-space on purpose, NOT Canvas UI, so a dragged item's real GameObject can be reparented straight in; a standalone scene object per slot with slotIndex set by hand and ONE BoxCollider2D for the whole tray -- which of the 3 slot transforms an accepted item lands in comes from its FoodCategory, and ResolvePlacementSlot overflows to another empty slot so two same-category items never render stacked; polls TrayManager's count every frame ONLY to catch a timeout scatter, reading count==0 as "externally cleared" because a manual pickup only ever decrements by one; the wrong-order shake (IsShakeable) and ClearSlot both skip an item the player is currently holding -- a second writer on a held item's transform is what pinned one at the tray's X while the finger moved away The seat scale divides by the item's OWN Config.OverallScale as well as by cellSize: BoardItem pads every food by that factor (0.85 on 19 of 21, 0.8/0.9 on the rest) so a board item does not touch its cell edges, and ignoring it delivered every tray item 10-15% under its authored size -- visible as gaps in the tray, and the reason the numbers were retuned twice before the cause was found. Cancelling the padding is right for a TRAY (three fixed places whose art should fill them) and wrong for the board, which is untouched. | h:41a7811b
ExpoTheExplorer/Assets/Scripts/UI/MetaBackdropView.cs | day scene backdrop: the decorated grounds, blurred | sys: MetaSystem | api: - (all private; built once in Start) | dep: MetaResolver,MetaLayout,MetaCatalog,MetaLocation,GameSession,SessionHost | used: - | crit: K3 | note: D-050. The day is played AT the expo the player decorates, so SampleScene's background becomes their own grounds -- the location art with the props they OWN standing on it -- blurred behind the board. Built ONCE in Start and never again: a purchase shows up in the NEXT day's backdrop, not halfway through the current one, which is correct because the grounds are not something the player can change while a day runs. After Start it is a small texture on a RawImage, so the per-frame cost is one draw call. NO CUSTOM SHADER: the blur is progressive halving through Graphics.Blit, and a bilinear sample at half size lands exactly between four source texels, so each step is an exact four-texel box average. That is the right answer HERE specifically because the project runs the BUILT-IN pipeline -- the URP package is installed but no pipeline asset is assigned in GraphicsSettings -- so renderer features are unavailable and the alternative was a multi-tap shader running full screen every frame for an image that never changes. The result is left SMALL: stretching it across the screen is itself a bilinear filter, so the upscale finishes the blur for free. Composed at composeScale (0.25) because the output is about to be blurred into soft shapes and full resolution would spend memory on detail thrown away one step later. WHICH location and WHAT stands on it are MetaResolver's two calls, the same ones the meta screen makes, so the backdrop cannot show a different expo than the one the player walks around in; it reads the catalog and the owned set and writes NOTHING. Every failure path leaves the RawImage exactly as the author left it -- that IS the fallback, and there is deliberately no fallbackTexture field, because a second place to author the same thing is a second place for it to be wrong. The final RenderTexture is OWNED and released in OnDestroy (graphics memory is not garbage collected, and a scene reloaded a few times would leak one backdrop per load), and it is cleared off the RawImage first so nothing draws a released texture. Sprites are drawn through the sprite's own textureRect, not the whole texture: drawing the whole texture works right up until the art is packed into an atlas, and then every prop becomes a collage of its neighbours. | h:67b027e3
STALE ExpoTheExplorer/Assets/Scripts/UI/DayRewardFlightView.cs | the day's payout, performed: stars seat, gems and coins fly to the HUD | sys: DayLifecycle | api: Play(IReadOnlyList<GameObject> starsToSeat, RectTransform coinOrigin); CompleteImmediately() | dep: StarScoreBarView,GameManager,DayRewardPurse,HapticsBinder,HapticConfig | used: DayCompletePopupView | crit: K2 | note: HAPTICS (D-070): one buzz per star, requested on the frame the star POPS rather than when its bounce settles, because the pop is what the eye reads as the landing and the two senses agreeing is the point. Three in a row is the day's crescendo -- which is why the day's success is felt HERE and not on the popup opening, and why DayCompleted was left out of the haptic table entirely. The COIN landings deliberately have no haptic: their count scales with the day's earnings, so one per coin would turn the phone into a buzzer; if they are ever wanted it needs a rule (first + last, or every Nth), not a call site. D-057. Nothing is credited during a day, so THIS is what hands the earnings over -- each landing icon calls GameManager.ClaimRewardGems/ClaimRewardSoftMoney, which is why the HUD views needed no change at all: they are already reactive, so what the player watches arrive and what they own are one event instead of two kept in step. Amounts are never passed in; they are read from GameManager's pending purse, so this class cannot disagree with the ledger. It CANNOT lose money -- every exit from a finished day commits the remainder first, so an unwired reference, a skipped sequence or a mid-flight tap all still pay in full; killing a tween deliberately does not run its OnComplete, so a destroyed icon never claims its share and the commit pays it as one lump instead. Two ordering rules are load-bearing: CompleteImmediately guards on IsPlaying, or the call at the top of Play would commit the purse OnDayCompleted had just filled; and RunSequence clears starsBeingSeated before its own commit so a later tap cannot re-enter. Relies on Unity's Awake/OnEnable-before-Start guarantee for the purse to exist when Play runs (GameManager subscribes DayCompleted in OnEnable, the popup in Start) -- if that ever inverted the animation would render an empty debt and the exit would still pay, so the failure is degraded rather than broken. Lives on the always-active popup object, NOT under popupRoot (a coroutine dies with its GameObject); flightRoot is outside popupRoot too, which also makes icons draw above the popup. Icons are built in code from two serialized sprites rather than a prefab -- 13 per completed day is event frequency, so pooling would be cost with no saving. Positions cross two canvases (receipt on the popup, counters on the shared HUD prefab), so everything converts through screen space; CameraFor walks up from a reference already handed in, which is not a scene lookup. ONE flightRoot serves all three stars -- it is the coordinate space the icons live in, not a launch point; each gem is measured off the star that just seated, so the three seats give three different origins. ToFlightLocal takes a plain Transform rather than a RectTransform so a non-UI star still launches its gem: the earlier cast-and-branch skipped silently, which is the wrong failure here (the gem vanishes, the money still arrives on the exit commit, and nothing on screen says why) D-061: an OPTIONAL score bar now PACES the star loop -- each star waits on the fill reaching its own notch instead of a fixed starSeatInterval, so the thing that earned the star and the star itself are one motion rather than two animations that coincide. The gem shares are untouched (still per star index), so money cannot drift out of it. Three guards keep the wait from hanging a coroutine that is holding the day's payout: it also breaks on IsFillFinished, an unwired fill Image reads CurrentFill 1, and an out-of-range notch reads as the right edge. Unwired, the fixed-interval rhythm is kept verbatim. D-071 adds GemLanded and CoinLanded, both requested on ARRIVAL inside Launch's onArrive rather than at launch: the buzz belongs to the counter ticking up, which is when the money becomes the player's. COINS ONLY BUZZ FIRST AND LAST, and that is arithmetic rather than taste -- coinSpawnInterval is 0.06s and there are up to coinCount of them, so one haptic per coin fires faster than a preset lasts and the hand would feel half a second of undifferentiated rumble instead of ten ticks. Two beats say it better: the money started arriving, and it has finished. Gems need no such rule (at most three, spaced by the star rhythm). The first/last flag is captured into a PER-ITERATION local because this is a `for` loop -- closing over `j` itself would read whatever it had reached by the time the flight lands. D-141 (2026-08-31) THE "GEM" IS A STAR ON SCREEN: gemSprite is wired to Art/UI/Popup/DayCompleteUI/Star.png and gemFlightTarget to the HUD star counter, so the code's gem is the LEDGER's word (the purse pays gems per star) and the sprite is its costume -- worth knowing before touching anything on this path, since the two names have already been read as two different things. That confusion had a cost: the star and the coin shared one flightIconSize (48), so the star the player had just earned arrived at the counter the size of one tenth of a total. Launch and SpawnIcon now take the size as an ARGUMENT and the star has its own serialized starIconSize, landed at 180 -- 2.5x by the user's first number and then 1.5x again on sight ("ya 1.5 kat daha buyutur musun"), so 3.75x the coin and nearly twice the 100px star it launches from. That the answer arrived in two passes is the argument for the field existing: the number is a look, it was always going to be tuned by eye, and the shared field could not have been moved without dragging the coins along. The asymmetry is the design: at most three stars, each a whole star, against up to coinCount coins that must read as a spray of change rather than ten separate prizes. Both numbers are also written into SampleScene.unity rather than left to the C# initializer, because a serialized MonoBehaviour that does not name a newly added field is this project's known stale-serialization trap. | h:3f92d8fc
ExpoTheExplorer/Assets/Scripts/UI/StarScoreBarView.cs | the star score as a fill bar, and the clock the star row runs on | sys: DayLifecycle | api: CurrentFill; IsFillFinished; PositionOf(int starIndex); Prepare(float scoreProgress, float twoStarDuePosition); BeginFill(); SnapToTarget(); ShowTargetWithoutAnimating() | dep: - | used: DayCompletePopupView,DayRewardFlightView | crit: K3 | note: added by D-061. Full == three stars, not 100% of the score: a bar that keeps filling past the last star communicates nothing. D-062 REMOVED its other half: it no longer positions the stars (three marker fields + PlaceMarker are gone), because the user rejected stars standing on their thresholds -- three 100px stars across a 450px bar stop reading as a three-star rating. A star's position is the scene's business now and no code writes one. Deleted rather than left unwired on purpose: PlaceMarker no-oped on a null marker, so the old behaviour would have been one drag away, and the scene's three markers all sat at the same point (0,0 inside Fill) so clearing the slots would have stacked the stars. It also PACES the seating: the flight waits on CurrentFill reaching PositionOf(i) instead of counting fixed intervals, which is what ties "my speed earned that star" to the star that pops. CurrentFill returns 1 when the fill Image is unwired, deliberately -- this value gates a sequence holding the day's payout, so an unwired bar must let it straight through rather than stall it | h:3f855661
ExpoTheExplorer/Assets/Scripts/UI/HapticButton.cs | makes one Button buzz when tapped | sys: HapticsSystem | api: - (no public surface; it hooks its own Button's onClick) | dep: HapticsBinder,HapticMoment | used: - (attached in the editor,never referenced from code) | crit: K3 | note: the dep? draft's five view classes are all COMMENT mentions -- this file references none of them, which is exactly its reason to exist. It replaces what would have been a haptics field plus a Request call in each of MainScreenView, DayCompletePopupView, GameOverPopupView, MetaShopView, MetaShopRowView, MetaGroundsView and NoKeysPopupView. THE DECIDING REASON IS NOT TIDINESS: the shop's BUY button lives on an INACTIVE template row that MetaShopView clones per offer, so the alternative design (a Button[] list on HapticsBinder) would have missed the button most worth feeling -- scene lists cannot name objects that do not exist yet. A component on the template rides along with every clone, and because Awake never runs on the inactive template, only the live rows register. The moment is a serialized field rather than a fixed UiTap so one button can be given its own character without code. Listener order against the view's own handler is irrelevant by construction: same frame, so HapticsService picks on priority and UiTap (5) sits below every outcome | h:c104933c
STALE ExpoTheExplorer/Assets/Scripts/UI/LifeLostHeartView.cs | a broken heart rising off the tray that cost a life | sys: LivesSystem | api: Show(int slotIndex) | dep: BoardAnimationConfig,GameState | used: GameManager | crit: K3 | note: D-078, reshaped by D-079 and D-080. THE AUTHORED OBJECT IS THE ANIMATED OBJECT: one INACTIVE canvas Image per slot, placed OVER that tray at the exact start position, switched on and drifted up and put back -- no template, no cloning, no anchors, because the thing you place should be the thing you see. Rest position and colour are captured once in Start and re-applied on every play, so a second heart never starts halfway up and half faded (a running flight is DOKilled and restarted from rest). D-080 moved it from a world SpriteRenderer inside the tray to a canvas Image over it, which killed two hazards at once: a sprite parked in the tray had to win a silent sorting-order fight with the tray (order -2) and its contents, and it sat under animations that sweep every child renderer. A canvas draws over the world, and these are not tray children, so there is no second writer on this alpha BY CONSTRUCTION rather than by checking. Rise is an anchoredPosition tween in RectTransform units (the ticket card's own 150-unit scale), never world units. Driven from GameManager.HandleLifeLoss because the tray sees only HALF the losses -- its poll notices a timeout only when there were items to scatter, so an untouched ticket expiring leaves no trace there, which is why the slot index was threaded through TicketSlotManager and TrayManager. An empty array slot warns and shows nothing: there is no sensible fallback position for a heart whose whole point is the position the author gave it | h:de0a3a78
ExpoTheExplorer/Assets/Scripts/UI/PowerupBarView.cs | the day scene's three powerup buttons + their charge counts | sys: PowerupSystem | api: - | dep: TutorialPowerupIntroView,TutorialPowerupSpotlightView,PowerupConfig,GameManager,PowerupManager,PowerupType,PowerupButton,PowerupShopView,TicketCardsView,GameState | used: - | crit: K3 | note: D-147 (2026-08-31) the dimming lesson now lights ONE card -- the ticket that armed it, which is the one Time Reset will refill. The other two slots' timer bars went dark with the powerup's reach: they had been kept lit so a red bar had something to be compared against, but that argument belonged to a powerup that refilled all three, and lighting a bar this press will not touch promises something the charge does not deliver. D-146b (2026-08-31) the triggering ticket is lit WHOLE and the other two keep only their bars (LitTargetsFor, renamed from LitTimersFor). Lighting the CARD is what made that one line rather than three: DangerImage, Clock and TicketTimerBar are all direct children of it, so the exclamation and the clock ride along and neither needed a reference of its own. The lit card is deliberately NOT also given its bar -- a lift inside a lift is a nested sorting scope that changes nothing and would need explaining forever. D-146 (2026-08-31) DECIDES WHICH LESSON DIMS, and decides it from the AUTHORED TRIGGER rather than from a powerup's name: LitTimersFor returns the three timer tracks for a TicketPatienceBelow press and null for everything else, so naming Time Reset in a UI file was avoided and the next scheduled powerup is not silently left out. It hands over EVERY slot's timer, not just the ticket that tripped the trigger -- the player has to learn to read the bar, and which ticket tripped it is TutorialDirector's business. The step's message flows again (D-145 had cut it when the lesson had nothing to say; PowerupConfig now authors a line for the deferred press). ticketCardsView is OPTIONAL and is a SCENE reference on a component living in the HUDCanvas prefab, so it can only be a per-scene override -- same constraint as gameManager, same warning about Apply All. Unwired, that lesson simply does not dim, which beats a dim over dark timers. D-145 (2026-08-31) STOPPED PASSING THE STEP'S MESSAGE to the spotlight: that lesson's marker is an arrow now and says nothing, so BuildSpotlight takes only the powerup type and Create takes only the button. TutorialStep.Message and TutorialDirector.CurrentMessage stay -- the MOVE steps do author sentences; only this reader left. D-119 (2026-08-28) a locked powerup also hides its Background child. It needed a serialized field for the dullest possible reason -- Render can only switch objects it holds a reference to, and Background had never been introduced to the code, so it showed through every lock. The powerup ICON is deliberately left alone: it is the Button's own targetGraphic (IconFor reads Button.image.sprite), the blocker covers it anyway, and the user named exactly two things. The hierarchy was read out of HUDCanvas.prefab rather than guessed -- the bar lives in that PREFAB, not in SampleScene as an older comment in this file still claims. D-117 (2026-08-28) draws and enforces the LOCK: a powerup untaught is dim, unpressable, shows an authored lock badge with the Day it opens on, and hides its count and Add badge (a number on something unusable is noise; an invitation to buy it is worse). Refused in OnPressed as well as dimmed in Render -- Render decides what the player sees, OnPressed holds if anything re-enables the button. THE DAY IS READ THROUGH gameManager.Session.CurrentDay, not a GameManager property, because Session is the seam both scene roots share. Re-rendered on State.CurrentDayIndexChanged and NOT on the tutorial's step event: ArmTutorial fires nothing at all on a Day that authors no lesson (most of them), and AdvanceToNextDay reuses this scene rather than reloading it, so a bar listening only to the tutorial would keep yesterday's locks for the rest of the game. Fails OPEN on every uncertainty. D-116 (2026-08-28) holds the two tutorial popups as PREFAB references and instantiates them, where it used to have the views build themselves from nothing. Both are OPTIONAL and fail differently on purpose: an unwired INTRO prefab skips the lesson (the panel was the only way to continue), while an unwired SPOTLIGHT prefab still runs it -- the press is required and still the only permitted action, it is just unmarked. The intro is instantiated with NO PARENT, because its root carries its own Overlay canvas and parenting it under this bar's Screen Space - Camera canvas is exactly how it would end up buried (D-086). FindFont went with the change: it borrowed a TMP font off a count label so a code-built panel could match the game's type, and an authored prefab carries its own. D-115 (2026-08-27) builds BOTH powerup tutorial steps, for the reason the target tray builds a move step's spotlight: it is the object that already holds what they need -- the icon is a sprite on a live button, and the target of a forced press IS a live button. OnTutorialStepChanged tears down first and builds second, both halves unconditional, so panel-to-press, press-to-nothing and an abort out of the middle of either are all the same code. OnPressed gained the tutorial's third gate (GameManager.CanUsePowerup) asked at PRESS time rather than by disabling the other two buttons -- the same choice D-069 made for keys and D-105 for the shop -- and completes the step on the press rather than on the effect, because an effect with nothing to do refuses and costs no charge. Every failure path here SKIPS the step rather than blocking the day, including the one that would genuinely softlock: a forced press against a button that was never dragged in. The spotlight is torn down through Dismiss rather than Destroy, because it puts objects on the BUTTON's hierarchy, which outlives this view. D-087 (2026-08-26) this view builds the tutorial's powerup-intro step, for the reason the target tray builds a move step's spotlight: it is the object that already holds what the step needs. The three icons are the sprites on the three live Buttons, and nothing else in the project knows where those are. The button fields stay PRIVATE -- only a read-only IconFor is added, because a caller has no business reaching the Button itself, which is what spends a charge. Every failure path (no font, no config) skips the step rather than leaving a modal blocking a frozen day. powerup-plan Adım 2. SPENDS, never sells -- buying is the main screen's job, so there is no price and no purchase path here. BUILDS NOTHING: the panel, the three buttons and their Count labels are authored by hand in SampleScene and every reference is dragged, so no setup script exists (a generator would be a second authority over objects the author already arranged). Three NAMED blocks rather than an array because the scene's sibling order (AutoCollect, LeakCleaner, TimerReset) is NOT the enum's -- an array would pair the middle button with the middle enum value and put the wrong count under the wrong icon, which reads as a balancing oddity rather than a wiring bug. Dimming is keyed on the CHARGE COUNT, deliberately not on PowerupManager.CanUse: CanUse also asks whether an effect is registered, and effects only arrive in Adım 4-6, so gating on it would draw all three dead from day one. `interactable = false` is what makes an empty press a no-op with no branch in the handler, and is why there is no popup here (an empty key locks a player out of playing; an empty charge costs a convenience). Start not Awake, the project-wide rule -- GameManager.Awake builds the session and Unity does not order Awake across objects. The scene object is called LeakCleanerButton while the type is NoiseClear; same thing, and renaming a third time would have cost another save migration. D-105 (2026-08-27) REVERSED "pressing an empty powerup does nothing": with an optional PowerupShopView dragged in, a powerup at 0 stays interactable, lights the author's inactive "Add" child (a new optional emptyBadge per block) and opens that shop, holding the day still through GameManager.HoldPause/ReleasePause while it is up. The branch is read from the LIVE charge count in OnPressed, not from what Render last drew, because the count can move under an open shop. Order is load-bearing: shop.Open() must SUCCEED before the pause is taken, or a day freezes with nothing on screen to lift it. Gated on GameManager.CanUsePowerups, the same gate a USE passes. Fails all the way back to the old behaviour with no shop wired -- dim, unpressable, no popup -- and ShopIsReachable also requires a PowerupManager, since Start attaches no click listeners without one | h:fcafbc6e
ExpoTheExplorer/Assets/Scripts/UI/PowerupShopView.cs | powerup shop panel: three rows, owned count, Gem price, BUY | sys: PowerupSystem | api: Closed; Open(); Close() | dep: SessionHost,GameSession,GameState,PowerupManager,PowerupType,PowerupShopRow,PopupFade,BoardAnimationConfig | used: - | crit: K2 | note: D-117 (2026-08-28) a LOCKED row refuses to sell and hides its price, showing the lock instead: a price on something that cannot be bought reads as an invitation. Asked through PowerupManager.IsUnlocked rather than a PowerupConfig of its own -- this view reads even the PRICE through the manager, and a second scene pointer to the same asset is invisible until one of them is wrong. Guarded in Buy as well as in Render, for the reason the bar guards twice, and silent there because the lock is already on screen saying why. The day is read once per refresh for all three rows so they cannot disagree. Fails OPEN: no manager or no resolved Day leaves everything buyable, because a powerup nobody can buy over a forgotten reference is worse than one that opens a day early. powerup-plan Adım 3, the step that closes the economy loop (earn on day complete -> buy here -> spend in the day scene). BUYING IS MENU-ONLY by design, not layout: a store opened mid-service suspends the time pressure the powerup exists to relieve. Deliberately NOT a tab inside MetaShopView -- that file owns props + SoftMoney and belongs to MetaSystem, so a tab would teach MetaSystem's view what a powerup is and give one 30KB class a tab state, a second currency and a second row shape. It carries its OWN open button exactly as MetaShopView carries marketButton, which is what keeps MainScreenView unaware powerups exist and is why this step added NO new blueprint arrow. Three authored rows, no cloned template: MetaShopView clones because only the catalog knows the row count, here it is three and written in the enum. NO confirm popup, unlike MetaShopView -- a prop is expensive and a placement decision, a charge is a few Gems with a visible effect. Unaffordable BUY is non-interactable rather than dimmed-but-tappable (the opposite of MetaShopRowView) because that row's popup can EXPLAIN a refusal and this shop has none; a silent no-op tap would be the worst of the three. Subscribes to BOTH ChargesChanged and GemsChanged and redraws all three rows, because one purchase moves the shared balance and can change what the OTHER rows may do. Save() sits in the same method as the purchase (MetaShopView's rule): without it the player spends Gems now and finds the charge gone next launch. Panel hidden BEFORE validation (D-036: an incomplete shop must be invisible, not stuck open). Start not Awake, project-wide rule. D-105 (2026-08-27) made it serve the DAY SCENE too, which reverses "buying lives on the menu, not in the day" -- the objection (a store suspends the time pressure a powerup relieves) is answered by the day being FROZEN behind it, and this view still knows nothing about that: the caller holds the pause. Open() is public and returns whether the panel actually came up, Close() is idempotent and raises Closed, and openButton left ValidateReferences' required list because the day scene deliberately has no opener (an empty powerup is it). panel and closeButton stay required -- a shop with no way out is a trap when it is also holding the day still. OnDestroy raises Closed if the panel was still up, so a scene load mid-purchase cannot strand a caller's pause. D-107 (2026-08-27) added an optional backdropButton wired to Close, reversing D-105's refusal to let the scrim dismiss: a full-screen block with only a corner X reads as being trapped. It needs no inside/outside hit-test, and that is why it is a wired Button and not a pointer handler -- the sheet is a CHILD of the backdrop and carries its own raycast-target Image, so UGUI gives a tap on the sheet to the sheet and only an outside tap reaches the button. Wire it to the same object `panel` points at 2026-08-31: the panel fades up through PopupFade at BoardAnimationConfig.PopupFadeInDuration, after RefreshAll and after SetActive — refreshing FIRST matters, because rows correcting their prices during a visible fade would be seen at this duration. This view lives on a PREFAB, so the optional animConfig is dragged there once and every instance carries it. Close() stays instant. | h:f2fc9152
STALE ExpoTheExplorer/Assets/Scripts/UI/AutoCollectRunner.cs | Auto-Collect's DOING half: returns tray junk, then carries out a closed plan | sys: PowerupSystem | api: Run() | dep: AutoCollectMove,BoardItem,BoardView,BoardItemDragHandler,GameManager,GameState,PowerupEffects,Ticket,TrayManager,WorldTrayView | crit: K2 | used: GameManager | note: D-113 (2026-08-27) every move now calls handler.SettleForAutoCollect() before handing the item over -- the pickup bookkeeping no finger is here to do. D-112 (2026-08-27) it calls TryAcceptAutoCollectDrop instead of TryAcceptDrop, so the items it fetches fly in at the authored multiple of the ordinary settle -- the return half was already slowed inside ReturnItemsToBoard. It holds no BoardAnimationConfig of its own: the tray reads the number. D-110 (2026-08-27) a press is now three ordered steps instead of a per-slot search loop: (1) every tray's unwanted leftovers go back to the board via WorldTrayView.ReturnItemsToBoard -- BEFORE the snapshot, which is why a returned item is part of this press's budget rather than a phantom; (2) the active tickets are captured by instance and PowerupEffects.PlanAutoCollect decides the WHOLE press against the board as it stands; (3) the plan is walked in order. Each step is re-verified but never re-decided: the slot's ticket must still be the captured instance (its own last move may have delivered it) and the cell must still hold the exact BoardItem the plan reserved -- taking whatever landed there instead is precisely the bug this rewrite removes, since a delivery backfills the cells behind it. Run() still answers the "no wasted press" question, and it now counts a press that ONLY returned items as work: the board visibly changes and a tray that was guaranteed to cost a life is un-jammed (the user's call, asked and answered). MaxMovesPerSlot is gone with the loop -- a closed plan cannot spin. powerup-plan Adım 6. It calls WorldTrayView.TryAcceptDrop with the board item's OWN drag handler rather than writing into TrayManager, and that is not stylistic: a tray's contents are drawn entirely by the dragged GameObject being reparented, so a data-only collect would be perfectly correct in state and show an EMPTY tray. Reusing the drop path also inherits every rule already attached to it -- detach-before-batch-check, the delivery lift/fade, the card's exit, the tray's re-entrance -- none re-implemented, none able to drift. ONE PRESS = THE TICKETS IN HAND: the active tickets are captured BY INSTANCE first and a slot stops the moment its ticket is replaced, because filling a tray delivers it, which synchronously assigns a new ticket whose required items then spawn -- a loop that kept going would chain deliveries until the Day's sequence ran out. Reference equality, not slot emptiness, because the replacement occupies the same slot. The plan is re-asked every iteration for the same cascade reason. MaxMovesPerSlot is a hang guard, not a design number (a ticket needs at most three items) and logs if ever reached. NO EditMode test can cover this file -- Assembly-CSharp is predefined and no asmdef may reference it -- which is exactly why every rule was pulled into PowerupEffects. A paced version (one item every authored interval, via a coroutine) was written on 2026-08-26 and REVERTED at the user's request the same day; it is not in the code and this note is the only trace, so that the next reader knows the instant feel is the current state rather than an oversight D-100 (2026-08-26) a tray now refuses drops while it is delivering (WorldTrayView.deliveryInProgress). This file cannot trip that guard: the ReferenceEquals check above stops a slot the moment its ticket is replaced, which is the same instant the guard goes up. | h:ad65b35a
ExpoTheExplorer/Assets/Scripts/UI/TutorialDim.cs | darkens the screen, keeps a few things bright, puts it all back | sys: Tutorial | api: DimSortingOrder; LitSortingBoost; LitElementSortingOrder; IsBuilt; Build(Transform parent, float opacity); LiftSprites(Transform target); LiftElement(GameObject target); LiftInteractiveElement(GameObject target); Curtain(RectTransform element, float opacity); Restore() | dep: - | used: TutorialSpotlightView,TutorialPowerupSpotlightView | crit: K3 | note: D-146c (2026-08-31) A NESTED SORTING CANVAS TOOK THE POWERUP BUTTON'S CLICKS AWAY, found in play, and the fix is why there are now TWO lift methods. Unity registers every Graphic against its NEAREST enabled Canvas ancestor and a GraphicRaycaster only tests the graphics registered to its OWN canvas -- so adding a Canvas to the button for sorting moved it out of the root canvas's raycaster's sight, and the one thing the player was allowed to press went dead with no error, no warning and a button that still drew and still highlighted. LiftInteractiveElement adds a GraphicRaycaster beside the canvas; LiftElement stays raycaster-free for the display-only lifts (ticket cards, timer bars). TEARDOWN ORDER IS LOAD-BEARING: raycasters are destroyed BEFORE canvases, because GraphicRaycaster carries [RequireComponent(typeof(Canvas))]. overrideSorting is also cleared before each canvas's destroy, so that if a Unity version ever refused that removal the leftover canvas is inert rather than parked above a dim that no longer exists. The comment this replaced claimed sorting order 'does not touch' clicks, which was wrong and is worth leaving recorded: the original method had only ever been used on display-only ticket cards, so the claim had never been tested. D-146 (2026-08-31) new, EXTRACTED from TutorialSpotlightView when the powerup lesson needed the same effect -- the third time this project has faced a duplicate of a subtle mechanism (D-123 and D-126 each deleted a second arrow-texture generator) and the first time it moved the original instead. A PLAIN CLASS, not a component, owned by whichever view is running a step: the sheet is parented to that view's transform, so a view that dies without calling Restore still takes its own sheet with it. What it cannot take with it are the sorting orders it changed on OTHER people's renderers, and that is the whole reason the bookkeeping exists rather than a bare AddComponent at the call site. THE THREE CONSTANTS ARE THE POINT of it being one file: 500 for the sheet (above the board and the trays, below BoardItemDragHandler's +1000, so a picked-up item stays visible for free), +600 for a lit world sprite, 800 for a lit CANVAS element -- and a canvas element needs lifting at all only because this scene's canvases are Screen Space - CAMERA at -1, where the sheet covers them. Build RETURNS FALSE with no camera, so a caller can decide whether a step without its dim is worth running; nothing is half-built on that path, which matters because a half-applied dim reads as a rendering bug rather than a lesson. Restore is idempotent because both callers restore at a step boundary AND again from OnDestroy -- the boundary one must be synchronous, since Unity defers Destroy to the end of the frame while the next step's visuals are built immediately. | h:4a1f21e6
ExpoTheExplorer/Assets/Scripts/UI/TutorialSpotlightView.cs | one tutorial step's look: dim, sorting lifts, ghost, arrows, message | sys: Tutorial | api: Create(TutorialDirector director, BoardAnimationConfig animConfig, Transform sourceItem, BoardItem sourceBoardItem, Transform trayTarget, IReadOnlyList<Transform> litExtras, IReadOnlyList<RectTransform> dimmedCards, TicketCardView targetCard); Dismiss() | dep: BoardAnimationConfig,BoardItem,ModificationConfig,TicketCardView,TutorialDirector,TutorialDim | used: WorldTrayView | crit: K3 | note: D-146 (2026-08-31) THE DIM AND THE LIFTS MOVED OUT to TutorialDim, unchanged in behaviour and with their three sorting constants: the powerup lesson needed the same effect and a second copy of numbers that must agree across files is how they stop agreeing (the shape D-123 and D-126 each deleted once). What is left here is what only a MOVE step has -- the ghost, the item and card arrows, the step-hints instance. This file lost 134 lines and its public API is untouched. D-126 (2026-08-28) the two MODIFICATION ARROWS joined the message in the authored prefab, and six methods left this file with them: BuildModificationArrows, BuildWorldArrow, BuildCanvasArrow, BuildMessage, ApproximateWorldSize and CreateArrowSprite -- the LAST generated-arrow-texture in the project (D-123 removed the other copy). BuildStepHints instantiates the prefab when EITHER flag is set and switches off what the step did not ask for, because highlightModification and a non-empty message are INDEPENDENT authored decisions: a step may point without speaking. The two arrows move DIFFERENTLY, after two corrections from play the same day. The CARD arrow is reparented into the ticket card's row and rides it. The ITEM arrow stays on the prefab's own canvas and is only POSITIONED over the board item -- parented to that item it rode along when the player picked the food up, and it marks a CELL to look at rather than an object to follow. It became a UI Image in the same pass (it was a world SpriteRenderer), which also removed the sorting-order this class used to set against its own dim: an Overlay canvas draws above every camera-rendered thing unconditionally. The board item is a world object and the arrow is on an Overlay canvas, so the two are bridged through SCREEN space, the only system they share, with the authored anchoredPosition kept as the offset and scaled by the canvas. The world arrow's SORTING ORDER stays in code: it is defined against the dim this class builds, so an authored number would be a second copy of a constant that must move with it, and a wrong one hides the arrow with nothing reported. D-122 (2026-08-28) BuildMessage stopped assembling a canvas, a plate and a label out of literals and now instantiates an authored prefab. The user asked for a bigger, more readable message, and the plate's colour, its height and its paddings were all in CODE -- so every tuning pass had to go through a programmer, and the height was fontSize*3, which clipped the moment a larger font wrapped the message onto a second line. The prefab comes off BoardAnimationConfig rather than a serialized field on this view, because WorldTrayView builds it and there are THREE tray views in the scene: a field there would be three drags that must never disagree. The TMP is taken out of the instance this method just created, which is not the scene search this project bans, and the ticket card's font is now only a FALLBACK for a prefab that shipped without one -- the author's font wins. Instantiated with no parent because the prefab's root carries its own Overlay canvas (D-086: on the cards' Screen Space - Camera canvas the message was drawn and then buried, every time). D-086 (2026-08-26) THE CANVAS PREMISE THIS CLASS WAS BUILT ON WAS FALSE. It assumed a Canvas composites above everything the camera renders, so the target ticket card would stay lit for free -- true only for Screen Space - OVERLAY. This scene's InGameCanvas is Screen Space - CAMERA at sortingOrder -1, and the dim is a sprite at 500, so the entire canvas was drawn and then buried: the target card, its modification arrow, and the step message. Two fixes, one cause. The message now builds its OWN Overlay canvas (copying the game canvas's CanvasScaler so the authored font size still means the same thing), deliberately at sortingOrder -1 so it stays BELOW Popup Canvas -- a ticket can time out mid-step and a tutorial line floating over the Game Over popup would be worse than one hidden behind it. The target card is raised by adding a Canvas with overrideSorting at 800: above the dim, below a dragged item, and its arrow rides along as a child. That Canvas is removed on teardown -- the card is not ours to destroy, only the component we added. DimCard is KEPT even though the non-target cards now sit under the world dim anyway: it makes "the other cards are dark" hold by construction instead of by the current render mode, which is the exact assumption that failed here. D-085 (2026-08-26) THE MESSAGE'S PLATE IS THE PARENT AND THE TEXT ITS CHILD, and that order is this method's correctness rather than a style choice: UGUI draws a Graphic BEFORE its children, always, so the first version -- text on the outer object, plate parented under it -- painted the plate straight over the glyphs and no message ever appeared. SetAsFirstSibling did not save it, because sibling index orders siblings and a child is never behind its parent. BuildMessage now also LOGS every path on which it declines to draw (no card, no Canvas, no borrowable TMP font); it fails invisibly by nature -- no message is also what an unauthored step looks like -- so a silent return here is a bug that hides itself. THE TICKET ARROW SITS LEFT OF THE MODIFICATION ROW AND POINTS RIGHT, and the right-hand placement it replaced was wrong twice over: the arrow landed far from the ingredient icon it labels, and the 180-degree rotation used to aim it back turned it about its own pivot, swinging the body across the row and leaving the tip in the middle of the box. Anchoring the arrow's TIP to the row's left edge makes the placement independent of how wide the row is, and needs no rotation at all since the generated sprite already points right. D-083 (2026-08-26) one of these now exists PER STEP, and it gained the two arrows and the message. Dismiss() is the important addition: it restores the lifted sorting orders SYNCHRONOUSLY, because Unity defers Destroy to the end of the frame while the next step's spotlight is built immediately -- left to OnDestroy, a step boundary would restore the old step's renderers AFTER the new step had lifted its own. RestoreAll is idempotent so OnDestroy can still run it on the paths that never reach Dismiss (a retry, a scene unload). The arrow is drawn into a texture in code -- this feature still ships no art -- and the message borrows the ticket card's own font, so it matches the game's type with nothing wired by hand. The board arrow finds the modification's layer by INDEX into BoardItem.ResolvedLayers, which is BoardView's own construction order for the container's children; both ends say so. THE GHOST IS BORN DURING THE FLY-IN, which is why neither its size nor its origin is copied off the source item: at Day Start that item is running BoardView's pop-in from localScale ZERO at the Starting Point, and this clone is made a tween tick later -- captured naively, the ghost was invisible for the whole day and looped from a spot the item merely passed through. The scale comes from the item's PARENT (a board item rests at localScale one, so the parent's lossy scale is the resting size and is not moving) and the origin is re-read every lap, which is self-correcting and costs one property access. That lap callback null-checks the item because the drop that ends the tutorial destroys it while this object's OnDestroy -- which kills the tween -- does not run until end of frame. Built entirely at runtime and authored NOWHERE -- no prefab, no scene object, no art, nothing dragged into an Inspector; that is the whole reason D-082 (2026-08-26) had no editor step. IT NEVER WRITES A COLOUR IT DOES NOT OWN, which is the design decision worth keeping: tinting each renderer to darken the board would have lost to BoardView, which rewrites its layer colours on every RefreshCell, so the board would flicker back to full brightness on the next spawn. Instead a black quad covers the camera at sortingOrder 500 and the two lit things are lifted +600 above it -- under BoardItemDragHandler's +1000 drag boost, so an item the player actually picks up stays visible over the dim for free, with no cooperation between the two classes. Ticket cards need the OTHER half: Canvas UI composites above everything the camera renders, so the quad cannot cover them -- the non-target cards get a stretched black Image child each, which also leaves the HorizontalLayoutGroup undisturbed and lets the TARGET card stay lit by doing nothing at all. The ghost is a CLONE of the live board item (colliders and every MonoBehaviour stripped), which is why the effect needs no art and always shows the exact food and modifications being asked for. Restore lives in OnDestroy rather than the completion handler, so a retry or a scene unload mid-step cannot leave the game permanently dark | h:b733d5d8
ExpoTheExplorer/Assets/Scripts/UI/TutorialPowerupIntroView.cs | binds the authored intro-panel prefab: one powerup, icon + description | sys: Tutorial | api: Create(TutorialPowerupIntroView instance, PowerupSettings settings, Sprite icon, Action onDismissed) | dep: PowerupSettings | used: PowerupBarView | crit: K3 | note: D-116 (2026-08-28) STOPPED BUILDING AND STARTED BINDING. Every comment in this file argued for the opposite -- "built entirely at runtime and authored nowhere: no prefab, no art, nothing dragged" -- and that was right only for as long as nobody needed to restyle it. A panel assembled from constants in C# cannot be restyled at all; you can only ask a programmer to change a number, which is what the user hit. The hierarchy is now Assets/Prefabs/UI/TutorialPowerupIntro.prefab, seeded once by TutorialPopupSetup. Create takes an INSTANCE rather than instantiating, so it never touches PrefabUtility and works identically on an object placed by hand. THE ROOT'S CANVAS MUST STAY SCREEN SPACE - OVERLAY (D-086): reparenting this panel under the scene's Screen Space - CAMERA canvas draws it beneath the world sprites and it vanishes with nothing reporting it. Every bound field is optional except the dismiss button -- a modal panel with no way out is a softlock behind a frozen day, so its absence is logged as an error and the step is completed rather than shown. D-115 (2026-08-27) SHOWS ONE POWERUP, not three (the user's decision): three explained in one panel is three things to remember at a moment when the player has used none of them, so each is introduced on its own Day. The heading is that powerup's DisplayName and the row is icon + Description, both read off the settings block -- the bold name line inside the row went with the change, because printing the name twice on a one-powerup panel reads as a layout mistake rather than emphasis. The icon arrives as a Sprite rather than a Func lookup now that there is one row to draw. THE AUTO-DRAFTED DEPENDENCY FIELD LISTED PowerupBarView AND THAT ARROW POINTS THE OTHER WAY -- the bar CREATES this view and is its only caller; this file names no view at all. Built at runtime and authored nowhere, like the rest of the tutorial: no prefab, no scene object, no art. It BUILDS ITS OWN SCREEN SPACE - OVERLAY CANVAS, which is the lesson of D-086 rather than a preference -- this scene's InGameCanvas is Screen Space - CAMERA at sortingOrder -1, so anything placed on it sits UNDER world sprites and gets drawn and then buried. It copies the game canvas's CanvasScaler so its proportions and font size resolve the way they do on a ticket card, and it carries its own GraphicRaycaster and a full-screen backdrop because it is MODAL: relying on the board gates to swallow presses would make this panel's correctness depend on a rule living in another system. Rows are positioned by hand rather than with a VerticalLayoutGroup -- three rows of known height need no layout pass, and a layout group would fight the explicit sizes the panel is built from. A powerup whose button has no sprite still gets a row (text is the part that teaches) plus a warning. Dismiss() clears the callback BEFORE invoking it, so a second press in the same frame cannot advance the tutorial twice | h:60da5fb3
ExpoTheExplorer/Assets/Scripts/UI/MainScreenTutorialView.cs | binds the main screen's two authored tutorial popups | sys: Tutorial | api: PointAtStoreAfterBlockedPlay(); MainScreenWelcomePopup.Bind(string titleText, string bodyText, string buttonLabel, Action onDismissed); MainScreenStoreHintPopup.Bind(string message, string buttonLabel, Action onDismissed); MainScreenStoreHintPopup.Arrow | dep: SessionHost,MetaShopView,TutorialTextConfig,MainScreenWelcomePopup,MainScreenStoreHintPopup,PopupFade,BoardAnimationConfig,DOTween | used: MainScreenView | crit: K3 | note: D-125 (2026-08-28) the two popup binders moved OUT of this file into their own -- a MonoBehaviour must live in a file matching its class name or Unity cannot bind it, which is why the generated prefabs carried a missing script. D-124 (2026-08-28) a missing popup prefab is an ERROR naming the cause, not a warning. This is the whole tutorial failing to appear for a brand-new player -- the most expensive silent failure on this screen -- and the way it happens is mundane: the setup step assigns the references in memory and marks the scene dirty, so a scene never SAVED afterwards comes back with empty fields and the view skips both halves without complaint. D-123 (2026-08-28) STOPPED BUILDING AND STARTED BINDING. It assembled a canvas, a backdrop, two panels, three labels, two buttons and a GENERATED arrow texture out of literals; the code it replaced argues the case on its own -- the hint's plate was fontSize*6.4 with a comment working out how to keep the text band and the button band from overlapping, arithmetic that breaks the next time a font changes. Two prefabs now, bound through two small MonoBehaviours declared in this same file (the way PowerupButton sits in PowerupBarView) because each popup has three texts and telling them apart by TYPE is the guess that breaks when an author adds a fourth. WHAT IS STILL COMPUTED: the arrow is reparented onto the store BUTTON and sized from it, since only the running layout knows where that button is and how big it ended up -- storeArrowScale survives for exactly that, while canvasSortingOrder and storeHintFontScale were deleted as second authorities over the prefabs' own canvas and TMP. CreateArrowSprite is gone from this file; the same generator still exists in TutorialSpotlightView, so this removed one of two copies. Popups are instantiated PARENTLESS: a Canvas nested inside another Canvas inherits its parent's RectTransform, and this view's rect is zero-sized -- that once crushed the hint to one character per line. The arrow rides the button and so outlives the hint; Cleanup destroys it explicitly. 2026-08-31: its private FadeIn used to BE the fade -- AddComponent<CanvasGroup> plus a DOFade with 0.25 written into the method. Both halves moved out: the mechanism to PopupFade, the number to BoardAnimationConfig.PopupFadeInDuration. These two plates were the first things in the project to fade at all, and that hardcoded 0.25 was a tuning number in code (CLAUDE.md's first invariant forbids it) which also let the main screen's popups drift away from the day scene's speed. FadeIn is now an instance method carrying only the optional-config null check; unwired, both plates appear instantly. | h:b4f801dd
ExpoTheExplorer/Assets/Scripts/UI/SettingsPopupView.cs | the day's pause menu: hold time, resume, retry, abandon, haptics switch | sys: Bootstrap | api: - (no public members; every entry point is a Button listener) | dep: GameManager,GameState,GameSession,DayLifecycleManager,NoKeysPopupView,PopupFade,BoardAnimationConfig | used: - | crit: K2 | note: added by D-094. IT OWNS THE PAUSE: GameState.IsPaused is written here and nowhere else, and GameManager.Update reads it as a third gate beside IsAwaitingContinue and the tutorial's reading hold -- Time.timeScale is untouched, so only the ticket clock stops while tweens and UI keep running. It DECIDES NOTHING about what an exit costs: Retry and Main Menu call the same GameManager methods the Game Over popup calls, and leaving a day that was not lost spends no key by construction (SpendKeyForLostDay is gated on IsAwaitingContinue) -- no economy rule is invented here. TWO CONFIRMATION PANELS rather than one whose message code rewrites, because a shared panel would put both authored questions into C# and break the root invariant; same reason the day caption receives only a NUMBER. Refuses to open while IsAwaitingContinue, and the open button GREYS OUT then -- interactable only, never SetActive, since D-103 (LivesDepleted/LivesChanged/DayCompleted/TicketAssigned, no per-frame check) rather than standing there fully lit and refusing; Start switches the object on once and nothing touches its active state again. That LivesChanged edge depends on LivesManager clearing IsAwaitingContinue BEFORE it writes Lives -- the publish is synchronous, and the other order left this button dead for the whole retry. Close() is the single place the pause lifts, and ConfirmRetry calls it BEFORE RetryDay for a real reason: that path resets the day in place with no scene load to tidy up, so a pause left standing would freeze the fresh attempt with no menu on screen. The haptics switch writes GameSession, never disk -- a mid-day save would bank an unfinished day's earnings. D-105 (2026-08-27) took the assignment away from it: Open/Close now call GameManager.HoldPause/ReleasePause, because the powerup shop can freeze a running day too and two views writing one bool would let whichever closed first start the day under the other. OnDestroy releases as well, so a view destroyed on its own cannot leak a hold D-135 (2026-08-29) both exits now cost a key. ConfirmRetry calls RetryDay(givingUpOnAttempt: true) and ConfirmMainMenu's ReturnToMainScreenAbandoningDay charges of its own accord -- the old rule only charged a LOST day, and this menu only opens while the day is still running, so its exits were free. Gained an OPTIONAL noKeysPopup (the scene's existing NoKeysPopupView) checked on the retry confirmation's YES, never by disabling a button; the refusing branch returns BEFORE Close(), so the pause is still held and the day stays frozen behind the explanation. ConfirmMainMenu is deliberately NOT gated -- blocking the only exit at zero keys is a softlock, it floors at zero instead (D-068). The completed day never reaches here: Open() refuses on dayIsOver, so replaying a finished day is still the Day Complete popup's free RetryCompletedDay. Needs its canvas to sort BELOW Popup Canvas, which D-135 raised to 300 against SettingsCanvas' 200. 2026-08-31: this menu AND both confirmation boxes now fade up through PopupFade at BoardAnimationConfig.PopupFadeInDuration, through a one-line private FadeIn wrapper that carries the optional-config null check for all three call sites. animConfig is OPTIONAL and deliberately outside ValidateReferences, for the reason every optional animation reference in this project is: an empty slot must never be able to suppress the only way out of a paused day -- unwired, all three appear instantly as before. Closing and cancelling stay INSTANT by the user's decision; the asymmetry is intentional and is not an oversight to be completed. | h:9c13739e
ExpoTheExplorer/Assets/Scripts/UI/TutorialPowerupSpotlightView.cs | binds the authored spotlight prefab: a bouncing arrow, and for a deferred step a dim and a line | sys: Tutorial | api: Create(TutorialPowerupSpotlightView instance, RectTransform targetButton, IReadOnlyList<RectTransform> litExtras, string message); Dismiss() | dep: TutorialDim,TutorialStepHints,BoardAnimationConfig | used: PowerupBarView | crit: K3 | note: D-146c (2026-08-31) the button is lifted through LiftInteractiveElement, not LiftElement -- it is the only pressable thing on a dimmed screen and the plain lift silently killed its clicks. D-146 (2026-08-31) THE DEFERRED PRESS DIMS THE SCREEN DOWN TO THREE THINGS and says why (the user's ask). It dims only when the step HANDS IT SOMETHING TO KEEP LIT, which is what lets one view serve both kinds of press step with no mode flag: the AtDayStart lessons pass nothing and are unchanged. Lit: the powerup button and the three ticket TIMER TRACKS, each given its own sorting scope above the sheet by TutorialDim; the arrow needs nothing, because its Overlay canvas already composites above everything the camera renders -- a property that fell out of D-145's fix and paid for itself immediately. D-115's 'IT DELIBERATELY DIMS NOTHING' is not reversed but sharpened: that rule existed to keep the TIMER BARS visible, since they are the reason the powerup is pressed, and dimming everything except them says it louder. THE SENTENCE IS A READER, NOT A PLATE: it borrows the TutorialStepHints prefab off BoardAnimationConfig -- the same one the day's other tutorial lines use, shown message-only with HideArrows(), which is exactly the palette-not-a-layout property D-126 built it for. So one authored look, one authored spot on screen, and this view still owns no text object of its own. The line itself is authored per powerup on PowerupConfig and arrives through the step, so the two AtDayStart lessons stay silent. animConfig is OPTIONAL and unwired costs the dim and the line, never the lesson. D-145 (2026-08-31) POINTS WITH THE TUTORIAL'S ARROW INSTEAD OF A FRAME, AND SAYS NOTHING AT ALL (the user's ask, in two steps). The sprite is Art/UI/Arrow.png, the one TutorialStepHints and MainScreenStoreHintPopup already point with, so the game has ONE gesture for "look here". The four bars, their CanvasGroup and the whole alpha pulse left the prefab and this file together. THE FIRST ATTEMPT REPARENTED THE ARROW ONTO THE BUTTON, the way the frame had been, and it was invisible in play with NOTHING REPORTING IT -- no error, no warning, a wired reference and a clone in the hierarchy. HUDCanvas is Screen Space - CAMERA at sortingOrder -1, so its content draws UNDER the world sprites. The frame had got away with that by hugging the button, down in the strip of screen the board never reaches; an arrow points UPWARD and the first thing it reached was the board's bottom row, which covered it completely. That is D-086 collecting its second victim inside one prefab. So the arrow lives on this prefab's own SCREEN SPACE - OVERLAY canvas and is POSITIONED over the button through SCREEN space rather than parented to it: TryMeasureButton hands back the button's top edge in that canvas's local space and its height in that canvas's units, the height taken in PIXELS and divided by the overlay canvas's scaleFactor so the two canvases' scalers are free to diverge. Bind opens with ONE Canvas.ForceUpdateCanvases, because instantiate and bind share a frame and without it both rects are still the zeroes they were serialized with. WHERE THE ARROW RESTS IS AUTHORED, not derived: arrowOffset (the arrow's CENTRE, from the middle of the button's top edge, negatives allowed) and bounceDistance replaced a 0.75x and a 0.3x of the arrow's size that had been sitting in this file -- tuning numbers in code, which the root invariant forbids, and which meant nothing in the Inspector could move the arrow at all. The size still comes from the BUTTON's height x arrowScale, the one thing a prefab cannot know, and the pivot is forced to CENTRE so the authored rotation swings the arrow about its middle rather than its base. localRotation is deliberately NOT reset: which way an arrow points is the author's only expression of it, where a rectangle had no facing to preserve. The bounce keeps SetUpdate(true) from the pulse it replaces, because this plays while the day is held still, and stays entirely ABOVE the resting place so an arrow authored close to the button never dips onto the icon. THE SENTENCE HALF IS GONE ENTIRELY: the user deleted the Message object from the prefab and asked for the plumbing to follow, so messageText, messageGap and ShowMessage left this file and Create lost its third parameter -- one caller, PowerupBarView, changed with it. D-121 (2026-08-28) had kept that plumbing because a step wanting its own line was one argument away; this closes the option rather than parking it, and if a line is ever wanted it comes back as a new argument, which is where it was. The canvas outlived the sentence it was built for, so messageCanvasRect is now overlayCanvasRect and the prefab object is OverlayCanvas -- the field is named for what it does, which is being the only thing here that draws above the world. NOTHING IS LIFTED ONTO THE BUTTON ANY MORE, so the reparent-outlives-its-host trap is gone from this file and Dismiss is one Destroy plus a tween kill; it stays because PowerupBarView calls it at every step boundary. The cost is that the arrow no longer RIDES the button; nothing moves the bar while a step is armed, and the arrangement that had that property was one nobody could see. D-116 (2026-08-28) became an authored prefab for the reason the intro panel did. D-115 (2026-08-27) new, for the forced-press step. IT DELIBERATELY DIMS NOTHING, which is the opposite of the move steps and is the design rather than a saving: Time Reset is taught at the moment a ticket is running out, and the thing the player has to SEE is that ticket's timer bar draining -- a dim would hide the reason the powerup is being pressed. Nothing needs dimming for safety either, since TutorialDirector already refuses the board, the trays and the other two powerups for the whole of an armed step. The arrow is not a raycast target -- a decoration that swallowed the press would make the step impossible in exactly the way that looks like a broken button. Every piece is optional and degrades to a lesson that still completes: no arrow, or no overlay canvas to draw it on, costs the pointer, switches the arrow OFF rather than parking it at the canvas origin, and logs why. | h:ae0d28f0
ExpoTheExplorer/Assets/Scripts/UI/NoiseClearRunner.cs | drops Noise Clear's cleared items off the board | sys: PowerupSystem | api: Run() | dep: GameManager,BoardView,BoardAnimationConfig,PowerupEffects,GameState,BoardItem | used: GameManager | crit: K3 | note: D-120 (2026-08-28) new, and it is the DOING half of the split PlanNoiseClear made -- the same shape AutoCollectRunner stands on, for the same reason: a tween needs a scene object and a MonoBehaviour, and PowerupSystem's whole value is having neither. IT RELEASES THE VISUAL BEFORE THE MODEL REMOVAL, which is the entire trick: RemoveItem publishes CellChanged and BACKFILLS the cell from the pending queue, so a container still owned by BoardView's pool would be hidden mid-fall or handed straight to the item that just landed. BoardView.TryGetDragHandler + ReleaseContainer are the two seams, and it drops the SAME object a finger would have grabbed rather than a fresh ghost -- a ghost would have to rebuild the item's modification layer and would drift the first time an item's visuals changed. Every entry is re-verified by REFERENCE against the planned instance, because the plan is a closed list and a backfill can put a required item into a cell already approved (D-110's rule, D-110's reason). THE DAY DOES NOT WAIT: the removal is instant, the charge is spent on the press, and what falls is a corpse -- this is NOT the old "dim the noise for a few seconds" design returning. Run() reports whether the MODEL changed, never whether a tween played, so an item with no built container still counts as cleared. | h:61c85ef7
ExpoTheExplorer/Assets/Scripts/UI/MainScreenStoreHintPopup.cs | the store hint prefab's references, arrow included | sys: Tutorial | api: Bind(string message, string buttonLabel, Action onDismissed); Arrow | dep: - | used: MainScreenTutorialView | crit: K3 | note: D-125 (2026-08-28) in its own file for the reason its sibling gives. Serves BOTH hint moments -- the first-run invitation and the answer to a Play pressed before the player owns anything -- since they differ only in wording. THE ARROW IS EXPOSED because it is the one part that does not stay where it was authored: the view reparents it onto the store button and sizes it from that button, so it outlives this object and the view destroys it explicitly. Its dismiss button is OPTIONAL, softer than the welcome panel's rule, because opening the store dismisses this hint too. | h:dc85022d
ExpoTheExplorer/Assets/Scripts/UI/MainScreenWelcomePopup.cs | the welcome prefab's own references | sys: Tutorial | api: Bind(string titleText, string bodyText, string buttonLabel, Action onDismissed) | dep: - | used: MainScreenTutorialView | crit: K3 | note: D-125 (2026-08-28) IN A FILE OF ITS OWN, and that is a Unity rule rather than a preference. It sat inside MainScreenTutorialView.cs for half an hour on the reasoning that PowerupButton sits inside PowerupBarView.cs -- but PowerupButton is a plain [Serializable] class, and a MONOBEHAVIOUR must live in a file matching its class name or Unity has no MonoScript to bind it to. The generated prefabs were written with m_Script: {fileID: 0} and the Editor then refused to save them at all. A component rather than name lookups inside the instance, because this prefab holds three texts and telling them apart by TYPE is the guess that breaks when an author adds a fourth. Every field is optional EXCEPT the button: a missing label costs a line, a missing button leaves a modal panel over an unreachable Play button, so that case completes the step instead. | h:d52f312c
ExpoTheExplorer/Assets/Scripts/UI/TutorialStepHints.cs | the step-hints prefab's three pieces: message and two arrows | sys: Tutorial | api: ItemArrow; CardArrow; ShowMessage(string message); HideMessage(); HideArrows(); NudgeArrows() | dep: - | used: TutorialSpotlightView | crit: K3 | note: D-126 (2026-08-28) new. A PALETTE, not a layout: the step picks which pieces appear from two INDEPENDENT authored flags, so a step can speak without pointing or point without speaking -- which is exactly why the arrows sit beside the message here instead of inside it. D-127 (2026-08-28) both arrows nudge back and forth, ALONG THEIR OWN FACING rather than a fixed axis -- the two point in different directions and an author who rotates either should get a nudge that still reads as pointing. The direction is the local rotation applied to right, which lands in the parent's space, the same space anchoredPosition uses. Started by the view AFTER placement, because the nudge is measured from where each arrow ended up, and SetLink'd to the ARROW rather than to this object -- the card arrow is reparented into the ticket card and outlives the instance. Its two numbers live here rather than on BoardAnimationConfig, the way the powerup spotlight's pulse lives on its own prefab: they are tuned while looking at the arrow they belong to. BOTH ARROWS ARE UI IMAGES ON THIS PREFAB'S CANVAS (the user's ask), and that is what makes them editable: the item arrow was a SpriteRenderer parked outside the canvas and the card arrow an Image parented OUTSIDE one -- an Image with no Canvas above it never renders, so the prefab stage showed nothing to click. They still move differently: the CARD arrow is reparented into the ticket card's row and rides it, while the ITEM arrow stays on this canvas and is only POSITIONED over the board item, since parented to the item it rode along when the player picked the food up. Every piece is optional: an author who deletes one costs that piece and never the lesson. In its own file because a MonoBehaviour must be (D-125, learned by having two prefabs refuse to save). | h:1553351e
ExpoTheExplorer/Assets/Scripts/UI/NewItemIntroPopup.cs | what a Day says about the food or modification it introduces | sys: DaySystem | api: IsDismissed; Bind(string displayName, string message, Sprite picture, bool? modificationIsAddition); Dismiss() | dep: TicketCardVisualsConfig,PopupFade,BoardAnimationConfig | used: GameManager | crit: K3 | note: D-142 (2026-08-31) new, and a COPY of MetaUnlockPopup's design rather than a reuse of it (the user's own reference: 'tasarım olarak mainscreendeki yeni bi şey açılmayı kullanabilirz'). The two say similar things and are deliberately separate objects -- one belongs to the meta grounds on the main screen and the other to the start of a day, and restyling either must not move the other. IT HOLDS THE DAY STILL: GameManager takes a pause hold for the whole sequence, so a player reading about a new item is not spending three customers' patience doing it, which is why the dismiss button is the one part that is NOT optional -- a popup with no way out would freeze the day behind it. IsDismissed is POLLED rather than a callback because the reader is a coroutine holding that pause, and it starts TRUE for a popup that could not present itself so a broken prefab never holds the day hostage. The message OBJECT is switched off for an unauthored line rather than merely emptied: an empty TMP still occupies its slot, which on a popup this size reads as a missing sentence instead of a design with none. The 'NEW ITEM!' header is authored in the prefab and never written by code, which is where every other player-facing string in this project lives. SECOND PASS (same day): the popup also introduces MODIFICATIONS. Bind's fourth argument is a NULLABLE bool -- null for a food, the direction for a modification -- so 'not a modification' cannot be confused with a removal. TWO AUTHORED HEADER OBJECTS, not one header plus two strings in code (the user: 'itemsa new item modificationsa new modification yazmalı başlıkta'): the words stay in the prefab where every player-facing string in this project lives, and the longer 'NEW MODIFICATION' carries its own font size without code knowing. Code only switches one on. The +/- sprite comes from TicketCardVisualsConfig held ON THE PREFAB -- an asset reference, which a prefab can hold, the same shape TicketCardsView.DirectionSpriteFor uses -- so the badge here and the badge on a ticket card cannot disagree. The badge object is switched OFF when the sprite is missing rather than left empty (an empty frame over the icon reads as a third, unnamed symbol) and that case is LOUD, because a modification silently losing its sign is the one failure that changes what the popup MEANS: 'extra cheese' and 'no cheese' look identical without it. 2026-08-31: fades itself up through PopupFade at BoardAnimationConfig.PopupFadeInDuration, on the LAST line of Bind, for the reasons its MetaUnlockPopup twin gives -- last because bind and instantiate share a frame, config held HERE so GameManager's day-start sequence knows nothing about presentation, dragged into the PREFAB, skipped on the no-dismiss-button path. | h:108f2e9f
ExpoTheExplorer/Assets/Scripts/UI/MetaUnlockPopup.cs | what a Day-unlocked prop says when it opens | sys: MetaSystem | api: IsDismissed; Bind(string titleText, string message, Sprite picture); Dismiss() | dep: PopupFade,BoardAnimationConfig | used: MetaGroundsView | crit: K3 | note: D-128 (2026-08-28) new. IT HOLDS THE CELEBRATION OPEN: CelebrateOne waits on IsDismissed before returning, which is what keeps the camera zoomed on the prop until the player presses the button (the user's requirement) and hands off to the next prop's FocusOn or the queue's end on dismissal. That is why the dismiss button is the ONE part that is not optional -- a popup with no way out strands the map zoomed in behind a full-screen skip catcher, so a prefab missing it sets IsDismissed true immediately and logs an error. IsDismissed is polled rather than a callback because the waiter is a coroutine, not an event handler. Its prefab's ROOT is its Canvas (D-126) and it is a MonoBehaviour in its own file (D-125). 2026-08-31: fades itself up through PopupFade at BoardAnimationConfig.PopupFadeInDuration, on the LAST line of Bind — last because instantiate and bind happen in the same frame, so fading earlier would fade up a panel still showing the prefab's authored placeholder. The optional animConfig is held HERE rather than by MetaGroundsView, so the celebration coroutine knows nothing about presentation and the 90KB view did not have to be touched; drag it into the PREFAB. The no-dismiss-button path returns before reaching it, so a popup being abandoned does not spend a fifth of a second arriving. | h:dce0e4eb
STALE ExpoTheExplorer/Assets/Scripts/UI/ConfettiView.cs | stages the confetti ParticleSystem and puts it over the UI | sys: CelebrationSystem | api: static BurstOnTop(ConfettiView prefab); static BurstBelow(ConfettiView prefab, RectTransform sibling) | dep: - | used: DayCompletePopupView,MetaGroundsView | crit: K3 | note: D-131 (2026-08-29) new, and REWRITTEN the same day -- it simulated UI Image paper first, and the user rejected that twice because it left them nothing to tune ("prefabin icinde yok ki particle sistem duzeltebileyim"). It now carries their OWN ParticleSystem. THE PROBLEM IT SOLVES: a ParticleSystem is a world renderer and a Screen Space - Overlay canvas is composited after every camera, so particles never appear over one -- confirmed from the scene files rather than assumed, DayCompletePopupView sits under `Popup Canvas` and MetaGroundsView under `Canvas`, both Overlay with no camera. THE BRIDGE: the particles are staged far off-origin, an orthographic camera films only them into a transparent RenderTexture, and a RawImage draws that texture as ordinary UI -- which turns the layering into a UI question this project can already answer. NO NEW LAYER AND NO CAMERA EDIT: the stage sits at x=10000 where neither scene has anything, so the game camera never frames it and its culling mask is untouched; that is cheaper than a dedicated layer and cannot be undone by someone editing a camera. It owns NO look -- every number is in the ParticleSystem inspector on Assets/Prefabs/Confetti.prefab, nested twice into the rig so tuning one asset retunes both cannons. It sets exactly two things on the particles, both clock and plumbing: loop off (the rig is torn down when the burst ends) and unscaled time (a day can be paused under a celebration). The two statics differ only in layering: BurstOnTop leaves the RawImage canvas a root Overlay canvas at 300; BurstBelow mounts it at a sibling s own index inside that sibling s canvas, walking UP first to the ancestor that is a direct child of the canvas, because inserting beside a popup two panels deep would put the confetti inside that panel where a mask would clip it. Fires one frame late: the RenderTexture is sized from the RawImage s resolved rect and a canvas instantiated this frame has not been sized yet. Polls IsAlive rather than timing the burst, so doubling a lifetime in the inspector needs no code change. OnDestroy is load-bearing THREE times: the RawImage canvas was DETACHED so it does not die with this object (the reparenting trap D-126/D-127 already carry), the temporary texture is released, and the camera is pointed away from it FIRST -- releasing a texture a live camera still targets is how a render loop draws into freed memory. THE ONE THING UNVERIFIABLE FROM OUTSIDE UNITY: URP needs the camera background solid with alpha 0 and post-processing off or the confetti arrives on an opaque rectangle; both are set in code rather than left to the asset, because getting it wrong does not look like a bad camera, it looks like the celebration broke the screen THE RENDER TARGET IS OWNED AND EXPLICITLY CREATED, WITH A DEPTH BUFFER, and every word of that is a bug fix. It was RenderTexture.GetTemporary with depth 0, which produced two symptoms one after the other. First the confetti drew as long ribbons -- whole trajectories, because nothing wiped the target between frames; it reads exactly like a Trail module left on, which is the wrong place to look, since the particle prefab has trails off and renders plain billboards. The wrong fix for that was a GL.Clear against RenderTexture.active in LateUpdate: it stopped the ribbons and broke the frame doing it, "Renderer2D Pass: Fake or uninitialized surface is not supported for attachment 0" and "EndRenderPass: Not inside a Renderpass" every frame, because reaching for the active render target from game code tears a hole in the pass the scriptable pipeline is mid-way through building. That error also named the real cause: a POOLED texture is not a real surface until something creates it, and a target with no depth attachment is not one the 2D renderer can attach -- which is why the camera's own clear never ran. A `new RenderTexture(w, h, 24, ARGB32)` followed by Create() fixes both, and the hand clear is gone. DO NOT reintroduce a manual clear here; if the ribbons ever come back the target is wrong, not the clear. D-140 (2026-08-31) Play now disables screenImage before MountScreen: the RawImage was mounted full-screen a frame BEFORE AllocateTexture gave it a texture, and an untextured RawImage draws Unity's default white texture at the prefab's opaque-white tint -- one frame of full-screen white just before the day-complete receipt (the fail popup fires no confetti, which is why only one of the two flashed). The re-enable stays inside AllocateTexture so visibility and content are set by one writer at one moment; the fix is in code rather than in the prefab so re-ticking the box cannot bring it back | h:20237ff6
ExpoTheExplorer/Assets/Scripts/UI/PopupFade.cs | the one way every popup appears: CanvasGroup fade-up | sys: BoardUI | api: static void In(GameObject target, float duration) | dep: DOTween | used: DayCompletePopupView,GameOverPopupView,NoKeysPopupView,SettingsPopupView,PowerupShopView,MetaShopView,MetaUnlockPopup,NewItemIntroPopup,MainScreenTutorialView | crit: K3 | note: added 2026-08-31. Takes a DURATION rather than the config, so it depends on nothing in this project -- every caller passes BoardAnimationConfig.PopupFadeInDuration and the helper stays a leaf. A STATIC HELPER on purpose: the eleven popups share nothing but "become visible" (some are scene objects toggled with SetActive, some are prefabs instantiated per use, one is a sheet swapped against a confirm box), so a base class could not reach them all and a MonoBehaviour would need adding to eleven objects across two scenes and four prefabs -- one of which would eventually be forgotten, silently. AddComponent<CanvasGroup> when absent, because not one popup root in either scene carries one. DOKill on the group BEFORE writing alpha is the load-bearing line: a popup closed and reopened faster than it fades would otherwise have two tweens writing one alpha, which reads as a flicker. SetLink but deliberately NOT SetUpdate(true) -- this project never touches Time.timeScale (SettingsPopupView says so at the top of its file), so an unscaled tween would assert a pause mechanism that does not exist here. duration <= 0 or a null target returns silently, which is what makes every caller's config reference optional: unwired degrades to today's instant appearance, never to an invisible popup. THERE IS NO FADE OUT by the user's decision (2026-08-31); the asymmetry is intentional and is not an oversight to be completed. | h:8179bfe1
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/DebugPanelRoot.cs | MISSING-role | sys: ? | api: Close(); CloseAndDestroy() | dep?: DebuggerTabController,IDebugService,SRMonoBehaviourEx,SRServiceManager,Service,Settings | used: ? | crit: ? | note: auto-added; AI must complete | h:811df15e
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/DebuggerTabController.cs | MISSING-role | sys: ? | api: OpenTab(DefaultTabs tab); ShowAboutTab() | dep?: DefaultTabs,IEnableTab,SRInstantiate,SRMonoBehaviourEx,SRTab,SRTabController | used: ? | crit: ? | note: auto-added; AI must complete | h:2a0e7974
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/MobileMenuController.cs | MISSING-role | sys: ? | api: Open(); Close() | dep?: SRMath,SRMonoBehaviourEx,SRTab,SRTabController | used: ? | crit: ? | note: auto-added; AI must complete | h:5626e0df
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Controls/ConsoleEntryView.cs | MISSING-role | sys: ? | api: SetDataContext(object data) | dep?: ConsoleEntry,IVirtualView,SRDebuggerUtil,SRMonoBehaviourEx,Settings,StyleComponent | used: ? | crit: ? | note: auto-added; AI must complete | h:661197cf
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Controls/ConsoleLogControl.cs | MISSING-role | sys: ? | api: - | dep?: ConsoleEntry,IConsoleService,SRMonoBehaviourEx,Service,VirtualVerticalLayoutGroup | used: ? | crit: ? | note: auto-added; AI must complete | h:1a5e419a
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Controls/InfoBlock.cs | MISSING-role | sys: ? | api: - | dep?: SRMonoBehaviourEx | used: ? | crit: ? | note: auto-added; AI must complete | h:25ab6b3e
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Controls/MultiTapButton.cs | MISSING-role | sys: ? | api: OnPointerClick(PointerEventData eventData) | dep?: - | used: ? | crit: ? | note: auto-added; AI must complete | h:19ad05d3
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Controls/PinEntryControl.cs | MISSING-role | sys: ? | api: PinEntryControlCallback(IList<int> result, bool didCancel); Show(); Hide(); Clear(); PlayInvalidCodeAnimation(); PushNumber(int number) | dep?: SRMonoBehaviourEx | used: ? | crit: ? | note: auto-added; AI must complete | h:0c501465
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Controls/ProfilerEnableControl.cs | MISSING-role | sys: ? | api: ToggleProfiler() | dep?: SRDebugStrings,SRMonoBehaviourEx | used: ? | crit: ? | note: auto-added; AI must complete | h:82e9f457
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Controls/ProfilerGraphAxisLabel.cs | MISSING-role | sys: ? | api: SetValue(float frameTime, float yPosition) | dep?: SRMonoBehaviourEx | used: ? | crit: ? | note: auto-added; AI must complete | h:2d132230
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Controls/ProfilerGraphControl.cs | MISSING-role | sys: ? | api: - | dep?: IProfilerService,ProfilerFrame,ProfilerGraphAxisLabel,SRServiceManager,Service | used: ? | crit: ? | note: auto-added; AI must complete | h:e075dc63
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Controls/SRTabButton.cs | MISSING-role | sys: ? | api: - | dep?: SRMonoBehaviourEx,StyleComponent | used: ? | crit: ? | note: auto-added; AI must complete | h:14f711d0
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Controls/Data/ActionControl.cs | MISSING-role | sys: ? | api: SetMethod(string methodName, SRF.Helpers.MethodReference method) | dep?: MethodReference,OptionsControlBase | used: ? | crit: ? | note: auto-added; AI must complete | h:6a4c3f4f
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Controls/Data/BoolControl.cs | MISSING-role | sys: ? | api: CanBind(Type type, bool isReadOnly) | dep?: DataBoundControl | used: ? | crit: ? | note: auto-added; AI must complete | h:f8bdda97
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Controls/Data/DataBoundControl.cs | MISSING-role | sys: ? | api: Bind(string propertyName, SRF.Helpers.PropertyReference prop); Refresh(); CanBind(Type type, bool isReadOnly) | dep?: OptionsControlBase,PropertyReference,SROptions | used: ? | crit: ? | note: auto-added; AI must complete | h:f1c8bd1b
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Controls/Data/EnumControl.cs | MISSING-role | sys: ? | api: CanBind(Type type, bool isReadOnly); GoToNext(); GoToPrevious() | dep?: DataBoundControl,SRMath,SRSpinner | used: ? | crit: ? | note: auto-added; AI must complete | h:2e7848a4
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Controls/Data/NumberControl.cs | MISSING-role | sys: ? | api: CanBind(Type type, bool isReadOnly) | dep?: DataBoundControl,IncrementAttribute,NumberRangeAttribute,SRNumberButton,SRNumberSpinner | used: ? | crit: ? | note: auto-added; AI must complete | h:f002b886
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Controls/Data/OptionsControlBase.cs | MISSING-role | sys: ? | api: Refresh() | dep?: OptionDefinition,SRMonoBehaviourEx | used: ? | crit: ? | note: auto-added; AI must complete | h:f336419f
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Controls/Data/ReadOnlyControl.cs | MISSING-role | sys: ? | api: CanBind(Type type, bool isReadOnly) | dep?: DataBoundControl | used: ? | crit: ? | note: auto-added; AI must complete | h:3217a5bb
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Controls/Data/StringControl.cs | MISSING-role | sys: ? | api: CanBind(Type type, bool isReadOnly) | dep?: DataBoundControl | used: ? | crit: ? | note: auto-added; AI must complete | h:911b5a5a
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Controls/Profiler/ProfilerFPSLabel.cs | MISSING-role | sys: ? | api: - | dep?: IProfilerService,SRMonoBehaviourEx | used: ? | crit: ? | note: auto-added; AI must complete | h:7ec6dc0e
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Controls/Profiler/ProfilerMemoryBlock.cs | MISSING-role | sys: ? | api: TriggerRefresh(); TriggerCleanup() | dep?: SRDebug,SRMonoBehaviourEx | used: ? | crit: ? | note: auto-added; AI must complete | h:70a78964
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Controls/Profiler/ProfilerMonoBlock.cs | MISSING-role | sys: ? | api: TriggerRefresh(); TriggerCollection() | dep?: SRDebug,SRMonoBehaviourEx | used: ? | crit: ? | note: auto-added; AI must complete | h:af422279
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Other/BugReportPopoverRoot.cs | MISSING-role | sys: ? | api: - | dep?: SRMonoBehaviourEx | used: ? | crit: ? | note: auto-added; AI must complete | h:7bbffd93
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Other/BugReportSheetController.cs | MISSING-role | sys: ? | api: Submit(); Cancel(); OpenPrivacyPolicy() | dep?: BugReport,BugReportScreenshotUtil,IBugReportService,ISystemInformationService,SRMonoBehaviourEx,SRServiceManager | used: ? | crit: ? | note: auto-added; AI must complete | h:8ca1c1ff
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Other/CategoryGroup.cs | MISSING-role | sys: ? | api: - | dep?: SRMonoBehaviourEx | used: ? | crit: ? | note: auto-added; AI must complete | h:85c24cd2
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Other/ConfigureCanvasFromSettings.cs | MISSING-role | sys: ? | api: - | dep?: SRDebug,SRDebuggerUtil,SRMonoBehaviour,Settings | used: ? | crit: ? | note: auto-added; AI must complete | h:aeff1a03
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Other/ConsoleTabQuickViewControl.cs | MISSING-role | sys: ? | api: - | dep?: IConsoleService,SRDebuggerUtil,SRMonoBehaviourEx | used: ? | crit: ? | note: auto-added; AI must complete | h:a33255cc
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Other/DebugPanelBackgroundBehaviour.cs | MISSING-role | sys: ? | api: - | dep?: SRMonoBehaviour,Settings,Style,StyleComponent,StyleSheet | used: ? | crit: ? | note: auto-added; AI must complete | h:ba561fb6
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Other/DockConsoleController.cs | MISSING-role | sys: ? | api: SetDropdownVisibility(bool visible); SetAlignmentMode(ConsoleAlignment alignment); ToggleDropdownVisible(); MenuButtonPressed(); ClearButtonPressed(); TogglesUpdated() | dep?: ConsoleAlignment,ConsoleLogControl,DefaultTabs,IConsoleFilterState,IConsoleService,SRDebug | used: ? | crit: ? | note: auto-added; AI must complete | h:355f6a3f
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Other/DockConsoleRoot.cs | MISSING-role | sys: ? | api: - | dep?: - | used: ? | crit: ? | note: auto-added; AI must complete | h:5684048b
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Other/ErrorNotifier.cs | MISSING-role | sys: ? | api: ShowErrorWarning() | dep?: - | used: ? | crit: ? | note: auto-added; AI must complete | h:62ee1add
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Other/FloatOverElement.cs | MISSING-role | sys: ? | api: SetLayoutHorizontal(); SetLayoutVertical() | dep?: - | used: ? | crit: ? | note: auto-added; AI must complete | h:da444314
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Other/HandleManager.cs | MISSING-role | sys: ? | api: SetAlignment(PinAlignment alignment) | dep?: PinAlignment,SRMonoBehaviour | used: ? | crit: ? | note: auto-added; AI must complete | h:9bdf56a6
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Other/IEnableTab.cs | MISSING-role | sys: ? | api: - | dep?: - | used: ? | crit: ? | note: auto-added; AI must complete | h:56a25ce8
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Other/LoadingSpinnerBehaviour.cs | MISSING-role | sys: ? | api: - | dep?: SRMonoBehaviour | used: ? | crit: ? | note: auto-added; AI must complete | h:486c61e2
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Other/PinnedUIRoot.cs | MISSING-role | sys: ? | api: - | dep?: DockConsoleController,FlowLayoutGroup,HandleManager,SRMonoBehaviourEx | used: ? | crit: ? | note: auto-added; AI must complete | h:ac2540f2
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Other/SRTab.cs | MISSING-role | sys: ? | api: - | dep?: SRMonoBehaviourEx,SRTabButton | used: ? | crit: ? | note: auto-added; AI must complete | h:a9c7e968
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Other/SRTabController.cs | MISSING-role | sys: ? | api: AddTab(SRTab tab, bool visibleInSidebar = true) | dep?: SRInstantiate,SRList,SRMonoBehaviourEx,SRTab,SRTabButton,Tabs | used: ? | crit: ? | note: auto-added; AI must complete | h:6be83e35
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Other/SafeAreaSizer.cs | MISSING-role | sys: ? | api: CalculateLayoutInputHorizontal(); CalculateLayoutInputVertical() | dep?: - | used: ? | crit: ? | note: auto-added; AI must complete | h:ae1df6d0
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Other/ScrollRectPatch.cs | MISSING-role | sys: ? | api: - | dep?: - | used: ? | crit: ? | note: auto-added; AI must complete | h:97cb0db2
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Other/ScrollSettingsBehaviour.cs | MISSING-role | sys: ? | api: - | dep?: SRDebuggerUtil | used: ? | crit: ? | note: auto-added; AI must complete | h:00a6b673
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Other/SetLayerFromSettings.cs | MISSING-role | sys: ? | api: - | dep?: SRMonoBehaviour,Settings | used: ? | crit: ? | note: auto-added; AI must complete | h:08589a22
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Other/TriggerRoot.cs | MISSING-role | sys: ? | api: - | dep?: ErrorNotifier,LongPressButton,MultiTapButton,SRMonoBehaviourEx | used: ? | crit: ? | note: auto-added; AI must complete | h:78e15163
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Other/VersionTextBehaviour.cs | MISSING-role | sys: ? | api: - | dep?: SRDebug,SRMonoBehaviourEx | used: ? | crit: ? | note: auto-added; AI must complete | h:ef5b0ecd
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Tabs/BugReportTabController.cs | MISSING-role | sys: ? | api: - | dep?: BugReportSheetController,IBugReportService,IEnableTab,SRDebug,SRInstantiate,SRMonoBehaviourEx | used: ? | crit: ? | note: auto-added; AI must complete | h:275e86f0
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Tabs/ConsoleTabController.cs | MISSING-role | sys: ? | api: CopyToClipboard(); Clear(); LogHandlerHasBeenOverridenOkayButtonPress(); LoggingDisableCloseAndIgnorePressed(); LoggingDisableReenablePressed() | dep?: ConsoleEntry,ConsoleLogControl,IConsoleFilterState,IConsoleService,IDebugPanelService,SRDebug | used: ? | crit: ? | note: auto-added; AI must complete | h:15aaa5ec
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Tabs/InfoTabController.cs | MISSING-role | sys: ? | api: Refresh(); ActivateRefreshEveryFrame() | dep?: FlashGraphic,ISystemInformationService,InfoBlock,InfoEntry,SRInstantiate,SRMonoBehaviourEx | used: ? | crit: ? | note: auto-added; AI must complete | h:b32d0756
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Tabs/OptionsTabController.cs | MISSING-role | sys: ? | api: SetSelectionModeEnabled(bool isEnabled) | dep?: ActionControl,CategoryGroup,IDebugPanelService,OptionControlFactory,OptionDefinition,OptionsControlBase | used: ? | crit: ? | note: auto-added; AI must complete | h:c77c53ba
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/UI/Tabs/ProfilerTabController.cs | MISSING-role | sys: ? | api: - | dep?: SRDebug,SRMonoBehaviourEx,Tabs | used: ? | crit: ? | note: auto-added; AI must complete | h:2330394f
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/ContentFitText.cs | MISSING-role | sys: ? | api: CalculateLayoutInputHorizontal(); CalculateLayoutInputVertical() | dep?: ComponentMenuPaths,CopySource,SRText | used: ? | crit: ? | note: auto-added; AI must complete | h:922b5534
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/CopyLayoutElement.cs | MISSING-role | sys: ? | api: CalculateLayoutInputHorizontal(); CalculateLayoutInputVertical() | dep?: ComponentMenuPaths,CopySource | used: ? | crit: ? | note: auto-added; AI must complete | h:c0b4e720
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/CopyPreferredSize.cs | MISSING-role | sys: ? | api: - | dep?: ComponentMenuPaths,CopySource | used: ? | crit: ? | note: auto-added; AI must complete | h:d7e870a8
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/CopyPreferredSizes.cs | MISSING-role | sys: ? | api: - | dep?: ComponentMenuPaths | used: ? | crit: ? | note: auto-added; AI must complete | h:edad05ae
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/CopySizeIntoLayoutElement.cs | MISSING-role | sys: ? | api: - | dep?: ComponentMenuPaths,CopySource | used: ? | crit: ? | note: auto-added; AI must complete | h:dd023b06
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/DragHandle.cs | MISSING-role | sys: ? | api: OnBeginDrag(PointerEventData eventData); OnDrag(PointerEventData eventData); OnEndDrag(PointerEventData eventData) | dep?: - | used: ? | crit: ? | note: auto-added; AI must complete | h:4fc43974
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/FlashGraphic.cs | MISSING-role | sys: ? | api: OnPointerDown(PointerEventData eventData); OnPointerUp(PointerEventData eventData); Flash(); FlashAndHoldUntilNextPress() | dep?: ComponentMenuPaths | used: ? | crit: ? | note: auto-added; AI must complete | h:3272d962
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/InheritColour.cs | MISSING-role | sys: ? | api: - | dep?: ComponentMenuPaths,SRMonoBehaviour | used: ? | crit: ? | note: auto-added; AI must complete | h:30e3d5f2
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/LongPressButton.cs | MISSING-role | sys: ? | api: OnPointerExit(UnityEngine.EventSystems.PointerEventData eventData); OnPointerDown(UnityEngine.EventSystems.PointerEventData eventData); OnPointerUp(UnityEngine.EventSystems.PointerEventData eventData); OnPointerClick(UnityEngine.EventSystems.PointerEventData eventData) | dep?: ComponentMenuPaths | used: ? | crit: ? | note: auto-added; AI must complete | h:ac01fbdc
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/ResponsiveBase.cs | MISSING-role | sys: ? | api: - | dep?: SRMonoBehaviour | used: ? | crit: ? | note: auto-added; AI must complete | h:f7ce7f84
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/ResponsiveEnable.cs | MISSING-role | sys: ? | api: - | dep?: ComponentMenuPaths,ResponsiveBase | used: ? | crit: ? | note: auto-added; AI must complete | h:6eb4448a
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/ResponsiveResize.cs | MISSING-role | sys: ? | api: - | dep?: ComponentMenuPaths,ResponsiveBase | used: ? | crit: ? | note: auto-added; AI must complete | h:03c05142
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/SRNumberButton.cs | MISSING-role | sys: ? | api: OnPointerDown(PointerEventData eventData); OnPointerUp(PointerEventData eventData) | dep?: ComponentMenuPaths,SRNumberSpinner | used: ? | crit: ? | note: auto-added; AI must complete | h:f02bd373
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/SRNumberSpinner.cs | MISSING-role | sys: ? | api: OnPointerClick(PointerEventData eventData); OnPointerDown(PointerEventData eventData); OnPointerUp(PointerEventData eventData); OnBeginDrag(PointerEventData eventData); OnDrag(PointerEventData eventData); OnEndDrag(PointerEventData eventData) | dep?: ComponentMenuPaths | used: ? | crit: ? | note: auto-added; AI must complete | h:8dfaf225
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/SRRetinaScaler.cs | MISSING-role | sys: ? | api: - | dep?: ComponentMenuPaths,SRMonoBehaviour | used: ? | crit: ? | note: auto-added; AI must complete | h:f2f11bd6
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/SRSpinner.cs | MISSING-role | sys: ? | api: OnBeginDrag(PointerEventData eventData); OnDrag(PointerEventData eventData) | dep?: ComponentMenuPaths | used: ? | crit: ? | note: auto-added; AI must complete | h:f1ad7886
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/SRText.cs | MISSING-role | sys: ? | api: SetLayoutDirty() | dep?: ComponentMenuPaths | used: ? | crit: ? | note: auto-added; AI must complete | h:fdeced29
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/ScrollToBottomBehaviour.cs | MISSING-role | sys: ? | api: Start(); Trigger() | dep?: ComponentMenuPaths | used: ? | crit: ? | note: auto-added; AI must complete | h:2bc47350
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/StyleComponent.cs | MISSING-role | sys: ? | api: Refresh(bool invalidateCache) | dep?: ComponentMenuPaths,SRMonoBehaviour,Style,StyleRoot | used: ? | crit: ? | note: auto-added; AI must complete | h:cacd5f0b
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/StyleRoot.cs | MISSING-role | sys: ? | api: GetStyle(string key); SetDirty() | dep?: ComponentMenuPaths,SRMonoBehaviour,Style,StyleSheet | used: ? | crit: ? | note: auto-added; AI must complete | h:c75c27a0
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/StyleSheet.cs | MISSING-role | sys: ? | api: Copy(); CopyFrom(Style style); GetStyle(string key, bool searchParent = true); AddStyle(string key); DeleteStyle(string key); GetStyleKeys(bool includeParent = true) | dep?: AssetUtil | used: ? | crit: ? | note: auto-added; AI must complete | h:ccef63a7
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/Unselectable.cs | MISSING-role | sys: ? | api: OnSelect(BaseEventData eventData) | dep?: ComponentMenuPaths,SRMonoBehaviour | used: ? | crit: ? | note: auto-added; AI must complete | h:681693c1
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/Layout/FlowLayoutGroup.cs | MISSING-role | sys: ? | api: CalculateLayoutInputHorizontal(); SetLayoutHorizontal(); SetLayoutVertical(); CalculateLayoutInputVertical(); SetLayout(float width, int axis, bool layoutInput); GetGreatestMinimumChildWidth() | dep?: ComponentMenuPaths | used: ? | crit: ? | note: auto-added; AI must complete | h:38dc5657
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/Layout/VirtualVerticalLayoutGroup.cs | MISSING-role | sys: ? | api: OnPointerClick(PointerEventData eventData); CalculateLayoutInputVertical(); SetLayoutHorizontal(); SetLayoutVertical(); AddItem(object item); RemoveItem(object item) | dep?: ComponentMenuPaths,SRInstantiate,SRList,StyleRoot,StyleSheet | used: ? | crit: ? | note: auto-added; AI must complete | h:9bd9f1fe
ExpoTheExplorer/Assets/ThirdParty/Feel/NiceVibrations/Demo/_Common/Scripts/UI/HapticCurve.cs | MISSING-role | sys: ? | api: UpdateCurve(float amplitude, float frequency) | dep?: MMSignal,NiceVibrationsDemoHelpers,SignalType | used: ? | crit: ? | note: auto-added; AI must complete | h:4998a200
ExpoTheExplorer/Assets/ThirdParty/Feel/NiceVibrations/Demo/_Common/Scripts/UI/MMFPSCounter.cs | MISSING-role | sys: ? | api: - | dep?: - | used: ? | crit: ? | note: auto-added; AI must complete | h:974c8046
ExpoTheExplorer/Assets/ThirdParty/Feel/NiceVibrations/Demo/_Common/Scripts/UI/MMFPSUnlock.cs | MISSING-role | sys: ? | api: - | dep?: - | used: ? | crit: ? | note: auto-added; AI must complete | h:5f7b442c
ExpoTheExplorer/Assets/ThirdParty/Feel/NiceVibrations/Demo/_Common/Scripts/UI/MMKnob.cs | MISSING-role | sys: ? | api: SetActive(bool status); SetValue(float value); OnPointerDown(PointerEventData eventData); OnPointerUp(PointerEventData eventData) | dep?: NiceVibrationsDemoHelpers,Settings | used: ? | crit: ? | note: auto-added; AI must complete | h:20cf8d27
ExpoTheExplorer/Assets/ThirdParty/Feel/NiceVibrations/Demo/_Common/Scripts/UI/MMProgressBar.cs | MISSING-role | sys: ? | api: UpdateBar(float currentValue, float minValue, float maxValue); Bump() | dep?: Settings | used: ? | crit: ? | note: auto-added; AI must complete | h:4f2f1d74
ExpoTheExplorer/Assets/ThirdParty/Feel/NiceVibrations/Demo/_Common/Scripts/UI/MMSignal.cs | MISSING-role | sys: ? | api: GetValue(float time, SignalType signalType, float phase, float amplitude, float frequency, float offset, bool Invert = false) | dep?: - | used: ? | crit: ? | note: auto-added; AI must complete | h:b5dc4c70
ExpoTheExplorer/Assets/ThirdParty/Feel/NiceVibrations/Demo/_Common/Scripts/UI/MMSpriteReplace.cs | MISSING-role | sys: ? | api: Swap(); SwitchToOffSprite(); SwitchToOnSprite() | dep?: MMTouchButton | used: ? | crit: ? | note: auto-added; AI must complete | h:feb8b127
ExpoTheExplorer/Assets/ThirdParty/Feel/NiceVibrations/Demo/_Common/Scripts/UI/MMSwitch.cs | MISSING-role | sys: ? | api: InitializeState(); SwitchState() | dep?: MMTouchButton | used: ? | crit: ? | note: auto-added; AI must complete | h:46e02198
ExpoTheExplorer/Assets/ThirdParty/Feel/NiceVibrations/Demo/_Common/Scripts/UI/MMTouchButton.cs | MISSING-role | sys: ? | api: OnPointerDown(PointerEventData data); OnPointerUp(PointerEventData data); OnPointerPressed(); OnPointerEnter(PointerEventData data); OnPointerExit(PointerEventData data); DisableButton() | dep?: - | used: ? | crit: ? | note: auto-added; AI must complete | h:86d0a306
ExpoTheExplorer/Assets/ThirdParty/Feel/NiceVibrations/Demo/_Common/Scripts/UI/MMUIShaker.cs | MISSING-role | sys: ? | api: Shake(float duration) | dep?: - | used: ? | crit: ? | note: auto-added; AI must complete | h:141d7a26
ExpoTheExplorer/Assets/ThirdParty/Feel/NiceVibrations/Demo/_Common/Scripts/UI/Pagination.cs | MISSING-role | sys: ? | api: InitializePagination(int numberOfPages); SetCurrentPage(int numberOfPages, int currentPage) | dep?: - | used: ? | crit: ? | note: auto-added; AI must complete | h:99d60f4f
