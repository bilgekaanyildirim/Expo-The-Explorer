<!-- stamp: cb09e97 2026-08-25 status: DEGRADED 2 stale, 0 orphan, 2 missing-role -->
ExpoTheExplorer/Assets/Scripts/UI/BoardItemDragHandler.cs | one board item's pickup/drag/drop gesture | sys: BoardUI | api: CurrentItem; IsDragging; WasAcceptedByTray; Configure(BoardGrid board, Camera dragCamera, BoardView boardView, GameManager gameManager, DragFeelSettings dragFeel, BoardAnimationConfig animConfig); SetCell(int x, int y, BoardItem item); OnPointerDown(PointerEventData eventData); OnPointerUp(PointerEventData eventData); OnBeginDrag(PointerEventData eventData); OnDrag(PointerEventData eventData); OnEndDrag(PointerEventData eventData); DetachFromBoard(); PlaceInSlot(Transform slotTransform, int slotIndex); ReleaseAndDestroy(); PlayDeliverySuccessAndDestroy(BoardAnimationConfig config) | dep: BoardAnimationConfig,BoardGrid,BoardItem,BoardView,GameManager,WorldTrayView | used: BoardView,WorldTrayView | crit: K2 | note: added in code by BoardView per item container, needs a Physics2DRaycaster on the scene camera; the SINGLE writer of a dragged item's transform, and never touches BoardGrid mid-drag (only on drop) so it can't fight BoardView's CellChanged/RefreshCell cycle; ApplyPickupVisuals COMPLETES any live tween (DOKill(true)) BEFORE snapshotting homePosition/homeScale, because a wrong-order scatter's fly-in is delayed for the tray shake and parks the container on the tray at scale ZERO -- a plain kill froze it there, so the item dragged invisibly and OnEndDrag re-applied the zero; X follows the finger by accumulated delta with no absolute anchor, so any other writer touching a held item's X strands it | h:d2719f89
ExpoTheExplorer/Assets/Scripts/UI/BoardView.cs | runtime board grid renderer + drag/drop | sys: BoardUI | api: ReleaseContainer(int x, int y); BeginFlyInOverride(Vector3? origin, float delay = 0f); EndFlyInOverride(); TryGetCellAt(Vector3 worldPosition, out int x, out int y) | dep: BoardAnimationConfig,BoardGrid,BoardItemDragHandler,BoardVisualsConfig,DragFeelConfig,GameManager | used: BoardItemDragHandler,WorldTrayView | crit: K2 | note: reactive, no per-frame polling -- it rebuilds only the cells BoardGrid reports via CellChanged, and cellSize/boardOrigin are DERIVED from the orthographic viewport so the board fits any aspect. RefreshCell recomputes a fly-in destination from CellPosition(x,y,-0.1f) and never reads it off container.position: a reused container only still stands on its cell if every tween that ever moved it completed, so one interrupted fly-in used to strand it and bake that spot in as the cell's destination for good. A DELAYED fly-in (only a wrong-order scatter, held for the tray's shake) also disables the container's collider for the length of the delay -- otherwise it sits active, scale-zero and grabbable right on the tray, and grabbing it snapshotted that zero as the item's home scale. BeginFlyInOverride/EndFlyInOverride are a plain flag pair, NOT nested or counted, so callers must not overlap their brackets | h:e3fa4ad3
ExpoTheExplorer/Assets/Scripts/UI/DayCompletePopupView.cs | day-complete receipt popup + star rating and its score breakdown + the payout handover + the three exits from a finished day | sys: DayLifecycle | api: - | dep: DayLifecycleManager,DayRewardFlightView,GameManager,GameState,StarScoreBarView | used: - | crit: K2 | note: D-061 added an OPTIONAL score bar: this view hands it the two positions (before popupRoot activates, since anchors resolve on activation) and the FLIGHT drives it, because the bar's fill paces the star seating and this view does not own that sequence. D-060 added three OPTIONAL TMP_Text rows (time bonus / mistakes / net score, integer percents) -- optional because the popup already exists in SampleScene and a required field would stop the whole receipt from appearing until the scene is wired; unwired they stay blank and Start logs it once. Rounding happens only here, so a day a hair under a threshold displays as 55 and still scores two stars rather than being quietly promoted. D-042: "Next Day" now asks GameManager one question (TryContinueIntoNextDay) and hides only if there is still a day scene to hide in. It used to branch on "was there a next Day"; adding the second reason here would have made a popup the place where scene routing is decided, so both reasons moved to the class that owns flow. hand-wired in the Editor, never instantiates UI. It only RENDERS the star count -- the rule (one star per life still held) lives in DayLifecycleManager.StarCount since D-008, which also deleted the per-Day authored thresholds this view used to compare Total against. D-012 gave it two navigation behaviours: Go Back exits to the main screen persisting the NEXT day, and Next Day now falls back to that same exit when there is no next Day, instead of hiding the popup and leaving the player on a finished day with no UI. Neither exit calls Hide() -- the scene load destroys the popup. D-057 turned it from a REPORT into the place the day actually PAYS: nothing is credited during a day, so Show no longer switches the stars on -- it switches them OFF and hands the earned ones, plus the Total text as the coin origin, to DayRewardFlightView, which seats them and flies the money to the HUD. Two ordering rules: the stars are hidden AFTER popupRoot is activated, or the flight measures positions on an inactive hierarchy and drops every icon at the origin; and all three buttons call CompleteRewardFlight FIRST, so tapping through the payout is a skip rather than a forfeit. rewardFlight is a REQUIRED reference like every other field here, on the user's instruction (it was briefly made optional and reverted 2026-08-24). Worth knowing before touching it: ValidateReferences returns BEFORE the DayCompleted subscription, so if this field is ever empty the popup never appears at all and the player is stranded on a finished day with no exit -- the MONEY is safe either way, since GameManager commits the reward on the exits, so the failure is a navigation dead end rather than a lost payout | h:8f23ece9
STALE ExpoTheExplorer/Assets/Scripts/UI/GameOverPopupView.cs | lives-depleted continue/retry/abandon popup | sys: LivesSystem | api: - | dep: GameManager,GameState,LivesConfig,LivesManager | used: - | crit: K2 | note: hand-wired in the Editor; lives on an always-active object so Start runs while popupRoot begins inactive. Main Menu is live since D-012 and abandons the attempt rather than merely navigating -- the money rules for that (revert earnings, keep spending, then write) sit in GameManager.ReturnToMainScreenAbandoningDay, not in this view | h:a0eefb24
ExpoTheExplorer/Assets/Scripts/UI/GemsView.cs | HUD gem counter | sys: ProgressionSystem | api: - | dep: HudWalletSource | used: HudCanvasPrefabSetup | crit: K3 | note: reactive in the day scene (binds GemsChanged, never polls); on the main screen there is no event to bind to, so the value renders once -- correct, since nothing can change a balance while the player sits on the menu. Holds no GameManager since D-013: HudWalletSource decides where the number comes from, so the rule lives in one place instead of three views. Reads through the SOURCE, not through liveState, so a single call serves both modes. Still subscribes from Start, not Awake -- Awake order across objects is unspecified and GameManager.State is assigned in its Awake | h:3145e38c
ExpoTheExplorer/Assets/Scripts/UI/HudWalletSource.cs | per-scene answer to where the HUD reads its numbers, plus forwarded change events | sys: ProgressionSystem | api: SoftMoneyChanged; GemsChanged; LivesChanged; SoftMoney; Gems; Lives; MaxLives | dep: GameManager,GameState,PlayerProfile,PlayerProfileStore | used: SoftMoneyView,GemsView,LivesView,HudCanvasPrefabSetup | crit: K2 | note: Binds to SessionHost, not to GameManager, since D-022 -- which is what makes the MENU HUD live rather than a one-shot save-file read. [FormerlySerializedAs("gameManager")] on that field is LOAD-BEARING: SampleScene's prefab instance carries an override under the old name, and renaming without it drops the override, producing exactly the quiet failure this file already documents (the HUD stops following the live wallet for the whole day with nothing different on screen). A PRIVATE LiveState helper walks sessionHost?.Session?.State -- private on purpose, since D-014 removed the public one so the three views would stop branching on mode, and the double ?. matters because a host may exist while its Session is still unassigned. The mode log line now names the host TYPE, and its meaning changed: a missing host used to be expected on the menu, and since D-022 it means a lost reference in either scene. exists because D-013 put ONE HUD Canvas prefab in both scenes and the two scenes answer differently -- day scene: the live GameState via GameManager, reactive; main screen: the save file, read once (no GameManager there by design, D-012). Strictly a READER; Wallet stays the single writer. Takes the GameManager as a [SerializeField], on the user's instruction, so the binding is visible and theirs to control. A PREFAB ASSET cannot store a scene reference, so that field is necessarily a prefab OVERRIDE on the day scene's instance and empty in the prefab itself -- HudCanvasPrefabSetup step 2 sets it per scene rather than leaving a manual drag that is easy to forget. KNOWN FAILURE MODE: Apply All from the day scene drops the override (Unity will not push a scene reference into an asset) and the symptom is QUIET -- the HUD stops following the live wallet and sits on the save file's numbers all day. That is why Resolve logs which of the two modes it picked, once per scene load: an empty field is not an error (it is how the main screen works), so the log line is the only thing distinguishing the two. Resolves LAZILY on the first view's Start and never in an Awake: GameManager assigns State in its Awake and Awake order across objects is unspecified, so resolving early would reintroduce exactly the null the views' Start-not-Awake rule exists to avoid. D-014 gave it three forwarded EventBus<int>s so the views no longer branch on mode: live, it re-publishes GameState's events (LivesChanged fed by BOTH lives events, since the label it drives renders current/max); on the main screen they never fire, which is correct because nothing there can change a value. LiveState is gone from the API for that reason. Lives off the day scene now reads the SAVED count, not a constant -- that is what made the menu's lives readout real. MaxLives stays the constant: nothing varies it. OnDestroy mirrors the forwarding; EventBus removes by delegate equality so the method-group expressions match | h:c023e6fd
ExpoTheExplorer/Assets/Scripts/UI/LivesView.cs | HUD lives counter, "current/max" | sys: LivesSystem | api: - | dep: HudWalletSource | used: HudCanvasPrefabSetup | crit: K3 | note: role and sys filled in by D-013 (was MISSING-role/sys: ?). Binds LivesChanged AND MaxLivesChanged -- LoseLife moves only Lives, TryContinue can move both; distinct from LivesDepleted, which fires once at 0. Refresh ignores its payload because the label always needs both halves. D-014 made the main screen's number REAL (lives are persisted now), so the constant "3/3" placeholder is gone, and the view no longer knows two modes exist: one subscription to the source's LivesChanged plus one render. OnLivesChanged is the ONE place a payload is deliberately dropped, confined to an adapter line, because the label re-renders both halves of current/max and cannot act on one | h:a2914d9d
ExpoTheExplorer/Assets/Scripts/UI/MainScreenView.cs | main screen: day readout, Play, and Start Over | sys: MainScreen | api: - | dep: PlayerProfile,PlayerProfileStore,SceneFlow | used: MainScreenSceneBuilder,MainScreenResetButtonSetup | crit: K2 | note: TWO serialized buttons since D-026, the day readout moved ONTO the Play button as "Continue Day X" (D-025). Since D-028 there is NO lookup here at all -- playLabel and resetLabel are serialized fields the author drags. D-025 had DEFENDED finding the Play caption with GetComponentInChildren ("a third field is a third thing to forget") and the user overruled it: a forgotten field is visible in the Inspector, while a search that starts matching the wrong object is visible nowhere. An unwired caption is a WARNING, not an error -- the button still works, it just says nothing. (two missing-reference errors had just come from exactly that). Still reads the PROFILE, not the session this scene has carried since D-022, so the profile loads twice here; accepted because taking it from the session needs another serialized reference and the day number cannot change on a menu. the meta side's navigation shell (D-012), deliberately content-free so the day<->meta flow is correct before any meta CONTENT is designed on it. Reads the SAVE FILE, not a GameState: there is no GameManager in this scene, which is exactly what keeps GameManager's "Awake builds the whole Day" shape intact, so the profile IS the hand-off between the two scenes. A pure reader -- it never saves, so the single-writer invariant on both balances and the day index still names GameManager alone. Gave up its own coins/gems labels in D-013: the shared HUD Canvas prefab is present in this scene too, and two labels showing one balance is how a screen contradicts itself -- MainScreenSceneBuilder no longer creates them and HudCanvasPrefabSetup deletes the four an earlier build made. Shows CurrentDayIndex + 1, a catalog POSITION and not a Day file's dayIndex, and can read one day high on a save that points past the last authored Day -- GameManager clamps that when the day itself loads. Hand-wired like the popups, but the wiring is done once by MainScreenSceneBuilder rather than by hand. D-026 added the OPTIONAL resetButton: two taps (the first swaps in the authored resetConfirmLabel for resetConfirmSeconds, and a coroutine puts the real caption back so the button cannot go on saying "tap again" after that tap stopped erasing anything) then PlayerProfileStore.Delete() and SceneFlow.LoadMainScreen(). It still writes no VALUE -- the delete is a command to the file's owner -- and the scene reload is deliberate: rebuilding the scene resets the session, the HUD, the grounds and the shop through their existing single writers, where patching the live GameState would need a second writer for the balance, the day index and the owned props. Optional on purpose: a MainScreen built before this existed still runs, and playButton stays the only reference ValidateReferences demands | h:c632f9de
ExpoTheExplorer/Assets/Scripts/UI/MainScreenRoot.cs | the main screen's composition root: builds the session | sys: MainScreen | api: MainScreenRoot.Session | dep: GameSession,SessionHost,GameConfig,LivesConfig,FoodCatalog | used: MainScreenSessionSetup,HudWalletSource | crit: K2 | note: gives the MENU a GameState and a Wallet, which is the precondition for a shop spending money at all (D-022) -- and lets the shared HUD follow it live instead of reading the save file once. Deliberately NOT a GameManager: it builds the session half only, because the day runtime (ticket slots, tray, board, the per-frame tick) behind a menu would time tickets out and cost lives, which is the concrete reason D-021 rejected moving GameManager here. Builds in Awake, never Start: every reader is a view's Start and Unity runs all Awakes first -- the same ordering GameManager relies on, so both scenes behave identically. Missing configs log an ERROR because the failure is otherwise quiet: with no session the HUD silently falls back to the save file and looks correct until the first purchase does not update the coin count | h:8d07d65a
ExpoTheExplorer/Assets/Scripts/UI/MetaGroundsView.cs | draws one location's grounds and the props on it | sys: MetaSystem | api: Refresh(); RefreshAfterPurchase(MetaItemDefinition purchased); ViewedLocation { get; }; ShowGhost(MetaItemDefinition item); ClearGhost(); TryGetGhostBounds(out Vector3 worldTopCentre, out Vector3 worldBottomCentre) | dep: MetaResolver,MetaCatalog,MetaLocation,MetaItemDefinition,GameSession,SessionHost | used: MetaGroundsSetup | crit: K2 | note: D-046: the grounds Image is now a CONTAINER that never draws -- the art moved into runtime children so the continuation layer can sit behind it, since a child always renders over its parent's own graphic. Order is [0] continuation, [1] grounds art, [2..] props. Chosen over inserting a real container object, which would have meant a new serialized reference and another delete-and-rebuild round (D-036); as it stands the scene is untouched, `background` is still the ScrollRect content, the zoom target and the props' parent. The continuation is a CHILD, so scrolling, the preview zoom and the framing are inherited rather than recomputed -- a second transform calculation is the "second authority" trap this class keeps avoiding. It is sized from its own pixels times the SAME PropScale the grounds use, and centred with no offset. Scroll range is deliberately unchanged: the content is still sized from the grounds, so the extra art is clipped by the viewport and fills the edges under zoom rather than letting the player pan into empty space. artLayerCount exists because SortedInsertIndex returns SIBLING indices and props now start after these layers -- forgetting that shift would put ghosts behind the grounds art, invisible, which is the silent-depth defect D-045 was reported for. D-045 fixed a draw-order bug the Meta Editor had been exposing: the upcoming-prop silhouette was APPENDED after the active props, so it drew in front of everything regardless of its SortOrder, while the editor canvas has always sorted. Reported as "the fryer is behind the sauces in the editor but in front in game", and the catalog confirmed it -- fryer is a DayUnlock prop at sortOrder 0, still closed, therefore drawn as the upcoming ghost. It is now placed at the sibling index its SortOrder asks for (SortedInsertIndex), and the celebration copy takes over the hidden real prop's exact index. The reason to fix it this way rather than force the ghost forward is the reason the preview exists (D-032, D-040): it shows where the prop WILL stand, and depth is part of where -- a ghost pulled to the front tells the truth about position and lies about occlusion. Accepted consequence: a prop authored at a low SortOrder really is behind the scenery and its ghost can be partly hidden; the celebration zoom still finds it. NOT changed, and flagged as an inconsistency for the author to settle: the PURCHASE ghost is still forced to the front (D-031), because the shop zooms to it and names it in a popup, where occlusion costs comprehension. D-043: after the last unlock the map pans on to the NEXT prop's ghost and lingers before zooming out. Not a new drawing pass -- Refresh runs before the celebration with the new day index, so upcomingProp is already the prop AFTER the one that just opened, at the right progress; a second copy would be the same thing twice. The timing pays for itself: D-040 measures the wait from the PREVIOUS unlock, so a prop that just opened leaves the next reading exactly %0, and "the counter starts now" is on screen without a word of copy. Its own duration field, not the celebration hold, because the two are different beats and one number would mean breaking one while tuning the other. skipRequested is reset first, so skipping the reveal does not silently skip the peek as well. Nothing coming (all open, or the next one area-gated and therefore undrawn) skips straight to the caller's finally, which is still the single place the framing is restored. D-041 plays the unlock celebration: zoom to the prop, watch it rise from silhouette to solid, then zoom back. It works out what is owed ITSELF via MetaResolver.DayUnlocksBetween rather than being told by the day scene, which is what makes it independent of the forced-return step that follows -- the player sees it however they got back, including after quitting for two days. Started ONLY from Start, never Refresh: a purchase also calls Refresh and a shopping trip is not the moment for a day-unlock fanfare. FocusGhost became FocusOn because it now has two callers; a second framing routine would have broken D-032's single authority. The real prop is HIDDEN while an animated copy takes its place, so the opening is watched rather than already-done, and the fill layer is shared with the upcoming-prop preview. The marker is written only after the WHOLE queue completes: an interrupted celebration replays next time, which is the cheap failure, where swallowing it is the expensive one. Its single writer is this class. Skip is a transparent full-screen Button parented to the CANVAS as last sibling -- not to the grounds, which are the canvas's FIRST child, so a catcher inside them would sit under the market button and let the shop open mid-celebration; and a Button rather than Input.GetMouseButtonDown so it does not depend on which input backend is configured. try/finally guarantees the framing comes back and the catcher goes: a screen left zoomed with an invisible full-screen button over it is unusable. D-040 draws the upcoming Day-unlock prop: a faint silhouette with the solid version filling from the bottom as days pass, and the COMPLETED percentage over it. TWO layers, not one, because a single Image cannot be both faint and partly solid -- fillAmount CUTS, it does not fade. The silhouette goes through CreateProp so the preview stands exactly where and at what size the real prop will (same reason as the purchase ghost); the fill is its stretched CHILD so placement is inherited rather than computed twice. Built in CODE, not cloned from an authored template: a template would be nicer to style but would add an object reference to this component, which would mean re-running Build Meta Grounds -- the step that refuses to rebuild, i.e. the D-036 trap. Tuning is serialized instead, and new serialized values are born with defaults without touching the scene. preserveAspect is deliberately unused on these props (size already comes from sprite pixels times background scale) and interacts badly with Filled -- leave it alone. A missing TMP font costs the caption, not the fill. Refresh() is PUBLIC again as of S4 (D-034) and only now: public for the first shop, private again when that shop was reverted (D-027) because an entry point with no caller reads as one that is used somewhere, and kept private through S1 and S3 on the same argument since neither bought anything. A purchase changes what MetaResolver.IsActive answers and nothing else would notice, so the caller arrived exactly then. S3c (D-033) animates the travel with DOTween instead of snapping. The destination is now MEASURED, not derived: the map is placed at its destination for an instant, the end scale, the end position and the ghost SETTLED edges are read there, and it is snapped back before anything renders. That was the only way to keep the world-space delta framing (D-032 rejected pivot/anchor arithmetic) while also tweening -- the delta can only be read once the scale is applied. TryGetGhostBounds therefore reports the SETTLED edges, which is why MetaShopView needed no change and carries no timing dependency on this animation: it places the popup once, where the prop is going, and the map slides under it. ShowGhost destroys the old ghost WITHOUT restoring the framing (DestroyGhostObject) while ClearGhost restores -- load-bearing since the travel is a tween, because restoring first would re-capture the "before" framing from halfway through the return tween and cancelling would send the map somewhere it had never been. The pre-focus framing is captured only when not already focused, for the same reason. Scrolling is handed back in the return tween's OnComplete, never at its start: the ScrollRect is Clamped and pulls the content toward its bounds every LateUpdate, so both would be dragging one value. DOKill before every tween and in OnDisable. S3b (D-032) made ShowGhost also FRAME the prop: zoom toward it and slide the map so the confirm popup fits above. The zoom is not authored per prop -- it is the zoom that would make the prop fill previewFill of the viewport, clamped to [1, previewMaxZoom]. A trash bin zooms to the ceiling; a fence as wide as the grounds needs a ratio below one, so the clamp holds it at 1x and the map does not move. That is the user's own observation falling out of the arithmetic instead of being an exception, and a middling prop added later gets a sensible number for free. ScrollRect is DISABLED while previewing -- honest (the player is answering a question, not exploring) and it keeps the ScrollRect's bounds arithmetic out of a fight with a scaled content rect, plus horizontal scrolling is off so a zoomed map would otherwise be unreachable sideways. Framing is a WORLD-space delta, never anchor arithmetic: the background is width-stretched with a top pivot and hand-rolled maths would silently focus the wrong spot the day either convention changes. ForceUpdateCanvases after the scale change, or the ghost's corners are read one frame stale and the framing lands off by the zoom factor. RestoreFocus is guarded by a flag, not by comparing values, because a preview that zoomed by 1x still disabled scrolling. S3 brought the ghost back (D-031): ShowGhost draws the previewed prop through CreateProp -- the SAME path the owned props take -- so a preview cannot disagree with where the thing will actually land, and PropScale is now one method rather than two call sites for that reason. Only the alpha differs, and it is a serialized field because it is cosmetic. The ghost is NOT in spawnedProps: it has its own field, because tying two different lifetimes to one list would make "was the ghost destroyed or the prop" unanswerable in S4. Refresh clears it -- a ghost belongs to ONE location layout and would otherwise stand on ground it was never positioned against. ClearGhost is safe in any state, because every exit the shop has calls it. Refresh() is STILL private: S3 buys nothing, so nothing changes what IsActive answers; it goes public in S4 when there is a caller. ViewedLocation came BACK in S2 (D-030) after D-027 deleted it: the objection was "no caller", and MetaShopView is now the caller. ViewedLocationChanged did NOT come back -- the shop rebuilds its list every time its panel opens, and the only moment the player can walk to another location is while that panel is closed, so an event would be a publisher written before its subscriber. The location bar's four references are OPTIONAL since D-025 -- with one location there is nothing to switch between, so requiring them forces scene clutter for a feature with no content, and deleting the bar is a legitimate choice. Only sessionHost/catalog/scroll/background stay required, because without them nothing can be drawn at all. The shop's ghost preview, ViewedLocation and ViewedLocationChanged were REMOVED with the shop (D-027): they had exactly one caller, and an entry point with no caller reads as one used somewhere. Refresh() went back to private for the same reason. decides NOTHING -- what is active comes from MetaResolver, where prices, area gates and unlock days were already settled and tested (D-019); this turns three answers into RectTransforms. Props are INSTANTIATED from the catalog, never placed in the scene: nineteen hand-placed objects would be a second copy of the catalog free to disagree with it silently. Two things are load-bearing. Canvas.ForceUpdateCanvases() before measuring: prop sizes derive from the background's WIDTH, and on the frame Start runs a stretched rect's width is still zero, which would size every prop to nothing and look like an empty screen rather than an error. And props are CHILDREN of the background with their normalizedPosition written straight onto anchorMin/anchorMax -- that is what K5's "normalize to the background rect, not the screen" was for: they ride the art through every resize and every scroll with no layout code. Background fits by WIDTH and the height falls out of the aspect (M10, the user's choice): fitting by height would letterbox every device, while fitting by width leaves vertical scrolling as the only variable, and that is zero on the phones the art was drawn for. Refresh() is public because Adım 8's purchase changes what IsActive returns and nothing else would notice; it runs at EVENT frequency (screen open, purchase), never per frame. Props are raycastTarget: false so they cannot swallow drags meant for the scroll view. Location stepping is between UNLOCKED locations only -- letting the player walk into a locked one would duplicate in the UI the refusal MetaPurchase.LocationLocked already owns D-048 added the PURCHASE PLACEMENT animation: a just-bought prop is prepared translucent and placementDropHeight above its spot in the SAME FRAME DrawProps drew it (waiting even one frame would show it standing in place and then jerk it into the air), then -- after previewTravelSeconds, so the map has finished travelling back out of the preview zoom, the same wait CelebrateOne makes for the same reason -- it falls on an IN ease while its alpha rises LINEARLY toward the prop's own alpha, and the ground takes the hit as it lands. Animated by a frame loop on Time.unscaledDeltaTime rather than a tween, which is CelebrateOne's choice and buys the same two things: a try/finally that guarantees the finished state through any interruption, and no tween left pointing at a RectTransform Refresh is about to destroy. The shake moves the CONTENT, not the viewport -- a moved mask window would open a sliver of empty screen at the edges, while the content carries D-046's oversized continuation layer as a child, which is the art that keeps them covered -- and it suspends the ScrollRect for its duration (D-032's call: Clamped scrolling drags the content toward its bounds every LateUpdate and would turn the shake into a stutter), handing it back under RestoreFocus's own `!focused` condition so two places never own that flag. The swing is a damped sine, negative first, not per-frame randomness: an impact has a direction and noise at 60fps reads as a glitch. Clear() stops the routine BEFORE destroying props, which makes "the props are gone" and "nothing is still animating them" one moment. D-049 KEEPS THE PREVIEW ZOOM through the placement and only then travels out. `placing` is the claim and RestoreFocus is where it is honoured -- a flag next to the existing `focused` guard rather than a parameter threaded through the shop's SetOpen/ClosePreview, because "may the zoom be given back right now" is this class's question. The order in MetaShopView.OnConfirmBuyClicked is load-bearing and was found by wiring it, not by reasoning: RefreshAfterPurchase must run BEFORE SetOpen(false), because the shop's exit goes through ClearGhost whose second half is the request for the zoom back -- claiming after it would let the zoom-out start and the prop would land on a map already travelling away. Both calls share one frame, so nothing is visibly half-done. Two waits and a scroll block DIED with this: the fixed previewTravelSeconds wait (the map no longer moves) and the shake's disable/re-enable of the ScrollRect (FocusOn turned it off at D-032 and RestoreFocus turns it back on, so the whole placement runs inside a window where it is already suspended). What replaced the wait is `while (DOTween.IsTweening(content))`, zero frames in the normal case and exactly the remainder for a player who taps BUY before the travel INTO the zoom has finished. INTERRUPTION OWNERSHIP is the subtle part: `placementGeneration` is bumped by every claim, and the finally hands the map back only if its own generation still matches -- so a second purchase takes the framing from the first placement instead of having the first one's teardown travel out from under the second one's drop. A location change is the opposite case and is handled by its own caller: Step calls CancelPlacement, which bumps the generation and gives the framing back SNAPPED (RestoreFocus(animated:false)), because Step sets the new location's scroll position one line later and an animated return would still be moving and would win. Clear cannot make that call -- it cannot tell a location change from a purchase -- which is why the decision sits in Step. Both cases are unreachable in today's scene (one location, D-025's optional arrows) and are written down because the second location is what makes the first one bite. D-050 routed the prop ARITHMETIC through MetaLayout: PropScale and the prop's size are asked of it rather than computed here, because the day scene's backdrop now composites the same prop and the two must agree pixel for pixel. The POSITION stayed anchors + pivot -- that is the better implementation of the same rule, since an anchored prop rides the background through every resize without a line of layout code, and MetaLayout.PropRect is that rule written out for the backdrop, which has no anchors to lean on. So this file remains the reference if the two ever disagree. D-051 fixed a DEPTH bug that had been live since D-045 and only became visible when D-047 started closing the shop after a purchase. Object.Destroy is DEFERRED to the end of the frame, so a redraw's old art layers, props, silhouette and ghost were all still children of the background while the new ones were being appended -- the parent carried BOTH sets for the rest of that frame. Anything that only APPENDS is fine (the new props keep their relative order), but DrawUpcoming moves its silhouette to an ABSOLUTE sibling index computed as though the dead children were gone. On the report that surfaced it the dead block was 13 children and the computed index was 12, so the silhouette landed inside the corpses, BEHIND the new background art, and vanished -- which read as "the prop was deleted by my purchase". Every spawned child now dies through Detach, which does SetParent(null) BEFORE Destroy so it leaves the child list on that line and every index computed afterwards counts only what is really there. The user's own first description -- "arkaplanda kaldi" -- was literally right and was misread as a framing complaint; the positions of the props they COULD see were what made it look like a scroll problem. | h:f9ba71e0
ExpoTheExplorer/Assets/Scripts/UI/MetaShopView.cs | the meta shop: market button + the panel it opens | sys: MetaSystem | api: - (all private; the shop is driven by its own buttons) | dep: - | used: MetaShopSetup | crit: K2 | note: D-039: rows bind offer.ShopIcon, not offer.Sprite. The ghost and the map prop still use Sprite. D-038: the confirm popup has NO backdrop -- the user removed it from the scene and the field went with it, so CANCEL is the only way out of a preview (MS6 is dropped) and the map is not dimmed while one is up. The backdrop's other job, eating taps meant for the map, costs nothing: the ScrollRect is already disabled for the duration of a preview (D-032). The cancel button is now the ONLY exit, which makes an unwired one the worst failure on this screen, so it stays required. D-037: owns both BUY colours and applies them on every bind, so a row always shows one of the two rather than sometimes inheriting the template's authored colour. The popup's BUY is red AND disabled when the money is short: the disabled state darkens the base colour, so it reads as "red, and off" instead of the generic grey that says nothing about which reason. The unaffordable red is the project's existing destructive-action red, so the game does not grow a second red. D-036: Start hides the panel and the popup BEFORE validating, with its own null checks. A missing reference must read as a console error, not as the shop covering the screen -- which is exactly what happened while the scene still held the S1-era shop: validation returned early, so nothing was switched off and the panel sat over the map with a market button that had no listener. An incomplete shop is invisible now, not stuck open. S5 (D-035): the view neither re-sorts nor re-filters -- ShopItems returns the list already filtered and ordered, so there is one authority for what a shop shows. Affordability per row is price-vs-balance rather than another Evaluate call: everything Evaluate could still refuse on has been filtered out, so money is the only question left and asking a six-verdict function for a yes/no would imply otherwise. The popup's BUY is now interactable only when the money is there -- until S5 it was live and did nothing on a short balance (Evaluate refused, logged, screen unchanged), which is a dead button in every sense the plan warned about; the commit still re-checks everything. S4 (D-034) made the purchase real, and it is the ONLY place the meta side spends money -- while owning no rule: MetaPurchase decides whether the buy is allowed, Wallet decides whether the balance carries it, GameSession.Save decides what reaches disk. Evaluate runs a SECOND time here, having already run when the row was drawn, because the panel stays open across purchases and the balance the row was drawn against goes stale; the cost is one call. The wallet is then asked anyway even though Evaluate just approved: Wallet is the single writer of the balance and its answer is the authority -- decision and transaction, not two answers to one question. ATOMIC BY CONSTRUCTION rather than by care: the balance and the owned set live on one GameSession and Save() writes both, so "money gone, item missing" is not expressible. A non-Ok verdict logs and leaves the popup OPEN -- saying why on screen is S5, and doing it here too would put the appearance of a verdict in two places. Panel stays open after a buy (MS4) and the row list is rebuilt so the bought prop drops out. S3b (D-032) moved the popup ONTO the prop: confirmBox has a bottom-centre pivot and is placed just above the ghost, flipping BELOW it when the prop sits near the top of the screen, and clamped away from the screen edges. Positions are computed in the parent RectTransform's local space, not world units, because the box's size is measured there and mixing the two drifts by whatever the canvas scaler is doing on the device. Recomputed on every preview -- a cached position would be wrong for the second prop. confirmBox is REQUIRED: unwired, the popup would quietly re-centre itself, and a silently-missing feature is worse than a named missing reference. S3 made this the whole state machine (D-031). Two fields hold it: isOpen (KAPALI/ACIK) and pendingItem (ONIZLEME when non-null, and it stores the ITEM so the popup and the S4 commit cannot disagree about which prop is being bought). Every transition goes through ApplyVisibility and every exit from a preview through ClosePreview -- ONE answer to "when does the ghost go away", which is exactly what the first shop lacked (D-024). The list and the popup are never both up: the panel is a bottom sheet covering the map the ghost stands on, so previewing with the list up would hide the thing being previewed and the feature would look broken rather than hidden. Panel visibility is therefore isOpen && !previewing -- activeSelf is neither field on its own. Rows are NOT destroyed while previewing; cancel brings the list back rather than rebuilding it. Tapping a second row REPLACES the preview (a change of mind, not an error). The popup BUY changes no state in S3 on purpose: closing would make S3's own verification ambiguous and S4 would write the closing twice. The popup's four interactive refs are REQUIRED -- an unwired cancel or backdrop strands the player in a preview with a ghost and no way back, the exact leak this step exists to make impossible; the two labels stay optional because they only say what is being bought. S2 filled the panel: a row per offer from MetaPurchase.ShopItems, cloned from an authored INACTIVE template in the scene rather than a prefab asset (the TicketCardView/modificationRowTemplate pattern) -- restyling that one object restyles the list, and there is no second asset to keep in sync. Which LOCATION is shopped is read from MetaGroundsView.ViewedLocation, NOT resolved again here, which is what keeps the MetaCatalog reference in exactly one place; two components free to point at two catalogs would break D-015 with nothing looking wrong. Rows are rebuilt on every OPEN (a purchase changes what ShopItems answers) and destroyed on close -- not pooled, same call MetaGroundsView.Clear makes. rowsParent is forced ACTIVE and the template forced INACTIVE in Start: hiding the container to get the sample row out of sight silently hides every real row, the trap TicketCardView records. Instantiate copies the template's inactive state, so each clone needs an explicit SetActive(true). All SIX references are required -- listing the first location's props when grounds is unwired would show a wrong list as though it were right, which is the failure D-028 was written about. Step S1 of meta-shop-plan.md, and DELIBERATELY thin: a market button and a panel that opens and closes. No rows (S2), no ghost, no confirm popup (S3). Reads nothing and writes nothing -- the catalog, the wallet and the six purchase verdicts stay in MetaPurchase where they were tested (D-019), and this screen will ASK them in S2 without owning a rule. The panel is toggled from CODE rather than by a Button-to-SetActive wire in the Inspector, even though that wire would do today's job: this class is going to own the shop's whole state machine (KAPALI/ACIK/ONIZLEME) and the ghost's lifetime hangs off these same transitions, so a machine half in the scene and half in code is one that disagrees with itself. SetOpen is the single place visibility changes, for the same reason -- the first shop's ghost leaked because its visibility had more than one owner (D-024/D-027). Forced CLOSED in Start whatever the scene was saved as: a shop already up hides the grounds the player came to look at, and the scene checkbox is easy to leave ticked after testing. Both references are REQUIRED, unlike the grounds' optional location bar -- a shop with no button cannot be opened and a button with no panel does nothing, so neither has a degraded mode worth shipping. isOpen is not persisted (same call K6-4 made for the viewed location: a profile field and a schema version for a preference nobody would miss) D-047: a SUCCESSFUL purchase closes the shop all the way to KAPALI instead of restoring the list. MS4 had deliberately kept the panel open so several props could be bought without tapping the market button between each one; the user reversed it ("bir sey satin alindiktan sonra yeniden dukkana donmesin") and the reason holds up in the geometry: the panel is a bottom sheet over most of the map, so returning to the list hides the very prop that was just paid for behind the ones that were not. OnConfirmBuyClicked therefore ends with SetOpen(false) then grounds.Refresh() -- through the single door rather than by clearing isOpen locally, so the ghost, the popup, the framing and the rows all come down on the one path that D-024's leak taught this class to keep. RebuildRows is GONE from that path, not moved: a closed panel has no rows, and the next open rebuilds them against the balance as it will be then. The FAILED paths (verdict != Ok, wallet refusal) still leave the screen exactly as it was -- closing on a refusal would read as a purchase that happened. D-048: the purchase's last call is grounds.RefreshAfterPurchase(item), not Refresh() -- the shop passes WHAT was bought and nothing about how it should look, so the drop-and-shake lives entirely in the grounds and this screen stays a thing that spends money and asks two views to catch up. D-049 moved this call BEFORE SetOpen(false) and the order is load-bearing: RefreshAfterPurchase claims the map's framing, and SetOpen's ClearGhost is the request to give it back -- closing first started the zoom-out before the grounds had heard about the purchase, so the prop landed on a map already travelling away from it. Same frame, no render between, so the panel and the drop begin together; SetOpen's ClearGhost is now a no-op on both halves by design, the ghost having been destroyed by the redraw above. | h:eeae51e2
ExpoTheExplorer/Assets/Scripts/UI/MetaShopRowView.cs | one shop row: icon, name, price, BUY | sys: MetaSystem | api: Bind(Sprite icon, string displayName, int price, bool affordable, Color buyColor, Action onBuyTapped) | dep: - | used: MetaShopView,MetaShopSetup | crit: K2 | note: D-037: the BUY turns red when the row cannot be afforded, on top of the dim. The colour is handed IN, not chosen here -- both colours live on MetaShopView, because this class is a binder that decides nothing and two components each holding the same pair is a pair that can drift. It paints Button.targetGraphic, the button's own declared graphic: no new serialized reference, so no scene rebuild (which D-036 had just cost a round). Writing to Graphic.color survives the ColorTint transition -- Selectable applies the state tint to the CanvasRenderer and MULTIPLIES with Graphic.color rather than overwriting it, which is also why the authored green shows through. S5 (D-035) added the dim: an unaffordable row fades via a CanvasGroup alpha, which over the panel's dark background reads as darker -- the user's wording. ALPHA only, never interactable: the row's BUY must still open the preview, because seeing where a prop would go and what it costs is what makes it worth saving for, and disabling the row would make it dead furniture. The popup's BUY is what refuses, visibly. Pure BIND component, the same shape ModificationSlotView has under TicketCardView and for the same reason: how many rows there are is something only the catalog and the player's purchases know, so they cannot be laid out in the Editor ahead of time. Decides NOTHING -- whether an item is listed comes from MetaPurchase.ShopItems, whether it can be afforded will come from MetaPurchase.Evaluate in S5. The BUY listener is wired ONCE in Awake and Bind only swaps the stored callback: adding the listener inside Bind would stack silently the first time a row is rebound, and a BUY that fires twice is the worst bug this screen could have. A null icon HIDES the Image rather than leaving a white box -- MetaCatalogValidator already reports a missing sprite as a content error, so this row does not add a second verdict about it. Price is a bare number: the HUD's coin icon is the unit on this screen | h:cf091478
ExpoTheExplorer/Assets/Scripts/UI/ModificationSlotView.cs | MISSING-role | sys: ? | api: SetModification(Sprite ingredientSprite, Sprite directionSprite, Color boxColor) | dep?: Editor,TicketCardView | used: ? | crit: ? | note: auto-added; AI must complete | h:3f9470ae
ExpoTheExplorer/Assets/Scripts/UI/SoftMoneyView.cs | HUD soft-money counter | sys: ProgressionSystem | api: - | dep: HudWalletSource | used: HudCanvasPrefabSetup | crit: K3 | note: reactive in the day scene (binds SoftMoneyChanged, never polls); on the main screen there is no event to bind to, so the value renders once. Holds no GameManager since D-013 -- see HudWalletSource for why a shared prefab cannot serialize one. Reads through the SOURCE so one call serves both modes; still subscribes from Start, not Awake | h:601afe0f
STALE ExpoTheExplorer/Assets/Scripts/UI/TicketCardView.cs | one ticket slot's card: content + timer bar | sys: TicketSystem | api: IsAnimating; SetTrayAnimating(bool animating); Initialize(GameManager gameManager, int slotIndex, TicketCardsView owner, BoardAnimationConfig animConfig); SuppressPollUntilDeliveryTransition(); PlayDeliveryTransition() | dep: BoardAnimationConfig,FoodCategory,FoodItemConfig,GameManager,GameState,ModificationSlotView,PatienceType,Ticket,TicketCardsView | used: TicketCardsView,TrayFillCounterView,WorldTrayView | crit: K2 | note: polls its slot every frame because TicketDelivered/TicketCancelled fire BEFORE the slot is reassigned and FillEmptySlots fires nothing; the timer bar drains a Filled Image over the ticket's whole TimeLimitSeconds and clones one anchored divider tick per segment boundary, so an unevenly-divisible limit ends short rather than shifting the ticks; an empty slot hides the WHOLE card through canvasGroup.alpha=0 (never SetActive, which would pull it out of the parent HorizontalLayoutGroup and slide the other cards) and PlayDeliveryTransition's entry fade targets 0 instead of 1 when no ticket replaced the delivered one | h:bdc3632a
ExpoTheExplorer/Assets/Scripts/UI/TicketCardsView.cs | spawns and owns the 3 ticket card slots | sys: TicketSystem | api: TicketSpriteFor(PatienceType patienceType); ModificationBoxColorFor(PatienceType patienceType); DirectionSpriteFor(bool isAddition); TimerSegmentSeconds; TimerFillColorFor(float remainingRatio); GetCard(int slotIndex) | dep: BoardAnimationConfig,EconomyConfig,GameManager,GameState,PatienceType,TicketCardVisualsConfig,TicketCardView,TrayFillCounterView | used: TicketCardView,WorldTrayView | crit: K2 | note: the single lookup point turning PatienceType (and the timer's remaining-time ratio) into visuals -- TicketCardVisualsConfig deliberately carries the values but no lookup logic; TimerFillColorFor takes its two THRESHOLDS from gameManager.EconomyConfig (they also pick the tip tier) and only its colours from the visuals config | h:a348b905
ExpoTheExplorer/Assets/Scripts/UI/TrayFillCounterView.cs | MISSING-role | sys: ? | api: Initialize(GameManager gameManager, int slotIndex, TicketCardView ticketCardView) | dep?: GameManager,TicketCardView,TrayManager,WorldTrayView | used: ? | crit: ? | note: auto-added; AI must complete | h:e057ae2e
ExpoTheExplorer/Assets/Scripts/UI/WorldTrayView.cs | one ticket slot's tray, and its drop target | sys: TraySystem | api: OnDrop(PointerEventData eventData); TryAcceptDrop(BoardItemDragHandler dragHandler); SetHighlighted(bool active) | dep: BoardAnimationConfig,BoardItemDragHandler,BoardView,FoodCategory,GameManager,GameState,Ticket,TicketCardView,TicketCardsView,TrayManager | used: BoardItemDragHandler | crit: K2 | note: world-space on purpose, NOT Canvas UI, so a dragged item's real GameObject can be reparented straight in; a standalone scene object per slot with slotIndex set by hand and ONE BoxCollider2D for the whole tray -- which of the 3 slot transforms an accepted item lands in comes from its FoodCategory, and ResolvePlacementSlot overflows to another empty slot so two same-category items never render stacked; polls TrayManager's count every frame ONLY to catch a timeout scatter, reading count==0 as "externally cleared" because a manual pickup only ever decrements by one; the wrong-order shake (IsShakeable) and ClearSlot both skip an item the player is currently holding -- a second writer on a held item's transform is what pinned one at the tray's X while the finger moved away | h:5e32e12a
ExpoTheExplorer/Assets/Scripts/UI/MetaBackdropView.cs | day scene backdrop: the decorated grounds, blurred | sys: MetaSystem | api: - (all private; built once in Start) | dep: MetaResolver,MetaLayout,MetaCatalog,MetaLocation,GameSession,SessionHost | used: MetaBackdropSetup | crit: K3 | note: D-050. The day is played AT the expo the player decorates, so SampleScene's background becomes their own grounds -- the location art with the props they OWN standing on it -- blurred behind the board. Built ONCE in Start and never again: a purchase shows up in the NEXT day's backdrop, not halfway through the current one, which is correct because the grounds are not something the player can change while a day runs. After Start it is a small texture on a RawImage, so the per-frame cost is one draw call. NO CUSTOM SHADER: the blur is progressive halving through Graphics.Blit, and a bilinear sample at half size lands exactly between four source texels, so each step is an exact four-texel box average. That is the right answer HERE specifically because the project runs the BUILT-IN pipeline -- the URP package is installed but no pipeline asset is assigned in GraphicsSettings -- so renderer features are unavailable and the alternative was a multi-tap shader running full screen every frame for an image that never changes. The result is left SMALL: stretching it across the screen is itself a bilinear filter, so the upscale finishes the blur for free. Composed at composeScale (0.25) because the output is about to be blurred into soft shapes and full resolution would spend memory on detail thrown away one step later. WHICH location and WHAT stands on it are MetaResolver's two calls, the same ones the meta screen makes, so the backdrop cannot show a different expo than the one the player walks around in; it reads the catalog and the owned set and writes NOTHING. Every failure path leaves the RawImage exactly as the author left it -- that IS the fallback, and there is deliberately no fallbackTexture field, because a second place to author the same thing is a second place for it to be wrong. The final RenderTexture is OWNED and released in OnDestroy (graphics memory is not garbage collected, and a scene reloaded a few times would leak one backdrop per load), and it is cleared off the RawImage first so nothing draws a released texture. Sprites are drawn through the sprite's own textureRect, not the whole texture: drawing the whole texture works right up until the art is packed into an atlas, and then every prop becomes a collage of its neighbours. | h:67b027e3
ExpoTheExplorer/Assets/Scripts/UI/DayRewardFlightView.cs | the day's payout, performed: stars seat, gems and coins fly to the HUD | sys: DayLifecycle | api: Play(IReadOnlyList<GameObject> starsToSeat, RectTransform coinOrigin); CompleteImmediately() | dep: StarScoreBarView,GameManager,DayRewardPurse | used: DayCompletePopupView | crit: K2 | note: D-057. Nothing is credited during a day, so THIS is what hands the earnings over -- each landing icon calls GameManager.ClaimRewardGems/ClaimRewardSoftMoney, which is why the HUD views needed no change at all: they are already reactive, so what the player watches arrive and what they own are one event instead of two kept in step. Amounts are never passed in; they are read from GameManager's pending purse, so this class cannot disagree with the ledger. It CANNOT lose money -- every exit from a finished day commits the remainder first, so an unwired reference, a skipped sequence or a mid-flight tap all still pay in full; killing a tween deliberately does not run its OnComplete, so a destroyed icon never claims its share and the commit pays it as one lump instead. Two ordering rules are load-bearing: CompleteImmediately guards on IsPlaying, or the call at the top of Play would commit the purse OnDayCompleted had just filled; and RunSequence clears starsBeingSeated before its own commit so a later tap cannot re-enter. Relies on Unity's Awake/OnEnable-before-Start guarantee for the purse to exist when Play runs (GameManager subscribes DayCompleted in OnEnable, the popup in Start) -- if that ever inverted the animation would render an empty debt and the exit would still pay, so the failure is degraded rather than broken. Lives on the always-active popup object, NOT under popupRoot (a coroutine dies with its GameObject); flightRoot is outside popupRoot too, which also makes icons draw above the popup. Icons are built in code from two serialized sprites rather than a prefab -- 13 per completed day is event frequency, so pooling would be cost with no saving. Positions cross two canvases (receipt on the popup, counters on the shared HUD prefab), so everything converts through screen space; CameraFor walks up from a reference already handed in, which is not a scene lookup. ONE flightRoot serves all three stars -- it is the coordinate space the icons live in, not a launch point; each gem is measured off the star that just seated, so the three seats give three different origins. ToFlightLocal takes a plain Transform rather than a RectTransform so a non-UI star still launches its gem: the earlier cast-and-branch skipped silently, which is the wrong failure here (the gem vanishes, the money still arrives on the exit commit, and nothing on screen says why) D-061: an OPTIONAL score bar now PACES the star loop -- each star waits on the fill reaching its own notch instead of a fixed starSeatInterval, so the thing that earned the star and the star itself are one motion rather than two animations that coincide. The gem shares are untouched (still per star index), so money cannot drift out of it. Three guards keep the wait from hanging a coroutine that is holding the day's payout: it also breaks on IsFillFinished, an unwired fill Image reads CurrentFill 1, and an out-of-range notch reads as the right edge. Unwired, the fixed-interval rhythm is kept verbatim. | h:4e14b141
ExpoTheExplorer/Assets/Scripts/UI/StarScoreBarView.cs | the star score as a fill bar, and the clock the star row runs on | sys: DayLifecycle | api: CurrentFill; IsFillFinished; PositionOf(int starIndex); Prepare(float scoreProgress, float twoStarDuePosition); BeginFill(); SnapToTarget(); ShowTargetWithoutAnimating() | dep: - | used: DayCompletePopupView,DayRewardFlightView | crit: K3 | note: added by D-061. Full == three stars, not 100% of the score: a bar that keeps filling past the last star communicates nothing. D-062 REMOVED its other half: it no longer positions the stars (three marker fields + PlaceMarker are gone), because the user rejected stars standing on their thresholds -- three 100px stars across a 450px bar stop reading as a three-star rating. A star's position is the scene's business now and no code writes one. Deleted rather than left unwired on purpose: PlaceMarker no-oped on a null marker, so the old behaviour would have been one drag away, and the scene's three markers all sat at the same point (0,0 inside Fill) so clearing the slots would have stacked the stars. It also PACES the seating: the flight waits on CurrentFill reaching PositionOf(i) instead of counting fixed intervals, which is what ties "my speed earned that star" to the star that pops. CurrentFill returns 1 when the fill Image is unwired, deliberately -- this value gates a sequence holding the day's payout, so an unwired bar must let it straight through rather than stall it | h:3f855661
