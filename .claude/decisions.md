# decisions — architectural decision record (ADR)

<!-- One line per decision. `affects:` is not optional: a decision nobody can
     trace to code is a decision nobody can revisit. check_blueprint.py and the
     postflight both read it.

     D-001: <decision> — <rationale> — affects: <system(s) / path(s)> — <date>
-->

D-001: Reconnect BoardDistributor to runtime for live/dynamic board food-spawn distribution (replacing DayContentGenerator's pre-authored BoardTimeline replay for everything except Day Start, TriggerStepIndex == -1, which stays authored), with guaranteed-ticket selection made probabilistic (a per-round budget, Poisson-sampled or manually fixed, per-Day configurable; picks tickets via an arrival-order-weighted lottery; any active ticket under a configurable remaining-time threshold, default 10s, is unconditionally guaranteed and consumes from — and may exceed — that budget) — rationale: ExpoTheExplorer/CLAUDE.md Section 3 ("Board / Food Distribution"), still marked Locked, already describes this live required-pool/noise-pool system as current design; it was never updated when an earlier, undocumented change (referred to only in code comments as "PR-6") disconnected BoardDistributor from runtime because a live ticket-lookahead buffer over-consumed the Day's fixed-length pre-authored TicketSequence — fixed here via a non-consuming DayTicketSequenceProvider.PeekUpcoming instead of permanently avoiding a live system; the probabilistic-selection layer (vs. the old deterministic "earliest N") is a new design choice made this session, not a restoration of prior behavior — affects: GameManager.cs, DayTicketSequenceProvider.cs, BoardDistributor.cs, BoardDistributionConfig.cs, DayEditorMetaJson (+ DayEditorModel.cs's editor surface for it), DayContentGenerator.cs, DayEditorBoardTimelinePreview.cs, DaySolvabilityChecker.cs, DayValidator.cs — 2026-08-14

D-002: Remove DaySolvabilityChecker and DayValidator's solvability pre-check entirely (Day Editor's Save-button gate keeps only the ticketSequence.Count == ticketsRequiredForDay check + RetryVariant recursion); delete DayEditorBoardTimelinePreview.cs and DayContentGenerator's per-step board simulation (RegenerateBoardTimeline, EnsureSolvable, and the round-robin delivery/spawn loop inside GenerateCore) — rationale: D-001's live reconnect means "is this Day completable" is now guaranteed BY CONSTRUCTION every OnOrderPlaced call at runtime, not something to pre-validate from a per-step authored schedule; DaySolvabilityChecker's counting math assumed exactly that kind of schedule (a ticket's deadline is satisfied by supply spawned at specific TriggerStepIndex values) and produces nonsensical results once BoardTimeline editing is restricted to Day Start (TriggerStepIndex == -1) only — every demand across the whole Day would need to be pre-supplied at Day Start, permanently failing validation for any real Day. Confirmed with the user (AskUserQuestion, chose "Kaldır") rather than deferring to the roadmap's original Phase 5 slot, since D-001's own Phase 4 (BoardTimeline editing restricted to -1) cannot ship coherently without this — affects: DaySolvabilityChecker.cs (deleted), DayValidator.cs, DayContentGenerator.cs, DayEditorBoardTimelinePreview.cs (deleted), DayEditorModel.cs, DayEditorWindow.cs — 2026-08-14

D-003: Remove the per-Day authored "retry variant" feature entirely — `DayDefinition.RetryVariant` (property + constructor parameter), the `hasRetryVariant`/`retryVariant` JSON sentinel pair on `DayRuntimeJson`, `DayCatalogParser`'s recursive variant resolution, `DayValidator`'s recursive `"RetryVariant: "`-prefixed validation, `DayCatalogNavigator.GetEffectiveDay` (the whole method, its only reason to exist), `GameManager.isRetryAttempt` (so `CurrentDay` is a plain `GetDayAt` again), and the Day Editor's "Retry Variant" foldout together with the `IsTopLevel` flag and `FromDayJson`'s `isTopLevel` parameter that existed solely to support a nested variant model — rationale: requested directly by the user ("projedeki retry variant özelliğinin hepsini kaldırır mısın"); the feature's only ever-realized runtime effect was swapping `TicketsRequiredForDay` on a retry, it carried a `JsonUtility` null-sentinel workaround (roadmap Risk 8) and five levels of empty nested blobs in every authored Day file, and CLAUDE.md Section 4's "difficulty scale-down on life loss" was never answered so nothing shipped depended on it. NOTE: this deletes the mechanism that open question would most naturally have used — a future answer to it starts from scratch rather than from `RetryVariant`. The retry *flow* is untouched: `GameManager.RetryDay()` (free life-loss path) and `RetryCompletedDay()` (voluntary star redo) both still replay the Day itself at unchanged difficulty — affects: DayDefinition.cs, DayJson.cs, DayCatalogParser.cs, DayValidator.cs, DayCatalogNavigator.cs, GameManager.cs, DayEditorModel.cs, DayEditorTicketCardPreview.cs, day_00.json, day_01.json, 7 EditMode suites (DayValidatorTests, DayCatalogNavigatorTests, DayCatalogParserTests, DayEditorModelConverterTests, DayContentGeneratorTests, DayFileIOTests, DayTicketSlotManagerIntegrationTests) — 2026-08-14

D-004: Board-distribution balancing is authored PER DAY in Day JSON (`runtime.boardDistribution`) and is the single runtime authority; `BoardDistributionConfig` is demoted to a Day-Editor seed asset (new-Day starting values + its Inspector preview) and leaves the runtime path entirely. Mechanically: a plain `BoardDistributionSettings` value object in Data replaces the ScriptableObject in `BoardDistributor`'s constructor; `DayCatalogParser` builds it from the Day file and hands it over via `DayDefinition.BoardDistribution`; `GameManager` rebuilds the distributor per Day from `CurrentDay.BoardDistribution` and no longer serializes a config reference; `BoardDistributionConfig.CloneWithOverrides` is deleted — rationale: requested by the user ("bu ayarlar Day Editör kısmında olsun ve bölümün JSON kısmına yazılsın", and explicitly "override kısmını da kaldır çünkü override gibi olmayacak"), so there is no base/override layer — every Day carries a complete block. The per-Day fields already existed but had been placed in `editorMeta`, the one section `DayCatalogParser` is contractually blind to (see the class comment on `DayJson`), so all eight were authored, saved and read by nothing at all; moving them under `runtime` is what makes them real. The plain-settings form (rather than `Instantiate`-ing the asset per Day and overwriting its fields) was chosen because the clone would need explicit `Destroy` on every Day change and retry — one leaked ScriptableObject per transition otherwise — and because CLAUDE.md Section 5 requires the Food Distribution Module to carry no Unity dependency; clamping moved with it into `BoardDistributionSettings`' constructor, which is now the single enforcement point for those bounds. NOTE: this changes gameplay. `BoardDistributionConfig.asset` predates four of the eight fields and reads them at the CLR default, which had silently disabled D-001's urgent-ticket guarantee (`urgentTimeThresholdSeconds` 10 → 0) and flattened its arrival-weighted lottery (`earlyTicketWeightDecay` 0.5 → 0); `day_00`/`day_01` were migrated with the DESIGNED values, not the asset's current ones, after confirming with the user — affects: BoardDistributionSettings.cs (new), BoardDistributionConfig.cs, BoardDistributor.cs, DayJson.cs, DayDefinition.cs, DayCatalogParser.cs, GameManager.cs, DayEditorModel.cs, day_00.json, day_01.json, BoardDistributionTests, BoardDistributionConfigCloneWithOverridesTests (deleted), DayCatalogParserTests, DayEditorModelConverterTests, DayValidatorTests, DayContentGeneratorTests, DayTicketSlotManagerIntegrationTests — 2026-08-17

D-005: The play-time half of `TicketGenerationConfig` — the three patience time limits and `upcomingQueueSize` — is authored per Day in Day JSON (`runtime.ticketRuntime`) and is the single runtime authority for those four values; the generation PROBABILITIES (side/drink inclusion, modification count/direction, main-dish weights) stay where they are, because a Day's `ticketSequence` is rolled at authoring time and baked into the file, so those knobs have already done their work before the Day is ever played. Mechanically: a plain `TicketRuntimeSettings` in Data carries the four values; `TicketRuntimeSettingsExtensions` in Core carries the Impatient/Normal/Patient switch (Data must not reference `Core.PatienceType`, the same constraint `EconomyConfig` already documents), replacing `TicketGenerationConfigExtensions`, which is deleted; `DayCatalogParser` resolves the block onto `DayDefinition.TicketRuntime`; `TicketEntryFactory` and `DayTicketSequenceProvider` take the settings instead of the config asset; `GameManager` reads the lookahead from `CurrentDay.TicketRuntime`. `TicketGenerationConfig` survives — unlike `BoardDistributionConfig` in D-004 — because it still owns the names database (a TextAsset reference that cannot live in Day JSON) and the generation knobs, and it gains `ToRuntimeSettings()` as the authoring-side seed. NOTE: the plan called for making `TicketFactory` per-Day as well; reading the code showed that is unnecessary — at play time `TicketFactory` is only a customer-name picker (`Create`/`PickRandomPatienceType` are authoring-only), and the name pool is not per-Day balancing, so it stays session-scoped. Unlike D-004 this changes no behaviour: `TicketGenerationConfig.asset` carried all four values (45/90/150/10) and both Days were migrated with exactly those — affects: TicketRuntimeSettings.cs (new), TicketRuntimeSettingsExtensions.cs (new), TicketGenerationConfigExtensions.cs (deleted), TicketGenerationConfig.cs, TicketFactory.cs, DayJson.cs, DayDefinition.cs, DayCatalogParser.cs, DayTicketSequenceProvider.cs, TicketEntryFactory.cs, GameManager.cs, DayEditorModel.cs, day_00.json, day_01.json, TicketRuntimeSettingsExtensionsTests (new), TicketGenerationConfigExtensionsTests (deleted), TicketEntryFactoryTests, DayTicketSequenceProviderTests, DayCatalogParserTests, DayEditorModelConverterTests, DayTicketSlotManagerIntegrationTests, DayValidatorTests, DayContentGeneratorTests — 2026-08-17

D-006: The ticket-GENERATION settings (side/drink inclusion chance, fallback modification-count lambda, modification-addition chance, main-dish weights) are authored per Day as a complete block in `editorMeta.ticketGeneration`, with no override layer: the `hasTicketGenerationOverride` toggle and the `...Override` field suffixes are gone, and `TicketGenerationConfig.CloneWithOverrides` (nullable params, inherit-what-you-omit) becomes `CloneForDayGeneration` (all five required). `modificationAdditionChance` and `mainDishWeights` were previously not per-Day at all and are added, so the record is complete; weights are stored as food ids and resolved via `FoodCatalog` (`DayContentGenerator.ResolveMainDishWeights`), dropping ids whose `FoodItemConfig` is gone, the same stance `ResolveFoodPool` takes. `MainDishWeight` gains a public constructor so a Day's resolved weights can be built in code — rationale: the user's stated reason for keeping these in the Day file is being able to return to a Day later, SEE which settings it was generated with, and re-generate from them; a partial record ("hangi ayarla yapılmış" minus two of the knobs) would not serve that. These stay in `editorMeta` rather than moving under `runtime` — unlike D-004/D-005 — because a Day's `ticketSequence` is rolled at authoring time and baked into the file, so the probabilities have already done their work before the Day is ever played; `DayCatalogParser`'s contract of never reading `editorMeta` is therefore preserved, and `ParseAll_EditorMetaFieldsIgnored_DoesNotAffectResolution` still enforces it. Deliberate asymmetry with D-004/D-005: a missing block here is NOT fatal (the runtime blocks drop the Day; this one falls back to the seed asset's real values and is written out on the next Save), because runtime correctness does not depend on it and an old Day must stay openable. The Instantiate-clone is also kept here, unlike D-004, because `TicketFactory` needs `namesDatabase` — a TextAsset reference that cannot live in Day JSON — and this path runs only at authoring time and destroys the clone in a `finally`, so there is no per-Day runtime lifetime to leak. No behaviour change: both Days were migrated with the asset's exact values (0.9 / 0.9 / 1.69 / 0.5 / burger weight 1 lambda 2; the weight entry's GUID resolved to Food_Burger, which both Days already serve) — affects: TicketGenerationConfig.cs, DayJson.cs, DayContentGenerator.cs, DayEditorModel.cs, day_00.json, day_01.json, TicketGenerationConfigCloneForDayGenerationTests (replaces TicketGenerationConfigCloneWithOverridesTests), DayContentGeneratorTests, DayCatalogParserTests, DayEditorModelConverterTests — 2026-08-17

D-007: Delete `BoardDistributionConfig` outright — the ScriptableObject class, its `.asset`, and `BoardDistributionConfigEditor` — and seed a new Day's board-distribution balancing from the highest-numbered existing Day instead. `GuaranteedTicketCountMode` moves to `BoardDistributionSettings.cs` (same namespace, so nothing that uses the enum changed), and the leak-distribution preview that only that custom editor drew moves into `DayEditorSettingsPreviews`, shared with the Day Editor and with `TicketGenerationConfigEditor`'s main-dish preview — rationale: after D-004 took the asset off the runtime path its only remaining consumer was its own inspector preview, i.e. a preview of numbers that no longer drove anything; the user asked for it to go ("SOlara gerek kalmadıysa silsek ya"). Seeding from the previous Day (user's choice among three options, 2026-08-17) keeps the numbers in DATA rather than in code, which the root CLAUDE.md invariant requires and which a coded default would have quietly broken — the coded defaults on `DayEditorBoardDistribution` now only apply to the very first Day of an empty catalog. It also matches how Days are actually authored: the next Day usually starts from the last one's balance. Only the three balancing blocks are copied; ticket sequence, Day Start board, star thresholds and food selection are not, since those are what distinguishes one Day from another and inheriting them would disguise an empty new Day as a finished one. `TicketGenerationConfig` survives this deletion for the reasons in D-006 (it owns `namesDatabase` and the generation knobs) and still seeds a new Day's generation/runtime blocks. NOTE: `SampleScene.unity` still carries a `boardDistributionConfig:` line (line 3003) left from the field removed in D-004; with the asset gone it is a dangling GUID on a property no class declares, which Unity drops the next time the scene is saved. The scene was deliberately not hand-edited: it is the project's only scene and K1, and one line of cosmetic cleanup does not justify editing serialized YAML by hand — affects: BoardDistributionConfig.cs (deleted), BoardDistributionConfig.asset (deleted), BoardDistributionConfigEditor.cs (deleted), BoardDistributionSettings.cs, DayEditorSettingsPreviews.cs (new), DayEditorModel.cs, DayEditorWindow.cs, TicketGenerationConfigEditor.cs, SampleScene.unity (stale reference, left for Unity to drop) — 2026-08-17

D-008: The Day Complete popup's 3-star rating is computed from lives lost that day -- 0 lost = 3 stars, 1 = 2, 2 = 1, 3 or more = 0 -- instead of comparing the day's earned score against per-Day authored thresholds. `startingLives` drops from 5 to 3 so the mapping covers the whole reachable range without a gap, the rule moves out of `DayCompletePopupView` into `DayLifecycleManager.StarCount` (which already owns the failure counter, and where it can be tested), and `star1Threshold`/`star2Threshold`/`star3Threshold` are deleted from Day JSON, `DayDefinition`, `GameManager`, the Day Editor and `DayValidator` along with the two warnings about them -- rationale: requested by the user ("elle ayarlamak yerine..."), and the thresholds were a per-Day balancing chore that nobody had actually done: both shipped Days sat at 0/0/0, which awarded 3 stars for any result at all. Lives-based rating needs no authoring per Day and scales with whatever difficulty a Day happens to have. NOTE this changes a rule CLAUDE.md marked as locked, on the user's decision, and it has two consequences worth keeping visible: (1) stars now measure accuracy alone -- speed and patience multipliers still drive the money earned but no longer affect the rating, so there is no star incentive to play fast; (2) 3 starting lives instead of 5 is a real difficulty increase, chosen deliberately over leaving a 3-and-4-losses gap in the mapping. The 3+ case is only reachable through a paid continue (which refills lives but deliberately leaves the failure counter alone) and floors at 0 stars: the day still completes and still pays out, it just earns nothing to show. The GDD .docx is not updated here -- binary file, left to the user -- affects: DayLifecycleManager.cs, DayCompletePopupView.cs, GameManager.cs, DayJson.cs, DayDefinition.cs, DayCatalogParser.cs, DayValidator.cs, DayEditorModel.cs, GameConfig.asset, day_00.json, day_01.json, DayLifecycleManagerTests, DayValidatorTests, DayEditorModelConverterTests, ExpoTheExplorer/CLAUDE.md — 2026-08-17

D-009: Remove the player XP / Level system from the game entirely — `LevelManager` (XP calculation, level-up loop, commit/discard/revert of progress), `LevelProgressionConfig` + its `.asset` (XP-per-item, the three patience XP multipliers, the `XpToNextLevel` threshold table), `GameState.Xp`/`GameState.Level` with their `XpChanged`/`LevelChanged` event buses and constructor seeds, the `XpBarView` and `LevelView` HUD components together with the whole `LevelUI` GameObject subtree in `SampleScene` (level badge, XP bar background/fill/frame, level count text — 6 GameObjects, 24 YAML blocks), `GameManager`'s XP wiring (the `levelProgressionConfig` serialized field, the profile load in `Awake`, the XP award in `OnTicketDelivered`, the `OnDayCompleted`/`OnDayRetried` handlers and their subscriptions, and the `dayStartProfile` half of `CaptureDayStartSnapshot`), and `ProgressionSystemTests` — rationale: requested directly by the user ("XP sistemine dair her şeyi siler misin, Level sistemi olmayacak artık oyunda"). Three consequences worth keeping visible: (1) **patience is now a one-effect stat** — the XP multiplier was its second effect, so patience type feeds nothing but the ticket's time limit, which sharpens GDD open question 22 rather than closing it; (2) **the game persists nothing at all** — Xp/Level was the only state ever written to disk, so every session now starts from `GameState`'s constructor, Day 0 included; (3) the powerup design's "granted on level-up" earn path is void, and powerups (already deferred) will need a new trigger if that scope reopens. `PlayerProfile`/`PlayerProfileStore` and the `ProgressionSystem` assembly are deliberately KEPT rather than deleted with the rest: they are the save *boundary*, not XP-specific, and two queued tasks land in exactly that file — persisting `CurrentDayIndex` (DaySystem_Roadmap Q4) and the wallet plus a schema version (`economy-plan.md` Adım 2). `PlayerProfile` is therefore an empty `[Serializable]` class today and `PlayerProfileStore` has no caller; `PlayerProfileStoreTests` was re-pointed from payload assertions to file behavior (missing/empty/corrupt/round-trip/directory creation, with fallback identity via `AreSame`) so that coverage survives the empty payload. Two `GameStateTests` cases that happened to sit on Xp/Level were re-pointed at SoftMoney rather than deleted — what they guard is the publish-only-on-change setter pattern shared by every reactive `GameState` field. An existing `player_profile.json` in `persistentDataPath` is simply never read again; no migration was written for it — affects: GameState.cs, GameManager.cs, DayCompletePopupView.cs, PlayerProfile.cs, PlayerProfileStore.cs, LevelManager.cs (deleted), LevelProgressionConfig.cs + .asset (deleted), LevelView.cs (deleted), XpBarView.cs (deleted), SampleScene.unity, ProgressionSystemTests (deleted), PlayerProfileStoreTests, GameStateTests, ExpoTheExplorer/CLAUDE.md, GDD (v1.0), docs/LevelSystem_Roadmap.md (deleted), docs/DaySystem_Roadmap.md, blueprint.md, fingerprint.md, economy-plan.md, day-config-plan.md — 2026-08-18

D-010: SoftMoney and Gems get a single, compiler-enforced writer, a day attempt becomes economically atomic, and the wallet becomes the only persisted state. Mechanically: a new `Wallet` in ProgressionSystem owns every balance change (`EarnSoftMoney`, `TrySpendSoftMoney`, `TrySpendGems`, `CaptureDayStart`, `RevertToDayStart`, `ApplyPersistedBalances`); `GameState`'s two balance setters drop from public to `internal` and a new `Core/AssemblyInfo.cs` names ProgressionSystem (plus `Tests.EditMode`) as the only friend assemblies, so Bootstrap, LivesSystem and UI cannot assign a balance at all -- `State.SoftMoney = x` outside those two is a build error. `Wallet` keeps a day-start snapshot of both currencies and a per-day spend ledger; `RevertToDayStart` applies `max(0, dayStartBalance - spentThisDay)` to both. `PlayerProfile` gains `Version` + `SoftMoney` + `Gems`, `PlayerProfileStore` owns `CurrentVersion = 1`, stamps it on every Save, decides the file location itself (`persistentDataPath/player_profile.json`, keeping System.IO out of Bootstrap) and on Load accepts versions 1..CurrentVersion while refusing 0 and anything newer. `GameManager` loses its own `dayStartSoftMoney` field and `CaptureDayStartSnapshot`, gains a `PlayerProfileStore`, and re-creates the `OnDayRetried` and `OnDayCompleted` handlers plus their subscriptions -- both had been deleted by D-009 along with the XP discard/commit they also performed — rationale: three separate audit findings shared one root cause, that nothing owned the currencies. SoftMoney had four writers (GameManager's payout and its RetryCompletedDay rollback, LivesManager's two paid Continues), violating the root CLAUDE.md single-writer invariant; a failed day KEPT the money it had earned, so deliberately losing a day was a way to farm income; and `RetryCompletedDay` handed back a paid Continue. The atomic-day contract ("a day's economic result is provisional until the day completes: earnings are taken back, spending is never refunded") closes the second and third at once and is the same shape the removed XP system already had. The `max(0, ...)` clamp is the user's decision D1, chosen over the alternative of barring Continue purchases funded by the current day's earnings -- one fewer rule for the player, at the cost that a player who funds a Continue that way can end a failed day poorer than they started it, though never in debt. The version-range check is deliberately NOT the plan's "reject anything != 1": that would delete a real player's v1 wallet the day a v2 field is added, whereas an older file is readable and simply lacks the newer fields; version 0 stays refused because an unversioned file's numbers could mean anything and are indistinguishable from a genuine 0 balance, which is the entire reason the invariant demands a version field. NOTE the ledger and snapshots reset only in `CaptureDayStart` (day start / day advance), never on a retry, so two Continues bought across two failed attempts of one day are both still spent and both measured against that day's single baseline. NOTE also `CurrentDayIndex` is deliberately NOT persisted (DaySystem_Roadmap Q4 is a gameplay decision left to the user); the versioned schema makes adding it later cheap, since an existing file reads the missing field as 0, which is exactly "start at Day 0". Only two places write to disk -- `DayCompleted` and `RetryCompletedDay`, the latter because it must correct a figure a completed day already banked -- so quitting mid-day loses that day's earnings by design — affects: Wallet.cs (new), AssemblyInfo.cs (new), WalletTests.cs (new), GameState.cs, PlayerProfile.cs, PlayerProfileStore.cs, LivesManager.cs, ExpoTheExplorer.Systems.LivesSystem.asmdef (+ProgressionSystem reference, new one-directional arrow LivesSystem -> ProgressionSystem), GameManager.cs, LivesSystemTests.cs, PlayerProfileStoreTests.cs, blueprint.md, fingerprint.md, ExpoTheExplorer/CLAUDE.md — 2026-08-18

D-011: A delivery's payout is the summed base prices of the ticket's food (fixed, always paid in full) plus a tip that is a percentage of that same order value, and the tip percentage steps down through three tiers keyed to the fraction of the ticket's OWN time limit still left. The 3-tier speed bonus (Lightning/Fast/Standard, its three multipliers and its two seconds-per-item thresholds) and the entire patience tip-decay machinery (`PatienceDecayStep`, the three authored curves, `ResolvePatienceDecayCoefficient`) are DELETED, not deferred. Mechanically: `FoodItemConfig` gains an authored `basePrice`; `EconomyCalculator.CalculateTip` becomes `CalculatePayout` returning `DeliveryPayoutResult` (`OrderValue`/`Tier`/`TipRate`/`Tip`/`Total`) with a new `TipTier` enum; `EconomyConfig` drops `baseTipPerItem` and the speed knobs and gains `tipRateFull`/`tipRateWarning`/`tipRateCritical` plus `warningRatio`/`criticalRatio`; the two ratios MOVE out of `TicketCardVisualsConfig` (which keeps only the three bar colours and the purely cosmetic `timerSegmentSeconds`) so that one authored value drives both the timer bar's colour and the tip tier, with `TicketCardsView` reading them through a new `GameManager.EconomyConfig` getter; `DayLifecycleManager` renames its private fields but keeps its public property names so `DayCompletePopupView` needed no change; `EconomyConfigEditor` was first rewritten as a tier preview and then deleted at the user's request — rationale: requested by the user across several turns. Item count no longer decides what a delivery is worth (a burger, fries and a cola are not interchangeable), which is what `baseTipPerItem` implied. The old speed tiers were removed even though the GDD marked them locked, because their thresholds were computed as seconds-per-item and were therefore invisible on screen, while the timer bar with its colour changes is already on the card -- of two mechanics doing the same job, the visible one was kept. Keying the tiers to a RATIO rather than seconds makes them scale per patience type for free: an Impatient 45s ticket drops a tier every 15s, a Patient 150s one every 50s, which is now the concrete mechanism behind "patience affects money only through the time limit it grants". Putting the ratios on `EconomyConfig` was the user's explicit instruction, twice confirmed; the visuals copy was deleted rather than kept alongside so the colour the player sees and the tip they are paid cannot drift apart, and the Day Editor's card preview turned out not to read those ratios at all, so no editor work was needed. Order value is never scaled by anything, so a late delivery loses tip but never the food's price -- which also means the Day Complete popup's "Tips" row can no longer go negative, a bug the old `TotalTip - BaseTip` subtraction produced whenever a patience coefficient below 1 pulled the total under its own base. NOTE this changes balance: with the authored 20/10/5 rates the spread between a perfect and a last-second delivery on a 21-value order is 25 vs 22, so speed is currently a minor income factor. NOTE also the deleted `EconomyConfigEditor` had warned about two authoring mistakes nothing now checks -- ratios authored so the Warning tier is unreachable, and rates ordered so a LATER delivery pays more — affects: FoodItemConfig.cs, EconomyConfig.cs, EconomyCalculator.cs, TicketCardVisualsConfig.cs, TicketCardsView.cs, GameManager.cs, DayLifecycleManager.cs, EconomyConfigEditor.cs (deleted), EconomySystemTests.cs, DayLifecycleManagerTests.cs, DayTicketSlotManagerIntegrationTests.cs, TicketSystemTests.cs, the nine Food_*.asset price fields, blueprint.md (new arrow TicketSystem -> EconomySystem), fingerprint.md, ExpoTheExplorer/CLAUDE.md, docs/Expo_the_Explorer_GDD_1.md (v0.8 → v1.1) — 2026-08-18

D-012: The game gets a second scene, `MainScreen`, and the day scene gets three working exits to it; `CurrentDayIndex` becomes persisted (profile schema v1 → v2), which closes DaySystem_Roadmap Q4. Mechanically: a new `SceneFlow` in Core holds the two scene names and the two Single-mode loads; `MainScreenView` (Scripts/UI) renders the day number and both balances read straight from `PlayerProfileStore.Load()` and sends the player back into the day; `PlayerProfile` gains `CurrentDayIndex` and `PlayerProfileStore.CurrentVersion` goes to 2; `GameManager` applies the persisted index in `Awake` through a new `ResolveStartingDayIndex` clamp (after the catalog is parsed, before the provider is built off `CurrentDay`), writes it in `SaveProfile`, and gains `ReturnToMainScreenFromCompletedDay` + `ReturnToMainScreenAbandoningDay`; `DayCompletePopupView.goBackButton` and `GameOverPopupView.mainMenuButton` lose their `interactable = false` lines and get listeners; `AdvanceToNextDay` now also saves. The scene itself is produced by a construction script, `MainScreenSceneBuilder` — rationale: requested by the user ("bölüm bitince ana ekrana gidebilmeliyim"), and both buttons had been sitting in `SampleScene` since their popups were built, disabled behind a `// TODO: wire once a menu/day-select scene exists`. Scope was confirmed with the user before any code: a navigation shell (not a day-select map, not a hub with upgrades — no meta CONTENT is designed here, only the flow it will hang off), resume-where-you-left-off, and a separate scene rather than an in-scene panel. **Separate Single-mode scenes** matter beyond taste: the day scene is destroyed on exit, so `GameManager` keeps its "Awake builds the whole Day" shape and its ~dozens of hand-wired Inspector references untouched, and no day runtime lives on behind the main screen. With no persistent scene, **the save file is the hand-off between the two scenes**, which is why `MainScreenView` reads the profile directly — it is a pure reader, so `GameManager` remains the single writer of both balances and the day index. Three sub-decisions worth keeping visible: (1) **"Go Back" advances the day.** The day is finished when that button is reachable, so the next thing the main screen should offer is the next Day. It deliberately does NOT call `AdvanceToNextDay`: only the index has to survive, and that method's board/tray/slot reset would be thrown away as the scene unloads while cascading `TicketAssigned` through views that are already tearing down. (2) **Leaving a failed attempt now WRITES to disk** — `Wallet.RevertToDayStart()` then `SaveProfile()` — which changes the older "a failed day never writes" line in this file's D-010 and in CLAUDE.md. It has to: the new button creates a hole that did not exist while there was nowhere to go, since without the write a Continue bought with **Gems** during an attempt the player then abandons would be silently refunded by walking out, making paid Continues free for anyone who ends up leaving. The write can only ever record money already SPENT, never earned, so it cannot bank a failed day's income — the atomic-day contract is extended to "leaving settles the attempt the way a retry does", not reversed. The day index is deliberately not advanced there, so the main screen still offers that same Day. (3) **The last authored Day gets an answer**: "Next Day" used to return false and just hide the popup, leaving the player on a finished day with no UI at all (the code called this "PR-9 decides"); it now exits to the main screen, whose Play replays that last Day since the index cannot advance. A real "all days finished" screen is content, deliberately not invented here. NOTE the scene is built by an Editor construction script rather than committed as YAML: a Canvas+TMP scene hand-written as serialized YAML is exactly the edit D-007 declined to make, it cannot be reviewed, and a scene file cannot put itself first in Build Settings. The script refuses to overwrite an existing `MainScreen.unity`, so restyling it later is safe. It lives under `Scripts/UI/Editor/` rather than `Assets/Editor/` because it references `MainScreenView` in the predefined Assembly-CSharp, which the `ExpoTheExplorer.Editor` asmdef cannot reference at all. NOTE also `GameManager` is structurally untestable from the EditMode suite (predefined assembly vs asmdef test assembly), so `ResolveStartingDayIndex` and both exit paths carry no test; the profile's v2 round-trip and the v1-reads-as-Day-0 migration do — affects: SceneFlow.cs (new), MainScreenView.cs (new), MainScreenSceneBuilder.cs (new), PlayerProfile.cs, PlayerProfileStore.cs, GameManager.cs, DayCompletePopupView.cs, GameOverPopupView.cs, PlayerProfileStoreTests.cs, MainScreen.unity (created by the script, not committed by hand), EditorBuildSettings.asset (reordered by the script), blueprint.md (new MainScreen system + the first real scene inventory + a Scripts/<Area>/Editor/ folder line), fingerprint.md, ExpoTheExplorer/CLAUDE.md — 2026-08-19

D-013: The HUD Canvas becomes a single prefab (`Assets/Prefabs/UI/HudCanvas.prefab`) instanced in BOTH scenes, showing coins, gems and lives in each, and a new `HudWalletSource` on its root becomes the one place that answers "where does this scene's HUD read its numbers from"; `MainScreenView` gives up its own coins/gems labels so the wallet is displayed once rather than twice. Mechanically: `HudWalletSource` (Scripts/UI) exposes `LiveState`/`SoftMoney`/`Gems`/`Lives`/`MaxLives` and resolves per scene — a `GameManager` if one exists (live `GameState`, reactive, unchanged gameplay behaviour) else `PlayerProfileStore.Load()` for the balances and `GameState.DefaultStartingLives` for lives; `SoftMoneyView`, `GemsView` and `LivesView` drop their `[SerializeField] GameManager` for a `walletSource` reference, subscribe only when `LiveState` is non-null, and read every VALUE through the source so one code path serves both modes; `MainScreenView` loses `softMoneyValueText`/`gemsValueText`; `MainScreenSceneBuilder` stops creating the four labels behind them; and `HudCanvasPrefabSetup` provides the migration — rationale: requested by the user ("bu canvas hem ana menüde hem de oyun sahnesinde olsun") right after they fixed the HUD's own layout. The blocker was that all three views treated `gameManager` as mandatory and bailed with a LogError when null, which is every main-screen instance, since that scene has no `GameManager` by design (D-012). Putting the resolution in ONE component rather than a null-check in each view is what stops three copies of the rule from drifting and three separate profile reads per scene load. **How the GameManager reference is bound was the one contested detail**: a prefab ASSET cannot store a scene reference, so it is either resolved at runtime or carried as a per-scene override. This shipped first as `FindAnyObjectByType` for exactly that reason, and was then changed to a `[SerializeField] GameManager` **on the user's explicit instruction** ("ya o aramasın ben serialize field olarak vereyim") so the binding is visible in the Inspector and theirs to control. The trade-off was stated before the change and is real: the field is a prefab override in the day scene, Apply All from that scene drops it, and an empty field is not an error (it is precisely how the main screen works), so the failure is QUIET — the HUD stops following the live wallet and sits on the save file's numbers for the whole day. Two things answer that: `HudWalletSource.Resolve` logs which of the two modes it resolved to once per scene load, and `HudCanvasPrefabSetup` step 2 writes the reference itself for whichever scene it is run on instead of leaving a manual drag. The views' own `walletSource` references are prefab-INTERNAL and need no override in either scene. Resolution is LAZY, on the first view's `Start`, never in an `Awake`: `GameManager` assigns `State` in its own `Awake` and Unity does not order `Awake` across GameObjects, so resolving earlier would reintroduce exactly the null that the views' long-standing Start-not-Awake rule exists to avoid. Three consequences worth keeping visible: (1) **the main screen's lives label is a constant "3/3"** — lives are never persisted and always start full, so there is no live value to show; the user was told this and chose to display it anyway rather than hide the widget, and it reads as "you will start the next day with 3". `HudWalletSource.Lives` is the single line a persistent-lives design would change. (2) Removing `gameManager` from the three views orphans their serialized values in `SampleScene`; Unity discards the property silently and nothing needs rewiring, but reverting the .cs alone would leave those fields empty. (3) `HudWalletSource` carries no test: it is a MonoBehaviour in the predefined `Assembly-CSharp`, which an asmdef test assembly cannot reference — the same wall `GameManager` sits behind — so both branches are covered by reasoning and a Play-mode pass only. NOTE the migration deliberately ships as TWO menu items (`ExpoTheExplorer > HUD > 1/2`) rather than one automated sweep: a single command would have to switch scenes, and switching forces a save or a discard, which destroys the undo stack and commits changes before the user has reviewed them. Each step touches only the open scene, writes nothing to disk but the prefab asset, and leaves the scene dirty so saving stays the user's call. Step 1 saves the prefab FROM the live object, so the HUD rect values the user authored are what ships — this decision re-parents nothing and re-derives no anchors. NOTE also this task recorded, but did not fix, a tooling bug found on its path: `build_unitymap.py`, `check_blueprint.py` and `build_assetmap.py` all resolve `<project_root>/Assets` while this repo nests the Unity project at `ExpoTheExplorer/Assets`, so `unitymap.md` has been stamping `scenes:0 status: OK` over a project with four scenes; `unitymap.md` now carries an explicit UNMAPPED note naming the cause, and the scene structure for this task was read by parsing `SampleScene.unity` directly — affects: HudWalletSource.cs (new), HudCanvasPrefabSetup.cs (new), SoftMoneyView.cs, GemsView.cs, LivesView.cs (its codemap line also went from MISSING-role/sys: ? to sys: LivesSystem), MainScreenView.cs, MainScreenSceneBuilder.cs, GameState.cs (the D-012 comment fix that had been left pending), HudCanvas.prefab + SampleScene.unity + MainScreen.unity (all three produced by the migration steps, not committed by hand), blueprint.md (first real prefab inventory), unitymap.md, ExpoTheExplorer/CLAUDE.md — 2026-08-19

D-014: `Lives` joins the save file (profile schema v2 → v3, with the project's first real migration) and the three HUD views stop knowing that two data sources exist. Mechanically: `PlayerProfile` gains `Lives`; `PlayerProfileStore.CurrentVersion` goes to 3 and gains `UpgradeToCurrent` (pre-v3 files get `Lives = GameState.DefaultStartingLives`) plus `Defaults()` (a brand-new player: zero money, FULL lives); `LivesManager` gains `ApplyPersistedLives`, clamped to 1..MaxLives, mirroring `Wallet.ApplyPersistedBalances`; `GameManager` applies it in `Awake` and writes `Lives` in `SaveProfile`; `HudWalletSource` gains three forwarded `EventBus<int>`s (`SoftMoneyChanged`/`GemsChanged`/`LivesChanged`, the last fed by both of GameState's lives events) and its `Lives` now returns the SAVED count off the day scene instead of a constant; `SoftMoneyView`/`GemsView`/`LivesView` lose their `LiveState` branch, their `liveState` field and their knowledge of modes, becoming one subscription plus one render each — rationale: the user asked for coins/gems/lives to be reachable from anywhere with clean code, and the reason the HUD had become ugly was that these values had TWO homes (GameState in a day, the save file on the menu), which forced a branch into three separate views. A ScriptableObject-as-runtime-home refactor was designed for this and REJECTED by the user in favour of "let the main-menu HUD just read the JSON" — the right call: the menu cannot change a balance, so one render there is sufficient, and 15 files of surgery across two K1 files with no runnable tests was a bad trade. What shipped instead is the same idea at a tenth of the cost: one home per question, but the mode choice confined to `HudWalletSource` rather than replicated in the views. Three consequences worth keeping visible: (1) **the main screen's lives label stopped being decorative** — persisting Lives is what made it real, which is also why this task and the lives decision arrived together; (2) **`ReturnToMainScreenAbandoningDay` now refills lives before saving**, and that line is load-bearing: it is only reachable from the Game Over popup with Lives at 0, so persisting that figure would have written a save file the player cannot play out of — dead before the first ticket, permanently. Abandoning resets the attempt exactly as the free Retry does; (3) `RetryCompletedDay`'s `SaveProfile` moved from first to LAST for the same reason — it refills lives, and saving before that wrote the pre-refill count, handing back a half-empty bar to anyone who quit mid-redo. NOTE persisting Lives opened no design question, contrary to my first read: lives ALREADY carried across days (`AdvanceToNextDay` deliberately skips `LivesManager`; only a failed-day retry or a paid Continue refills), so this extends an existing rule across sessions rather than inventing one. NOTE also what was deliberately NOT fixed: the `GameManager` reference on the HUD prefab instance is still a prefab OVERRIDE, so "Apply All" from the day scene still drops it and the symptom is still quiet — only an asset reference removes that, and that was the rejected refactor. `HudWalletSource`'s mode log stays as the tell — affects: PlayerProfile.cs, PlayerProfileStore.cs, LivesManager.cs, GameManager.cs, HudWalletSource.cs, SoftMoneyView.cs, GemsView.cs, LivesView.cs, PlayerProfileStoreTests.cs, LivesSystemTests.cs, ExpoTheExplorer/CLAUDE.md, fingerprint.md — 2026-08-19

D-015: The meta side of the game — the expo grounds the player decorates between days — gets a designed shape and a new `MetaSystem`, built MULTI-LOCATION from the first line. This is the project's first DESIGN-ONLY decision record: no code ships with it, because ExpoTheExplorer/CLAUDE.md's "Meta content … is not designed yet — build the design first, then hang it here" makes the design itself the deliverable, and `.claude/meta-plan.md` is its 11-step execution plan. Six mechanisms, each with the alternative it beat. (1) **One slot = one Image with two sprites.** A catalog entry carries `unownedSprite` and `ownedSprite`, either of which may be empty, so the whole view layer is `image.sprite = active ? owned : unowned; image.enabled = image.sprite != null`. This collapses what looked like four product types into one code path: a cosmetic decor has only an owned sprite; the ruined `StartBuilding` that hides the finished stand has only an UNOWNED one and therefore vanishes when bought; an area expansion (`Square`) is a decor that additionally unlocks slots; a Day-unlocked prop differs only in what decides `active`. The rejected alternative was a `kind` enum with a switch per type — four branches to keep in sync for a two-field data change. (2) **A Day-unlocked prop's openness is DERIVED, never persisted**: `active ⟺ some Day in 0..CurrentDayIndex serves the trigger content`. Because `CurrentDayIndex` names the day about to be played, that scan opens the prop exactly BEFORE the day that needs it, which is what the user asked for ("sonraki bölümde sos vardır, o bölümden önce açılması gerekir") and it needs no `+1`. Persisting the unlocked set was rejected outright: it is a second authority that starts lying the moment a Day is re-authored. The trigger is `DayUnlockTrigger` with four optional fields OR-ed together — `foods` (FoodItemConfig refs), `foodCategories` (FoodCategory), `modifications` (ModificationConfig refs) and a plain `minDayIndex` — because the three props Meta1 actually has each look at a DIFFERENT concept: the fryer at one food (`Food_Fries`), the drinks fridge at a whole category (six beverages, so a seventh added later cannot silently fail to trigger it), and the sauces stand at a set of modifications (`Mod_ExtraKetchup`/`Mayonnaise`/`Mustard` — note `FoodCategory` has no `Sauce` member; sauces are modifications, which is why one trigger shape could not serve all three). Everything is an ASSET REFERENCE, not a string id, so the root invariant on content-never-in-code holds and renames cannot break it. (3) **The wallet keeps its single writer on the main screen, via a `GameSession` extracted from `GameManager` rather than a second construction path.** A shop that spends money needs a `Wallet`, and the main screen has neither `GameManager` nor `GameState` by design (D-012/D-013). `GameManager.Awake`'s first five steps — `GameState`, `Wallet` + `ApplyPersistedBalances`, `LivesManager` + `ApplyPersistedLives`, the Day catalog parse, and the `ResolveStartingDayIndex` clamp — are exactly what the meta screen needs and exactly one contiguous block, so they become `GameSession` and BOTH screens construct it. Writing a separate `MetaScreenController` that re-implemented those five was designed and REJECTED: four load-bearing ordering rules live in that block (the wallet must exist before `ApplyPersistedBalances` because GameState's setters are `internal`; `LivesManager` before anything that can cost a life; the catalog before the clamp, which needs its `Count`; and `ApplyPersistedBalances` re-takes the day-start snapshot), and duplicating them across two files is the exact failure the single-writer invariant exists to prevent. Moving `GameManager` itself to the main screen — the user's question — was rejected for a concrete reason: `Awake` ends in `ApplyDayStartBoardPreSeed()` + `TicketSlotManager.FillEmptySlots()` and `Update()` runs `TicketSlotManager.Tick`, so the day would RUN behind the shop, timing tickets out and costing lives; a `DontDestroyOnLoad` variant fails differently, since every view in `SampleScene` holds a `[SerializeField] GameManager` that a carried-in object cannot satisfy, which would reverse the user's explicit D-013 instruction. `GameSession` being plain C# in an asmdef also closes a coverage gap D-012 recorded: `ResolveStartingDayIndex`'s clamp becomes EditMode-testable for the first time. Alongside it, `player_profile.json` gets ONE writer class (`ProfileSaver`) that both `GameManager` and the meta screen call with a whole profile, so the file cannot be written with a stale day index or lives; and `HudWalletSource` binds to a `GameSession` provider instead of a concrete `GameManager`, which makes the main-screen HUD live after a purchase and follows D-013/D-014's own rule to extend the source rather than branch inside a view. (4) **No third scene**: the grounds render inside `MainScreen`, which the GDD already designated ("hang it here") and which leaves D-012's two-scene shape untouched — a third scene would add a second save-file hand-off. (5) **Slot positions are normalized (0..1) to the background rect**, not pixels: `Main.png` is 853×1844 (0.463, a phone portrait ratio) but real devices run 0.42–0.56, so pixel offsets drift under aspect-fill while normalized ones do not — and it lets locations with differently-sized backgrounds share one data shape. 19 slots are too many to hand-author, so an Editor placement tool writes positions back into the asset, the same authoring-tool pattern DayEditor and MainScreenSceneBuilder already use. (6) **Location is a first-class entity and decor belongs to it** (`MetaCatalog.locations` → `MetaLocation{ id, displayName, backgroundSprite, unlockAtDayIndex, items[] }`), because the user stated a second restaurant is coming and that which props are cosmetic vs. Day-unlocked "metadan metaya değişebilir". Consequences: the persistence key is `$"{location.Id}.{item.Id}"` COMPUTED by the resolver, so assets keep clean local ids while uniqueness is structural and two locations can carry a same-named decor; a location's unlock is derived from `CurrentDayIndex >= unlockAtDayIndex`, the same shape as (2), so no save field; the wallet is SHARED across locations while decor is not; which location is being viewed is UI state, not saved (the screen opens on the newest unlocked one); and a location is self-contained — a `requiresAreaId` pointing into another location is a validation error. Doing this now costs a wrapper level in the catalog and a location parameter on the resolver; doing it later would have meant changing the schema of already-saved ownership keys, i.e. a save migration. Deliberately NOT built: any gameplay effect (decor is purely cosmetic — no income multiplier, no lives), Gems anywhere on the meta side (they stay Continue-only), free placement (fixed slots, the user's choice), a day-select map, per-location economies, and decor transferable between locations. Two design consequences worth keeping visible: a Day-unlocked prop's trigger reads GLOBAL content rather than a location's own timeline, so a location unlocking at Day 12 opens with its fridge already present if drinks entered at Day 3 (correct — the new restaurant is built with everything already on the menu); and the "old location stays visitable and decorable" rule is what removes the sunk-cost trap that a hard move would have created, where every coin spent before the move stops being visible and the rational player therefore ignores the meta system entirely — rationale: requested by the user ("şimdi meta oyunu implement etmeye başlıycaz"), with the six locked answers gathered before any design (fixed slots; SoftMoney only and price is the only gate; Day-CONTENT-driven required props; Square as an area expansion; locations accumulate rather than replace; a Day number unlocks a location). The art set is already organized per location (`Assets/Art/Meta1/`, 20 PNGs), which is what surfaced (6) before any code existed — affects: THIS TASK wrote only maps — blueprint.md (new `MetaSystem` system line + its two arrows, `MainScreen` gains a third arrow, and the folder layout gains the `Scripts/Bootstrap/` and `Scripts/Systems/<System>/` lines it had never carried), decisions.md, index.md, .claude/meta-plan.md. PLANNED code surface, none of it written yet: MetaCatalog.cs + MetaCatalog.asset (new), MetaResolver.cs + MetaPurchase.cs + MetaSystem asmdef (new), MetaSystemTests.cs (new), GameSession.cs + GameSessionTests.cs (new), ProfileSaver.cs (new), MetaBoardView.cs + MetaShopView.cs (new), a Meta placement Editor tool (new), PlayerProfile.cs + PlayerProfileStore.cs (schema v3 → v4), GameManager.cs, HudWalletSource.cs, MainScreen.unity, and the Meta1 sprite import settings. `shards.json` is deliberately unchanged: `Scripts/Systems/MetaSystem/**` matches no pattern and lands in the `core` catch-all, exactly like every other `Scripts/Systems/*` folder on disk — 2026-08-20

D-016: A meta prop has exactly ONE sprite and is simply absent until it is active; D-015's `unownedSprite`/`ownedSprite` pair drops to a single `sprite`. Mechanically: `MetaItemDefinition` loses `unownedSprite`, renames `ownedSprite` to `sprite` (matching `FoodItemConfig.sprite`) and drops one constructor parameter; `MetaCatalogValidator`'s "has neither sprite" error becomes "has no sprite"; `MetaCatalogValidatorTests` loses `Validate_ItemWithOnlyUnownedSprite_IsFine` (the case being removed) and its realistic-Meta1 fixture swaps the ruined-building entry for an ordinary bought `HotdogStand` — rationale: requested by the user ("unowned owned muhabbetini kaldıralım sadece alınca gözüksün, spriteları da ona göre düzenleyeyim ben"). The second sprite existed for exactly one prop: the ruin that hides the finished stand baked into `Main.png`, so that BUYING it made it vanish. Expressing that in the ART instead — the background carries the ruin, and the stand becomes a normal prop drawn over it — produces the same thing on screen with one less mechanism, and the art is where "what does the ground look like before anything is built" belongs anyway. The whole view layer is now `image.enabled = active`. NOTE what this genuinely gives up: the system can no longer express "buying this removes it" AT ALL. That was the only field capable of it, so a future prop needing that behaviour needs a new mechanism rather than a value — worth knowing before someone reaches for it and finds nothing. NOTE also this changes an art obligation the user accepted: `Main.png` currently ships the FINISHED stand, so it has to be re-exported with the ruin instead, and a stand sprite added; until that happens the catalog simply has no entry for either, since Adım 2 shipped the schema and not the content. Cheap to make now — three files written this same session, no resolver, no UI and no asset reading them yet, so there was no caller to break; the same edit after Adım 7 would have touched the view and the placement tool as well — affects: MetaCatalog.cs, MetaCatalogValidator.cs, MetaCatalogValidatorTests.cs, codemap-core.md, meta-plan.md (K1 rewritten), Art/Meta1/Main.png + a stand sprite (the user's art work, not done here) — 2026-08-20

D-017: A Day-unlocked meta prop appears at ONE authored day index and nothing else; D-015's content-driven trigger is deleted. Mechanically: the whole `DayUnlockTrigger` class goes (its `foods`/`foodCategories`/`modifications`/`minDayIndex` fields, `IsEmpty`, `HasMissingReferences`), replaced by a single `MetaItemDefinition.unlockAtDayIndex` -- the same shape and meaning `MetaLocation.unlockAtDayIndex` already had, so props and locations now unlock by exactly one rule; `MetaCatalogValidator` loses the empty-trigger error and the deleted-reference warning, keeping only the "priced but not for sale" warning; `MetaCatalogValidatorTests` drops five trigger tests for three; and `MetaCatalog.cs` stops referencing `FoodItemConfig`, `FoodCategory` and `ModificationConfig` altogether — rationale: requested by the user ("sadece hangi günden önce açılacağını seçebilelim, böyle çok gereksiz karmaşa var ve gereksiz arama tarama var"). They are right about the ratio: D-015 bought "the fridge appears exactly when drinks join the menu, and un-appears if you re-author them away" at the price of four OR-ed trigger fields, a `DayContentFacts` scan over every Day up to the player's, and a `MetaSystem -> DaySystem` dependency. **Three things vanish with it, not one:** `meta-plan.md`'s entire Adım 6 (the content-scan step), the blueprint arrow to DaySystem, and open questions M3/M4/M4b (which modifications count as sauce, whether the fridge keys on a category or six ids, whether the fryer keys on `Food_Fries` or all Sides) -- all three were only questions BECAUSE the unlock read content. What is genuinely given up: the two numbers can now drift. Move sauces from Day 5 to Day 9 and the sauces stand still appears at Day 5 until someone edits the catalog too, and no validator can catch it -- `Data` has no access to the Day catalog by construction, so the check is unwriteable rather than merely unwritten. One authored number per prop, visible in the Inspector, is the mitigation. NOTE the index is a CATALOG POSITION, 0-based, not the day number the player sees (which is index + 1, per `MainScreenView`): "waiting before their fifth day" is authored as 4, and the tooltip says so, because this is exactly the confusion `fingerprint.md` already records between `CurrentDayIndex` and a Day file's own `dayIndex`. NOTE also `MetaUnlockKind` SURVIVES: with two int fields (`price`, `unlockAtDayIndex`) the discriminator is what says which one governs, and inferring it ("price > 0 means it is for sale") would make a mis-authored 0 silently change a prop's kind. Free to do now -- the three files were written the same session, the `.asset` the user created is still empty (`locations: []`), and no resolver, UI or Editor tool reads any of it yet, so there was no caller to break and no authored data to migrate — affects: MetaCatalog.cs, MetaCatalogValidator.cs, MetaCatalogValidatorTests.cs, blueprint.md (MetaSystem loses its DaySystem arrow), meta-plan.md (K2 rewritten, Adım 6 deleted, M3/M4/M4b removed), codemap-core.md, index.md — 2026-08-20

D-018: The meta catalog stays a ScriptableObject and gets a custom `MetaEditorWindow` to author it; it does NOT move to JSON like Day content. Mechanically: two new files under `Assets/Editor/` -- `MetaEditorWindow` (an `OdinMenuEditorWindow`: one tree node per location, a toolbar carrying the auto-discovered catalog field and the validation counts) and `MetaLocationLayoutGUI` (the layout canvas: the location's background drawn at its real aspect with every prop DRAGGABLE on top) -- plus one comment line on `DayEditorSpriteGUI`, whose `DrawTexCoords` is reused as a third consumer. No asmdef changes: `ExpoTheExplorer.Editor` already references `ExpoTheExplorer.Data` — rationale: the user asked for Day-Editor-style authoring instead of clicking through nested Inspector lists ("day editör gibi editörde dediklerini yapsak ya SO yerine"), and then, once the storage question was separated from the UI question, chose to keep the SO ("SO ile tutalım o zaman editörde düzenleyelim"). **Separating those two questions was the substance of this decision**: a custom EditorWindow does not require JSON — the deleted `EconomyConfigEditor` drew a ScriptableObject the same way — so the authoring gain was available without touching storage at all, which also meant D-015's schema and D-016/D-017's narrowings survived untouched instead of being rewritten hours after they shipped. JSON was considered seriously and rejected for a concrete reason, recorded here because the question will be asked again: Day JSON references foods by string id BECAUSE foods live elsewhere and are shared across every Day, and `FoodCatalog.GetById` is that bridge — the JSON does not avoid a ScriptableObject, it depends on one. Meta props are not shared; each belongs to exactly one location, so the id indirection would buy nothing while adding a mismatch class (a prop id in JSON with no entry in the sprite catalog) that the SO cannot have, since there the sprite sits beside the price. Sprites cannot live in JSON at all, which the project already recorded when `TicketGenerationConfig` survived deletion for owning `namesDatabase`. Three implementation choices worth keeping visible: (1) **the window edits through `SerializedObject`, not by assigning to the plain `MetaLocation` objects.** They live inside the SO's list, so a direct write changes memory and leaves the asset unmarked -- the edit would survive until the next domain reload and then silently vanish. It also buys Unity's undo and Ctrl+S rather than a hand-rolled Save, which is the opposite division of labour from `DayEditorWindow` (mutable model, JSON on Save) and deliberate: there is no second copy of meta data to keep in step. ONE `ApplyModifiedProperties` per repaint is what makes a drag undo as a single gesture instead of one step per mouse-move event. (2) **Odin draws only the chrome and the tree; every field goes through Unity's own property drawer**, so the page cannot drift from whatever the schema does next. (3) **"Depth from Y" is a button, not automatic** -- deriving sort order on every drag would wipe any hand-set exception the moment its prop is nudged, and exceptions are the whole reason that field is authored rather than computed. NOTE the canvas flips every vertical conversion (GUI space Y grows downward, a normalized UI position grows upward) and derives prop size as sprite-pixels x the background's fit scale, mirroring the runtime, because D-015 deliberately left no per-prop scale field -- so the preview stays a preview rather than a second opinion. NOTE also `sys: MetaSystem` rather than a new `MetaEditor` system, which is an asymmetry with blueprint's `DayEditor`: two files do not earn a system, and it can be split out if the window grows. NOTE finally this ABSORBS `meta-plan.md` Adım 7's separate "Editor placement tool" line item -- there is no second tool to build — affects: MetaEditorWindow.cs (new), MetaLocationLayoutGUI.cs (new), DayEditorSpriteGUI.cs (comment: third consumer), codemap-editor.md, meta-plan.md (Adım 2.5 added, Adım 7's tool item absorbed) — 2026-08-20

D-019: The meta rules ship as a dependency-free decision core: a new `ExpoTheExplorer.Systems.MetaSystem` assembly referencing `ExpoTheExplorer.Data` and NO system, and D-015's planned `MetaSystem -> ProgressionSystem` arrow is dropped rather than realised. Mechanically: `MetaResolver` answers what is on the grounds (`IsOwned`, `IsLocationUnlocked`, `UnlockedLocations`, `DefaultLocationIndex`, `IsActive`, `IsAreaSatisfied`, `ActiveItems`, each with a list overload beside the asset one) and `MetaPurchase` answers whether a prop can be bought (`MetaPurchaseVerdict{Ok,NotForSale,AlreadyOwned,LocationLocked,AreaLocked,NotEnoughMoney}`, `Evaluate`, `CanBuy`, `ShopItems`); `MetaSystemTests` covers 25 cases; the EditMode test asmdef gains the new reference — rationale: `meta-plan.md` Adım 3, written before any UI so the rules are pinned by tests rather than discovered by playing. **The arrow came off because writing the code showed it was never needed**: the rules take the owned-key set and the SoftMoney balance as PARAMETERS, so the money and the owned-item list keep the single writers they already have in ProgressionSystem (`Wallet`, the profile writer) and the screen's composition root joins the two — the same shape `GameManager` uses to join systems. What that buys is not tidiness but reach: zero system dependencies and zero MonoBehaviour means every rule is exercisable from a test, which is exactly what `GameManager` cannot offer (D-012 recorded that gap). The cost is a rule for callers, stated in the class comment: nothing is looked up, so a caller passing a stale day index gets a confidently wrong answer rather than an exception. Four decisions inside the rules are worth keeping visible. (1) **A verdict, not a bool.** The shop has to say six different things and "you cannot afford it" versus "buy the square first" are different screens; neither is recoverable from true/false. The check ORDER is load-bearing and runs most-fundamental-first — `NotForSale` before anything about money, because reporting `NotEnoughMoney` for a prop that was never purchasable would send a player off to earn money for nothing; and `AreaLocked` before `NotEnoughMoney`, because an area-gated prop's price is irrelevant until the area is owned. (2) **Area gating applies to being ACTIVE, not only to being purchasable.** Owning a prop whose area is unowned should be unreachable, but an author can add `requiresAreaId` to a prop players already have, and then their table would hang over ungravelled grass; tying visibility to the area keeps the picture coherent whatever the catalog does next. An area id naming nothing, or naming a non-area item, counts as UNSATISFIED so the content bug the validator reports stays visible instead of hiding behind a working prop. (3) **`ActiveItems` uses insertion sort, not `List.Sort`**, because `List.Sort` is not stable: two props sharing a `SortOrder` would swap places between calls and the visible symptom is overlapping props flickering. A test pins it. (4) **`ShopItems` deliberately KEEPS unaffordable and area-gated props** and drops only owned and Day-unlocked ones — seeing what is still to come, and what it costs, is most of what a shop is for. NOTE `DefaultLocationIndex` walks back from the END of the catalog rather than picking the highest unlock day: catalog order is authored order and two locations may legitimately share a day; -1 means nothing is unlocked, which `MetaCatalogValidator` already reports as a content error rather than something this class should paper over. NOTE also the tests seed the catalog's private list by REFLECTION rather than through `SerializedObject` the way `EconomySystemTests` does, because the difference is what is being set — floats write in one line each, a list of plain serializable objects would need every field of every nested object walked by name, which is more fragile than the reflection it replaces and would need updating on every schema change; a public setter would exist purely for tests. NOTE finally that this step is verified by COMPILATION only (the assembly and the tests against it, both 0 errors, via temporary csproj files since Unity had not yet generated one for the new asmdef): the Editor holds the project lock, so the suite itself has not been run — affects: ExpoTheExplorer.Systems.MetaSystem.asmdef (new), MetaResolver.cs (new), MetaPurchase.cs (new), MetaSystemTests.cs (new), ExpoTheExplorer.Tests.EditMode.asmdef (+MetaSystem reference), blueprint.md (MetaSystem loses its last dependency arrow), codemap-core.md (3 new lines, plus the Tests.EditMode.asmdef line finished from its auto-added state), meta-plan.md — 2026-08-20

D-020: The props a player has bought join the save file: `PlayerProfile.OwnedMetaItemIds` (a `List<string>` of qualified `"<location>.<item>"` keys) and `PlayerProfileStore.CurrentVersion` goes 3 → 4. Mechanically: the new field plus a `Normalize` step in `PlayerProfileStore` that guarantees a non-null list on every load, four new cases in `PlayerProfileStoreTests`, and the schema line in `ExpoTheExplorer/CLAUDE.md` — rationale: `meta-plan.md` Adım 4; without it a purchase evaporates when the game closes, which makes the whole meta side pointless. **The interesting part is that v4 breaks the pattern the previous three versions established, in the opposite direction to v3.** v1→v3 all reasoned about whether absent-reads-as-ZERO was safe: v2's `CurrentDayIndex` was fine (0 means the first Day), v3's `Lives` was not (0 lives is a dead player), and that asymmetry is why `UpgradeToCurrent` exists. This is the first REFERENCE-typed field, so the question is not what value an absent field reads as but whether it arrives at all — and the safe default is an empty list, which no zero can express. That produced the one decision worth keeping visible: **`Normalize` sits OUTSIDE `UpgradeToCurrent`, and that placement is load-bearing.** `UpgradeToCurrent` returns early once the file is already current, which is correct for a version-specific fix but wrong here, because a CURRENT-version file can still carry an explicit null — hand-edited, half-written, or produced by a future field someone forgets to initialize — and every one of those would reach a caller as a `NullReferenceException` rather than as an empty inventory. It also wraps the fallback returns, since a caller-supplied fallback is an ordinary object someone else built and its list can be null just as easily as a parsed one's. A test uses a literal v4 payload with `"OwnedMetaItemIds":null` precisely to pin that gap, because the obvious v3-payload test passes either way. Three smaller calls: a `List<string>` rather than a `HashSet`, because `JsonUtility` round-trips a HashSet as `"{}"` with no error at all (the same trap `PlayerProfile`'s own comment already documents about auto-properties) and callers wanting set semantics convert on read — `MetaResolver` already takes an `ISet<string>`; the key FORMAT is not decided here, `MetaCatalog.OwnershipKey` stays its single authority and this class only carries strings, because a joined key written down in two places is exactly what one authority prevents; and the field holds PURCHASES only — whether a location is unlocked and whether a Day-unlocked prop is on screen stay derived from the day index (D-015/D-017) and must never be written here, since a second copy starts lying the moment the catalog is re-authored. NOTE the single WRITER of this set is deliberately absent: as of this step nothing writes it at all (`MetaSystem` takes the set as a parameter, D-019), so there is no dual-writer risk yet, and squeezing the writer in here would have made Adım 5's `ProfileSaver` work half-done. NOTE also the set needs less machinery than `Wallet` when it does arrive: meta purchases happen on the main screen, outside a day attempt, so there is no `RevertToDayStart` counterpart — the set only ever grows. NOTE finally the real risk this step carried was PLAYER DATA, not code: a botched version bump makes an existing v3 file unreadable and costs a real player their wallet, day and lives; the range check in `IsVersionReadable` (1..4, so v3 still loads), `UpgradeToCurrent` touching only fields a version ADDED, and an explicit "a v3 file keeps every field and owns nothing" test are the three things holding that shut — affects: PlayerProfile.cs, PlayerProfileStore.cs, PlayerProfileStoreTests.cs, ExpoTheExplorer/CLAUDE.md, fingerprint.md, codemap-core.md, meta-plan.md — 2026-08-20

D-021: `GameManager.Awake`'s session half becomes a shared `GameSession` in its own assembly, and `GameSession.Save` becomes the single composer of what reaches the save file -- which cancels the separate `ProfileSaver` class D-015 had planned. Mechanically: a new `ExpoTheExplorer.Session` asmdef (referencing Core, Data, ProgressionSystem, LivesSystem, DaySystem) holding `GameSession`, which builds `GameState`, `Wallet` + `ApplyPersistedBalances`, `LivesManager` + `ApplyPersistedLives`, the parsed Day catalog and the clamped day index, and owns the live `OwnedMetaItemIds` set; `GameManager` constructs it in one line, forwards `State` and `LivesManager` as properties under their ORIGINAL names, drops its `wallet`/`profileStore`/`dayCatalog` fields and its own `ResolveStartingDayIndex`, and its `SaveProfile()` becomes `Session.Save()`; `GameSessionTests` covers 12 cases; the EditMode test asmdef gains the reference — rationale: `meta-plan.md` Adım 5, split into 5a (this) and 5b (the HUD binding and the main screen's root) because the two halves touch different K1 surfaces and a Play-mode failure in one should not be ambiguous with the other. **The extraction exists to stop a duplicate, not to tidy up.** The main screen needs the same five construction steps, and those steps carry four ordering rules -- the wallet before `ApplyPersistedBalances` (GameState's setters are `internal` to ProgressionSystem, so nothing else can seed them), `LivesManager` before anything that can cost a life, the catalog parse before the clamp (which reads `Count`), and `ApplyPersistedBalances` re-taking the day-start snapshot so a session's first retry reverts to the RESTORED balance rather than to zero. A second controller re-implementing those four is precisely the failure the single-writer invariant exists to prevent. **Moving `GameManager` itself was rejected for a concrete reason** (asked directly by the user earlier): `Awake` ends in `ApplyDayStartBoardPreSeed()` + `TicketSlotManager.FillEmptySlots()` and `Update()` runs `TicketSlotManager.Tick`, so the day would RUN behind the shop -- timing tickets out and costing lives -- and a `DontDestroyOnLoad` variant fails differently, since every view in the day scene holds a `[SerializeField] GameManager` that a carried-in object cannot satisfy. Three decisions worth keeping visible. (1) **`ProfileSaver` is cancelled.** `GameSession` already loads the profile and holds the wallet, lives, day index and owned set, so it is what must compose the write; a third class would need a COPY of every field, and a field forgotten in that copy does not throw -- it silently writes a zero over a real player's money. `PlayerProfileStore` stays the file boundary, ProgressionSystem's job per blueprint. This also closes the writer gap D-020 deliberately left open for the owned-meta set. (2) **The folder was forced, not chosen.** `Scripts/Core/` is impossible -- ProgressionSystem, LivesSystem and DaySystem all reference Core already, so a Core-resident GameSession would be a cycle -- and `Scripts/Bootstrap/` carries no asmdef on purpose, since adding one there would pull `GameManager` in and put every hand-wired view reference at risk. Hence `Scripts/Session/`. (3) **`State` and `LivesManager` keep their names as forwarding properties**, which is the single reason roughly a dozen views in `SampleScene` needed no rewiring; renaming them would have turned a refactor into a scene migration. NOTE the coverage this buys: `ResolveStartingDayIndex`'s clamp has guarded a real crash since D-012 -- an index past the last authored Day leaves `CurrentDay` null and throws on the first ticket -- and had NO test, because it lived in a MonoBehaviour in a predefined assembly. It now has four. NOTE also `GameManager` itself remains structurally untestable, so what holds this refactor is `GameSessionTests` plus a Play-mode pass, and the Play-mode pass has NOT been done: the Editor holds the project lock, so verification here is compilation only (Session, Assembly-CSharp and the test assembly, all 0 errors, via temporary csproj files since Unity had not yet generated one for the new asmdef) — affects: ExpoTheExplorer.Session.asmdef (new), GameSession.cs (new), GameManager.cs, GameSessionTests.cs (new), ExpoTheExplorer.Tests.EditMode.asmdef, blueprint.md (two folder-layout lines), codemap-core.md, meta-plan.md — 2026-08-20

D-022: The main screen gets a `GameSession` of its own, and the shared HUD stops depending on the concrete `GameManager`: a new abstract `SessionHost` becomes the thing a scene offers, `GameManager` and a new `MainScreenRoot` both derive from it, and `HudWalletSource` binds to that instead. Mechanically: `SessionHost` (abstract MonoBehaviour in the Session assembly, exposing `GameSession Session`); `GameManager : SessionHost` with `Session` as an `override`; `MainScreenRoot` (Scripts/UI, builds the session in `Awake` from three serialized configs and logs an error if they are missing); `HudWalletSource`'s field becomes `[FormerlySerializedAs("gameManager")] SessionHost sessionHost` with a private `LiveState` helper replacing four repeated null-checks; and `MainScreenSessionSetup`, a menu step that adds the root to the open MainScreen, fills its configs and points the HUD at it — rationale: `meta-plan.md` Adım 5b. A shop cannot spend money on a screen that has no `Wallet`, and the menu HUD reading the save file ONCE (D-013) would show a stale coin count the moment a purchase happened. Two decisions were forced by Unity rather than chosen. (1) **An abstract MonoBehaviour, not the interface the plan called for**: `[SerializeField]` cannot serialize an interface, so an Inspector field that is both draggable and type-checked has to name a concrete type. The alternatives were a `MonoBehaviour` field plus a runtime cast — which makes any component draggable and fails at play time instead of in the Inspector — or a runtime lookup, which the user explicitly ruled out for this binding in D-013. `SessionHost` lives in the Session assembly because it must SEE `GameSession`, and because Assembly-CSharp can derive from an asmdef type while the reverse is impossible. (2) **`[FormerlySerializedAs("gameManager")]` is load-bearing, not tidiness.** `SampleScene`'s HUD prefab instance carries an override under the old field name; renaming without the attribute drops it, and the result is exactly the quiet failure `HudWalletSource` already documents — the HUD stops following the live wallet for a whole day with nothing different on screen. The attribute carries the value across, and it still type-checks because `GameManager` IS a `SessionHost` now. Changing `GameManager`'s base class disturbs nothing serialized: a component's type identity is unchanged by its base. NOTE what deliberately did NOT change: the three HUD views. D-013/D-014's rule is that the mode branch belongs in the SOURCE, never in a view, and they still only read `walletSource`. `MainScreenView` also stayed as it was, so the menu now loads the profile TWICE — once for the session, once for the day number — which is accepted rather than overlooked: converting it means a second scene binding, and the only thing it reads cannot change from a purchase, so a stale read is harmless. Adım 7 rebuilds that screen anyway. NOTE the mode log line's MEANING changed and the comment says so: a missing host used to be expected on the menu, and now means a lost reference in either scene. NOTE finally this step ships with NO new tests, honestly rather than by omission: all three new types are MonoBehaviours and `HudWalletSource` sits in the predefined Assembly-CSharp, which no asmdef test assembly can reference (the wall D-012/D-013 recorded). Verification is compilation only — Session, Assembly-CSharp, Assembly-CSharp-Editor and the test assembly, all 0 errors — and the Play-mode pass is the user's: the first thing to check is `HudWalletSource`'s log line reading "live GameState" in BOTH scenes — affects: SessionHost.cs (new), MainScreenRoot.cs (new), MainScreenSessionSetup.cs (new), GameManager.cs, HudWalletSource.cs, codemap-core.md, codemap-ui.md, codemap-editor.md, meta-plan.md — 2026-08-20

D-023: The meta grounds get a runtime view: `MetaGroundsView` draws one location's background and the props the player has unlocked on it, scrolling vertically when the art is taller than the screen, with a bar to walk between unlocked locations; `MetaGroundsSetup` is a menu step that builds the container and wires it. Mechanically: the background fits by WIDTH with its height derived from the sprite's aspect, sits in a `ScrollRect` with `movementType: Clamped`, and every prop is instantiated at run time as a child of the background with its `normalizedPosition` written straight onto `anchorMin`/`anchorMax`; `MetaResolver.ActiveItems` decides what exists and already returns draw order, so sibling index carries depth; `Refresh()` is public for Adım 8's purchase to call — rationale: `meta-plan.md` Adım 7, with the fit strategy answering open question M10 the way the user chose ("kaydırılabilir olsun"): fitting by height would letterbox every device, while fitting by width leaves vertical scrolling as the only variable, and that variable is ZERO on the phones the art was drawn for (0.463 art against a 0.5625 authored canvas means up to ~18% overflow on a short screen, which is scrolled rather than cropped — cropping would put some props permanently out of reach). Four decisions worth keeping visible. (1) **Props are instantiated, never placed in the scene.** Nineteen hand-placed objects would be a second copy of the catalog, free to disagree with it silently; the catalog stays the single authority (D-015) and the editor step builds only the container. (2) **`Canvas.ForceUpdateCanvases()` before measuring, and it is load-bearing.** Prop sizes derive from the background's WIDTH, and on the frame `Start` runs a stretched rect's width is still zero because Unity has not run its layout pass yet — measuring then sizes every prop to nothing, and the symptom is an empty-looking screen rather than an error. (3) **Props are CHILDREN of the background with the normalized position on their anchors.** This is what K5's "normalize to the background rect, not the screen" was for: they ride the art through every resize and every scroll with no layout code, which is also why scrolling needed no per-prop handling at all. (4) **Stepping is between UNLOCKED locations only** — letting the player walk into a locked one would duplicate in the UI the refusal `MetaPurchase.LocationLocked` already owns; a locked NEXT location shows as an "Unlocks on Day N" hint instead, with the +1 the player-facing day number needs. NOTE the view decides nothing: prices, area gates and unlock days were settled and tested in D-019, and this class turns three answers into RectTransforms. Props are `raycastTarget: false` so they cannot swallow drags meant for the scroll view. NOTE the editor step REFUSES to rebuild an existing hierarchy and only refreshes references, the same promise `MainScreenSceneBuilder` makes by refusing to overwrite a scene, and it finds the canvas via `MainScreenView` rather than taking the first `Canvas` — the HUD is its own Canvas prefab and building inside it would land on the wrong sorting layer. The root is the FIRST sibling because sibling order is the only thing deciding draw order in a Canvas and the grounds are a backdrop. NOTE this ships with NO tests, for the same structural reason as D-022: both types are MonoBehaviours and one is in the predefined Assembly-CSharp. Verification is compilation only — and this time the new files were explicitly added to the temporary csproj and confirmed present in its Compile list before the build was trusted, which is the gap that let a `CS0121` and a `CS0104` reach the user earlier in this session — affects: MetaGroundsView.cs (new), MetaGroundsSetup.cs (new), codemap-ui.md, codemap-editor.md, meta-plan.md (Adım 7, M10 closed) — 2026-08-20

D-024: The shop ships, and with it the meta system runs end to end for the first time: `MetaShopView` lists a location's remaining offers, previews where a selected prop would stand, and commits a purchase; `MetaGroundsView` gains the ghost preview and a `ViewedLocationChanged` event; `MetaShopSetup` builds the strip. Mechanically: offers come from `MetaPurchase.ShopItems`, each row's state from `MetaPurchase.Evaluate`, and the commit runs `Evaluate -> Wallet.TrySpendSoftMoney -> OwnedMetaItemIds.Add -> GameSession.Save -> grounds.Refresh + shop Refresh` — rationale: `meta-plan.md` Adım 8. **The commit order is the substance of this decision.** `TrySpendSoftMoney` is asked even though `Evaluate` already answered Ok, because the wallet is the compiler-enforced single writer of the balance (D-010) and its own answer is the authority; if the two ever disagree, a player must not end up owning something they did not pay for, and a refusal aborts before anything is recorded. **The flow is atomic by construction rather than by effort**, which is worth stating because it fell out of earlier decisions instead of being designed here: the money and the owned set both live on the same `GameSession` and `Save` writes them together, so a crash before `Save` loses both (the money comes back) and reaching `Save` writes both — "paid but did not receive" is not a state this code can produce. The ownership key comes only from `MetaCatalog.OwnershipKey`; a hand-joined string would be a second spelling of it, and that failure is the nasty kind — the purchase is written but never recognised again, so the player pays and keeps seeing the item on sale. Three further decisions worth keeping visible. (1) **Only three of the six verdicts reach the screen**, and that is what the enum bought: `ShopItems` drops owned and never-for-sale rows and a locked location empties the list, leaving buyable / too expensive / area-locked — and an `AreaLocked` row shows "Needs Square" rather than a dead button, which is the whole reason that verdict exists as its own case rather than a bool false. (2) **The row selects, the Buy button inside it commits.** Previewing where a prop goes must not cost money, and one tap doing both would make that impossible. (3) **The HUD is deliberately not touched** — it has followed the live wallet since D-022, so the coin count drops on its own; adding a second update path here is how two numbers start disagreeing. NOTE the GHOST preview is not a return of what D-016 deleted: that was a CATALOG field, authored per prop and part of the data model, while this is a translucent copy the view draws while a row is selected and destroys when it is not. It is held apart from the spawned props and redrawn AFTER them, so buying some other prop does not make it vanish for no visible reason, and it is skipped when the selected item does not belong to the viewed location. NOTE `ViewedLocationChanged` is a plain event rather than a reference to the shop, keeping the dependency one-directional — the grounds must not know what else is on screen. NOTE no tests, structurally: both types are MonoBehaviours in the predefined Assembly-CSharp; the purchase RULES have 25 tests from D-019 and this step calls them. Verification is compilation only, with the four touched files confirmed present in the csproj Compile list first — the gap that let a CS0121 and a CS0104 reach the user earlier in this session. NOTE finally that prices are still unauthored (open question M1): the shop works, but `MetaCatalogValidator` reports a purchasable prop priced at 0 as an error, so a usable catalog needs Adım 9 — affects: MetaShopView.cs (new), MetaShopSetup.cs (new), MetaGroundsView.cs (ghost preview + ViewedLocation + ViewedLocationChanged), codemap-ui.md, codemap-editor.md, meta-plan.md — 2026-08-20

D-025: The main screen becomes one button, and the day number moves onto it: `MainScreenView` loses `dayValueText` and writes "Continue Day X" into the Play button's own caption; `MetaGroundsView`'s location bar (`locationLabel`, `previousButton`, `nextButton`, `lockedHintLabel`) becomes OPTIONAL; `MainScreenSceneBuilder` stops creating the title and day captions — rationale: the user stripped the screen to Play + HUD and both components were still treating the deleted objects as REQUIRED, so every scene load logged two errors. The design change is the user's ("day value kısmını da butona continue day x şeklinde kullanalım") and it is the better shape anyway: the button now says what it will DO rather than a number beside it saying where the player is. Three decisions worth keeping visible. (1) **The button's label is found inside the button, not wired as a field.** The caption is already a child of the Play button in every scene that has one, so a serialized reference would be a third thing to forget on a screen whose missing references had just cost two errors. This is not the runtime lookup D-013 argued against: that was about finding an object elsewhere in the SCENE, while this searches the button's own children, and a button with no label degrades to a warning rather than a broken screen. (2) **The location bar is optional rather than removed.** With one location there is nothing to switch between, so requiring it forces scene clutter for a feature with no content yet — and deleting it is a legitimate authoring choice, not a mistake. It becomes meaningful when a second restaurant exists, and its absence is then visible on screen (no name, no arrows) rather than silent. `MetaGroundsSetup` still builds it, for whoever starts a fresh scene. Only four references stay required — `sessionHost`, `catalog`, `scroll`, `background` — because without them the screen cannot draw anything at all. (3) **`MainScreenView` still reads the PROFILE rather than the session this scene has carried since D-022**, which means the profile is loaded twice on this screen. Accepted rather than overlooked: taking it from the session needs another serialized reference, which is exactly the failure mode being cleaned up here, and the only value read cannot change while the player sits on a menu. NOTE the third log line the user reported was not an error at all — `HudWalletSource ... live GameState (session host: MainScreenRoot)` confirms D-022 resolved correctly, which is worth recording because that line is the only visible difference between a live HUD and one silently reading the save file. NOTE also trimming the scene builder changes nothing today, since it refuses to overwrite an existing scene; it exists so the builder and the scene cannot drift permanently apart — affects: MainScreenView.cs, MetaGroundsView.cs, MainScreenSceneBuilder.cs, codemap-ui.md, codemap-editor.md, meta-plan.md — 2026-08-20

D-026: A new player opens the game with an AUTHORED coin balance (`GameConfig.StartingSoftMoney`, set to 1000 in `GameConfig.asset`), and the main screen gets an optional two-tap **Start Over** button that deletes the save file and reloads the scene — rationale: both halves are the user's ask ("main menuye bir tuş eklesek de en baştan başlatsa oyuna, bir de oyunun en başında 1000 coinimiz olsun"). Four decisions worth keeping visible. (1) **The number lives on GameConfig, not in code and not on EconomyConfig.** Content data is never embedded in code (root CLAUDE.md), and `GameConfig` is the only config asset BOTH scene roots already hold — `EconomyConfig` is the per-ticket tip math consumed by `EconomyCalculator` and is unreachable from the main screen, so a field there would mean a new serialized reference that can sit quietly unwired, which this screen has already paid for twice. The asset was edited in the same commit: a new serialized field reads 0 out of an existing `.asset`, the exact trap that produced the stale `EconomyConfig.asset` fixed in 50be70c. (2) **The grant is a property of the NEW-PLAYER PROFILE, not of the wallet.** `PlayerProfileStore.NewPlayer(grant)` is the one place that answers "what does a player who has never played hold" (it already answered it for lives), and `GameSession` passes it as the `Load` FALLBACK. So the money reaches the player through the existing `Wallet.ApplyPersistedBalances` call — no second writer of a balance, and the grant lands inside the day-start snapshot, so a new player's first retry cannot revert it away. A saved file always wins, so nobody is topped up on a later launch; an unreadable file (corrupt, or from a newer build) is treated as a new player deliberately, because this build cannot know what those numbers meant and dropping such a player to zero coins is the worse failure. (3) **Reset DELETES the file and reloads the scene; it does not patch anything.** Writing a fresh profile over the old one would require `PlayerProfileStore` to know every field — the payload knowledge it is built without — and a field forgotten there leaves a scrap of the old player behind. Reloading MainScreen rebuilds the session, the HUD, the grounds and the shop through their existing single writers, where editing the live `GameState` would need a second writer for the balance, the day index and the owned props. The view writes no value at all: it commands the file's owner and navigates. (4) **Two taps, and the button is optional.** One mis-tap must not be able to erase a real player's progress, and the cheapest guard that cannot be missed is the button contradicting itself for a few seconds (caption authored as `resetConfirmLabel`, restored by a coroutine so it cannot keep saying "tap again" after that tap stopped working) — a confirmation popup would be a second piece of UI to build and wire for one button. `resetButton` is optional so a MainScreen built before this existed still runs, which also means the existing scene needs `MainScreenResetButtonSetup` (menu step, touches only the open scene, never saves) — `MainScreenSceneBuilder` refuses to overwrite an existing scene, and hand-editing `MainScreen.unity` YAML is the review-proof edit D-007 declined — affects: GameConfig.cs, GameConfig.asset, PlayerProfileStore.cs, GameSession.cs, MainScreenView.cs, MainScreenSceneBuilder.cs, MainScreenResetButtonSetup.cs, GameSessionTests.cs, PlayerProfileStoreTests.cs, MainScreen.unity (Editor step, user-run) — 2026-08-20

D-027: The shop D-024 shipped is REVERTED — `MetaShopView` and `MetaShopSetup` are deleted, and the pieces `MetaGroundsView` had grown only for them (the ghost preview, `ViewedLocation`, `ViewedLocationChanged`, and `Refresh` being public) go with them. D-024 is superseded, not deleted: a decision record describing code that no longer exists is the decision-log version of a map that lies — rationale: requested by the user, who anticipated it when asking for the pre-shop commit ("belki geri alır farklı bi dükkan yaparız ama şimdi commit at da buraya geri dönebilelim") and then asked for it. No fault was found in the shop; a different design is wanted. **What was NOT reverted is the substance of this decision.** (1) `git reset` to the `meta-system-before-shop` branch was rejected outright: that commit predates the shop, so returning to it would also have deleted D-025 (the "Continue Day X" button, the optional location bar) and D-026 (the reset-progress button, the 1000-coin opening balance, `GameConfig.StartingSoftMoney`, `PlayerProfileStore.NewPlayer`) — the second of which is another session's work and has nothing to do with this request. The revert is therefore surgical. (2) `MetaResolver` and `MetaPurchase` STAY, with their 25 tests. Prices, area gates and unlock days are rules about the catalog, not about a screen; whatever shop replaces this one needs the same six verdicts. What was deleted is UI and the commit flow, not the model. (3) `PlayerProfile.OwnedMetaItemIds` STAYS at schema v4. Reverting it would mean v4 → v3, which makes every existing save unreadable — the cost of undoing a UI decision cannot be player data. The set is therefore back to the state D-020 recorded on purpose: it exists, and nothing writes it, because the only writer was the shop. NOTE the removed accessors were removed rather than kept "in case the next shop wants them": they had exactly one caller each, and an entry point with no caller reads as one that is used somewhere. Re-adding them is three lines. `Refresh()` went private for the same reason. NOTE the scene needed a manual step, and only because the user had already run `Build Meta Shop` and saved: deleting `MetaShopView` leaves `--MetaShop--` as a missing-script shell, which the user removes in one click. The scene YAML was NOT hand-edited to do it — that object is a GameObject plus RectTransform, Image, ScrollRect, the component and three children, i.e. a dozen interlinked fileIDs, which is exactly the edit D-007 declined; one click is cheaper and safer. NOTE the intermediate state is harmless: the shell does nothing but show a warning in the Inspector — affects: MetaShopView.cs (deleted), MetaShopSetup.cs (deleted), MetaGroundsView.cs, codemap-ui.md, codemap-editor.md, meta-plan.md, MainScreen.unity (the user deletes one object) — 2026-08-21

D-028: Runtime code never resolves a scene reference — by name, by type, or by walking children. Every one is a `[SerializeField]` the author wires. Mechanically: `MainScreenView` gains `playLabel` and `resetLabel` fields and loses both `GetComponentInChildren<TMP_Text>()` calls; `MetaGroundsSetup` finds its root by COMPONENT instead of by the name `--MetaGrounds--`, and its now-unused `FindChild`/`RefreshExisting` helpers are deleted; `MainScreenResetButtonSetup` drops `FindButtonByName` entirely and its `Create` hands the caption back so both fields are wired from the object it just built; `MainScreenSceneBuilder` wires `playLabel` too — rationale: stated as a project rule by the user ("bundan sonra isimle arama falan yok sen serialize field yapıyorsun ben de sahnede sürüklüyorum"), generalising the instruction D-013 already recorded for the HUD's GameManager field. **The concrete failure that prompted it is worth keeping**: `MetaGroundsSetup` looked its root up by name, the user renamed the object, and the step therefore concluded the grounds did not exist — it would have built a SECOND hierarchy while its own log claimed it had left the existing one alone. A search is a guess, and it fails SILENTLY the moment the hierarchy changes. NOTE an argument for a lookup was made and overruled, and the record matters more than the outcome: D-025 defended finding the Play caption with `GetComponentInChildren` on the grounds that a third serialized field is a third thing to forget. The user rejected it. The objection is sound — a forgotten field is visible in the Inspector, whereas a search that starts matching the wrong object is visible nowhere. **Scope was decided by the user rather than assumed**, after an audit of every lookup in the project turned up three categories: (1) two runtime lookups, both converted; (2) editor tooling, where NAME lookups go but type lookups and `AssetDatabase.FindAssets` stay — they run at author time under the user's eye and the step logs what it wired, and removing them would break the Day Editor's config bar; (3) three sites the rule does not reach at all. **That third category is the part to remember**: `WorldTrayView`, `BoardItemDragHandler` and `TicketCardsView` call `GetComponentsInChildren` over children the code itself spawned at run time. Those objects do not exist at author time and vary in number, so no field can point at them. They are not exceptions to the rule — they are outside its subject, which is guessing which EXISTING scene object is meant, not reading the parts of something you just built. NOTE an unwired caption is a WARNING, not an error: a button with no label still works, it just says nothing, and killing the screen over a cosmetic gap is disproportionate; the buttons themselves stay required. NOTE also the reset step now treats its serialized field as the ONLY record of whether the button exists, so an author who clears the field and keeps the object gets a duplicate — accepted, because the alternative is the name search that caused this — affects: MainScreenView.cs, MetaGroundsSetup.cs, MainScreenSceneBuilder.cs, MainScreenResetButtonSetup.cs, codemap-ui.md, codemap-editor.md — 2026-08-21

D-029: The second shop begins as a SKELETON: `MetaShopView` owns a market button and a panel that opens and closes, and nothing else; `MetaShopSetup` builds that container in the scene. — rationale: `meta-shop-plan.md` Ş1, whose whole purpose is that each step is verifiable BY EYE, because this work is visual and I cannot see the screen. **Deviation from my own plan, deliberate:** the plan put the ghost API (`ShowGhost`/`ClearGhost`, public `Refresh`) in Ş1 too. It moved to Ş3. D-027 removed those exact members with the reason *an entry point with no caller reads as one that is used somewhere* — re-adding them in Ş1, where nothing calls them, would carry dead API for two steps and re-earn the criticism I had just recorded. Ş3 both adds and uses them. **What this step does NOT do is most of it:** it reads no data, writes no data, and touches no rule. `MetaResolver`/`MetaPurchase` and their 25 tests are untouched, the profile schema is untouched, and the panel is EMPTY — the rows come from the catalog at run time in Ş2 because their number depends on the location and on what is already owned, and the catalog is their single authority (D-015). Three things are load-bearing rather than cosmetic. (1) The panel is toggled from CODE, not by a Button-to-SetActive wire in the Inspector, even though the wire would do today's job: this class will own the shop's state machine (KAPALI/AÇIK/ÖNİZLEME) and the ghost's lifetime hangs off these same transitions, so `SetOpen` is the single place visibility changes. The first shop's ghost could leak precisely because its visibility had more than one owner (D-024). (2) Depth is sibling order inside ONE Canvas, and no second Canvas is created: the bug that ate the HUD this session came from two Canvases both sitting at sorting order 0. The grounds are the FIRST sibling, the shop root the LAST, and the market button is created AFTER the panel so it stays on top of it — not styling: the panel is a bottom sheet covering the bottom-right corner, so a button drawn underneath could not be tapped to close it and the shop would be a one-way door. (3) The panel background is `raycastTarget = true`, because the grounds behind it are a `ScrollRect` and a panel that does not eat raycasts lets a drag on the panel scroll the map — which reads as the panel being broken, not as a layering mistake. `MetaShopSetup`'s existing-shop path deliberately refreshes NOTHING: its references point at objects it created, so re-pointing them would require a search, which D-028 forbids. It names any empty field instead and leaves the fix to the author's drag or a delete-and-re-run. Both of `MetaShopView`'s references are REQUIRED (unlike the grounds' optional location bar, D-025) — a shop with no button cannot be opened and a button with no panel does nothing, so neither has a degraded mode worth shipping. Shop art is a PLACEHOLDER (coloured square reading SHOP) because the project has no shop art at all (MS5); a borrowed icon from another system would read as final. **Not done, and known:** the top-gap bug in `MetaGroundsSetup` (`viewport.offsetMax` insets the TOP edge while the location bar is anchored to the BOTTOM, so the inset was always on the wrong edge) is still in the code. The user fixed their own scene by hand and declined the code fix ("ya üst boşluğu düzelttim ben zaten"), so a scene built from scratch — the second location, when it comes — will show the gap again.

D-030: The shop's list is built by cloning an authored, INACTIVE row template kept in the scene -- `MetaShopRowView` binds it, `MetaShopView` clones one per offer, `MetaShopSetup` builds the template and the ScrollRect around it. — rationale: `meta-shop-plan.md` Ş2. The project already had two ways to make repeated UI: `TicketCardsView` instantiates a prefab ASSET, `TicketCardView` clones an authored template living in its own hierarchy (`modificationRowTemplate`, `timerDividerTemplate`) with a bind-only component on it (`ModificationSlotView`). A shop row is structurally the second case — a repeating icon+text row whose count only the catalog and the player's purchases know — so it follows it. Concretely: styling the one template restyles every row, and there is no second asset to keep in sync with the scene. **Which location is shopped comes from `MetaGroundsView.ViewedLocation`, which D-027 had deleted.** That deletion's reason was *no caller*, and Ş2 supplies the caller, so the objection lapses rather than being overridden. `ViewedLocationChanged` did NOT come back: the list is rebuilt every time the panel opens, and the only moment the player can walk to another location is while it is closed, so the event would be a publisher written before its subscriber. The payoff is that `MetaShopView` has **no `MetaCatalog` field**: the catalog reference stays in one component, where two of them free to point at two catalogs would break D-015's single authority with nothing on screen looking wrong. Four things are load-bearing. (1) The BUY listener is wired ONCE in `MetaShopRowView.Awake` and `Bind` only swaps the stored callback — adding it inside `Bind` would stack silently the first time a row is rebound, and a BUY that fires twice is the worst bug this screen could have. (2) `rowsParent` is forced ACTIVE and the template forced INACTIVE in `Start`: hiding the container to get the sample row out of sight is what silently hides every real row, a trap `TicketCardView` had already written down. (3) `Instantiate` copies the template's inactive state, so every clone needs an explicit `SetActive(true)` — the same two lines `TicketCardView` needs. (4) Scroll height is computed by `VerticalLayoutGroup` + `ContentSizeFitter` rather than authored: sixteen offers overflow a bottom sheet and the count changes with every purchase. All six of `MetaShopView`'s references are REQUIRED, unlike the grounds' optional location bar (D-025): listing the first location's props when `grounds` is unwired would present a WRONG list as though it were right the day a second restaurant exists, which is precisely the class of silent failure D-028 was written about. **MS1 and MS2 are settled by this step.** MS1 (price in the row) is yes, my recommendation, because otherwise comparing prices means opening every item's popup one at a time. MS2 stopped being a question: `MetaPurchase.ShopItems` already keeps the unaffordable and the area-gated in the list and drops only the owned, with the reason in its own comment — Ş2 draws that answer as-is. **Ş2 deliberately does NOT show WHY a row cannot be bought** (red price, disabled BUY, "Önce Meydan"): that is Ş5, and doing it here early would mean two places deciding what a verdict looks like. Every row looks the same and every BUY logs. Rows are destroyed and rebuilt rather than pooled — sixteen rows built on a tap do not need a pool, and a pool adds a path where a row survives with stale contents; `MetaGroundsView.Clear` made the same call.

D-031: The shop's preview is real: a row's BUY puts a translucent ghost of the prop where it would stand and a confirm popup over it, and `MetaShopView` owns the whole state machine that governs both. — rationale: `meta-shop-plan.md` Ş3, whose stated job is not drawing the popup but **closing every path by which the ghost could leak**. Two fields hold the machine: `isOpen` (KAPALI/AÇIK) and `pendingItem` (ÖNİZLEME when non-null). It stores the ITEM rather than a bool so the popup's labels and Ş4's commit read the same value and cannot come to disagree about which prop is being bought. Every transition passes through `ApplyVisibility`; every exit from a preview passes through `ClosePreview` — cancel, tapping outside, tapping a second row, the market button, closing the shop, and a location change on the grounds side. That single door is the decision: the first shop's ghost could survive the panel that summoned it (D-024) because more than one thing decided when it went away. **The list and the popup are never both on screen, and that is machine behaviour rather than styling.** The panel is a bottom sheet covering most of the map, and the ghost stands ON the map — so previewing with the list up would hide the very thing being previewed, and the feature would read as broken rather than as hidden. It also matches how the user described the flow ("ilk hayalet şeklinde yapının hayalet halini göreceğiz ve ardından üstünde yine bir popup olacak"). The consequence is that panel visibility is `isOpen && !previewing` — `activeSelf` is neither field on its own — and that rows are NOT destroyed while previewing: cancelling brings the list back rather than rebuilding it. Tapping a second row REPLACES the preview, because that is a change of mind and not an error. `ShowGhost` draws through `CreateProp`, the same path the owned props take, and `PropScale` became one method instead of two call sites so that it cannot drift: a preview that lies about where the prop lands is worse than no preview. Only the alpha differs, and it is a serialized field because it is cosmetic. The ghost is deliberately NOT in `spawnedProps` — sharing that list would give it the behaviour wanted today while tying two different lifetimes together, and in Ş4 "was the ghost destroyed, or the prop" becomes a question worth being able to answer. `Refresh` clears it, because a ghost belongs to one location's layout. **Two things this step deliberately does NOT do.** `Refresh()` stays PRIVATE: Ş3 buys nothing, so nothing changes what `IsActive` answers, and the same argument that deferred the ghost API out of Ş1 (D-029) applies to `Refresh` here — it goes public in Ş4, where a purchase gives it a caller. And the popup's BUY changes no state, it only logs: closing would make Ş3's own by-eye verification ambiguous ("did it buy, or just close?") and Ş4 would then have to write the closing a second time. On the editor side the popup's SIBLING POSITION carries the design — after the panel so it draws over the list, before the market button so that button still draws over it, because the machine can go from ÖNİZLEME straight to KAPALI and a backdrop covering the market button would strand the player in a preview with a ghost and no way out. Backdrop and box share ONE root the view toggles rather than being two objects switched by hand, since two switches is one that gets left on. The backdrop is a real `Button`, not a bare `Image` — the tap has to be HANDLED, not merely blocked (MS6) — with `Transition.None`, because a flashing dismiss area reads as a button the player was supposed to aim at. The box is `raycastTarget = true` or taps meant for it fall through and cancel the popup being read. MS3 and MS6 are settled by this step as recommended: cancelling removes the ghost, and tapping outside cancels.

D-032: The confirm popup is placed ON the previewed prop, and the map zooms toward it — by an amount derived from the prop's own size rather than authored per prop. — rationale: the user's two requests ("popup itemin üstünde çıkmalı" and "mümkünse zoom yapılsın iteme doğru") plus their own observation about the limit: *"trashbine zoom yapılır da fencelere yapılamaz büyüklüklerinden dolayı"*. **That limit is implemented as arithmetic, not as a special case.** The zoom is the one that would make the prop fill `previewFill` of the viewport, clamped to `[1, previewMaxZoom]`. A trash bin is a small fraction of the map, so the ratio is large and it zooms to the ceiling. A fence as wide as the grounds needs a ratio BELOW one to fit, so the clamp holds it at 1× and the map does not move at all. The user's rule therefore falls out of the formula, and a prop of some middling size added tomorrow gets a sensible number without anyone authoring one — which a per-prop flag or a hand-kept list of "zoomable" items would not have given. Three implementation choices carry weight. (1) **The ScrollRect is DISABLED while previewing.** Not a workaround: the player is answering a question, not exploring, so panning should be off. It also keeps the ScrollRect's own bounds arithmetic out of a fight with a scaled content rect, and horizontal scrolling is off on this map, so a zoomed-in view would otherwise be unreachable sideways. (2) **Framing is a world-space delta** — where the ghost is versus where it should be — never anchor arithmetic. The background is width-stretched with a top pivot, and hand-rolled maths would keep working right up until one of those conventions changed, then focus the wrong spot silently. (3) `Canvas.ForceUpdateCanvases()` after the scale change, because the ghost's world corners are otherwise read at the previous scale and the framing lands off by exactly the zoom factor — the same measure-before-layout trap already recorded for `Refresh`. The popup box gained a **bottom-centre pivot** so the view can place it in one assignment ("your lower edge sits just above the ghost") instead of doing half-height arithmetic; the pivot is the only reason the box is no longer screen-centred, and since its children anchor to the box's unchanged rect, nothing inside it moved. It **flips below the prop** when the prop sits near the top of the screen — better than covering the thing it describes — and is clamped away from the screen edges. Positions are computed in the parent RectTransform's local space rather than world units, because the box's size is measured there and mixing the two would drift by whatever the canvas scaler is doing on the device. The prop is framed BELOW the viewport centre (`previewFocusHeight`, default 0.38) precisely so there is room above it; centred, the box would flip underneath every time and the request would not be met. **No new entry point.** Focusing happens inside `ShowGhost` and restoring inside `ClearGhost`, so the six exits D-031 funnelled through one door now restore the zoom too — there is no seventh path where a zoomed map could get stuck. `RestoreFocus` is guarded by a flag rather than by comparing values, because a preview that happened to zoom by 1× still turned scrolling off and must still turn it back on. `previewFill`, `previewMaxZoom`, `previewFocusHeight` and the popup's gap are all serialized: they are look, and look is quicker to settle in the Inspector than to argue about in a comment. Zoom is instant — easing it is Ş6's business, and adding it now would make it harder to see by eye whether the framing is right.

D-033: The preview's camera move is animated with DOTween instead of snapping, and the destination is MEASURED rather than derived. — rationale: the user asked for it directly ("ışınlanmak yerine tween ile gitse ya zooma"); D-032 had deferred easing to Ş6, and that deferral is simply overtaken. DOTween because it is already this project's idiom — four UI files use the same `DOKill()` / `DOAnchorPos(...).SetEase(...)` shape — and duration plus ease are serialized, because the feel of a screen is not something to settle in a comment. **The interesting part is how the destination is obtained.** D-032 framed the map by a world-space delta (where the ghost is versus where it should be) and explicitly rejected pivot/anchor arithmetic, because the background is width-stretched with a top pivot and hand-rolled maths keeps working right up until one of those conventions changes, then focuses the wrong spot silently. But that delta can only be read once the scale is applied — which is exactly what a tween must not do. Rather than give up the robust version, the map is now put at its destination for an instant, the end scale, the end position AND the ghost's settled edges are measured there, and it is snapped back before anything renders. Nothing is computed; everything is observed. That measurement is also why **`MetaShopView` needed no change at all**: `TryGetGhostBounds` now reports the SETTLED edges, so the shop places the popup once, at the position the prop is travelling to, and the map slides in underneath it. The two alternatives — repositioning the popup every frame, or delaying it until the tween finished — would both have tied the shop to the map's animation length, i.e. created a timing dependency between two components that currently have none. **The tween exposed a latent bug, now fixed.** `ShowGhost` used to call `ClearGhost`, so walking from one row to the next restored the old framing first. While the move was instant that was invisible; with a tween it means the "before" framing gets re-captured from a position halfway through the return tween, and cancelling would then send the map somewhere it had never been. So the two are split: `ShowGhost` calls `DestroyGhostObject` (no restore) and only a real exit calls `ClearGhost`; and the pre-focus framing is captured only when not already focused. Row-hopping now goes straight to the new frame, and cancel always returns to the true starting frame however many rows were browsed. One ordering detail is load-bearing: scrolling is handed back in the return tween's `OnComplete`, never at its start. The `ScrollRect` is `Clamped` and pulls the content toward its bounds every `LateUpdate`, so re-enabling it mid-tween would have both of them dragging the same value. `OnComplete` does not fire on a `DOKill`, so `FocusGhost` disables the flag itself as well — an interrupted return therefore leaves a coherent state from either side rather than depending on the tween finishing. `DOKill` before every tween and in `OnDisable`, since a tween outliving the transform it drives is DOTween's classic null and both meta screens are torn down by a scene load. The popup itself is still not animated: the request was about the zoom, and a popup entrance is a line in Ş6.

D-034: The meta shop can spend money. `MetaShopView.OnConfirmBuyClicked` runs `MetaPurchase.Evaluate` → `Wallet.TrySpendSoftMoney` → `OwnedMetaItemIds.Add` → `GameSession.Save` → `ClosePreview` → `MetaGroundsView.Refresh` → rebuild rows. — rationale: `meta-shop-plan.md` Ş4. **This step adds no rule and no writer.** `MetaPurchase` decides whether a buy is allowed (six verdicts, 25 tests, D-019), `Wallet` decides whether the balance carries it and stays the single writer of the balance, `GameSession.Save` decides what reaches disk, and the profile schema already carried `OwnedMetaItemIds` from D-020. What is new here is only the composition. **Atomic by construction, not by care taken in this method:** the balance and the owned set live on the same `GameSession`, and `Save()` writes both together, so "the money went but the item did not arrive" is not a state this code can express. That is what makes the literal ordering above safe to read. Two apparent redundancies are deliberate. `Evaluate` runs a SECOND time here even though the row was drawn from it, because the panel stays open across purchases (MS4) and the balance the row was drawn against goes stale; the cost is one function call. And the wallet is asked even after `Evaluate` approves, because `Wallet` is the single writer of the balance and its answer is the authority — one is the decision, the other is the transaction, and only the transaction can refuse on the real number. `MetaGroundsView.Refresh()` is **public again, and only now**. It was public for the first shop, went back to private when that shop was reverted (D-027) on the grounds that an entry point with no caller reads as one that is used somewhere, and it stayed private through Ş1 (D-029) and Ş3 (D-031) on the same argument, since neither of those steps bought anything. A purchase changes what `MetaResolver.IsActive` answers while nothing else would notice, so the caller this method needed came into existence exactly at this step — which is the argument being honoured rather than waived. **`HudWalletSource` was NOT touched, and that is the part worth recording.** A main-screen HUD reading the save file once would sit on a stale balance after a purchase, and the root CLAUDE.md forbids the obvious patch ("Do not add a second null-check-and-fall-back to any view; extend the source instead"). No extension was needed: D-022 had already widened its `sessionHost` field from `GameManager` to `SessionHost` and the main screen now supplies a session, so `LiveState` is non-null there and the source is subscribed to `GameState.SoftMoneyChanged`. The HUD is therefore already live and the coin count falls on its own. The one thing that can still make it look otherwise is an empty `Session Host` on the HUD instance, which drops the source into save-file mode — it logs which mode it picked on every scene load for exactly this reason. A non-`Ok` verdict logs and leaves the popup OPEN: saying WHY on screen is Ş5's job, and doing it here as well would put the appearance of a verdict in two places. **No new tests, and that is not a gap.** Every decision is in `MetaPurchase` and covered; what remains here is composition inside a MonoBehaviour, which EditMode cannot exercise. Moving the ordering somewhere testable would mean `MetaSystem` depending on the session, and `MetaSystem` referencing only `Data` is precisely what keeps the rules testable in the first place. A failed `Save()` leaves the session correct in memory and the file one purchase behind, with `PlayerProfileStore` logging why; there is no partial write to recover from because the profile is one file written whole.

D-035: The shop hides what the player cannot reach, dims what they cannot afford, and lists the rest affordable-first then cheapest-first. `MetaPurchase.ShopItems` gained a `softMoney` parameter and now does both the filtering and the ordering. — rationale: the user's four changes to Ş5, given together: an area-locked prop should not appear at all until it unlocks, a prop they cannot afford should look darker, buyable things should be at the top, and the order should be by ascending price. **This reverses half of the plan's MS2, which was my recommendation, and the reversal is theirs.** I had argued for showing area-gated props because "seeing what is still to come is most of what a shop is for". The user kept that for unaffordable props and rejected it for area-locked ones. Implemented as asked, with one cost named rather than argued: nothing on screen now tells the player that buying the Square opens more props, so the discoverability that MS2 was protecting is gone for that case. That is a "coming soon" affordance for a later step (and it needs M7 answered — which props the Square gates), not a reason to keep showing rows the user does not want. **Both changes went into `ShopItems`, not the view.** That function's documented contract already was "everything the shop should list for a location", and it is that contract which changed; putting the filter in a MonoBehaviour would move a listing rule out of the tested layer. Ordering went in with it, which is why the signature gained `softMoney` — "buyable at the top" is a function of the balance, and splitting filter from order would answer "what does the shop show, and in what order" in two places. **The sort is an insertion sort, deliberately not `List.Sort`.** `List.Sort` is unstable, so two props at the same price would be free to swap places between one open of the shop and the next — a defect that presents as "it sometimes reorders itself", which is the hardest kind to be told about. Ties therefore keep authored catalog order, and `SortsBefore` answers false on equal keys precisely to preserve that. `MetaResolver.ActiveItems` made the same call for the same reason (its comment once claimed `List.Sort` was stable and was corrected), and both orderings are now pinned by tests. On the view side the dim is a `CanvasGroup` **alpha** and never `interactable`: over the panel's dark background less alpha reads as darker, which is the user's wording, and one knob fades icon, name, price and button together. The row's BUY stays tappable on purpose — it opens the preview, and seeing where a prop would stand and what it costs is what makes it worth saving for; disabling the row would turn it into dead furniture. What now refuses **visibly** is the popup's BUY, which is `interactable` only when the money is there. Until this step that button was live and did nothing on a short balance: it called `Evaluate`, got `NotEnoughMoney`, logged, and left the screen unchanged — a dead button in exactly the sense the plan warned against. The commit path still re-checks everything (D-034); this only stops the player asking a question whose answer is already on screen. The view neither re-sorts nor re-filters, and reads affordability as price-vs-balance rather than calling `Evaluate` again — everything `Evaluate` could still refuse on has already been filtered out, so money is the only question left. `Evaluate` keeps all six verdicts including `AreaLocked`: it is the general decision function, and a prop being hidden from one list does not make the verdict wrong. **Tests were rewritten, not deleted** — the old `ShopItems_KeepsUnaffordableAndGatedProps_...` asserted the now-false half of MS2 — and three were added: owning the area brings the gated prop BACK (so the filter test cannot pass by dropping gated props forever), the two-key ordering, and equal prices keeping authored order, which is what pins the insertion sort against someone reaching for `List.Sort` later. **They have not been RUN**: Unity holds the project lock, so this step is verified by compilation only.

D-036: `MetaShopSetup` offers to REBUILD a shop whose references a later step added, and `MetaShopView` hides itself before validating. — rationale: the user reported the shop panel and popup stuck on screen at Play with a dead market button. Read from `MainScreen.unity`: the scene's `MetaShopView` had only `marketButton` and `panel` wired — the Ş1-era shop — so the nine fields added by Ş2/Ş3/Ş3b/Ş5 were null, `Start` returned early from validation, nothing was ever switched off, and no listener was attached. Two separate defects, and **neither of them was the user's mistake.** **(1) The tool put the burden in the wrong place.** The step promised not to rebuild an existing shop, which was right in Ş1 — the point was to protect hand-made layout. But every later step added objects AND required references to that hierarchy, and the step could only warn that fields were empty. I wrote "delete the root and run it again" into three consecutive postflights; when an instruction has to be repeated three times and the scene is still broken, the thing to fix is the tool, not the reader. It now names what is missing and asks whether to rebuild. The promise it actually made was never "never rebuild" but "never rebuild BEHIND YOUR BACK", and asking keeps that intact — a complete shop is still left untouched. Repair-in-place was rejected: the list and the popup are nested hierarchies (viewport → content → template; root → backdrop + box), and threading new pieces into a half-built one in the right order would mean maintaining two construction paths forever. `RequiredFields` is deliberately the same list `MetaShopView` refuses to run without — a second opinion about what "healthy" means would let this step bless a shop the screen then rejects at `Start`, which is exactly the confusion being fixed. A property that does not EXIST counts as missing too, because that means step and component are out of step and the author should see it. **(2) The screen did not fail safe.** `Start` returned early while leaving the panel and popup in whatever state the scene had saved, so a mis-wired scene showed the player a shop pasted over the map. Both are now switched off BEFORE validation, each behind its own null check: a missing reference is a console error, not a full-screen overlay. That removes the visible half of the complaint even when nothing is wired at all. `MainScreen.unity` was NOT edited to fix this. D-007 declined to hand-edit this project's scene YAML because such a change cannot be reviewed, and that still holds — the repair runs from the menu item, under the author's eye, through `Undo`, and without saving the scene.

D-037: An unaffordable BUY is RED, both on the row and in the confirm popup, on top of the dim D-035 introduced. — rationale: the user asked for it ("satın alınamayanlar aynı zamanda buy kırmızı halde olsun") — "aynı zamanda", so the fade stays and the red rides on it. The red is the project's existing destructive-action red from `MainScreenResetButtonSetup`, so the game does not grow a second red for the same idea. Both colours are serialized. **No new serialized reference, therefore no scene rebuild.** The button's graphic is `Button.targetGraphic` — the button's own declared graphic, already wired by the setup step. That is not the kind of lookup D-028 forbids: nothing is being guessed about which object was meant. Adding a dedicated `Image` field would have been more explicit but would have cost another rebuild of the shop, which is exactly the round D-036 had just been spent recovering. Verified rather than assumed: writing to `Graphic.color` is NOT clobbered by the Button's `ColorTint` transition. `Selectable` applies the state tint through `CrossFadeColor` on the CanvasRenderer, which MULTIPLIES with `Graphic.color` instead of overwriting it — which is also why the green the setup step authors shows up at all. In the popup that multiplication is the point: the disabled state darkens the red, so the button reads as "red, and off" rather than the generic grey that says nothing about WHY. The colours live on `MetaShopView` and are passed into `Bind`, not held by `MetaShopRowView`. The row stays a binder that decides nothing, and two components each holding the same pair of colours is a pair that can drift. They are applied on EVERY bind, not only the unaffordable one, so a row always shows one of the two rather than sometimes inheriting whatever the inactive template was authored with — the setup step's green now only decides what the template looks like in the Editor. The row's red BUY stays tappable, keeping D-035's call: red says "you cannot afford this", not "do not press this", and the preview it opens is still worth seeing.

D-038: The confirm popup's full-screen backdrop is removed. — rationale: the user deleted it from the scene and asked for it out of the code ("backdropu kaldırır mısın koddan sildim ben inspectorda"). This was not optional once they had: `confirmBackdrop` was a REQUIRED reference, so with the object gone `Start` failed validation and the whole shop stopped working. The backdrop did two jobs and they are worth separating. **(1) Tap outside to cancel (MS6) is GONE.** I had recommended it and it is the expected mobile behaviour, but the object is the user's call; CANCEL remains, so the player is never stranded in a preview — D-031's set of exits loses one member without any of them closing. That also makes the cancel button the ONLY way out, which is why it stays required and why its comment now says so. **(2) Blocking taps aimed at the map costs nothing**, because the `ScrollRect` is already disabled for the duration of a preview (D-032), so a drag landing behind the popup cannot pan anything. A side effect the user may well have wanted: the map is no longer dimmed during a preview, which shows the ghost more clearly rather than less. The field was DELETED rather than made optional. An unused serialized field reads as one that is used somewhere — the same argument D-027 made for a public method with no caller. Two follow-through details mattered more than the deletion itself. `"confirmBackdrop"` had to come out of `MetaShopSetup.RequiredFields`, or D-036's rebuild prompt would have reported a permanently missing field and offered to rebuild the shop on every single run — the cost of that list being the single definition of a healthy shop is that it has to be maintained with the component. And four comments that described the backdrop as present were rewritten rather than left: the box's `raycastTarget` in particular was justified by "or taps fall through to the backdrop and cancel the popup being read", which is no longer true, so it is now documented as ordinary panel behaviour that nothing relies on. A comment that survives the code it describes is the same defect as a stale map line.

D-039: A prop may carry its own SHOP ICON, separate from the art it is drawn with on the map. `MetaItemDefinition` gains an optional `shopIcon`; `ShopIcon` returns it or falls back to `sprite`. — rationale: the user asked whether the shop's row icons could be different sprites; there was no such field, the row was showing `item.Sprite`, the same image the prop is drawn with on the grounds. **The fall-back lives in the data class, not in the shop.** "Empty means use the prop's art" is one rule, and a rule spread across its callers is one a caller eventually forgets — here that forgetting would draw nothing for every prop without a custom icon, which reads as missing ART rather than as missing code. **`Sprite` deliberately keeps doing what it did**: it is what the map prop and the purchase ghost use. That split is the point of the feature rather than an oversight — the ghost answers "how will this look where it stands" and the icon answers "how do we represent this in a list", and a preview drawn from a list icon would be a preview that lies, which is the same reason D-032 kept `CreateProp` as the single authority for placement. The field is optional and an empty one is NOT a validation error: every catalog authored before today stays valid and behaves exactly as it did. A prop with no sprite at all is already reported, so "no icon anywhere" is covered without a second warning. Two things were checked rather than assumed. The constructor parameter was inserted after `sprite`, in the middle of the list, which would silently reassign arguments for any positional caller — every caller passes named arguments past the first two, verified before the edit. And `MetaEditorWindow` draws prop fields BY NAME rather than through a default inspector, so the new field would have been invisible in the Meta Editor without an explicit line; that is now noted on its map entry, because it will apply to the next field too. Two tests: an authored icon wins and leaves `Sprite` untouched, and no icon falls back to the art. Like Ş5's, they compile but have NOT been run — Unity holds the project lock.

D-040: The next Day-unlocked prop is shown before it opens — a faint silhouette on the grounds that fills in from the bottom as days pass, with the completed percentage over it. — rationale: the user's request. Three design forks were genuinely undecidable from the description and were put to them: where the fill STARTS (there was no second number on a Day-unlock prop, so the percentage had no denominator), how many props preview at once, and whether the label counts up or down. Their answers: the wait starts at the previous Day-unlock milestone, only the next prop previews, and the label shows completed percentage. **The chosen start needs no new authored data**, which is why it was recommended: the wait begins at the most recent Day-unlock day the player has already passed, and day 0 when there is none, so every existing catalog works unchanged. The accepted consequence is that pace varies — unlocks eight days apart fill slowly, two days apart fill fast. That is the catalog's own rhythm showing through rather than a defect, and the alternatives each cost a number per prop or a global one. **The user's actual question was whether THEY had to prepare anything — alpha versions, prefabs. They do not.** One sprite is enough: the faintness is a runtime colour alpha and the fill is `Image.Type.Filled` with a vertical bottom origin. It does take TWO layers rather than one, because a single Image cannot be both faint AND partly solid — `fillAmount` cuts the image off, it does not fade it — so the back layer says "this is coming" and the front layer says "this much of the wait is done". The silhouette goes through `CreateProp`, the same path the real prop and the purchase ghost take, so the preview cannot disagree about where the thing will stand; the fill is its stretched child, inheriting that placement instead of computing it again. **Area gating applies to the preview too**, and that is the one judgement call inside the rule: what holds an area-gated prop back is a PURCHASE, not time, so a bar creeping toward full would promise "wait and it comes" when the honest answer is "buy the square". A prop whose day has passed while its area is unowned therefore shows nothing at all — the same silence `IsActive` already gives it. The visuals are built in CODE rather than cloned from an authored template, which is the weaker choice on style and the right one here: a template would mean a new object reference on `MetaGroundsView`, which would mean re-running Build Meta Grounds — the step that refuses to rebuild what exists, i.e. exactly the trap D-036 was written about. Tuning is serialized instead (silhouette alpha, label size, colour, offset), and new serialized values are born with their defaults without anyone touching the scene. A missing TMP font costs the caption, not the fill. `preserveAspect` stays unused on these props — their size already comes from sprite pixels times the background scale, and it interacts badly with `Filled`. The rule lives in `MetaResolver` with five tests, because "which prop is next" and "how far along" are questions about the catalog. Nothing is persisted: the preview is derived every time, so D-020's rule that the save file holds purchases only is untouched. Like the rest of this session's tests, they compile but have NOT been run — Unity holds the project lock.

D-041: A Day-unlocked prop opening is now an EVENT: the meta screen zooms to it, the prop rises from silhouette to solid, and the map zooms back out. Profile schema goes to v5 with a `LastCelebratedDayIndex` marker. — rationale: the user's request, of which this is the FIRST HALF. They asked for two things — the celebration, and being forced back to the main menu when an unlock happens — and I split them and **reversed the order** deliberately: the forced return on its own would be a REGRESSION (marched to the menu and shown nothing for it), while the celebration on its own is a complete gain. The split is only possible because of the next point. **The main screen works out what it owes for ITSELF** — `MetaResolver.DayUnlocksBetween` over the profile's marker versus the current day — rather than being handed a message by the day scene. So it needs nothing from the forced-return step, and the player gets the celebration however they came back: through Day Complete, through Game Over, or by quitting and relaunching two days later. **No cross-scene static was used**, and that was the tempting shortcut. This project's posture is "no persistent scene, every scene builds itself from scratch", and its documented hand-off between the two scenes is the save file. A static would also have destroyed the celebration silently if the app closed in between, where a stored marker shows the missed unlock at the next launch. **v5's default is dangerous, which is the whole reason it carries a migration.** Read as 0, an existing save claims every Day-unlocked prop the player got days ago is still owed a fanfare, and they would be met with a queue of them. `UpgradeToCurrent` sets the marker to `CurrentDayIndex` — whatever they have, they have seen. This is the project's second real migration and the same shape as v3's `Lives`, which is exactly what the root invariant's "check whether 0 is safe" exists for. A NEW player at 0 needs no migration because the comparison is strictly-after, so a prop authored at day 0 is scenery rather than an event; both directions are pinned by tests. **The marker's single writer is `MetaGroundsView`**, the thing that plays the celebration, because "has this been shown" is only knowable by whatever showed it. `GameManager` remains the single writer of the balances and the day index; a different datum having a different writer is what the invariant allows. It is written only after the WHOLE queue finishes, so an interrupted celebration replays next launch — replaying is the cheap failure, swallowing is the expensive one. Four implementation choices carry weight. (1) Started only from `Start`, never `Refresh` — a purchase also calls `Refresh`, and a shopping trip is not the moment for a day-unlock fanfare. (2) `FocusGhost` became `FocusOn`: two callers now, and writing a second framing routine would have broken D-032's single-authority argument. (3) The real prop is HIDDEN while an animated copy takes its place, so the opening is something the player watches rather than something that already happened before the map arrived; the two-layer fill is shared with D-040's upcoming preview rather than copied. (4) Skip is a transparent full-screen `Button` parented to the CANVAS as its last sibling — not to the grounds, which are the canvas's FIRST child (D-029), so a catcher inside them would sit under the market button and let the shop open mid-celebration; and a Button rather than `Input.GetMouseButtonDown` so it does not depend on which input backend the project is configured for. `try/finally` guarantees the framing returns and the catcher goes, because a screen left zoomed in under an invisible full-screen button is unusable. Six tests, none RUN — Unity holds the project lock. **Still to do: the forced return** ("Next Day" routing to the main screen when the next day opens a prop), which is the second half of the request.

D-042: Finishing a day that opens a Day-unlocked prop sends the player to the MAIN SCREEN instead of straight into the next day, so D-041's celebration cannot be skipped. — rationale: the second half of the user's request, deliberately done after the first (D-041) because on its own it would have been a regression — marched to the menu and shown nothing. **Almost nothing was written; the behaviour already existed.** `ReturnToMainScreenFromCompletedDay` persists the next index, saves, and loads the main screen, which is exactly the wanted outcome. Its own comment also explains why it does NOT go through `AdvanceToNextDay`: that method clears the board, discards trays and refills slots, which microseconds before the scene unloads is either thrown away or an event cascade through views that are already tearing down. That reasoning covers the new case identically, so the unlock branch must not touch `AdvanceToNextDay` either — and does not. **The decision went into `GameManager`, not the popup.** `DayCompletePopupView` already asked one question and branched on it; adding a second would have made a popup the place where scene routing is decided. It now asks one thing — `TryContinueIntoNextDay` — and hides itself only if there is still a day scene to hide in. Rule in `MetaResolver`, decision in `GameManager`, view deciding nothing. **"Sent back and shown nothing" is made unrepresentable rather than merely unlikely.** The check goes through a new catalog-level overload of `DayUnlocksBetween` that resolves the same location the meta screen will open on. It is the same location by construction: the celebration only ever starts from `Start`, and at that moment the viewed location IS the newest unlocked one. Both sides therefore call one function instead of computing the answer twice — with one location authored this is invisible, which is exactly why it was worth getting right now rather than after a second restaurant exists. The two overloads reintroduce the `null`-names-neither ambiguity that produced a CS0121 on `DefaultLocationIndex` earlier in this session; noted at the declaration, and no bare null is passed. `GameManager` gains one OPTIONAL serialized `MetaCatalog`. Optional, because a day scene must not be broken by a missing reference over what is, from the day's side, a routing nicety — but NOT silent: `Awake` says so once, since an empty field and a catalog with no upcoming unlocks look identical on screen. That is the same posture, for the same reason, as `HudWalletSource` logging which mode it resolved to. **No asmdef change, verified before planning**: `Scripts/Bootstrap` and `Scripts/UI` carry no asmdef of their own, so both live in `Assembly-CSharp`, which already references `MetaSystem` through `MetaGroundsView`. No new dependency arrow, so the root invariant on one-directional arrows is untouched. This step writes no data and adds no schema — v5 from D-041 is sufficient, and the celebration marker's single writer stays `MetaGroundsView`. Two tests cover the rule. The DECISION itself (which scene loads) is not covered and cannot be: it is a MonoBehaviour calling `SceneFlow`. Stating that plainly rather than implying the step is fully tested — what is tested is the question, not the routing.

D-043: After the unlock celebration, the map pans on to the NEXT prop's ghost and lingers there before zooming out. — rationale: the user asked for it. Small and entirely presentational: no rule was added, so no test was added either — the prop being looked at is what `MetaResolver.NextDayUnlock` already returns, covered by five tests from D-040, and everything new here is the order the camera moves in. **The timing works out for free, and that is worth recording rather than rediscovering.** D-040 measures a prop's wait from the PREVIOUS unlock, so the moment one opens, the next one reads exactly %0. The peek therefore says "the counter for this one starts now" using nothing but the number already on screen — no extra copy, no emphasis, no second label. It is also not a new drawing pass. `Refresh` runs BEFORE the celebration and already has the new day index, so the silhouette on screen is the prop AFTER the one being unlocked, drawn at the right progress. The peek only has to focus it; drawing another would have put two copies of the same thing on the grounds. The peek gets its OWN duration rather than reusing `celebrationHoldSeconds`: that beat is "look at what you got" and this one is "and this is next", and sharing one number means breaking one while tuning the other. `skipRequested` is reset before it, so a player who skipped the reveal is not taken to have skipped the peek as well — if they want past that too, one more tap does it. Nothing coming (everything open, or the next prop area-gated and therefore not drawn) falls through to the caller's `finally`, which remains the single place the framing is restored, so the zoom-out still has exactly one owner.

D-044: The required-pool guarantee budget is counted over **active** tickets only and reaches the queued lookahead solely once every active ticket is already covered. Board spawning stays **order-triggered and nothing else** — `OnOrderPlaced` remains `BoardDistributor`'s only entry point, and the urgency override is evaluated on that same path — rationale: a playtest screenshot showed three active tickets, a board holding one ketchup hotdog and one cola, and no legal move: every visible ticket wanted something else. `SelectGuaranteedTickets` drew the budget from ONE pool of active + upcoming tickets, so with the shipped balancing (`guaranteedTicketCount: 1`, `earlyTicketWeightDecay: 0.5`, a 10-deep lookahead, i.e. ~13 candidates of which 3 are on screen) the single guaranteed slot regularly went to a ticket the player could not see — and because selection is deliberately STICKY (D-001) and a queued ticket never leaves the pool, it then held that slot for many rounds while all three visible tickets got nothing. Reproducing the exact algorithm over the authored Days (2000 simulated days, a perfect instant player): **18.7% of distribution rounds left ZERO active ticket guaranteed and 2.3% left all three slots full with nothing completable**, costing ~0.3 forced life losses per day; after the change both are 0.0% and the only round with no active ticket guaranteed is the one at day end that has no tickets left. This is a **restoration, not a redesign**: `ExpoTheExplorer/CLAUDE.md`'s locked Board/Food Distribution rule already reads "the minimum set needed to complete ACTIVE tickets — at least one ticket must always be completable", and a ticket that has not arrived completes nothing. Everything else about D-001's scheme is untouched — the arrival-weighted lottery, the Poisson/Manual budget, stickiness, and the "at least one, not every ticket" reading all stand, and reaching into the lookahead still happens when the budget exceeds the number of active tickets (`OnOrderPlaced_GuaranteedCountExceedsActiveTickets_ReachesIntoUpcomingQueue` passes unchanged). A queued ticket already picked keeps its spawned items and stays in the set — nothing here ever un-spawns — it simply stops consuming the active budget, and starts counting again once it reaches a slot. **A second change was built and then reverted on the user's instruction, recorded here so it is not re-proposed.** Diagnosis surfaced a related hole: D-001's "any active ticket under `UrgentTimeThresholdSeconds` is guaranteed unconditionally" can only be evaluated when something calls into the distributor, and the sole caller is ticket assignment — so if a round ever left nothing completable, the player delivers nothing, nothing is assigned, no further round runs, and that rescue rule fires exactly never in the situation it was written for. A time-driven entry point (`OnTicketBecameUrgent`, plus a per-slot urgency edge detector in `GameManager.Update`) was implemented for it and then removed: the user's decision is that **spawning is order-triggered, period** — no timer, no per-frame board poll — and the about-to-expire check belongs on the `OnOrderPlaced` path, which is where it already lives. That holds together because of the first change rather than in spite of it: with the active-first budget every round already leaves at least one ON-SCREEN ticket completable, so reaching a no-move board now requires the player to strand it themselves (parking a required item in a tray, which they can undo by dragging it back out) instead of it arising from the distribution rule. Consequence to keep visible: `Update` ticks ticket timers and nothing else, so if the guarantee ever looks starved again the lever is the active-first budget or `guaranteedTicketCount` in the Day file, never a new trigger. NOTE this changes balance in the player's favour on identical Day files (~0.3 fewer forced life losses per day) and no Day JSON was touched: `guaranteedTicketCount: 1` on Days 03–07 means one of the three visible tickets is covered, which is a designer's call — the fix only guarantees that the one covered ticket is always one the player can actually see. NOTE also what was ruled out while diagnosing, so it is not re-investigated: `TraySlot.Matches`/`RequiredItemKey` agree with the spawner field for field, `BoardGrid`'s pending-spawn queue was not involved (2 of 20 cells occupied), and all three hotdog modifications do have authored sprite layers, so the board really was showing an item no active ticket wanted rather than mis-drawing one it did — affects: BoardDistributor.cs, BoardDistributionTests.cs (2 new cases), GameManager.cs (touched and then fully reverted — no net change), codemap-core.md (also repaired 5 `MISSING-role` Core lines on this path — Ticket, TruncatedPoisson, RequiredItemKey, BoardGrid, BoardItem — and a `dep:` on BoardDistributor still naming the BoardDistributionConfig that D-007 deleted) — 2026-08-23

D-045: The upcoming-prop silhouette respects SortOrder instead of being drawn on top of everything. — rationale: the user reported the fryer sitting BEHIND the sauces in the Meta Editor but IN FRONT of them in game. The catalog settled it: `fryer` is a DayUnlock prop at `sortOrder = 0` that opens on day 12, so it is not active yet and is drawn as D-040's upcoming ghost — and `DrawUpcoming` appended that ghost after the active-prop loop, making it the last sibling and therefore frontmost whatever its depth. The editor canvas (`MetaLocationLayoutGUI.SortedIndices`) has always sorted, so **the editor was right and the game was wrong**, which is the worse way round: the preview tool the author trusts was telling the truth while the game contradicted it. Fixed by placing the ghost at the sibling index its SortOrder asks for, not by forcing it forward. The reason is the reason the preview exists at all (D-032, D-040): it shows where the prop WILL stand, and depth is part of where — a ghost pulled to the front tells the truth about position and lies about occlusion. The consequence is accepted rather than worked around: a prop authored at a low SortOrder really is behind the scenery, so its ghost can be partly or wholly hidden, and the celebration zoom is what still makes it findable. The catalog was NOT edited — the fryer's `sortOrder = 0` is a content decision, and the code was the thing disagreeing with it. A SECOND site in the same class had the same defect and is fixed with it: the celebration's temporary copy was also appended, so a prop would jump to the front for the length of its own celebration and drop back when the copy went away. It now takes over the hidden real prop's exact sibling index, which makes "the copy and the original sit at the same depth" true by construction rather than by a second calculation. `SortedInsertIndex` takes the location so it can break ties by authored catalog order — the same single ordering rule `MetaResolver.ActiveItems` and the editor both apply; inferring authored order from the active list would have been a second, quietly different rule. **Deliberately NOT changed, and flagged rather than settled:** the PURCHASE ghost is still forced to the front (D-031). The same truth-in-preview argument applies to it, but the shop zooms to it and names it in a popup, so occlusion there costs comprehension — and it was not what was reported. Left for the author to decide. **Process note, because it cost real time.** Two partial edits to this file were made through Bash, which this project's root invariant forbids ("C# changes are made only with the Edit/Write tools"). Meanwhile another session had overwritten `.claude/preflight/current.md` with its own BoardDistribution preflight, so the user's APPROVE hashed that file instead of this plan and the guard correctly refused the third edit — leaving `Assembly-CSharp` broken on a missing helper (`CS0103`). Had the Edit tool been used from the start, the guard would have refused the FIRST edit and the mismatch would have surfaced before anything broke. The invariant is not bookkeeping: it is what makes the preflight gate able to stop a bad edit.

D-046: A location may carry a larger copy of its grounds art, drawn behind them, so the edges keep going when the map zooms in. One optional field, `backgroundBgSprite`, centred with no offset. — rationale: the user added `Art/Meta1/BackgroundBg.png` and asked that whatever applies to the background apply to it too, sitting exactly behind. **The offset field this started as was deleted because the art was fixed instead.** The first export was measured (by scale sweep and correlation) at 1074x2550 with the grounds sitting at (117, 387) — asymmetric, where centring would have been (110, 353), a 34px vertical error that shows on screen. That was going to cost a `Vector2` in the catalog. The user re-exported it as 1129x2444: +600 height (300 top, 300 bottom), +276 width (138 each side), both growth ratios equal to within one pixel of rounding, and correlation then found the grounds at exactly (138, 300) — dead centre, deviation zero. So centring is now a CONTRACT on the art rather than a number in data, which is what keeps this one field instead of a field plus a pair that goes stale on the next export. Measuring rather than trusting is the point: the first export would have failed the same assumption. **The grounds Image is now a container that never draws.** A child always renders over its parent's own graphic, so a continuation layer cannot sit behind a parent that draws — the art therefore moved into runtime children, ordered [0] continuation, [1] grounds, [2..] props. The alternative, inserting a real container object into the hierarchy, would have meant a new serialized reference and another delete-the-root-and-rebuild round (D-036); as built, the scene is untouched and `background` is still the ScrollRect content, the zoom target and the props' parent, with every authored normalized position still measured against the same rect. The continuation is a CHILD precisely so scrolling, the preview zoom and the framing are inherited rather than recomputed — a second transform calculation is the "second authority" trap this class has now avoided several times (D-032, D-040, D-043). It is sized from its own pixels times the SAME `PropScale` the grounds use. **Scroll range is deliberately unchanged**: the content is still sized from the grounds, so the extra art is clipped by the viewport and fills the edges under zoom instead of letting the player pan into empty space. `artLayerCount` exists because `SortedInsertIndex` returns SIBLING indices and props now start after these layers; forgetting that shift would place ghosts behind the grounds art, invisible — the same silent-depth defect D-045 was reported for. The Meta Editor canvas draws the continuation too, in the same order, because a preview that leaves out a layer is one that gets trusted and then contradicted — which is exactly what D-045 was reported for. **A plan correction worth recording:** the preflight said `MetaEditorWindow` would need a line for the new field. It does not. That window lists the ITEM fields by name (D-039's lesson) but draws the LOCATION with a generic `PropertyField`, so a new `MetaLocation` field appears on its own under the "Location & full prop list" foldout. No second editing surface was added for one datum. No new tests: one data field and a drawing order, with the rules layer untouched.

D-047: A successful meta purchase CLOSES the shop. `MetaShopView.OnConfirmBuyClicked` ends with `SetOpen(false)` followed by `grounds.Refresh()`; the panel does not come back and the row list is not rebuilt — rationale: the user's instruction ("satın alma kısmında bir şey satın alındıktan sonra yeniden dükkana dönmesin"). **This reverses MS4, which is the part worth recording.** MS4 kept the panel open on purpose so a player buying several props in a row would not have to tap the market button between each one, and that reasoning was about the *number of taps*. It ignored the geometry: the panel is a bottom sheet covering most of the map, so restoring the list puts the props NOT bought yet on top of the one that was just paid for. The payoff of a purchase is the prop standing on the grounds, and until now the player never got to see it — the map travelled back and the list slid straight over it. One extra tap to reopen the shop is the cheaper of the two costs, and it is a tap the player takes only if they actually want a second prop. **Mechanically the change is a subtraction, not an addition.** `SetOpen(false)` already does everything this path needs — it routes through `ClosePreview` (ghost destroyed, framing tweened back, D-031's single door), calls `ApplyVisibility` once for both the panel and the popup, and drops the rows via `ClearRows` — so the fix is to use that door instead of `ClosePreview` alone, which left `isOpen` true and had `ApplyVisibility` switch the panel straight back on. `RebuildRows()` is deleted from the path rather than moved: a closed panel has no rows to build, and the next `SetOpen(true)` builds them against the balance as it will be at that moment rather than as it is one frame after a purchase. Clearing `isOpen` inline would have worked too and was rejected — the first shop's ghost leaked precisely because a transition took a shortcut around the single door (D-024/D-027), and a second such shortcut is how that returns. **The refusal paths are deliberately untouched:** a non-`Ok` verdict from `MetaPurchase.Evaluate` and a `Wallet.TrySpendSoftMoney` refusal both still `return` with the popup up and the ghost standing, because closing the shop is now the SIGNAL that money moved — doing it on a refusal would read as a purchase that happened. `grounds.Refresh()` stays AFTER the shop comes down so the newly-owned prop is redrawn solid on a map nothing is covering; it is safe there because the return tween runs on `background.rectTransform` while `Refresh` rebuilds that object's children, which is the same order the previous code already relied on. NOTE nothing else moved: the double `Evaluate`, the wallet-is-the-single-writer ordering, `session.Save()` writing balance and ownership as one file, and the atomic-by-construction property are exactly as D-020 left them — affects: MetaShopView.cs, codemap-ui.md — 2026-08-24

D-048: A purchased prop is PLACED, not merely present — it falls the last `placementDropHeight` into its spot while fading in, and the map takes a short damped shake as it lands. `MetaShopView` calls `grounds.RefreshAfterPurchase(item)` in place of `grounds.Refresh()`; everything about what that looks like lives in `MetaGroundsView` — rationale: the user's instruction ("satın alma olunca aldığımız item hafif yukarıdan fade in olarak gelse ve kamerada düşme animasyonunun bitmesiyle hafif titrese"). It completes D-047: closing the shop gave the player a clear view of the prop they had just paid for, and this is the moment that view is worth having. Six choices carry weight. **(1) One public method, not two.** `RefreshAfterPurchase` composes the redraw and the animation because the two are order-dependent in a way a caller cannot see: the prop must be pushed up and made translucent in the SAME FRAME `DrawProps` drew it, since a bought prop is drawn solid and in place, and yielding even one frame first would show it standing there and then jerk it back into the air. A `Refresh()` + `Animate()` pair is a pair that is eventually called in the wrong order, and the failure would be a visual stutter nobody could name. The shop therefore says only WHAT was bought. **(2) A frame loop, not a tween.** `CelebrateOne` already animates this way and the reasons transfer exactly: a `try/finally` can then guarantee the finished state through any interruption, and no tween is left pointing at a `RectTransform` that the next `Refresh` destroys. `DOVirtual.EasedValue` supplies the easing curve, so the `Ease` field stays serialized and tunable like `previewTravelEase` without a tween owning the object. **(3) The drop waits `previewTravelSeconds` first.** The shop has just closed, which restored the preview framing over exactly that duration, and `CelebrateOne`'s own comment states the rule — an animation played against a moving background reads as one confused motion instead of two clear ones. `WaitOrSkip` was deliberately NOT reused for that wait: it honours `skipRequested`, which belongs to the celebration's full-screen catcher, and a leftover `true` from an earlier celebration would silently skip the wait. **(4) The shake moves the CONTENT, not the viewport.** Shaking the viewport moves the mask window and opens a sliver of empty screen at the edges; the content carries D-046's oversized continuation layer as a child, which is precisely the art that keeps the edges covered while it moves. The ScrollRect is suspended for the shake's duration — D-032's call, for D-032's reason: it is Clamped and drags the content toward its bounds every `LateUpdate`, so leaving it enabled would have the two of them pulling at one value and the shake would come out as a stutter. It is handed back under `RestoreFocus`'s own `!focused` condition, so that flag never has two owners. There is no race with `RestoreFocus`'s `OnComplete` (which also re-enables scrolling): the shake starts a full `placementDropSeconds` after the wait ends, so the tween has long since completed. **(5) A damped sine, negative first, not per-frame randomness.** An impact has a direction — the ground gives way under the thing that hit it — and random jitter at 60fps reads as a glitch rather than as weight. The amplitude falls linearly to zero, so the last frame is already home and the `finally` has nothing to correct. **(6) Every number is serialized on the view, none in the catalog.** D-015 holds `MetaCatalog` to price/position/art/unlock, and "how hard the map shakes" is none of those; the six values sit beside `ghostAlpha` and `previewTravelSeconds`, and a prop authored tomorrow needs no new content to get this behaviour. NOTE the interruption path is where the real risk was, and it is closed in one place: `Clear()` stops the routine BEFORE destroying props, which makes "the props are gone" and "nothing is still animating them" the same moment — and `Clear` is the only place props are destroyed, so it is the only place that has to know. The routine's `finally` then snaps the prop to its landed position and full alpha (null-checked, because the likeliest stopper is the `Refresh` that destroyed it), puts the content back, and returns scrolling. NOTE also the fade lerps toward the prop's OWN alpha rather than to 1: `CreateProp` leaves props opaque so the two are the same number today, but a translucent prop would otherwise be faded up to solid and stay wrong until the next redraw — affects: MetaGroundsView.cs, MetaShopView.cs, codemap-ui.md — 2026-08-24

D-049: The preview zoom is HELD through the placement animation and the map travels out only once the prop has landed and the shake is over. `MetaGroundsView` gains `placing` (the claim), `ReleaseFraming()` (the one way it ends), a `placementGeneration` counter and a snapping `RestoreFocus(animated: false)`; `MetaShopView.OnConfirmBuyClicked` calls `grounds.RefreshAfterPurchase(item)` BEFORE `SetOpen(false)` — rationale: the user's instruction ("animasyon falan bitene kadar zoom geri dönmese de animasyon bittikten sonra zoom out yapsak"). It is the third and last step of the purchase feel (D-047 closed the shop, D-048 dropped the prop in): the player previewed the prop at a magnified size and the payoff is watching it land at that size, not watching it land as a speck after the map has already pulled away. **A flag, not a parameter.** The obvious alternative was to thread a "keep the framing" bool through `SetOpen` and `ClosePreview`, and it was rejected: "may the zoom be given back right now" is `MetaGroundsView`'s question, and answering it in the shop would put a second owner on a piece of state whose whole design (D-031) is that it has one. The flag is honoured inside `RestoreFocus`, next to the `focused` guard that is already there for the same kind of reason, so every path that asks for the zoom back — and there are two on a purchase, `SetOpen`'s `ClearGhost` and `Refresh`'s own `ClearGhost` — is suppressed in one place. **The call order in the shop is load-bearing, and it was found by wiring the change rather than by reasoning about it.** The first attempt kept `SetOpen(false)` first and did nothing at all: the shop's exit runs `ClearGhost`, whose second half is `RestoreFocus`, before the grounds have heard about the purchase, so the zoom-out was already under way when the flag went up and the placement simply waited for it. Reversing the two calls fixes it, and they share one frame with no render between, so nothing is visibly half-done — the panel closing and the drop beginning are the same instant. **Two waits and a scroll block died with this change,** which is the part worth noticing: D-048's fixed `previewTravelSeconds` wait existed because the map was travelling during the drop, and it no longer is; and the shake's disable/re-enable of the `ScrollRect` was dead the moment the framing was held, because `FocusOn` turned scrolling off when the preview opened (D-032) and `RestoreFocus` is what turns it back on — the whole placement now runs inside a window where it is already suspended. Dead code that claims to own a flag is worse than no code, so it went rather than being left as insurance. What replaced the wait is `while (DOTween.IsTweening(content))`: zero frames in the normal case, and exactly the remainder for a player who taps BUY before the travel INTO the zoom has finished — a fixed number would have been too long in the first case and the wrong length in the second. **Interruption ownership is the subtle part and is settled with a generation counter.** Every claim bumps `placementGeneration`, and a placement's `finally` hands the map back only if its own generation still matches. Without it, a second purchase would have the first placement's teardown travel the map home from under the second one's drop. A location change is the opposite case and belongs to its own caller: `Step` calls `CancelPlacement`, which bumps the generation and gives the framing back SNAPPED, because `Step` sets the new location's scroll position one line later and an animated return would still be moving and would overwrite it — the player would arrive somewhere they never asked for. `Clear` deliberately does not make that decision: it cannot tell a location change from a purchase, and the two want opposite things. NOTE both interruption cases are unreachable in today's scene — the catalog has one location and D-025 makes the arrows optional — and are written down because the second location is exactly what makes the first one bite, far from this code. NOTE also the two units differ on purpose and it is not a bug: the drop is measured inside the content, so it grows with the zoom and stays the same fraction of the prop's own height, while the shake moves the content's `anchoredPosition`, which lives in the viewport's space and is therefore a fixed number of screen units at any magnification. A camera shake is about the screen; a falling prop is about the prop — affects: MetaGroundsView.cs, MetaShopView.cs, codemap-ui.md — 2026-08-24

D-050: The day scene is played AT the expo the player decorates. `SampleScene`'s background becomes their own grounds — the unlocked location's art with the props they OWN standing on it — composed and blurred once at day start. New: `MetaLayout` (the prop arithmetic, extracted), `MetaBackdropView` (the day-side view), `MetaBackdropSetup` (the menu step); `MetaGroundsView` routes its scale and size through `MetaLayout` — rationale: the user asked to "blur the meta part and carry it into the other scene", and then asked the question that shaped the design: *"metada yeni bi şeyler aldıkça gündeki arkaplan da değişecek mi"*. **The first plan said no and that made the feature pointless**, so it was reversed before any code was written: a backdrop showing only the static location art would have left the decoration loop with no reach into the game, and "you are playing at the expo you decorated" is the whole reason to put it there. It now recomputes from the owned set at every day start, so a prop bought today stands in tomorrow's backdrop — and NOT halfway through the current day, which is correct, because the grounds are not something the player can change while a day is running. **The blur needs no shader, and that is a consequence of a fact about this project rather than a preference**: the URP package is installed but `GraphicsSettings` assigns no pipeline asset, so the built-in pipeline is what actually runs and URP renderer features are unavailable. Progressive halving through `Graphics.Blit` fills the gap exactly — a bilinear sample at half resolution lands precisely between four source texels, so each step is an exact four-texel box average and N steps compound into a wide blur. The result is deliberately left SMALL: stretching a tiny texture across the screen is itself a bilinear filter, so the upscale finishes the blur for free and the memory held for the rest of the day is a few kilobytes. Composition runs at `composeScale` 0.25 for the same kind of reason — the output is about to become soft shapes, and full resolution would spend ~11MB of transient memory on detail thrown away one step later. **The layering was already there and was not invented.** `SampleScene` carries a `Backgroundcanvas` at sorting order -100 with the world sprites at -2, `InGameCanvas` at -1 and the popup canvas on top; the backdrop takes over the graphic already sitting on it, so nothing about the day's render order changes. **The extraction is the part with real risk and it was taken deliberately.** Two things now draw the same prop, and `MetaGroundsView`'s own comment had long warned that "every place that draws a prop must compute it identically or a preview lies about where the thing will land" — written when there was one such place. Copying the four lines would have been the safe-looking choice and the wrong one, so `PropScale`/`PropSize` moved into `MetaLayout` and the grounds now ask for them. The POSITION deliberately did not move: Unity's anchors implement the same rule better, because an anchored prop rides the background through every resize without a line of layout code, and `MetaLayout.PropRect` is that rule spelled out only for the medium that has no anchors. If the two ever disagree, the anchors are right, and that is written into both files. NOTE the catalog gained NOTHING: a blur is an operation, not art, and D-015 holds `MetaCatalog` to price/position/art/unlock. NOTE the view writes nothing at all — it reads the catalog and the owned set, and every failure path (no session, no catalog, no unlocked location) leaves the `RawImage` exactly as the author left it, which IS the fallback; a `fallbackTexture` field was considered and rejected as a second place to author the same thing. NOTE the final `RenderTexture` is owned and released in `OnDestroy`, and cleared off the `RawImage` first, because graphics memory is not garbage collected and a `RawImage` pointing at a released texture draws whatever the driver left there. NOTE also sprites are blitted through `sprite.textureRect` rather than the whole texture: the difference is invisible until the art is packed into an atlas, at which point every prop would become a collage of its neighbours. **Not verified in Unity**: no test pins `PropRect` against a real `RectTransform` — that would be the mechanical guarantee that the two drawing paths agree, and it fell outside the approved manifest — and the scene change itself is a menu step the author runs, not something written here — affects: MetaLayout.cs (new), MetaBackdropView.cs (new), MetaBackdropSetup.cs (new), MetaGroundsView.cs, codemap-core.md, codemap-ui.md, codemap-editor.md — 2026-08-24

D-051: Every GameObject the grounds spawn is now destroyed through one method, `Detach`, which calls `SetParent(null)` **before** `Destroy`. This fixes the "prop deleted by my purchase" bug — rationale: the user reported that buying fences made the drink fridge's upcoming-silhouette disappear, and their first description of it — *"arkaplanda kaldı"*, it stayed in the background — was literally correct. It was misread here as a framing complaint, because the props they could still see happened to be the ones in the top half of the map, which made a scroll problem look plausible. **The mechanism**: `Object.Destroy` is deferred to the end of the frame, and until then the object remains in its parent's child list. A redraw destroys the old art layers, props, silhouette and ghost and then immediately appends the new ones, so for the rest of that frame the background carries BOTH sets. That is harmless for anything that only appends — the new props keep their relative order — but `DrawUpcoming` moves its silhouette to an **absolute** sibling index from `SortedInsertIndex`, computed as though the dead children were already gone. In the reported case the dead block was 13 children (2 art layers + 9 props + 1 silhouette + 1 ghost) and the computed index was 12, so the silhouette was inserted among the corpses — behind the new `BackgroundArt` layer — and disappeared. **The arithmetic was never wrong; its input was.** `SortedInsertIndex` counts what SHOULD be there, and the fix makes the hierarchy agree with it rather than teaching the arithmetic about corpses, which would have spread knowledge of a Unity lifetime detail into a sorting rule. `DestroyImmediate` would also have worked and is the wrong tool at run time. **Two facts about when this bites are worth keeping.** It has been live since D-045 introduced `SetSiblingIndex` for the silhouette, and it fires on every redraw that had something to clear — a purchase or a location step — never on the first draw, because `Clear` finds nothing then. It only became *visible* with D-047, which closes the shop after a purchase and so finally lets the player look at the map at the moment it is wrong. NOTE `CelebrateOne`'s animated copy goes through `Detach` too: it lives under the background and the celebration reveals props one after another, so there is always a next absolute index to shift. NOTE the celebration's full-screen skip catcher does NOT, because it is parented to the Canvas, where nothing computes an absolute index. NOTE also `CelebrateOne`'s other `SetSiblingIndex` call is safe on its own terms — it reads `realProp.GetSiblingIndex()`, a RELATIVE lookup, which is the shape that never had this problem — affects: MetaGroundsView.cs, codemap-ui.md — 2026-08-24

D-052: The Day Editor's `Main Dish Weights` list is no longer authored — its MEMBERSHIP is derived from the Mains picked under Food Selection, by `DayEditorMetaModel.SyncMainDishWeights`, and the list's add/remove buttons and each row's `Food` picker are gone — rationale: the user asked for the list to fill itself from the selected main dishes. The two halves of one setting had been sitting in two places with nothing tying them together: a designer picked a burger under Food Selection and then had to remember to hand-add a burger row below, and forgetting silently fell back to `MainDishWeight.DefaultWeight` — a Day that looked balanced because the list looked populated. **The split of ownership is the actual decision, not the auto-fill.** Membership is derived and has exactly one writer (Food Selection); the numbers in each row — weight, λ, max ceiling — stay hand-authored content and the sync never touches them, which is why an existing row is carried over **by reference** rather than rebuilt. Leaving the `+`/`-` buttons in place would have created the second writer the root invariant forbids, and the sync would have silently undone whichever the designer used last; the row's `Food` field is read-only for the same reason (repointing it would either be reverted on the next sync, or author a weight for a dish the Day never serves). **The single call site is the load-bearing part.** It sits at the end of `DrawFoodSelection`, not in the three writers — `SetFoodAllowed`, `FromDayJson`, `CopySettingsFrom` — because that section owns the input and, at `PropertyOrder` -5, draws before the list at -3: one call therefore covers opening a file, clicking a tile, the per-category All/None, Select All/Clear All, a catalog assigned late in the toolbar, and a new Day seeded from the previous one, and a future fourth writer for free. Three hand-placed calls would have been three places to forget. The price of putting it in a draw method is that it runs every repaint, so `MainDishWeightsMatch` early-outs with indexed loops and no LINQ — **do not add allocation to that path**; only the rebuild allocates, and only when the selection actually moved. `FromDayJson` was deliberately NOT touched: the existing `DayEditorModelConverterTests` fixture never sets `category`, and `FoodCategory.Main` is 0, so all three of its foods are Main-category — syncing on the load path would have broken tests over a fixture quirk, and the draw path sits outside them. NOTE one behaviour change, taken knowingly: a leftover row for a Main this Day does NOT serve is dropped, so it disappears from the file on the next open-and-save. That row was already dead — `TicketFactory` only picks from the pool built out of `AllowedFoodItemIds`, so its weight was never read. NOTE `DayEditorTicketGeneration.ModificationCountLambda` becomes unreachable on this Day's own Generate (every Main in the pool now has a row of its own) and was kept anyway: it still travels to the runtime config clone, and removing it would be a Day-JSON schema change for a value that costs nothing. It is NOT dead in general — the single-ticket "Generate Random Ticket" button builds its factory from the shared `TicketGenerationConfig` asset rather than the Day's clone, so that path still reads the asset's own weights and fallback. That inconsistency predates this change and was left alone. **Not verified in Unity**: the editor project is open, so the five new EditMode tests were compiled (`dotnet build` on both csprojs, 0 warnings) but not executed — they need a Test Runner pass, and the inspector's draw order (-5 before -3) is asserted from `PropertyOrder`, not observed — affects: DayEditorModel.cs, DayEditorModelConverterTests.cs, codemap-editor.md, codemap-core.md — 2026-08-24

D-053: No UnityEngine **value** attribute goes into `DayEditorModel.cs` any more — the four `[UnityEngine.Min]` on `DayEditorTicketRuntime` became Odin's `[MinValue]`, which is what every other numeric setting in the file already used — rationale: the user reported that the Ticket Runtime box swallowed the mouse wheel (the page would not scroll with the cursor over it) and that its fields arrived a frame late while scrolling. **The mechanism is the hosting, not the attribute's meaning.** `DayEditorWindow` is an `OdinMenuEditorWindow` whose menu items are plain `DayEditorModel` C# objects, so there is no `SerializedObject` behind them; `UnityEngine.MinAttribute` is drawn by Unity's `MinDrawer : PropertyDrawer`, which requires a `SerializedProperty`, so Odin must bridge it through an emulated one. That bridge takes its height from Unity's `GetPropertyHeight`, which disagreed between the layout and the repaint pass (the late fields), and processes the event against its own rect (the dead wheel). **This is the SECOND time the same bridge broke drawing in this window**, which is why the rule is now file-wide rather than another one-off fix: the first was `[UnityEngine.Range]`, whose slider ran past the window's right edge with its number field unreachable, and `[Min]` was the last survivor of that clean-up. `[Tooltip]` and `[HideInInspector]` stay — Odin reads both natively, they have no drawer — and an audit confirms they are the only UnityEngine attributes left in the file. **One design-visible change came with it, and it is a fix rather than a side effect**: the three patience limits now floor at **0**, not 1. A 0-second limit must stay authorable, because it is precisely what `DayValidator` refuses the Save over with the reason spelled out — `FromJson` deliberately does not clamp those three for that reason, and `FromDayJson_ZeroTimeLimit_IsNotSilentlyRepaired` pins it on the load path — yet `[Min(1f)]` had made that path unreachable by hand, so the inspector was silently preventing the one value the validator exists to catch. Negatives stay blocked, being nonsense rather than a reportable authoring mistake. `UpcomingQueueSize` floors at 1, matching `FromJson`'s own `Mathf.Max(1, ...)`. **Not verified in Unity**: the editor is open, so this was confirmed only by a clean `dotnet build` and by the attribute audit; whether the wheel and the frame lag are actually cured has to be seen in the window. If they are not, the next suspect is named in the preflight — the Food Selection grid derives its HEIGHT from `lastFoodGridWidth`, which is only written on Repaint, so a scrollbar appearing can feed back through columns -> rows -> height — affects: DayEditorModel.cs, codemap-editor.md — 2026-08-24

D-054: A ticket's (or Start Board cell's) modifications are picked from a **visual tile grid of what the owning food actually offers**, through ONE helper — `DrawModificationPicker` — replacing the unfiltered `ModificationConfig` ObjectField and the blank-row "+ Add Modification" button — rationale: the user asked to see only the modifications specific to the ticket's current main dish, and to see them visually. **The constraint turned out to be the runtime's own rule, not a new one.** `TicketFactory.Create` pulls modifications from `main.AvailableModifications` and nothing else, and `CreateModification` takes direction from `ModificationConfig.AllowedDirection`, rolling a coin only for `Both`. The old picker offered every `ModificationConfig` in the project and appended entries with `IsAddition` left at false, so a Hotdog could be given a Burger's modification, and an AdditionOnly modification could be authored as a removal — "remove Extra Ketchup": valid data, impossible content, and a topping the dish has no sprite layer for. `DayEditorModification.ForConfig` is now the only way one is hand-authored and it seeds direction from the type; `CanFlipDirection` is why only a `Both` gets the right-click flip. Those two are deliberately pure and static so the one rule that can silently go wrong has four tests, while the IMGUI around it has none. **The scope was widened on purpose and declared before approval**: the same modification editor existed TWICE, byte-identical, in the ticket editor and the Start Board cell editor. Fixing one would have left the file carrying two divergent pickers, so both now call the helper — the owner is `entry.MainItem` for a ticket and `entry.Item` for a cell, which is the only thing that ever differed. The grid follows the Food Selection grid's conventions (stride, blue highlight, fade, tooltip, width-measured-on-repaint) because it is the same gesture, but keeps its **own** `lastModGridWidth`: the two sections need not be the same width and one shared field would let each overwrite the other's column count every repaint. Direction is shown by the same +/- badge `DayEditorTicketCardPreview` draws on a card, so it is read off the tile instead of a checkbox. **Two things are deliberately not silent.** A modification the owning food does NOT offer — authored earlier, or stranded by changing the food afterwards — is surfaced in a warning list with an x rather than dropped, because it still ships in the Day JSON and the designer has to see it; this is the same call `DayEditorMainDishWeight.FromJson` makes for a deleted `FoodItemConfig`. And the badge falls back to a `+`/`-` label when `TicketCardVisualsConfig` is unassigned, because direction is the one thing on the tile that must never be invisible. NOTE the picker filters nulls out of `AvailableModifications` before drawing, so a deleted modification asset leaves a gap in the food's list rather than a blank tile. **Not verified in Unity**: the editor is open, so this is confirmed only by clean `dotnet build`s on both assemblies and by the four direction tests, which have not been executed either — the grid's layout, the click/right-click split and the badge placement all need a Day Editor pass — affects: DayEditorModel.cs, DayEditorModelConverterTests.cs, codemap-editor.md, codemap-core.md — 2026-08-24

D-055: A Day authors **how many tickets of each patience type** Generate lays down, and lays them down **Patient first, then Normal, then Impatient**. New: `TicketGenerationJson.patient/normal/impatientTicketCount`, `DayContentGenerator.BuildPatiencePlan`, a "Patience Mix" box with a live readout in the Day Editor — rationale: the user asked to choose the mix instead of getting a uniform roll, and to have the sequence ordered. **This closes a gap the code had flagged itself**: `TicketFactory.PickRandomPatienceType`'s own comment reads "which PatienceType a ticket gets is an unspecified balancing decision (GDD never states a distribution). This uniform roll is a placeholder, not a design choice." **The counts do NOT become a second authority on how long a Day is.** `TicketsRequiredForDay` stays that, and the three counts are measured against it: a shortfall pads with Normal, an excess is cut off the tail — which takes Impatient first, then Normal, so over-authoring loses the harshest tickets rather than the Patient ones that were asked for. The user chose this over a remainder-style third field and over proportional weights. **The fixup is not silent, and that is the point**: `DrawPatienceMixReadout` states the effective counts before Generate is ever pressed, and it gets them by *calling* `BuildPatiencePlan` rather than repeating its arithmetic — a preview holding its own copy of the rule is a preview that eventually lies. **The mismatch warning is deliberately not a `DayValidator` rule.** `DayValidator` validates a `DayDefinition`; these counts live in `editorMeta`, which the runtime never reads (D-006, and `DayCatalogParser` was checked — it only reads the `runtime` block). Putting them into `DayDefinition` so the validator could see them would invert that arrow, so the warning sits in the section where the designer is typing. **The ordering needs no sort pass**: the plan is built in `PatienceOrder` and handed to the loop in sequence, so the output is sorted by construction. `PatienceOrder` is written out explicitly because `PatienceType` declares `Impatient, Normal, Patient` — the exact reverse — and iterating the enum would have opened every Day with its harshest tickets. **All three counts at zero means UNAUTHORED, not an empty Day**, which is the `DayJson` "tell unauthored from authored zero" rule: a Day file written before these fields carries none of them and reads them as 0, so `BuildPatiencePlan` returns an empty list and `GenerateCore` falls back to the old roll. The feature is therefore opt-in per Day and no already-authored Day changes balance; already-generated sequences were never at risk either, since each ticket carries its own `patienceType` and the counts only steer the next Generate. NOTE `BuildPatiencePlan` is public for the tests and the readout, and is the only place the rule exists. **Not verified in Unity**: the editor is open, so this is confirmed only by clean builds across DaySystem, Core, TicketSystem, Assembly-CSharp, Editor and the test assembly, plus eight new tests that have not been executed — the box's layout and the readout's wording need a Day Editor pass — affects: DayJson.cs, DayContentGenerator.cs, DayEditorModel.cs, DayContentGeneratorTests.cs, DayEditorModelConverterTests.cs, codemap-core.md, codemap-editor.md — 2026-08-24

D-056: A COMPLETED day pays the player one Gem per star it scored, at an authored rate (`GameConfig.GemsPerStar`, set to 1 in `GameConfig.asset`), awarded in `GameManager.OnDayCompleted` immediately before `SaveProfile()` — rationale: the user's ask ("kazandığımız yıldızlar gem olarak gelsin bize her yıldız 1 gem"). Four things worth keeping visible. (1) **Gems had no earn path at all until now.** `Wallet` could only ever subtract them (paid Continues), so `WalletTests` could not even tell "untouched" from "spending not refunded" by balance. `Wallet.EarnGems` is that path, and it is one-directional exactly like `EarnSoftMoney` — a negative amount is ignored rather than quietly subtracting — so it can never become a second spending route around `TrySpendGems`' affordability check. That also makes a 0-star day, or an authored rate of 0, a no-op instead of a case the caller has to remember. `Wallet` stays the compiler-enforced single writer of both balances; no new dependency arrow exists, since Bootstrap already reaches both DayLifecycle and ProgressionSystem. (2) **The rate is authored, the star count is not.** `DayLifecycleManager.StarCount` (3 minus lives lost, D-008) is a RULE and stays in code; what a star is WORTH is a balancing number and would violate the root invariant if compiled in. It lives on `GameConfig` rather than `EconomyConfig`, repeating D-026's reasoning verbatim — `EconomyConfig` is the per-ticket tip math consumed by `EconomyCalculator`, `GameConfig` is the asset both scene roots already hold — and `GameConfig.asset` was given `gemsPerStar: 1` in the same task, because a new serialized field reads 0 out of an existing `.asset` (the trap behind the stale `EconomyConfig.asset` fixed in 50be70c). (3) **The trigger carries the correctness, and it was already there.** `TicketSlotManager.IsDayComplete` guards the `DayCompleted` publish, so the handler fires exactly once per attempt and only on the winning path: no double payment, and a failed or abandoned day pays nothing — which is the right answer anyway, since it earns no stars. Ordering against `SaveProfile` is load-bearing: award first, or the gems the player watched land in the HUD are absent from the file they relaunch into. (4) **Replaying a finished day does not farm gems.** Earned gems are deliberately NOT recorded in the per-day spend ledger, so `RevertToDayStart` — which `RetryCompletedDay` calls before the redo — restores the day-start snapshot and takes them all back; the second attempt's stars therefore REPLACE the first attempt's rather than stacking, and the atomic-day rule (D1) reaches the new currency for free, including the clamp when a Continue was funded by the day's own gems. NOT DONE deliberately: no "+N gems" row on the Day Complete popup — it would need new hand-dragged Inspector fields, and the HUD's `GemsView` already ticks up at the instant the popup opens. **Not verified in Unity**: the editor is open, so this is confirmed by clean builds of `Assembly-CSharp` and the EditMode test assembly plus six new `WalletTests` cases that have not been executed — affects: GameConfig.cs, GameConfig.asset, Wallet.cs, GameManager.cs, WalletTests.cs, codemap-core.md — 2026-08-24

D-057: A day's earnings are no longer credited as they are made — nothing reaches the player's balance during a day, and the Day Complete popup PERFORMS the payout: the earned stars seat one at a time, each releases a gem that flies to the HUD gem counter, then coins appear around a small circle at the receipt's Total and fly to the coin counter, with every landing icon crediting its own share — rationale: the user's ask ("bölüm başarıyla bitirilene kadar para ve gem artmasın... popuptan bir animasyonla... tween ile gitsin"). Five things worth keeping visible. (1) **The animation IS the payout, not a skin over one.** The alternative — credit at `OnDayCompleted` and make the HUD views lag behind the real balance — was rejected because it needs a "hold the display" mode on `SoftMoneyView`/`GemsView` that must be armed BEFORE `GameManager.OnDayCompleted` runs, and subscriber order (GameManager subscribes `DayCompleted` in `OnEnable`, the popup in `Start`) is the wrong thing to hang money on; `EventBus.Publish` is synchronous and this project has been bitten by cascade ordering before. Crediting on each landing instead means the HUD views needed NO change at all — they are already reactive, so what the player watches arrive and what they own are one event rather than two kept in step. (2) **`DayRewardPurse` holds a DEBT, which is why it does not break the single-writer invariant.** Its numbers have never been in the wallet; `Wallet` still owns both balances, and `GameManager` — the only class holding both — pairs every `Take` with exactly one `Earn`. Every `Take` returns what was ACTUALLY taken, so a miscounted animation can underpay but can never mint money the day did not make, and `ShareOf` differences two running totals so the shares sum to exactly the total with no coin lost to rounding. (3) **It cannot lose the player money.** `AdvanceToNextDay` and `ReturnToMainScreenFromCompletedDay` commit the remainder before doing anything else, so an unwired `rewardFlight`, a skipped sequence, or a tap on Next Day mid-flight all pay in full; killing a tween deliberately does not run its `OnComplete`, so a destroyed icon never claims its share and the commit pays it as one lump. `RetryCompletedDay` DISCARDS the purse instead — a voluntary redo voids the debt, and `RevertToDayStart` takes back whatever the flight had already handed over, which together are what stop a finished day being farmed for its reward. `OnDayCompleted` still saves, and still saves none of the reward: what that write banks is lives and the day index, so a force-quit on the popup costs the ~2s of money in flight and nothing else. (4) **A real gameplay consequence, accepted rather than hidden: a paid Continue can no longer be funded by the day's own earnings**, because those earnings do not exist until the day is completed. It is forced by the ask and it agrees with the atomic-day rule (D1), but it does make Continues strictly harder to afford — revisit `LivesConfig`'s two prices if that bites. (5) **No new asset, no prefab.** The flying icons are built in code from two serialized sprites (13 per completed day is event frequency; pooling would be cost with no saving), and the feel knobs are serialized fields on the one component that reads them rather than a `RewardFlightConfig` with a single instance — the same shape `BoardAnimationConfig`'s own knobs have, so the "content is never embedded in code" invariant is satisfied without a hand-authored GUID. **Not verified in Unity**: the editor is open, so this is confirmed by clean builds of `Assembly-CSharp`, `ProgressionSystem` and the EditMode test assembly (the new test file was not yet in Unity's generated csproj, so it was compiled through a temporary clone and the compiler command line checked for it) plus 14 new `DayRewardPurseTests` cases that have not been executed — and the whole animation is unwired until the seven Inspector references are dragged, listed in the report — affects: GameManager.cs, DayCompletePopupView.cs, DayRewardFlightView.cs, DayRewardPurse.cs, DayRewardPurseTests.cs, blueprint.md, codemap-core.md, codemap-ui.md — 2026-08-24

D-057: The Start Board cell's item picker is a **visual tile grid fed from the Day's own food pool**, replacing a raw unfiltered `ObjectField`; the tile-grid geometry the window's now-three grids share moved into one `DrawTileGrid` — rationale: the user asked for the cell picker to look like Food Selection and the ticket's modification picker instead of hunting through ScriptableObjects. **The visual change carried a real fix with it.** That `ObjectField` was completely unfiltered — not even category- or pool-restricted, unlike the ticket's own Main/Side/Drink popups — so it could place food on the board that this Day's Food Selection excludes, which `DayValidator` then refuses (it checks the board against the same `AllowedFoodPool`). The editor was authoring data its own validator rejects; feeding the grid from the pool closes that without a separate rule. Clicking the selected tile again empties the cell, which is what took over from the old "None" entry, and the existing Clear Cell button stays. An item outside the pool — authored earlier, or stranded by deselecting it from Food Selection afterwards — is surfaced in a warning rather than silently cleared, the same call the modification picker makes, because it still ships in the Day JSON. **The extraction was declared before approval and bounded on purpose.** This was the third grid, and all three shared the "count columns from the width measured on the last repaint" scaffolding — which is precisely the code carrying this window's documented scrollbar bug (sizing against `EditorGUIUtility.currentViewWidth` forced a horizontal scrollbar onto every inspector row). Three copies would have scattered that lesson across three places. Only the geometry moved; **per-tile drawing stayed with each caller**, because they do genuinely different work (toggling a selection, drawing a direction badge, single-select) and only the measuring is common. NOTE each grid keeps its OWN `lastWidth`, passed by reference: the cell picker draws inside a fixed 220px column and settles on ~3 columns while the other two get the full content width, so one shared measurement would have them overwrite each other's column count every repaint — nothing inside `DrawTileGrid` may assume the inspector's width, and the 220px column was deliberately not widened because it sits beside the grid preview and this window's history is full of fixed widths causing overflow. NOTE the ticket's own Main/Side/Drink remain name popups (`DrawCategoryItemField`); the user did not ask for those and they were left alone, which is the obvious next step if they want it. NOTE the tile *chrome* (highlight, fade, sprite, tooltip) is now duplicated three ways and is the natural follow-up extraction — it was left out because the approved scope said geometry only. **No tests**: the change is drawing plus which pool feeds it, the add/update/remove semantics around it are untouched, and there is no new pure rule to pin. **Not verified in Unity**: confirmed only by a clean `ExpoTheExplorer.Editor.csproj` build and by grepping that the grid geometry now exists in exactly one place — how the grid wraps inside 220px needs a Day Editor pass — affects: DayEditorModel.cs, codemap-editor.md — 2026-08-24

D-058: The Start Board cell editor is **two-state**: an empty cell shows the whole grouped food grid, a filled one shows only the item's own tile and gives the room to that item's modifications — and nothing at all when it has none. New: `DrawModificationPicker`'s `hideWhenEmpty` — rationale: the user asked that clicking a food stop showing everything else, that only its modifications appear and only if it has any, and that clicking again or Clear Cell bring the whole larder back. **The cell is what selects the state, so there is no flag to keep in sync and nothing to reset.** That also means emptying needs no "go back" path of its own: clicking the selected tile again and the Clear Cell button both leave the cell empty, and the next draw is already the grid. The click branches are now separate too, which removed the compare-against-`current` subtlety the one-branch version needed — in the empty branch a click can only be a pick, and in the filled branch it can only be an unpick. `hideWhenEmpty` suppresses the header AND the "this food has no modifications" box, which is noise in a 220px panel; the ticket editor leaves it at the `false` default, where that same box is useful advice because it names the `FoodItemConfig` to go author them on — so the ticket side is unchanged by construction rather than by inspection. **One deliberate exception, and it matters more than the feature**: `hideWhenEmpty` never silences the orphan list. D-054 made modifications the owning food no longer offers visible precisely because they still ship in the Day JSON, and quieting them in order to tidy up an empty state would have undone that decision sideways. "Show nothing" means nothing to show, not nothing to admit. NOTE the assumption that the selected tile itself stays visible was stated before approval and is what makes click-again-to-empty reachable; hiding it too would leave Clear Cell as the only way out. **No tests**: the state is read straight off `entry?.Item` and the add/update/remove semantics around it are untouched, so there is no new pure rule to pin, and the rest is IMGUI. **Not verified in Unity**: confirmed by a clean `ExpoTheExplorer.Editor.csproj` build and by checking that the ticket editor's call site still passes no `hideWhenEmpty` — affects: DayEditorModel.cs, codemap-editor.md — 2026-08-24

D-059: A ticket's Main / Side / Drink are picked from **visual tile grids** (`DrawItemSlot`), and `Modifications` moved to sit directly under Main Item. `DrawCategoryItemField` is deleted, which removed the **last raw `ObjectField`** in `DayEditorModel.cs` — rationale: the user asked for the ticket's item slots to look like the cell picker and Food Selection, and for modifications to move under the main item. **Nothing about what is selectable changed.** The category filter and the `AllowedFoodPool` restriction are exactly the ones the name popup already applied — a Side still cannot land in the Main slot, a food this Day did not select is still not offered — so this is presentation only, and it is built out of `DrawTileGrid` and `DrawCellItemTile` rather than new drawing code. **The modifications move is structural, not cosmetic**: the picker is keyed on `entry.MainItem` because `TicketFactory` takes a ticket's modifications from the main dish and nothing else, so sitting between Side and Drink made it read as belonging to the whole ticket. Under the dish, the relationship is visible. **A deliberate inconsistency with D-058, stated before approval**: slot grids stay OPEN when filled, where a filled Start Board cell hides its alternatives. The common action on a ticket slot is SWITCHING (Hotdog to Burger) and hiding the rest would make that two clicks; the cell hides them because a filled cell hands its 220px of room to the modifications below, which a slot in the wide column does not need to do. Clicking the highlighted tile still clears the slot, which is what took over from the popup's "None" — Side and Drink are genuinely optional, so clearing had to stay reachable. **One narrowing, deliberate**: the deleted popup fell back to an unfiltered `ObjectField` when no catalog was assigned; that is now a HelpBox. Not a regression but an alignment — Food Selection and the cell picker already behaved that way, and with no catalog Generate is blocked too, so there is no meaningful work to do down that path. NOTE all three slots SHARE one `lastTicketSlotGridWidth`, which does not contradict the per-grid fields elsewhere: those exist because those grids sit in sections of different widths (the cell picker's fixed 220px column), while these three are drawn one after another in the same column, so one measurement is the correct one. NOTE a slot holding food outside the pool is surfaced in a warning instead of being injected into the option list the way the popup did — same intent, said out loud, and consistent with the cell and modification pickers. **No tests**: the selectable set is unchanged and the rest is IMGUI. **Not verified in Unity**: confirmed by a clean `ExpoTheExplorer.Editor.csproj` build, by grepping that `DrawCategoryItemField` and every `ObjectField` are gone from the file, and by reading back the final section order — how three grids stack in that column needs a Day Editor pass — affects: DayEditorModel.cs, codemap-editor.md — 2026-08-24

D-060: A day's **stars come from a score, not from a count of surviving lives**: `StarScore = clamp01(SavedSeconds / TotalTicketSeconds - wrongDeliveries x 0.20 - timeouts x 0.05)`, where `SavedSeconds` is the seconds still on each delivered ticket's clock, summed, and `TotalTicketSeconds` is every authored ticket's own time limit for that Day, summed. 3 stars at >= 0.55, 2 at >= 0.30, and **completing the day is worth 1** — rationale: the user asked for speed to matter ("biletlerin toplam süresini o gün için hesaplıycak ve sonra kullanıcının kaç saniyede bölümü tamamladığını hesaplıycak"), which D-008's rule ignored entirely: under it every mistake cost exactly one star no matter how the day was played, and playing fast bought nothing. Now a fast player can absorb one slip, which is the whole point of the change. **The user's stated form was wall-clock — total ticket time minus how long the level took — and it was replaced with per-ticket seconds after the problem was shown to them: three slots run CONCURRENTLY** (`GameState.TicketSlotCount`), so a day whose tickets total 390s (day_11: six Patient at 45 + four Normal at 30) is over in ~130-150s and `390 - 140` is hugely positive for everyone — every player would score 3 stars. Wall clock would need dividing by the slot count plus a tail correction (day_00's five Patient tickets have a floor of 90s, not 225/3 = 75s, because the last slots cannot stay 3-wide), and that correction would have to be re-derived every time the slot count or a Day's shape changed. Per-ticket seconds need none of it: they are immune to how many slots happen to be busy, they are already read at delivery for the tip tier, and they can be shown per ticket later. **The two penalties are deliberately unequal, and that asymmetry is the design**: a timed-out ticket contributes 0 to the numerator while its limit stays in the denominator, so it has already forfeited its whole share of the day's clock (~0.11 on a 10-ticket day) — charging it the full 0.20 again would bill one mistake twice. A wrong delivery costs a shake and a scatter, i.e. almost no time, so its bill has to be explicit or failing would be cheaper than being slow. The net cost of either lands near 0.20, which is roughly one star band. **Two rules survive from D-008 unchanged**: the ceiling is 3, and a day finished with >= `GameState.DefaultStartingLives` failures scores 0 — that is only reachable by paying to continue, which refills lives but deliberately does not reset the failure count, since it is still the same attempt. The completion floor is new: below the two-star threshold a COMPLETED day is worth its first star, because finishing is not nothing. **The four numbers live on a new `StarScoreConfig` asset**, not on `GameConfig`: `GameConfig.GemsPerStar` is what a star is WORTH once earned and stays there, while this is what EARNS one — and the star curve is the number a designer retunes dozens of times during balancing, which should not sit in the same asset as the board dimensions that must never move. The rule itself stays in code (`DayLifecycleManager`), where it is testable, exactly as D-008 left it. **Three single-authority moves came with it, none optional**: (1) the override-vs-patience time-limit rule moved out of `TicketEntryFactory` into `ResolvedTicketEntry.TimeLimitSecondsWith`, because `DayDefinition.TotalTicketSeconds` would otherwise be a second copy of it — one deciding the ticket the player plays, the other the score they are graded on; (2) the delivered ticket's remaining seconds ride on `DeliveryPayoutResult` rather than being re-read from the `Ticket`, so the tip paid and the time scored come from one measurement of one instant (and it is the last moment that number exists — the slot refills immediately after); (3) `GameManager` now hands `TicketSlotManager` and `TrayManager` two DIFFERENT named methods (`HandleTicketTimeout` / `HandleWrongDelivery`) instead of one shared `HandleLifeLoss`, so the cause is decided in the one class that composes both and no system had to learn what kind of failure it causes. Their signatures did not change. **The Day Complete popup shows the arithmetic** (time bonus / mistakes / net, as integer percents): a hidden score would leave a player who lost a star unable to tell which half of the day cost it. Those three rows are OPTIONAL serialized fields — the popup already exists in SampleScene and a required field would stop the whole receipt from appearing until the scene is wired; unwired they are skipped and `Start` logs it once. **The numbers are starting estimates, not balanced ones**: real `E` depends on when `BoardDistributor` puts items on the board, so the thresholds are meant to be re-set from play — which is why the popup shows the net score at all. **Verification**: `dotnet build Assembly-CSharp.csproj` clean (the new `StarScoreConfig.cs` IS in `ExpoTheExplorer.Data.csproj`, checked per the trap in the C#-verification notes); the EditMode suite could not be run from the session (Unity holds the project lock), so `DayLifecycleManagerTests` — rewritten with the score table, the wrong-delivery-vs-timeout pair, the clamps and the no-config case — is unrun. NOT verified in Unity: the two wiring steps (the config into GameManager, the three text rows into the popup) are the user's — affects: StarScoreConfig.cs, StarScoreConfig.asset, DayLifecycleManager.cs, EconomyCalculator.cs, DayDefinition.cs, TicketEntryFactory.cs, GameManager.cs, DayCompletePopupView.cs, DayLifecycleManagerTests.cs, DayTicketSlotManagerIntegrationTests.cs, TicketSystemTests.cs, ExpoTheExplorer/CLAUDE.md, fingerprint.md, codemap-core.md, codemap-ui.md — 2026-08-24

D-061: The star score is shown as a **fill bar the three stars stand on**, and the bar's fill is what **paces the star seating** — each star pops the moment the fill crosses its own notch — rationale: the user asked what UI the new score deserves and chose this over two flat readout layouts. D-060 left the rating a hidden number: a player who lost a star could not tell how close they had been, and three bare percentages under the star row read as "each star is worth this much" rather than as one line of arithmetic (that is exactly how the first pass looked on screen). A bar with the thresholds marked on it answers "where am I, where is the line, how much did I miss it by" in one glance, which no number does. **The bar is scaled 0..ThreeStarScore, not 0..1**: full has to mean maxed, and a bar that keeps filling after the last star has been earned communicates nothing. Consequences that follow from that choice: the three-star notch IS the right edge, the two-star notch sits at `TwoStarScore / ThreeStarScore` (0.55 at the shipped thresholds), and the **one-star notch is the left edge** — it is the completion floor, earned by finishing rather than by speed, so it is already lit while the bar is still empty. **No threshold position is placed by hand.** `DayLifecycleManager` derives both numbers (`ScoreProgressToMaxStars`, `TwoStarProgressPosition`) from the config it is already the single reader of, and `StarScoreBarView` writes them as ANCHOR fractions on three marker RectTransforms that the star objects are parented under — so retuning `StarScoreConfig` moves the notches AND the stars, with nothing to re-drag, and a Canvas Scaler cannot drift them the way a pixel position would. Hand-placing them would have been faster to build and would have started lying at the first tuning pass, which is guaranteed to happen (D-060 shipped estimates on purpose). **The pacing change is the actual feature**, not the bar: `DayRewardFlightView`'s star loop used to wait a fixed `starSeatInterval` between stars; it now waits on `CurrentFill >= PositionOf(i)`. Two animations that merely happen at the same time do not say "my speed earned that star" — one driving the other does. The gem-per-star payout is untouched (shares are still per star index), so the money cannot drift out of this. Three guards, each for a way the wait could hang a coroutine that is HOLDING the day's payout: the wait also breaks on `IsFillFinished`, `CurrentFill` reads 1 when the fill Image is unwired, and `PositionOf` returns the right edge for an out-of-range index rather than a free pass at 0. **Everything is optional and degrades to D-057's behaviour**: an unwired bar leaves the fixed-interval rhythm verbatim, so the scene keeps working before it is built and the popup's own validation is untouched (the bar joins the three D-060 percent rows in the one-line "not wired" log rather than becoming a required reference). It is its own component rather than more fields on `DayCompletePopupView` because the FLIGHT has to talk to it directly and the popup does not own the flight's sequence; the popup hands it the two numbers, the flight drives it. One ordering subtlety is guarded explicitly: `Play` begins by finishing any previous performance, which on a REDO of a completed day would snap the bar to the new target for one frame before the fill it had just reset began — hence `fillStarted`, and hence two named methods (`SnapToTarget` cuts a performance short, `ShowTargetWithoutAnimating` is the broken-reference path where none can happen). `Prepare` runs before `popupRoot.SetActive(true)` on purpose: anchors resolve on activation, unlike the flight's own position measurements, which need the layout to exist first. **Verification**: `dotnet build` clean on both `Assembly-CSharp.csproj` and `ExpoTheExplorer.Tests.EditMode.csproj` (`StarScoreBarView.cs` IS in the generated csproj, checked per the new-file trap); four new EditMode cases cover the two derived numbers, the retune-moves-the-notch case, the zero-threshold divide and the no-config case — unrun, Unity holds the project lock. NOT verified in Unity: the bar does not exist in `SampleScene` yet — building it and re-parenting the three stars under the markers is the user's editor step, and until then the popup runs on the old rhythm — affects: StarScoreBarView.cs, DayCompletePopupView.cs, DayRewardFlightView.cs, DayLifecycleManager.cs, DayLifecycleManagerTests.cs, ExpoTheExplorer/CLAUDE.md, codemap-ui.md, codemap-core.md — 2026-08-25

D-062: **A star's position is the scene's business; no code writes one.** D-061's marker mechanism — three RectTransforms whose x anchors were written at runtime from StarScoreConfig, with the star groups parented under them so each star stood on its own threshold — is REMOVED, on the user's instruction after seeing it on screen: "yıldızlar hep oldukları yerde kalsınlar hareket etmesinler hiç" — rationale: the mechanism worked exactly as designed and the design is what was rejected. Three 100px stars spread across a 450px bar stop reading as a three-star rating: the row's gestalt is the thing players recognise, and standing the stars on their thresholds traded it away for information (how far the next star is) that the user did not want to buy at that price. Two cheaper fixes were offered first and declined -- shrinking the stars, and insetting the fill's track so the outer stars stop hanging off the bar's ends (the user had already re-parented the markers under Fill for that second one). **What survives is the half that carries the meaning without moving anything**: the fill still travels, and `DayRewardFlightView` still waits on `CurrentFill >= PositionOf(i)` before seating each star, so the star still pops at the moment its threshold is crossed. That is timing, not movement, and it keeps "my speed earned that star" legible with the stars sitting in whatever row the scene puts them in. `PositionOf` therefore stops being a place on screen and becomes purely a moment in the fill's travel; `DayLifecycleManager.TwoStarProgressPosition` keeps its job of saying WHEN, having lost its job of saying WHERE. **The removal is a writer deleted, not a feature flag**: leaving the fields in place and simply unwiring them in the Inspector would have "worked" (PlaceMarker no-ops on a null marker) and was rejected as a state nobody can read -- the next person to wire those three slots would silently get moving stars back, and with all three markers authored at the same point (0,0 inside Fill, as the scene had them) clearing the slots would have stacked the three stars on top of each other. Deleting the fields makes the old behaviour unreachable rather than one drag away. **Cost of the reversal, stated plainly**: the visible link between a star and the threshold it stands on is gone, and the bar no longer marks its thresholds at all -- it shows progress toward three stars and nothing else. If that link is ever wanted again, the honest form is small notch TICKS positioned from the config (marks may move; stars may not), not a return to moving the stars. **Verification**: `dotnet build Assembly-CSharp.csproj` clean. No test changed: the two derived numbers and their four EditMode cases are untouched, since what was removed consumed them rather than producing them. NOT verified in Unity -- the user still has to lift Star1/2/3 out of the markers back into their row and delete the three empty markers; Unity drops the dead serialized fields on the next scene save — affects: StarScoreBarView.cs, ExpoTheExplorer/CLAUDE.md, codemap-ui.md — 2026-08-25

D-063: A wrong delivery left the player holding a broken item, and the fix is the same rule in three places: **nobody writes to a transform whose owner is mid-gesture, and nothing half-initialised is interactable.** The bug was one trigger with two faces — an item dropped into a tray raised the counter to `1/2` but drew nothing on the plate, and the item being held sat pinned up-and-left of the cursor instead of following it. Both come out of the 0.5 s window `WorldTrayView.OnTraySlotScatterBegin` opens by calling `BeginFlyInOverride(trayPosition, ScatterShakeDuration)` so the scattered items stay hidden while the tray shakes. **Face one, a scale-zero snapshot.** For that whole delay `BoardView.RefreshCell` leaves each scattered container active, `CurrentItem` assigned, parked on the tray at `localScale = Vector3.zero`, with its collider live — a stack of invisible but fully grabbable items exactly where the finger just released. `ApplyPickupVisuals` then snapshotted `homeScale`/`homePosition` FIRST and called `transform.DOKill()` second, so it captured that zero and cancelled the tween that would have undone it; `OnEndDrag`'s own `localScale = homeScale` re-applied the zero after `PlaceInSlot`, so the item stayed invisible in a slot `TrayManager` had correctly counted. Fixed by completing rather than cancelling, and before the snapshot: `transform.DOKill(true)`. Every tween that can be live on a board item at pickup — fly-in, snap-back, tray-settle — ends at that item's real resting place at full scale, so completing them is what makes the snapshot mean anything. **Face two, two writers on one transform.** `PlayWrongOrderShakeThenScatter` collected the tray's slot children once and then wrote their world X every frame for the full 0.5 s, snapping them back on complete, never rechecking whether the player had since picked one up. `OnDrag` set X from the finger, the next `OnUpdate` overwrote it back toward the tray, and Y kept following normally — and since a held item hovers a full cell above the finger by design (`DragFeelConfig.offsetFraction: 1`), the result read as "stuck up and to the left". Fixed with `IsShakeable`, checked EVERY FRAME rather than filtered once up front, because the pickup happens DURING the shake; `finalItem` is deliberately given a null handler so it still shakes, since it is being released rather than held even though its `OnEndDrag` has not run yet. **The third fix is the one no symptom pointed at.** `RefreshCell` read a reused container's fly-in destination straight off `container.position`, on the reasoning that a reused container "never moved". That holds only while every tween that ever moved it ran to completion, so a single interrupted fly-in stranded the container and then baked that stranded spot in as the cell's destination for good. It now recomputes from `CellPosition(x, y, -0.1f)` — the cell is the authority on where its item belongs, not the transform. Paired with holding the collider disabled for the length of `flyInDelay`, which closes the window rather than just surviving it. **What was NOT changed, and why.** `TrayManager`/`TraySlot` are correct throughout: the counter read `1/2` because the item genuinely WAS accepted — the model was right and the view was lying, which is why every fix landed in the view. **One narrower window is left open and is not a regression**: an item picked back up out of a tray mid-shake has already had a copy scattered to the board by `ScatterBackToBoard`, so the player ends up one item ahead. Closing that means changing who owns the scatter, which this task deliberately did not touch. **Verification**: `dotnet build Assembly-CSharp.csproj` clean, 0 errors (the single CS0618 `ContactFilter2D.NoFilter()` warning is pre-existing, on an untouched line). No EditMode test changed or added — all three fixes are MonoBehaviour/DOTween view code with no seam the EditMode suite can reach. NOT yet verified in Unity: the reproduction is manual (deliver a wrong order, then immediately grab an item near the tray). **Rests on one engine assumption**: DOTween's `DOKill(true)` completes a tween still inside its `SetDelay` rather than discarding it. If that is false, the collider gate closes the same hole anyway and the `DOKill(true)` degrades to a no-op rather than a regression. **Found on the way, not fixed**: `Data/BoardAnimationConfig.asset` carries only 4 of its class's 16 serialized fields — every `scatterShake*`, `delivery*`, `ticket*` field and `popInJumpPower` is absent, so those 12 run on their C# initializers and cannot be tuned in the Inspector until the asset is re-saved in Unity (fourth instance of this trap after D-004, `EconomyConfig`, `TicketCardVisualsConfig`); `GameOverPopupView.cs:55` throws `NullReferenceException` in `OnDestroy` on every play-mode exit; and `UpdateHoveredTray`'s fixed 8-collider `OverlapPoint` buffer can fill up with stacked tray items and miss the tray's own hitbox, silently breaking the highlight and the `OnEndDrag` drop fallback — affects: BoardItemDragHandler.cs, BoardView.cs, WorldTrayView.cs, codemap-ui.md — 2026-08-25

D-064: **The lives HUD is a row of heart sprites, it lives only in the day scene, and lives stop persisting.** Three changes the user asked for together, and the third is the one with teeth. **(1) `LivesView` was rewritten** from a TMP label rendering `"3/3"` into a heart-row driver. The user had already built and saved the row: `HUDCanvas`'s prefab INSTANCE in `SampleScene` carries one added object, `LowerPanel` → `HeartPanel` → `Heart{1,2,3}`, each an `Image` on `EmptyHeart.png` with a single `FilledHeart` child on `FullHeart.png`. **That hierarchy dictated the API**: the empty heart is the PARENT and the filled heart its CHILD, so switching a parent off would take its filled child down with it — the view takes ONE `GameObject[] filledHearts` and an `emptyHearts` array would have been actively wrong rather than merely redundant. It binds a serialized `GameManager` and subscribes to `GameState.LivesChanged` **and** `MaxLivesChanged` from `Start`, caching the `GameState` it subscribed to so `OnDestroy` cannot unsubscribe from a different object. **(2) "Not on the main menu" needed no code at all, and that is the finding.** The obvious implementation — hide the row when there is no live session — is DEAD since D-022 gave the main screen its own `SessionHost`: `LiveState != null` is true in both scenes, so the menu can no longer be detected from the session. The user's structural answer sidesteps it entirely: the row is on the prefab INSTANCE, not the asset, so `MainScreen` never receives it. Verified from YAML — `HUDCanvas.prefab` is untouched by that edit and the instance's `addedObjects` is exactly `[LowerPanel]`. **The hazard this creates is real and is recorded rather than coded around**: Apply All / Apply Added GameObject on that instance would push the row into the asset and put hearts on the menu, the same shape as the `HudWalletSource` override trap. The old readout it replaces is `UpperPanel/HealthUI` (`HeartImage` + `HealthText`) INSIDE the prefab, which is what was putting lives on both screens; deleting it is a hand step for the user. **`HudWalletSource` lost `Lives`/`MaxLives`/`LivesChanged`** — `LivesView` was their only reader and the save-file branch behind them died with the profile field, so the class is now exactly what its name says. `HudCanvasPrefabSetup` wires two views, not three: leaving `LivesView` in its target list would not merely be dead, it would make `FindProperty("walletSource")` miss and log the tool's own out-of-step error on every run. **(3) Lives stopped persisting, which required overturning a documented rule.** `PlayerProfile.Lives` is gone, `CurrentVersion` 5 → 6 — the first version to REMOVE a field, and it carries no upgrade branch by design, since `JsonUtility` drops a JSON key with no matching field. `GameSession` neither applies nor writes it; `LivesManager.ApplyPersistedLives` was deleted (single writer, one fewer entry point). The rule overturned: **`AdvanceToNextDay` used to skip `LivesManager` deliberately** — its comment read "Lives are NOT reset on a successful advance, only a failed retry pays that cost" — and it now refills like every other day-start path. Leaving the skip would have split behaviour by ROUTE rather than by rule: menu-then-Play giving three hearts while Next Day kept a half-empty row, for no reason a player could see. `LivesManager.RetryDay` → **`RefillForNewDay`**, because with the advance path calling it, three of its four call sites were no longer retries; `GameManager.RetryDay` keeps its name, being a genuine retry. Two D-014 ordering rules are now dead but their lines stay with comments saying so: `ReturnToMainScreenAbandoningDay`'s refill-before-save (the save can no longer carry a life count) and `RetryCompletedDay`'s `SaveProfile`-last. **Tests**: the three `ApplyPersistedLives` clamp cases went with the method, replaced by `NewState_OpensAtAFullBar` — the one thing that now decides a day's starting lives; `Lives_ComeFromTheProfile` was INVERTED into `Lives_IgnoreTheProfile_AndOpenAtAFullBar` rather than deleted, because "the load path stopped reading a value" is exactly what a later save-field addition could quietly undo; and `Save_WritesNoLivesKey` asserts against the file's raw TEXT, because a reloaded object cannot tell an ignored key from an absent one. The v3 payload deliberately still carries `"Lives":2`, which is what proves dropping an unmatched key is safe rather than assumed. **Verification**: `dotnet build ExpoTheExplorer.Tests.EditMode.csproj --no-incremental` and `dotnet build Assembly-CSharp-Editor.csproj --no-incremental` both clean, 0 errors (4 pre-existing CS0649 warnings in an untouched `TicketGenerationConfig.cs`). **NOT run**: the EditMode suite itself, which needs the Unity Editor — the save-format change is the part that most wants it. **Not done, and it contradicts the code**: `ExpoTheExplorer/CLAUDE.md` Section 3 still states that lives persist and that `CurrentVersion` is 4; it is outside this task's approved manifest — affects: LivesView.cs, HudWalletSource.cs, HudCanvasPrefabSetup.cs, GameSession.cs, PlayerProfile.cs, PlayerProfileStore.cs, LivesManager.cs, GameManager.cs, LivesSystemTests.cs, PlayerProfileStoreTests.cs, GameSessionTests.cs, codemap-ui.md, codemap-core.md, codemap-editor.md, blueprint.md — 2026-08-25

D-065: **Keys — the meta resource that gates playing.** Plan: `.claude/key-plan.md` (5 steps); this entry records step 1, the model, which ships `KeyConfig` + `KeyManager` + 27 tests with **no caller anywhere in the game**, so behaviour is unchanged and no gate closes. **The four rules are the user's, given as answers this turn**: cap **5**; 40 Gems **fills to the cap** rather than adding 5, so the cap is never exceeded and there is one rule instead of two; a key is spent **only when leaving a LOST day** (Retry or Main Menu), which makes a paid Continue free and makes starting a day a GATE rather than a price; and the 30-minute clock **runs while the game is closed**, with backward-clock protection. **Keys are not lives, and the distinction is the whole design**: D-064 had just made lives a per-day allowance that resets every morning and is never saved, so the thing that persists and paces the player needed a home of its own. Neither reads the other; they are adjacent in the HUD and nowhere else. **The count lives in a PRIVATE field on `KeyManager`, not on `GameState`** — a deliberate departure from `Lives`, whose public setter leaves its single-writer rule resting on a comment, where here the compiler holds it; and `GameState` is the central state of a DAY, while a key outlives the day, the scene and the session. The step-1 verification compiled `KeyManager` under its real assembly name against the real `Core.dll`, which proves the other half: **it is a compile error for this class to assign `GameState.Gems`**, so the 40 Gems necessarily go through `Wallet`, still their single writer (new one-directional arrow `KeySystem → ProgressionSystem`, identical in shape to `LivesSystem`'s). **The clock is injected** as a `Func<DateTime>` defaulting to `DateTime.UtcNow`, and this is the project's **first and only** wall-clock dependency (a scoped grep confirmed zero prior uses of `DateTime` in `Scripts/`). That is not decoration: every rule in this class is a time rule, and a class reading the clock directly would put all of them out of reach of a test — for a rule the player experiences as "when do I get to play again", untestable is not acceptable. **`Refresh` is idempotent and cheap and every mutating method calls it first**, so correctness never depends on a tick existing; a per-second `Update` in step 3 will be display only, and per-frame was rejected outright (≈60 clock reads/s for a value that changes 1800× less often). **Three rules a naive implementation gets wrong, each pinned by a test**: a FULL bar drags the anchor to `now` so idle time is never banked — otherwise a key spent after three idle days is handed straight back; a partial interval survives because the anchor advances by WHOLE intervals rather than to `now`, or every `Refresh` would rob the player of up to 30 minutes; and a BACKWARD clock pulls the anchor to `now` rather than freezing the player until real time catches up, which for a clock set months back is effectively forever. Forward clock abuse stays possible, is unsolvable client-side, and costs the player nothing but their own pacing. **`TryRefillWithGems` checks the cap BEFORE touching the wallet**, so a full player is never charged — there is no path that spends without granting. **The save format's `-1` marker is decided here even though persistence is step 2**: `Keys` is the third field where 0 is not a safe default (0 keys is a locked-out player, exactly v3-`Lives`'s trap), but `PlayerProfileStore` has no config and must not learn what a key means, so it writes `-1` for "this save predates keys" and `KeyManager.ApplyPersisted` resolves it to the cap. A zero anchor means "no timestamp", which is what keeps `System.DateTime` out of the store as well. **`KeyConfig.asset` is deliberately NOT written by the AI** — hand-written `.asset` YAML has silently shipped missing fields four times here (D-004, `EconomyConfig`, `TicketCardVisualsConfig`, `BoardAnimationConfig`) — so the C# defaults carry the authored numbers and the asset is created from the menu in step 2. **Verification**: the three new files are absent from Unity's generated `.csproj`s, so a plain `dotnet build` would have looked at none of them and reported success — a known way for this project's build check to lie. Compiled instead through three throwaway csprojs that reproduce the real assembly boundaries (`KeySystem` outside Core's `InternalsVisibleTo`): 0 errors, 0 warnings. `dotnet build Assembly-CSharp-Editor.csproj` and the EditMode package also clean. **NOT run**: the 27 EditMode tests, which need the Unity Editor — and for a step whose entire safety net is those tests, that is the gap the user has to close. **CORRECTION, same turn**: the verification described above was INCOMPLETE and the user hit it — Unity refused to compile with CS0234/CS0246, because `ExpoTheExplorer.Tests.EditMode.asmdef` lists its references EXPLICITLY and `KeySystem` was not among them. The three throwaway csprojs reproduced assembly BOUNDARIES faithfully (which is what proved the `Gems` write is a compile error) but not Unity's asmdef GRAPH, so they never asked whether the reference was DECLARED. Fixed by adding the reference; proved by rebuilding Unity's own generated Tests csproj with the one `ProjectReference` Unity will add on its next regeneration — clean. A repo-wide check now backs it: every `using ExpoTheExplorer.*` across all 16 asmdefs resolves to a declared reference. **Standing rule from this**: adding an asmdef means editing the reference list of every asmdef that consumes it, in the same task — the symptom otherwise surfaces in the TEST file, far from the new system. It also forced a real map repair: the test asmdef's codemap line carried `sys: ?`, defended in its own note as honest because the assembly belongs to no single system; that is an argument for a shared owner rather than a question mark, so blueprint gained a `Testing` entry and the line now names it. **Also fixed here, a debt from last task**: `fingerprint.md` and `ExpoTheExplorer/CLAUDE.md` still asserted that lives persist and that `CurrentVersion` is 4; both were false, and this system is built directly on that save format — affects: KeyConfig.cs, ExpoTheExplorer.Systems.KeySystem.asmdef, KeyManager.cs, KeySystemTests.cs, codemap-core.md, blueprint.md, fingerprint.md, key-plan.md, CLAUDE.md — 2026-08-25

D-066: **Keys persist — save v7, and the `-1` marker that keeps the file boundary ignorant.** Step 2 of `.claude/key-plan.md`; D-065 built the model, this wires it to the session and the save file. **Still nothing on screen and no gate closed** — the only player-visible change is that an invisible counter now survives quitting. `PlayerProfile` gains `Keys` and `LastKeyRegenUtcTicks`; `GameSession` builds `KeyManager` and both writes and restores it; `GameManager` and `MainScreenRoot` take a required `keyConfig`. **The marker is the decision worth recording.** `Keys` is the third field where 0 is not a safe default (after v3's `Lives` and v5's `LastCelebratedDayIndex`), but it differs *in kind* from both: every earlier migration KNEW the right answer, and this one does not — the answer is `KeyConfig.MaxKeys`, and `PlayerProfileStore` has no config reference and must never gain one, since that would both break its file-boundary role and put a second copy of a balancing number in the codebase. So `UpgradeToCurrent` writes `-1`, the marker crosses the boundary, and `KeyManager.ApplyPersisted` — the one place the cap is known — resolves it. **0 could not double as "absent"** because it is a real, reachable count meaning "locked out, go wait"; had the upgrade written 0, every existing player would have launched unable to start a day with nothing on screen explaining why. `NewPlayer` takes the identical path, because "never played" and "played before keys existed" deserve the identical answer. `PlayerProfileStore.KeysAbsentMarker` is public so the value has one name across the upgrade, `NewPlayer` and the tests — but the reading end deliberately accepts ANY negative rather than referencing the constant, so a hand-edited `-2` means the same thing and cannot lock someone out. **The anchor needs no migration at all**: `0` means "no timestamp recorded" and the load path starts the clock then, which is also what keeps `System.DateTime` out of the store. It is stored as TICKS because `JsonUtility` round-trips a `DateTime` as `{}` with no error — the same trap `PlayerProfile` already documents for auto-properties and `HashSet`. **`Save` writes BOTH halves**, and that pairing is load-bearing: persisting the count alone would restart the interval in flight on every launch, handing a player who quit 29 minutes into a wait a fresh 30 every single time. **A fifth ordering rule** joins `GameSession`'s four: `KeyManager` is built after the `Wallet` (the refill is charged through it) with `ApplyPersisted` immediately after. `keyConfig` is a REQUIRED ctor parameter rather than optional — an optional one leaves a null `KeyManager` and pushes a null check into every future caller — so both roots guard for it, `GameManager` before building the session (otherwise the failure is a `NullReferenceException` from inside a constructor rather than a sentence naming the field), and `MainScreenSessionSetup` fills it plus WARNS when it could not, because an unfilled field takes the whole main screen down rather than degrading one widget. **Verification**: the asmdef trap from D-065 was checked FIRST this time and `ExpoTheExplorer.Session.asmdef` did need `KeySystem` — caught before writing a line of code rather than by the user's compiler. The repo-wide asmdef graph check passes across all 16 assemblies. Because Unity generates `.csproj` from asmdefs and had not regenerated yet, the real `Session.csproj` was patched in place with the `ProjectReference` Unity will add (the file is gitignored, so Unity overwrites it anyway) and the FULL tree built clean: `Assembly-CSharp-Editor` (covering `GameManager`, `MainScreenRoot`, the setup tool) and the EditMode package, 0 errors. **NOT run**: the EditMode tests, which need the Editor. **Still owed by the user before this step is real**: `KeyConfig.asset` does not exist yet — it is created from the menu, deliberately not written as YAML by the AI (four prior silent field-drops: D-004, `EconomyConfig`, `TicketCardVisualsConfig`, `BoardAnimationConfig`) — and until it is created and dragged into both scene roots, both scenes refuse to build a session and say so — affects: PlayerProfile.cs, PlayerProfileStore.cs, GameSession.cs, ExpoTheExplorer.Session.asmdef, GameManager.cs, MainScreenRoot.cs, MainScreenSessionSetup.cs, PlayerProfileStoreTests.cs, GameSessionTests.cs, codemap-core.md, codemap-ui.md, codemap-editor.md, key-plan.md, fingerprint.md, CLAUDE.md — 2026-08-25

D-067: **The key readout goes on screen, and it deliberately takes the OPPOSITE route to the heart row.** Step 3 of `.claude/key-plan.md`. `KeysView` renders "current/max" in the shared HUD prefab, `HudWalletSource` forwards the count and runs the display tick, `HudCanvasPrefabSetup` wires it. **Still no gate and no spend** — those are steps 4 and 5. **The routing decision is the substance.** D-064 gave `LivesView` a direct `GameManager` reference and took it *off* `HudWalletSource`; this step does the reverse for keys, and the two are consistent rather than contradictory because the widgets have opposite shapes: the heart row is a `SampleScene` object added to the prefab INSTANCE (which is what keeps hearts off the menu), so it can hold a scene reference; the key readout is prefab CONTENT shown on both screens, and **a prefab asset cannot store a scene reference at all** — the exact problem `HudWalletSource` was built for in D-013, whose `sessionHost` field is already the per-scene override. Both gerekçeler are written into the two files so the next reader does not "fix" one into the other. **Keys hang off the SESSION, not off `GameState`** (D-065 keeps the count private to `KeyManager`), which has two mechanical consequences in `HudWalletSource`: the subscribe sits OUTSIDE its live-vs-save-file branch, and the `OnDestroy` unsubscribe sits BEFORE that method's null-return — pairing it with the `LiveState` check would leak the subscription in exactly the case where a session exists but `LiveState` does not. **The display tick is display-only, by contract.** D-065 made `Refresh` idempotent and had every gate call it, so no rule anywhere depends on this tick existing; its only job is that the number climbs while the player watches. It runs **once per second, not per frame** — 60 clock reads a second for a value that moves every 30 minutes is three orders of magnitude of waste — and on **unscaled** time, so a popup that pauses the game by zeroing `timeScale` cannot freeze the wait being counted down. It lives on the source rather than the view so there is one ticker per scene, and because step 5's popup will want the same freshness. **A missing session renders NOTHING rather than a number.** Since D-022 both screens are meant to have a `SessionHost`, so no session is a lost reference, not a mode — and D-014 deliberately removed mode branches from views, which this does not reintroduce. The only number the view could invent is "0/5", which would tell the player they are out of keys: a far worse failure than a label left as the scene authored it. **No tests, structurally**: both files are in the predefined `Assembly-CSharp`, which an asmdef test assembly cannot reference (D-012). Every key RULE is already covered by `KeySystemTests`; what this step adds is render and tick. **Verification**: asmdef graph clean across 16 assemblies. `KeysView.cs` was initially absent from Unity's generated Compile list — the known way a plain `dotnet build` reports success without looking at a new file — but Unity was running and regenerated mid-check, so the passing build genuinely compiled it (confirmed by finding the `Compile Include` line and its `.meta`). `Assembly-CSharp-Editor` and the EditMode package both clean, 0 errors. Unity had also regenerated `Session.csproj` with the `KeySystem` reference D-066 predicted by hand. **Owed by the user**: a key SPRITE does not exist in the project yet (`Art/UI/` has no key image), and the prefab surgery — rename `HealthUI`→`KeysUI`, swap the sprite, add `KeysView`, run the HUD menu step — is manual, because prefab YAML is not written by the AI — affects: KeysView.cs, HudWalletSource.cs, HudCanvasPrefabSetup.cs, codemap-ui.md, codemap-editor.md, blueprint.md, key-plan.md — 2026-08-25

D-068: **Giving up on a lost day costs a key — and that forces a documented rule to narrow.** Step 4 of `.claude/key-plan.md`. Both exits from a lost day charge one key: the Game Over popup's Retry and its Main Menu. **No button is disabled and nothing is blocked yet** — step 5 adds the check, and per the user's correction this turn it will be a check AT CLICK TIME that opens a popup, never an `interactable = false`: a greyed button does not tell the player why, a popup does, and it can offer the two ways out (wait, or 40 Gems). **Gated on `State.IsAwaitingContinue`, not on which method ran.** The user's rule is that a key is spent when a day is LOST and then left, so the flag states it literally — and it keeps `DebugTicketDeliveryController`'s R key from eating a key when it replays a day that was going fine. `RetryCompletedDay` stays free, because a voluntary redo of a day that SUCCEEDED is not losing one. **Two ordering rules, both silent if broken**, which is why each is spelled out at the line: (1) the flag is read BEFORE `LivesManager.RefillForNewDay` clears it — read it after and giving up quietly becomes free; (2) `RetryDay`'s save is LAST, after `DayRetried` has already triggered `wallet.RevertToDayStart` — move it earlier and a failed attempt's earnings get banked, which is precisely the "a day attempt is atomic" contract this file exists to hold. **`RetryDay` writing to disk at all is the narrowing.** That path deliberately wrote nothing, on the reasoning that a failed day was never permanent. A key spend is permanent, and if it is not written the player can press Retry, force-quit, and have the key back — retries become free and the economy is decorative. What reaches disk is the ROLLED-BACK wallet, i.e. the same figure `ReturnToMainScreenAbandoningDay` already writes, so no new money is banked; only the key becomes real. D-012 narrowed the same rule once before, for the abandon path; this is the second and last narrowing. **The spend result is deliberately ignored**: at zero keys the player must still be able to leave, because blocking that exit is a softlock — `TrySpendKey` floors at zero and the exit proceeds. **One private helper, not two call sites**, because `GameManager` is unreachable from the EditMode suite (predefined `Assembly-CSharp`, D-012) and having exactly one copy of the rule to read is the only protection it has; `KeyManager` also gains a forwarding property beside `LivesManager`, which step 5's popup will read. **Verification**: `Assembly-CSharp-Editor` builds clean, 0 errors, and both call sites were re-read after the edit to confirm the flag is captured before the refill and the save lands after the revert. **Not testable here**, and that gap is real rather than acceptable-by-default: the *rules* of spending are covered by `KeySystemTests`, but *when* it is called is not covered by anything — affects: GameManager.cs, codemap-core.md, key-plan.md, CLAUDE.md — 2026-08-25

D-069: **The out-of-keys popup, and the gate that lives inside it.** Step 5 of `.claude/key-plan.md`, the last one — the key economy is now complete end to end. `NoKeysPopupView` explains the block and offers the only two ways forward (wait for the timer, or 40 Gems for a refill to the cap); `MainScreenView`'s Play and `GameOverPopupView`'s Retry ask it on every click. **The gate lives on the popup, not in its callers.** `HasKeyOrShow()` returns true when the player may proceed and otherwise shows itself and returns false, so there is ONE answer to "what happens when there are no keys" rather than two copies drifting apart — and neither caller needs a session reference, which matters because `MainScreenView` has never had one. The name states the side effect deliberately: a gate that silently opens a window would be a trap at the call site. **Nothing is ever disabled** (the user's explicit correction two turns ago): both buttons stay pressable and the check happens on the click, because a greyed-out control tells the player nothing about why, while the popup names the reason and hands them both exits. **It fails OPEN.** Missing references, or a scene whose `SessionHost` has no session, return true and log loudly at `Start` — a forgotten drag must never leave the player unable to play at all. An uncharged key is the smaller wrong by a wide margin, and it would otherwise be indistinguishable from a bug in the economy itself. **The close button is REQUIRED, as a softlock guard**: the popup only self-closes when a key ARRIVES, so a player with no keys and not enough Gems would otherwise be stuck in it for up to a full regen interval — the user caught this while reviewing the preflight, where it had been marked optional. **Main Menu is deliberately not gated at all**, for the same family of reason: blocking the only exit from a lost day at zero keys is a softlock, so it always works and simply spends nothing (D-068). **`Hide()` is deliberately not called on Retry's refused branch** — the Game Over popup has to stay behind the explanation, or refusing a retry would drop the player onto a finished day with no UI whatsoever. **Auto-closes on `KeysChanged > 0`**, because its single claim stops being true the moment a key lands: the player waits, the key arrives, the window gets out of the way and Retry works. It owns a 1-second **unscaled** redraw that runs only while visible; `SecondsUntilNextKey` refreshes for itself, so the popup is correct even in a scene with no HUD, and unscaled means a paused game cannot freeze the wait it is asking the player to sit through. A failed Gem purchase leaves the window OPEN — the only possible cause is insufficient Gems, since it is shown at zero keys — so the player keeps seeing the price they are short of. **One instance per scene rather than a shared prefab**: `sessionHost` is a scene reference, and a prefab asset cannot carry one (the lesson D-013 wrote down about the HUD). **Verification**: `NoKeysPopupView.cs` was absent from Unity's generated Compile list, which this time surfaced as a hard error rather than a silent pass because the new type is referenced — the csproj (gitignored, Unity regenerates it) was patched and the tree built clean, 0 errors. **No tests, structurally**: all three files are in the predefined `Assembly-CSharp`, unreachable from an asmdef test assembly (D-012); every key RULE is covered by `KeySystemTests`, and what this step adds is when the question is asked plus render. **Owed by the user**: the popup hierarchy is built by hand in BOTH scenes — root, message, countdown, Gem button, close button, optional not-enough-Gems label — then `NoKeysPopupView` is added and wired, and dropped into the `noKeysPopup` field on `MainScreenView` and `GameOverPopupView`. Scene YAML is not written by the AI (D-007) — affects: NoKeysPopupView.cs, MainScreenView.cs, GameOverPopupView.cs, codemap-ui.md, blueprint.md, key-plan.md, CLAUDE.md — 2026-08-25

D-070: **Haptics, as a system: eight authored moments, one vendor file, one buzz per frame.** Nice Vibrations 4.1.2 was imported by the user and is now wired to the game through a new `HapticsSystem`. **Which moment plays which haptic is content, not code** — `HapticConfig.asset` carries a row per moment (preset, priority, enabled), so retuning the feel is an Inspector edit and never a code change; `Reset()` seeds all eight, because a missing row is silent and silence is the hardest failure to notice. **The vendor is quarantined in exactly one file.** `HapticsBinder` is the only place in the project that names `Lofelt.NiceVibrations`; everything above it speaks in `HapticMoment`, and the config stores the project's own `HapticPreset` MIRROR of `HapticPatterns.PresetType`. That mirror is the deliberate cost of keeping `ExpoTheExplorer.Data`'s reference list EMPTY — which in this project is how "data depends on nothing" is said — and it is translated by a written-out nine-arm switch rather than a cast, so a renumbering upstream breaks the build instead of quietly playing the wrong haptic. Swapping the haptics vendor is now a one-file change. **The per-frame coalescer is a correctness fix, not an optimisation.** `EventBus.Publish` is synchronous and its handlers publish in turn, so moments routinely arrive together: a correct drop queues `ItemDroppedInTray` then `OrderDelivered`, the last life queues `LifeLost` then `GameOver`. Requests are collected and `HapticsBinder` drains them once in `LateUpdate`, highest priority winning and ties going to whoever asked FIRST — one frame of latency (~16 ms, below what a hand resolves) bought in exchange for one clear answer instead of a smear. **Two moments were left OUT for that same reason** rather than added and then lost to the priority rule: `TicketAssigned` (a delivery cascades straight into the next ticket's assignment) and `TraySlotScatterBegin` (a scatter only ever happens alongside a life loss, which outranks it). Coin landings are out too, on a different ground: their count scales with the day's earnings, so one buzz per coin turns the phone into a buzzer — if they are ever wanted they need a rule (first + last, or every Nth), not a call site. **The split between `HapticsService` and `HapticsBinder` is forced by the same predefined-assembly constraint as D-012**: the binder needs `GameManager`, which lives in `Assembly-CSharp`, and an asmdef cannot reference a predefined assembly — so the testable half is the half that made it into the asmdef, and `HapticsSystemTests` pins the priority rule against the two real cascades. **One component, two modes**: with `gameManager` wired (day scene) the binder subscribes to `TicketDelivered`/`LivesChanged`/`LivesDepleted`; left EMPTY on the main screen, which has no `GameState` at all, it is only a play surface the shop calls directly — an empty field there is a valid setup, not a mistake, so it is not validated. Life loss is read from `LivesChanged`'s DROP, so a refill or a paid Continue never buzzes. **Everything fails open**: an unwired config or binder costs a haptic and logs once, never a frame — the same stance D-069 took on the key gate, and for the same reason. **Verification**: `ExpoTheExplorer.Systems.HapticsSystem`, `Assembly-CSharp` and `ExpoTheExplorer.Tests.EditMode` all build clean, 0 errors, and — the trap this project has been bitten by twice — each new file was confirmed PRESENT in Unity's generated Compile list first, so the clean build is not the silent pass of a file nobody compiled. Unity's own log carries no `error CS` after the latest compile. **The EditMode suite was NOT run**: Unity holds the project lock, so it is owed from the Test Runner. **Owed by the user**: create `HapticConfig.asset`, and in BOTH scenes add a `HapticReceiver` (Nice Vibrations wants exactly one per scene, like an `AudioListener`) and a `HapticsBinder`, then drag that binder into `BoardView`, `DayRewardFlightView`, `MetaShopView` and `MetaGroundsView`. Scene YAML is not written by the AI (D-007) — affects: HapticConfig.cs, HapticsService.cs, ExpoTheExplorer.Systems.HapticsSystem.asmdef, HapticsBinder.cs, BoardView.cs, BoardItemDragHandler.cs, DayRewardFlightView.cs, MetaShopView.cs, MetaGroundsView.cs, HapticsSystemTests.cs, ExpoTheExplorer.Tests.EditMode.asmdef, blueprint.md, codemap-core.md, codemap-ui.md — 2026-08-25

D-071: **Haptics, second pass: seven more moments, and a button mechanism that touches no view.** D-070 shipped the system with the six moments that carry the game's core loop; this closes the list the user and the AI worked through together — button taps, the invalid drop, the reward ceremony's gem and coin landings, a refused purchase, a spent key, and the out-of-keys gate. **Buttons get a component, not a call site in seven classes.** `HapticButton` hooks its own `Button.onClick` and asks for a serialized moment, so `MainScreenView`, `DayCompletePopupView`, `GameOverPopupView`, `MetaShopView`, `MetaShopRowView`, `MetaGroundsView` and `NoKeysPopupView` were all left untouched. The deciding reason is mechanical rather than aesthetic: **the shop's BUY button lives on an INACTIVE template row that `MetaShopView` clones per offer**, so the obvious alternative — a `Button[]` list on `HapticsBinder` — would have missed the button most worth feeling, because a scene list cannot name objects that do not exist yet. A component on the template rides along with every clone, and since `Awake` never runs on the inactive template, only the live rows register. **Coins buzz first and last only, and that came out of arithmetic**: `coinSpawnInterval` is 0.06s with up to `coinCount` icons, so one haptic per coin fires faster than a preset lasts — the hand would read half a second of undifferentiated rumble rather than ten ticks. Two deliberate beats say it better: the money started arriving, and it has finished. Gems need no such rule (at most three, spaced by the star rhythm). Both are requested on ARRIVAL, inside `Launch`'s `onArrive`, because the buzz belongs to the counter ticking up. **`UiTap` sits at the bottom of the priority table (5) on purpose**, which is the coalescer earning its place a second time: one tap produces both the press and its consequence — BUY gives `UiTap` + `PropPurchased`, a refused buy gives `UiTap` + `PurchaseRefused`, Retry at zero keys gives `UiTap` + `BlockedByNoKeys` — and in every case the player feels the OUTCOME, with the tap surviving only when nothing else happened, which is exactly what it means. **New moments are APPENDED to the enum, never sorted in.** Unity serializes an enum field by value and every authored row in `HapticConfig.asset` stores one, so an insertion would silently re-point every later row at the wrong moment: an asset that still loads, still validates, and plays the wrong haptic for everything past the insertion. **A trap in the first pass was closed with code rather than an instruction**: `Reset()` only runs on a brand-new asset, so an asset created in D-070's turn would carry no rows for these seven and all of them would be silently dead. Telling the author to hit Reset would have worked and would also have discarded every preset they had tuned; instead an editor-only `OnValidate` GROWS the asset with any missing moment at its default and touches nothing already there. That is append-only, consistent with what the file already asks (silence a row by unticking Enabled, not by deleting it), and the runtime meaning of a missing row is unchanged because `OnValidate` does not exist in a build. **`GameManager` was touched, deliberately and minimally**: the key spend lives there and nowhere else, so `KeySpent` is one optional field and one line beside `TrySpendKey`. It is requested unconditionally rather than on that call's result, because at zero keys the spend floors and returns false and that is precisely when the player most needs telling; D-068's rule is untouched. **A known gap, stated rather than hidden**: most routes through `SpendKeyForLostDay` replace the scene, so the request can be lost before `LateUpdate` flushes it — a device check, not worth a special immediate path that would bypass the coalescer for one moment only. **`DropRejected` fires only on the two `OnEndDrag` branches that genuinely refuse**; the relocation branch is silent because moving an item to another empty cell is the drop WORKING, and `OnPointerUp`'s settle-back is silent because `ItemPickup` already fired on that press. **Deliberately still out**: the ticket-timer urgency pulse, the one remaining item from the design conversation and the only one needing new code (the card has no threshold-crossing event, and it is the haptic whose dose is easiest to get wrong) — the user chose to feel this batch on a device first. **No new test**: what this pass adds is moments, not rules, and the existing `TheDefaultTable_CoversEveryMoment` iterates the enum, so forgetting to seed one of the seven turns that test red on its own. **Verification**: `Assembly-CSharp`, `ExpoTheExplorer.Data` and `ExpoTheExplorer.Tests.EditMode` build clean, 0 errors; `HapticButton.cs` was absent from Unity's generated Compile list, so the csproj was patched, built and restored rather than trusting a pass that never saw the file; `UNITY_EDITOR` was confirmed defined in the Data project, so the `OnValidate` block was genuinely compiled and not skipped. EditMode suite still owed — Unity holds the lock. **Owed by the user**: Add Component → Haptic Button on every button that should buzz (on the shop's TEMPLATE row for the shop), binder dragged in; plus the binder into the new `haptics` fields on `GameManager` and `NoKeysPopupView` — affects: HapticConfig.cs, HapticButton.cs, BoardItemDragHandler.cs, DayRewardFlightView.cs, MetaShopView.cs, NoKeysPopupView.cs, GameManager.cs, codemap-core.md, codemap-ui.md — 2026-08-25

D-072: **Haptics, third pass: the three silent successes.** A sweep for what was still uncovered turned up three gaps that were inconsistencies rather than omissions — in each case the sibling event already buzzed and this one did not. **A paid Continue succeeding** (`GameOverPopupView.OnGemClicked`) was the worst of them: the most expensive tap in the game, hard currency spent to come back from a day that was over, and it felt exactly like tapping a menu button, because the gem button's `UiTap` was all that frame produced — `LivesChanged` moves UP on a refill and `HapticsBinder` deliberately reads only the drop. Its refusal side needs nothing, unlike the shop's: `Show()` disables that button when the player cannot afford it, so an unaffordable Continue is never a click to answer. **A Gem key refill succeeding** (`NoKeysPopupView.OnGemClicked`) is the same family and got its OWN moment rather than sharing the Continue's, even though both are "Gems spent, resource restored" — a rescue mid-day and a meta top-up are different stakes, separate rows let them be tuned apart, and the defaults already differ (Success vs MediumImpact). That is deliberately NOT the same reasoning as D-071's single `PurchaseRefused` for every refusal: refusals all say the same thing (no, nothing happened), while these two successes differ in what they cost and what they save. **The meta celebration** (`MetaGroundsView.CelebrateOne`) was a whole choreography with no haptic at all while its purchased sibling `PropLanded` had one. `PropUnlocked` is a separate moment on purpose: both end with a prop standing there, but a bought prop FALLS and hits the ground with the map shaking under it, while this one rises from silhouette to solid — giving a gradual reveal an impact haptic would describe a blow the screen never struck. **Its anchor is the line that snaps the reveal to finished**, which is the one place both routes reach: the fill running its course, and the player tapping past it. Skipping skips the ANIMATION, not the event, so it buzzes either way. Not inside the reveal loop, for the reason the coin burst already established — that loop drives `fillAmount` frame by frame, so one request per iteration would be a second of undifferentiated rumble instead of one beat. Several props unlocking at once each buzz, like the star row, and cannot machine-gun: `CelebrateOne` resets `skipRequested` per prop so each needs its own tap, and the coalescer caps the frame regardless. **Enum entries appended, not sorted in**, for the third time and the same serialization reason (D-071): the asset was at fifteen authored rows, every one storing a value, and `OnValidate` grows it to eighteen on the next load without touching what is there. **Deliberately still out**: the not-enough-Gems branch of the key refill (offered in the same conversation, deferred; `PurchaseRefused` would cover it in one line), splitting timeout from wrong delivery, and the ticket-timer urgency pulse. **No new test** — this adds moments, not rules, and `TheDefaultTable_CoversEveryMoment` iterates the enum, so a missing seed turns it red on its own. **A comment written in this turn was wrong and was corrected before it shipped**: it claimed the key-refill refusal already had a moment, which it does not — the line now states the asymmetry as the scope decision it is. **Verification**: `ExpoTheExplorer.Data`, `Assembly-CSharp` and `ExpoTheExplorer.Tests.EditMode` build clean, 0 errors; all four touched files were confirmed present in Unity's generated Compile lists first (`HapticButton.cs` from D-071 has since been imported, so no csproj patch was needed this time). EditMode suite still owed — Unity holds the lock. **Owed by the user**: one drag — the SampleScene binder into `GameOverPopupView`'s new `haptics` field; `NoKeysPopupView` and `MetaGroundsView` already carry theirs from D-070/D-071 — affects: HapticConfig.cs, GameOverPopupView.cs, NoKeysPopupView.cs, MetaGroundsView.cs, codemap-core.md, codemap-ui.md — 2026-08-25

D-073: **The danger pulse: one condition, one rhythm, two amplitudes.** A ticket whose remaining time enters the danger zone now says so twice at once — an authored danger image blinks in over the card, and the card itself dims and brightens with it. **The danger zone is not a new number.** `TicketCardsView.TimerFillColorFor` already turned the bar red at `EconomyConfig.CriticalRatio`, and that same ratio picks the tip tier (CLAUDE.md — Tip Tiers), so the critical tier was simply NAMED (`IsInDangerZone`) and the colour method now CALLS it: "the bar is red" and "the card is pulsing" are one expression, and the window where one says the ticket is in trouble while the other stays calm is no longer expressible. Authoring a danger threshold of its own would have been a third number the player can already see the wrong answer to, since the bar sits on the card. The warning (orange) tier was left out deliberately: two thirds remaining is where the tip starts stepping down, not where a ticket is about to be lost. **The pulse is computed inside the per-frame timer read, NOT run as a looping tween, and that is a lifetime decision rather than a style one.** `PlayDeliveryTransition` does `canvasGroup.DOKill()` and re-fades that exact CanvasGroup, so a loop tween surviving the hand-off would fight it for the alpha and the card would flicker through its own exit animation. `Update` is *already* silent for the whole transition (`transitionInProgress`), which means a pulse computed there cannot overlap the one animation it could break — the suppression the file already relies on does the job a tween would have needed new bookkeeping for, and `RefreshTimer` was chosen because the card polls its slot every frame anyway (`RemainingSeconds` is mutated directly, so there is no event to bind). Cost is a cosine and two property writes per card, on a canvas the draining `fillAmount` already dirties every frame. **One cosine, two amplitudes**: the danger image takes the full 0→1 swing and genuinely blinks out, while the card only dims to `BoardAnimationConfig.TicketDangerDimAlpha` (0.45) — a card that vanished each cycle would take the timer bar the player is watching with it. Both read the same value, so the icon and the ticket cannot drift out of step, and authoring the floor at 0 gives the card the full blink too: the choice lives in the asset. The phase comes from `Time.time` rather than a per-card accumulator, so all three cards pulse together — three alarms out of step read as clutter, one rhythm reads as an alarm. **A zero period means solid-on, not a strobe.** This project has been bitten twice by an asset carrying a value the code did not expect, and clamping a stray 0 to some minimum would have produced a ~20Hz flash — unreadable and a real accessibility hazard — so the code branches on it and shows the danger image steady instead. **`dangerImage` is the one optional reference on this card**, against the file's own convention of failing loudly: `ValidateReferences` sets `isValid` false, which would take the WHOLE card down — an invisible ticket — over a decoration that has a working degraded mode (the card still dims). It warns once per card at `Initialize` instead, naming the prefab, so the feature cannot be silently absent. It is toggled with `Image.enabled` rather than `SetActive` so it keeps its layout footprint and never slides the card's other rows. **The alpha has one owner at a time**: `ResetDangerPulse` hands it back explicitly — `RebuildContent` calls it before writing its own alpha (0 for an empty slot, 1 for a ticket), so a card mid-pulse when its ticket is delivered, cancelled or times out does not carry that frame's dim over to whatever fills the slot next. **No haptic, deliberately** — D-071 and D-072 both deferred the ticket-timer urgency pulse for the same reason (the buzz whose dose is easiest to get wrong; the user wanted to feel the existing batch on a device first). The threshold now living in exactly one place means adding it later is one line against `IsInDangerZone` rather than a new comparison. **No new test**: this adds a look, not a rule — `IsInDangerZone` names an expression `TimerFillColorFor` was already comparing, and both touched views need a scene to exist. **Verification**: `ExpoTheExplorer.Data` and `Assembly-CSharp` build clean, 0 errors (the 4 remaining `CS0649` warnings are pre-existing, in `TicketGenerationConfig`); all three touched files were confirmed present in Unity's generated Compile lists first, so the clean build is not the silent pass of a file nobody compiled. Nothing was verified in the Editor. **Owed by the user**: add the danger visual to the `TicketCard` prefab as ONE `Image`, outside any layout group, with a sprite of their own (the repo has no danger/warning art) — the two variants `TicketCard Into` and `TicketCard UpDown` inherit it from the base — then drag it into the new `Danger Image` field; and confirm `BoardAnimationConfig.asset` shows 0.6 / 0.45 for the two new Ticket Card fields rather than 0. Prefab YAML is not written by the AI (D-007) — affects: BoardAnimationConfig.cs, TicketCardsView.cs, TicketCardView.cs, codemap-core.md, codemap-ui.md — 2026-08-25

D-074: **The danger flash speaks in colour, not transparency — and it switches rather than fades.** D-073 shipped the urgency signal on the wrong channel and the user rejected it on sight ("niye transparanlıkları artıp azalıyor kötü olmuş... renkleri daha cırtlak olmalı"). The reasoning behind the reversal is mechanical, not aesthetic: **`CanvasGroup.alpha` fades everything printed on the card**, so dimming it took the customer name, the dish photo, the modification rows and the timer bar down with the paper — the card was at its least readable at the one moment its numbers matter most, which is the opposite of what an urgency cue is for. Tinting `background.color` touches only the paper and leaves every glyph and icon fully opaque. **And the shape changed with the channel: a square wave, not a cosine.** A smoothly interpolated flash reads as *fading*, which was the specific complaint; an alarm switches. `Mathf.Repeat(Time.time, period) < period * 0.5f` — lit for the first half, rested for the second — and the period is now the only speed knob. **The danger icon blinks hard on/off** (`Image.enabled`) instead of ramping its alpha, so the sprite the author draws keeps its own colours at full saturation rather than being seen through a half-transparent version of itself; it shares the paper's phase deliberately, so both halves of the cycle say one thing (DANGER / normal) instead of trading places and reading as motion. **The two colours live on `TicketCardVisualsConfig`, the period stays on `BoardAnimationConfig`**, and that split is the point: how loud the alarm is, is a visual choice belonging beside the ticket sprites and the bar's three colours (and shared with the Day Editor's preview through the same asset); how fast it blinks is timing. `TicketCardsView.DangerFlashColorFor(bool lit)` takes a *half* and answers with a colour, so the card asks which side of the blink it is on and holds no opinion about the palette — the same single-lookup shape `TimerFillColorFor` already had. **The rest colour is the one knob for "how garish":** left white, the paper flashes red and returns to its own artwork; put a second loud colour there and it ALTERNATES between two (red/yellow). One field, both designs, no code change. **`ticketDangerDimAlpha` was DELETED rather than left unread** — it existed only for the alpha pulse, and dead config data in this project has a way of being re-authored later by someone who assumes it does something. **The paper returns to the PREFAB's own tint, not to the flash's rest colour**, which are two different things that happen to look alike at the default: the rest colour is half of a choreography, `authoredBackgroundColor` is the card's own paint. It is captured once in `Initialize`, because after the first flash the live value is an alarm colour and anything reading it later would learn the wrong thing. **`canvasGroup.alpha` is now untouched by the danger code**, so the delivery transition is its single owner again — the alpha coordination D-073 had to reason about simply stopped existing. What did NOT change: the threshold (`IsInDangerZone`, still `EconomyConfig.CriticalRatio`, so the flash still begins exactly when the bar turns red), the per-frame placement inside `RefreshTimer` and its no-tween argument, the shared `Time.time` phase across all three cards, the 0-period meaning (flash off, icon solid, never a clamped 20Hz strobe), and `dangerImage` staying the card's one optional reference. **A stale asset was found while diagnosing the user's "I can't change the blink period"**: `BoardAnimationConfig.asset` serializes only 4 of its 20 keys — the whole Delivery Success and Ticket Card blocks are absent from the file, so those values have always come from their C# initializers. This is exactly the trap `EconomyConfig.asset` carried until 50be70c, and it is the user's fix to make (touch any field in the Inspector and Unity re-serializes all of them); it is recorded on the codemap line so the next reader does not diagnose it twice. `TicketCardVisualsConfig.asset` was checked and is healthy, which is a second reason the two colours belong there. **Verification**: `ExpoTheExplorer.Data` and `Assembly-CSharp` build clean, 0 errors; no new file, so no csproj patch. Unity's own last compile (17:13) is newer than the D-073 sources and carries no `error CS`. Nothing was verified in the Editor. **Owed by the user**: still D-073's one wiring job (the danger `Image` on the `TicketCard` prefab, outside any layout group, its sprite theirs to choose), plus tuning the two new colours under `TicketCardVisualsConfig.asset` → Danger Flash, and re-serializing `BoardAnimationConfig.asset` as above — affects: TicketCardVisualsConfig.cs, BoardAnimationConfig.cs, TicketCardsView.cs, TicketCardView.cs, codemap-core.md, codemap-ui.md — 2026-08-25

D-075: **One menu item against a trap recorded four times: the configs get re-serialized, and a config edit finally sticks.** The user hit the same wall twice in one session — "blink period değerini değiştiremiyorum", then "bu yanacak rengi ben seçebilsem ya" — and offered the choice of moving the danger colours onto the prefab instead. They chose to keep them in the config and fix the reason it felt unreachable, which is the better half of the trade: `TicketCardVisualsConfig` is one place for all three TicketCard variants and is the same asset the Day Editor's preview reads, while two colours on a prefab would be three copies free to diverge with no player-visible reason. **The wall has two halves and one fix covers both.** A `.asset` does not update itself when its class gains a field — Unity leaves the absent key at the C# initializer and writes nothing — so `BoardAnimationConfig.asset` was down to 4 serialized keys out of 19, the whole Delivery Success and Ticket Card blocks missing. And a ScriptableObject edit made in the Inspector lives in memory until `AssetDatabase.SaveAssets` runs, so a value typed and then lost to the next script recompile looks exactly like a field that refuses to change. Dirtying every config under `Assets/Data` and saving once writes each object WHOLE: every declared field lands in the file, and from then on there is somewhere for an Inspector edit to land. **It changes no value, and that is the property that makes it safe to run on all twelve assets at once.** A key present in the file keeps its authored value (it was loaded from there); a key absent from the file is written at the initializer the game is *already* running on — the behaviour after is identical, the file simply stops lying about what it holds. The only subtraction is a key whose field no longer exists (`TicketCardVisualsConfig.asset` still carried `timerWarningRatio`/`timerCriticalRatio`, moved to `EconomyConfig` back in D-011), which is the same cleanup `50be70c` did by hand. **The folder, not the asset.** A one-asset button would not have prevented the fifth occurrence; this trap is on the record four times (D-004's `BoardDistributionConfig.asset`, where it silently disabled D-001's urgent-ticket guarantee; D-011's `EconomyConfig.asset`, where every tip rate came from a code default; D-063's finding on `BoardAnimationConfig.asset`; and today's D-074). **The before/after report counts top-level non-`m_` keys in the YAML rather than reflecting over the class**, deliberately: the question is "what does this file carry", and what Unity does or does not serialize is precisely the thing that has been wrong here before — asking the file avoids adding a second theory about it. A nested asset like `MetaCatalog` therefore reports a small number, which is its shape and not an error. **`DataTooling` is a new blueprint system line**, not a DayEditor file: this tool names no game type at all (it works on `ScriptableObject` and a folder path), so filing it under the Day authoring tools would imply a dependency it does not have — and a codemap line needs a real `sys:`, never `?`. It is K3: editor-only, and runs zero times in a build. **No test**: the deliverable is a menu click whose proof is `git diff Assets/Data` showing the files grow. **Verification**: `ExpoTheExplorer.Editor` builds clean, 0 errors — and because the new file was absent from Unity's generated Compile list, the csproj was patched, built and restored (the D-071 procedure), so the pass is not the silent one of a file nobody compiled. Nothing was run in the Editor; the tool has not been invoked. **Owed by the user**: press **ExpoTheExplorer → Re-serialize Data Configs** (top level of that menu, beside Day Editor -- NOT the Project window's right-click Create tree, whose ExpoTheExplorer/Data submenu is the CreateAssetMenu list for these same configs and is what the first attempt found instead) once Unity recompiles, read the before/after lines in the console (expect `BoardAnimationConfig.asset: 4 -> 19`), and check `git diff Assets/Data` before committing — then the Danger Flash colours and the blink period are ordinary editable fields — affects: DataConfigReserializer.cs, blueprint.md, codemap-editor.md — 2026-08-25

D-076: **The delivery receipt: a correct delivery finally says what it earned.** Until now success said "it worked" three ways — the tray grows and lifts, the ticket card slides out and its replacement drops in, the `OrderDelivered` haptic — and said nothing about the money, while `GameManager.OnTicketDelivered` was already computing the entire `DeliveryPayoutResult` (order value, tip, rate and **tier**) and handing it straight to `DayLifecycleManager.RecordDelivery`. So the one variable the player actually controls — speed, which picks the tip tier — was being decided invisibly. A number now rises and fades over the delivering slot, in that tier's colour, with the tier's authored word above it. **A RECEIPT, NOT A PAYMENT, and that boundary is the whole design.** D-057 deliberately deferred the money to the day-complete ceremony so the payout has ONE performance; a per-delivery coin flying to the HUD would give it two. So nothing here touches `Wallet`, reads `PendingReward`, or animates the HUD — it states a figure and leaves. **The figure is the banked one, not an approximation**: `RecordDelivery` rounds once per delivery (`Mathf.RoundToInt(payout.Total)`), so rounding that same value on screen matches the day's running total to the coin — which is what makes it safe to show a number the player will be paid much later. **The payout is passed IN as the struct that was just banked**, never recomputed: handing the view an `EconomyCalculator` would be a second opinion about what one ticket was worth, and it is what keeps this a view rather than a second economy. That also settled where the call goes — the tray and the card both learn a delivery happened but neither is ever told its value, so `OnTicketDelivered` is the only place with both facts. **No new event was added.** The obvious alternative — a `GameState` EventBus carrying the payout — would have made `ExpoTheExplorer.Core` name a type from `EconomySystem`, an arrow pointing the wrong way (and a reference cycle waiting to happen). A serialized field on `GameManager` was already the established shape for exactly this: it mirrors `haptics` field-for-field, optional and silent when unwired, so a forgotten drag costs a readout and never a delivery. **The colours are the timer bar's own three**, read from `TicketCardVisualsConfig` rather than authored again: the receipt then speaks the language the bar spent the whole ticket teaching ("my bar was green, the number came up green, it paid more"), and retuning the bar's red retunes the receipt's red. The `TipTier`→colour switch lives in the view because `ExpoTheExplorer.Data` references nothing by design and cannot name the enum — the same split the config's `PatienceType` colours already use. **The tier words are three authored strings** (`PERFECT`/`GOOD`/`LATE` as defaults), so an author who wants numbers only empties the fields and no code changes; the word and the amount share ONE TMP label (`word\namount`) rather than two references that could disagree about alignment. **An authored INACTIVE template is cloned per delivery and destroyed on complete** — the `TicketCardView`/`MetaShopView` template pattern, not pooled, because two slots can be delivered in the same frame and a single reused label would have to interrupt itself. It fades through `TMP_Text.alpha` rather than `DOFade`: this project's DOTween carries no TextMeshPro module, and a `CanvasGroup` instead would put an extra component requirement on every authored template. **`slotAnchors` is the one optional reference and falls back to the view's own RectTransform**, deliberately fail-open: a receipt in roughly the wrong place still tells the player what the delivery earned, while refusing to draw reads as a broken feature — and it means the thing can be seen working after two drags, gaining per-slot precision when the anchors are placed. A short anchor array warns rather than errors. **A map repair rode along**: `blueprint.md`'s Bootstrap line claimed `depends on: DaySystem, BoardDistribution, TicketSystem` while `GameManager` has been constructing and calling `EconomyCalculator` all along — `EconomySystem` was added, since the lie sat on exactly the handler this task touched. **No test**: the addition is a readout, and the arithmetic behind its number is already pinned by `DayLifecycleManagerTests`/`DayRewardPurseTests`. **Verification**: `ExpoTheExplorer.Data` and `Assembly-CSharp` build clean, 0 errors, with `DeliveryPayoutView.cs` inserted into Unity's generated Compile list first (it was absent, being new) and the csproj restored afterwards — the D-071 procedure, so the pass is not the silent one of a file nobody compiled. Nothing was verified in the Editor. **Owed by the user**: author an INACTIVE TMP label on the day canvas (its font and two-line layout are the receipt's whole look) into `Label Template`; drop three empty RectTransforms over the trays into `Slot Anchors` in slot order; wire `TicketCardVisualsConfig` and `BoardAnimationConfig`; drag the view into `GameManager`'s new `Delivery Payout View`; and run D-075's re-serialize menu item so the two new Delivery Payout numbers land in the asset — affects: DeliveryPayoutView.cs, GameManager.cs, BoardAnimationConfig.cs, blueprint.md, codemap-core.md, codemap-ui.md — 2026-08-25

D-077: **D-076 REVERTED — the delivery receipt is out, and it is not to be re-proposed.** The user asked for it ("olur yapalım hadi"), saw it described, and rejected it on sight ("yok ya geri al bunu beğenmedim"). Reverted the same turn: `DeliveryPayoutView.cs` deleted (with its `.meta`, so Unity does not report a missing script), `GameManager`'s optional field, its one call and the `ExpoTheExplorer.UI` using removed, and `BoardAnimationConfig`'s two Delivery Payout numbers with their properties taken back out — the config is exactly what it was before D-076. `OnTicketDelivered` is once again two lines: compute the payout, record it. **Two things were deliberately NOT reverted.** The blueprint's Bootstrap arrow (`EconomySystem`, added by D-076) STAYS: the arrow was never the receipt's — `GameManager` has constructed and called `EconomyCalculator` since long before this, so the line was simply wrong and reverting a map repair would put a lie back. And D-074's danger flash and D-075's re-serialize tool are untouched; "bunu" was this feature, not the session. **The finding survives the feature**, and it is worth keeping on the record rather than losing with the code: a correct delivery still says "it worked" three ways (tray, card, haptic) and never says what it earned, while `OnTicketDelivered` computes the full `DeliveryPayoutResult` and shows none of it — so the one variable the player controls, speed picking the tip tier, is still decided invisibly. The two cheaper shapes offered alongside the receipt and not chosen are also still on the table: a short green flash on the tray (success currently whispers while a wrong delivery shakes and scatters, so failure reads louder than success), or a "paid" stamp riding the card's existing exit. **Do not rebuild the rising number without being asked for it by name.** **Verification**: `ExpoTheExplorer.Data` and `Assembly-CSharp` build clean, 0 errors after the revert — which needed one extra step worth recording: Unity had already imported the new file and REGENERATED `Assembly-CSharp.csproj` with it, so deleting the source left the generated project pointing at a missing file (`error CS2001`) until that Compile entry was removed. Unity drops it on its own next refresh; a revert done between two Unity refreshes has to clear it by hand to get an honest build — affects: DeliveryPayoutView.cs (deleted), GameManager.cs, BoardAnimationConfig.cs, blueprint.md, codemap-core.md, codemap-ui.md — 2026-08-25

D-078: **A broken heart rises off the tray that cost the life — and the slot index had to be threaded to make it truthful.** The user asked for a broken-heart sprite appearing on the tray and drifting up while fading out, and chose (over a single fixed spot) that it appear on the tray where the failure actually happened. That choice is what forced the plumbing, and the reason is a blind spot rather than a preference: **`WorldTrayView` can only see half of the life losses.** It detects a timeout by polling its item count and reacting when it falls to zero, so a ticket that expires over a tray the player never touched leaves no trace there at all — and an animation that plays for a wrong delivery and for *some* timeouts reads as a bug, not as a signal. The only place that sees every loss is `GameManager.HandleLifeLoss`, where both routes end. **So both `loseLife` delegates widened from `Action` to `Action<int>`.** This does NOT walk back D-060: that decision keeps `TicketSlotManager` and `TrayManager` ignorant of the failure's CAUSE, and the cause still splits in GameManager's two named entry points. A slot index is not a cause — it is the index both classes are already working on (`i` in the timeout loop, `slotIndex` in the batch resolve), handed on so a view can point at the ticket that failed instead of guessing between three trays. `TrayManager` already took `deliverTicket` as an `Action<int>`, so its two delegates now match. Fifteen test construction sites moved from `() =>` to `_ =>`, mechanically; the EditMode assembly compiling is what proves none was missed. **The heart is world-space, not canvas**, because the thing it is drawn over is: the trays and every item in them are `SpriteRenderer`s, so a sprite rising from one sits in their layer at their scale with no camera projection, and it fades through the same SpriteRenderer alpha `WorldTrayView` already fades a whole tray with on a delivery. Cloned from an authored INACTIVE template per loss and destroyed on complete — never pooled, since two can be drifting at once and one reused sprite would interrupt itself. Parented to its anchor with `worldPositionStays`, so it keeps the template's authored scale (rather than inheriting a tray's) and still dies with that hierarchy; the rise is a WORLD tween so one authored distance means the same at a scaled tray as at an unscaled one. `Ease.OutSine` on the rise against `Ease.InQuad` on the fade, so it is still solid while moving fastest and thins as it slows — drifting away rather than a light switching off. **It is asked for AFTER `LivesManager.LoseLife()` has run**, never before: a heart flying up for a loss that did not happen is the one lie this readout must not tell. **Both causes are treated alike** — to the player a heart is a heart, and which mistake spent it is what the wrong-order shake or the vanishing card already says. It fires on the *last* life too, alongside the Game Over popup, because that is still a life lost and a special case there would go quiet at the system's most important moment. Optional and silent when unwired, the shape `haptics` established (D-070); `slotAnchors` is optional even inside that, falling back to the view's own transform, because a heart in roughly the wrong place still says a life is gone while silence looks broken. Two numbers on `BoardAnimationConfig` (rise 1.2 world units — the tray itself lifts 1.5 on a delivery, for scale — and 1.0s). **No new test**: what was added is a readout, and the delegate's widening is covered by the paths already pinned (`TraySystemTests` on the wrong delivery, `DayTicketSlotManagerIntegrationTests` on the timeout). **Verification**: `ExpoTheExplorer.Data`, `ExpoTheExplorer.Systems.TicketSystem`, `ExpoTheExplorer.Systems.TraySystem`, `Assembly-CSharp` and `ExpoTheExplorer.Tests.EditMode` all build clean, 0 errors, with `LifeLostHeartView.cs` inserted into Unity's generated Compile list first and the csproj restored after (the D-071 procedure). The EditMode suite was not RUN — Unity holds the lock — but it compiling is the specific thing this task needed from it. **Owed by the user**: find a broken-heart sprite (the repo has none), build an INACTIVE world-space SpriteRenderer from it — watch its sorting layer, behind the tray it will never be seen — into `Heart Template`; drop three empty Transforms over the trays into `Slot Anchors` in slot order; wire `BoardAnimationConfig`; drag the view into `GameManager`'s new `Life Lost Heart View`; and run D-075's re-serialize item so the two Life Lost numbers land in the asset — affects: LifeLostHeartView.cs, GameManager.cs, TicketSlotManager.cs, TrayManager.cs, BoardAnimationConfig.cs, TraySystemTests.cs, TicketSystemTests.cs, DayTicketSlotManagerIntegrationTests.cs, codemap-core.md, codemap-ui.md — 2026-08-25

D-079: **The heart the author places IS the heart that flies.** D-078 shipped the broken heart as a template plus three anchors — clone at the anchor, animate the clone, destroy it. The user replaced that with something simpler on the spot ("direkt trayin içine nerede başlamasını istiyorsam oraya inaktif image olarak yerleştirsem"), and it is the better shape: with a template and an anchor, the thing you place is not the thing you see, so a start position had to be read off two objects at once and neither of them showed you the result. Now each slot's heart is one INACTIVE `SpriteRenderer` parked inside its own tray at exactly the position, scale and sorting layer it should start from; `Show` switches that object on, drifts it up, fades it, and puts it back. No template, no `Instantiate`, no anchors array, and one fewer authored object per tray. **Living inside the tray was CHECKED, not assumed** — this project has been bitten by two writers on one property more than once. `WorldTrayView.PlayWrongOrderShakeThenScatter`, the animation that runs on the very same frames as a wrong-delivery heart, collects only the SLOT children and shakes the tray in X; it never touches a child's alpha. The two sweeps that DO fade every child renderer (`GetComponentsInChildren<SpriteRenderer>(true)`) both live in `PlayDeliverySuccess` — the delivery path, which by definition never coincides with a life loss. So the heart rides the tray's rattle while it climbs, which is the tray moving its child, not a fight over the same property. **What replaced the clone's guarantees**: a clone was always born fresh, so the animated object's state needed no thought; a reused authored object does. The rest position and colour are captured once in `Start` and re-applied at the START of every play (not only at the end), and a flight still in the air is `DOKill`ed and restarted from rest — otherwise a second loss on the same slot (a wrong order, then its replacement ticket timing out) would begin halfway up and half faded. The put-back on complete restores position and colour as well as deactivating, for the same reason. The rise stays a WORLD tween even though the heart is parented, so one authored distance still means the same thing at a tray an author has scaled. **An empty array entry warns and shows nothing** rather than falling back to the view's own transform as D-078 did: the fallback made sense for a clone that needed *somewhere* to spawn, but a heart whose whole point is the position the author gave it has no sensible default. Everything else from D-078 is untouched: the `Action<int>` widening, the call after `LivesManager.LoseLife()`, both causes treated alike, the two `BoardAnimationConfig` numbers. **Verification**: `Assembly-CSharp` builds clean, 0 errors — and this time the file was already in Unity's regenerated Compile list (it had been imported), so no csproj patch was needed. Nothing verified in the Editor. **Owed by the user (revised, and shorter)**: park an inactive broken-heart SpriteRenderer inside each of the three trays where it should start, drop the three into `Slot Hearts` in slot order, wire `BoardAnimationConfig`, drag the view into `GameManager`'s `Life Lost Heart View`, and run D-075's re-serialize item — affects: LifeLostHeartView.cs, codemap-ui.md — 2026-08-25

D-080: **The broken heart is a canvas Image, not a world sprite — asked as "why can't I just place an Image?", and the answer was that it should be one.** A UI `Image` only renders under a Canvas, and `TrayArea` is a world object, so D-079's design necessarily asked for a `SpriteRenderer`. But the user's instinct was the better design and the reason is a hazard D-079 had accepted: a sprite parked inside the tray has to win a sorting-order fight with the tray itself (`Default`, order -2) and with every item sitting in it, and **losing that fight is silent** — the heart simply never appears, which is the worst failure mode a decoration can have. A canvas draws over the world entirely, so there is nothing to lose. It also removes the second hazard by construction instead of by inspection: the hearts are no longer children of the tray, so `WorldTrayView`'s animations — the wrong-order shake, and the two sweeps that fade EVERY child renderer on a delivery — cannot reach them. D-079 had to prove those sweeps never coincide with a life loss; now the question cannot be asked. What is lost is that the heart no longer rides the tray's rattle, which nobody asked for. **The rise unit changed with the parent**, and that matters more than it looks: the distance is now an `anchoredPosition` tween in RectTransform units, so the authored default moved from 1.2 (world) to 120 (canvas — the same scale as the ticket card's 150-unit exit lift). A 1.2 left in that field would have produced a heart drifting a pixel and looking broken. Caught because `BoardAnimationConfig.asset` did not carry the field yet: the user had already run D-075's re-serialize tool (the asset is at 29 keys, up from 4, and `ticketDangerBlinkPeriod` is authored at 0.8 rather than its 0.6 default — the tool works and the danger flash is being tuned), but that run predates these two fields, so the corrected C# default is what they will get. Everything else from D-078/D-079 stands: the authored object IS the animated object, rest position and colour captured once and re-applied at the start of every play, a flight in the air killed and restarted, both life-loss causes treated alike, the call placed after `LivesManager.LoseLife()`. **Verification**: `ExpoTheExplorer.Data` and `Assembly-CSharp` build clean, 0 errors; `DOTweenModuleUI` was confirmed present in `Assets/Plugins/Demigiant/DOTween/Modules/` before relying on `Image.DOFade`/`DOAnchorPosY` — the same check that sent D-074's danger flash to `TMP_Text.alpha` when the TextMeshPro module turned out to be missing. **Owed by the user**: three inactive broken-heart Images on the day scene's canvas, positioned over the three trays where each should start, dropped into `Slot Hearts` in slot order (0 = leftmost); wire `BoardAnimationConfig`; drag the view into `GameManager`'s `Life Lost Heart View`; re-run the re-serialize item so the two Life Lost numbers land in the asset — affects: LifeLostHeartView.cs, BoardAnimationConfig.cs, codemap-ui.md — 2026-08-25

D-081: **The three GDD 5.2 powerups were built, and buying them was moved to the main screen.** The user reopened a scope that had been marked "deferred, do not build" in both the GDD and the project CLAUDE.md since v0.7. Plan and step-by-step record: `.claude/powerup-plan.md`. **The shape is KeySystem's, deliberately**: charges live in `PowerupManager`'s own private array rather than on `GameState`, because a charge outlives the day, the scene and the session, and a private field makes the single-writer rule a compile error instead of a comment. Gems keep `Wallet` as their one writer — a purchase asks through `TrySpendGems`. Every number (starting stock, Gem price, per-day grant) is authored on `PowerupConfig`; the save carries three named charge fields at profile v9. **PowerupSystem does not know what a powerup DOES.** The three effects need tickets, the board and the trays; taking references to them would have pointed one system at three others, so each effect is handed in as a `Func<bool>` by `GameManager` — the day scene's composition root — exactly as `TrayManager` and `BoardDistributor` already avoid each other. **That seam is also the two-screen split, not just tidiness.** The user moved buying to the main screen ("bu marketi... gem ile powerup alma işi aslında mainscreende yapılmalı"), because a store opened mid-service suspends the very time pressure a powerup exists to relieve. Both screens build a `GameSession` and therefore both hold a `PowerupManager` — the menu needs the counts to sell against — but only `GameManager` registers effects, so a use button that ended up on the menu by mistake physically cannot spend a charge: `TryUse` finds nothing registered and refuses. The rule is enforced by what is ABSENT rather than by a scene check. **The `Func<bool>` return value is a rule, not plumbing**: false means the effect had no work to do, and GDD 5.2 says such a press costs no charge. All three have reachable moments with nothing to do (no active tickets, an empty board, a finished day), so this is the difference between a convenience and a resource quietly lost — and it is the one property that cannot be tested from the manager's side, which only ever sees a bool. **A shared gate sits in front of all three**: `!IsAwaitingContinue && !IsDayComplete`. When the last life goes the day freezes behind the Continue popup, and spending there would be paying for a world the player cannot see; `BoardItemDragHandler` gates dragging on the identical flag. **Two boundaries were crossed knowingly.** First, `Ticket.RemainingSeconds` now has three writers — the constructor opens it, `TicketSlotManager.Tick` spends it, `PowerupEffects` puts it back. The first two are "time moves forward"; this is the only place it moves back. Routing it through `TicketSlotManager` to preserve one writer was rejected because that class would have had to learn what a powerup is. The boundary is pinned by a test, per `ownership.md`'s shared-ownership rule. Second, Auto-Collect moves items through `WorldTrayView.TryAcceptDrop` — the ordinary drop path — rather than writing into `TrayManager`, because a tray's contents are drawn entirely by the dragged object being reparented and a data-only collect would be correct in state and show an empty tray. **One press = the tickets in hand**: the active tickets are captured by INSTANCE and a slot stops the moment its ticket is replaced, or filling a tray would deliver it, assign the next ticket, spawn its items, and chain until the Day ran out. **Noise Clear was corrected mid-build** and the correction is the reason this entry is worth reading twice: it was designed (and half-planned) as a few seconds of dimming, and the user replaced it with outright removal ("soluklaştırmıycak... direkt boarddan kalkıcak"). That made the step SMALLER — no timer class, no `BoardView` change at all, since `BoardGrid.RemoveItem` already publishes `CellChanged` — and made the powerup stronger than its authored price assumes. Its rule is by IDENTITY, not by count: a ticket wanting one burger protects every burger, because a player calls a third one "too many", not "unwanted". The user asked directly whether it could break the guaranteed-ticket rule; `BoardDistributor` was read rather than assumed about, and the answer is two independent guarantees, both now tests — everything an active ticket requires is kept, and `SpawnMissingRequiredItems` caches nothing, so a recount re-spawns any gap on the next order. **Known consequence, recorded not fixed**: `leakedTickets` lets an upcoming ticket leak only once, so clearing that leaked item removes its noise contribution permanently. **What was deliberately NOT built**: no cooldown or per-day use cap (the stock is the limit); no confirm popup on the purchase (a prop is a placement decision, a charge is a few Gems); no event-reward earn path (there is no event system — day completion is the candidate GDD 5.2 itself named). `TicketRequirements` was extracted to Core in the same work with three consumers; the same rule still lives twice more inside `BoardDistributor` and that is left as a small task of its own rather than silently half-done.
affects: PowerupType.cs, PowerupConfig.cs, PowerupManager.cs, PowerupEffects.cs, TicketRequirements.cs, TraySlot.cs, PlayerProfile.cs, PlayerProfileStore.cs, GameSession.cs, GameManager.cs, MainScreenRoot.cs, MainScreenSessionSetup.cs, PowerupBarView.cs, PowerupShopView.cs, PowerupShopSetup.cs, AutoCollectRunner.cs, BoardView.cs, PowerupSystemTests.cs, PowerupEffectsTests.cs, TicketRequirementsTests.cs, blueprint.md, powerup-plan.md — 2026-08-25

D-082: **Day 0 opens with a forced first move: everything darkens except one hotdog and one tray, and a ghost hotdog loops between them until the player makes that move.** The user's ask, in their words: only one hotdog selectable at first, a ghost sliding to one of the trays, forced on the player, then the game continues normally -- refined mid-plan to "everything except 1 hotdog and 1 tray darkens", and then to the target tray's ticket card staying lit with them. **The content lives in the Day file, not in a config asset.** `runtime.tutorial` names the source cell and the target tray; a `TutorialConfig` asset was rejected because it would be a SECOND authority for what happens on Day 0, free to name a cell the Day's own `boardTimeline` never fills, with nothing checking the two against each other. Keeping both halves in one file is exactly what let `DayValidator` check the pairing -- a rule no other file is positioned to make. **The JSON block needs an explicit `enabled` bool**, which is the one place in this schema a bool is not a redundant toggle: `JsonUtility` returns a zeroed instance rather than null for a missing object, and every field of a zeroed tutorial block is a REAL cell and a REAL tray index, so absence cannot be detected by content the way `boardDistribution` and `ticketRuntime` detect it. The resolved side has no flag -- `DayDefinition.Tutorial` is null or it is a tutorial, so presence IS the answer and a half-configured one is unrepresentable. **`TutorialDirector` references nothing at all.** Three ints in; two questions and a completion out. It does not know what a `BoardItem` or a `Ticket` is, which is what makes every rule reachable from a test with no MonoBehaviour and no scene -- the same trick `PowerupSystem` uses to avoid pointing at the board, the trays and the tickets at once. Both gates answer TRUE once the step is done, so a finished tutorial and a Day that never had one are indistinguishable downstream; that is what keeps the two call sites reading as ordinary permission checks. **It is not a framework**: one authored step exists, so there is no step list, no interface and no state machine. **The darkening never writes a colour it does not own.** Tinting each renderer was the obvious approach and would have lost -- `BoardView` rewrites its layer colours on every `RefreshCell`, so the board would flicker back to full brightness on the next spawn. Instead a black quad covers the camera at sortingOrder 500 and the two lit things are lifted +600 above it, under `BoardItemDragHandler`'s +1000 drag boost, so an item the player actually picks up stays visible over the dim for free. Ticket cards needed the other half: Canvas UI composites above everything the camera renders, so the quad cannot cover them -- the two non-target cards get a stretched black `Image` child each, which leaves the `HorizontalLayoutGroup` undisturbed and lets the **target card stay lit by doing nothing at all**. **Nothing was authored in the editor.** No prefab, no scene object, no art, nothing dragged into an Inspector, and `SampleScene.unity` was never opened: the spotlight is built at runtime by the TARGET `WorldTrayView`, which is the only object already holding both of the ghost's endpoints (a serialized `BoardView` for the source cell, its own transform for the destination). The ghost is a CLONE of the live board item with colliders and every MonoBehaviour stripped, so it always shows the exact food and modifications being asked for. `unitymap.md` could not answer which objects those were -- it is UNMAPPED by a known generator bug -- so `SampleScene.unity` was parsed directly; that finding is what collapsed the editor step to nothing. **A pre-existing bug was fixed on the way through, not silently.** Refusing a press in `BoardItemDragHandler.OnPointerDown` never stopped a drag: UGUI still calls `OnBeginDrag`/`OnDrag` for the same gesture and their `CurrentItem == null` guards pass, so an item the Continue popup had "blocked" was still picked up and flown around with none of the pickup bookkeeping done -- and the comment above the method claimed the opposite. A per-gesture `pickupRefused` flag now gates all five handlers, which is what makes the tutorial's refusal real and closes the old leak with it. **Two guards cover the softlock, on different doors**: `DayValidator` errors at authoring time (the Save button is gated on it), and `GameManager` refuses to arm when the source cell is empty at Day Start, logging the cell and running the day as an ordinary one -- a hand-edited Day file never passes through the editor at all. Arming lives inside `ApplyDayStartBoardPreSeed` because all FOUR day-start paths run through it, and the director is REBUILT rather than kept, so Day 1 -- which authors no tutorial -- opens every gate again instead of inheriting Day 0's lock. Powerups are blocked while the step runs: Auto-Collect would sweep the very item the player is being told to drag. **A content bug was caught by checking rather than assuming.** The block was first authored at cell (2,0), which the Day's own `boardTimeline` fills with a hotdog carrying EXTRA MUSTARD, against tray 0, whose ticket wants a plain one -- the forced move would have been a wrong order costing a life on the game's first interaction. Moved to (1,2), a plain hotdog, verified against `TicketSlotManager.FillEmptySlots` assigning ticket *i* to slot *i*. **`DayValidator` deliberately does NOT check that pairing**, and this is the recorded limit of the guard: the rule for what a ticket requires lives in `TicketRequirements.RequiredCounts` and takes a runtime `Ticket`, so re-deriving it from a `ResolvedTicketEntry` would be the FOURTH copy of a rule D-081 consolidated to stop exactly that. The validator catches "no item on that cell"; "the wrong item on that cell" is still on the author. **The Day Editor carries the block through load/save without a UI** -- `ToDayJson` rebuilds the runtime section field by field, so any field the model does not name is silently DELETED the first time a Day is opened and saved, which would have meant day_00 losing its tutorial because someone retimed a ticket. **Deliberately not built**: no "seen it" profile flag, so the step replays on a Day 0 retry (persisting it would bump the save version, and the board is re-seeded anyway); the HUD is not dimmed (a darkened currency counter reads as a bug, not as focus); the two unpickable hotdogs are not greyed -- the ghost is the affordance.

D-083: **The Day 0 tutorial became a SEQUENCE, and its second step teaches modifications.** The user's ask: right after the first forced move, the extra-mustard hotdog at (2,0) must be dragged to its own tray, with the ghost as before, plus an arrow on the mustard ON the hotdog, a second arrow on the modification ON the ticket, and a line of text saying modifications must be read. **This is the repetition that earned the generalisation D-082 deliberately refused.** That entry shipped one step as one step and said a second moment would be what justifies a list; it arrived, so `tutorial` is now `steps[]` -- and still not a framework: a list and an index, no interface, no state machine class, no per-step subclass. **`Completed` became `StepChanged`**, because with more than one step the interesting moment is the boundary rather than the end; it also fires for the end and for an abort, which is how a tray knows to tear its spotlight down. **The pairing was verified, not assumed** -- the same check that caught D-082's bug. At Day Start slot *i* holds ticket *i*, so after step one delivers slot 0, tray 2 still holds the extra-mustard order and (2,0) still holds the extra-mustard hotdog; both steps were re-verified item-for-item and modification-for-modification against the shipped JSON before this was called done. **Finding the mustard needed a fact the game already computed and threw away.** A food's layers are just a stack of sprites, so "which of these IS the extra mustard" had no answer; `ResolvedLayer` now carries the `ModificationConfig` that made it visible, which is non-null only for a `HiddenByDefault` layer -- one that is on the item BECAUSE of a modification. The arrow then reaches the matching child by index, since the container's children are those layers in BoardView's own construction order; both ends of that coupling say so. **The arrows live in two rendering worlds**, the same split the dim already forced: the one on the hotdog is a `SpriteRenderer`, the one on the ticket is an `Image` parented to the modification row itself, so it follows the card's `HorizontalLayoutGroup` instead of being positioned against a screen that can re-flow. `TicketCardView` exposes that row read-only, handing back a `RectTransform` rather than the `ModificationSlotView` so a caller gets a position and no way to repaint a row that is rebuilt from the ticket on every refresh. **Still no art and still nothing to wire**: the arrow is drawn into a texture in code, and the message borrows the ticket card's own font, so it matches the game's type without a font being dragged into an Inspector. **The step boundary exposed a real ordering hazard, fixed rather than papered over.** Unity defers `Destroy` to the end of the frame while the next step's spotlight is built immediately, so a spotlight that restored its lifted sorting orders in `OnDestroy` would have put the OLD step's renderers back after the NEW step had already lifted its own -- permanently wrong ordering. `Dismiss()` now restores synchronously, and `RestoreAll` is idempotent so `OnDestroy` still covers the paths that never reach it (a retry, a scene unload). **The softlock guard had to move.** D-082 checked the source cell once, at Day Start; that is not enough for a list, because the board moves as the tutorial is played and a later step's item can be gone by the time its turn comes. Every step is now re-validated against the LIVE board as it begins, and a step that has become impossible aborts the whole tutorial with a logged reason. `DayValidator` gained the matching authoring-time rule plus one only a whole-file reader can make: two steps naming the same source cell is a guaranteed softlock, since the first consumes that item. **The message is content, not a string literal** -- authored in the Day JSON beside the step, in ENGLISH, which was checked against the game's existing UI ("Orders delivered", "Continue Day X", "Start Over") rather than assumed from the language of the conversation. The text: *"Modifications matter. This order wants extra mustard - bring the hotdog that already has it."* -- the rule as a headline, then the specific action, so it pairs one clause to each arrow. **Deliberately not built**: no message on step one (its ghost speaks for itself); no per-step timing or skip button; and the tutorial still replays on a Day 0 retry, since persisting a "seen it" flag would bump the save version.

D-084: **The tutorial's two gates left a hole that could strand the day in one gesture, and a third gate closes it.** Reported by the user: during the tutorial the item could be picked up and dropped on another board cell, and then it could not be picked up again. That is the whole failure -- the pickup gate permits exactly ONE cell, so the moment the item sat anywhere else it was beyond reach, no tray would accept anything, and the step could never be completed. **It was a known risk that got lost between design and implementation**: the very first design pass for D-082 named the board-to-board relocation branch as something the tutorial had to refuse, and the shipped code gated only the pickup and the tray drop. Worth recording as a process fact rather than just a bug: the two gates that WERE written are the two that appear in the feature's happy path, and the third is on a branch you only reach by doing something the tutorial does not want -- which is exactly the kind of path a happy-path reading misses. **The fix adds no movement code.** `BoardItemDragHandler.OnEndDrag` now asks `GameManager.IsBoardRelocationAllowed()` before the `TryGetCellAt && IsCellEmpty` branch; refusing it falls through to the snap-back that was already there, which tweens to `homePosition` and touches `BoardGrid` not at all -- so the item returns to its starting cell in the view and never left it in the model. That is precisely the behaviour the user asked for ("başlangıç yerine geri dönsün") and it cost one condition. **The gate is its own question rather than `IsPickupAllowed(destination)`.** The two agree on today's content, but they ask different things -- may this cell be picked up FROM, versus may an item be put down at all -- and a single predicate serving both would be true by coincidence of the current authoring rather than by rule. Same reasoning that kept the pickup and tray gates separate. **Both directions are pinned by tests**, and the second half matters more than it looks: written backwards, this gate would lock every board in the game forever, so there are tests for "refused during a step" AND for "released once the tutorial ends or aborts". **Not changed**: no content, no Day file, no scene, no asset -- this is a rule, and with no tutorial running the new check answers "allowed" and every drop path behaves exactly as it did.

D-085: **The tutorial's step message was drawn every time and never once visible: it was behind its own backing plate.** Reported by the user as simply "the text does not appear". The cause is a UGUI rule rather than anything about fonts, anchors or the Day content: **a Graphic is drawn before its children, always**. The first version put `TextMeshProUGUI` on the outer object and parented the dark plate underneath it, so the plate painted straight over the glyphs. The code even carried a comment claiming the plate was "added FIRST so it draws behind the glyphs" -- `SetAsFirstSibling` orders SIBLINGS, and a child is never behind its parent, so that reasoning was wrong and the comment made it look considered. **The fix inverts the two**: the plate is now the parent and the text its child, inset by a padding derived from the font size. No anchoring, position, font or fade changed -- the placement was never the problem, which is worth stating because "text not showing" invites moving things around. **The second half of the fix is that it can no longer fail silently.** `BuildMessage` had three quiet `return`s (no card, no Canvas, no borrowable TMP font) and this method fails invisibly by nature -- a step with no message looks exactly the same as a step whose message was dropped -- so each now logs which condition declined. That is the difference between the next occurrence being reported as "the text does not appear" and it naming itself. **Not changed**: day_00, the config numbers, the arrows, the ghost, the dim. `enableWordWrapping` was replaced with `textWrappingMode` in passing since the project's TMP version provides it.

D-086: **The tutorial's whole UI half rested on a false premise about Canvas rendering, and the invisible message was only its most visible symptom.** D-082 wrote, and D-083/D-085 inherited, that "Canvas UI composites over everything the camera renders, so the target ticket card stays lit by doing nothing at all". **That is true only of Screen Space - OVERLAY canvases.** This scene's `InGameCanvas` -- which carries every ticket card -- is Screen Space - **Camera** at sortingOrder **-1**, and the dim is a SpriteRenderer at 500. A camera-space canvas sorts against sprites like any other renderer, so the dim covered the entire canvas: the target card, the modification arrow parented to it, and the step message were all built correctly and then buried. **The user reported it as "the text still does not appear" after a first fix (D-085) that was real but not sufficient** -- the plate/child ordering was genuinely wrong and genuinely fixed; it just was not the only thing wrong. **The lesson is the shape of the mistake, not the mistake.** The premise was carried in a comment from `WorldTrayView`, reused across three decisions, and never checked against the scene -- while the scene was readable the whole time. It was settled here in one step by parsing `SampleScene.unity` for canvas render modes and sorting orders, which is what should have happened before the first line was written. **Two fixes, one cause.** The message now builds its OWN Screen Space - Overlay canvas, copying the game canvas's `CanvasScaler` so the authored font size keeps meaning what it means elsewhere; its sortingOrder is **-1 on purpose**, keeping it above every camera-rendered thing (Overlay always is) but BELOW `Popup Canvas` at 0 -- a ticket can time out mid-step, and a tutorial line floating over the Game Over popup is worse than one hidden behind it. The target card is raised by adding a `Canvas` with `overrideSorting` at 800: above the dim, below a dragged item so an item dragged across it still passes in front, and its arrow rides along as a child. That component is destroyed on teardown -- the card is not ours to destroy, only what we added to it. A card that already has a Canvas is left alone rather than reconfigured. **`DimCard` is deliberately kept** even though the non-target cards now sit under the world dim regardless: it makes "the other cards are dark" hold by construction rather than by the current render mode, which is precisely the assumption that failed here. **Not changed**: day_00, the config numbers, the ghost, the world-space dim and the board-side lifts, all of which were correct.

D-087: **The tutorial's third step introduces the three powerups, and a step now has a KIND.** The user's ask: after the last forced move, a panel with three stacked rows -- each powerup's button image on the left, its description on the right -- with the wording left to me. **This is where the step list earns a second shape.** D-083 generalised one step into a list; this generalises a list of one shape into a list of two, and no further: `TutorialStepKind` has exactly two values, no per-kind class, no interface, and a third kind is a design decision that has to be argued for at that enum. A step with no `kind` is a `ForcedMove`, so the two authored steps and their JSON did not change. **The kind is mirrored, not shared.** `Tutorial` keeps its own copy of the enum for the same reason it keeps its own `TutorialStep`: taking DaySystem's would put the first entry on an assembly whose empty reference list is what makes its rules testable with no Day catalog. `GameManager` translates, written out as a switch rather than cast, so a kind added upstream without teaching Tutorial about it stops the build. It is the one file holding both enums, which is what the using-alias at its top is for. **Where the words live was the real decision.** The three powerups had NO name and NO description anywhere -- the shop hand-labels its rows in the scene -- so both strings went onto `PowerupConfig`, beside each powerup's cost and charges. They describe the POWERUP, not the moment it is explained, so the shop can read the same two strings later instead of keeping a copy free to disagree. The tutorial's own JSON says nothing but `"kind": "PowerupIntro"`. The descriptions: Auto-Collect *"Fills your trays with what the current orders still need."*, Time Reset *"Gives every waiting customer their full time back."*, Noise Clear *"Clears the board of every item no current order asks for."* -- one line each, verb first, each naming the difficulty GDD 5.2 says it relieves. **The icons forced the ownership choice.** They are not on `PowerupConfig`; they are the sprites on the three live `Button`s, so `PowerupBarView` builds this step -- the same rule that has the target tray build a move step's spotlight: whichever object already holds what the step needs. Its three button fields stay private and only a read-only `IconFor` is added, because a caller has no business reaching the `Button`, which is what spends a charge. **Time stops while the panel is up**, and that is correctness rather than polish: three descriptions take longer to read than a Patient ticket has, so a running clock would make the tutorial itself cost lives. Its own condition in `Update`, NOT a second use of `IsAwaitingContinue`, which means "the Continue popup is up" and is read by four other places. All three gates refuse during an intro step for the same reason -- anything reachable behind a modal is a way to lose a life while reading -- and the panel additionally carries its own raycaster and backdrop, so its modality does not depend on a rule living in another system. **Two completions, kept separate**: `NotifyTrayAccepted` and `NotifyReadingFinished` each ignore the other kind, so a stray tray drop cannot skip an unread panel and a dismissal cannot stand in for a move. **Two guards that would have been silent bugs**: `DayValidator` applies its cell and tray rules to `ForcedMove` steps only (an intro step names neither and would have been rejected for leaving them at zero), and `EnsureCurrentTutorialStepIsPossible` skips the board check for a non-move step (it would otherwise validate an intro step against its unused (0,0) and abort the whole tutorial). **Deliberately not built**: dismissal is a "Got it" button rather than tap-anywhere, because a reflex tap on a just-appeared modal is how a player closes it without reading a word; and the panel does not light the real HUD buttons, it copies their sprites, which is what "each row has that powerup's button image" describes.

D-088: **The main screen gets its own first-run tutorial -- a welcome that says what the game is, then an arrow at the store telling the player to renovate -- and it deliberately shares no code with the day scene's.** The user asked for the store hint and then added the welcome ahead of it. **The reuse question was the real decision.** `TutorialDirector` exists, works and is tested, but its entire surface is board cells, trays and drop gates, and this screen has no board, no trays and no Day; the only way to reuse it was to carry Day-shaped fields into a screen with none of them. So the two flows share the `Tutorial` system name and nothing else, which is written into blueprint.md so the next reader does not "fix" it into one. **"First opened" is derived, not remembered.** The condition is day index 0 AND nothing owned -- state that already exists -- rather than a `PlayerProfile` flag, which would have meant a save-version bump. It stops on its own the moment the player finishes a day or buys a prop, and it has a property a flag does not: a player who ignored the hint is offered it again. **The welcome is modal and the hint is not**, and that asymmetry is the design: the welcome is a wall of text, so a backdrop with its own raycaster stops a stray tap falling through to Play and starting a day the player has not read about; the hint ASKS them to tap the store, so the store must stay tappable, and opening it counts as dismissal alongside its own button. **The arrow is parented to the store button**, so it follows the layout rather than a hard-coded corner -- which is also why teardown destroys it explicitly: it is the one piece of this view living outside the view's own hierarchy, and it would otherwise hang on the button forever. **Where the words live**: `[TextArea]` fields on the component, authored in the SCENE. Not a config asset, because every other player-facing string in this project is already scene text (the shop's rows, the main screen's labels), so an asset would have been the odd one out; the values in code are the starting draft, the scene is the authority. The drafts: *"Welcome to the expo -- You're running the food stall. Every customer brings one order, drag the matching items off the board into their tray before their patience runs out. Serve them well and the day pays."* and *"Spend what you earn on the grounds. Tap the store to start renovating."* **`MetaShopView` exposes only `MarketButtonRect`** -- the RectTransform, not the Button, so a caller gets a position and no way to press the store on the player's behalf. **Asked rather than assumed**: whether the hint should also point out the expo grounds themselves (what "renovate" refers to). The user chose the store button alone. **This one has an editor step**, unlike D-082 through D-087: the day scene had objects that already held everything a step needed (the target tray, the powerup bar), and this screen has none, so a new object with two dragged references is honest rather than invented.

D-090: **Play does not start a day until the player has bought their first building, and it says so rather than going dead.** The user asked for a Play button that cannot be pressed until then. **That half was declined, deliberately and out loud**, because the project has a locked rule against it -- CLAUDE.md/D-069: *"No button is ever disabled... a greyed-out button does not tell the player why"* -- and a dead Play on a brand-new player's first screen is the strongest case that rule will ever see. The outcome the user asked for is delivered in full (no day starts), but the press ANSWERS: the arrow reappears on the store with its own line, *"Your stall isn't ready for customers yet. Tap the store to renovate it first."* Reverting to a literal grey-out is two lines if they want it. **The condition is derived, not stored**: owning nothing IS the state, so there is no new profile field and no save-version bump -- and it is the same condition the welcome tutorial reads, so the two can never disagree about who counts as new. Nothing un-buys a prop, so it can never re-trigger; Start Over wipes the profile and correctly brings it back. **It is read from the live session, never from the profile snapshot `MainScreenView` loads at Start** -- buying the building is exactly the event that opens this gate, and a snapshot taken before it would keep the player locked out after they had complied. **Every uncertainty resolves toward LET THEM PLAY** (unwired reference, no session, null list), which is not defensiveness: this gate sits on the only path into the game, so failing closed is an unplayable build that looks exactly like a save bug -- the same stance, and the same reasoning, as the key popup's fail-open. **The wiring rides the existing menu item** rather than a second one, because the gate and the tutorial are one feature: a project where only one of two setup steps had been run would have a Play button that blocks and explains nothing.

D-091: **A dev tab that plays any Day, and the number it writes is a POSITION, not a Day number.** The user asked for a tab in the editor to reach whichever level they want, and chose the dev reading of it: pick a Day, start the game there. `ExpoTheExplorer/Play From Day` lists the catalog and one click sets the saved profile's day and enters play. **The whole correctness of it sits in one field's meaning.** `PlayerProfile.CurrentDayIndex` is a position in the RESOLVED catalog, not a Day file's own `dayIndex` — `DayCatalogNavigator.GetDayAt` indexes the list directly, and `DayCatalogParser` DROPS a Day whose ids fail to resolve, sliding every later Day down one. The two numbers agree today only because `day_00..day_40` are contiguous and all resolve. So the window runs the SAME parse the game runs (`DayJsonSource` + `DayCatalogParser`) and writes the row's position. Listing `Resources/Days` by filename would have been half the code and would have sent the tester to the WRONG Day precisely when a Day file is broken — i.e. precisely when they are debugging it. **No runtime code changed.** The tool only calls existing public API, and it writes exactly one profile field through `PlayerProfileStore.Save`, so the save file keeps its single writer and its version stamp and no new schema field exists. It touches neither wallet, keys, powerup stock nor owned props: a tester at Day 30 wants their real inventory, and seeding one here would be a second answer to what `PlayerProfileStore.NewPlayer` already answers. **Two entry points, because they test different things**: *Play* opens the day scene directly and deliberately skips the key spend and the meta screen (the main screen's job, not what is under test), while *Main Screen* takes the real route in. **It refuses to jump while play mode runs** rather than half-applying: `GameSession` reads the profile once at construction, so a mid-play write would be invisible on screen and then overwritten by the running session's next Save. **It confirms first**, because this edits the real save file — moving back to Day 3 to check a tutorial is indistinguishable at the file level from losing thirty days of progress. Editor-only in every sense: `Assets/Editor`, `includePlatforms: Editor`, ships in no build — which is what makes "edit the player's save" a safe capability to build at all. It gets its own blueprint system (`PlayTesting`) rather than a home inside `DayEditor`, for DataTooling's reason: it authors nothing, it runs the finished catalog in the other direction, and it needs a `ProgressionSystem` arrow that `DayEditor` does not have and should not gain.

D-092: **A runtime cheat panel on SRDebugger, and the day index finally gets a single writer.** The user installed SRDebugger and asked for a debugger with gems, money, key refills, powerup grants and "go to whichever level we want". **The panel commands owners and assigns nothing.** Every cheat routes to the class that already owns the data — money through `Wallet`, keys through `KeyManager`, charges through `PowerupManager`, lives through `LivesManager`, the day index through `GameSession`. A cheat menu is the most tempting place in a codebase to write `state.Gems = 9999`, and one such line hands a balance a second writer and voids the root invariant for a debug button. Two owners had no free write path at all and gained a dev-only one **inside themselves** rather than exposing a field: `KeyManager.DebugSetKeys` (the only other ways were a 40-Gem purchase that refuses at the cap, and spending) and `PowerupManager.DebugGrant`. **The second is not `ApplyPersisted`, and that distinction is the whole point:** `ApplyPersisted` assigns the three counts and publishes nothing, because at load time nothing is subscribed — reusing it would set the numbers correctly and leave the HUD showing the old ones, so the cheat would look broken while working perfectly. **The day jump forced an ownership question the project had been getting away with.** `GameState.CurrentDayIndex` was written in three places — `GameManager`'s day advance and its completed-day exit, plus `GameSession`'s constructor — while `fingerprint.md` asserted `GameManager` was its single writer. That was simply false, and invisible only because the three never ran together. The user wanted to jump **from the main screen**, where `GameManager` does not exist, which is where a split ownership stops being a documentation error and becomes a real one. So `GameSession.GoToDay(int)` is now the single writer and the other three call it, the constructor included — a single-writer rule that exempts its own constructor is worth nothing. `GoToDay` clamps (an index past the last authored Day resolves `CurrentDay` to null and throws on the first ticket) and deliberately does **not** save: callers already decide that, two of them write several fields in the same breath, and saving here would double-write on those paths. It inherits D-091's hard-won rule — the number is a POSITION in the resolved catalog, not a Day file's `dayIndex`. **The jump is main-screen only and refuses inside a day rather than half-working**, because leaving a running day is a wallet decision `ReturnToMainScreenAbandoningDay` already owns and a cheat button has no business answering differently; afterwards it reloads `MainScreen`, since `MainScreenView` reads the day once in `Start` and the meta views read `CurrentDayIndex` without subscribing, so anything less leaves the screen lying. **Two traps are handled out loud rather than silently.** Granting money mid-day would VANISH on the next retry (the atomic-day contract), so the grant re-takes the day-start baseline and logs that it did — the price is that this day's legitimate earnings stop being revertible, which a tester chasing a revert bug must not discover the hard way. And the profile is written only at a few day-lifecycle moments, so a `Save Now` button exists because otherwise every cheat is lost on quit. **Binding is serialized, not searched:** `SROptions` is constructed by SRDebugger with no scene presence, so `DebugMenuBinder` carries a `[SerializeField] SessionHost` a human drags in (D-013 ruled out runtime lookups for this binding), and it registers the HOST rather than the session — `SessionHost.Session` is null until that host's `Awake` runs and Unity does not order `Awake` across GameObjects, so caching the session would be a per-load race. `Scripts/Debug/` carries **no asmdef and that is load-bearing**: the panel is a `partial` half of SRDebugger's global `SROptions`, and a partial's halves must share an assembly. Everything is wrapped in `UNITY_EDITOR || DEVELOPMENT_BUILD`, and SRDebugger's own trigger moved to `DevelopmentBuildsOnly` — it shipped set to `Enabled` with no entry code, which would have let any player triple-tap the release iOS build and mint gems. `DebugTicketDeliveryController` was deleted into this: its 1/2/3/N/R shortcuts are now buttons, because a keyboard cheat cannot be pressed on the device the game ships to. **It supersedes D-091's `PlayTesting` window**, which the user will remove separately.

D-093: **The debug panel gets its own opener, because SRDebugger's is a corner rect nobody can hit.** The user reported that triple-tapping did nothing and pointed out the real requirement: "mobilde de debuglamam gerekicek". SRDebugger's shipped trigger is a small `RectTransform` pinned to ONE screen corner and sized at runtime from screen DPI — so tapping the middle of the screen three times is a no-op, which reads exactly like a broken install, and on a phone that corner is both a few millimetres wide and the place the game's own HUD already lives. Verified it is NOT the enable-mode: `DevelopmentBuildsOnly` resolves through `Debug.isDebugBuild`, which is **true in the editor**, so the trigger was enabled the whole time; and it is not an Input System mismatch either — SRDebugger reads `Keyboard.current` under `#if ENABLE_INPUT_SYSTEM` and this project is Input-System-only. So `DebugMenuBinder` grew an opener of its own: **a whole-screen multi-finger touch** (default three), which cannot be missed and collides with nothing, since no mechanic in this game is played with three fingers at once. It calls `SRDebug.Instance.ShowDebugPanel` directly, which is not gated by the trigger's enable mode at all. SRDebugger's own trigger and shortcuts are left on — this is an addition, not a replacement. **The keyboard path is deliberately modifier-free**, and that is not a detail: SRDebugger's shortcuts are Ctrl+Shift+F1..F4, so an unguarded `keyboard[F1].wasPressedThisFrame` fires on Ctrl+Shift+F1 too, both openers run on one keypress, and the panel toggles open and straight back shut — a bug whose symptom is "the shortcut does nothing", i.e. the exact complaint this opener exists to fix. It was written and caught before it shipped. `Update()` is frame-frequency, which the cost model normally makes us argue for: it is a few memory reads and an int compare, O(1), at the cheapest tier, in a file compiled out of release builds — nothing measurable. **The mobile consequence the user must act on:** everything here sits behind `UNITY_EDITOR || DEVELOPMENT_BUILD`, so the *Development Build* checkbox in Build Settings is what decides whether a phone build has a debug panel at all. That is the intended posture (D-092 moved the trigger to `DevelopmentBuildsOnly` because it shipped as `Enabled` with no entry code), but it means a release TestFlight build has no panel by design; making one reachable there is a separate decision, and the honest lever for it is the entry code rather than deleting the compile guard.

D-094: **The day scene gets a settings menu, and the only genuinely new thing in it is that time stops.** The user asked for a settings section in `SampleScene` with Retry and Main Menu guaranteed, and picked the rest from a shortlist: Resume, a haptics switch, a day + star readout, pause while open, and a confirmation on both exits. **The pause is the feature.** Every ticket runs its own countdown, so a menu that let them drain would charge the player lives for reading it — `GameState.IsPaused` is a third gate on the one `TicketSlotManager.Tick` call, beside `IsAwaitingContinue` and the tutorial's reading hold. Three separate conditions rather than one combined one, so a clock that never restarts still says which holder forgot to let go. `Time.timeScale` is deliberately untouched (this project has never used it): only the ticket clock stops, and tweens, the reward flight and the UI keep running, which is what a menu over a frozen day should look like. **No economy rule was invented here, and that was a real temptation.** A mid-day give-up is FREE on both routes today — `SpendKeyForLostDay` is gated on `IsAwaitingContinue`, so `RetryDay` from a running day spends no key and writes nothing, and `ReturnToMainScreenAbandoningDay` reverts the wallet and saves without a key either. Charging for it is arguably the consistent reading of D-068 ("charging one route but not the other just teaches them which button is cheaper") and it is **left alone on purpose**: it is a balance change, it belongs to whoever tunes the key economy, and slipping it in under "add a settings menu" would make it findable only by a player who noticed their keys draining. Flagged, not done. **Two confirmation panels rather than one shared panel whose message code rewrites**, and that is the root invariant rather than a style call: a shared panel puts both authored questions into C#. Same reason the day caption receives only a NUMBER and the word beside it is a scene label. `SettingsPopupView` contains no authored string at all. **The menu button removes itself whenever the day is over, won or lost**, rather than staying put and refusing — a visible, pressable control that silently does nothing reads as the game hanging. On a WON day it would be worse than useless: this menu's Retry is `RetryDay`, the lost-day path, while a completed day has its own Retry with its own money rules on the Day Complete popup, and two buttons answering that question differently is a bug waiting for a player to find it. Four subscriptions cover every edge with no per-frame check, and one of them is not the obvious one: the way back from a completed day is `TicketAssigned`, **not** `LivesChanged`, because a day finished with all three hearts refills 3 → 3, which publishes nothing, and the button would have stayed gone for the rest of the session. **`ConfirmRetry` closes before it retries**, which is not tidiness: `RetryDay` resets the day in place with no scene load to tidy up after it, so a pause left standing would freeze the fresh attempt with no menu on screen to lift it. **The haptics switch is one bool that cost a save version.** `PlayerProfile.HapticsEnabled` at v10, and the first bool in that file — a bool has no spare value to mean "absent" the way `Keys` and the charge fields use `-1`, so the absent case can only be answered by a version branch, which writes `true`: absent-reads-as-false would switch haptics off for every existing player with nothing on screen to connect it to. Deliberately **no `= true` initializer** on the field, tempting as it was — it would have covered the migration case by accident and made the two answers inseparable, which is exactly what `UpgradeToCurrent`'s own comment refuses to rest on. **It is not saved on the flip**, and that is the one real compromise: `GameSession.Save` writes the whole profile, so a mid-day write would bank an unfinished day's earnings and break the atomic-day contract. The value lives on `GameSession` and reaches disk at the next ordinary save point; flipping it and force-quitting mid-day loses the flip, and nothing else. A settings file separate from the profile would fix that and is over-engineering for one bool today. **The mute is read in `HapticsBinder.LateUpdate`, after `TryTakePending`** — that is what makes it a mute rather than a second set of rules: every request still reaches the service, so coalescing and priority behave identically either way, and no stale moment is left pending to fire minutes later when the switch comes back on. It fails ON in every uncertain case, for the reason the key gate fails open: always-on haptics are a nuisance, a game that silently cannot vibrate looks like the vendor being broken on that device. **It has an editor step** (`ExpoTheExplorer/Settings Popup/Build In Day Scene`) because the menu is fourteen required references across three nested panels, and by hand that is thirteen chances to wire one confirmation's Yes button into the other. It builds its **own canvas** rather than hanging off the HUD prefab instance, where an Apply All would push a day-scene menu onto the main screen — the hazard the blueprint already records for the heart row. It also wires each button's `HapticButton` at edit time, which is a different thing from the runtime lookups D-013 ruled out: the reference lands serialized in the scene exactly as a drag would leave it.

D-095: **Start Over leaves the main menu; the debug panel takes it over.** The user asked whether the main screen's Start Over button could move into the debug panel. Half of it was already there — `Save > Delete Save File` has called the same `PlayerProfileStore.Delete()` since D-092 — so the work was the missing half and the removal. **The missing half was the RELOAD, and it is the half that mattered.** D-026 built that button to delete *and* reload, because everything on the main screen — the session and its wallet, the HUD, the grounds, the shop — is built from the profile in `Awake`/`Start`; rebuilding the scene is the only path that resets every one of them through its existing single writer, where patching the live session would need a second writer for the balance, the day index and the owned props. The panel now does the same, and **only on the main screen**: reloading out of a running day would abandon that attempt without settling its wallet, which is a question `ReturnToMainScreenAbandoningDay` already answers, so inside a day it deletes and says to leave first — the same host test `Go To Day` makes. **The two-tap confirm did not come across, deliberately.** It existed because a *player* could mis-tap a button sitting on a screen players use; someone who opened a debug panel with three fingers and walked into the Save category is not making that mistake. Carrying the guard over would have been cargo-culting the mechanism while its reason stayed behind. **Removal touched three places, one of which the maps did not name:** `MainScreenView` (the field pair, the two authored confirm knobs, the arm/disarm coroutine, `OnResetClicked`, and the now-unused `using System.Collections`), `MainScreenResetButtonSetup.cs` (deleted whole — it existed only to retrofit the button into an already-built scene), and `MainScreenSceneBuilder`, found by grep rather than by the codemap, which built and wired the button when creating MainScreen from scratch. `WireView` now takes the Play button alone. **Nothing about ownership moved**: `PlayerProfileStore` is still the save file's single writer and both the old button and the panel only ever COMMANDED it — deleting a caller cannot create a writer. **D-026 is superseded, not deleted**, and specifically its two-tap design is left standing in the record: this decision moves a testing convenience out of shipping UI, it does not rule that players may never reset their progress. If a real player-facing reset is ever wanted, D-026 describes how that guard worked and is where to start. The scene keeps the button object until the user deletes it by hand — the same one-click removal D-027 chose over hand-editing scene YAML.

D-096: **An area unlock is the one purchase that brings the shop back, and it waits for the animation to finish first.** The user asked for it in those terms: the shop reopens when the selected item unlocks an area, not on the others, and "açılmak için animasyonun bitmesini beklemeli". **This does not reverse D-047.** The shop still closes on every purchase, and the screen still ends on the prop rather than on a list — that stays the default and every ordinary prop keeps it. What D-047 could not see is that one purchase in this game has a payoff that is *partly a list*: `MetaPurchase.ShopItems` **hides** area-locked props entirely (D-019 plus the user's 2026-08-21 instruction that reversed MS2's "show what is coming"), so buying the paved square makes a handful of rows exist that did not exist a moment ago — and closing for good leaves them behind a market button the player has no reason to tap again. An area unlock would feel like it bought nothing. **The condition is the catalog's own `UnlocksArea` flag**, read at the commit, so authoring a second expansion in the Meta window needs no code and no id list anywhere. **The wait is a poll, and the callback shape was rejected on a real hazard rather than on taste.** `MetaGroundsView` gained one read-only property, `IsSettlingPurchase`; the shop waits on it in a coroutine. A completion callback on `RefreshAfterPurchase` was the obvious alternative and is wrong here: that method's early exits (a celebration is running, the bought prop is not among the active ones) finish **synchronously**, so the callback would fire *before* the shop's own `SetOpen(false)` — which D-049 requires to run after — and the reopen would be undone by the close that follows it. The grounds would have had to promise "never called synchronously", which nothing in the type system can hold. A property costs one bool and one tween query per frame for the ~1s a placement lasts, at most once per purchase, and it keeps each side owning what it already owned: the grounds decide when the animation is over, the shop decides what to do about it. **It waits for the WHOLE settle, not just the drop** — `placing || DOTween.IsTweening(content)`, which spans the fall, the ground shake and `RestoreFocus`'s journey back out. The two halves are needed because the framing changes hands between them, and the second starts in the same call that clears the first, so there is no frame where the property reads false in the middle; a placement that never zoomed the map leaves no tween and settles the instant the drop ends, which is correct rather than a gap. Reopening at the earlier moment would slide a bottom sheet covering most of the map over a map still travelling — the shop interrupting the payoff it is meant to reward. **`SetOpen` is now also where a pending reopen dies**, which is what makes the auto-open safe: every caller of it is either the player working the shop or the purchase path, so a timer from a second ago can never act on a decision the player has already overtaken. The purchase path is unharmed because it starts its wait *after* its own `SetOpen(false)`, and the coroutine clears the handle before calling `SetOpen(true)`, so that cancel cannot kill the coroutine calling it. The one abandon case `SetOpen` does not cover is **walking to another location** — that goes through the location bar, not the shop — so it is checked explicitly against the location the purchase was made on; reopening there would rebuild the rows for grounds the player never asked to shop. **The failure mode is deliberately soft:** if the wait somehow never ends, the shop simply stays closed and the market button still works. Nothing changed in the rules layer, nothing was added to the save file, and there is no editor step — the shop already holds the serialized `MetaGroundsView` reference this needs.

D-097: **The tutorial holds the day's clock for the whole of its run, not only for the steps that ask the player to read.** The user reported it as a fact rather than a theory: "tutorial sırasında arkadaki biletlerin süreleri akmaya devam ediyor". They did. **This widens D-087 rather than reversing it, and the interesting part is why the narrow version looked complete at the time.** D-087 stopped the clock for `IsHoldingForReading`, reasoning that the powerup panel carries three descriptions and takes longer to read than a Patient ticket has — a real argument, correctly applied, that happened to be stated about the wrong property of the step. The property that matters is not "this step involves reading"; it is **"the game is holding the player somewhere they cannot play"**, and a forced move does that just as completely: it refuses every board cell but one and every tray but one, so the three tickets already sitting in the slots cannot be worked on at all while their timers run. A first-time player working out which item goes where is reading the board for the first time, at first-time-player speed, and can lose a life to a ticket the tutorial itself forbade them to touch. **The fix is the condition widening, one line: `Tutorial.IsHoldingForReading` becomes `Tutorial.IsActive`.** It stays a fourth early-return of its own in `GameManager.Update` rather than folding into `IsAwaitingContinue` or `IsPaused`, for the reason D-094 gave when it added the third: all of them mean "hold the ticket clock", they are held by different things for different reasons, and a combined condition makes a stuck clock impossible to attribute. **It cannot strand the clock**, and that is by construction rather than by care: every way a tutorial ends — the last step completing, `EnsureCurrentTutorialStepIsPossible` aborting an impossible one, `Abort()` — moves the same `IsActive` this gate reads, so there is no third state where the gates are open and time is still held. **`IsHoldingForReading` survives, unchanged and still on `TutorialDirector`**, because it has a second reader with a different question: `PowerupBarView` builds and destroys the intro panel from it, and that genuinely is asking "is this step a panel". Deleting it and comparing `Current.Kind` at that call site would have moved a fact about the step list into a view. **Nothing else needed to change**, which is the sign the gate was in the right place already: `TicketSlotManager.Tick` stays a dumb "advance by this many seconds" with no tutorial flag inside it, `RemainingSeconds` keeps its single writer, and the ticket cards' timer bars freeze on their own because they only ever read that field. No config, no save version, no scene, no editor step. **What the player sees is the absence of something**, deliberately: no paused badge and no frozen-clock icon — the tutorial's dim is already the signal that the game is being held, and the bars simply stop.

D-098: **A tutorial step's unused fields are zero, not absent — and zero is a real tray.** The user pasted a console error: `WorldTrayView: the tutorial step's source cell (0, 0) has an item in the board model but no rendered container`. Day 0's third step is `{"kind": "PowerupIntro"}` and authors nothing else, so `sourceX`, `sourceY` and `targetTraySlotIndex` all deserialize to **0**. `WorldTrayView.IsTutorialTarget` compared `step.TargetTraySlotIndex == slotIndex` **without asking the Kind first**, so tray 0 read a step that named no tray as naming *it*, and went looking for an item to point at in a cell no step had named. **The collision is not a coincidence and this is the part worth remembering: `StepOneTray` in day_00 is 0.** The unset value is indistinguishable from a legitimate authored one, so the bug could never have been caught by looking at the number — only by asking what kind of step produced it. **Every other reader already asked.** `GameManager.EnsureCurrentTutorialStepIsPossible` returns early for a non-move step (D-087), `DayValidator` skips them in all three of its tutorial checks, and `TutorialDirector`'s own gates return `false` before touching the fields. `WorldTrayView` was the one place that did not, which is what made it look correct in review: it was surrounded by call sites doing the right thing. **The fix puts the answer in the step list, not in the view:** a new `TutorialDirector.SpotlightTraySlotIndex` returns the authored tray for a `ForcedMove` and **-1** for everything else — no step, a panel, a finished tutorial, an aborted one — and the view asks it instead of doing arithmetic on `Current`. That is the same reasoning `IsHoldingForReading` was created under: which kinds have a tray is a property of the steps, and a `Kind` comparison inside a `MonoBehaviour` is that property leaking into rendering. The sentinel is safe by the rules that already exist: slots are `0..TicketSlotCount-1`, and `DayValidator` refuses an authored index outside that range. **The visible symptom was the lesser one.** The error only fired because cell (0,0) happened to have no rendered container; had an item been sitting there, the tray would have raised a *real* spotlight — dimming the board and looping a ghost from an arbitrary item to tray 0 — on top of the powerup panel, with no error at all. A LogError that fires on the harmless branch of a bug is luck, not diagnostics. **It also repaired two sentences without touching them:** `RaiseTutorialSpotlight`'s failure message asserts "the model has an item the view never built a container for", which is only true because `GameManager` refuses to start a *move* step whose cell is empty — a guarantee that never covered the panel step it was printing about. **Four tests pin it** (`TutorialDirectorTests`), and the first asserts the trap before the rule: the panel step's tray field really is 0, and 0 really is a tray a real step uses. The same pass renamed `AForcedMove_DoesNotStopTheClock` to `AForcedMove_IsNotAReadingStep` — D-097 had made the old name assert the opposite of the shipped behaviour while its body stayed correct, which is the cheapest kind of lying test to leave behind. No config, no save version, no Day-JSON edit, no editor step: a panel step authoring no cell is correct content, and the code is now able to say so.

D-099: **A held day resolves nothing — the failure's own cascade waits for Continue.** The user asked whether the popup stops the game "generally, not just the timers", having watched new items spawn behind a Game Over popup. **The premise needed correcting before the fix: nothing was "still running".** There is no `Update` and no coroutine in any system; the only clock is `GameManager.Update`'s tick, and D-097 had already stopped it. What the user saw was not a world that kept going — it was **the failure's own event cascade landing after the popup had already opened**. `Tick` → `loseLife(i)` → last life → `IsAwaitingContinue = true` → *popup shows* → **then** `CancelTicket(i)` → `AssignTicket` → `TicketAssigned` → `BoardDistributor.OnOrderPlaced` (new items) and `TrayManager.OnTicketAssigned` (orphaned tray scattering). `TrayManager.TryAddItem` had the identical shape: `loseLife(slotIndex)` then `ScatterBackToBoard`. Both orderings are correct and deliberate — the life must be spent before the consequence, which is what makes D-078's broken heart land on the right tray — so the bug was never the order; it was that the consequence had nowhere to wait. **The fix is a deferral, not a reordering.** Moving the cascade *before* the popup would not have worked: the spawn and the scatter are tweened over several frames, so a one-frame head start still plays behind the popup. Each system now leaves its own action undone while `IsAwaitingContinue` stands — the timed-out ticket is left standing at 0 seconds, the wrong-order tray keeps its items — and performs it on the first live frame after the hold. Nothing is queued or stored: the postponed work is each class's OWN existing method on its own data, so all that is remembered is which slot it is owed on. **The resume is detected by READING the flag in `Update`, and rejecting the event was the main design decision.** A "day resumed" event on `LivesManager` is the obvious shape and is a trap here: the hold is lifted by four callers — `TryContinueWithSoftMoney`, `TryContinueWithGems`, `RefillForNewDay`, and SRDebugger's refill — so the invariant would be "every future caller remembers to publish", and the cost of one forgetting is a slot that never refills and a day that can never complete, discovered by a player rather than by a compiler. Reading the flag has no such failure mode: it is the same flag the gate above it already reads, in the one method that was already asking. **Deferrals are per-SLOT, and that is the one place a simpler version is genuinely wrong.** Two tickets can expire in the same `Tick`; deferring only "the one that ended the day" leaves the second sitting at 0 to expire again on the next live frame — spending a life from the bar the player just paid to refill. **Order on resume is tray, then tickets**: that is the order the failures happened in, and a deferred cancellation assigns a new ticket whose required items are spawned *from the board*, so the scattered items belong back on it before that question is asked. Each flag is cleared **before** its action, because both actions publish synchronously back through `GameManager` into these same classes. **Three clean-up paths already existed and now carry the deferrals too**: `ResetSlotsForNewDay` and `DiscardAllForNewDay` clear them — a deferral carried across a reset would have the new day open having already thrown a ticket away or dropped the old day's food on the new board — and the abandon path destroys the scene. **It also closes a bug nobody had reported:** with the timed-out ticket left standing, `AllSlotsEmpty` cannot go true behind the popup, so a last-ticket-last-life frame can no longer raise the Day Complete popup on top of Game Over. **What deliberately did NOT change:** `GameState` gained no field and no event, `LivesManager` was not opened, `BoardDistributor` stays order-triggered with no timer and no poll (the D-044 reversal of 2026-08-23 stands), and the two `loseLife`-first orderings are untouched. The settings popup needed nothing — it cannot be opened while awaiting Continue, its backdrop blocks board input, and the clock is stopped, so nothing can cascade behind it. Nine EditMode tests cover it across the two suites, including the same-frame double expiry and both reset paths.

D-100: **The delivery waits for the last item to land.** The user's report was mechanical and exact: the ticket “doesn't wait for the item I just put in the tray to go to its place — it goes straight away”. It was right, and the reason was that the drop which COMPLETED an order was the one drop that never got seated. Every other accepted item is reparented into a slot and flown to it over `TraySettleDuration` (`BoardItemDragHandler.PlaceInSlot`); the resolving one went to `PlayDeliverySuccessAndDestroy` instead, which grew and lifted it **from wherever the finger let go**, on its own transform, in parallel with the tray leaving. So the item never travelled and the tray never waited. **The fix is one hand-off, not a delay:** `WorldTrayView.TryAcceptDrop` now calls `PlaceInSlotAndDeliver`, which seats the item exactly as any other drop does and passes `PlayDeliverySuccess` as that settle tween's `OnComplete`. The wait is **the tween itself** rather than a `DOVirtual.DelayedCall(TraySettleDuration)` — a timer would be a second copy of a duration the tween already owns, and the two could drift apart the moment the ease or the number changes. **`PlayDeliverySuccessAndDestroy` is deleted rather than delayed.** Once the item is seated it is an ordinary child of the tray, so the tray's own grow/lift/fade already carries it and `DestroySlotChildrenImmediate` already cleans it up; running the old parallel animation on a parented item would double both the scale and the lift. That is also why `PlayDeliverySuccess` stopped taking a `finalItem` at all — there is no longer a special item to know about. **The ticket card needed nothing**, and that is the quiet reason the wait could be inserted at all: `SuppressPollUntilDeliveryTransition` is still called in the delivery frame and holds the card on the delivered ticket until `PlayDeliveryTransition` fires, however long that now takes. **Two guards ship with it, and both close holes the longer window opens rather than adding behaviour.** (1) `deliveryInProgress` — the tray refuses every drop from acceptance until its slots are emptied, because `TrayManager` emptied that tray and handed the slot to the next order the instant the batch resolved: an item accepted in that window would be counted by the model and then destroyed unseen. That window existed before this change (the grow/lift) and this lengthens it by one settle, so the guard belongs to the change. Auto-Collect never reaches it — its `ReferenceEquals` check stops a slot at the same instant the guard goes up. (2) `Update` releases that flag if the delivering item is destroyed before its tween completes, because **DOTween kills such a tween instead of completing it** — a retry landing in the gap would otherwise leave a tray that refuses drops for the rest of the session. **The wrong-order path is deliberately left alone.** Its shake still starts on the un-parented final item the instant the batch resolves: that beat is an interruption by design, and seating an item that is about to be thrown back onto the board would only stretch it.

D-101: **The outcome gets watched before it gets covered.** The user asked for both end-of-attempt popups — the win and the loss — to arrive “2-3 seconds delayed, so the player can see the animation”. Both were instant: `DayCompletePopupView` on `DayCompleted`, `GameOverPopupView` on `LivesDepleted`, each doing its work inside the subscriber itself. The delivery that finished the day and the broken heart that ended the run were therefore both played **underneath a popup**. `Show` is now the wait and `ShowNow` is the old body, scheduled through `DOVirtual.DelayedCall(...).SetLink(gameObject)`. **The popup is delayed; the outcome is not** — and that split is the whole decision. `LivesManager` sets `IsAwaitingContinue` and `TicketSlotManager` sets `IsDayComplete` before either publishes, so the day is already held for the entire wait: the clock is stopped, the board refuses every pickup on the loss side, no ticket is assigned on the win side, every slot is empty so `TrayManager` refuses every drop, and D-099's deferred scatter is still sitting in the tray. **Delaying the EVENT instead was rejected** for precisely that reason: the hold and the popup would come apart and the day would keep playing behind the animation the wait exists to show. **The two numbers live in `BoardAnimationConfig`**, not in a popup config and not as scene fields, because each one's job is to be longer than durations already in that file — the win delay than `TraySettleDuration + DeliveryGrow + DeliveryFade + DeliveryReentry` (D-100 added the first of those), the loss delay than `LifeLostHeartDuration`. A number kept away from the ones it must outlast can be tuned into covering the very animation it was added to reveal. Both default to 2.5s; both read 0 as “instant”, which is the old behaviour. **Both references are OPTIONAL and outside `ValidateReferences`, and that is the risk decision rather than laziness:** the failure this project has actually suffered is a popup that never appeared — an empty `rewardFlight` slot once failed validation, returned out of `Start`, and left a finished day showing nothing at all. So an unwired config costs the pause and nothing else, and a forgotten drag can never strand a day. Adding the fields made `BoardAnimationConfig.asset` stale in the way D-075 documented; the re-serialize menu item is part of shipping this.

D-102: **The last life loses its tray the same way every other life does.** The user reported that no tray animation plays when their life ends. It was true, and the cause was one day old: **D-099 defers the last life's scatter by leaving the tray UN-CLEARED**, and `WorldTrayView.TryAcceptDrop` forks on `count == 0`. So the deferred case fell into the ordinary "tray not full yet" branch, the item settled quietly into its slot, and the shake, the wrong-order visual and the scatter never ran — three lives fail loudly and the fourth fails silently. **D-099's own justification had already expired**: it names exactly one hazard, "throwing this tray's items across the board is the most visible thing that can happen behind a Game Over popup", and D-101 gave that popup a 2.5s delay — there is no behind any more. So the deferral is removed and the final wrong order shakes, flashes and scatters like the three before it. **`deferredScatters`, `ResolveDeferredScatters` and its `DiscardAllForNewDay` clear are deleted rather than left unset**, along with `GameManager.Update`'s call: a deferral nothing sets is dead code, and dead code shaped like a safety net is worse than no safety net. **The view side needed no change at all** — clearing the tray is what puts `TryAcceptDrop` back on its existing `newCount == 0` fork, and `PlayWrongOrderShakeThenScatter` then runs as written. That is the tell that the deferral was never a visual decision: it changed a MODEL fact the view reads to decide which beat to play. **The TIMEOUT half of D-099 stays**, and the asymmetry is the point: `ScatterBackToBoard` only returns the tray's own items and assigns nothing, while a deferred cancellation replaces the ticket and spawns a whole board round — a fresh order appearing on a day that just ended is wrong whether or not a popup is covering it. **A last-life TIMEOUT therefore still shows no tray scatter, and that is a named remainder rather than an oversight**: closing it would mean letting `TrayManager` scatter on behalf of a ticket `TicketSlotManager` has not cancelled, across a dependency arrow that is one-directional by design. The four EditMode tests were **inverted, not deleted** — the last-life test now asserts the scatter happens, and `DiscardAllForNewDay` keeps a test of the property it always really had (it empties without scattering, because the board is wiped in the same reset). `GameOverPopupDelay`'s tooltip gained the scatter: the wait must now clear `ScatterShakeDuration` and the board fly-in as well as the heart, and 0 puts the scatter back under the popup — the old problem, now an authored choice with its consequence written down.
D-103: **The settings button greys out instead of vanishing, and the resume flag is cleared before the event that announces it.** The user reported that after a failed day the HUD's settings button stays gone until they place something in the tray, and asked for the visibility toggle to become an `interactable` toggle — explicitly adding that the display fault must not simply become an interactability fault, i.e. Retry must leave the button usable. Both halves were needed, because **the second half was a real ordering bug hiding behind the first**. `LivesManager.RefillLivesAndResume` wrote `state.Lives = state.MaxLives` and only then `state.IsAwaitingContinue = false`; `GameState.Lives` publishes `LivesChanged` **from its setter, synchronously**, so `SettingsPopupView.OnLivesChanged` ran inside that assignment and read the flag as still `true`. It switched the button off again on the very frame Retry was meant to bring it back, and since nothing publishes afterwards the button stayed gone until the next `LivesChanged` — **which is the next life LOST**, i.e. the wrong tray drop the user described. Switching to `interactable` alone would have shipped the identical bug as a permanently grey button. **The fix is the order, not a re-read:** the flag is cleared before the Lives write, so every synchronous subscriber sees a resume that has already finished, and the "full bar of lives while still awaiting Continue" pair — true for no frame a player can observe — is never handed to anyone. This is the third time this project has been bitten by `EventBus.Publish` being synchronous, so the rule is written down here: **a state change and the event announcing it are not independent; the state is finished first and the publish goes last.** `LoseLife`'s ordering is deliberately NOT mirrored — it sets the flag after the drop, but no subscriber reads it on that edge, and rearranging a working path for symmetry is churn. **The button now stays active for the whole day and only `interactable` moves**, with `Start` switching the object on once so a scene saved while the old code had hidden it still opens correctly; nothing touches its active state after that. The disabled LOOK is the Button's own Disabled transition, authored in the scene — no colour is written in C#, and the root invariant holds. **`Open()`'s guard is unchanged**: a day that is over, won or lost, still refuses, for D-094's original reason (the popup on screen already offers those exits under rules that fit it, and this menu's Retry is the lost-day path). **The completed day greys the button too**, not just the failed one — they share the one code path, and a control that vanishes on one ending and dims on the other would be worse than either. **Two EditMode tests pin the order from the only place it can be observed**, a `LivesChanged` subscriber asserting the flag is already clear inside the handler; the existing end-state assertions stayed green throughout the bug, which is exactly why the new ones assert something else — what broke was never the final values, it was the state the event carried on the way there.

D-104: **Withdrawn.** A camera shake on the wrong-order fail animation was built on 2026-08-27 and taken straight back out at the user's request ("beğenmedim, geri al"). `CameraShakeView.cs` was deleted, every touched file reverted, and the three `BoardAnimationConfig` fields removed. The number is retired rather than reused, so a comment anywhere referring to D-104 is referring to something that never shipped. The reasoning is preserved in the preflight archive only.

D-105: **An empty powerup is a way into the shop, and the day stops while it is open.** The user placed an inactive "Add" object inside each of the three powerup buttons in `SampleScene` and asked for it to light up when that powerup hits zero, for a tap on a zero powerup to open the shop "like on the main screen", and for the game behind it to stop. All three parts reverse a rule this project had written down twice — **"buying happens on the main screen only; the day scene only spends"**, and **"pressing a powerup with no charges does nothing at all, no popup"** (GDD 5.2, root `CLAUDE.md`). Both are now void, and the reversal answers the old rule's actual argument rather than ignoring it: the objection was that *opening a store mid-service suspends the very time pressure a powerup exists to relieve* — and the day is **held still** behind this shop, so the pressure is paused rather than suspended, which is a different thing from a store that lets the clock run. **No second shop was written.** `PowerupShopView` is the same component on both screens; it gained a public `Open()` that reports whether the panel actually came up, an idempotent `Close()`, and a `Closed` event, and its `openButton` left the required list because in the day scene there IS no opener — the empty powerup is it. `panel` and `closeButton` stay required, because a shop with no way out is a trap when it is also the thing holding the day. **The view still knows nothing about pausing**, which is the property that lets one component serve a screen with no day behind it: the caller holds the pause. **`PowerupBarView` is that caller**, and the branch lives in `OnPressed`, read from the LIVE charge count rather than from what `Render` last drew — the count moves under an open shop, and a stale read would spend a charge the player meant to buy. Order is load-bearing: `shop.Open()` must SUCCEED **before** the pause is taken, or a shop that could not open leaves a day frozen with nothing on screen able to lift it. It is gated on `GameManager.CanUsePowerups` — now public — so buying under the Game Over popup, over the day-complete receipt or mid-tutorial is refused for the same three reasons spending there is. **The `Add` badge is switched on the COUNT alone, deliberately not on "a shop is reachable"**: it says *this powerup is empty*, which is true either way, and a badge that also encoded whether a reference had been dragged would make a wiring mistake look like a full stock. Everything degrades to the pre-D-105 behaviour with no shop wired: dim, unpressable, no popup — and `ShopIsReachable` additionally requires a `PowerupManager`, because `Start` attaches no click listeners without one and a live button that does nothing is worse than a dim one. **`GameManager` became the single writer of `GameState.IsPaused`** (`HoldPause`/`ReleasePause` over a `HashSet` of holders), and `SettingsPopupView` was migrated onto it. That is the root single-writer invariant, and the bug it prevents is concrete: two panels can now freeze a running day, and two views each assigning one bool means whichever closes first starts the day running underneath the other. **A set, not a counter** — a counter's failure is silent, since one panel releasing twice lifts someone else's hold; adding or removing a holder that is already in or out is a no-op, which is what makes releasing from both the close path and `OnDestroy` correct rather than merely tolerated. **The day-scene shop gets a full-screen raycast-catching backdrop and the menu's does not**, and that asymmetry is the second half of "the game stops": `IsPaused` stops the ticket CLOCK, not a finger. The board sits in the half of the screen the bottom sheet does not cover, and `BoardItemDragHandler` routes through the `EventSystem` — so without something swallowing those touches a player could keep dragging items into trays, and batch-deliver an order in a day that is meant to be held still. The backdrop deliberately does **not** close on tap: its whole job is to swallow, and a mis-aimed tap dismissing a shop mid-purchase is worse than reaching for the close button already sitting in the sheet's corner. `PowerupShopSetup` builds both variants from one file rather than growing a second near-identical step to drift out of sync, and fills the bar's `Shop` field itself.

D-106: **The powerup shop's table gets a legend.** The user looked at the built panel and asked for the column headings the table never had: `name` over the powerup names, `count` over the owned numbers, `price` over the Gem prices. **Built as a ROW, not as three placed labels**, through the same `HorizontalLayoutGroup` and the same three column widths the powerup rows use — that is the only construction that keeps a heading over its column, because the panel's width is a fraction of the screen and therefore unknown at build time, so anything hand-placed drifts the first time the shop opens on a different aspect ratio. **The unlabelled `BuySpacer` is load-bearing rather than tidiness:** without a fourth element of `BuyWidth`, the layout group hands the BUY column's width to the three headings to share and every one slides right of what it labels. It is deliberately blank — a heading over a column of buttons has nothing to say. **No background Image, dimmer and smaller type**, which is the whole visual difference between a legend and data: a heading carrying the rows' faint tint reads as a fourth, unbuyable powerup, exactly the mistake a legend must not make. The three strings are constants beside the existing row labels under the same rule those were written under — fixed identities of the columns, written once into scene objects the author is free to retype, nothing reads them back, and nothing about them varies per save or per Day. **`PowerupShopSetup` will now ADD this to a shop it did not build**, which is the first exception to its "reports, never repairs" posture and a narrow one: `TryAddHeaderRow` inserts a first child and touches nothing else, so hand styling survives — whereas rebuilding the panel to obtain three labels would be the destructive way round. **Whether a header is already present is decided STRUCTURALLY, never by name** (D-028; `MetaGroundsSetup` is the cautionary tale, where a renamed object made a name search find nothing and it would have built a second hierarchy while logging that it had not): the three wired BUY buttons name the three real rows, so a rows container whose first child is not one of them already has something there and is left alone. Rename the header, restyle it, or replace it with your own and re-running the step does nothing; delete it and it comes back.

D-107: **The shop closes on a tap outside it.** The user asked for it plainly while checking the wiring: "bu shop dışında bi yere tıklarsak pencere kapansın". It **reverses D-105**, which built the day-scene backdrop as deliberately non-dismissing on the reasoning that a mis-aimed tap would close a shop the player is mid-purchase in, with a close button already sitting in the sheet's corner. That cost is real but small next to what it bought: a full-screen block whose only exit is a corner X is the shape players read as being trapped, and every other modal on a phone closes this way. **It needs no inside/outside hit-test, and that is why it is a wired `Button` rather than a pointer handler doing geometry.** The sheet is a CHILD of the backdrop and carries its own raycast-target Image, so UGUI hands a tap on the sheet to the sheet and only a tap outside it ever reaches the backdrop — the hierarchy does the filtering, and there is no rect comparison to drift out of step with a restyle. The field (`backdropButton`) is OPTIONAL, like `openButton`: the main screen's shop is a bottom sheet with nothing behind it to tap, and a shop without one behaves exactly as it did. **Transition is None**, which matters more here than on an ordinary button: the default ColorTint would flash the whole dim layer lighter on every press, so a tap near the sheet's edge would look like the screen blinking. Closing this way goes through the same `Close()` as the X, so it raises `Closed` once and the day-scene caller releases its pause — the pause needs no new path. `PowerupShopSetup.BuildBackdrop` now returns the Button and fills the field, so a freshly built day-scene shop has this without any dragging.

D-108: **The meta shop gets the powerup shop's modal treatment.** The user asked for it in one sentence — "başlık ekleme header ekleme ve arkada panel ekleyip ona da dokununca çıkma ve sağ üst köşeye x şeklinde kapatma tuşu ekleme gibi şeyleri meta shopuna da yapalım orada sadece item listesi var" — i.e. the four things D-105 to D-107 gave the powerup shop: a title, a column-heading row, a full-screen backdrop that dismisses on an outside tap, and an X in the corner. Before this the meta shop was a bottom sheet with a list and nothing else: the only way out was the market button, back in the screen's opposite corner. **The code half is two optional fields and one handler**, and the constraint that shaped them is the class's oldest rule: `SetOpen` is the single owner of the panel's visibility, because the first shop's ghost leaked precisely when that ownership was split (D-024/D-027). So the X and the backdrop are CALLERS, never a second door — a button wired straight to `SetActive` would skip `ClosePreview` and `CancelPendingReopen` and reintroduce the same class of bug. They close unconditionally rather than toggling, unlike the market button: both are only reachable while the shop is up. **`panel` now points at the backdrop, with the sheet as its child**, which is what makes the block and the list appear and vanish as one modal — and it also means the backdrop correctly disappears during a preview, since the ghost stands on the map and must be visible. The outside-tap needs no inside/outside test: the sheet carries its own raycast-target Image, so UGUI hands it the taps that land on it and only the rest reach the backdrop. **This is NOT a re-do of D-038.** That removed a backdrop from the CONFIRM POPUP at the user's request, so CANCEL is the only way out of a preview; the preview is untouched here, and the two surfaces are different questions. **The editor half is the first setup step in this project that RESTRUCTURES an existing hierarchy** rather than only appending to one — it reparents the sheet under a new backdrop. That is justified by the alternative: rebuilding the panel to obtain a backdrop and three labels would throw away every bit of hand styling, which is the more destructive of the two mistakes available. It is guarded, and every branch goes through `Undo`, so one Ctrl+Z removes the whole upgrade. Nothing moves on screen, because the shop root is already full-screen and the backdrop inherits an identical rect. **Idempotency is structural, never by name** (D-028): the sheet is a direct child of the root until it is wrapped and a grandchild afterwards. The consequence is written down rather than hidden — delete the Title or the Header later and re-running will not bring them back, since the wrap is what the step asks about; only the X has a serialized field of its own to test. `BuildFresh` calls the same upgrade method instead of carrying a second copy of the four builders, so a fresh shop and an upgraded one cannot drift into looking different. **The headings copy their horizontal placement off the row template's own `Name`/`Price` rects**, read through `SerializedObject` because those fields are private — and that is not a shortcut but the only correct construction: a meta row is hand-anchored (icon left, name stretched, price and BUY right) rather than laid out by a group, so nothing else guarantees a heading stays over its column when the list is restyled. The `Header` sits in the SHEET and never in the scrolling `Content`, because a legend that scrolls away with the list is worse than none. `closeButton` and `backdropButton` are deliberately kept OUT of `RequiredFields`: that list drives `TryReplaceIncomplete`, which offers to DESTROY the shop and rebuild it, and an optional addition must never be able to trigger that dialog.

D-109: **The drop that succeeds gets its own haptic — D-071's silence there was a mistake, not a design.** `OnEndDrag`'s successful-placement branch was the last half-gesture in the drag: `ItemPickup` answered the grab and nothing answered the release. D-071 left it silent on the reasoning that "a relocation is the drop WORKING, not failing", which is a correct observation with the wrong conclusion — dropping into a tray is also the drop working, and that has buzzed since D-070. The user caught it by feel, from the player's side rather than the code's. `ItemPlacedOnBoard` now fires there. **One moment for both origins the branch covers** — board-to-board and tray-back-to-board — because `ItemPickup` does not care where the item came from either; splitting would make the same gesture feel finished or unfinished for a reason the player cannot see. If board shuffling turns out noisy on a device there are two cheap exits, both recorded at the line: untick `enabled` in the asset, or narrow to `!wasOnBoard`, which the branch already computes one line above. **`RigidImpact` was chosen because it was the last impact preset with no default row**, and that was the point rather than a coincidence: the drag now has four outcomes and each needs its own answer — `LightImpact` picking up, `RigidImpact` onto the board, `MediumImpact` into the tray (the real move, so the fuller one), `SoftImpact` refused. Sharing `DropRejected`'s `SoftImpact` would have made "it landed" and "it did not" identical in the hand, which is the one distinction this branch exists to draw. **Priority 16 is for readability, not arbitration**: this branch publishes `CellChanged`, nothing haptic subscribes to it, and delivery/life-loss only ever arrive through the tray path — so it collides with nothing and simply sits inside its own gesture family between `ItemPickup` (10) and `DropRejected` (18). **Appended to the enum, for the fourth time and the same reason** (D-071, D-072): the asset holds eighteen authored rows and each stores a value, so an insertion would re-point every later row; `OnValidate` grows it to nineteen on the next load without touching what is there. **No test and no editor task** — this adds a moment rather than a rule, `TheDefaultTable_CoversEveryMoment` iterates the enum so a missing seed turns red on its own, and no serialized field was added, so there is nothing to drag. **Verification**: `ExpoTheExplorer.Data`, `Assembly-CSharp` and `ExpoTheExplorer.Tests.EditMode` build clean, 0 errors; both touched files were confirmed present in Unity's Compile lists first. EditMode suite still owed — Unity holds the lock. **Still true after four passes**: none of the nineteen moments has ever been felt on real hardware — Nice Vibrations does not vibrate in the editor — affects: HapticConfig.cs, BoardItemDragHandler.cs, codemap-core.md, codemap-ui.md — 2026-08-25

D-110: **Auto-Collect decides the whole press before it moves anything, and the board it decides on is the one the player pressed.** The user reported two faults in one sentence — it fetches ingredients that were not on the board, and it sometimes leaves an order unfinished that could have been finished — and then dictated the algorithm: know what is on the board first; walk the tickets in order and finish the ones the items in hand can finish; then put what is left into the tickets that were not finished; and **do not look at the tickets that arrived after that first round**. Both faults came from one shape. `TryFindAutoCollectItem` answered "what should this slot take NEXT" and `AutoCollectRunner` re-asked it after every single move, against the LIVE board. **Fault one is the backfill.** `BoardGrid.RemoveItem` refills the cell it empties from the pending-spawn queue, and a delivery cascades synchronously into the next ticket's required items spawning — so the moment slot 0 delivered, slots 1 and 2 helped themselves to what that delivery had just produced. The existing `ReferenceEquals` guard stopped only the DELIVERING slot from continuing; it never stopped the other two from eating the new arrivals, and from the player's chair that is ingredients appearing from nowhere. **Fault two is greed.** Walking slot 0, 1, 2 and grabbing whatever each one happened to want is blind to the others: one burger on the board, slot 0 wanting burger + a cola that exists nowhere, slot 1 wanting only that burger — slot 0 took it, stranded it in a tray that could never resolve, and the completable ticket got nothing. **The fix is a PLAN, not a better search.** `PlanAutoCollect` reads the board ONCE into a budget of `key -> cells` and pays every ticket out of that fixed pot: pass 1, slots in order, completes any ticket the budget can pay IN FULL; pass 2, slots in order, partially fills only the ones pass 1 could not. Asking "can this be FINISHED" before spending anything is the entire second fix, and closing the list before the first move is the entire first one — an item that spawns during the run cannot enter a list that was closed before it existed, and neither can a ticket. **Coordinates survive the mutation and are still not trusted:** removing an item only ever backfills the cell it emptied, never moves another, so a planned cell stays valid — but each move re-checks the exact `BoardItem` INSTANCE at its cell before taking it, because taking whatever landed there instead is precisely the bug being removed. That is why a move carries its item and not just an (x, y). **Greedy in slot order rather than an optimal assignment**, which is the rule the user asked for and can never complete fewer tickets than the old code did. **A tray's junk goes home instead of being worked around.** The first draft SKIPPED a slot whose tray held something its ticket never asked for, since that tray can never resolve; the user rejected that and asked for the item to be thrown back on the board and counted in — "eğer istenmeyen malzeme varsa onu boarda atıp dahil etse". So `UnwantedTrayItems` names them (a mis-drop, or a surplus copy beyond the required count) and `WorldTrayView.ReturnItemsToBoard` sends them back BEFORE the snapshot is taken, which both un-jams the tray and returns the item to the pot for whichever ticket does want it. It needed no new tray API: `TrayManager.RemoveItem` already exists for a player pulling an item out by hand, and the visual half is the move `BoardItemDragHandler.OnEndDrag` already performs for a tray pickup dropped somewhere invalid — remove, `RequestSpawn` under a fly-in override from this tray, destroy the object. It lives on `WorldTrayView` because a tray item's GameObject is a child of one of its three slots and nothing else can take it out without leaving the model and the view disagreeing. **`lastKnownCount` is resynced in that same call**, because `Update`'s poll reads a drop to exactly zero as "cleared externally" and would run `ClearAllSlotVisuals` over objects that are already gone. **THE POWERUP STILL CANNOT COST A LIFE, and that is now structural rather than incidental.** The tray's batch check fires the instant the tray is FULL, so the plan either fills a slot exactly — a correct order, hence a delivery — or deliberately leaves one space free (`MaxMovesFor` is the tray's free space, and pass 2 stops one short of it). The old code had this property by accident, from only ever offering items the ticket was short of; a tray holding junk the runner could not send home (the player is holding it mid-drag) would have broken it, and now cannot. **A press that only sent items home still costs a charge**, asked and answered explicitly: the board visibly changes and a tray guaranteed to cost a life is un-jammed, so `Run` returns true for it. **`MaxMovesPerSlot` is deleted with the loop** — a closed plan cannot spin, and a hang guard shaped like a design number is worse than none. **Verification**: `ExpoTheExplorer.Systems.PowerupSystem`, `Assembly-CSharp` and `ExpoTheExplorer.Tests.EditMode` build clean, 0 errors. The nine `TryFindAutoCollectItem` tests were REPLACED, not deleted — thirteen `PlanAutoCollect` cases and six `UnwantedTrayItems` cases, including one that pins the stranded-burger report and one that pins the never-fills-a-doomed-tray property. EditMode suite still owed — Unity holds the project lock, so it must be run from the Test Runner window — affects: PowerupEffects.cs, AutoCollectRunner.cs, WorldTrayView.cs, PowerupEffectsTests.cs, codemap-core.md, codemap-ui.md — 2026-08-27
