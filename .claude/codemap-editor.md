<!-- stamp: 29b329a 2026-08-20 status: DEGRADED 3 stale, 0 orphan, 2 missing-role -->
STALE ExpoTheExplorer/Assets/Editor/DayEditorModel.cs | ticket/board data model + Day Editor inspector GUI (per-Day food selection restricts Generate and the ticket item pickers; Day Start authored via the click-to-place grid editor; per-Day board-distribution balancing and star thresholds authored here) | sys: DayEditor | api: Generate(); Save(); Configure(FoodCatalog catalog, GameConfig gameConfig, TicketGenerationConfig ticketConfig, TicketCardVisualsConfig ticketCardVisuals, BoardVisualsConfig boardVisuals, Action<DayEditorModel> onSave, Action<DayEditorModel> onDuplicate, Action<DayEditorModel> onDelete); ToDayJson(); ToDayDefinition() | dep: BoardDistributionJson,BoardSpawnEntryJson,BoardVisualsConfig,DayContentGenerator,DayDefinition,DayEditorDayStartPreview,DayEditorMetaJson,DayEditorTicketCardPreview,DayJson,DayValidationResult,DayValidator,FoodCatalog,FoodCategory,FoodItemConfig,GameConfig,GuaranteedTicketCountMode,Modification,ModificationConfig,ModificationEntryJson,PatienceType,ResolvedBoardDistribution,ResolvedBoardSpawnEntry,ResolvedTicketEntry,Ticket,TicketCardVisualsConfig,TicketEntryJson,TicketFactory,TicketGenerationConfig | used: DayEditorWindow,DayEditorTicketCardPreview,DayEditorModelConverterTests | crit: K3 | note: the food grid sizes its columns from the width measured on the last repaint, never EditorGUIUtility.currentViewWidth -- that includes the Day list and made the grid demand a wider row than the content column had, which forced a horizontal scrollbar onto every row in the inspector. Numeric settings use Odin's [PropertyRange], NOT UnityEngine.Range -- the Unity attribute's slider ran past the window's right edge and its number field was never reachable. FromJson tolerates a Day file with no boardDistribution block (falls back to designed defaults) where DayCatalogParser refuses it, so an old Day can still be opened and re-saved | h:5369591b
ExpoTheExplorer/Assets/Editor/DayEditorTicketCardPreview.cs | hand-drawn IMGUI ticket card strip preview (drag-to-reorder, hand-clipped horizontal scroll) | sys: DayEditor | api: DrawStrip(List<DayEditorTicketEntry> entries, TicketCardVisualsConfig visuals, ref Vector2 scrollPos, ref int selectedIndex, ref int draggedIndex, ref Vector2 dragStartMousePos, ref float lastViewWidth) | dep: DayEditorSpriteGUI,DayEditorTicketEntry,FoodItemConfig,Modification,ModificationConfig,PatienceType,TicketCardVisualsConfig | used: DayEditorModel | crit: K3 | note: scrolls via GUI.BeginClip + an explicit HorizontalScrollbar, NOT a nested scroll view -- a nested one swallowed the mouse wheel and turned page scrolling into strip scrolling. Everything inside the clip must stay rect-based GUI.* (no GUILayout). Whether the scrollbar is emitted is decided from lastViewWidth, not from the fresh rect: GetRect reports 0 during layout passes, so reading it raw made layout and repaint disagree on the element count and the strip refused to scroll | h:77ad9ca7
ExpoTheExplorer/Assets/Editor/DayEditorDayStartPreview.cs | single-grid Day Start (TriggerStepIndex == -1) board preview: left-click-drag relocates an occupied cell's item (swaps if the target is occupied too), right-click toggles cell selection for the item/modifications editor | sys: DayEditor | api: DrawGrid(GameConfig gameConfig, BoardVisualsConfig visualsConfig, List<DayEditorBoardSpawnEntry> boardTimeline, ref int selectedX, ref int selectedY, ref int draggedX, ref int draggedY, ref Vector2 dragStartMousePos) | dep: BoardGrid,BoardVisualsConfig,DayBoardTimelinePlayer,DayEditorBoardSpawnEntry,DayEditorSpriteGUI,GameConfig | used: DayEditorModel | crit: K3 | note: - | h:3af3ceea
ExpoTheExplorer/Assets/Editor/DayEditorSpriteGUI.cs | shared atlas-sprite + layered IMGUI draw helpers | sys: DayEditor | api: DrawSpriteFit(Rect rect, Sprite sprite); DrawTexCoords(Rect rect, Sprite sprite); DrawLayeredSprite(Rect rect, Sprite sprite, Vector2 offsetFraction, float scale) | dep: - | used: DayEditorTicketCardPreview,FoodItemConfigEditor,MetaLocationLayoutGUI | crit: K3 | note: - | h:825991b2
ExpoTheExplorer/Assets/Editor/MetaEditorWindow.cs | Meta catalog authoring window + per-location page | sys: MetaSystem | api: MetaEditorWindow.DefaultLabelWidth; BuildMenuTree(); DrawMenu(); OnBeginDrawEditors(); MetaLocationPage(MetaCatalog catalog, int locationIndex).LocationIndex | dep: MetaCatalog,MetaCatalogValidator,MetaLocation,MetaLocationLayoutGUI,MetaLayoutState | used: - | crit: K3 | note: clicking a prop on the canvas draws THAT prop's fields directly under the picture, which is the point of the window -- and the panel shows only the unlock field its mode actually reads (price for Purchase, unlockAtDayIndex for DayUnlock), which removes the one real cost of D-017's enum discriminator: nothing to remember, and no tuning a number that is ignored. The full location/prop list is a collapsed foldout below, for structure only (add/remove/reorder). Add/remove LOCATION buttons sit in a strip above the tree via a DrawMenu override, not in the top toolbar the way DayEditorWindow puts "+ New Day" -- here the toolbar already belongs to the catalog and the selected location. Two traps handled there: Unity's InsertArrayElementAtIndex DUPLICATES the previous element, so every field of a new location is set explicitly and its items array cleared or "+ Location" would silently mean "duplicate location"; and a new location inherits the previous one's unlockAtDayIndex rather than 0, which keeps the list in the non-decreasing order the validator expects instead of claiming a second location is available from day one. Remove is confirm-gated and paired with add deliberately: this window shows one location at a time and never the list as an editable array, so an add with no remove would make a mis-click permanent junk. A prop LIST sits to the right of the canvas, same height, and replaced an earlier toolbar dropdown: a dropdown shows one entry at a time, while placing props wants the whole set visible with the selected one obvious. It also guarantees reachability -- a prop authored far outside 0..1 (which the validator warns about rather than forbids) draws off-canvas and would otherwise be reachable only by hunting it down in the raw list. Rows carry the list index so blank or duplicated ids stay distinguishable, and are drawn in LIST order rather than draw order because that is how an author refers to a prop and how validator messages number them. Each row has a PREVIEW visibility toggle (plus Show/Hide all): editor-only, never serialized, because at runtime visibility is decided by ownership and unlock state and a saved flag would be a second authority arguing with the resolver -- it exists to see what sits BEHIND something while placing it. Hidden props are also unclickable, since a target you cannot see changing the selection reads as a bug. The hidden set is keyed by INDEX and cleared whenever the prop count changes (MetaLayoutState.SyncTo) -- cruder than remapping, but it cannot silently hide the wrong prop. Same OdinMenuEditorWindow shell as DayEditorWindow but the OPPOSITE division of labour -- the Day Editor owns a mutable model and writes JSON on Save, this edits the ScriptableObject in place through SerializedObject so Unity's own dirtying/undo/Ctrl+S apply and there is no second copy to keep in step. Odin draws only the chrome and the tree; every field goes through Unity's own property drawer on purpose, so the schema can change without this file guessing. ONE ApplyModifiedProperties per repaint is load-bearing: it makes a drag undo as one gesture instead of one step per mouse-move. sys is MetaSystem, NOT a new MetaEditor system -- an asymmetry with blueprint's DayEditor, accepted because two files do not earn a system; split it out if the window grows. SerializedObject is rebuilt lazily, never cached across domain reloads (a stale one throws on first access and the window outlives a recompile) | h:86317c2f
ExpoTheExplorer/Assets/Editor/MetaLocationLayoutGUI.cs | draggable prop layout canvas over a location background | sys: MetaSystem | api: MetaLayoutState(SelectedIndex; DraggingIndex; Zoom; Pan; IsPanning; Hidden; KnownItemCount; SyncTo(int itemCount); IsHidden(int index); SetHidden(int index, bool hidden); ResetView()); Draw(Rect area, MetaLocation location, SerializedProperty locationProperty, MetaLayoutState state) | dep: MetaLocation,MetaItemDefinition,DayEditorSpriteGUI | used: MetaEditorWindow | crit: K3 | note: the one thing the window exists for -- dragging beats typing twenty pairs of normalized floats. EVERY vertical conversion is flipped: GUI space Y grows downward, a normalized UI position grows upward (the trap DayEditorSpriteGUI's own comment names); getting it wrong mirrors the layout vertically, which is loud rather than silent. Prop size is DERIVED as sprite pixel size x the background's fit scale, mirroring the runtime, because D-015 deliberately left no per-prop scale field -- this stays a preview rather than a second opinion. Mutations go through SerializedProperty, never the plain MetaLocation objects: the locations live inside a ScriptableObject's list, so a direct write would change memory and leave the asset unmarked, surviving only until the next domain reload. Hit-testing walks TOPMOST first so overlapping props pick what the eye sees, and it covers the WHOLE canvas area rather than the background rect -- gating on the background made every prop that overhangs an edge unselectable, which was a real reported bug (2026-08-20): a portrait background sits centred in a wide area, so an overhanging prop's visible part is precisely out there. VerticalSlack exists for the same reason on the other axis: fitted to the full height a portrait background leaves nowhere to DRAW an overhang, and it would paint over the panel below. ZOOM (wheel, 1x-12x) and PAN (middle-drag OR right-drag; the right-button path only works because EventType.ContextClick is swallowed over the canvas first -- the menu wins the event before MouseDrag ever arrives) needed almost no new maths, because every prop rect and hit test derives FROM the background rect -- growing that one rectangle scales sprites, click targets and drag sensitivity together, and a drag delta divided by the ZOOMED width is exactly the precision zoom exists to buy (a 70x104 plant draws ~20px when a 853x1844 background is fitted). It runs inside GUI.BeginClip, which is what zoom REQUIRES: a zoomed background is larger than the canvas and would otherwise paint over the panels beneath. Consequences of that clip: all coordinates are clip-local (BeginClip translates Event.current.mousePosition to match), and the selection outline is drawn with EditorGUI.DrawRect rather than Handles, because Handles goes through GL and would ignore the clip. Zoom pins the point under the cursor by MEASUREMENT (re-derive the rect, shift Pan by how far the point moved) rather than by algebra, so it stays correct if the rect definition changes; Pan is dropped when Zoom returns to 1, or zooming out would leave the background off-centre with no obvious recovery | h:f5f8d5f5
ExpoTheExplorer/Assets/Editor/DayEditorWindow.cs | Day Editor EditorWindow: menu tree + config toolbar + fixed-pixel label width + page scroll view | sys: DayEditor | api: - | dep: BoardVisualsConfig,DayEditorModel,DayFileIO,FoodCatalog,GameConfig,TicketCardVisualsConfig,TicketGenerationConfig | used: - | crit: K3 | note: the toolbar's config fields are MinWidth/MaxWidth, never fixed -- a fixed floor there widens the whole content column and pushes every row past the right edge. UseScrollView is overridden to true -- without it the page had no scroll view at all and content ran off the bottom unreachable. DefaultLabelWidth is overridden in PIXELS -- Odin reads 0..1 as a percentage, and the inherited percentage default made the label column grow with the window so the value column never gained room. Unity [MenuItem] window -- opened from the Editor menu, not called by project code | h:253950f0
ExpoTheExplorer/Assets/Editor/DayFileIO.cs | MISSING-role | sys: ? | api: GetFilePath(string daysFolderPath, int dayIndex); Save(string daysFolderPath, DayJson dayJson); Delete(string daysFolderPath, int dayIndex); LoadAll(string daysFolderPath) | dep?: DayEditorWindow,DayJson,Editor | used: ? | crit: ? | note: auto-added; AI must complete | h:78ac4d99
ExpoTheExplorer/Assets/Editor/ExpoTheExplorer.Editor.asmdef | MISSING-role | sys: ? | api: asmdef ExpoTheExplorer.Editor | dep?: ExpoTheExplorer.Data,ExpoTheExplorer.Core,ExpoTheExplorer.Systems.DaySystem,ExpoTheExplorer.Systems.TicketSystem | used: ? | crit: ? | note: auto-added; AI must complete | h:fa5dbd56
ExpoTheExplorer/Assets/Editor/FoodItemConfigEditor.cs | FoodItemConfig layered board-preview inspector | sys: DayEditor | api: OnInspectorGUI() | dep: BoardItem,DayEditorSpriteGUI,FoodItemConfig,Modification,ModificationConfig,ModificationDirection | used: - | crit: K3 | note: Unity CustomEditor -- invoked by the Inspector, not called by project code | h:49cd01c7
STALE ExpoTheExplorer/Assets/Editor/TicketGenerationConfigEditor.cs | main-dish spawn-chance preview for the seed asset | sys: TicketSystem | api: OnInspectorGUI() | dep: DayEditorSettingsPreviews,TicketGenerationConfig | used: - | crit: K3 | note: delegates the maths to DayEditorSettingsPreviews, which the Day Editor draws too; this asset is a seed since D-006 | h:e33bd200
STALE ExpoTheExplorer/Assets/Editor/DayEditorSettingsPreviews.cs | shared read-only tuning previews: noise-leak distribution, guaranteed-ticket budget, main-dish spawn/modification breakdown | sys: DayEditor | api: DrawLeakPreview(int maxLeakCount, float noiseLeakCountLambda); DrawGuaranteedTicketPreview(GuaranteedTicketCountMode mode, int guaranteedTicketCount, float guaranteedTicketCountLambda); DrawMainDishPreview(IReadOnlyList<(FoodItemConfig Food, float Weight, float ModificationCountLambda, int MaxModificationCount)> entries, IReadOnlyList<FoodItemConfig> allowedFoods = null) | dep: FoodCategory,FoodItemConfig,GameState,GuaranteedTicketCountMode,MainDishWeight,TruncatedPoisson | used: DayEditorModel,TicketGenerationConfigEditor | crit: K3 | note: the two board previews are drawn side by side by DayEditorModel with no fixed widths (fixed widths are what made this window overflow sideways before), so the budget note under DrawGuaranteedTicketPreview is word-wrapped rather than truncated. DrawGuaranteedTicketPreview mirrors BoardDistributor's budget line including its +1 shift (the draw is over 0..SlotCount-1 then shifted, which is what keeps the budget from ever being 0) -- if that line changes, this preview silently goes stale, it has no test holding it. The maths lived in the two custom editors before the Day became where these numbers are authored; kept in one place so the Day view and the seed asset's view cannot drift. DrawModCountBreakdown truncates at Math.Min(available, MaxModificationCount), mirroring TicketFactory.Create -- rows above the cap are omitted rather than listed at 0%, and a note names the cap only when it is the shorter of the two | h:504ecdcd
ExpoTheExplorer/Assets/Scripts/UI/Editor/MainScreenSceneBuilder.cs | one-shot construction script for the MainScreen scene | sys: MainScreen | api: CreateMainScreenScene() | dep: MainScreenView | used: - | crit: K3 | note: menu item ExpoTheExplorer > Create MainScreen Scene, run ONCE; it REFUSES to overwrite an existing MainScreen.unity so restyling can never be wiped by a re-run. Exists because a Canvas+TMP scene hand-written as serialized YAML is the edit D-007 already declined to make and cannot be reviewed -- and because a scene file cannot fix Build Settings for itself (it puts MainScreen at index 0, which is what a built player launches). Since D-013 it no longer creates the CoinsCaption/CoinsValue/GemsCaption/GemsValue labels -- the shared HUD Canvas prefab shows the wallet in that scene, so building a second pair would put one balance on screen twice; WireView is down to dayValueText + playButton. Lives under Scripts/UI/Editor/ rather than Assets/Editor/ for a hard reason: it references MainScreenView, which is in the predefined Assembly-CSharp, and an asmdef assembly (Assets/Editor carries one) cannot reference a predefined assembly -- only an Editor folder OUTSIDE any asmdef compiles into Assembly-CSharp-Editor. shards.json still files it here: its first pattern is **/Editor/**. Wires the view's private [SerializeField]s through SerializedObject, so the property-name strings must stay in step with the field names. Resolves the Input System UI module by name rather than referencing it, so a checkout without that package still compiles | h:412230bf
ExpoTheExplorer/Assets/Scripts/UI/Editor/MainScreenSessionSetup.cs | menu step: give the open MainScreen a session and point the HUD at it | sys: MainScreen | api: Wire() [MenuItem ExpoTheExplorer/Meta/Wire MainScreen Session] | dep: MainScreenRoot,HudWalletSource,GameConfig,LivesConfig,FoodCatalog | used: - | crit: K3 | note: exists because the manual alternative is three drags, one of which (HudWalletSource's host) fails QUIETLY when forgotten. Same posture as HudCanvasPrefabSetup: touches only the already-open scene and does NOT save -- saving would commit changes before they are reviewed and destroy the undo stack, so it stays the author's call. Config fields are only FILLED when empty, since "the first asset of this type" is a guess and an author who deliberately picked a second one keeps it. Under Scripts/UI/Editor/ for the reason blueprint.md records: it references Assembly-CSharp types, which the ExpoTheExplorer.Editor asmdef cannot do at all | h:7f34aa02
ExpoTheExplorer/Assets/Scripts/UI/Editor/MetaGroundsSetup.cs | menu step: build the meta-grounds container and wire the view | sys: MetaSystem | api: Build() [MenuItem ExpoTheExplorer/Meta/Build Meta Grounds] | dep: MetaGroundsView,MainScreenRoot,MainScreenView,MetaCatalog | used: - | crit: K3 | note: builds the CONTAINER only (ScrollRect + masked viewport + background Image + location bar); the props are instantiated at run time because the catalog is their single authority (D-015). Refuses to REBUILD an existing hierarchy and only refreshes references, so a layout the author has adjusted by hand is never overwritten -- the same promise MainScreenSceneBuilder makes by refusing to overwrite a scene. Finds the canvas via MainScreenView rather than taking the first Canvas in the scene, because the HUD is its own Canvas prefab and building the grounds inside it would put them on the wrong sorting layer. The root is set as FIRST sibling: sibling order is the only thing deciding draw order in a Canvas, and the grounds are a backdrop that the day readout and Play must stay on top of. RectMask2D not Mask (no extra material, no stencil buffer), and ScrollRect movementType Clamped not Elastic -- a background that already fits should do nothing rather than bounce. Touches only the open scene and does NOT save, so the undo stack survives and saving stays the author's call | h:0e366ebc
ExpoTheExplorer/Assets/Scripts/UI/Editor/HudCanvasPrefabSetup.cs | finishes the hand-made HUD prefab + drops it into a scene | sys: MainScreen | api: WirePrefab(); AddToOpenScene() | dep: GameManager,HudWalletSource,SoftMoneyView,GemsView,LivesView,MainScreenView | used: - | crit: K3 | note: menu ExpoTheExplorer > HUD > 1/2 (D-013). It does NOT create the prefab -- that already exists, made by hand -- so step 1 only adds HudWalletSource and points the three views' walletSource at it, writing into the PREFAB ASSET via LoadPrefabContents/SaveAsPrefabAsset so every instance inherits it rather than one scene carrying a private override. Finds the prefab BY CONTENT (the prefab containing a SoftMoneyView), not by path: the first version of this file hardcoded Assets/Prefabs/UI/HudCanvas.prefab and a root named 'HUD Canvas' while the real asset is HUDCanvas.prefab with root 'HUDCanvas' -- AssetDatabase paths are case-SENSITIVE and macOS's filesystem is not, so File.Exists matched and LoadAssetAtPath returned null, failing in both directions at once. Step 2 instantiates into the OPEN scene, WRITES that scene's GameManager into the instance's HudWalletSource (a prefab override; absent on the main screen, which is the expected case and not an error) and deletes the four duplicate MainScreen labels by name, reporting rather than guessing when one is missing; it leaves the scene dirty so saving stays the user's call and Ctrl+Z works. Lives under Scripts/UI/Editor/ for the Assembly-CSharp reason in blueprint.md | h:955e1a81
