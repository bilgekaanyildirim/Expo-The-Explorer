<!-- stamp: df4d616 2026-08-29 status: DEGRADED 8 stale, 0 orphan, 0 missing-role -->
STALE ExpoTheExplorer/Assets/Editor/DayEditorModel.cs | ticket/board data model + Day Editor inspector GUI (per-Day food selection restricts Generate and the ticket item pickers, and derives the Main Dish Weights list; Day Start authored via the click-to-place grid editor; per-Day board-distribution and ticket-runtime balancing authored here; a Day's tutorial block is CARRIED through load/save but has no UI yet) | sys: DayEditor | api: Generate(); Save(); SyncMainDishWeights(FoodCatalog catalog); DayEditorModification.ForConfig(ModificationConfig config); DayEditorModification.CanFlipDirection(ModificationConfig config); DayEditorTicketGeneration.PatientTicketCount/NormalTicketCount/ImpatientTicketCount; DrawTileGrid(int count, ref float lastWidth, Action<Rect,int> drawTile); DrawModificationPicker(FoodItemConfig owner, List<DayEditorModification> mods, string ownerLabel, bool hideWhenEmpty = false); DrawItemSlot(string label, FoodItemConfig current, FoodCategory category); Configure(FoodCatalog catalog, GameConfig gameConfig, TicketGenerationConfig ticketConfig, TicketCardVisualsConfig ticketCardVisuals, BoardVisualsConfig boardVisuals, Action<DayEditorModel> onSave, Action<DayEditorModel> onDuplicate, Action<DayEditorModel> onDelete); ToDayJson(); ToDayDefinition(); MarkSaved(); RefreshUnsavedState(bool force = false); RevertToSaved(); HasUnsavedChanges; HasSavedState; CanRevert; IsSaveable; ValidationSummary | dep: BoardDistributionJson,BoardSpawnEntryJson,BoardVisualsConfig,DayContentGenerator,PatienceType,DayDefinition,DayEditorDayStartPreview,DayEditorSpriteGUI,DayEditorMetaJson,DayEditorTicketCardPreview,DayJson,DayValidationResult,DayValidator,FoodCatalog,FoodCategory,FoodItemConfig,GameConfig,GuaranteedTicketCountMode,MainDishWeight,Modification,ModificationConfig,ModificationDirection,ModificationEntryJson,PatienceType,ResolvedBoardDistribution,ResolvedBoardSpawnEntry,ResolvedTicketEntry,Ticket,TicketCardVisualsConfig,TicketEntryJson,TicketFactory,TicketGenerationConfig | used: DayEditorWindow,DayEditorTicketCardPreview,DayEditorModelConverterTests | crit: K3 | note: D-082 (2026-08-26) added the `Tutorial` field and the two lines that carry it through ToDayJson/FromDayJson. It has no inspector and is not meant to be edited here -- it exists because this class REBUILDS DayRuntimeJson field by field on every save, so any runtime field the model does not name is silently deleted the first time a Day is opened and saved. day_00 losing its forced first move because someone retimed a ticket is exactly that bug, and this is the one-line inoculation against it; the same trap is what the CopySettingsFrom comment already warns about for the balancing blocks. ToDayDefinition resolves it via DayCatalogParser.ResolveTutorial rather than re-implementing the enabled-means-present rule, which is what makes DayValidator's tutorial check fire on Save instead of only at runtime. the food grid sizes its columns from the width measured on the last repaint, never EditorGUIUtility.currentViewWidth -- that includes the Day list and made the grid demand a wider row than the content column had, which forced a horizontal scrollbar onto every row in the inspector. NO UnityEngine value attribute belongs anywhere in this file -- numeric settings use Odin's [PropertyRange]/[MinValue] only. This window is an OdinMenuEditorWindow over plain DayEditorModel objects, so there is no SerializedObject behind them and a Unity value attribute has to be bridged through an emulated SerializedProperty; that bridge broke drawing TWICE. [Range] put the slider past the window's right edge with its number field unreachable, and [Min] on DayEditorTicketRuntime made that box's height disagree between the layout and repaint pass (fields arriving a frame late while scrolling) while consuming the event against its own rect (the mouse wheel died over the box instead of scrolling the page). [Tooltip] is fine -- Odin reads it natively, it has no drawer. FromJson tolerates a Day file with no boardDistribution block (falls back to designed defaults) where DayCatalogParser refuses it, so an old Day can still be opened and re-saved. Main Dish Weights MEMBERSHIP is derived, not authored (D-052): DayEditorMetaModel.SyncMainDishWeights rebuilds it from the selected Mains in catalog order, called from DrawFoodSelection -- the single call site, chosen because that section owns the input and PropertyOrder -5 draws it before the list at -3, so file-open/tile-click/All-None/late-catalog/CopySettingsFrom are all covered. Existing rows are carried over BY REFERENCE so authored weight/lambda/max survive; the add/remove buttons and the row's Food picker are gone, or membership would have two writers. It is safe in a draw method only because MainDishWeightsMatch early-outs without allocating -- do not add LINQ to that path. Inspector order is ALL PropertyOrder and every value here is negative, because Save/Duplicate/Delete declare none and therefore sit at the default 0: reading top to bottom it is Food Selection -5, Day -4, Generation Settings -3/-2.9, Ticket Runtime -2.4, Generate -2, Ticket Sequence + Start Board -1/-0.5, Board Distribution -0.2/-0.15 (moved to the bottom on purpose -- it is the pass you make once the Day has content), then the buttons. A new block needs a value in that scale, not an arbitrary one, and a BoxGroup's two members must share a slot or the previews tear away from their knobs. Modifications are picked from a visual tile grid (D-054), ONE helper — DrawModificationPicker — serving both the ticket editor and the Start Board cell editor, which each carried a byte-identical copy of an unfiltered ObjectField before. Its options come from the owning food's AvailableModifications, the same authority TicketFactory.Create reads, and direction is seeded by DayEditorModification.ForConfig from AllowedDirection with only Both flippable (right-click) — so the editor can no longer author a dish/modification or direction pair the generator could never produce. Modifications the owning food does NOT offer are surfaced in a warning list with an x, never dropped silently; the badge falls back to a +/- label when TicketCardVisualsConfig is unassigned. The grid keeps its own lastModGridWidth: sharing the food grid's field would let two differently-sized sections overwrite each other's column count. Patience Mix (D-055) authors three ticket counts in editorMeta and DrawPatienceMixReadout shows what Generate will really lay down -- it calls DayContentGenerator.BuildPatiencePlan rather than repeating the padding/truncation arithmetic, because a preview with its own copy of the rule is a preview that lies. The readout lives on the model, not beside the counts, because it needs TicketsRequiredForDay, which DayEditorTicketGeneration cannot see -- the same split as the food selection. The mismatch warning is deliberately NOT a DayValidator rule: DayValidator sees a DayDefinition and these counts are editorMeta, which the runtime never reads (D-006), so moving them into DayDefinition to be validated would invert that arrow. THREE tile grids now share DrawTileGrid (Food Selection, the modification picker, the Start Board cell picker) and it is the ONE place the width-measured-on-repaint rule lives -- the food grid's scrollbar bug above is why, and per-tile drawing deliberately did NOT move because the three do different work. Each grid keeps its own lastWidth field: the cell picker draws inside a fixed 220px column and settles on ~3 columns while the others get the full content width, so one shared measurement would have them overwrite each other every repaint -- nothing in DrawTileGrid may assume the inspector's width. The Start Board cell's item picker (D-057) replaced a raw unfiltered ObjectField and is fed from AllowedFoodPool, which also closed a real hole: that field could author a board item DayValidator then refused, since the validator checks the board against the same pool. Clicking the selected tile again empties the cell, taking over from the old "None". An item outside the pool is surfaced in a warning rather than cleared. The ticket's own Main/Side/Drink stay name POPUPS (DrawCategoryItemField) -- not yet visual. The cell picker is TWO-STATE (D-058) and the cell itself selects between them, so there is no flag to keep in sync: empty draws the whole grouped grid, filled draws ONLY the item's own tile and hands the room to its modifications. Emptying needs no separate path -- clicking that tile again and the Clear Cell button both leave the cell empty and the next draw is the grid. DrawModificationPicker's hideWhenEmpty (cell editor true, ticket editor left at the false default, so that side is unchanged by construction) suppresses the header and the no-modifications box entirely, but NEVER the orphan list: D-054 made orphans visible because they still ship in the Day JSON, and quieting them to tidy an empty state would undo that decision sideways. The ticket's three food slots are visual too (D-059, DrawItemSlot) and DrawCategoryItemField is GONE with them -- that removed the last raw ObjectField in this file. Its category + AllowedFoodPool filtering was kept exactly; only the presentation changed, and its catalog-unassigned fallback to an unfiltered ObjectField became a HelpBox, matching what Food Selection and the cell picker already did. Slot grids stay OPEN when filled, unlike the cell picker: switching (Hotdog to Burger) is the common action and hiding the alternatives would cost a second click, whereas a filled cell hands its 220px to the modifications below it. All three slots SHARE lastTicketSlotGridWidth because they are drawn one after another in the same column -- the per-grid fields elsewhere exist for sections of different widths, which this is not. Modifications is drawn between Main Item and Side Item on purpose: the picker is keyed on entry.MainItem because TicketFactory takes a ticket's modifications from the main dish alone, so it belongs under the dish rather than under the whole ticket D-114 (2026-08-27): unsaved state is a COMPARISON against savedSnapshot -- the Day serialized through ToDayJson at load and after each save -- never an isDirty flag. A flag cannot work here: the food tile grid, the Start Board cell picker, the ticket strip's drag-reorder and the All/None buttons are hand-drawn IMGUI that no [OnValueChanged] reaches, so one writer would always be forgotten. RestoreFrom() assigns the same eight fields ToDayJson writes and does it IN PLACE, because DayEditorWindow's menu tree holds this exact reference. | h:04f17fb2
ExpoTheExplorer/Assets/Editor/DayEditorTicketCardPreview.cs | hand-drawn IMGUI ticket card strip preview (drag-to-reorder, hand-clipped horizontal scroll) | sys: DayEditor | api: DrawStrip(List<DayEditorTicketEntry> entries, TicketCardVisualsConfig visuals, ref Vector2 scrollPos, ref int selectedIndex, ref int draggedIndex, ref Vector2 dragStartMousePos, ref float lastViewWidth) | dep: DayEditorSpriteGUI,DayEditorTicketEntry,FoodItemConfig,Modification,ModificationConfig,PatienceType,TicketCardVisualsConfig | used: DayEditorModel | crit: K3 | note: scrolls via GUI.BeginClip + an explicit HorizontalScrollbar, NOT a nested scroll view -- a nested one swallowed the mouse wheel and turned page scrolling into strip scrolling. Everything inside the clip must stay rect-based GUI.* (no GUILayout). Whether the scrollbar is emitted is decided from lastViewWidth, not from the fresh rect: GetRect reports 0 during layout passes, so reading it raw made layout and repaint disagree on the element count and the strip refused to scroll | h:77ad9ca7
ExpoTheExplorer/Assets/Editor/DayEditorDayStartPreview.cs | single-grid Day Start (TriggerStepIndex == -1) board preview: left-click-drag relocates an occupied cell's item (swaps if the target is occupied too), right-click toggles cell selection for the item/modifications editor | sys: DayEditor | api: DrawGrid(GameConfig gameConfig, BoardVisualsConfig visualsConfig, List<DayEditorBoardSpawnEntry> boardTimeline, ref int selectedX, ref int selectedY, ref int draggedX, ref int draggedY, ref Vector2 dragStartMousePos) | dep: BoardGrid,BoardVisualsConfig,DayBoardTimelinePlayer,DayEditorBoardSpawnEntry,DayEditorSpriteGUI,GameConfig | used: DayEditorModel | crit: K3 | note: - | h:3af3ceea
ExpoTheExplorer/Assets/Editor/DayEditorSpriteGUI.cs | shared atlas-sprite + layered IMGUI draw helpers | sys: DayEditor | api: DrawSpriteFit(Rect rect, Sprite sprite); DrawTexCoords(Rect rect, Sprite sprite); DrawLayeredSprite(Rect rect, Sprite sprite, Vector2 offsetFraction, float scale) | dep: - | used: DayEditorModel,DayEditorTicketCardPreview,FoodItemConfigEditor,MetaLocationLayoutGUI | crit: K3 | note: - | h:825991b2
ExpoTheExplorer/Assets/Editor/MetaEditorWindow.cs | Meta catalog authoring window + per-location page | sys: MetaSystem | api: MetaEditorWindow.DefaultLabelWidth; BuildMenuTree(); DrawMenu(); OnBeginDrawEditors(); MetaLocationPage(MetaCatalog catalog, int locationIndex).LocationIndex | dep: MetaCatalog,MetaCatalogValidator,MetaLocation,MetaLocationLayoutGUI,MetaLayoutState | used: - | crit: K3 | note: D-128 (2026-08-28) draws unlockMessage and unlockImage in the DAY-UNLOCK branch, beside unlockAtDayIndex and not with the shared fields: a purchased prop cannot open on its own, so offering it an unlock popup would be a promise the game never keeps. This window draws props field by field, so a new field is invisible until it is named -- the file already says so about shopIcon, and these two are the second instance. D-039: draws the new shopIcon field directly under sprite. This window lists prop fields BY NAME rather than using a default inspector, so any field added to MetaItemDefinition is invisible here until it is named -- worth remembering next time one is added. clicking a prop on the canvas draws THAT prop's fields directly under the picture, which is the point of the window -- and the panel shows only the unlock field its mode actually reads (price for Purchase, unlockAtDayIndex for DayUnlock), which removes the one real cost of D-017's enum discriminator: nothing to remember, and no tuning a number that is ignored. The full location/prop list is a collapsed foldout below, for structure only (add/remove/reorder). Add/remove LOCATION buttons sit in a strip above the tree via a DrawMenu override, not in the top toolbar the way DayEditorWindow puts "+ New Day" -- here the toolbar already belongs to the catalog and the selected location. Two traps handled there: Unity's InsertArrayElementAtIndex DUPLICATES the previous element, so every field of a new location is set explicitly and its items array cleared or "+ Location" would silently mean "duplicate location"; and a new location inherits the previous one's unlockAtDayIndex rather than 0, which keeps the list in the non-decreasing order the validator expects instead of claiming a second location is available from day one. Remove is confirm-gated and paired with add deliberately: this window shows one location at a time and never the list as an editable array, so an add with no remove would make a mis-click permanent junk. A prop LIST sits to the right of the canvas, same height, and replaced an earlier toolbar dropdown: a dropdown shows one entry at a time, while placing props wants the whole set visible with the selected one obvious. It also guarantees reachability -- a prop authored far outside 0..1 (which the validator warns about rather than forbids) draws off-canvas and would otherwise be reachable only by hunting it down in the raw list. Rows carry the list index so blank or duplicated ids stay distinguishable, and are drawn in LIST order rather than draw order because that is how an author refers to a prop and how validator messages number them. Each row has a PREVIEW visibility toggle (plus Show/Hide all): editor-only, never serialized, because at runtime visibility is decided by ownership and unlock state and a saved flag would be a second authority arguing with the resolver -- it exists to see what sits BEHIND something while placing it. Hidden props are also unclickable, since a target you cannot see changing the selection reads as a bug. The hidden set is keyed by INDEX and cleared whenever the prop count changes (MetaLayoutState.SyncTo) -- cruder than remapping, but it cannot silently hide the wrong prop. Same OdinMenuEditorWindow shell as DayEditorWindow but the OPPOSITE division of labour -- the Day Editor owns a mutable model and writes JSON on Save, this edits the ScriptableObject in place through SerializedObject so Unity's own dirtying/undo/Ctrl+S apply and there is no second copy to keep in step. Odin draws only the chrome and the tree; every field goes through Unity's own property drawer on purpose, so the schema can change without this file guessing. ONE ApplyModifiedProperties per repaint is load-bearing: it makes a drag undo as one gesture instead of one step per mouse-move. sys is MetaSystem, NOT a new MetaEditor system -- an asymmetry with blueprint's DayEditor, accepted because two files do not earn a system; split it out if the window grows. SerializedObject is rebuilt lazily, never cached across domain reloads (a stale one throws on first access and the window outlives a recompile) | h:6982bb23
ExpoTheExplorer/Assets/Editor/MetaLocationLayoutGUI.cs | draggable prop layout canvas over a location background | sys: MetaSystem | api: MetaLayoutState(SelectedIndex; DraggingIndex; Zoom; Pan; IsPanning; Hidden; KnownItemCount; SyncTo(int itemCount); IsHidden(int index); SetHidden(int index, bool hidden); ResetView()); Draw(Rect area, MetaLocation location, SerializedProperty locationProperty, MetaLayoutState state) | dep: MetaLocation,MetaItemDefinition,DayEditorSpriteGUI | used: MetaEditorWindow | crit: K3 | note: D-046: the canvas draws the continuation layer BEFORE the grounds, matching the runtime order. Included rather than skipped because a preview that leaves out a layer is one that gets trusted and then contradicted -- exactly the editor-versus-game divergence D-045 was reported for. Centred on the grounds with no offset, per that field's contract. Note the LOCATION's own fields need no line here: MetaEditorWindow draws them with a generic PropertyField, so a new MetaLocation field appears on its own. It is the ITEM fields (DrawSelectedProp) that are listed by name -- D-039's lesson applies there, not here. the one thing the window exists for -- dragging beats typing twenty pairs of normalized floats. EVERY vertical conversion is flipped: GUI space Y grows downward, a normalized UI position grows upward (the trap DayEditorSpriteGUI's own comment names); getting it wrong mirrors the layout vertically, which is loud rather than silent. Prop size is DERIVED as sprite pixel size x the background's fit scale, mirroring the runtime, because D-015 deliberately left no per-prop scale field -- this stays a preview rather than a second opinion. Mutations go through SerializedProperty, never the plain MetaLocation objects: the locations live inside a ScriptableObject's list, so a direct write would change memory and leave the asset unmarked, surviving only until the next domain reload. Hit-testing walks TOPMOST first so overlapping props pick what the eye sees, and it covers the WHOLE canvas area rather than the background rect -- gating on the background made every prop that overhangs an edge unselectable, which was a real reported bug (2026-08-20): a portrait background sits centred in a wide area, so an overhanging prop's visible part is precisely out there. VerticalSlack exists for the same reason on the other axis: fitted to the full height a portrait background leaves nowhere to DRAW an overhang, and it would paint over the panel below. ZOOM (wheel, 1x-12x) and PAN (middle-drag OR right-drag; the right-button path only works because EventType.ContextClick is swallowed over the canvas first -- the menu wins the event before MouseDrag ever arrives) needed almost no new maths, because every prop rect and hit test derives FROM the background rect -- growing that one rectangle scales sprites, click targets and drag sensitivity together, and a drag delta divided by the ZOOMED width is exactly the precision zoom exists to buy (a 70x104 plant draws ~20px when a 853x1844 background is fitted). It runs inside GUI.BeginClip, which is what zoom REQUIRES: a zoomed background is larger than the canvas and would otherwise paint over the panels beneath. Consequences of that clip: all coordinates are clip-local (BeginClip translates Event.current.mousePosition to match), and the selection outline is drawn with EditorGUI.DrawRect rather than Handles, because Handles goes through GL and would ignore the clip. Zoom pins the point under the cursor by MEASUREMENT (re-derive the rect, shift Pan by how far the point moved) rather than by algebra, so it stays correct if the rect definition changes; Pan is dropped when Zoom returns to 1, or zooming out would leave the background off-centre with no obvious recovery | h:263a9842
ExpoTheExplorer/Assets/Editor/DayEditorWindow.cs | Day Editor EditorWindow: Day list with per-row delete and drag-reorder + config toolbar + fixed-pixel label width + page scroll view | sys: DayEditor | api: - | dep: BoardVisualsConfig,DayEditorModel,DayFileIO,FoodCatalog,GameConfig,TicketCardVisualsConfig,TicketGenerationConfig | used: - | crit: K3 | note: D-132 (2026-08-29) every row in the Day list carries its own X, drawn from OdinMenuItem.OnDrawItem -- Odin fires that after the row draws and BEFORE the row handles its own mouse input, which is what stops one click both deleting and selecting. The click is queued on EditorApplication.delayCall for the D-114 reason below (modal dialog + ForceMenuTreeRebuild inside Odin's own iteration over the menu items). One AddDayRow feeds all three places a row appears (BuildMenuTree, CreateNewDay, OnDuplicateRequested), and the button calls the same OnDeleteRequested as the Day page's own Delete button -- one delete path, not two. Because the click is queued, OnDeleteRequested opens by checking the Day is still in loadedDays -- acting on a Day that already went is worse than doing nothing, since after a renumber its LastSavedDayIndex names a file that now belongs to somebody else. Deleting then calls DayFileIO.Renumber so the list has no gap, and DeleteQuestionFor states in the dialog how many Days will move, because the renumber reaches past the Day being deleted and nothing that names a Day by NUMBER elsewhere (MetaCatalog unlock days, PowerupConfig.tutorialIntroDayIndex) is adjusted with it. D-133 (2026-08-29) a row can also be DRAGGED to a new place in the list: Unity's own DragAndDrop carries the drag (cursor feedback + state machine), so HandleRowDrag only decides where a drop lands and DrawInsertionLine says so. Three things there are load-bearing and non-obvious -- MouseDown is NOT consumed (Odin still needs it to select the row, so the press is only remembered and a drag begins past DragThreshold); the MouseDrag case is deliberately NOT gated on the row's rect, because by then the pointer is often over a different row and the drag belongs to the row it started on; and the insertion line is drawn on Repaint from stored state, since DragUpdated is not a repaint event and drawing there draws nothing. MoveDay converts the drop slot (measured with the dragged Day still counted) into a final position and is refused by ReorderProblem while any Day has no file or an edited-but-unsaved Day Index -- otherwise the row order the drop was measured in and the file order being rewritten are two different orderings. Same turn: the delete no longer falls back to the live DayIndex when LastSavedDayIndex is absent -- a never-saved Day has no file, and the fallback would have deleted another Day's file the moment its Day Index was typed over an existing number. The toolbar's config fields are MinWidth/MaxWidth, never fixed -- a fixed floor there widens the whole content column and pushes every row past the right edge. UseScrollView is overridden to true -- without it the page had no scroll view at all and content ran off the bottom unreachable. DefaultLabelWidth is overridden in PIXELS -- Odin reads 0..1 as a percentage, and the inherited percentage default made the label column grow with the window so the value column never gained room. Unity [MenuItem] window -- opened from the Editor menu, not called by project code D-114 (2026-08-27): leaving a Day with unsaved edits is caught by comparing MenuTree.Selection.SelectedValue against the Day drawn last pass, NOT by OdinMenuTreeSelection's SelectionChanged -- that event fires from inside Odin's selection bookkeeping and 'Keep Editing' has to re-select the Day being left, mutating the list Odin is iterating. The dialog itself runs from EditorApplication.delayCall: a modal dialog opened mid-OnGUI leaves the pass's layout half-built. OnDeleteRequested and OnDestroy prompt too, because ForceMenuTreeRebuild re-reads every Day from disk and used to discard other Days' unsaved edits silently. | h:eeed3812
ExpoTheExplorer/Assets/Editor/DayFileIO.cs | Day JSON file layer: path, save, delete, load-all, renumber, reorder | sys: DayEditor | api: DayFile.FileName/Json; GetFilePath(string daysFolderPath, int dayIndex); Save(string daysFolderPath, DayJson dayJson); Delete(string daysFolderPath, int dayIndex); Renumber(string daysFolderPath); Move(string daysFolderPath, int fromDayIndex, int toPosition); LoadAll(string daysFolderPath) | dep: DayJson | used: DayEditorWindow,DayFileIOTests | crit: K3 | note: takes the folder as a parameter and touches no UnityEditor API, which is what keeps it EditMode-testable against a temp directory; AssetDatabase.Refresh is DayEditorWindow's job, never this class's. D-132 (2026-08-29) Renumber closes the holes a delete leaves: keep the Days in dayIndex order, rewrite as 0..n-1 by position, moving the file NAME and the dayIndex INSIDE it together. File-side rather than through DayEditorModel.ToDayJson on purpose -- a Day with validation errors must renumber with the rest or the numbering is wrong for every Day after it. D-133 (2026-08-29) Move is that same rewrite given a different order: one Day lifted out and re-inserted at a position the CALLER computed, because only the caller knows which gap in the drawn list the user pointed at. Both go through the private WriteInOrder rather than through two implementations of 0..n-1 that could drift apart. WriteInOrder's ordering is what makes an arbitrary reorder safe and not just a compaction: every Day is read into memory, THEN the files whose path must change are deleted, THEN all are written -- so nothing is removed that is not immediately rewritten, and a file that keeps its path is the only file that can want that path, since a wanted path is one per position | h:42b2bf2f
STALE ExpoTheExplorer/Assets/Editor/ExpoTheExplorer.Editor.asmdef | Editor-only assembly for the authoring/dev tools | sys: DayEditor | api: asmdef ExpoTheExplorer.Editor | dep: ExpoTheExplorer.Data,ExpoTheExplorer.Core,ExpoTheExplorer.Systems.DaySystem,ExpoTheExplorer.Systems.TicketSystem,ExpoTheExplorer.Systems.ProgressionSystem | used: - | crit: K2 | note: sys: DayEditor because that is the system the assembly was created for and still mostly holds; MetaEditorWindow, DataTooling and PlayTesting also compile in it, so the sys: field names the owner rather than the only tenant. includePlatforms Editor, so nothing here ships in a build -- which is exactly what lets DayJumpWindow edit the real save file without that capability existing on a player's device. ProgressionSystem was added 2026-08-26 for DayJumpWindow (PlayerProfileStore/PlayerProfile); it is the only reference here that reaches a SAVE boundary rather than authored content, and it is one-directional -- ProgressionSystem knows nothing about the editor. The assembly deliberately CANNOT see Assembly-CSharp, which is why editor tooling that touches GameManager/MainScreenView lives under Scripts/<Area>/Editor/ instead (blueprint.md). | h:7e947ba3
ExpoTheExplorer/Assets/Editor/DayJumpWindow.cs | dev jump: play any authored Day from the editor | sys: PlayTesting | api: - (MenuItem ExpoTheExplorer/Play From Day) | dep: DayCatalogParser,DayJsonSource,DayCatalogNavigator,DayDefinition,FoodCatalog,PlayerProfile,PlayerProfileStore,SceneFlow | used: - | crit: K3 | note: writes ONE profile field (CurrentDayIndex) through PlayerProfileStore.Save, so the save file keeps its single writer and its version stamp -- nothing here knows the JSON shape. The load-bearing detail is that CurrentDayIndex is a POSITION in the resolved catalog, not a Day file's dayIndex: the two agree only while day_00..day_40 stay contiguous AND every Day resolves, because DayCatalogParser DROPS an unresolvable Day and slides every later one down. So the list is built from the SAME parse the runtime runs (DayJsonSource + DayCatalogParser) and the row's position is what is written; listing Resources/Days directly would send the tester to the wrong Day exactly when a Day file is broken. Two entry points on purpose: Play opens the day scene and deliberately skips the key spend and the meta screen, Main Screen takes the real route in. Refuses to jump during play mode, because GameSession reads the profile once at construction and a mid-play write would be invisible and then overwritten. Editor-only (Assets/Editor, includePlatforms Editor). | h:61a094c4
ExpoTheExplorer/Assets/Editor/FoodItemConfigEditor.cs | FoodItemConfig layered board-preview inspector | sys: DayEditor | api: OnInspectorGUI() | dep: BoardItem,DayEditorSpriteGUI,FoodItemConfig,Modification,ModificationConfig,ModificationDirection | used: - | crit: K3 | note: Unity CustomEditor -- invoked by the Inspector, not called by project code | h:49cd01c7
STALE ExpoTheExplorer/Assets/Editor/TicketGenerationConfigEditor.cs | main-dish spawn-chance preview for the seed asset | sys: TicketSystem | api: OnInspectorGUI() | dep: DayEditorSettingsPreviews,TicketGenerationConfig | used: - | crit: K3 | note: delegates the maths to DayEditorSettingsPreviews, which the Day Editor draws too; this asset is a seed since D-006 | h:e33bd200
STALE ExpoTheExplorer/Assets/Editor/DayEditorSettingsPreviews.cs | shared read-only tuning previews: noise-leak distribution, guaranteed-ticket budget, main-dish spawn/modification breakdown | sys: DayEditor | api: DrawLeakPreview(int maxLeakCount, float noiseLeakCountLambda); DrawGuaranteedTicketPreview(GuaranteedTicketCountMode mode, int guaranteedTicketCount, float guaranteedTicketCountLambda); DrawMainDishPreview(IReadOnlyList<(FoodItemConfig Food, float Weight, float ModificationCountLambda, int MaxModificationCount)> entries, IReadOnlyList<FoodItemConfig> allowedFoods = null) | dep: FoodCategory,FoodItemConfig,GameState,GuaranteedTicketCountMode,MainDishWeight,TruncatedPoisson | used: DayEditorModel,TicketGenerationConfigEditor | crit: K3 | note: the two board previews are drawn side by side by DayEditorModel with no fixed widths (fixed widths are what made this window overflow sideways before), so the budget note under DrawGuaranteedTicketPreview is word-wrapped rather than truncated. DrawGuaranteedTicketPreview mirrors BoardDistributor's budget line including its +1 shift (the draw is over 0..SlotCount-1 then shifted, which is what keeps the budget from ever being 0) -- if that line changes, this preview silently goes stale, it has no test holding it. The maths lived in the two custom editors before the Day became where these numbers are authored; kept in one place so the Day view and the seed asset's view cannot drift. DrawModCountBreakdown truncates at Math.Min(available, MaxModificationCount), mirroring TicketFactory.Create -- rows above the cap are omitted rather than listed at 0%, and a note names the cap only when it is the shorter of the two | h:504ecdcd
ExpoTheExplorer/Assets/Scripts/UI/Editor/MainScreenSceneBuilder.cs | one-shot construction script for the MainScreen scene | sys: MainScreen | api: CreateMainScreenScene() | dep: MainScreenView | used: - | crit: K3 | note: Builds TWO buttons since D-026 (ONE since D-025): the title, the DAY caption and the day number are gone, because the day number now lives on the button itself. Changes nothing today (it refuses to overwrite an existing scene) and exists so the builder and the scene cannot drift permanently apart. menu item ExpoTheExplorer > Create MainScreen Scene, run ONCE; it REFUSES to overwrite an existing MainScreen.unity so restyling can never be wiped by a re-run. Exists because a Canvas+TMP scene hand-written as serialized YAML is the edit D-007 already declined to make and cannot be reviewed -- and because a scene file cannot fix Build Settings for itself (it puts MainScreen at index 0, which is what a built player launches). Since D-013 it no longer creates the CoinsCaption/CoinsValue/GemsCaption/GemsValue labels -- the shared HUD Canvas prefab shows the wallet in that scene, so building a second pair would put one balance on screen twice; WireView is playButton + resetButton (D-026; the reset button is smaller, muted red and directly under Play, because it destroys progress and must not read as the main action). Lives under Scripts/UI/Editor/ rather than Assets/Editor/ for a hard reason: it references MainScreenView, which is in the predefined Assembly-CSharp, and an asmdef assembly (Assets/Editor carries one) cannot reference a predefined assembly -- only an Editor folder OUTSIDE any asmdef compiles into Assembly-CSharp-Editor. shards.json still files it here: its first pattern is **/Editor/**. Wires the view's private [SerializeField]s through SerializedObject, so the property-name strings must stay in step with the field names. Resolves the Input System UI module by name rather than referencing it, so a checkout without that package still compiles D-095: no longer builds or wires a Start Over button -- WireView takes the Play button alone. A freshly built MainScreen ships no save-erasing button; that capability lives in the debug panel now, and MainScreenResetButtonSetup (the retrofit menu step) was deleted with it. | h:4ce7c9c2
ExpoTheExplorer/Assets/Scripts/UI/Editor/MainScreenSessionSetup.cs | menu step: give the open MainScreen a session and point the HUD at it | sys: MainScreen | api: Wire() [MenuItem ExpoTheExplorer/Meta/Wire MainScreen Session] | dep: MainScreenRoot,HudWalletSource,GameConfig,LivesConfig,KeyConfig,PowerupConfig,FoodCatalog | used: - | crit: K3 | note: D-065 step 2 added Fill<KeyConfig>, and a WARNING when that field is still empty afterwards. The warning is not decoration: MainScreenRoot refuses to build a session without a KeyConfig, so an unfilled field takes the whole main screen down rather than degrading one widget -- and the tool cannot fill what does not exist yet, since the asset is created by hand from the CreateAssetMenu item. Saying so at the moment it could not assign is far cheaper than finding it at play time. exists because the manual alternative is three drags, one of which (HudWalletSource's host) fails QUIETLY when forgotten. Same posture as HudCanvasPrefabSetup: touches only the already-open scene and does NOT save -- saving would commit changes before they are reviewed and destroy the undo stack, so it stays the author's call. Config fields are only FILLED when empty, since "the first asset of this type" is a guess and an author who deliberately picked a second one keeps it. Under Scripts/UI/Editor/ for the reason blueprint.md records: it references Assembly-CSharp types, which the ExpoTheExplorer.Editor asmdef cannot do at all | h:ab11c7b7
ExpoTheExplorer/Assets/Scripts/UI/Editor/MetaGroundsSetup.cs | menu step: build the meta-grounds container and wire the view | sys: MetaSystem | api: Build() [MenuItem ExpoTheExplorer/Meta/Build Meta Grounds] | dep: MetaGroundsView,MainScreenRoot,MainScreenView,MetaCatalog | used: - | crit: K3 | note: D-128 (2026-08-28) does NOT know the meta unlock popup exists, deliberately: it briefly seeded that prefab and the user objected, because this step rebuilds the grounds container and rewires the whole view -- too many side effects to accept for one asset. The popup has its own step, MetaUnlockPopupSetup. **CORRECTION (D-046 turn): this line used to claim the root is found by COMPONENT since D-028. It is not.** Read from the file: it still calls FindChild(canvas.transform, "--MetaGrounds--") and still carries the FindChild helper, and `FindAnyObjectByType<MetaGroundsView>` has never existed in this file's git history. So D-028's change to THIS file was never applied (its changes to MainScreenView, MainScreenSceneBuilder and MainScreenResetButtonSetup are real), and the map asserted otherwise for several turns. Consequence still live: the project rule against name lookups is violated here, and the name it searches for is the one the author had already renamed -- the exact failure D-028 was written about, so this step can still build a SECOND hierarchy while logging that it left the existing one alone. Needs its own step to actually fix. builds the CONTAINER only (ScrollRect + masked viewport + background Image + location bar); the props are instantiated at run time because the catalog is their single authority (D-015). Refuses to REBUILD an existing hierarchy and only refreshes references, so a layout the author has adjusted by hand is never overwritten -- the same promise MainScreenSceneBuilder makes by refusing to overwrite a scene. Finds the canvas via MainScreenView rather than taking the first Canvas in the scene, because the HUD is its own Canvas prefab and building the grounds inside it would put them on the wrong sorting layer. The root is set as FIRST sibling: sibling order is the only thing deciding draw order in a Canvas, and the grounds are a backdrop that the day readout and Play must stay on top of. RectMask2D not Mask (no extra material, no stencil buffer), and ScrollRect movementType Clamped not Elastic -- a background that already fits should do nothing rather than bounce. Touches only the open scene and does NOT save, so the undo stack survives and saving stays the author's call | h:2cdb80da
STALE ExpoTheExplorer/Assets/Scripts/UI/Editor/MetaShopSetup.cs | menu step: build the meta shop container and wire the view | sys: MetaSystem | api: Build() [MenuItem ExpoTheExplorer/Meta/Build Meta Shop] | dep: MetaShopView,MainScreenView,MetaShopRowView | used: - | crit: K3 | note: D-136 (2026-08-29) UpgradeExisting gained the COMING SOON label -- the same guarded, per-field branch D-108 used for the X, so an already-styled shop gains one object instead of being rebuilt and one Ctrl+Z takes it back out. Built LAST and INACTIVE, and it copies the VIEWPORT's rect rather than carrying hand-written numbers, so it stays centred over the list through a restyle -- copied, not parented, because a child of the viewport would scroll out of the sheet. The words are a const here beside MARKET/NAME/PRICE, not in the catalog: WHICH locations show it is data, but the sentence is the same on every one of them. D-038: builds no backdrop, and "confirmBackdrop" is out of RequiredFields -- leaving it there would have had the D-036 rebuild prompt report a permanently missing field and offer a rebuild on every run. The popup root is kept even with only the box under it: the root is what the view's state machine toggles. D-036: the step now OFFERS to rebuild a shop that is missing references a later step added, instead of only reporting them. Its promise was never "never rebuild" but "never rebuild behind your back", and silently reporting left the author stuck for three steps with a broken scene. Repair-in-place is not on the table: the list and the popup are nested hierarchies, and threading new pieces into a half-built one would mean maintaining two construction paths forever. RequiredFields is deliberately the SAME list MetaShopView refuses to run without -- a second opinion about "healthy" would let this step bless a shop the screen then rejects at Start, which is the confusion that produced D-036. A property that does not EXIST counts as missing too, since that means step and component are out of step. Destroy goes through Undo and the scene is still never saved. S5: the row template carries a CanvasGroup (D-035) so one knob fades the whole row when it cannot be afforded. Only its alpha is ever driven -- interactable is left alone on purpose, since the row's BUY still has to open the preview. S3b: the popup box's pivot is bottom-centre (D-032) so the view can say "your lower edge sits over the ghost" in one assignment rather than doing half-height arithmetic. The pivot is the ONLY reason the box is no longer screen-centred; its children anchor to the box's rect, which did not change, so nothing inside it moves. S3 added the confirm popup, and its SIBLING POSITION is the design: after the panel so it draws over the list, before the market button so that button still draws over IT -- the machine can go from ONIZLEME straight to KAPALI, so a backdrop covering the market button would lock the player in a preview with a ghost on the map and no way out. The box is raycastTarget TRUE or taps meant for it fall through and cancel the popup being read. S2 added the list inside the panel: a ScrollRect whose content sizes itself from the rows (VerticalLayoutGroup + ContentSizeFitter), so scrolling falls out of the row count instead of being a number to maintain -- sixteen offers overflow a bottom sheet and the count changes with every purchase, so the height cannot be authored. It also builds the row TEMPLATE (icon | name | price | BUY) and leaves it INACTIVE: it is a template, not the first row. The container above it stays active, because hiding the container to get the sample row out of sight is what would silently hide every real row. Name label is NoWrap + Ellipsis against a fixed-height LayoutElement -- a wrapped name would be clipped mid-letter instead of ending in an ellipsis. sessionHost and grounds are TYPE lookups filled only-if-empty, the author-time latitude D-028 leaves for editor tools. Same posture as MetaGroundsSetup: touches only the open scene, does NOT save, refuses to REBUILD. Finds the shop by COMPONENT and the canvas via MainScreenView -- never by name (D-028), and never the first Canvas in the scene, because the HUD is a Canvas of its own and building the shop inside it would make the wallet a child of the thing you spend it in. Two orderings are load-bearing and both are sibling order inside ONE Canvas, which is the whole point: the bug that ate the HUD came from two Canvases both at sorting order 0. The root is the LAST sibling so the panel draws over the grounds (which are the FIRST sibling), and the market button is created AFTER the panel so it stays on top of it -- not cosmetic: the panel covers the bottom-right corner, so a button underneath could not be tapped to close it and the shop would be a one-way door. The panel background is raycastTarget TRUE for the same class of reason: the grounds behind it are a ScrollRect, and a panel that does not eat raycasts lets a drag on the panel scroll the map, which reads as the panel being broken. Its existing-shop path deliberately REFRESHES NOTHING -- its references point at objects it created, so re-pointing them would mean searching, which the project rule forbids; instead it names any empty field and leaves the fix to the author's drag or a delete-and-re-run. Panel is a bottom sheet (0.68 of the screen) rather than a full page, because buying a prop while looking at where it will go is the reason a decoration shop lives on this screen. Market button art and the panel frame are PLACEHOLDERS -- the project has no shop art at all (MS5), and a coloured square saying SHOP is honest about being unfinished where a borrowed icon would read as final. D-108 (2026-08-27) added UpgradeExisting: a backdrop (Image + Button, transition None) with the existing sheet REPARENTED inside it, a Title, a NAME / PRICE Header and an X, filling panel/backdropButton/closeButton. This is the first step here that restructures an existing hierarchy rather than only appending -- every branch is guarded and everything goes through Undo. Idempotency is STRUCTURAL, not by name: the sheet is a direct child of the root until wrapped and a grandchild after, so delete the Title or Header later and re-running will NOT restore them; only the X has a field of its own to test. BuildFresh calls the same method, so a fresh shop and an upgraded one cannot drift apart. The headings copy their horizontal placement off the row template's own Name/Price rects (via SerializedObject, those fields being private) because a meta row is hand-anchored rather than a layout group -- copying is the only thing that keeps a heading over its column. The Header lives in the SHEET, never in the scrolling Content, which would scroll the legend away. closeButton/backdropButton are deliberately NOT in RequiredFields: that list drives TryReplaceIncomplete, which offers to DESTROY and rebuild, and an optional addition must never trigger that. | h:d6493993
STALE ExpoTheExplorer/Assets/Scripts/UI/Editor/HudCanvasPrefabSetup.cs | finishes the hand-made HUD prefab + drops it into a scene | sys: MainScreen | api: WirePrefab(); AddToOpenScene() | dep: GameManager,HudWalletSource,SoftMoneyView,GemsView,KeysView,DayNumberView,MainScreenView | used: - | crit: K3 | note: D-065 step 3 put KeysView INTO WireViews' targets, in the slot D-064 emptied -- and the reason is the mirror image: LivesView left because the heart row stopped being prefab content, KeysView belongs because the key readout IS prefab content shown on both screens, so its walletSource is prefab data every instance should inherit. It wires THREE views again. D-064 dropped LivesView from WireViews' targets. Not merely dead: LivesView has no 'walletSource' field any more, so leaving it in would make FindProperty miss and this tool log its own out-of-step error on every run. It wires TWO views now. menu ExpoTheExplorer > HUD > 1/2 (D-013). It does NOT create the prefab -- that already exists, made by hand -- so step 1 only adds HudWalletSource and points the three views' walletSource at it, writing into the PREFAB ASSET via LoadPrefabContents/SaveAsPrefabAsset so every instance inherits it rather than one scene carrying a private override. Finds the prefab BY CONTENT (the prefab containing a SoftMoneyView), not by path: the first version of this file hardcoded Assets/Prefabs/UI/HudCanvas.prefab and a root named 'HUD Canvas' while the real asset is HUDCanvas.prefab with root 'HUDCanvas' -- AssetDatabase paths are case-SENSITIVE and macOS's filesystem is not, so File.Exists matched and LoadAssetAtPath returned null, failing in both directions at once. Step 2 instantiates into the OPEN scene, WRITES that scene's GameManager into the instance's HudWalletSource (a prefab override; absent on the main screen, which is the expected case and not an error) and deletes the four duplicate MainScreen labels by name, reporting rather than guessing when one is missing; it leaves the scene dirty so saving stays the user's call and Ctrl+Z works. Lives under Scripts/UI/Editor/ for the Assembly-CSharp reason in blueprint.md D-130 (2026-08-28) step 1's WireViews gained DayNumberView, on the ticket KeysView came in on: the day badge is prefab content shown on both screens, so its walletSource is prefab data every instance should inherit. The COMPONENT is still added by hand -- this tool wires references, it does not create hierarchy, and the user authored HUDPanel/DayUI themselves. | h:b136c790
ExpoTheExplorer/Assets/Scripts/UI/Editor/MetaBackdropSetup.cs | menu step: converts the day background to the backdrop | sys: MetaSystem | api: - (MenuItem ExpoTheExplorer/Meta/Build Day Backdrop) | dep: MetaBackdropView,MetaCatalog,SessionHost | used: - | crit: K3 | note: D-050. Swaps SampleScene's background Image for a RawImage (a RenderTexture is not a sprite) and wires MetaBackdropView. Same posture as MetaGroundsSetup -- touches only the already-open scene, does NOT save, refuses to redo what it has done (an existing view only gets its references refreshed, so a tuned tint or blur is never overwritten). What it does that the others do not is DESTROY a component, so it goes out of its way to be reversible: the Image's sprite texture is carried onto the RawImage, which is exactly what the view falls back to, and the whole swap collapses into one Undo step. The background object is found by COMPONENT under the background canvas, never by the name Background -- SampleScene has two objects with that name, and D-028 exists because a name search fails silently the moment a hierarchy changes. The canvas itself is matched by name AND checked for a negative sorting order, because the name is an author's label while the sorting order is what actually decides what ends up behind the board. AssetDatabase.FindAssets stays (D-028 removed NAME lookups from runtime; author-time type lookups under the author's eye are the category it deliberately left alone) and more than one MetaCatalog is reported rather than picked between silently -- two would let the day scene and the meta screen show different expos. | h:496ab8da
ExpoTheExplorer/Assets/Editor/DataConfigReserializer.cs | menu step: re-serialize every Assets/Data config | sys: DataTooling | api: - (MenuItem ExpoTheExplorer/Re-serialize Data Configs) | dep: - | used: - (menu-invoked) | crit: K3 | note: D-075. Top level of the ExpoTheExplorer menu on purpose: the CreateAssetMenu entries for these same configs already own ExpoTheExplorer/Data/... in the Project window's Create tree, so a submenu there reads as one of them -- which cost a round of hunting. The script's dep? draft named BoardAnimationConfig/EconomyConfig/MetaCatalog -- those appear in COMMENTS only; this file names no game type at all, which is exactly why it is its own system rather than a DayEditor file. Dirties every ScriptableObject under Assets/Data and calls SaveAssets once, so Unity re-serializes each object WHOLE and a .asset stops carrying a subset of what its class declares (BoardAnimationConfig.asset was at 4 keys of 19). Changes NO value: a present key keeps its authored value, an absent one is written at the initializer the game already runs on; the only subtraction is a key whose field no longer exists (the 50be70c cleanup, by tool this time). The before/after report counts top-level non-m_ keys in the YAML rather than reflecting over the class, deliberately: what Unity serializes is the thing that has been wrong here before, so the file is asked instead of a second theory. A nested asset (MetaCatalog) therefore reports a small number -- that is its shape, not an error. K3: editor-only, runs zero times in a build | h:0aafb489
STALE ExpoTheExplorer/Assets/Scripts/UI/Editor/PowerupShopSetup.cs | menu step: build a powerup shop in either scene and wire it | sys: PowerupSystem | api: Build() [MenuItem ExpoTheExplorer/Meta/Build Powerup Shop] | dep: PowerupShopView,PowerupBarView,MainScreenView,SessionHost | used: - | crit: K3 | note: powerup-plan Adım 3b, added because the author asked for the same treatment MetaShopSetup gives the meta shop. IT BUILDS THE ROWS, which is the one real difference from MetaShopSetup: that step deliberately does not, because only the catalog knows its row count, while here the answer is three and is written in PowerupType. Same posture otherwise -- touches only the open scene, does NOT save, and REFUSES TO REBUILD: an existing shop is reported (missing fields named) and left alone, because a half-wired shop is usually one the author is midway through styling and destroying it is the worse mistake. Nothing is looked up by NAME (D-028; MetaGroundsSetup is the cautionary tale) -- the canvas comes from MainScreenView, the session from a SessionHost component, and an existing shop from its own component. Price and owned labels are built EMPTY on purpose: PowerupShopView fills them from PowerupConfig at run time, so a number written here would be a second authority that lies the moment the config is retuned. Its open button is placed above the meta shop's market button by an INFORMED GUESS -- the two steps share no constants, so overlap is possible if the author moved that button, and Build() says so in its log rather than pretending to know. Four layout helpers (CreateRect/Stretch/CreateText/FillIfEmpty) are duplicated from MetaShopSetup rather than extracted: extracting means editing a second step this task has no reason to touch, and abstraction-level.md puts the burden on the third consumer, not the second D-105 (2026-08-27) taught it the DAY scene as well: opened in SampleScene it anchors on PowerupBarView instead of MainScreenView (that component names the right Canvas AND is what will open the shop), builds NO open button -- an empty powerup is the opener -- adds a full-screen raycast-catching Backdrop as the object PowerupShopView toggles, and fills the bar's Shop field itself. One file rather than a second near-identical step, because the two builds differ in exactly two ways and a copy would drift. openButton left RequiredFields for the same reason and is checked only on the menu path; re-running in an existing day scene still offers the bar wiring, since a scene built before D-105 has the shop without the pointer. D-106 (2026-08-27) added the NAME / COUNT / PRICE heading row, built as a ROW through the same HorizontalLayoutGroup and the same three column widths -- the panel's width is a fraction of the screen and unknown here, so a hand-placed heading would drift on another aspect ratio. It carries an unlabelled BuySpacer of BuyWidth, without which the group hands the BUY column's width to the three headings and every one slides right of what it labels. No background Image, dimmer and smaller type, so the legend cannot read as a fourth unbuyable powerup. This is also the ONE thing the step will add to a shop it did not build: TryAddHeaderRow inserts a first child and touches nothing else, deciding whether a header is already there STRUCTURALLY rather than by name -- the three wired BUY buttons name the three real rows, so a first child that is not one of them is left alone (rename or restyle it freely; delete it and it comes back). D-107 (2026-08-27) made BuildBackdrop return a Button (transition None, so the dim layer does not flash on every press) and fill the view's backdropButton, so a day-scene shop built by this step closes on an outside tap. | h:e721ab0b
ExpoTheExplorer/Assets/Scripts/UI/Editor/MainScreenTutorialSetup.cs | menu step: wires the main screen tutorial and seeds its two popup prefabs | sys: Tutorial | api: - | dep: MainScreenTutorialView,MainScreenWelcomePopup,MainScreenStoreHintPopup,MainScreenRoot,MainScreenView,MetaShopView,TutorialTextConfig | used: - | crit: K3 | note: D-125 (2026-08-28) a prefab whose binder does not resolve is REFUSED with the file to delete named, never silently rebuilt: during a compile error every script unloads, so the same check returns null for perfectly good prefabs and regenerating then would destroy real styling work. D-124 (2026-08-28) the prefabs are built BEFORE the SerializedObject is opened. Creating assets and calling AssetDatabase.SaveAssets with unapplied edits pending on a live SerializedObject risks the whole block being dropped -- which would take sessionHost, shop and texts with it and leave the tutorial silently unwired. D-123 (2026-08-28) also builds Assets/Prefabs/UI/MainScreenTutorialWelcome.prefab and MainScreenTutorialStoreHint.prefab and assigns them onto the view -- there is exactly ONE MainScreenTutorialView in the scene, which is why the references live there and nothing has to be dragged. It REFUSES to rebuild an existing prefab and only fills an EMPTY field, the same refusal it already makes about the scene object: from the author's first edit these are hand-styled assets and a regeneration would be indistinguishable from losing that work. Everything is resolved by TYPE, never by name (D-028, whose cautionary tale is MetaGroundsSetup finding nothing after a rename). The seeded plate hugs its text (VerticalLayoutGroup + ContentSizeFitter) rather than taking the fixed height it replaces. | h:436acb3f
STALE ExpoTheExplorer/Assets/Scripts/UI/Editor/SettingsPopupSetup.cs | builds the day scene's settings menu and wires every field on it | sys: Bootstrap | api: - (a single [MenuItem] entry point, ExpoTheExplorer/Settings Popup/Build In Day Scene) | dep: GameManager,HapticsBinder,HapticButton,SettingsPopupView | used: - | crit: K3 | note: added by D-094. Exists because the menu is fourteen required references across three nested panels, which by hand is thirteen chances to wire one confirmation's Yes button into the other. Keeps MainScreenResetButtonSetup's posture exactly: touches only the already-open scene, NEVER saves, and REFUSES to rebuild an existing menu so hand styling is never overwritten. BUILDS ITS OWN CANVAS rather than hanging off the scene's, which keeps it clear of the shared HUD prefab INSTANCE where an Apply All would push a day-scene menu onto the main screen -- the hazard the blueprint already records for the heart row. Also wires each button's HapticButton to the scene's binder: that is an EDIT-TIME lookup, which is a different thing from the runtime lookups D-013 ruled out -- the reference lands serialized in the scene exactly as a drag would leave it. The captions it writes are placeholders in an EDITOR script and do not touch the runtime invariant: SettingsPopupView itself holds no authored string, which is why both confirmations get their own panel rather than sharing one D-135 (2026-08-29) both placeholder questions now end with the key price, since both exits spend one -- a resource that leaves without warning reads as a bug rather than a price. Also finds the scene's existing NoKeysPopupView and wires the view's new optional noKeysPopup slot (found, never built: one per scene by design, since it needs a scene sessionHost), warning loudly when the scene has none, because the quiet version of that is a free retry nobody notices. SortingOrder stays 200 and its comment was corrected: it is now BELOW Popup Canvas (raised to 300), which matters because the no-keys popup shares that canvas and is opened BY this menu. | h:e293f659
ExpoTheExplorer/Assets/Scripts/UI/Editor/TutorialPopupSetup.cs | menu step: seeds the two tutorial powerup popup prefabs | sys: Tutorial | api: - | dep: TutorialPowerupIntroView,TutorialPowerupSpotlightView | used: - | crit: K3 | note: THE STEP-HINTS PREFAB'S ROOT IS ITS CANVAS, and that is what makes it editable. It was a plain RectTransform with the canvas as a CHILD for one round -- needed then to host a world-space SpriteRenderer arrow -- and the prefab stage showed an empty frame: Unity DRIVES an Overlay canvas's rect from the screen and only gives a ROOT canvas that treatment, so a nested one sits at 0x0 and collapses every child into it. Authoring a size does not help either, because the Canvas overwrites it (tried, and the value came back 0x0 in the saved asset). With both arrows now UI Images nothing needs a non-canvas root. An Image parented outside a Canvas never renders at all, which is what the card arrow was doing. D-126 (2026-08-28) seeds TutorialStepHints.prefab in place of TutorialStepMessage.prefab -- same asset, two arrows richer. Its root is a plain Transform with the message's canvas as a CHILD, because a SpriteRenderer (the world arrow) under a Screen Space - Overlay canvas sits at screen coordinates and looks broken in the prefab stage. D-122 (2026-08-28) seeds a THIRD prefab, TutorialStepMessage, and additionally writes it into BoardAnimationConfig's tutorialMessagePrefab field when that is empty -- so this one needs no manual step at all, unlike the two powerup prefabs which still want a scene drag. The config is found by TYPE (t:BoardAnimationConfig) rather than by a hardcoded path. The plate it builds HUGS ITS TEXT (VerticalLayoutGroup + ContentSizeFitter) instead of taking a fixed height, because the fixed one it replaced was fontSize*3 and the whole point of the task was a bigger font. D-116 (2026-08-28) new, and it exists because a UI hierarchy is the one thing never to hand-author as .prefab YAML: Canvas + CanvasScaler + GraphicRaycaster + Images + TMP + Button, where a mistyped fileID gives a prefab that imports cleanly and is quietly missing a component. scene-structure.md names the Editor-script path the preferred one, and PowerupShop and HUDCanvas already took it. IT REFUSES TO OVERWRITE AN EXISTING PREFAB, which is the whole difference between a seed and an authority: the moment the author styles one of these, a rebuild would be indistinguishable from losing that work. Delete the asset to have it regenerated. It wires each view's serialized fields through SerializedObject, so the property-path strings here must stay in step with the field names on the two views. It ALSO wires the open scene: it finds PowerupBarView by TYPE (never by name -- a name lookup is banned in this project's editor steps as well as at runtime) and fills the two prefab fields, but only when they are EMPTY, so it cannot replace a variant an author deliberately pointed at. An earlier version of this line claimed that step was impossible from a script; that was wrong -- assigning a prefab ASSET to a field on a scene component is ordinary Editor work, and HudCanvasPrefabSetup already did it. Only a reference to another SCENE OBJECT would be out of reach. NOTHING IS PLACED IN THE SCENE: the popups are instantiated at runtime, and a copy sitting in the hierarchy would be on screen from the first frame of every day. | h:1e00d176
ExpoTheExplorer/Assets/Scripts/UI/Editor/MetaUnlockPopupSetup.cs | menu step: seeds the meta unlock popup prefab and wires it | sys: MetaSystem | api: - | dep: MetaUnlockPopup,MetaGroundsView | used: - | crit: K3 | note: D-128 (2026-08-28) ITS OWN STEP, and that is the user's correction: it started as a line inside Build Meta Grounds, which rebuilds the grounds container and rewires the whole view -- needing that to obtain one prefab means accepting a pile of unrelated side effects. Every other step in this folder owns one concern, and a popup is a concern. It does BOTH halves so it is self-sufficient: builds the prefab and points the scene's MetaGroundsView at it, finding that view by TYPE (never by name, D-028) and filling the field only when EMPTY. It refuses to rebuild an existing prefab and reports one whose script does not resolve by naming the file to delete rather than regenerating -- during a compile error every script unloads and that check returns null for perfectly good assets. The seeded root is its Canvas (D-126). Marks the scene dirty but does not save it, like every step here. | h:8f6cc4a9
ExpoTheExplorer/Assets/Scripts/UI/Editor/ConfettiSetup.cs | menu step: seeds the confetti rig and wires whichever celebrating view is open | sys: CelebrationSystem | api: - | dep: ConfettiView,DayCompletePopupView,MetaGroundsView | used: - | crit: K3 | note: D-131 (2026-08-29) new, and REWRITTEN the same day with ConfettiView. ExpoTheExplorer > Celebration > Build Confetti, run once per scene -- it fills MetaGroundsView on MainScreen and DayCompletePopupView on SampleScene, whichever the open scene has, found by TYPE and never by name (D-028). IT BUILDS A RIG, NOT A LOOK: a camera, a RawImage on its own canvas, and TWO NESTED INSTANCES of Assets/Prefabs/Confetti.prefab -- the user s own tuned ParticleSystem, instanced rather than copied, so editing that one asset retunes both cannons. A missing Confetti.prefab is FATAL rather than substituted, because it is the confetti and this step is only the plumbing. THE ROOT IS A PLAIN TRANSFORM, the one prefab here where that is right (compare D-126): it holds a world branch ten thousand units from the origin and a UI branch, and one transform cannot sensibly parent both, so they are siblings and ConfettiView detaches the UI half at runtime. THE AIM NEEDED NO GUESSING: the user s prefab already points up and to the RIGHT in the screen plane -- its authored euler works out to (0.5, 0.866, 0), a 60-degree launch -- so the LEFT cannon holder carries NO rotation at all and the right one is that same aim mirrored, exactly 60 degrees about Z. Neither particle instance s own transform is touched, so opening Confetti.prefab still shows the author the aim they tuned. Seeds the prefab BEFORE opening any SerializedObject on a scene object, which is D-124 s lesson: creating assets with unapplied edits pending on a live one risks the block being dropped, and that looks exactly like a step nobody ran. REFUSES to rebuild an existing prefab and only fills an EMPTY field; a prefab whose ConfettiView does not resolve is reported and NOT regenerated, because during a compile error every script unloads and a good prefab looks identical (D-125). Marks the scene dirty but does not save it, like every step here | h:4786bbce
