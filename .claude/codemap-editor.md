<!-- stamp: b723d46 2026-09-04 status: DEGRADED 5 stale, 1 orphan, 29 missing-role -->
<!-- The twelve one-shot scene/prefab builders under Assets/Scripts/UI/Editor (ConfettiSetup,
     HudCanvasPrefabSetup, MainScreenSceneBuilder, MainScreenSessionSetup, MainScreenTutorialSetup,
     MetaBackdropSetup, MetaGroundsSetup, MetaShopSetup, MetaUnlockPopupSetup, PowerupShopSetup,
     SettingsPopupSetup, TutorialPopupSetup) were DELETED on 2026-08-30 (audit R-02). They are out of
     every dep:/used: field below. A note: that still names one is describing HISTORY -- how a prefab
     or scene came to exist -- not a menu step you can run. The scenes and prefabs on disk are the
     only copy now; the builders live in git history. -->
ExpoTheExplorer/Assets/Editor/DayEditorModel.cs | ticket/board data model + Day Editor inspector GUI (per-Day food selection restricts Generate and the ticket item pickers, and derives the Main Dish Weights list; Day Start authored via the click-to-place grid editor; per-Day board-distribution and ticket-runtime balancing authored here; a Day's tutorial AND tray pre-seed blocks are CARRIED through load/save but have no UI yet; the items a Day introduces are authored at the bottom behind an Introduce New Item toggle) | sys: DayEditor | api: Generate(); Save(); SyncMainDishWeights(FoodCatalog catalog); DayEditorModification.ForConfig(ModificationConfig config); DayEditorModification.CanFlipDirection(ModificationConfig config); DayEditorTicketGeneration.PatientTicketCount/NormalTicketCount/ImpatientTicketCount; DrawTileGrid(int count, ref float lastWidth, Action<Rect,int> drawTile); DrawModificationPicker(FoodItemConfig owner, List<DayEditorModification> mods, string ownerLabel, bool hideWhenEmpty = false); DrawItemSlot(string label, FoodItemConfig current, FoodCategory category); Configure(FoodCatalog catalog, GameConfig gameConfig, TicketGenerationConfig ticketConfig, TicketCardVisualsConfig ticketCardVisuals, BoardVisualsConfig boardVisuals, Action<DayEditorModel> onSave, Action<DayEditorModel> onDuplicate, Action<DayEditorModel> onDelete); ToDayJson(); ToDayDefinition(); MarkSaved(); RefreshUnsavedState(bool force = false); RevertToSaved(); HasUnsavedChanges; HasSavedState; CanRevert; IsSaveable; ValidationSummary | dep: BoardDistributionJson,BoardSpawnEntryJson,BoardVisualsConfig,DayContentGenerator,PatienceType,DayDefinition,DayEditorDayStartPreview,DayEditorSpriteGUI,DayEditorMetaJson,DayEditorTicketCardPreview,DayJson,DayValidationResult,DayValidator,FoodCatalog,FoodCategory,FoodItemConfig,GameConfig,GuaranteedTicketCountMode,MainDishWeight,Modification,ModificationConfig,ModificationDirection,ModificationEntryJson,PatienceType,ResolvedBoardDistribution,ResolvedBoardSpawnEntry,ResolvedTicketEntry,Ticket,TicketCardVisualsConfig,TicketEntryJson,TicketFactory,TicketGenerationConfig,ItemIntroJson,ItemIntroEntryJson,DayCatalogParser,TrayPreSeedJson | used: DayEditorWindow,DayEditorTicketCardPreview,DayEditorModelConverterTests | crit: K3 | note: D-165 (2026-09-03) added `TrayPreSeed` alongside `Tutorial` and for the same reason, in all FOUR places the pass-through needs (field, ToDayJson, FromDayJson, RestoreFrom) -- plus a fifth that the tutorial block did not need: ToDayDefinition resolves it too, because DayValidator refuses a tray-move step whose source tray nothing fills, so omitting it would make the editor report a correctly authored Day as broken on every Save. D-082 (2026-08-26) added the `Tutorial` field and the two lines that carry it through ToDayJson/FromDayJson. It has no inspector and is not meant to be edited here -- it exists because this class REBUILDS DayRuntimeJson field by field on every save, so any runtime field the model does not name is silently deleted the first time a Day is opened and saved. day_00 losing its forced first move because someone retimed a ticket is exactly that bug, and this is the one-line inoculation against it; the same trap is what the CopySettingsFrom comment already warns about for the balancing blocks. ToDayDefinition resolves it via DayCatalogParser.ResolveTutorial rather than re-implementing the enabled-means-present rule, which is what makes DayValidator's tutorial check fire on Save instead of only at runtime. the food grid sizes its columns from the width measured on the last repaint, never EditorGUIUtility.currentViewWidth -- that includes the Day list and made the grid demand a wider row than the content column had, which forced a horizontal scrollbar onto every row in the inspector. NO UnityEngine value attribute belongs anywhere in this file -- numeric settings use Odin's [PropertyRange]/[MinValue] only. This window is an OdinMenuEditorWindow over plain DayEditorModel objects, so there is no SerializedObject behind them and a Unity value attribute has to be bridged through an emulated SerializedProperty; that bridge broke drawing TWICE. [Range] put the slider past the window's right edge with its number field unreachable, and [Min] on DayEditorTicketRuntime made that box's height disagree between the layout and repaint pass (fields arriving a frame late while scrolling) while consuming the event against its own rect (the mouse wheel died over the box instead of scrolling the page). [Tooltip] is fine -- Odin reads it natively, it has no drawer. FromJson tolerates a Day file with no boardDistribution block (falls back to designed defaults) where DayCatalogParser refuses it, so an old Day can still be opened and re-saved. Main Dish Weights MEMBERSHIP is derived, not authored (D-052): DayEditorMetaModel.SyncMainDishWeights rebuilds it from the selected Mains in catalog order, called from DrawFoodSelection -- the single call site, chosen because that section owns the input and PropertyOrder -5 draws it before the list at -3, so file-open/tile-click/All-None/late-catalog/CopySettingsFrom are all covered. Existing rows are carried over BY REFERENCE so authored weight/lambda/max survive; the add/remove buttons and the row's Food picker are gone, or membership would have two writers. It is safe in a draw method only because MainDishWeightsMatch early-outs without allocating -- do not add LINQ to that path. Inspector order is ALL PropertyOrder and every value here is negative, because Save/Duplicate/Delete declare none and therefore sit at the default 0: reading top to bottom it is Food Selection -5, Day -4, Generation Settings -3/-2.9, Ticket Runtime -2.4, Generate -2, Ticket Sequence + Start Board -1/-0.5, Board Distribution -0.2/-0.15 (moved to the bottom on purpose -- it is the pass you make once the Day has content), then the buttons. A new block needs a value in that scale, not an arbitrary one, and a BoxGroup's two members must share a slot or the previews tear away from their knobs. Modifications are picked from a visual tile grid (D-054), ONE helper — DrawModificationPicker — serving both the ticket editor and the Start Board cell editor, which each carried a byte-identical copy of an unfiltered ObjectField before. Its options come from the owning food's AvailableModifications, the same authority TicketFactory.Create reads, and direction is seeded by DayEditorModification.ForConfig from AllowedDirection with only Both flippable (right-click) — so the editor can no longer author a dish/modification or direction pair the generator could never produce. Modifications the owning food does NOT offer are surfaced in a warning list with an x, never dropped silently; the badge falls back to a +/- label when TicketCardVisualsConfig is unassigned. The grid keeps its own lastModGridWidth: sharing the food grid's field would let two differently-sized sections overwrite each other's column count. Patience Mix (D-055) authors three ticket counts in editorMeta and DrawPatienceMixReadout shows what Generate will really lay down -- it calls DayContentGenerator.BuildPatiencePlan rather than repeating the padding/truncation arithmetic, because a preview with its own copy of the rule is a preview that lies. The readout lives on the model, not beside the counts, because it needs TicketsRequiredForDay, which DayEditorTicketGeneration cannot see -- the same split as the food selection. The mismatch warning is deliberately NOT a DayValidator rule: DayValidator sees a DayDefinition and these counts are editorMeta, which the runtime never reads (D-006), so moving them into DayDefinition to be validated would invert that arrow. THREE tile grids now share DrawTileGrid (Food Selection, the modification picker, the Start Board cell picker) and it is the ONE place the width-measured-on-repaint rule lives -- the food grid's scrollbar bug above is why, and per-tile drawing deliberately did NOT move because the three do different work. Each grid keeps its own lastWidth field: the cell picker draws inside a fixed 220px column and settles on ~3 columns while the others get the full content width, so one shared measurement would have them overwrite each other every repaint -- nothing in DrawTileGrid may assume the inspector's width. The Start Board cell's item picker (D-057) replaced a raw unfiltered ObjectField and is fed from AllowedFoodPool, which also closed a real hole: that field could author a board item DayValidator then refused, since the validator checks the board against the same pool. Clicking the selected tile again empties the cell, taking over from the old "None". An item outside the pool is surfaced in a warning rather than cleared. The ticket's own Main/Side/Drink stay name POPUPS (DrawCategoryItemField) -- not yet visual. The cell picker is TWO-STATE (D-058) and the cell itself selects between them, so there is no flag to keep in sync: empty draws the whole grouped grid, filled draws ONLY the item's own tile and hands the room to its modifications. Emptying needs no separate path -- clicking that tile again and the Clear Cell button both leave the cell empty and the next draw is the grid. DrawModificationPicker's hideWhenEmpty (cell editor true, ticket editor left at the false default, so that side is unchanged by construction) suppresses the header and the no-modifications box entirely, but NEVER the orphan list: D-054 made orphans visible because they still ship in the Day JSON, and quieting them to tidy an empty state would undo that decision sideways. The ticket's three food slots are visual too (D-059, DrawItemSlot) and DrawCategoryItemField is GONE with them -- that removed the last raw ObjectField in this file. Its category + AllowedFoodPool filtering was kept exactly; only the presentation changed, and its catalog-unassigned fallback to an unfiltered ObjectField became a HelpBox, matching what Food Selection and the cell picker already did. Slot grids stay OPEN when filled, unlike the cell picker: switching (Hotdog to Burger) is the common action and hiding the alternatives would cost a second click, whereas a filled cell hands its 220px to the modifications below it. All three slots SHARE lastTicketSlotGridWidth because they are drawn one after another in the same column -- the per-grid fields elsewhere exist for sections of different widths, which this is not. Modifications is drawn between Main Item and Side Item on purpose: the picker is keyed on entry.MainItem because TicketFactory takes a ticket's modifications from the main dish alone, so it belongs under the dish rather than under the whole ticket D-114 (2026-08-27): unsaved state is a COMPARISON against savedSnapshot -- the Day serialized through ToDayJson at load and after each save -- never an isDirty flag. A flag cannot work here: the food tile grid, the Start Board cell picker, the ticket strip's drag-reorder and the All/None buttons are hand-drawn IMGUI that no [OnValueChanged] reaches, so one writer would always be forgotten. RestoreFrom() assigns the same eight fields ToDayJson writes and does it IN PLACE, because DayEditorWindow's menu tree holds this exact reference. D-142 (2026-08-31) THE ITEM-INTRODUCTION BLOCK, and it is the first authored block in this window whose picker had a choice to make: it reuses DrawTileGrid + DrawCellItemTile + AllowedFoodPool rather than an ObjectField, because the thing being chosen is a PICTURE -- a list of ObjectFields would make the author match ids to art in their head, which is the exact mistake the popup exists to spare the player. Two states, like the Start Board cell picker and for the same reason: a chosen item shows only itself so the Name and Message fields get the room, and clicking it again puts the larder back, which is why there is no clear button. Its grid keeps its OWN width field (lastItemIntroGridWidth) rather than sharing the cell picker's -- that one is boxed into a 220px column and this one gets the full inspector, so one shared measurement would make each re-flow whenever the other drew. Removal is DEFERRED to after the loop: IMGUI draws the list twice per event and removing mid-draw shifts every later index between the layout and repaint passes. IntroduceNewItem is a BOOL rather than 'the list is non-empty', so unticking keeps what was authored -- the same call TutorialJson.enabled makes, and the JSON carries the same flag so neither side has to translate 'off' into 'empty'. ToDayDefinition resolves the block through DayCatalogParser.ResolveItemIntros rather than a second resolver here, which is what makes DayValidator's intro warnings fire on the same reading the running game uses. The Name field is a PLACEHOLDER, never pre-filled: a copied display name goes stale when the food is renamed and can no longer be told apart from a deliberate override. SECOND PASS (same day): a row is a FOOD or a MODIFICATION, chosen by a kind toolbar rather than inferred from which reference is set -- an author has to be able to say 'this row is a modification' BEFORE there is anything picked to infer it from, and switching kinds clears the other side so a row can never carry both. DrawIntroModificationTile is single-select against the row's own pick, which is the one thing that stops it reusing DrawModificationTile, whose whole contract is toggling membership of a List; it keeps that grid's left-pick / right-flip split and its DrawDirectionBadge, so the two grids read the same way. The pool is DERIVED from the Day's food selection (every modification those foods offer), the same restriction the food picker works under and the same one DayValidator checks. Picking seeds IsAddition from AllowedDirection rather than leaving the field default, so a RemovalOnly modification is never picked up as an addition -- the same seeding DayEditorModification.ForConfig does for a ticket. DayEditorItemIntro.ToJson writes exactly ONE id, which is what keeps the ambiguity the runtime would have to resolve out of a saved Day, while FromJson reads the kind OFF which id the file carries -- the one place that inference is right, since the file has no flag and a non-empty modificationId is what makes an entry a modification for the parser too. | h:6c4ee6fb
ExpoTheExplorer/Assets/Editor/DayEditorTicketCardPreview.cs | hand-drawn IMGUI ticket card strip preview (drag-to-reorder, hand-clipped horizontal scroll) | sys: DayEditor | api: DrawStrip(List<DayEditorTicketEntry> entries, TicketCardVisualsConfig visuals, ref Vector2 scrollPos, ref int selectedIndex, ref int draggedIndex, ref Vector2 dragStartMousePos, ref float lastViewWidth) | dep: DayEditorSpriteGUI,DayEditorTicketEntry,FoodItemConfig,Modification,ModificationConfig,PatienceType,TicketCardVisualsConfig | used: DayEditorModel | crit: K3 | note: scrolls via GUI.BeginClip + an explicit HorizontalScrollbar, NOT a nested scroll view -- a nested one swallowed the mouse wheel and turned page scrolling into strip scrolling. Everything inside the clip must stay rect-based GUI.* (no GUILayout). Whether the scrollbar is emitted is decided from lastViewWidth, not from the fresh rect: GetRect reports 0 during layout passes, so reading it raw made layout and repaint disagree on the element count and the strip refused to scroll | h:77ad9ca7
ExpoTheExplorer/Assets/Editor/DayEditorDayStartPreview.cs | single-grid Day Start (TriggerStepIndex == -1) board preview: left-click-drag relocates an occupied cell's item (swaps if the target is occupied too), right-click toggles cell selection for the item/modifications editor | sys: DayEditor | api: DrawGrid(GameConfig gameConfig, BoardVisualsConfig visualsConfig, List<DayEditorBoardSpawnEntry> boardTimeline, ref int selectedX, ref int selectedY, ref int draggedX, ref int draggedY, ref Vector2 dragStartMousePos) | dep: BoardGrid,BoardVisualsConfig,DayBoardTimelinePlayer,DayEditorBoardSpawnEntry,DayEditorSpriteGUI,GameConfig | used: DayEditorModel | crit: K3 | note: - | h:3af3ceea
ExpoTheExplorer/Assets/Editor/DayEditorSpriteGUI.cs | shared atlas-sprite + layered IMGUI draw helpers | sys: DayEditor | api: DrawSpriteFit(Rect rect, Sprite sprite); DrawTexCoords(Rect rect, Sprite sprite); DrawLayeredSprite(Rect rect, Sprite sprite, Vector2 offsetFraction, float scale) | dep: - | used: DayEditorModel,DayEditorTicketCardPreview,FoodItemConfigEditor,MetaLocationLayoutGUI | crit: K3 | note: - | h:825991b2
STALE ExpoTheExplorer/Assets/Editor/MetaEditorWindow.cs | Meta catalog authoring window + per-location page | sys: MetaSystem | api: MetaEditorWindow.DefaultLabelWidth; BuildMenuTree(); DrawMenu(); OnBeginDrawEditors(); MetaLocationPage(MetaCatalog catalog, int locationIndex).LocationIndex | dep: MetaCatalog,MetaCatalogValidator,MetaEconomyGUI,MetaLocation,MetaLocationLayoutGUI,MetaLayoutState,MetaUnlockKind | used: - | crit: K3 | note: D-166 (2026-09-04) draws the new `scale` field, beside normalizedPosition/pivot/sortOrder rather than with the art fields: where a prop stands and how big it stands there are the same question asked twice. Typed here AND draggable by the canvas grip, for the reason the position is both -- roughing a size out is done by eye, matching two props exactly is done by number. This is the THIRD instance of the rule this line has been restating since D-039, and by now it should be read as structural: the window lists prop fields BY NAME, so any field added to MetaItemDefinition is invisible until someone names it here. -- older: 2026-08-30 the page also draws MetaEconomyGUI, LAST -- under the validation messages, at the very bottom -- with prices measured against what the authored Days are guaranteed to pay. Bottom rather than beside the prop fields (where it first landed, moved on the user's call the same day) because it reads the WHOLE location and every location before it, not the one prop selected: a per-catalog readout in the middle of per-prop editing interrupts what this window is for, and "can the player afford this" only arises after the prices are in front of you. One call, no restructuring. D-128 (2026-08-28) draws unlockMessage and unlockImage in the DAY-UNLOCK branch, beside unlockAtDayIndex and not with the shared fields: a purchased prop cannot open on its own, so offering it an unlock popup would be a promise the game never keeps. This window draws props field by field, so a new field is invisible until it is named -- the file already says so about shopIcon, and these two are the second instance. D-039: draws the new shopIcon field directly under sprite. This window lists prop fields BY NAME rather than using a default inspector, so any field added to MetaItemDefinition is invisible here until it is named -- worth remembering next time one is added. clicking a prop on the canvas draws THAT prop's fields directly under the picture, which is the point of the window -- and the panel shows only the unlock field its mode actually reads (price for Purchase, unlockAtDayIndex for DayUnlock), which removes the one real cost of D-017's enum discriminator: nothing to remember, and no tuning a number that is ignored. The full location/prop list is a collapsed foldout below, for structure only (add/remove/reorder). Add/remove LOCATION buttons sit in a strip above the tree via a DrawMenu override, not in the top toolbar the way DayEditorWindow puts "+ New Day" -- here the toolbar already belongs to the catalog and the selected location. Two traps handled there: Unity's InsertArrayElementAtIndex DUPLICATES the previous element, so every field of a new location is set explicitly and its items array cleared or "+ Location" would silently mean "duplicate location"; and a new location inherits the previous one's unlockAtDayIndex rather than 0, which keeps the list in the non-decreasing order the validator expects instead of claiming a second location is available from day one. Remove is confirm-gated and paired with add deliberately: this window shows one location at a time and never the list as an editable array, so an add with no remove would make a mis-click permanent junk. A prop LIST sits to the right of the canvas, same height, and replaced an earlier toolbar dropdown: a dropdown shows one entry at a time, while placing props wants the whole set visible with the selected one obvious. It also guarantees reachability -- a prop authored far outside 0..1 (which the validator warns about rather than forbids) draws off-canvas and would otherwise be reachable only by hunting it down in the raw list. Rows carry the list index so blank or duplicated ids stay distinguishable, and are drawn in LIST order rather than draw order because that is how an author refers to a prop and how validator messages number them. Each row has a PREVIEW visibility toggle (plus Show/Hide all): editor-only, never serialized, because at runtime visibility is decided by ownership and unlock state and a saved flag would be a second authority arguing with the resolver -- it exists to see what sits BEHIND something while placing it. Hidden props are also unclickable, since a target you cannot see changing the selection reads as a bug. The hidden set is keyed by INDEX and cleared whenever the prop count changes (MetaLayoutState.SyncTo) -- cruder than remapping, but it cannot silently hide the wrong prop. Same OdinMenuEditorWindow shell as DayEditorWindow but the OPPOSITE division of labour -- the Day Editor owns a mutable model and writes JSON on Save, this edits the ScriptableObject in place through SerializedObject so Unity's own dirtying/undo/Ctrl+S apply and there is no second copy to keep in step. Odin draws only the chrome and the tree; every field goes through Unity's own property drawer on purpose, so the schema can change without this file guessing. ONE ApplyModifiedProperties per repaint is load-bearing: it makes a drag undo as one gesture instead of one step per mouse-move. sys is MetaSystem, NOT a new MetaEditor system -- an asymmetry with blueprint's DayEditor, accepted because two files do not earn a system; split it out if the window grows. SerializedObject is rebuilt lazily, never cached across domain reloads (a stale one throws on first access and the window outlives a recompile) | h:f0165703
ExpoTheExplorer/Assets/Editor/MetaEconomyProjection.cs | authored Days costed as a guaranteed coin budget | sys: MetaSystem | api: DayIncomeFloor(DayIndex; TicketPayouts; OrderValue; UnresolvedItems; UnpricedItems; TicketCount; CleanFloor; FloorWith(int lostTickets)); MetaLocationCost(PurchaseCount; PurchaseTotal; DayUnlockCount; FreeCount; Of(MetaLocation location); Of(MetaCatalog catalog, int throughIndex = -1)); MetaEconomyProjection.Days/StartingSoftMoney/Problem/DayCount/IsEmpty/UnresolvedItems/UnpricedItems/LastDayIndex/Cached/MaxAbsorbableLosses; MinimumBeforeDay(int dayIndex, int lostTicketsPerDay); MinimumAfterDay(int dayIndex, int lostTicketsPerDay); FloorBetween(int firstDayIndex, int lastDayIndex, int lostTicketsPerDay); TotalFloor(int lostTicketsPerDay); Invalidate(); Build(); Build(string daysFolderPath, FoodCatalog foodCatalog, EconomyConfig economyConfig, GameConfig gameConfig) | dep: DayFileIO,DayJson,DeliveryPayoutResult,EconomyConfig,FoodCatalog,FoodItemConfig,GameConfig,GameState,MetaCatalog,MetaItemDefinition,MetaLocation,TipTier | used: MetaEconomyGUI | crit: K3 | note: added 2026-08-30. Replays the economics that already exist over every Day file at once; it invents none of its own, which is the whole reason it can be trusted as a price reference. It builds the REAL DeliveryPayoutResult(orderValue, Critical, tipRateCritical) rather than typing value*(1+rate), and rounds per delivery with Mathf.RoundToInt exactly where DayLifecycleManager.RecordDelivery rounds -- a second copy of either rule is the drift this tool exists to detect. THREE runtime facts it rests on, all read rather than assumed: (1) a day does NOT require every ticket delivered -- TicketSlotManager ends it when the sequence is exhausted AND every slot is empty, and a timeout reaches that same end because Tick cancels the expired ticket and pulls the next entry into the freed slot; (2) only a TIMEOUT costs money -- GameManager.HandleWrongDelivery spends a life and leaves the ticket standing, so a wrong delivery costs time, not income; (3) Order Value is never scaled, so the Critical tip is the floor of a delivered ticket. If any of those three changes, this file is wrong and nothing will say so -- it has tests, but they test the arithmetic, not the runtime's continued agreement with these three sentences. TicketPayouts is sorted DESCENDING at construction so FloorWith(n) is a slice: the worst case is that the priciest orders expired, and erring that way is deliberate for an authoring floor -- a price set against it is affordable for everyone rather than for the lucky. MaxAbsorbableLosses reads GameState.DefaultStartingLives - 1 rather than a literal 2: losing the LAST life ends the day in the Continue popup, and a paid Continue is a gem cost this does not model. Spending is not modelled at all (powerups, refills, Continues share the wallet), so every figure is a ceiling on the guarantee, not a prediction -- MetaEconomyGUI says so on screen. NOT a duplicate of DayEditorSimulationPanel/SimulationRunner: that one plays ONE day stochastically with a bot policy and reports what a typical run yields; this is a deterministic floor across the whole catalog. Different questions, and 22 simulations per repaint would not be affordable anyway. Missing configs leave an EMPTY projection with Problem set rather than a zero floor -- a silent 0 reads as 'the days pay nothing'. Cached because the Meta window redraws continuously; MetaEconomyAssetWatcher (an AssetPostprocessor in this file) drops the cache when a .asset or anything under Resources/Days lands. That is an import hook and not an OnFocus override on purpose: OdinMenuEditorWindow owns the EditorWindow message methods and Unity delivers each message to one member only, so a subclass OnFocus would silently hide the base one -- and a Day saved while the Meta window already has focus would never raise it anyway | h:30f5250d
ExpoTheExplorer/Assets/Editor/MetaEconomyGUI.cs | Meta Editor economy panel: prop prices vs the guaranteed day income | sys: MetaSystem | api: Draw(MetaCatalog catalog, int locationIndex) | dep: MetaCatalog,MetaEconomyProjection,MetaItemDefinition,MetaLocation,MetaLocationCost,MetaUnlockKind | used: MetaEditorWindow | crit: K3 | note: added 2026-08-30. Split from MetaEconomyProjection along the TESTABLE line: the arithmetic is a pure function of assets on disk and has unit tests, this is IMGUI and can have none. Nothing here computes money -- every figure comes from the projection or MetaLocationCost. The collapsed headline carries the three numbers an author checks without expanding: this location's price, the coins guaranteed by the time it stops being the newest one, and the gap. Expanded it adds the budget box, the cost box (cumulative, because a player reaching the third location paid for the first two as well -- costing a location alone would flatter every price in the catalog), a verdict, a Day-Unlock prop table and a per-day floor table. 'Tickets lost per day' is the one assumption the author sets; it is capped at MetaEconomyProjection.MaxAbsorbableLosses and lives in EditorPrefs with the foldout flags, because it is a balancing stance taken once that should survive the next recompile. The verdict deliberately does NOT call a deficit a bug: it means the player must beat the floor or leave something unbought, which is only wrong if the props are meant to be gates rather than choices. The window end day walks FORWARD for the first location opening strictly later rather than trusting list position, and is clamped at the location's own unlock day -- a catalog authored out of order then yields a one-day window instead of a negative one | h:343305ec
STALE ExpoTheExplorer/Assets/Editor/MetaLocationLayoutGUI.cs | draggable prop layout canvas over a location background | sys: MetaSystem | api: MetaLayoutState(SelectedIndex; DraggingIndex; ScalingIndex; ScaleAtDragStart; DistanceAtDragStart; Zoom; Pan; IsPanning; Hidden; KnownItemCount; SyncTo(int itemCount); IsHidden(int index); SetHidden(int index, bool hidden); ResetView(); EndDrag()); Draw(Rect area, MetaLocation location, SerializedProperty locationProperty, MetaLayoutState state) | dep: MetaLocation,MetaItemDefinition,MetaLayout,DayEditorSpriteGUI | used: MetaEditorWindow | crit: K3 | note: D-166 (2026-09-04) added the SCALE GRIP: a 10px square at the selected prop's top-right that writes MetaItemDefinition.scale, the same argument the position drag rests on -- a size is judged against the art around it, so typing 0.8 and recompiling to look is the wrong loop. Top-right and not bottom, because the default pivot is bottom-centre: the contact point is what stays put while a prop grows, and a grip on the edge that cannot move would read as doing nothing. Resize is by RATIO FROM THE ANCHOR (how much further the cursor is than when the press landed), with the start scale and start distance captured on MouseDown rather than accumulated from Event.delta, and a 1px floor on that distance so a (1,1) pivot -- where the anchor IS the grip -- degrades to a very sensitive drag instead of an instant clamp. ScalingIndex is deliberately a SECOND field beside DraggingIndex, not a mode flag: the two mean opposite things about one gesture, the scale case is ordered first in HandleInput, and BeginScaleAt returns early from SelectAt so a press on the grip never also starts a move. The grip wins over props drawn in FRONT of the selected one -- a 10px target on something the author already picked out, where the alternative is a resize silently turning into a move of the wrong thing. Both drags end through MetaLayoutState.EndDrag so an out-of-order MouseUp cannot leave one armed. The canvas STOPPED computing its own prop size in the same change: RectFor now asks MetaLayout.PropSize (hence the new asmdef reference), which is what made the field safe to add -- a second factor in a duplicated formula is a second place to forget it, and the editor quietly disagreeing with the game about a size is exactly the preview-that-lies failure D-045 was reported for. The `scale` variable inside Draw is still the BACKGROUND's fit scale alone; the per-prop nudge is applied one level down, so there is one place that knows about both. -- older: D-046: the canvas draws the continuation layer BEFORE the grounds, matching the runtime order. Included rather than skipped because a preview that leaves out a layer is one that gets trusted and then contradicted -- exactly the editor-versus-game divergence D-045 was reported for. Centred on the grounds with no offset, per that field's contract. Note the LOCATION's own fields need no line here: MetaEditorWindow draws them with a generic PropertyField, so a new MetaLocation field appears on its own. It is the ITEM fields (DrawSelectedProp) that are listed by name -- D-039's lesson applies there, not here. the one thing the window exists for -- dragging beats typing twenty pairs of normalized floats. EVERY vertical conversion is flipped: GUI space Y grows downward, a normalized UI position grows upward (the trap DayEditorSpriteGUI's own comment names); getting it wrong mirrors the layout vertically, which is loud rather than silent. Prop size is DERIVED as sprite pixel size x the background's fit scale x the prop's authored scale, mirroring the runtime because it now literally IS the runtime's function -- this stays a preview rather than a second opinion. Mutations go through SerializedProperty, never the plain MetaLocation objects: the locations live inside a ScriptableObject's list, so a direct write would change memory and leave the asset unmarked, surviving only until the next domain reload. Hit-testing walks TOPMOST first so overlapping props pick what the eye sees, and it covers the WHOLE canvas area rather than the background rect -- gating on the background made every prop that overhangs an edge unselectable, which was a real reported bug (2026-08-20): a portrait background sits centred in a wide area, so an overhanging prop's visible part is precisely out there. VerticalSlack exists for the same reason on the other axis: fitted to the full height a portrait background leaves nowhere to DRAW an overhang, and it would paint over the panel below. ZOOM (wheel, 1x-12x) and PAN (middle-drag OR right-drag; the right-button path only works because EventType.ContextClick is swallowed over the canvas first -- the menu wins the event before MouseDrag ever arrives) needed almost no new maths, because every prop rect and hit test derives FROM the background rect -- growing that one rectangle scales sprites, click targets and drag sensitivity together, and a drag delta divided by the ZOOMED width is exactly the precision zoom exists to buy (a 70x104 plant draws ~20px when a 853x1844 background is fitted). It runs inside GUI.BeginClip, which is what zoom REQUIRES: a zoomed background is larger than the canvas and would otherwise paint over the panels beneath. Consequences of that clip: all coordinates are clip-local (BeginClip translates Event.current.mousePosition to match), and the selection outline is drawn with EditorGUI.DrawRect rather than Handles, because Handles goes through GL and would ignore the clip. Zoom pins the point under the cursor by MEASUREMENT (re-derive the rect, shift Pan by how far the point moved) rather than by algebra, so it stays correct if the rect definition changes; Pan is dropped when Zoom returns to 1, or zooming out would leave the background off-centre with no obvious recovery | h:fb8b47be
ExpoTheExplorer/Assets/Editor/DayEditorWindow.cs | Day Editor EditorWindow: Day list with per-row delete and drag-reorder + config toolbar + fixed-pixel label width + page scroll view | sys: DayEditor | api: - | dep: BoardVisualsConfig,DayEditorModel,DayFileIO,FoodCatalog,GameConfig,TicketCardVisualsConfig,TicketGenerationConfig | used: - | crit: K3 | note: D-132 (2026-08-29) every row in the Day list carries its own X, drawn from OdinMenuItem.OnDrawItem -- Odin fires that after the row draws and BEFORE the row handles its own mouse input, which is what stops one click both deleting and selecting. The click is queued on EditorApplication.delayCall for the D-114 reason below (modal dialog + ForceMenuTreeRebuild inside Odin's own iteration over the menu items). One AddDayRow feeds all three places a row appears (BuildMenuTree, CreateNewDay, OnDuplicateRequested), and the button calls the same OnDeleteRequested as the Day page's own Delete button -- one delete path, not two. Because the click is queued, OnDeleteRequested opens by checking the Day is still in loadedDays -- acting on a Day that already went is worse than doing nothing, since after a renumber its LastSavedDayIndex names a file that now belongs to somebody else. Deleting then calls DayFileIO.Renumber so the list has no gap, and DeleteQuestionFor states in the dialog how many Days will move, because the renumber reaches past the Day being deleted and nothing that names a Day by NUMBER elsewhere (MetaCatalog unlock days, PowerupConfig.tutorialIntroDayIndex) is adjusted with it. D-133 (2026-08-29) a row can also be DRAGGED to a new place in the list: Unity's own DragAndDrop carries the drag (cursor feedback + state machine), so HandleRowDrag only decides where a drop lands and DrawInsertionLine says so. Three things there are load-bearing and non-obvious -- MouseDown is NOT consumed (Odin still needs it to select the row, so the press is only remembered and a drag begins past DragThreshold); the MouseDrag case is deliberately NOT gated on the row's rect, because by then the pointer is often over a different row and the drag belongs to the row it started on; and the insertion line is drawn on Repaint from stored state, since DragUpdated is not a repaint event and drawing there draws nothing. MoveDay converts the drop slot (measured with the dragged Day still counted) into a final position and is refused by ReorderProblem while any Day has no file or an edited-but-unsaved Day Index -- otherwise the row order the drop was measured in and the file order being rewritten are two different orderings. Same turn: the delete no longer falls back to the live DayIndex when LastSavedDayIndex is absent -- a never-saved Day has no file, and the fallback would have deleted another Day's file the moment its Day Index was typed over an existing number. The toolbar's config fields are MinWidth/MaxWidth, never fixed -- a fixed floor there widens the whole content column and pushes every row past the right edge. UseScrollView is overridden to true -- without it the page had no scroll view at all and content ran off the bottom unreachable. DefaultLabelWidth is overridden in PIXELS -- Odin reads 0..1 as a percentage, and the inherited percentage default made the label column grow with the window so the value column never gained room. Unity [MenuItem] window -- opened from the Editor menu, not called by project code D-114 (2026-08-27): leaving a Day with unsaved edits is caught by comparing MenuTree.Selection.SelectedValue against the Day drawn last pass, NOT by OdinMenuTreeSelection's SelectionChanged -- that event fires from inside Odin's selection bookkeeping and 'Keep Editing' has to re-select the Day being left, mutating the list Odin is iterating. The dialog itself runs from EditorApplication.delayCall: a modal dialog opened mid-OnGUI leaves the pass's layout half-built. OnDeleteRequested and OnDestroy prompt too, because ForceMenuTreeRebuild re-reads every Day from disk and used to discard other Days' unsaved edits silently. | h:eeed3812
ExpoTheExplorer/Assets/Editor/DayFileIO.cs | Day JSON file layer: path, save, delete, load-all, renumber, reorder | sys: DayEditor | api: DayFile.FileName/Json; GetFilePath(string daysFolderPath, int dayIndex); Save(string daysFolderPath, DayJson dayJson); Delete(string daysFolderPath, int dayIndex); Renumber(string daysFolderPath); Move(string daysFolderPath, int fromDayIndex, int toPosition); LoadAll(string daysFolderPath) | dep: DayJson | used: DayEditorWindow,DayFileIOTests | crit: K3 | note: takes the folder as a parameter and touches no UnityEditor API, which is what keeps it EditMode-testable against a temp directory; AssetDatabase.Refresh is DayEditorWindow's job, never this class's. D-132 (2026-08-29) Renumber closes the holes a delete leaves: keep the Days in dayIndex order, rewrite as 0..n-1 by position, moving the file NAME and the dayIndex INSIDE it together. File-side rather than through DayEditorModel.ToDayJson on purpose -- a Day with validation errors must renumber with the rest or the numbering is wrong for every Day after it. D-133 (2026-08-29) Move is that same rewrite given a different order: one Day lifted out and re-inserted at a position the CALLER computed, because only the caller knows which gap in the drawn list the user pointed at. Both go through the private WriteInOrder rather than through two implementations of 0..n-1 that could drift apart. WriteInOrder's ordering is what makes an arbitrary reorder safe and not just a compaction: every Day is read into memory, THEN the files whose path must change are deleted, THEN all are written -- so nothing is removed that is not immediately rewritten, and a file that keeps its path is the only file that can want that path, since a wanted path is one per position | h:42b2bf2f
STALE ExpoTheExplorer/Assets/Editor/ExpoTheExplorer.Editor.asmdef | Editor-only assembly for the authoring/dev tools | sys: DayEditor | api: asmdef ExpoTheExplorer.Editor | dep: ExpoTheExplorer.Data,ExpoTheExplorer.Core,ExpoTheExplorer.Systems.DaySystem,ExpoTheExplorer.Systems.TicketSystem,ExpoTheExplorer.Systems.ProgressionSystem,ExpoTheExplorer.Systems.EconomySystem,ExpoTheExplorer.Systems.MetaSystem | used: - | crit: K2 | note: sys: DayEditor because that is the system the assembly was created for and still mostly holds; MetaEditorWindow, DataTooling and PlayTesting also compile in it, so the sys: field names the owner rather than the only tenant. includePlatforms Editor, so nothing here ships in a build -- which is exactly what lets DayJumpWindow edit the real save file without that capability existing on a player's device. ProgressionSystem was added 2026-08-26 for DayJumpWindow (PlayerProfileStore/PlayerProfile); it is the only reference here that reaches a SAVE boundary rather than authored content, and it is one-directional -- ProgressionSystem knows nothing about the editor. EconomySystem was added 2026-08-30 for MetaEconomyProjection, and the reference exists precisely so that tool does NOT re-type the payout formula: it constructs the real DeliveryPayoutResult instead. Also one-directional -- EconomySystem is a leaf that references only Core and Data. MetaSystem was added 2026-09-04 (D-165) for the same kind of reason and it is the strongest case of the three: MetaLocationLayoutGUI's canvas was keeping its own copy of the prop-sizing formula, and the reference let it call MetaLayout.PropSize instead -- the edit REMOVED a duplicate rather than adding a dependency's worth of new surface. One-directional as well; MetaSystem's own asmdef still references nothing at all. Note this is an EDITOR-to-runtime arrow only: it changes nothing about what a build contains, since includePlatforms is Editor. The assembly deliberately CANNOT see Assembly-CSharp, which is why editor tooling that touches GameManager/MainScreenView lives under Scripts/<Area>/Editor/ instead (blueprint.md). | h:9aae277f
ExpoTheExplorer/Assets/Editor/DayJumpWindow.cs | dev jump: play any authored Day from the editor | sys: PlayTesting | api: - (MenuItem ExpoTheExplorer/Play From Day) | dep: DayCatalogParser,DayJsonSource,DayCatalogNavigator,DayDefinition,FoodCatalog,PlayerProfile,PlayerProfileStore,SceneFlow | used: - | crit: K3 | note: writes ONE profile field (CurrentDayIndex) through PlayerProfileStore.Save, so the save file keeps its single writer and its version stamp -- nothing here knows the JSON shape. The load-bearing detail is that CurrentDayIndex is a POSITION in the resolved catalog, not a Day file's dayIndex: the two agree only while day_00..day_40 stay contiguous AND every Day resolves, because DayCatalogParser DROPS an unresolvable Day and slides every later one down. So the list is built from the SAME parse the runtime runs (DayJsonSource + DayCatalogParser) and the row's position is what is written; listing Resources/Days directly would send the tester to the wrong Day exactly when a Day file is broken. Two entry points on purpose: Play opens the day scene and deliberately skips the key spend and the meta screen, Main Screen takes the real route in. Refuses to jump during play mode, because GameSession reads the profile once at construction and a mid-play write would be invisible and then overwritten. Editor-only (Assets/Editor, includePlatforms Editor). | h:61a094c4
ExpoTheExplorer/Assets/Editor/FoodItemConfigEditor.cs | FoodItemConfig layered board-preview inspector | sys: DayEditor | api: OnInspectorGUI() | dep: BoardItem,DayEditorSpriteGUI,FoodItemConfig,Modification,ModificationConfig,ModificationDirection | used: - | crit: K3 | note: Unity CustomEditor -- invoked by the Inspector, not called by project code | h:49cd01c7
STALE ExpoTheExplorer/Assets/Editor/TicketGenerationConfigEditor.cs | main-dish spawn-chance preview for the seed asset | sys: TicketSystem | api: OnInspectorGUI() | dep: DayEditorSettingsPreviews,TicketGenerationConfig | used: - | crit: K3 | note: delegates the maths to DayEditorSettingsPreviews, which the Day Editor draws too; this asset is a seed since D-006 | h:e33bd200
STALE ExpoTheExplorer/Assets/Editor/DayEditorSettingsPreviews.cs | shared read-only tuning previews: noise-leak distribution, guaranteed-ticket budget, main-dish spawn/modification breakdown | sys: DayEditor | api: DrawLeakPreview(int maxLeakCount, float noiseLeakCountLambda); DrawGuaranteedTicketPreview(GuaranteedTicketCountMode mode, int guaranteedTicketCount, float guaranteedTicketCountLambda); DrawMainDishPreview(IReadOnlyList<(FoodItemConfig Food, float Weight, float ModificationCountLambda, int MaxModificationCount)> entries, IReadOnlyList<FoodItemConfig> allowedFoods = null) | dep: FoodCategory,FoodItemConfig,GameState,GuaranteedTicketCountMode,MainDishWeight,TruncatedPoisson | used: DayEditorModel,TicketGenerationConfigEditor | crit: K3 | note: the two board previews are drawn side by side by DayEditorModel with no fixed widths (fixed widths are what made this window overflow sideways before), so the budget note under DrawGuaranteedTicketPreview is word-wrapped rather than truncated. DrawGuaranteedTicketPreview mirrors BoardDistributor's budget line including its +1 shift (the draw is over 0..SlotCount-1 then shifted, which is what keeps the budget from ever being 0) -- if that line changes, this preview silently goes stale, it has no test holding it. The maths lived in the two custom editors before the Day became where these numbers are authored; kept in one place so the Day view and the seed asset's view cannot drift. DrawModCountBreakdown truncates at Math.Min(available, MaxModificationCount), mirroring TicketFactory.Create -- rows above the cap are omitted rather than listed at 0%, and a note names the cap only when it is the shorter of the two | h:504ecdcd
ExpoTheExplorer/Assets/Editor/DataConfigReserializer.cs | menu step: re-serialize every Assets/Data config | sys: DataTooling | api: - (MenuItem ExpoTheExplorer/Re-serialize Data Configs) | dep: - | used: - (menu-invoked) | crit: K3 | note: D-075. Top level of the ExpoTheExplorer menu on purpose: the CreateAssetMenu entries for these same configs already own ExpoTheExplorer/Data/... in the Project window's Create tree, so a submenu there reads as one of them -- which cost a round of hunting. The script's dep? draft named BoardAnimationConfig/EconomyConfig/MetaCatalog -- those appear in COMMENTS only; this file names no game type at all, which is exactly why it is its own system rather than a DayEditor file. Dirties every ScriptableObject under Assets/Data and calls SaveAssets once, so Unity re-serializes each object WHOLE and a .asset stops carrying a subset of what its class declares (BoardAnimationConfig.asset was at 4 keys of 19). Changes NO value: a present key keeps its authored value, an absent one is written at the initializer the game already runs on; the only subtraction is a key whose field no longer exists (the 50be70c cleanup, by tool this time). The before/after report counts top-level non-m_ keys in the YAML rather than reflecting over the class, deliberately: what Unity serializes is the thing that has been wrong here before, so the file is asked instead of a second theory. A nested asset (MetaCatalog) therefore reports a small number -- that is its shape, not an error. K3: editor-only, runs zero times in a build | h:0aafb489
ORPHAN ExpoTheExplorer/Assets/Editor/DayEditorSimulationPanel.cs | MISSING-role | sys: ? | api: Draw(DayEditorModel model, GameConfig gameConfig, TicketGenerationConfig ticketConfig, DayValidationResult validation, ref UiState state) | dep?: DayEditorModel,DayValidationResult,EconomyConfig,GameConfig,SimulationOptions,SimulationResult | used: ? | crit: ? | note: auto-added; AI must complete | h:b0cd07ec
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/Editor/ApiSignup.cs | MISSING-role | sys: ? | api: ApiSignupResultCallback(bool success, string apiKey, string email, string error); SignUp(string email, string invoiceNo, ApiSignupResultCallback resultCallback); Verify(string apiKey) | dep?: Json,SRDebugApi,SRDebugApiUtil | used: ? | crit: ? | note: auto-added; AI must complete | h:b10ecc18
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/Editor/ApiSignupWindow.cs | MISSING-role | sys: ? | api: Open() | dep?: SRInternalEditorUtil,Styles | used: ? | crit: ? | note: auto-added; AI must complete | h:0bf31e1a
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/Editor/IntegrityChecking.cs | MISSING-role | sys: ? | api: GetFixes(); Execute(); Execute() | dep?: - | used: ? | crit: ? | note: auto-added; AI must complete | h:acbe0084
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/Editor/Migrations.cs | MISSING-role | sys: ? | api: RunMigrations(bool forceRun = false); RunMigration(Migration migration) | dep?: SRInternalEditorUtil | used: ? | crit: ? | note: auto-added; AI must complete | h:faeda4ff
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/Editor/ProfilerCameraListenerEditor.cs | MISSING-role | sys: ? | api: - | dep?: - | used: ? | crit: ? | note: auto-added; AI must complete | h:97cb0db2
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/Editor/SRDebugEditor.Compiler.cs | MISSING-role | sys: ? | api: - | dep?: SRInternalEditorUtil | used: ? | crit: ? | note: auto-added; AI must complete | h:6f65a8c4
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/Editor/SRDebugEditor.Resources.cs | MISSING-role | sys: ? | api: SetDirectoryEnabled(bool enable) | dep?: SRInternalEditorUtil | used: ? | crit: ? | note: auto-added; AI must complete | h:af8b613f
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/Editor/SRDebugEditor.cs | MISSING-role | sys: ? | api: SetEnabled(bool enable) | dep?: DelegateFix,EditorSettings,Fix,IntegrityIssue,ResourceDirectory,SRInternalEditorUtil | used: ? | crit: ? | note: auto-added; AI must complete | h:7be96c0b
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/Editor/SRDebugEditorPaths.cs | MISSING-role | sys: ? | api: - | dep?: SROptions,Settings | used: ? | crit: ? | note: auto-added; AI must complete | h:81dc2d8a
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/Editor/SRDebugEditorStrings.cs | MISSING-role | sys: ? | api: - | dep?: SRDebug | used: ? | crit: ? | note: auto-added; AI must complete | h:a480d4b2
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/Editor/SRIntegrityCheckWindow.cs | MISSING-role | sys: ? | api: Open(); RefreshIntegrityCheck() | dep?: Fix,IntegrityIssue,SRDebugEditor,SRInternalEditorUtil,Styles | used: ? | crit: ? | note: auto-added; AI must complete | h:fe53076c
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/Editor/SRInternalEditorUtil.cs | MISSING-role | sys: ? | api: GetRootPath(); GetAssetPath(string relativeToRoot); TryGetOrCreate(out Settings instance, out string message); ClearCache(); GetLogo(); GetWelcomeLogo() | dep?: ConsoleAlignment,Entry,OptionsAlignment,PinAlignment,ProfilerAlignment,SRDebug | used: ? | crit: ? | note: auto-added; AI must complete | h:5e6609b3
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/Editor/SROptionsWindow.cs | MISSING-role | sys: ? | api: Open(); OnInspectorUpdate() | dep?: IOptionsService,NumberControl,NumberRangeAttribute,OptionDefinition,PropertyReference,SRDebugEditor | used: ? | crit: ? | note: auto-added; AI must complete | h:6d6a6b03
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/Editor/SettingsEditor.cs | MISSING-role | sys: ? | api: OnInspectorGUI() | dep?: SRDebuggerSettingsWindow,SRInternalEditorUtil,Settings | used: ? | crit: ? | note: auto-added; AI must complete | h:a230b468
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/Editor/SettingsWindow.cs | MISSING-role | sys: ? | api: Open() | dep?: ApiSignup,ApiSignupTermsWindow,ConsoleAlignment,DefaultTabs,EditorSettings,Entry | used: ? | crit: ? | note: auto-added; AI must complete | h:e20fe1e5
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/Editor/StompyRobot.SRDebugger.Editor.asmdef | MISSING-role | sys: ? | api: asmdef StompyRobot.SRDebugger.Editor | dep?: StompyRobot.SRDebugger,StompyRobot.SRF | used: ? | crit: ? | note: auto-added; AI must complete | h:edb63a4e
ExpoTheExplorer/Assets/StompyRobot/SRDebugger/Scripts/Editor/WelcomeWindow.cs | MISSING-role | sys: ? | api: Open() | dep?: SRDebug,SRDebugEditorPaths,SRDebugEditorStrings,SRDebuggerSettingsWindow,SRInternalEditorUtil,Settings | used: ? | crit: ? | note: auto-added; AI must complete | h:d240d5a0
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/Editor/CopyLayoutElementEditor.cs | MISSING-role | sys: ? | api: OnInspectorGUI() | dep?: CopyLayoutElement,CopySource | used: ? | crit: ? | note: auto-added; AI must complete | h:7202a2d4
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/Editor/CopyPreferredSizeEditor.cs | MISSING-role | sys: ? | api: OnInspectorGUI() | dep?: CopyPreferredSize,CopySource | used: ? | crit: ? | note: auto-added; AI must complete | h:0ba10bd9
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/Editor/CopyPreferredSizesEditor.cs | MISSING-role | sys: ? | api: OnInspectorGUI() | dep?: CopyPreferredSizes | used: ? | crit: ? | note: auto-added; AI must complete | h:f92f9fa4
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/Editor/LongPressButtonEditor.cs | MISSING-role | sys: ? | api: OnInspectorGUI() | dep?: LongPressButton | used: ? | crit: ? | note: auto-added; AI must complete | h:dd184923
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/Editor/SRNumberButtonEditor.cs | MISSING-role | sys: ? | api: OnInspectorGUI() | dep?: SRNumberButton | used: ? | crit: ? | note: auto-added; AI must complete | h:0b55f515
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/Editor/StompyRobot.SRF.Editor.asmdef | MISSING-role | sys: ? | api: asmdef StompyRobot.SRF.Editor | dep?: StompyRobot.SRF | used: ? | crit: ? | note: auto-added; AI must complete | h:ac15a419
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/Editor/StyleComponentEditor.cs | MISSING-role | sys: ? | api: OnInspectorGUI() | dep?: Style,StyleComponent,StyleRoot,StyleSheet | used: ? | crit: ? | note: auto-added; AI must complete | h:1b96b89a
ExpoTheExplorer/Assets/StompyRobot/SRF/Scripts/UI/Editor/StyleSheetEditor.cs | MISSING-role | sys: ? | api: OnInspectorGUI() | dep?: Style,StyleSheet | used: ? | crit: ? | note: auto-added; AI must complete | h:34824adb
ExpoTheExplorer/Assets/ThirdParty/Feel/NiceVibrations/Scripts/Editor/HapticImporter.cs | MISSING-role | sys: ? | api: OnImportAsset(AssetImportContext ctx) | dep?: GamepadRumble,HapticClip | used: ? | crit: ? | note: auto-added; AI must complete | h:afb7a191
ExpoTheExplorer/Assets/ThirdParty/Feel/NiceVibrations/Scripts/Editor/HapticSourceInspector.cs | MISSING-role | sys: ? | api: OnInspectorGUI() | dep?: HapticClip,HapticSource,Preset | used: ? | crit: ? | note: auto-added; AI must complete | h:95def5ca
ExpoTheExplorer/Assets/ThirdParty/Feel/NiceVibrations/Scripts/Editor/Lofelt.NiceVibrations.Editor.asmdef | MISSING-role | sys: ? | api: asmdef Lofelt.NiceVibrations.Editor | dep?: GUID:57a0b9bc628ab4740af4b6f1f0b2e134 | used: ? | crit: ? | note: auto-added; AI must complete | h:bb30a7ac
ExpoTheExplorer/Assets/Editor/CIBuild.cs | CI batch-mode iOS build entry point | sys: BuildTooling | api: BuildIOS(); SetExportCompliance(BuildTarget target, string pathToBuiltProject) | dep: - | used: - (invoked by GitLab CI via -executeMethod CIBuild.BuildIOS) | crit: K3 | note: SUPPLIED BY THE BUILD FARM, byte-identical to the copy they shipped -- diff before touching it and expect edits to be overwritten by their next handout. `used:` is empty on purpose: nothing in this repo calls it, the caller is /usr/local/intern-ci/build-unity.sh on their Mac, so a reference search will always come back empty and that is not an orphan. Must stay directly under Assets/Editor/ (their -executeMethod string is unqualified `CIBuild.BuildIOS`, and the class is in the global namespace even though this folder's asmdef sets rootNamespace ExpoTheExplorer.Editor -- the file declares no namespace, so it lands global; do NOT "tidy" that). Compiles into ExpoTheExplorer.Editor.dll, verified present there, so the asmdef is not blocking it: overrideReferences is false, which is what auto-references UnityEditor.iOS.Extensions.Xcode.dll and makes PlistDocument resolve -- setting overrideReferences true would break SetExportCompliance at compile time. Version comes from the git tag (CI_COMMIT_TAG minus the ios_ prefix) and build number from CI_PIPELINE_IID, so bundleVersion in ProjectSettings.asset is NOT the authority for a CI build and editing it changes nothing. Both exit paths call EditorApplication.Exit explicitly because batch-mode Unity returns 0 on a failed build otherwise. K3: editor-only, zero code in the player | h:1677614a
